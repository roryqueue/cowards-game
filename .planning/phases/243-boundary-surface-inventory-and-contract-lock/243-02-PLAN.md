---
phase: 243-boundary-surface-inventory-and-contract-lock
plan: 2
type: execute
wave: 1
depends_on:
  - 243-01
files_modified:
  - scripts/evaluate-v1-35-boundary-surface-inventory.ts
  - .planning/artifacts/v1.35-boundary-surface-inventory.md
  - .planning/artifacts/v1.35-boundary-surface-inventory.json
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
must_haves:
  truths:
    - "Developer can review one v1.35 inventory covering every INV-01 surface group."
    - "Every inventoried row has exactly one D-04 disposition and all D-05 decision-register fields."
    - "Inventory coverage is exhaustive across every named Phase 243 surface family and every known route, artifact, monitor, provider-proof, sandbox-claim, package-policy, TinyGo visibility, and privacy-monitor class discovered by the required static commands."
    - "Every row maps to affected downstream requirement IDs from ACCT, ENTRY, AUTH, PRIV, API, SBOX, LABEL, PKG, or PROOF as applicable, not only INV IDs."
    - "Artifacts fail evaluator checks when they contain contradictory sandbox/package/TinyGo overclaims or public/default private-marker leakage."
    - "Rows hand off behavior-changing work to Phases 244-248 instead of changing account, auth, sandbox, package, alias, label, or proof-gate behavior in Phase 243."
  artifacts:
    - path: ".planning/artifacts/v1.35-boundary-surface-inventory.md"
      provides: "Authoritative human-readable inventory and locked decision register"
      contains: "Decision Register"
    - path: ".planning/artifacts/v1.35-boundary-surface-inventory.json"
      provides: "Monitor-friendly inventory rows consumed by evaluator"
      contains: "v1.35-boundary-surface-inventory"
    - path: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      provides: "Static row source and deterministic artifact rendering"
  key_links:
    - from: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      to: ".planning/artifacts/v1.35-boundary-surface-inventory.md"
      via: "--write markdown render"
      pattern: "renderV135BoundarySurfaceInventoryMarkdown"
    - from: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      to: ".planning/artifacts/v1.35-boundary-surface-inventory.json"
      via: "--write JSON render"
      pattern: "renderV135BoundarySurfaceInventoryJson"
---

<objective>
Populate the authoritative v1.35 boundary surface inventory and decision register with concrete rows for the trust-boundary cleanup.

Purpose: Later phases need a locked route/code/artifact/test matrix so they can change behavior without rediscovering scope or accidentally promoting unsupported runtime, sandbox, package, TinyGo, alias, or privacy claims.
Output: `.planning/artifacts/v1.35-boundary-surface-inventory.md` and `.planning/artifacts/v1.35-boundary-surface-inventory.json`, generated from the evaluator and passing `--check`.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-CONTEXT.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-RESEARCH.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-PATTERNS.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-VALIDATION.md
@.planning/artifacts/v1.34-workshop-checker-inventory.md
@.planning/artifacts/v1.34-workshop-checker-contract.md
@.planning/artifacts/v1.34-workshop-checker-proof.md
@.planning/artifacts/v1.32-language-surface-inventory.md
@.planning/artifacts/v1.32-four-language-parity-matrix.md
@.planning/artifacts/v1.24-production-sandbox-readiness-matrix.md
@.planning/artifacts/v1.33-tinygo-wasi-spike-evidence.md
@apps/go-backend/live_backend.go
@apps/go-backend/runtime_service_client.go
@apps/web/lib/account-revision-write-boundary.ts
@apps/web/app/api/account/revisions/save/route.ts
@apps/web/app/api/account/revisions/[revisionId]/source/route.ts
@apps/web/app/api/workshop/source/route.ts
@apps/web/app/api/workshop/revisions/[revisionId]/source/route.ts
@apps/web/app/workshop/workshop-client-state.ts
@apps/web/app/matches/[matchId]/replay/owner-debug.ts
@packages/spec/src/runtime.ts
@packages/spec/src/workshop-checker.ts
@packages/spec/src/public-output-privacy.ts
@scripts/evaluate-v1-35-boundary-surface-inventory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate required surface rows in the evaluator source</name>
  <files>scripts/evaluate-v1-35-boundary-surface-inventory.ts</files>
  <read_first>
    - scripts/evaluate-v1-35-boundary-surface-inventory.ts
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-CONTEXT.md
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-RESEARCH.md
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-PATTERNS.md
    - .planning/artifacts/v1.34-workshop-checker-inventory.md
  </read_first>
  <action>
    Populate the static `V135BoundarySurfaceRow[]` source in `scripts/evaluate-v1-35-boundary-surface-inventory.ts` with concrete rows covering every D-01/INV-01 group and every discovered member of the named route/artifact/monitor classes below. Use row IDs in the form `v135-{surfaceGroup}-{shortName}`.

    Before writing rows, run and record the results of these static discovery commands in the markdown `Source Coverage Audit` section and use them to drive row creation:
    - `rg --files apps/web/app/api | sort`
    - `rg -n "createStrategyRevision|getStrategyRevisionSource|createMatchSet|getPublicReplayEvidence|account/revisions|matchset|competition|exhibition|replay|source" apps/go-backend/live_backend.go apps/go-backend/*.go`
    - `rg -n "validate-strategy|providerProof|provider proof|runtime.validateStrategy|sourceArtifact|artifactHash|artifactBytes|COWARDS_PROVIDER_VALIDATION_SECRET" apps packages scripts .planning/artifacts`
    - `rg -n "workshop.*(source|revision|submit|save)|compat|alias" apps/web/app/api apps/web/lib apps packages scripts`
    - `rg -n "player:workshop-local|ownerDebug|owner-debug|owner-private|ownerPrivate|StrategyMemory|SoldierMemory|objectivePayload|Awareness Grid" apps packages scripts .planning/artifacts`
    - `rg -n "sandbox|certified|certification|provenance-only|WASM isolation|WASI|Preview 1|TinyGo|spike-only|hidden" apps packages scripts .planning/artifacts`
    - `rg -n "package.mode|package mode|packages|dependency|host import|forbidden_import|package_or_dependency|rich package|Cargo|npm|PyPI|Zig package" apps packages scripts .planning/artifacts`
    - `rg -n "PUBLIC_OUTPUT_FORBIDDEN_FIELDS|assertPublicOutputLeakSafe|raw diagnostics|artifact bytes|host paths|env values|tokens|DB details|private runtime internals|bytesBase64|artifactBytesBase64" apps packages scripts .planning/artifacts`
    - `rg -n "v1\\.34|v1\\.33|v1\\.32|v1\\.24|boundary monitor|boundary:monitors|check-boundary-monitors" .planning/artifacts scripts package.json`

    Required rows must include every concrete surface discovered in these classes:
    - `account-save`: `apps/web/app/api/account/revisions/save/route.ts`, `apps/web/lib/account-revision-write-boundary.ts`, `apps/go-backend/live_backend.go#createStrategyRevision`; disposition `fix-now`; downstreamPhase `244`; current behavior records TypeScript local account-save/provider-proof drift without changing it.
    - `account-source-read`: `apps/web/app/api/account/revisions/[revisionId]/source/route.ts`, Go source read route in `apps/go-backend/live_backend.go`; disposition `quarantine` or `document-only` based on current server authorization evidence; downstreamPhase `245`.
    - `owner-debug-replay`: `apps/web/app/matches/[matchId]/replay/owner-debug.ts`, `apps/web/app/matches/server.test.ts`; disposition `quarantine`; downstreamPhase `245`.
    - `workshop-alias`: `apps/web/app/api/workshop/source/route.ts`, `apps/web/app/api/workshop/revisions/[revisionId]/source/route.ts`, and every source/submit/save compatibility alias found by the alias discovery command; disposition one of `deprecate-remove`, `quarantine`, or `document-only`; downstreamPhase `245`.
    - `competition-entry`: Go exhibition/MatchSet creation and persistence entry paths found in `apps/go-backend/live_backend.go` and prior v1.34 inventory; disposition `fix-now`; downstreamPhase `244`.
    - `go-read-write`: every Go read/write surface discovered in `apps/go-backend/live_backend.go` and related Go files for account revisions, account source reads, public replay evidence, MatchSet/exhibition creation, competition/entry, public reads, and session/owner checks; disposition `document-only` unless the row documents a later-phase fix; downstreamPhase `244`, `245`, or `248` as appropriate.
    - `provider-proof`: `apps/go-backend/runtime_service_client.go`, `apps/runtime-service/src/server.ts`, `packages/spec/src/workshop-checker.ts`; disposition `fix-now`; downstreamPhase `244`.
    - `sandbox-claim`: `packages/spec/src/runtime.ts`, `.planning/artifacts/v1.24-production-sandbox-readiness-matrix.md`, and every current Learn/evidence/label/script/artifact surface found by the sandbox discovery command; disposition `document-only` or `fix-now`; downstreamPhase `246`.
    - `package-policy`: `packages/spec/src/runtime.ts`, `packages/spec/src/workshop-checker.ts`, runtime providers and checker diagnostics; disposition `fix-now`; downstreamPhase `247`.
    - `tinygo-visibility`: `.planning/artifacts/v1.33-tinygo-wasi-spike-evidence.md`, `packages/spec/src/workshop-checker.ts`, production source-format lists; disposition `quarantine` or `document-only`; downstreamPhase `246` or `247`.
    - `privacy-monitor`: `packages/spec/src/public-output-privacy.ts`, `scripts/check-boundary-monitors.ts`, v1.34 checker proof/artifacts; disposition `fix-now`; downstreamPhase `248`.
    - `evidence-artifact`: prior inventory/proof artifacts named in 243-CONTEXT canonical refs; disposition `document-only`; downstreamPhase `248` or `"none"`.

    For every row, fill currentOwner, intendedOwner, trustBoundary, dataClass, affectedRequirements, currentBehavior, disposition, requiredTestsOrProof, privacyRisks, and downstreamPhase. Use only the allowed D-04 dispositions. Use D-09 forbidden-marker wording in privacy risks. Each row must include affected downstream requirement IDs from REQUIREMENTS.md as applicable: ACCT/ENTRY for Phase 244, AUTH/PRIV/API for Phase 245, SBOX/LABEL for Phase 246, PKG for Phase 247, and PROOF for Phase 248. Do not allow rows to cite only INV IDs when they hand off downstream behavior or proof work. Do not edit behavior files in this plan; record any serious finding as a row assigned to Phase 244, 245, 246, 247, or 248 per D-06.

    Add explicit coverage rows and required tests/proof for contradictory overclaim and leakage guards: production sandbox certification, TinyGo production support, package ecosystem/rich-package/host import support, TypeScript/Python WASM isolation claims, public/default private-marker leakage, raw diagnostics, Strategy source, artifact bytes, host paths, env values, package paths, tokens, DB details, private runtime internals, StrategyMemory, SoldierMemory, and objective payloads. These rows must point to Phases 246, 247, or 248 and must not implement the behavior fix in Phase 243.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-35-boundary-surface-inventory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` exits 0.
    - `pnpm exec tsx scripts/evaluate-v1-35-boundary-surface-inventory.ts --write` creates both `.planning/artifacts/v1.35-boundary-surface-inventory.md` and `.planning/artifacts/v1.35-boundary-surface-inventory.json`.
    - `pnpm exec tsx scripts/evaluate-v1-35-boundary-surface-inventory.ts --check` exits 0 after write.
    - The markdown `Source Coverage Audit` lists every required discovery command above and marks each surface family `COVERED` with row IDs: account-save, account-source-read, owner-debug-replay, workshop-alias, competition-entry, go-read-write, provider-proof, sandbox-claim, package-policy, tinygo-visibility, privacy-monitor, evidence-artifact.
    - `node -e "const inv=require('./.planning/artifacts/v1.35-boundary-surface-inventory.json'); const rows=inv.surfaces||[]; const ids=['account-save','account-source-read','owner-debug-replay','workshop-alias','competition-entry','go-read-write','provider-proof','sandbox-claim','package-policy','tinygo-visibility','privacy-monitor']; for (const id of ids) if (!rows.some(r=>r.surfaceGroup===id)) throw new Error('missing '+id); for (const r of rows) if (!Array.isArray(r.affectedRequirements)||r.affectedRequirements.length===0||((r.downstreamPhase!==undefined&&r.downstreamPhase!=='none')&&r.affectedRequirements.every(x=>String(x).startsWith('INV-')))) throw new Error('bad requirements '+r.id)"` exits 0.
    - `rg -n 'fix-now|quarantine|deprecate-remove|document-only|future' .planning/artifacts/v1.35-boundary-surface-inventory.md` returns every disposition that exists in the row source; no other disposition appears in non-heading rows.
    - `rg -n 'Phase 244|Phase 245|Phase 246|Phase 247|Phase 248' .planning/artifacts/v1.35-boundary-surface-inventory.md` returns downstream handoff evidence for behavior-changing findings.
    - `rg -n 'production sandbox certification|TypeScript/Python WASM isolation|TinyGo production support|package ecosystem|rich-package|host import|raw diagnostics|artifact bytes|host paths|env values|tokens|DB details|private runtime internals|StrategyMemory|SoldierMemory|objective payload' .planning/artifacts/v1.35-boundary-surface-inventory.md .planning/artifacts/v1.35-boundary-surface-inventory.json` returns rows or guard text assigning forbidden overclaim/leakage risks to later phases.
  </acceptance_criteria>
  <done>The evaluator source contains a concrete, D-01-complete inventory row set with D-04 dispositions and D-05 fields, and generated artifacts pass the evaluator check.</done>
</task>

<task type="auto">
  <name>Task 2: Write the authoritative markdown and JSON decision register</name>
  <files>.planning/artifacts/v1.35-boundary-surface-inventory.md, .planning/artifacts/v1.35-boundary-surface-inventory.json</files>
  <read_first>
    - scripts/evaluate-v1-35-boundary-surface-inventory.ts
    - .planning/artifacts/v1.34-workshop-checker-inventory.md
    - .planning/artifacts/v1.34-workshop-checker-contract.md
    - .planning/artifacts/v1.34-workshop-checker-proof.md
    - .planning/artifacts/v1.32-language-surface-inventory.md
    - .planning/artifacts/v1.24-production-sandbox-readiness-matrix.md
    - .planning/artifacts/v1.33-tinygo-wasi-spike-evidence.md
  </read_first>
  <action>
    Run the evaluator write command to generate `.planning/artifacts/v1.35-boundary-surface-inventory.md` and `.planning/artifacts/v1.35-boundary-surface-inventory.json`. The markdown must be the authoritative human-readable artifact and must include these sections: `Executive Summary`, `Scope and Non-Goals`, `Decision Register`, `Surface Inventory`, `Downstream Handoff`, `Privacy and Claim Calibration`, `Required Tests and Proof`, and `Source Coverage Audit`.

    The `Scope and Non-Goals` section must state that Phase 243 is inventory/contract/characterization only and must not change account proof, auth, sandbox, package, aliases, labels, runtime execution, or proof gates. The `Privacy and Claim Calibration` section must state: TypeScript/Python are provenance-only, Rust/Zig are immutable WASM/WASI Preview 1 artifact-backed, TinyGo is spike-only/hidden, no current lane is production sandbox certified by default, package mode remains `none`, rich packages/package ecosystems/host imports are not production-supported, and public/default output must not expose the D-09 forbidden marker set.

    The JSON must include schemaVersion, milestone, generatedBy, generatedAt, requiredSurfaceGroups, allowedDispositions, allowedRequirementIds, forbiddenOverclaimPatterns, forbiddenPublicDefaultLeakageMarkers, globalPolicies, and surfaces. Keep generatedAt deterministic, for example `2026-06-14`, unless the existing evaluator pattern requires a different fixed value.
  </action>
  <verify>
    <automated>pnpm exec tsx scripts/evaluate-v1-35-boundary-surface-inventory.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/artifacts/v1.35-boundary-surface-inventory.md` exists and contains `# v1.35 Boundary Surface Inventory`.
    - `.planning/artifacts/v1.35-boundary-surface-inventory.json` exists and contains `"schemaVersion": "v1.35-boundary-surface-inventory"`.
    - `rg -n 'account-save|account-source-read|owner-debug-replay|workshop-alias|competition-entry|go-read-write|provider-proof|sandbox-claim|package-policy|tinygo-visibility|privacy-monitor' .planning/artifacts/v1.35-boundary-surface-inventory.md` returns all D-01 groups.
    - `rg -n 'ACCT-01|ENTRY-01|AUTH-01|PRIV-01|API-01|SBOX-01|LABEL-01|PKG-01|PROOF-01' .planning/artifacts/v1.35-boundary-surface-inventory.md .planning/artifacts/v1.35-boundary-surface-inventory.json` returns downstream requirement traceability.
    - `rg -n 'TypeScript/Python are provenance-only|Rust/Zig are immutable WASM/WASI Preview 1 artifact-backed|TinyGo is spike-only/hidden|no current lane is production sandbox certified|package mode remains `none`|host imports are not production-supported' .planning/artifacts/v1.35-boundary-surface-inventory.md` returns all claim-calibration statements.
    - `rg -n 'Strategy source|StrategyMemory|SoldierMemory|objective payload|raw diagnostics|artifact bytes|host paths|env values|package paths|tokens|DB details|private runtime internals' .planning/artifacts/v1.35-boundary-surface-inventory.md` returns the public/default forbidden-marker set.
  </acceptance_criteria>
  <done>The committed markdown and JSON artifacts are deterministic, complete for INV-01, classified for INV-02, and usable as the locked INV-03 decision register for later phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/artifact discovery -> inventory row | Static code and prior evidence are summarized into rows that downstream phases will treat as authoritative. |
| private implementation detail -> public planning artifact | Inventory must identify private data risks without copying private Strategy data, raw diagnostics, artifact bytes, tokens, paths, or runtime internals into artifacts. |
| Phase 243 contract -> Phase 244-248 behavior | Rows assign behavior-changing work to downstream phases; Phase 243 must not silently fix or relabel behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-05 | Tampering | `.planning/artifacts/v1.35-boundary-surface-inventory.json` | mitigate | Generate JSON from evaluator row source and verify with `--check`; reject stale manual edits. |
| T-243-06 | Information Disclosure | `.planning/artifacts/v1.35-boundary-surface-inventory.md` | mitigate | Describe privacy risk categories and source file paths only; do not include Strategy source, artifact bytes, raw diagnostics, tokens, env values, DB details, host paths, package paths, or private runtime internals. |
| T-243-07 | Repudiation | decision register | mitigate | Every row must carry affected requirements, disposition, required tests/proof, and downstream phase so later behavior changes can trace to the locked contract. |
| T-243-08 | Elevation of Privilege | alias/account/provider rows | mitigate | Treat retained aliases, local Workshop identity, account source reads, provider proof, package policy, TinyGo visibility, and owner-debug as potential bypass surfaces until later phases prove otherwise. |
</threat_model>

<verification>
Run:

```bash
pnpm exec vitest run scripts/evaluate-v1-35-boundary-surface-inventory.test.ts
pnpm exec tsx scripts/evaluate-v1-35-boundary-surface-inventory.ts --check
```
</verification>

<success_criteria>
- `.planning/artifacts/v1.35-boundary-surface-inventory.md` and `.planning/artifacts/v1.35-boundary-surface-inventory.json` exist.
- Required D-01 surface groups are present.
- Required static discovery commands are represented in the Source Coverage Audit and every discovered required surface family is covered by row IDs.
- D-04 dispositions and D-05 row fields are complete.
- Every row has affected downstream requirement IDs from the full v1.35 requirement set where applicable.
- Serious findings are assigned to Phases 244-248, not fixed in Phase 243.
- Public/default privacy and sandbox/package/TinyGo claim calibration are explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-02-SUMMARY.md`
</output>
