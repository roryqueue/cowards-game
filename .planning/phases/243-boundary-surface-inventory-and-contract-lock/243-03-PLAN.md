---
phase: 243-boundary-surface-inventory-and-contract-lock
plan: 3
type: execute
wave: 2
depends_on:
  - 243-02
files_modified:
  - package.json
  - scripts/check-boundary-monitors.ts
  - scripts/check-boundary-monitors.test.ts
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
must_haves:
  truths:
    - "Developer can run a named v1.35 boundary inventory command to regenerate or check the locked artifacts."
    - "The broader boundary monitor chain can fail loudly when the v1.35 inventory artifacts are missing or stale."
    - "Monitor wiring remains deterministic and does not require Phase 244-248 behavior to be fixed."
  artifacts:
    - path: "package.json"
      provides: "v1.35 boundary inventory scripts and monitor chain integration"
      contains: "v1.35:boundary-inventory:check"
    - path: "scripts/check-boundary-monitors.ts"
      provides: "Static boundary monitor check for v1.35 inventory artifacts"
      exports:
        - "runBoundaryMonitorChecks"
    - path: "scripts/check-boundary-monitors.test.ts"
      provides: "Regression coverage for v1.35 inventory monitor wiring"
  key_links:
    - from: "package.json"
      to: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      via: "v1.35:boundary-inventory and v1.35:boundary-inventory:check scripts"
      pattern: "v1\\.35:boundary-inventory"
    - from: "scripts/check-boundary-monitors.ts"
      to: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      via: "checkV135BoundarySurfaceInventoryArtifacts import"
      pattern: "checkV135BoundarySurfaceInventoryArtifacts"
---

<objective>
Wire the completed v1.35 inventory into deterministic commands and the existing boundary monitor chain.

Purpose: Phase 243 should leave a durable locked contract, not a one-off artifact. The monitor integration makes missing or stale inventory fail loudly while still avoiding any behavior-changing account, auth, sandbox, package, alias, label, runtime, or proof-gate fixes.
Output: package scripts and monitor tests that check `.planning/artifacts/v1.35-boundary-surface-inventory.md` and `.planning/artifacts/v1.35-boundary-surface-inventory.json`.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-CONTEXT.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-VALIDATION.md
@package.json
@scripts/check-boundary-monitors.ts
@scripts/check-boundary-monitors.test.ts
@scripts/evaluate-v1-35-boundary-surface-inventory.ts
@.planning/artifacts/v1.35-boundary-surface-inventory.md
@.planning/artifacts/v1.35-boundary-surface-inventory.json

<interfaces>
Existing monitor contract:

```typescript
export interface BoundaryMonitorCheck {
  layer: MonitorLayer
  name: string
  ok: boolean
  detail: string
}

export const runBoundaryMonitorChecks = async (): Promise<
  BoundaryMonitorCheck[]
>
```

Plan 03 should add a check that reports layer `contract_drift` or `privacy`, name `v1.35 boundary surface inventory`, and detail from `checkV135BoundarySurfaceInventoryArtifacts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add package scripts for inventory write and check</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - scripts/evaluate-v1-35-boundary-surface-inventory.ts
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `pnpm v1.35:boundary-inventory` runs the evaluator with `--write`.
    - Test 2: `pnpm v1.35:boundary-inventory:check` runs the evaluator with `--check`.
    - Test 3: `pnpm boundary:monitors` includes `pnpm v1.35:boundary-inventory:check` before `pnpm exec tsx scripts/check-boundary-monitors.ts`.
  </behavior>
  <action>
    Update `package.json` scripts with exact entries:
    - `"v1.35:boundary-inventory": "pnpm exec tsx scripts/evaluate-v1-35-boundary-surface-inventory.ts --write"`
    - `"v1.35:boundary-inventory:check": "pnpm exec tsx scripts/evaluate-v1-35-boundary-surface-inventory.ts --check"`

    Update the existing `"boundary:monitors"` script to include `pnpm v1.35:boundary-inventory:check` after `pnpm v1.34:workshop-checker:check` if that command is already in the chain, otherwise immediately before `pnpm exec tsx scripts/check-boundary-monitors.ts`. Preserve all existing monitor commands and ordering around contract, imports, Go parity, sandbox, WASM/WASI, runtime abuse, match execution, public discovery, and topology checks.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['v1.35:boundary-inventory']||!p.scripts['v1.35:boundary-inventory:check']||!p.scripts['boundary:monitors'].includes('pnpm v1.35:boundary-inventory:check')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); console.log(p.scripts['v1.35:boundary-inventory']); console.log(p.scripts['v1.35:boundary-inventory:check']);"` prints the two exact evaluator commands.
    - `node -e "const p=require('./package.json'); if(!p.scripts['boundary:monitors'].includes('pnpm v1.35:boundary-inventory:check')) process.exit(1)"` exits 0.
    - `pnpm v1.35:boundary-inventory:check` exits 0 after Plan 02 artifacts are present.
  </acceptance_criteria>
  <done>Named package scripts exist for inventory write/check, and the static check is part of the boundary monitor chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic boundary monitor coverage for the locked inventory</name>
  <files>scripts/check-boundary-monitors.ts, scripts/check-boundary-monitors.test.ts</files>
  <read_first>
    - scripts/check-boundary-monitors.ts
    - scripts/check-boundary-monitors.test.ts
    - scripts/evaluate-v1-35-boundary-surface-inventory.ts
    - .planning/artifacts/v1.35-boundary-surface-inventory.md
    - .planning/artifacts/v1.35-boundary-surface-inventory.json
  </read_first>
  <behavior>
    - Test 1: the monitor suite includes a result named `v1.35 boundary surface inventory`.
    - Test 2: the monitor check reports pass when `checkV135BoundarySurfaceInventoryArtifacts()` returns no failures.
    - Test 3: the monitor check reports fail and includes missing/stale artifact detail when evaluator check failures are returned.
    - Test 4: monitor wiring does not require runtime-service, Go backend, Docker, browser, database, network, Strategy execution, or Phase 244-248 behavior changes.
  </behavior>
  <action>
    In `scripts/check-boundary-monitors.ts`, import `checkV135BoundarySurfaceInventoryArtifacts` from `./evaluate-v1-35-boundary-surface-inventory.ts`. Add a small helper such as `checkV135BoundarySurfaceInventory()` that calls the evaluator check and returns a success detail like `v1.35 boundary surface inventory artifacts are current` or throws with joined failure messages. Add it to `runBoundaryMonitorChecks()` as:
    - layer: `contract_drift`
    - name: `v1.35 boundary surface inventory`

    In `scripts/check-boundary-monitors.test.ts`, add tests matching existing monitor helper style. If direct mocking is awkward in the existing module structure, assert the monitor result name exists in `runBoundaryMonitorChecks()` and add a unit test for the helper or exported validation function that simulates missing/stale artifacts using the evaluator's temp-repo check. Keep tests static and deterministic.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/check-boundary-monitors.test.ts scripts/evaluate-v1-35-boundary-surface-inventory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'v1\\.35 boundary surface inventory|checkV135BoundarySurfaceInventoryArtifacts' scripts/check-boundary-monitors.ts scripts/check-boundary-monitors.test.ts` returns monitor implementation and test coverage.
    - `pnpm exec vitest run scripts/check-boundary-monitors.test.ts scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` exits 0.
    - `pnpm v1.35:boundary-inventory:check` exits 0.
    - No files under `apps/go-backend/live_backend.go`, `apps/go-backend/runtime_service_client.go`, `apps/runtime-service/src/server.ts`, account save routes, owner-debug routes, Workshop alias routes, sandbox labels, package enforcement, or provider-proof gates are modified in this plan.
  </acceptance_criteria>
  <done>The existing boundary monitor suite fails loudly on missing/stale v1.35 inventory artifacts through a deterministic static check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package script -> evaluator | Named commands give developers and CI a stable way to regenerate and check the inventory artifacts. |
| evaluator artifact check -> boundary monitor suite | Monitor chain converts missing/stale inventory into a loud contract drift failure. |
| static monitor -> later behavior phases | Monitor must not assume Phase 244-248 fixes have already shipped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-09 | Tampering | `package.json` monitor chain | mitigate | Add `pnpm v1.35:boundary-inventory:check` without removing existing monitor commands; verify via `node -e` script check. |
| T-243-10 | Repudiation | `scripts/check-boundary-monitors.ts` | mitigate | Add a named monitor result so stale or missing Phase 243 artifacts are attributable to `v1.35 boundary surface inventory`. |
| T-243-11 | Information Disclosure | monitor output | mitigate | Report only missing/stale artifact paths and validation messages; do not print artifact bytes, raw diagnostics, Strategy source, host paths beyond repo-relative artifact paths, env values, tokens, DB details, or private runtime internals. |
| T-243-12 | Denial of Service | boundary monitor runtime | accept | Static inventory check is file comparison only and does not introduce live service dependencies; it is low-cost relative to existing monitor chain. |
</threat_model>

<verification>
Run:

```bash
pnpm v1.35:boundary-inventory:check
pnpm exec vitest run scripts/check-boundary-monitors.test.ts scripts/evaluate-v1-35-boundary-surface-inventory.test.ts
```

If this plan changes the full monitor chain, also run:

```bash
pnpm boundary:monitors
```
</verification>

<success_criteria>
- `package.json` has `v1.35:boundary-inventory` and `v1.35:boundary-inventory:check`.
- `boundary:monitors` includes `pnpm v1.35:boundary-inventory:check`.
- `scripts/check-boundary-monitors.ts` includes a named static check for the v1.35 inventory artifacts.
- Monitor tests and evaluator tests pass.
- No Phase 244-248 behavior-changing files are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-03-SUMMARY.md`
</output>
