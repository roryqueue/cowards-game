---
phase: 11-doctrine-debugging-ux
plan: 06
type: execute
wave: 4
depends_on: [11-03, 11-05]
files_modified:
  - packages/replay/src/project.ts
  - packages/replay/src/project.test.ts
  - apps/web/app/matches/server.test.ts
  - apps/web/app/matches/[matchId]/replay/owner-debug.test.ts
  - apps/web/e2e/replay.fixture.spec.ts
  - .planning/phases/11-doctrine-debugging-ux/11-VALIDATION.md
autonomous: true
requirements: [DEBUG-01, DEBUG-02, DEBUG-03, DEBUG-04, DEBUG-05, DEBUG-06]
must_haves:
  truths:
    - "Public replay projection strips owner debug explanations and private runtime facts."
    - "Owner replay requires explicit opt-in and owner player id."
    - "Browser-level replay checks prove owner debug explanations do not appear publicly."
    - "Phase validation evidence maps DEBUG-01 through DEBUG-06 to automated commands."
  artifacts:
    - path: "packages/replay/src/project.test.ts"
      provides: "Projection privacy regression coverage for new debug fields"
    - path: "apps/web/app/matches/server.test.ts"
      provides: "Replay server public/owner DTO privacy coverage"
    - path: "apps/web/e2e/replay.fixture.spec.ts"
      provides: "Browser privacy and owner debug explanation checks"
    - path: ".planning/phases/11-doctrine-debugging-ux/11-VALIDATION.md"
      provides: "Command-backed Phase 11 acceptance evidence"
  key_links:
    - from: "packages/replay/src/project.ts"
      to: "packages/replay/src/project.test.ts"
      via: "public projection denylist/privacy tests"
      pattern: "ownerDebug|soldierInactivity"
    - from: "apps/web/app/matches/server.test.ts"
      to: "apps/web/app/matches/replay-ready.ts"
      via: "public and owner replay DTO construction"
      pattern: "ownerDebug"
    - from: "apps/web/e2e/replay.fixture.spec.ts"
      to: "replay UI"
      via: "public vs owner query modes"
      pattern: "Why no Action"
---

<objective>
Close Phase 11 with public privacy gates and command-backed validation evidence.

Purpose: DEBUG-06 and D-03/D-04 require owner-only debugging to remain opt-in and invisible in public replay, while every DEBUG requirement needs executable proof.
Output: Projection/server/browser privacy tests and `11-VALIDATION.md`.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-doctrine-debugging-ux/11-CONTEXT.md
@.planning/phases/11-doctrine-debugging-ux/11-RESEARCH.md
@.planning/phases/11-doctrine-debugging-ux/11-PATTERNS.md
@.planning/phases/11-doctrine-debugging-ux/11-03-SUMMARY.md
@.planning/phases/11-doctrine-debugging-ux/11-05-SUMMARY.md
@packages/replay/src/project.ts
@packages/replay/src/project.test.ts
@apps/web/app/matches/server.test.ts
@apps/web/app/matches/[matchId]/replay/owner-debug.test.ts
@apps/web/e2e/replay.fixture.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend projection and server privacy tests</name>
  <files>packages/replay/src/project.ts, packages/replay/src/project.test.ts, apps/web/app/matches/server.test.ts</files>
  <behavior>
    - Test 1: public projection serialization does not contain `ownerDebug`, `soldierInactivity`, Strategy source, StrategyMemory, SoldierMemory, objective payloads, raw Awareness Grid details, raw runtime details, or private event refs.
    - Test 2: owner projection includes only the requested owner's private section and owner explanation DTOs after opt-in.
    - Test 3: server public replay DTO omits owner debug fields even when the underlying Chronicle contains owner/private facts.
  </behavior>
  <action>Update the public projection hard gate if the new debug fields can appear in Chronicle/projection payloads. Extend package and server tests for every new owner-debug field introduced by Plans 11-04 and 11-05. Preserve D-04: public replay stays clean while owner/debug mode can show richer explanations.</action>
  <verify>
    <automated>pnpm --filter @cowards/replay test -- project.test.ts</automated>
    <automated>pnpm --filter @cowards/web test -- server.test.ts</automated>
  </verify>
  <done>Public projection and server DTO tests fail on any new owner-debug or private-data leak.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add browser privacy and owner debug checks</name>
  <files>apps/web/app/matches/[matchId]/replay/owner-debug.test.ts, apps/web/e2e/replay.fixture.spec.ts</files>
  <behavior>
    - Test 1: owner debug route options remain disabled by default and require explicit owner player id.
    - Test 2: public replay fixture page does not render `Owner debug`, `Why no Action?`, cause-code labels, or owner explanation remediation.
    - Test 3: owner replay fixture page with explicit owner debug query renders the owner debug control and at least one explanation.
  </behavior>
  <action>Extend owner debug guard tests and Playwright replay fixture tests. Use fixture-based replay checks so Phase 11 does not depend on service-backed local Postgres; service-backed reliability remains Phase 12. Keep assertions focused and deterministic.</action>
  <verify>
    <automated>pnpm --filter @cowards/web test -- owner-debug.test.ts</automated>
    <automated>PLAYWRIGHT_TEST=1 pnpm e2e -- replay.fixture.spec.ts</automated>
  </verify>
  <done>Browser-level tests prove owner debug explanations are opt-in and absent from public replay.</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase 11 validation evidence</name>
  <files>.planning/phases/11-doctrine-debugging-ux/11-VALIDATION.md</files>
  <action>Create `11-VALIDATION.md` with a DEBUG-01 through DEBUG-06 evidence table. For each requirement, list the plan(s), touched files, and exact passing commands. Include these gates: `pnpm --filter @cowards/spec test -- spec.test.ts`, `pnpm --filter @cowards/runtime-js test -- validation.test.ts`, `pnpm --filter @cowards/persistence test -- workshop.test.ts`, `pnpm --filter @cowards/replay test -- debug-explanations.test.ts project.test.ts`, `pnpm --filter @cowards/web test -- workshop-client.test.tsx replay-state.test.ts replay-client.test.tsx server.test.ts owner-debug.test.ts`, `PLAYWRIGHT_TEST=1 pnpm e2e -- replay.fixture.spec.ts`, and relevant package typechecks. Do not mark a row complete without a command-backed result.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec test -- spec.test.ts && pnpm --filter @cowards/runtime-js test -- validation.test.ts && pnpm --filter @cowards/persistence test -- workshop.test.ts && pnpm --filter @cowards/replay test -- debug-explanations.test.ts project.test.ts && pnpm --filter @cowards/web test -- workshop-client.test.tsx replay-state.test.ts replay-client.test.tsx server.test.ts owner-debug.test.ts</automated>
    <automated>PLAYWRIGHT_TEST=1 pnpm e2e -- replay.fixture.spec.ts</automated>
  </verify>
  <done>`11-VALIDATION.md` contains command-backed evidence for DEBUG-01 through DEBUG-06 and does not rely on manual inspection claims.</done>
</task>

</tasks>

<risks>
- Risk: public privacy tests check only one layer. Mitigation: package projection, server DTO, route guard, and browser tests all assert public absence.
- Risk: service-backed E2E instability blocks UX/privacy validation. Mitigation: use fixture replay browser tests in this phase and explicitly record service-backed Workshop-to-replay as optional until Phase 12 reliability work.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Chronicle projection -> public replay | Private owner/debug facts must be stripped before public consumption. |
| route query -> owner replay mode | Owner debug mode is enabled only by trusted env/test gate plus explicit query parameters. |
| validation evidence -> phase closure | Phase closure depends on automated proof, not manual review alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-16 | I | public replay projection | mitigate | Projection/server/browser tests assert no owner debug DTOs or private markers in public output. |
| T-11-17 | S/E | owner debug route options | mitigate | Existing env gate and owner player id requirement remain covered by unit tests. |
| T-11-18 | R | validation evidence | mitigate | `11-VALIDATION.md` maps every DEBUG requirement to exact command results. |
</threat_model>

<verification>
Run projection, web unit, and Playwright fixture privacy checks, then record results in `11-VALIDATION.md`.
</verification>

<success_criteria>
DEBUG-06 and the overall phase are complete when public replay has automated privacy gates at projection/server/browser layers, owner debug remains opt-in, and `11-VALIDATION.md` maps DEBUG-01 through DEBUG-06 to passing commands.
</success_criteria>

<output>
After completion, create `.planning/phases/11-doctrine-debugging-ux/11-06-SUMMARY.md`
</output>
