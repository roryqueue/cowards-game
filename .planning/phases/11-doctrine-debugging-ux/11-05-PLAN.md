---
phase: 11-doctrine-debugging-ux
plan: 05
type: execute
wave: 3
depends_on: [11-04]
files_modified:
  - apps/web/app/matches/types.ts
  - apps/web/app/matches/replay-ready.ts
  - apps/web/app/matches/[matchId]/replay/replay-state.ts
  - apps/web/app/matches/[matchId]/replay/replay-state.test.ts
  - apps/web/app/matches/[matchId]/replay/replay-client.tsx
  - apps/web/app/matches/[matchId]/replay/replay-client.test.tsx
autonomous: true
requirements: [DEBUG-04, DEBUG-05]
must_haves:
  truths:
    - "Owner replay DTOs include Soldier inactivity explanations only when owner debug mode is active."
    - "Replay UI renders owner explanations behind the existing owner debug opt-in control."
    - "React helpers consume DTO fields and do not infer game rules."
  artifacts:
    - path: "apps/web/app/matches/types.ts"
      provides: "ReplayReadyDto owner debug explanation fields"
    - path: "apps/web/app/matches/replay-ready.ts"
      provides: "Server-side DTO assembly using replay explanation generator"
    - path: "apps/web/app/matches/[matchId]/replay/replay-state.ts"
      provides: "Pure selector helpers for displaying explanation DTOs"
    - path: "apps/web/app/matches/[matchId]/replay/replay-client.tsx"
      provides: "Owner-only explanation rendering"
  key_links:
    - from: "apps/web/app/matches/replay-ready.ts"
      to: "packages/replay/src/debug-explanations.ts"
      via: "buildSoldierInactivityExplanations"
      pattern: "buildSoldierInactivityExplanations"
    - from: "apps/web/app/matches/[matchId]/replay/replay-client.tsx"
      to: "apps/web/app/matches/[matchId]/replay/replay-state.ts"
      via: "DTO selector helper"
      pattern: "get.*InactivityExplanation"
---

<objective>
Wire replay explanation DTOs through the owner replay page and render them without React-side rule inference.

Purpose: DEBUG-04, DEBUG-05, D-01 through D-04 require owner-only, opt-in explanations that answer why a Soldier did nothing.
Output: Replay DTO wiring, selector helpers, UI rendering, and unit coverage.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-doctrine-debugging-ux/11-CONTEXT.md
@.planning/phases/11-doctrine-debugging-ux/11-RESEARCH.md
@.planning/phases/11-doctrine-debugging-ux/11-PATTERNS.md
@.planning/phases/11-doctrine-debugging-ux/11-04-SUMMARY.md
@apps/web/app/matches/types.ts
@apps/web/app/matches/replay-ready.ts
@apps/web/app/matches/[matchId]/replay/replay-state.ts
@apps/web/app/matches/[matchId]/replay/replay-client.tsx

<interfaces>
From `apps/web/app/matches/types.ts`:
```typescript
export interface ReplayReadyDto {
  status: "ready"
  mode: ReplayViewMode
  metadata: ReplayMetadataDto
  projection: ChronicleProjection
  timeline: ReplayTimelineEntryDto[]
  states: ReplayStateDto[]
  initialSequence: 0
  ownerPlayerId?: PlayerId | undefined
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add owner explanation fields to replay DTO assembly</name>
  <files>apps/web/app/matches/types.ts, apps/web/app/matches/replay-ready.ts, apps/web/app/matches/[matchId]/replay/replay-client.test.tsx</files>
  <behavior>
    - Test 1: public `ReplayReadyDto` has no owner explanation field.
    - Test 2: owner `ReplayReadyDto` includes `ownerDebug.soldierInactivityExplanations` when owner debug is allowed and owner player id is present.
    - Test 3: owner explanation generation receives Chronicle/replay-state data from `buildReadyReplayFromChronicle`.
  </behavior>
  <action>Add an optional owner-only debug section to `ReplayReadyDto`, for example `ownerDebug?: { soldierInactivityExplanations: SoldierInactivityExplanationDto[] }`. In `buildReadyReplayFromChronicle`, call the Plan 11-04 replay helper only in owner mode after projection/replay state construction. Public mode must not include the field per D-03 and D-04.</action>
  <verify>
    <automated>pnpm --filter @cowards/web test -- replay-client.test.tsx</automated>
    <automated>pnpm --filter @cowards/web typecheck</automated>
  </verify>
  <done>Replay DTO assembly carries explanations only for owner debug mode and omits them from public replay data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add replay-state explanation selectors</name>
  <files>apps/web/app/matches/[matchId]/replay/replay-state.ts, apps/web/app/matches/[matchId]/replay/replay-state.test.ts</files>
  <behavior>
    - Test 1: selector returns null for public replay data.
    - Test 2: selector returns the selected Soldier explanation nearest to or at the current sequence.
    - Test 3: selector exposes only DTO label/cause/remediation/details and does not inspect board geometry or legal move rules.
  </behavior>
  <action>Add pure selector/view-model helpers such as `getSoldierInactivityExplanation(data, soldierId, sequence)` and `formatSoldierInactivityCause(cause)`. These helpers must consume the DTO field generated by `replay-ready.ts`; they must not derive legality from positions, facings, bounds, or movement rules.</action>
  <verify>
    <automated>pnpm --filter @cowards/web test -- replay-state.test.ts</automated>
  </verify>
  <done>Replay state helpers retrieve and format owner explanation DTOs without rule inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render owner-only explanations in replay UI</name>
  <files>apps/web/app/matches/[matchId]/replay/replay-client.tsx, apps/web/app/matches/[matchId]/replay/replay-client.test.tsx</files>
  <behavior>
    - Test 1: public replay does not render `Why no Action?` or owner debug explanation text.
    - Test 2: owner replay hides explanations until the existing owner debug checkbox is enabled.
    - Test 3: owner replay renders cause label and remediation for selected Soldier when an explanation exists.
  </behavior>
  <action>Render the selected Soldier's inactivity explanation behind the existing owner debug toggle. Use canonical terminology in labels: Soldier, Match, Action, STONE, FALLEN. Keep display concise and do not add rule calculations to JSX; call replay-state helpers only.</action>
  <verify>
    <automated>pnpm --filter @cowards/web test -- replay-client.test.tsx replay-state.test.ts</automated>
    <automated>pnpm --filter @cowards/web typecheck</automated>
  </verify>
  <done>Owner replay can opt in to see data-driven inactivity explanations and public replay remains visually clean.</done>
</task>

</tasks>

<risks>
- Risk: React component starts computing rule explanations. Mitigation: task actions require all computation to happen in replay DTO assembly or replay-state selectors consuming DTOs.
- Risk: owner debug UI becomes noisy. Mitigation: render explanations only after the existing owner debug toggle is enabled and only for the selected Soldier/current sequence.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| replay DTO assembly -> client props | Server-derived owner debug explanations become client-rendered data. |
| owner toggle -> visible UI | Owner-private explanation content becomes visible only after explicit opt-in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-13 | I | `ReplayReadyDto.ownerDebug` | mitigate | Add field only in owner mode and verify public DTOs do not include it. |
| T-11-14 | T/R | replay-state selectors | mitigate | Selectors consume DTOs only; tests prevent rule inference from geometry/legal movement. |
| T-11-15 | I | owner debug UI | mitigate | Existing owner debug toggle gates rendering and tests cover public absence. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/web test -- replay-client.test.tsx replay-state.test.ts` and `pnpm --filter @cowards/web typecheck`.
</verification>

<success_criteria>
DEBUG-04 and DEBUG-05 are user-visible when owner replay renders DTO-derived inactivity explanations for the selected Soldier, public replay shows none of them, and React contains no new game-rule inference.
</success_criteria>

<output>
After completion, create `.planning/phases/11-doctrine-debugging-ux/11-05-SUMMARY.md`
</output>
