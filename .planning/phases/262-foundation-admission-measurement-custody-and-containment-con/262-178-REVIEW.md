---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "178"
reviewed: 2026-09-08T16:30:00Z
depth: deep
files_reviewed: 8
files_reviewed_list:
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/check-v1-38-lean-admission.ts
  - packages/spec/src/schemas.ts
  - packages/runtime-js/src/container-subprocess-adapter.ts
  - apps/runtime-service/src/runtime-config.ts
  - apps/runtime-service/src/execute-match.ts
  - .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v1.json
  - .planning/artifacts/v1.38-lean-runner-direct-validity-review-v1.json
findings:
  critical: 1
  warning: 0
  info: 0
  total: 1
status: issues_found
blockingFindingCount: 1
admitsPlan175: false
---

# Phase 262 Plan 178: Direct Result-Validity Review v2

**Outcome:** blocked. The preflight failed before any Match, no v2 authorization exists, and Plan 175 is not admitted. Every downstream authority remains false.

## Summary

This was exactly one fresh independent review of runnable source commit `a734dc36794e5de2d64a99793b3e075e0df33148`, tree `c09ccacfda174609ac36ea348055c98be8090fa4`, executable closure root `sha256:53cc9f23c709b99a642d58337c42a2047cf36d8d8bc4efc54fb3b3264c6fb58f`, the committed non-pass preflight, authorization absence, and actual selector reachability.

The review did not run the full preflight, any Match, or any live/recovery selector. It independently rechecked only Docker server availability, the locally present pinned image digest, Git ancestry/source closure, effect absence, lock inventory, schema requirements, and static runtime reachability. Docker remains at `29.4.0`; the exact local RepoDigest remains `node:24-alpine@sha256:2bdb65ed1dab192432bc31c95f94155ca5ad7fc1392fb7eb7526ab682fa5bf14`; tracked bytes are clean; and exactly 36 authenticated successor locks remain the only worktree residue.

## Narrative Findings (AI reviewer)

### CR-01: The `soldierBrain` preflight probe cannot satisfy its input schema

**Classification:** BLOCKER  
**Category:** `tuple_or_schedule_drift`  
**File:** `scripts/run-v1-38-lean-runner-feasibility.ts:423-426`

**Issue:** `preflightProbeInput("soldierBrain")` creates 25 cells containing only `dx`, `dy`, and `contents`. `AwarenessCellSchema` requires integer `absoluteX` and `absoluteY` for every cell (`packages/spec/src/schemas.ts:348-355`). Consequently `SoldierBrainInputSchema.parse` throws at input construction before the adapter can measure either fixture's complete method pair. The committed preflight truthfully reports zero accepted samples, no throughput projection, zero Match invocations, and no authorization.

**Fix:** In a separately planned source-repair commit, derive `absoluteX` and `absoluteY` from the probed Soldier's canonical position plus `dx`/`dy`, add a regression test that parses all 25 cells for both fixture methods, and only then run a fresh non-consuming preflight. Do not reuse the failed preflight or issue authorization from it.

## Seven-Category Outcome

| # | Category | Status | Independent conclusion |
|---:|---|---|---|
| 1 | `source_or_dirty_byte_drift` | PASS | The exact commit/tree/closure resolves, remains the latest runnable-source change, and matches current tracked runnable bytes. Current descendant changes are planning-only; 36 authenticated locks are the only residue. |
| 2 | `multiple_launch` | PASS | Reviewed-ready and preflight precede exclusive marker creation. Authorization v2 is absent, so launch cannot become ready. All v2 live effects are absent. |
| 3 | `tuple_or_schedule_drift` | **BLOCKER** | The `soldierBrain` probe omits schema-required absolute coordinates, so the fixed fixture/runtime schedule cannot be preflighted and has no feasible-duration evidence. |
| 4 | `supervised_runtime_escape` | PASS | Reachable Match construction selects `container-subprocess` / `runtime-js-container-subprocess`, the exact pinned image and restrictive Docker controls, with no fallback. |
| 5 | `partial_interrupted_or_unclean_evidence` | PASS | The non-pass has zero reusable samples and zero Match invocations; no authorization, marker, terminal, adjudication, or eligibility exists. |
| 6 | `private_data_disclosure` | PASS | Both new artifacts are aggregate-only and contain no source, memory, objective, streams, environment, credentials, host paths, or raw diagnostics. |
| 7 | `non_pass_authority` | PASS | Non-pass produced no authorization; `admitsPlan175:false` and all authority flags are false. No authorization root is claimed. |

## Expected Plan/Source Mismatch

The committed checker cannot validate this blocked branch as written. Its v2 code accepts only the pass-path `v1.38-lean-runner-container-preflight-v1` at `.planning/artifacts/v1.38-lean-runner-container-preflight-v1.json`, then requires an authorization-backed `v1.38-lean-runner-direct-validity-review-v2` containing `admitsPlan179`. Plan 178 instead requires the direct preflight path, a truthful non-pass branch without authorization, and `admitsPlan175`. The plan's advertised `--check-direct-container-preflight-outcome-v1` selector is absent. Therefore the machine artifact deliberately uses the distinct schema `v1.38-lean-runner-direct-validity-review-blocked-outcome-v2`; invoking the pass-path checker would be misleading and was not done.

This mismatch does not make the failed preflight pass and grants no authority. It is recorded for the required source-repair/replan step.

## History and Effect Preservation

- Plan 174 remains denied history with authorization root `sha256:3c546e446e8f5fb9f062676d88ebf18b58ccf2070c6d9faa636fe8312634c04c`, review root `sha256:8c0b81a777ec5b5513a4afbf582906f7d19a12e8102b1e1688d12e39f453d073`, and two active findings in its original review.
- Plan 172's `CR-V6-01` through `CR-V6-04` and `WR-V6-01` remain open certification-only, nonblocking history under D-34L.1. None is erased, fixed by prose, or counted as zero.
- Authorization v2, invocation v2, terminal v2, adjudication v2, and eligibility v2 are absent.
- The failed preflight consumed no Match opportunity: `matchInvocations:0`, `markerWritten:false`, and `terminalWritten:false`.

---

_Reviewer: fresh independent gsd-code-reviewer `/root/execute_262_178/review_262_178_nonpass`_  
_Disposition: blocked; Plan 175 denied; source repair prohibited in this plan_
