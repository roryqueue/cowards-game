---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: 27
type: tdd
wave: 22
depends_on: [262-26]
files_modified:
  - scripts/lib/v1-38-current-matrix-child-protocol.ts
  - scripts/lib/v1-38-current-matrix-reproduction.ts
  - scripts/fixtures/v1-38-current-matrix-child-protocol-v2-fixture.ts
  - scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-27-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-02, ADMIT-03, ADMIT-04]
must_haves:
  truths:
    - "A real production shard child emits canonical protocol-v2 control frames and the production parent decodes those exact bytes; the parent never fabricates a child failure frame locally."
    - "Expected typed runtime-service failures remain in the normal result envelope, while unexpected integrity exceptions reduce to exactly CHILD_BOOTSTRAP_FAILED, CHILD_TRANSPORT_FAILED, RUNTIME_EXECUTION_FAILED, or SHARD_COORDINATION_FAILED."
    - "A dedicated bounded control channel implements exact ready/terminal ordering and fails closed on missing, malformed, duplicate, conflicting, unknown, oversized, invalid-UTF-8, or out-of-order frames."
    - "Standalone synthetic subprocess tests have no route-artifact setup and no Strategy, Match, provider, live-observation, host-pressure, database, or production-authority dependency."
    - "Plan 262-25 and all prior evidence remain byte-identical; this plan creates no authority and performs no live observation or evidence write."
  artifacts:
    - path: scripts/lib/v1-38-current-matrix-child-protocol.ts
      provides: "Canonical bounded protocol-v2 frame schema, decoder, and closed state reducer"
    - path: scripts/lib/v1-38-current-matrix-reproduction.ts
      provides: "Production child emission and parent decoding through the real shard launch topology"
    - path: scripts/fixtures/v1-38-current-matrix-child-protocol-v2-fixture.ts
      provides: "Synthetic fixed-mode child with no gameplay/runtime-provider imports"
    - path: scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts
      provides: "Standalone protocol-v2 unit and real subprocess proof"
  key_links:
    - from: scripts/lib/v1-38-current-matrix-reproduction.ts
      to: scripts/lib/v1-38-current-matrix-child-protocol.ts
      via: "real child writes canonical frames; real parent invokes protocol-v2 decoder on received control bytes"
      pattern: "decodeV138CurrentMatrixChildProtocolV2"
    - from: scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts
      to: scripts/fixtures/v1-38-current-matrix-child-protocol-v2-fixture.ts
      via: "Node --import tsx subprocess modes over the same bounded control topology"
---

<objective>
Implement and prove the offline child control protocol that makes a later successor materially different from the expired route.

Purpose: Repair the information-loss seam before any new authority exists, per D-01 through D-10 and D-16, while preserving D-03 runtime ownership, D-04 privacy/production unreachability, D-05 charging, and D-22 formation absence.
Output: Protocol-v2 source, production child/parent wiring, standalone synthetic fixtures/tests, and `262-27-SUMMARY.md`.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md

Use RED -> GREEN -> REFACTOR and atomic commits. This plan is wholly offline. No writer CLI, provider, live observation, `ps`, memory-pressure command, Strategy, Match, preflight, calibration, reproduction, or `.planning/artifacts` mutation is authorized.
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-27-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-26-SUMMARY.md
@scripts/lib/v1-38-current-matrix-child-protocol.ts
@scripts/lib/v1-38-current-matrix-reproduction.ts
@scripts/evaluate-v1-38-foundation-contract-successor-routes.test.ts
</context>

<prohibitions>
- Do not change the inclusive 2,500-basis-point gate, exact 200 ms RSS observation contract, 8 attempts/4 shards, conditional 540 cells, v1.18/v1.19/MATCH_KERNEL authority, historical predicate, gameplay, public/default taxonomy, privacy, or formation absence.
- Do not expose text, detail, status, signal, stderr, source, memory, objective, path, environment, host, process identifiers, attempt payloads, tokens, database details, or security internals in any frame or projection.
- Do not add environment, global-symbol, test-registration, caller-supplied classification, or caller-supplied authority hooks. Test injection is allowed only at pure closed function boundaries.
- Do not synthesize a canonical child failure frame in the parent. No missing or malformed frame may fall back to the legacy catch-all classification.
- Do not import Strategy, Match, provider, execution-observation, RSS/host-pressure, or route-artifact modules from the standalone fixture or test.
- Do not edit files outside the exact allowlist below.
</prohibitions>

<changed_file_allowlist>
scripts/lib/v1-38-current-matrix-child-protocol.ts
scripts/lib/v1-38-current-matrix-reproduction.ts
scripts/fixtures/v1-38-current-matrix-child-protocol-v2-fixture.ts
scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts
.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-27-SUMMARY.md
</changed_file_allowlist>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the closed protocol-v2 state machine in standalone tests</name>
  <files>scripts/fixtures/v1-38-current-matrix-child-protocol-v2-fixture.ts, scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts, scripts/lib/v1-38-current-matrix-child-protocol.ts</files>
  <read_first>
    - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md — mapped protocol, fixture, exact-key, mutation, subprocess, and import.meta.url patterns
    - scripts/check-v1-37-audit-reproduction.test.ts — closest exact-key inventory and one-mutation-at-a-time fixture analog
    - scripts/lib/v1-38-current-matrix-child-protocol.ts — existing canonical-byte decoder and finite v1 taxonomy
    - scripts/lib/v1-38-current-matrix-reproduction.ts:3487-3840 — current spawn, buffering, cleanup, and local-synthesis seam
    - scripts/lib/v1-38-current-matrix-reproduction.ts:7462-7493 — current whole-handler catch
    - scripts/evaluate-v1-38-foundation-contract-successor-routes.test.ts:814-871 — coupled protocol tests to replace with standalone coverage
  </read_first>
  <behavior>
    - RED: exact ready followed by one exact success/runtime-failure/shard-failure terminal is accepted; every missing, duplicate, conflicting, unknown, oversized, invalid-UTF-8, noncanonical, or out-of-order frame is rejected.
    - RED: spawn failure is CHILD_TRANSPORT_FAILED; spawned exit/timeout before ready is CHILD_BOOTSTRAP_FAILED; ready plus invalid transport is CHILD_TRANSPORT_FAILED; ready plus canonical unexpected runtime/shard terminal preserves its exact family.
    - RED: success result and expected typed runtime-service failure envelopes stay byte/schema compatible and do not enter the integrity-family channel.
    - RED: captured control/stdout/stderr bytes contain none of the prohibited private, runtime, host, or process fields.
  </behavior>
  <action>Follow the shared `262-PATTERNS.md` protocol/fixture assignment and the exact-key inventory plus one-mutation-at-a-time style in `scripts/check-v1-37-audit-reproduction.test.ts`. Create a new versioned canonical frame contract with exact keys, finite discriminants, strict byte/cardinality/order bounds, and a pure parent-observed reducer. Use a dedicated inherited pipe or an equally strong closed channel physically separate from stdout success bytes and stderr. The four integrity families are exhaustive and non-extensible. Build the mapped fixed-mode synthetic fixture as a child that imports only Node primitives plus the protocol module, emits deterministic no-op frames/results, and supports isolated mutations of keys, discriminants, bytes, cardinality, and ordering without gaining production authority. Delete protocol-fixture execution from production module scope if the new fixture supersedes it. Derive repository paths from `import.meta.url`, run the standalone file first, and retain an explicit failing RED commit.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=120000 --bail=1</automated>
  </verify>
  <acceptance_criteria>
    - The standalone suite runs from the current post-live checkout without reading or constructing any route artifact.
    - All four families and unchanged success/expected-failure envelopes have exact positive tests; every malformed topology accepts zero results.
    - Static imports in the fixture/test contain no Strategy, Match, provider, live-observation, RSS/host-pressure, database, or route-seal module.
  </acceptance_criteria>
  <done>The closed state machine is specified by standalone failing tests and a synthetic subprocess topology that cannot reach live/gameplay authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire real child-emitted frames into the production parent</name>
  <files>scripts/lib/v1-38-current-matrix-child-protocol.ts, scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts</files>
  <read_first>
    - scripts/lib/v1-38-current-matrix-reproduction.ts:3125-3182 — expected runtime-service outcome handling
    - scripts/lib/v1-38-current-matrix-reproduction.ts:3487-3840 — production parent shard runner
    - scripts/lib/v1-38-current-matrix-reproduction.ts:7462-7493 — production child entry
    - scripts/lib/v1-38-current-matrix-child-protocol.ts — Task 1 contracts
  </read_first>
  <behavior>
    - GREEN: the production child emits ready, then either success or one canonical unexpected integrity terminal at the exact decode/inventory/runtime/result-serialization boundary.
    - GREEN: the production parent decodes received child bytes through the protocol-v2 decoder and never constructs the canonical failure frame itself.
    - GREEN: request decoding, inventory lookup, and result validation/serialization exceptions become SHARD_COORDINATION_FAILED; unexpected runtime invocation exceptions become RUNTIME_EXECUTION_FAILED.
    - GREEN: expected typed runtime-service system failures remain ordinary per-attempt outcomes in the result envelope.
  </behavior>
  <action>Split `runShardCli` into explicit stage boundaries without catching away the stage identity. Emit ready only after bootstrap is complete enough for the child handler to own the channel. Emit canonical runtime or shard terminal from the child, then exit through one bounded path. In `createV138SubprocessShardRunner`, consume the actual control pipe bytes with the v2 decoder and state reducer; keep stdout exclusively for the existing result envelope. Preserve timeout/overflow/signal/spawn/cleanup handling, buffer zeroing, and the closed public/default coarse projection. Remove the current whole-handler catch and current parent-side canonical message construction. Add structural tests that fail if production decoder usage disappears or local synthesis returns.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=120000 --bail=1 &amp;&amp; pnpm typecheck &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Repository usage shows the protocol-v2 decoder is called by the production parent on child-emitted bytes.
    - The current catch-all and current parent-local frame synthesis seam are absent from the production path.
    - Four injected families are pairwise distinguishable in operator/lab terminal objects while public/default surfaces remain coarse.
    - The exact 200 ms RSS contract, inclusive 2,500-bp gate, 8/4 shape, conditional 540, runtime/kernel/historical predicate, gameplay, privacy, and formation identities are unchanged.
  </acceptance_criteria>
  <done>Production child/parent wiring is materially changed and the standalone protocol-v2 suite, typecheck, and diff checks pass without authority or live execution.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- Protocol-v2 source and production wiring in the two existing library modules.
- One standalone synthetic fixture and one standalone Vitest file.
- `262-27-SUMMARY.md` recording RED/GREEN commits, exact commands/verdicts, material-change evidence, changed-file allowlist proof, and explicit proof that no evidence/live command ran.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Child process → control channel | Untrusted child bytes must be canonical, finite, ordered, and bounded. |
| Control channel → production parent | Parent may classify only observed frames/state, never caller assertions. |
| Runtime result → stdout envelope | Expected typed failures and successes remain separate from integrity frames. |
| Synthetic fixture → test process | Fixture must be unable to import gameplay, provider, authority, or live observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-149 | Spoofing | control family | critical | mitigate | Exact finite frames plus parent-observed ready/terminal state machine. |
| T-262-150 | Tampering | frame order/cardinality | critical | mitigate | Canonical bytes, exact keys, bounds, order, single terminal, fail-closed reducer. |
| T-262-151 | Information Disclosure | child output | critical | mitigate | No detail fields; separate channel; byte scans; empty stderr; buffer zeroing. |
| T-262-152 | Elevation of Privilege | test injection | high | mitigate | Pure dependency injection only; no env/global/registration/caller authority hooks. |
| T-262-153 | Repudiation | parent-local synthesis | critical | mitigate | Structural production-usage test and review gate require decoder-on-child-bytes. |
| T-262-154 | Denial of Service | missing/oversize frame | high | mitigate | Existing bounded timeout/output/cleanup plus protocol byte cap. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-current-matrix-child-protocol-v2.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=120000 --bail=1 &amp;&amp; pnpm typecheck &amp;&amp; git diff --check &amp;&amp; test -z "$(git status --short -- .planning/artifacts)"</automated>
</verification>

<success_criteria>
- Standalone protocol-v2 proof passes independently of all route artifacts.
- Production parent decodes canonical frames emitted by the real child topology; current local synthesis/catch-all is gone.
- Expected typed runtime failures remain in the normal result envelope; unexpected exceptions use only the four closed families.
- No private diagnostics or authority bypass exists, and no frozen policy/identity/evidence byte changes.
</success_criteria>

<source_audit>
| Source | ID | Requirement / constraint | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Exact-authority current-rules reproduction must become diagnosable before another attempt | Tasks 1-2 | COVERED |
| REQ | ADMIT-02 | Bind exact source/runtime/protocol identities | Task 2 | COVERED |
| REQ | ADMIT-03 | Offline prerequisite for a later exact 540/540 route; not itself satisfaction | Tasks 1-2 | COVERED — remains blocked |
| REQ | ADMIT-04 | Missing/malformed control evidence fails closed | Tasks 1-2 | COVERED |
| REQ | ADMIT-01, MEAS-01..10, SEAL-01, DECI-02 | Existing custody/downstream plans remain unchanged | none | EXCLUDED — already covered or downstream |
| RESEARCH | — | Closed four-family protocol, standalone fixture, real child/parent production wiring, material-change gate | Tasks 1-2 | COVERED |
| CONTEXT | D-01..D-10, D-16, D-22 | Immutable evidence, exact runtime, privacy/accounting, no formation | All tasks/prohibitions | COVERED |
| CONTEXT | D-11..D-15, D-17..D-21 | Measurement/custody contracts owned by existing downstream plans | none | EXCLUDED — downstream |
| CONTEXT | Deferred Ideas | Planner/factory/league/formation/product/rule work | none | EXCLUDED |
</source_audit>

<output>
Create `262-27-SUMMARY.md` with complete RED/GREEN/refactor commits, exact test/typecheck verdicts, production decoder call-path evidence, absence of catch-all/local synthesis, changed-file allowlist proof, no-live/no-artifact proof, and handoff to Plan 262-28.
</output>
