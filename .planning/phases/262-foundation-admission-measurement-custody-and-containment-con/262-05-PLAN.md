---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "05"
type: execute
wave: 11
depends_on: ["262-04"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-study-contract.ts
  - scripts/lib/v1-38-measurement.ts
  - scripts/lib/v1-38-containment.ts
  - .planning/artifacts/v1.38-pre-search-contract.json
autonomous: true
requirements: [MEAS-10, DECI-02]
must_haves:
  truths:
    - "The three literal coordinate protocols, unchanged inward facings, equal-compute dimensions, telemetry, classifiers, and rejection logic are precommitted only as protocol_only data per D-22."
    - "One profile-neutral cold common root binds source/training/prompt/cache eligibility, common holdout opponent/schedule construction, and an auditable exclusion of profile-conditioned and current-trained inputs; matched execution order is preregistered and reorder-equivalent per D-20."
    - "Classifier fixtures are synthetic, profile-agnostic, symmetry/ID/orientation invariant, exact-denominator, and never instantiate GameState."
    - "No executable formation/profile/candidate/prompt/cache/trace/replay/result namespace, adapter, alternate state, or production import exists before Phase 266 freezes."
  artifacts:
    - path: "scripts/lib/v1-38-measurement.ts"
      provides: "Canonical opening clusters and causal classifier definitions"
      exports: ["canonicalizeV138OpeningCluster", "evaluateV138Classifiers"]
    - path: "scripts/lib/v1-38-containment.ts"
      provides: "AST, artifact, schema, and mutation-based pre-formation containment monitor"
      exports: ["analyzeV138PreFormationContainment"]
    - path: "scripts/lib/v1-38-study-contract.ts"
      provides: "Closed protocol schemas and producer for cold-common lineage, matched order, and common holdout construction"
      exports: ["V138ProfileNeutralCommonRootProtocolSchema", "V138MatchedExecutionOrderProtocolSchema", "V138CommonHoldoutConstructionProtocolSchema", "deriveV138ProfileNeutralProtocolRoot"]
  key_links:
    - from: "scripts/lib/v1-38-containment.ts"
      to: "scripts/lib/v1-38-study-contract.ts"
      via: "allow protocol-only records while denying executable materialization"
      pattern: "protocol_only"
---

<objective>
Operationalize the profile-neutral causal classifiers and prove the pre-formation non-materialization boundary, implementing D-03, D-04, D-06, and D-22.

Purpose: Freeze how the later experiment will be interpreted without creating any executable formation or production path.
Output: Pure classifiers, exact profile-neutral common-lineage/order/holdout protocols, synthetic invariant fixtures, and a static/adversarial containment monitor whose final receipt is generated only after all authorized Phase 262 files are finalized in Plan 07.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-04-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement profile-neutral classifier and denominator contracts</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md</file>
    <file>scripts/lib/v1-38-measurement.ts</file>
    <file>packages/spec/src/canonical-json.ts</file>
    <file>.planning/artifacts/v1.38-pre-search-contract.json</file>
  </read_first>
  <files>scripts/lib/v1-38-measurement.ts, scripts/evaluate-v1-38-foundation-contract.test.ts, .planning/artifacts/v1.38-pre-search-contract.json</files>
  <behavior>
    - Horizontal reflection, entrant swap plus 180-degree rotation, opaque-ID rename, and Soldier-order permutation preserve canonical opening-cluster identity.
    - Scripted opening, convoy, reserve hoarding, persistent turtle, STONE shield, interaction, inactivity, draw, Match length, response gap, pure worst case, and confounders bind exact denominators and replication rules.
    - Positive, negative, boundary, mirrored, renamed, permuted, obfuscated, near-miss, malformed, missing, duplicate, conflicting, and fuzz/property fixtures are covered.
  </behavior>
  <action>Add `classifiers` property/mutation tests, then implement pure canonical projection, opening cluster, entropy normalization, and the complete DECI-02 classifier inventory. Bind denominator type, eligible-cell root, replication unit, missingness, normalization, and implementation root for every metric. Use only bounded synthetic projections; forbid filesystem/runtime/engine imports and any `GameState` construction. Encode non-compensating hard classifier logic.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t classifiers --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>All invariance and boundary fixtures pass, malformed/conflicting evidence fails closed, and no fixture contains candidate, current-trained, or holdout material.</acceptance_criteria>
  <done>DECI-02 and the classifier portion of MEAS-10 are exact, profile-neutral, and reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze literal profiles as non-materializable protocol data</name>
  <read_first>
    <file>.planning/milestone-proposals/v1.38-competitive-strategy-factory-and-adversarial-league/ACTIVATION-PROMPT.md</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md</file>
    <file>scripts/lib/v1-38-study-contract.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-study-contract.ts, .planning/artifacts/v1.38-pre-search-contract.json, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - Current-edge is x=2..9 at y=0/11; inward is x=2..9 at y=1/10; bracket coordinates exactly match the activation contract.
    - Every record has protocol_only true and materialization forbidden before Phase 267, including current-edge control.
    - The cold common root, matched-order protocol, and common holdout-construction lineage prove that source data, training data, prompts, caches, opponents, and schedules use neither profile-conditioned nor current-trained input.
    - No exported converter, state constructor, manifest, cache, prompt, trace, replay, result, or candidate/profile artifact is created.
  </behavior>
  <action>Extend the pre-search contract with the exact literal coordinate/facing protocols, rationale, equal-compute dimensions, telemetry, classifier IDs, rejection logic, and non-authorization text per D-22. In `v1-38-study-contract.ts`, define exact-keyed `V138ProfileNeutralCommonRootProtocolSchema`, `V138MatchedExecutionOrderProtocolSchema`, and `V138CommonHoldoutConstructionProtocolSchema`, plus `deriveV138ProfileNeutralProtocolRoot`. The cold-common schema must bind eligible source-dataset policy root, training-data eligibility predicate/root, prompt-template policy root, cache policy, construction-code roots, exclusion-proof schemas, and explicit required invariants `profileConditionedInputs: 0`/`currentTrainedInputs: 0`. The matched-order schema must bind opaque slot-shape policy, matched root-seed-block derivation, branch-neutral counterbalancing/permutation algorithm root, fixed side/initiative/arena order policy, and reorder-equivalence proof procedure. The common-holdout schema must bind only the expected profile-agnostic source-eligibility, opponent-construction, schedule-construction, commitment-lineage schema, and equality-policy roots; it must not claim or contain a real commitment/source-lineage value before custody authorization and must not expose opponent or schedule preimages. Derive and persist one domain-separated public `profileNeutralProtocolRoot` from these policy/schema records and add mutation tests for every omitted/swapped/profile/current-trained field. Treat all three arms as private lab protocol data, including current-edge, and create no formation, candidate, opponent, schedule, or real commitment output.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t 'classifiers|protocol.only' --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Coordinates/facings are exact protocol text with no callable or importable path to GameState or any product surface.</acceptance_criteria>
  <done>MEAS-10 precommits the experiment, cold-common lineage, matched execution order, and common holdout construction while every formation/candidate/holdout output remains unmaterialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove pre-formation and production containment</name>
  <read_first>
    <file>scripts/check-v1-37-integrity-boundaries.ts</file>
    <file>scripts/check-boundary-monitors.ts</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md</file>
    <file>AGENTS.md</file>
  </read_first>
  <files>scripts/lib/v1-38-containment.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - AST analysis detects direct, aliased, and disguised forbidden imports or executable state even under an allowed filename.
    - Exact artifact inventory rejects formation/profile/candidate/prompt/cache/trace/replay/result namespaces and unapproved keys.
    - Seeded bypass mutations are all detected before the monitor can report a passing scan.
  </behavior>
  <action>Add `containment` mutation fixtures, then implement AST import/symbol/alias analysis plus exact artifact/schema/content inventories. Detect dynamic code, Node vm, direct Strategy execution/imports, engine state constructors in protocol/classifier code, alternate schedulers/rules, production imports of v1.38 lab tooling, mutable aliases, forbidden namespaces, private receipt keys, and materialization functions. Expose deterministic scan and receipt-building APIs, but do not persist the final receipt in this plan: Plan 07 must run the monitor against the finalized authorized Phase 262 scope so later custody/root files cannot stale the proof. Require every seeded bypass to be caught and the preliminary real-tree scan to have zero findings per D-03/D-04/D-06/D-22.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t containment --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The monitor fails on every seeded bypass and passes the real bound inventory with zero findings.</acceptance_criteria>
  <done>The monitor and bypass suite are ready, and the preliminary scan proves the current tree clean without falsely sealing a scope that later plans still modify.</done>
</task>

</tasks>

## Artifacts this phase produces

- `canonicalizeV138OpeningCluster` and `evaluateV138Classifiers` in `scripts/lib/v1-38-measurement.ts`
- `analyzeV138PreFormationContainment` in `scripts/lib/v1-38-containment.ts`
- Synthetic classifier fixtures inside `scripts/evaluate-v1-38-foundation-contract.test.ts`
- The final `.planning/artifacts/v1.38-pre-formation-containment.json` is intentionally deferred to Plan 07 after the authorized implementation/artifact scope is finalized.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Protocol records → executable code | Literal profiles must not become executable state before the freeze gate. |
| Private lab tooling → production graph | Experimental logic/data must remain unreachable from product surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-16 | Elevation of Privilege | profile protocol | high | mitigate | Closed protocol-only schema, no converters/imports, AST and seeded-bypass checks. |
| T-262-17 | Information Disclosure | production import graph | high | mitigate | Dependency/source analysis and exact artifact allowlist. |
| T-262-18 | Tampering | classifiers | high | mitigate | Invariance/property fixtures, exact denominators, roots, and non-compensating evaluation. |
| T-262-19 | Spoofing | containment receipt | medium | mitigate | Bind monitor source, scanned inventory, allowlist, mutation outcomes, and tree root. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
Confirm no new executable formation/candidate namespace or product dependency appears in the preliminary tree.
</verification>

<success_criteria>
- The experiment protocol and causal classifiers are frozen without materialization.
- Every classifier has exact normalization, denominators, and invariant fixtures.
- The monitor detects both obvious and disguised boundary bypasses.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-05-SUMMARY.md` when done.
</output>
