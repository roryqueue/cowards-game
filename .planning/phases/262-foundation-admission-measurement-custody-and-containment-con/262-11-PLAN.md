---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "11"
type: execute
wave: 6
depends_on: ["262-10"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-current-matrix-reproduction.ts
  - .planning/artifacts/v1.38-current-matrix-calibration-v2.json
  - .planning/artifacts/v1.38-current-matrix-reproduction-v3.json
autonomous: false
gap_closure: true
requirements: [ADMIT-03]
must_haves:
  truths:
    - "Failed receipt sha256:99187d35b9a14e263be6cc35a6335bdd3957d5fede647345326c8e015891b280 and all earlier charged roots remain immutable; only new successor paths are written per D-01/D-02/D-05."
    - "An injected RSS command adapter classifies synchronous and callback command denial as RESOURCE_SAMPLER_SPAWN_DENIED or RESOURCE_MEASUREMENT_UNAVAILABLE, never a generic runner exception or a successful zero measurement."
    - "Every post-spawn failure terminates the real process group, awaits actual close/error evidence, performs an orphan probe, and cannot synthesize positive cleanup claims."
    - "No retry calibration runs without explicit user selection of one of the two permitted sampler policies; there is no default or inferred approval."
    - "Authorized calibration:v2 and a fresh full run preserve the 90-minute gate, exact v1.18/v1.19/MATCH_KERNEL authority, complete accounting, zero partial reuse, and atomic 0-or-540 publication."
  artifacts:
    - path: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      provides: "Injected RSS adapter, denial classification, truthful cleanup barrier, calibration:v2, and successor-only commands"
      exports: ["V138RssCommandAdapter", "sampleV138ChildRss", "createV138SubprocessShardRunner", "writeV138ParallelCalibrationV2Receipt", "writeV138AuthoritativeMatrixV3Receipt"]
    - path: ".planning/artifacts/v1.38-current-matrix-calibration-v2.json"
      provides: "New authorized calibration:v2 admitted or stopped successor"
    - path: ".planning/artifacts/v1.38-current-matrix-reproduction-v3.json"
      provides: "New exact passing reproduction or stopped full-run successor"
  key_links:
    - from: ".planning/artifacts/v1.38-current-matrix-calibration-v2.json"
      to: ".planning/artifacts/v1.38-current-matrix-reproduction.json"
      via: "immutable predecessor and charged calibration:v1 lineage"
      pattern: "sha256:99187d35b9a14e263be6cc35a6335bdd3957d5fede647345326c8e015891b280"
    - from: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      to: "node:child_process"
      via: "injected RSS adapter handling synchronous throw and callback error"
      pattern: "RESOURCE_SAMPLER_SPAWN_DENIED"
    - from: ".planning/artifacts/v1.38-current-matrix-reproduction-v3.json"
      to: ".planning/artifacts/v1.38-current-matrix-calibration-v2.json"
      via: "checked admitted calibration required before full launch"
      pattern: "calibrationV2ReceiptRoot"
---

<objective>
Repair the real subprocess sampling/cleanup boundary, obtain explicit sampler authorization, then make one new content-addressed calibration:v2/full-run successor attempt without modifying prior receipts or weakening ADMIT-03.

Purpose: Close the proven `ps` permission-denial and false-cleanup-evidence defect while preserving every charged failure and exact runtime/resource gate.
Output: Real-boundary regression tests, injected sampling and truthful cleanup, a blocking authorization decision, and new v2/v3 successor artifacts.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-10-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/artifacts/v1.38-current-matrix-reproduction.json
@scripts/lib/v1-38-current-matrix-reproduction.ts
@scripts/evaluate-v1-38-foundation-contract.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the real sampler and post-spawn cleanup boundary</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-10-SUMMARY.md</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
    <file>scripts/evaluate-v1-38-foundation-contract.test.ts</file>
    <file>apps/runtime-service/src/execute-match.ts</file>
    <file>packages/runtime-js/src/worker-host.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - RSS command invocation catches both synchronous throw and callback error; proven permission denial maps to RESOURCE_SAMPLER_SPAWN_DENIED and other unavailable/ambiguous samples map to RESOURCE_MEASUREMENT_UNAVAILABLE.
    - Every sampler/runner failure after child spawn enters one idempotent cleanup barrier with actual group signals, real close/error evidence, and a real orphan probe.
    - `exitAwaited: true` requires a captured terminal event and `orphanProcessIds: []` requires a completed probe; unknown evidence fails cleanup.
    - Real-boundary smoke covers one shard, four concurrent shards, both sampler denial forms, and exact `node --import tsx ... --execute-shard`.
  </behavior>
  <action>Add failing `matrix real process boundary`, `matrix sampler denial`, and `matrix real cleanup proof` tests first. Inject `V138RssCommandAdapter` into `createV138SubprocessShardRunner`; catch the adapter call itself and its callback error so managed-sandbox `spawn EPERM` cannot reject outside supervision. Parse one positive integer RSS value for the exact PID; reject empty, multiple, zero, negative, malformed, signalled, missing-command, nonzero-exit, or unit-ambiguous output. Emit only public-safe RESOURCE_SAMPLER_SPAWN_DENIED for proven command-spawn permission denial and RESOURCE_MEASUREMENT_UNAVAILABLE otherwise; keep raw errors and host details private. Remove synthetic `runnerExceptionTerminal` cleanup success. Route every post-spawn failure through one bounded cleanup barrier that records observed graceful/forced group signals, awaits real `close` or `error`, probes the process group for orphans, and leaves unknown fields false/non-empty rather than inventing success. Any uncertainty becomes CLEANUP_PROOF_FAILED with zero accepted cells. Add real subprocess smoke using distinct charged `diagnostic_test:v2:*` identities: run one exact shard and four-wide, then trigger synchronous and callback sampler denial after real spawn and prove termination/orphan evidence. Mutation-test denial code, adapter/source identity, PID binding, RSS parser/units, callback multiplicity, event source/order, orphan-probe denial, false `exitAwaited`, false empty-orphan claims, and diagnostic/calibration aliasing per D-02/D-03/D-05/D-10.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix real process boundary|matrix sampler denial|matrix real cleanup proof" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Real child tests reproduce both denial forms without a generic runner exception, prove observed termination/orphan accounting, reject synthetic cleanup mutations, and charge all diagnostic work with zero accepted/reusable cells.</acceptance_criteria>
  <done>The real process boundary fails closed with specific sampler reasons and truthful cleanup before any retry calibration is authorized.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize the resource sampler policy for calibration:v2</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-11-PLAN.md</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-10-SUMMARY.md</file>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <action>Run only the focused authorization-parser tests, then pause and present exactly the two options below. Record the user's literal selection and, for an equivalent sampler, its complete exact policy fields. Do not run calibration, request sampler command escalation, create either successor artifact, or infer approval while awaiting a valid response.</action>
  <decision>Select the sole resource sampler policy authorized for the new calibration:v2 attempt. No option is preselected; silence, auto-advance, prior approval, or passing tests are not authorization.</decision>
  <context>The managed sandbox denies parent `ps -o rss= -p PID` with synchronous `spawn EPERM`, although exact shard children work alone and four-wide. Calibration remains blocked until explicit authorization.</context>
  <options>
    <option id="authorized-unsandboxed-ps">
      <name>Authorize unsandboxed ps sampler</name>
      <pros>Uses the existing frozen RSS command, units, aggregation, limits, and projection; only exact read-only sampler/orphan probes cross the sandbox for Plan 262-11.</pros>
      <cons>Explicitly permits `ps -o rss= -p PID` and the read-only process-group orphan probe outside the managed sandbox.</cons>
    </option>
    <option id="approved-equivalent-sampler">
      <name>Approve equivalent sampler policy</name>
      <pros>Allows a separately reviewed read-only RSS/process-group source if unsandboxed ps is unavailable.</pros>
      <cons>The user must specify/approve exact adapter identity, command/API, PID binding, units, aggregation, permission boundary, failures, and orphan proof; execution remains blocked until complete and mutation-tested.</cons>
    </option>
  </options>
  <resume-signal>Reply exactly `authorized-unsandboxed-ps`, or `approved-equivalent-sampler` followed by the complete exact policy. Do not continue on any other response.</resume-signal>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix sampler authorization" --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>The authorization parser rejects missing, defaulted, inferred, unknown, incomplete-equivalent, or previously granted permission and accepts only one explicit permitted selection.</acceptance_criteria>
  <done>One exact sampler policy is explicitly selected and recorded, or Plan 262-11 remains paused without calibration or successor artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Run authorized calibration:v2 and the fresh authoritative successor</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-11-PLAN.md</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-10-SUMMARY.md</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/artifacts/v1.38-historical-matrix-expectation.json</file>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
    <file>scripts/evaluate-v1-38-foundation-contract.test.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts, .planning/artifacts/v1.38-current-matrix-calibration-v2.json, .planning/artifacts/v1.38-current-matrix-reproduction-v3.json</files>
  <action>Require the recorded Task 2 selection without inference/default. For authorized-unsandboxed-ps, use only exact read-only `ps -o rss= -p PID` plus approved group probe outside the sandbox; all shard/runtime work and frozen policy remain unchanged. For approved-equivalent-sampler, implement only the exact approved contract, bind its policy/source root, and rerun Task 1 tests before calibration. Never modify `.planning/artifacts/v1.38-current-matrix-reproduction.json`; verify its bytes/root remain `sha256:99187d35b9a14e263be6cc35a6335bdd3957d5fede647345326c8e015891b280`. Create `calibration:v2:{0..7}:{templateAttemptId}` identities and invoke `--calibrate-parallel-v2-receipt .planning/artifacts/v1.38-current-matrix-calibration-v2.json` under the unchanged eight-attempt/four-shard, 2 GiB child, 4 GiB aggregate, 25% headroom, ten-minute shard, deterministic projection, and 5,400,000 ms policy. Bind explicit authorization, sampler policy/source, immutable Plan 262-10 root, earlier charged roots, diagnostic/calibration dispositions, real samples, and truthful cleanup. Any failure seals only this new path as stopped_process_failure with zero accepted cells and no full run. Only a checked calibration_admitted v2 receipt may invoke `--write-authoritative-v3-receipt .planning/artifacts/v1.38-current-matrix-reproduction-v3.json --calibration .planning/artifacts/v1.38-current-matrix-calibration-v2.json`. Run all 540 cells fresh through exact v1.18 prepared service, supervised worker-thread v1.19 ABI, and selected MATCH_KERNEL; use distinct `reproduction:v3:{cellId}` execution IDs while retaining canonical cell IDs; charge every disposition; reuse zero diagnostic/calibration/prior partial cells; enforce unchanged resources/90 minutes; and publish only after exact 540/540 inventory, independent predicate, aggregate, cleanup, and root checks. Add exact v2/v3 checker and branch-require modes plus authorization, sampler, predecessor, old-path, calibration:v1/diagnostic reuse, missing-lineage, stopped-zero, improper-launch, and final-root mutations. Any stop creates a stopped 262-11 Summary and leaves Plan 262-03 blocked.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix sampler denial|matrix real cleanup proof|matrix successor lineage|matrix calibration v2 branches|matrix authoritative v3 receipt" --maxWorkers=1 &amp;&amp; node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --check-calibration-v2-receipt .planning/artifacts/v1.38-current-matrix-calibration-v2.json &amp;&amp; node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --check-authoritative-v3-receipt .planning/artifacts/v1.38-current-matrix-reproduction-v3.json &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Success requires explicit sampler authorization, admitted calibration:v2, fresh 540/540 v3 within unchanged gates, zero failure/cleanup uncertainty/reuse, and exact successor/lineage roots. Any failure yields a checked stopped successor with zero accepted cells and no downstream unblock.</acceptance_criteria>
  <done>ADMIT-03 closes only through checked v3 passed_exact; all prior failures remain immutable charged lineage.</done>
</task>

</tasks>

## Artifacts this plan produces

- Real sampler-denial, subprocess, cleanup, orphan, and successor-lineage tests
- Injected sampler and truthful process-group cleanup implementation
- `.planning/artifacts/v1.38-current-matrix-calibration-v2.json` after explicit authorization
- `.planning/artifacts/v1.38-current-matrix-reproduction-v3.json` only after admitted calibration launches a fresh run

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User authorization → sampler | Host process sampling crosses the sandbox only after explicit policy selection. |
| RSS/orphan command → scheduler | Output is untrusted until PID, units, parser, terminal, and policy checks pass. |
| Spawned child → cleanup receipt | Positive cleanup requires observed close/error and orphan-probe evidence. |
| Failed v1 evidence → v2/v3 | Prior paths are immutable inputs; only new successors may be written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-45 | Spoofing | sampler authorization | high | mitigate | Blocking two-option decision; reject absent, inferred, default, or incomplete authorization. |
| T-262-46 | Denial of Service | RSS/orphan adapter | high | mitigate | Catch sync/callback denial, bounded lifecycle, typed stops, and no generic escape. |
| T-262-47 | Repudiation | cleanup proof | critical | mitigate | Actual close/error, observed group signals, bounded escalation, real orphan probe, no synthetic positives. |
| T-262-48 | Tampering | predecessor/successor lineage | critical | mitigate | Preserve root 99187d35..., use new paths, ordered canonical roots, and mutation tests. |
| T-262-49 | Elevation of Privilege | Match execution | critical | mitigate | Exact v1.18/v1.19/MATCH_KERNEL only; sampler approval grants no engine/loader/rule/runtime change. |
| T-262-50 | Information Disclosure | sampler/receipts | high | mitigate | Public-safe codes and scalar measurements only; omit raw errors, paths, source, memories, objectives, diagnostics. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix real process boundary|matrix sampler denial|matrix real cleanup proof|matrix successor lineage|matrix calibration v2 branches|matrix authoritative v3 receipt" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
The executor must additionally run the explicitly selected sampler path and both successor checkers; a stopped artifact is valid evidence but does not satisfy the success gate.
</verification>

<success_criteria>
- No retry calibration occurs without explicit selection of one permitted sampler policy.
- Sync/callback denial yields precise codes and real post-spawn cleanup.
- No positive cleanup exists without actual terminal/orphan evidence.
- Plan 262-10 and earlier receipts remain byte-identical and charged.
- calibration:v2/reproduction:v3 cannot alias or reuse prior work.
- The 90-minute, resource, runtime/kernel, accounting, and 0-or-540 gates remain unchanged.
- Only checked v3 passed_exact unblocks Plan 262-03; failures seal stopped successors with zero accepted cells.
</success_criteria>

<source_audit>
| Source | ID | Gap requirement/constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Exact authority and immutable pre-search contract | 262-11 | COVERED |
| REQ | ADMIT-03 | Exact supervised matrix reproduction; fixtures only | 262-11 | COVERED |
| VERIFICATION | — | Bounded runtime, inventory, cleanup, accounting, expectation, 90-minute gate | 262-11 | COVERED |
| DEBUGGER | — | Sync ps EPERM, callback escape, false cleanup, missing real-boundary tests | Task 1 | COVERED |
| CONTEXT | D-01/D-02/D-03/D-05/D-10/D-16 | Immutable fail-closed supervised evidence and charged accounting | Tasks 1/3 | COVERED |
| CONTEXT | Deferred | Planner/factory/league/formations/product/rule changes | none | EXCLUDED |
</source_audit>

<output>
Create `262-11-SUMMARY.md`. Mark success only for checked v3 passed_exact; otherwise record stopped_process_failure and keep downstream blocked.
</output>
