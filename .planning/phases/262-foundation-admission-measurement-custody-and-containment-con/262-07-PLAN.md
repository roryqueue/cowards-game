---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "07"
type: execute
wave: 12
depends_on: ["262-06"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.ts
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - package.json
  - .planning/artifacts/v1.38-custody-public-reference.json
  - .planning/artifacts/v1.38-pre-formation-containment.json
  - .planning/artifacts/v1.38-foundation-contract-root.json
  - .planning/artifacts/v1.38-foundation-contract-stop.json
autonomous: true
requirements: [SEAL-01]
must_haves:
  truths:
    - "The repository receives only an approved opaque custody reference and never secrets, preimages, raw access/query detail, private store paths, or evaluator internals per D-19/D-20."
    - "On the authorized route, one aggregate content-addressed root joins exact admission, reproduction, pre-search contract, custody reference, final non-stale containment, and generator/checker identities; no mutable latest exists per D-01."
    - "Any missing, stale, mismatched, contaminated, incomplete, non-reproducible, or unauthorized prerequisite prevents the aggregate root per D-02."
    - "On the no-authority route, Plan 07 emits only a terminal public-safe stopped artifact and summary, creates no custody reference, containment receipt, or aggregate root, cannot claim Phase 262 success, and blocks authoritative Phase 263 work."
  artifacts:
    - path: "scripts/evaluate-v1-38-foundation-contract.ts"
      provides: "One write/check orchestration command and aggregate-root builder"
      exports: ["generateV138FoundationContractRoot", "checkV138FoundationContract"]
    - path: ".planning/artifacts/v1.38-custody-public-reference.json"
      provides: "Approved bounded custody commitment/reference"
    - path: ".planning/artifacts/v1.38-foundation-contract-root.json"
      provides: "Immutable Phase 262 aggregate root consumed by downstream phases"
    - path: ".planning/artifacts/v1.38-foundation-contract-stop.json"
      provides: "Terminal non-authoritative stop artifact used only when custody authority is unavailable"
  key_links:
    - from: "scripts/evaluate-v1-38-foundation-contract.ts"
      to: ".planning/artifacts/v1.38-pre-formation-containment.json"
      via: "exact prerequisite root join"
      pattern: "containment"
    - from: ".planning/artifacts/v1.38-foundation-contract-root.json"
      to: ".planning/artifacts/v1.38-custody-public-reference.json"
      via: "approved opaque custody root only"
      pattern: "custody"
---

<objective>
Bind the approved custody reference and every passed Phase 262 gate into one reproducible aggregate root, implementing D-01 through D-06 and D-19 through D-22.

Purpose: Give Phases 263–270 one immutable pre-search authority while preserving privacy, containment, and fail-closed semantics.
Output: The final write/check command, package scripts, safe custody reference, and aggregate foundation root.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-06-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route terminally on custody authority and project only an authorized reference</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-06-SUMMARY.md</file>
    <file>scripts/lib/v1-38-custody.ts</file>
    <file>packages/spec/src/public-projection-privacy.ts</file>
    <file>.planning/artifacts/v1.38-pre-search-contract.json</file>
  </read_first>
  <files>scripts/evaluate-v1-38-foundation-contract.test.ts, .planning/artifacts/v1.38-custody-public-reference.json, .planning/artifacts/v1.38-foundation-contract-stop.json</files>
  <behavior>
    - Only a schema-valid authenticated existing-controls handoff can create the reference; stop/no-authority produces only the terminal stop artifact.
    - The reference exposes commitment profile/digest, opaque role/store/key/command/policy IDs, encrypted-object digest/length, the exact Plan 05 profile-neutral protocol root, and safe-projection bounds only.
    - Secret, salt, preimage, private path, raw access/query, hidden identity, evaluator state, and decryption fields are rejected.
  </behavior>
  <action>Parse `262-06-SUMMARY.md` as the exact discriminated Plan 06→07 contract. For `authorized_custody_handoff`, validate `V138AuthorizedCustodyHandoffSchema`, recompute the canonical handoff digest, and authenticate it through `verifyV138AuthorizedCustodyHandoff` against the approved control-plane receipt/verifier policy or optional already-authorized managed signer. Independently read `profileNeutralProtocolRoot` from the exact Plan 05-bound pre-search contract and require timing-safe/equivalent canonical equality with the authenticated handoff field before projection; add mutations for absent, stale, substituted, malformed, and correctly shaped but unequal roots, and prove all block custody reference and aggregate creation. Use `projectV138SafeCustodyReceipt` to render exactly the public allowlist, including the authenticated commitment digest, encrypted-object digest/length, equal `profileNeutralProtocolRoot`, opaque authority/policy IDs, and safe-projection bounds; reject secrets and private paths. For `stop_no_custody_authority`, lineage inequality, or any absent/stale/mismatched/unapproved handoff field, exclusively create `.planning/artifacts/v1.38-foundation-contract-stop.json` with a public-safe typed reason, delete/create neither custody reference nor authoritative artifacts, terminate Plan 07 immediately without executing Tasks 2–3, write a stopped `262-07-SUMMARY.md`, make no Phase 262 success claim, and explicitly block authoritative Phase 263 work per D-02/D-19/D-20. No-authority is a terminal route, not a failing test to bypass.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t custody --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The reference is bounded and privacy-safe, and every forbidden or unauthorized projection mutation blocks it.</acceptance_criteria>
  <done>Execution has either entered the authenticated authorized route with a bounded reference, or terminated honestly with no authoritative root and no downstream authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize authorized code and seal a non-stale containment scope</name>
  <read_first>
    <file>scripts/generate-v1-37-strategy-foundation-handoff.ts</file>
    <file>.planning/artifacts/v1.38-foundation-admission.json</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/artifacts/v1.38-pre-search-contract.json</file>
    <file>.planning/artifacts/v1.38-calibration-freeze-policy.json</file>
    <file>scripts/lib/v1-38-containment.ts</file>
    <file>.planning/artifacts/v1.38-custody-public-reference.json</file>
  </read_first>
  <files>scripts/evaluate-v1-38-foundation-contract.ts, scripts/evaluate-v1-38-foundation-contract.test.ts, package.json, .planning/artifacts/v1.38-pre-formation-containment.json</files>
  <behavior>
    - Authorized execution reaches this task only with a schema-valid authenticated custody reference; the stop route can never enter.
    - Final containment binds every finalized Phase 262 implementation, test, package-script, and prerequisite artifact in an exact allowlisted inventory.
    - The calibration-freeze-policy artifact is present in containment and `outputInventoryExcludingAggregateSelf`, and its byte digest and policy root equal the values bound by the pre-search contract.
    - The containment scan scope excludes only its own generated receipt bytes and the later aggregate self-artifact; the aggregate schema/source is scanned now and its canonical non-self component frame is validated separately.
    - The aggregate component frame joins exactly admission, reproduction, pre-search contract, custody reference, final containment root, and generator/checker source identities.
    - Write/check modes resolve the repository from import.meta.url, reject implicit discovery/mutable aliases, and emit only a bounded safe stdout summary.
    - Any prerequisite mutation, missing artifact, dirty identity, contamination, or forbidden inventory prevents root generation.
  </behavior>
  <action>Authorized-route precondition: fail if the authenticated custody reference from Task 1 is absent or a stop artifact exists. Implement and fully test `generateV138FoundationContractRoot` and `checkV138FoundationContract` with strict `--write`/`--check` dispatch, then add the package scripts and make no further source/test/package edits after this task's containment seal. Define `aggregateComponents` non-circularly as the exact roots for admission, matrix reproduction, pre-search contract, custody public reference, final containment, and generator/checker source identity. Define `outputInventoryExcludingAggregateSelf` as exact path/digest entries for every authorized Phase 262 output except `.planning/artifacts/v1.38-foundation-contract-root.json`; it must explicitly include `.planning/artifacts/v1.38-calibration-freeze-policy.json`, while the aggregate artifact's root field and full-file digest are never members of their own preimage. Parse the calibration policy artifact canonically, recompute its byte digest and policy root, and require both to equal the exact values bound by `.planning/artifacts/v1.38-pre-search-contract.json`. Add mutations that omit the policy artifact from the output inventory or containment allowlist, alter its bytes, substitute its policy root, or alter either pre-search binding; every mutation must block containment and aggregate generation. Validate immutable prerequisites byte-for-byte and precompute the aggregate non-self frame. Finally run `analyzeV138PreFormationContainment` over an explicit sorted allowlist containing every finalized Phase 262 implementation, shared test, `package.json` script entry, admission/reproduction/calibration-freeze-policy/pre-search/custody artifact, and the aggregate generator/schema source. Persist `.planning/artifacts/v1.38-pre-formation-containment.json` only now. Its scan scope excludes only the receipt's own bytes and the not-yet-written aggregate self-artifact; bind the aggregate non-self frame/schema root and require the later checker to closed-schema/privacy-validate that artifact. This precise exclusion cannot stale because Task 3 may write only the derived aggregate artifact and may not change any scanned file. Per D-01/D-02, there is no mutable alias, partial root, repair, override, contamination bypass, or post-seal source edit.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Mutation tests prove exact aggregate framing and containment coverage; omission of the calibration policy from either inventory, or any policy byte-digest/policy-root mismatch against the pre-search binding, fails before sealing; the final containment receipt binds the immutable sorted scan scope and any source/artifact/package mutation or forbidden addition makes check mode fail.</acceptance_criteria>
  <done>All authorized code and prerequisite artifacts are finalized, the non-circular aggregate frame is ready, and the final containment receipt cannot become stale when Task 3 writes only the derived self-excluded root artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the non-circular root and run the full authorized phase gate</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VALIDATION.md</file>
    <file>.planning/artifacts/v1.38-pre-formation-containment.json</file>
    <file>.planning/artifacts/v1.38-custody-public-reference.json</file>
    <file>scripts/evaluate-v1-38-foundation-contract.ts</file>
    <file>package.json</file>
  </read_first>
  <files>.planning/artifacts/v1.38-foundation-contract-root.json</files>
  <action>Authorized-route precondition: require the authenticated custody reference, final containment receipt, absence of the stop artifact, and byte-identical finalized scan scope; otherwise terminate without a root. Generate `.planning/artifacts/v1.38-foundation-contract-root.json` from the canonical non-self frame: hash only schema version, `aggregateComponents`, `outputInventoryExcludingAggregateSelf`, and bounded environment identities, then place the resulting domain-separated root beside that frame. Explicitly exclude the aggregate root field and aggregate file digest from their own preimage; `--check` recomputes the frame/root and separately validates the full artifact's closed schema, privacy, and derived byte stability. Run the complete Phase 262 suite, exact artifact check, admission command, final containment monitor, `pnpm typecheck`, and final serialized Turbo suite. Record focused latency separately from long integration gates. Verify the forbidden-artifact inventory and that only protocol-only profile records exist. Do not convert an environment, custody, matrix, or timing failure into an empirical result.</action>
  <verify>
    <automated>pnpm v1.38:foundation-contract:check &amp;&amp; pnpm typecheck &amp;&amp; pnpm turbo test --concurrency=1</automated>
  </verify>
  <acceptance_criteria>All focused and serialized checks pass, exact regeneration is clean, the forbidden inventory is empty, and the aggregate root resolves every required artifact.</acceptance_criteria>
  <done>The authorized route alone satisfies the validation contract and can hand one immutable non-circular authority root to Phase 263.</done>
</task>

</tasks>

## Artifacts this phase produces

Complete Phase 262 created-symbol/path inventory:

- `scripts/evaluate-v1-38-foundation-contract.test.ts`
- `evaluateV138FoundationAdmission` in `scripts/lib/v1-38-foundation-admission.ts`
- `reproduceV138CurrentMatrix` in `scripts/lib/v1-38-current-matrix-reproduction.ts`
- `V138StudyContractSchema`, `calibrateV138StudyContract`, and `validateV138AccountingClosure` in `scripts/lib/v1-38-study-contract.ts`
- `evaluateV138FrozenGates`, `validateV138ReportState`, `lintV138Claim`, `canonicalizeV138OpeningCluster`, and `evaluateV138Classifiers` in `scripts/lib/v1-38-measurement.ts`
- `analyzeV138PreFormationContainment` in `scripts/lib/v1-38-containment.ts`
- `createV138CustodyCommitment`, `executeV138CustodyCommand`, and `projectV138SafeCustodyReceipt` in `scripts/lib/v1-38-custody.ts`
- `generateV138FoundationContractRoot` and `checkV138FoundationContract` in `scripts/evaluate-v1-38-foundation-contract.ts`
- `.planning/artifacts/v1.38-foundation-admission.json`
- `.planning/artifacts/v1.38-current-matrix-reproduction.json`
- `.planning/artifacts/v1.38-calibration-freeze-policy.json`
- `.planning/artifacts/v1.38-pre-search-contract.json`
- `.planning/artifacts/v1.38-custody-public-reference.json`
- `.planning/artifacts/v1.38-pre-formation-containment.json`
- `.planning/artifacts/v1.38-foundation-contract-root.json`
- `v1.38:foundation-contract:write` and `v1.38:foundation-contract:check` in `package.json`

## Multi-Source Coverage Audit

| Source | ID/range | Plan(s) | Status | Notes |
|---|---|---|---|---|
| GOAL | Phase 262 goal | 01-07 | COVERED | Exact admission through immutable scientific/custody/containment root. |
| REQ | ADMIT-01, ADMIT-02, ADMIT-04 | 01 | COVERED | Exact predecessor join and typed stop. |
| REQ | ADMIT-03 | 02 | COVERED | Supervised 540-Match regression reproduction. |
| REQ | MEAS-01..MEAS-04 | 03 | COVERED | Study, structural opportunity, and two-ledger contract. |
| REQ | MEAS-05..MEAS-09 | 04 | COVERED | Calibrated gates, reports, and claims. |
| REQ | MEAS-10, DECI-02 | 05 | COVERED | Protocol-only profiles, classifiers, and containment. |
| REQ | SEAL-01 | 06-07 | COVERED | Synthetic custody, real authorization checkpoint, safe reference, and root. |
| RESEARCH | Admission join + safe matrix reproduction | 01-02 | COVERED | Exact authorities and canonical runtime path. |
| RESEARCH | Calibration + frozen contract | 03-04 | COVERED | Profile-neutral calibration precedes freeze. |
| RESEARCH | Classifiers + non-materialization | 05 | COVERED | Pure fixtures and adversarial monitor. |
| RESEARCH | Custody + aggregate root | 06-07 | COVERED | Separate real authority and bounded projection. |
| CONTEXT | D-01..D-06 | 01-07 | COVERED | Immutable/fail-closed evidence, canonical boundaries, privacy, charging, and scope fences. |
| CONTEXT | D-07..D-10 | 01-02 | COVERED | Exact predecessor and regression reproduction. |
| CONTEXT | D-11..D-18 | 03-04 | COVERED | Estimands, structural vectors, calibration, gates, and reports. |
| CONTEXT | D-19..D-22 | 05-07 | COVERED | Custody, contamination, protocol-only profiles, and non-materialization. |

Deferred ideas from CONTEXT.md are excluded: planner/factory/league/freeze/materialization/retraining/opening/decision/certification/release work remains in Phases 263–270, and later rules experiments are not planned here.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved custody handoff → repository | Only opaque approved reference data may enter Git. |
| Prerequisite artifacts → aggregate root | Every identity must be exact, complete, uncontaminated, and reproducible. |
| Phase 262 root → downstream phases | Later work must bind this root without mutating or bypassing it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-25 | Spoofing | custody public reference | high | mitigate | Verify approved opaque authorization/store identities and reject fixtures/unknown controls. |
| T-262-26 | Tampering | aggregate root | high | mitigate | Exact path/digest/source joins, deterministic rendering, and mutation tests for every prerequisite. |
| T-262-27 | Information Disclosure | CLI/reference/root | high | mitigate | Closed schemas, safe stdout, privacy scan, and forbidden-key mutations. |
| T-262-28 | Repudiation | phase closure | high | mitigate | Complete output inventory, source bindings, immutable summaries, and exact check command. |
| T-262-29 | Elevation of Privilege | downstream consumption | high | mitigate | Root binds containment proof and cannot exist on unauthorized/contaminated/partial input. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts --maxWorkers=1 &amp;&amp; pnpm v1.38:foundation-contract:check &amp;&amp; pnpm typecheck &amp;&amp; pnpm turbo test --concurrency=1</automated>
Confirm exact artifact regeneration and absence of forbidden executable formation/candidate material.
</verification>

<success_criteria>
- On the authorized route, one non-circular content-addressed root joins every Phase 262 authority and the exact output inventory excluding only its own artifact.
- Real custody is represented by approved opaque controls, never simulated.
- All 16 requirement IDs and D-01 through D-22 are covered with no deferred feature leakage.
- Downstream work can bind the root, but cannot bypass the canonical kernel/runtime, privacy, accounting, or pre-formation fences.
- On the no-authority route, the only valid completion is a stopped summary/artifact with no custody reference, no containment receipt, no aggregate root, no Phase 262 success claim, and an explicit Phase 263 authority block.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-07-SUMMARY.md` when done.
</output>
