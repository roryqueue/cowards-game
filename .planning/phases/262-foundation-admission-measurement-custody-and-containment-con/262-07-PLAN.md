---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "07"
type: execute
wave: 7
depends_on: ["262-06"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.ts
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - package.json
  - .planning/artifacts/v1.38-custody-public-reference.json
  - .planning/artifacts/v1.38-foundation-contract-root.json
autonomous: true
requirements: [SEAL-01]
must_haves:
  truths:
    - "The repository receives only an approved opaque custody reference and never secrets, preimages, raw access/query detail, private store paths, or evaluator internals per D-19/D-20."
    - "One aggregate content-addressed root joins exact admission, reproduction, pre-search contract, custody reference, containment, and generator/checker identities; no mutable latest exists per D-01."
    - "Any missing, stale, mismatched, contaminated, incomplete, non-reproducible, or unauthorized prerequisite prevents the aggregate root per D-02."
  artifacts:
    - path: "scripts/evaluate-v1-38-foundation-contract.ts"
      provides: "One write/check orchestration command and aggregate-root builder"
      exports: ["generateV138FoundationContractRoot", "checkV138FoundationContract"]
    - path: ".planning/artifacts/v1.38-custody-public-reference.json"
      provides: "Approved bounded custody commitment/reference"
    - path: ".planning/artifacts/v1.38-foundation-contract-root.json"
      provides: "Immutable Phase 262 aggregate root consumed by downstream phases"
  key_links:
    - from: "scripts/evaluate-v1-38-foundation-contract.ts"
      to: ".planning/artifacts/v1.38-pre-formation-containment.json"
      via: "exact prerequisite root join"
      pattern: "containment"
    - from: ".planning/artifacts/v1.38-foundation-contract-root.json"
      to: ".planning/artifacts/v1.38-custody-public-reference.json"
      via: "approved opaque custody root only"
      pattern: "custody"
---

<objective>
Bind the approved custody reference and every passed Phase 262 gate into one reproducible aggregate root, implementing D-01 through D-06 and D-19 through D-22.

Purpose: Give Phases 263–270 one immutable pre-search authority while preserving privacy, containment, and fail-closed semantics.
Output: The final write/check command, package scripts, safe custody reference, and aggregate foundation root.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-06-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project the authorized custody reference and reject unauthorized inputs</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-06-SUMMARY.md</file>
    <file>scripts/lib/v1-38-custody.ts</file>
    <file>packages/spec/src/public-projection-privacy.ts</file>
    <file>.planning/artifacts/v1.38-pre-search-contract.json</file>
  </read_first>
  <files>scripts/evaluate-v1-38-foundation-contract.test.ts, .planning/artifacts/v1.38-custody-public-reference.json</files>
  <behavior>
    - Only an approved existing-controls handoff can create the reference; stop/no-authority produces no root.
    - The reference exposes commitment profile/digest, opaque role/store/key/command/policy IDs, encrypted-object digest/length, lineage root, and safe-projection bounds only.
    - Secret, salt, preimage, private path, raw access/query, hidden identity, evaluator state, and decryption fields are rejected.
  </behavior>
  <action>Consume the approved opaque handoff from Plan 06 and use `projectV138SafeCustodyReceipt` to render the exact repository-safe reference. Verify authorization receipt identity and store/control-plane posture without reading secret material into Git. If the checkpoint selected stop or any authorization/store field is absent, stale, mismatched, or unapproved, emit `stop_no_custody_authority` and do not create the aggregate root per D-02/D-19/D-20.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t custody --maxWorkers=1</automated>
  </verify>
  <acceptance_criteria>The reference is bounded and privacy-safe, and every forbidden or unauthorized projection mutation blocks it.</acceptance_criteria>
  <done>The real custody contract is represented honestly by opaque approved authority without secret disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the one-command exact aggregate root</name>
  <read_first>
    <file>scripts/generate-v1-37-strategy-foundation-handoff.ts</file>
    <file>.planning/artifacts/v1.38-foundation-admission.json</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/artifacts/v1.38-pre-search-contract.json</file>
    <file>.planning/artifacts/v1.38-pre-formation-containment.json</file>
    <file>.planning/artifacts/v1.38-custody-public-reference.json</file>
  </read_first>
  <files>scripts/evaluate-v1-38-foundation-contract.ts, scripts/evaluate-v1-38-foundation-contract.test.ts, package.json, .planning/artifacts/v1.38-foundation-contract-root.json</files>
  <behavior>
    - The root joins exactly the five prerequisite artifact roots, complete output inventory, and generator/checker source identities.
    - Write/check modes resolve the repository from import.meta.url, reject implicit discovery/mutable aliases, and emit only a bounded safe stdout summary.
    - Any prerequisite mutation, missing artifact, dirty identity, contamination, or forbidden inventory prevents root generation.
  </behavior>
  <action>Implement `generateV138FoundationContractRoot` and `checkV138FoundationContract` with strict `--write`/`--check` dispatch. Validate existing immutable prerequisite artifacts byte-for-byte rather than silently rewriting them, bind exact input paths/digests, source identities, tool/runtime/platform identity only where behavior varies, and produce the aggregate root only after every gate passes. Add `v1.38:foundation-contract:write` and `v1.38:foundation-contract:check` package scripts, then wire the check into the boundary-monitor chain only after deterministic tests are green. Per D-01/D-02, there is no mutable alias, partial root, repair, override, or contamination bypass.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts --maxWorkers=1 &amp;&amp; pnpm v1.38:foundation-contract:check</automated>
  </verify>
  <acceptance_criteria>Two fixed-input runs produce the same aggregate bytes/root; changing any prerequisite byte or adding a forbidden artifact makes check mode fail.</acceptance_criteria>
  <done>The immutable Phase 262 root is ready for exact downstream binding.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full phase verification gate</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VALIDATION.md</file>
    <file>.planning/artifacts/v1.38-foundation-contract-root.json</file>
    <file>package.json</file>
  </read_first>
  <files>scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <action>Run the complete Phase 262 suite, exact artifact check, admission command, containment monitor, workspace checks, and final serialized Turbo suite. Record focused latency separately from the long historical reproduction and full-suite integration gates. Verify the forbidden-artifact inventory and that only protocol-only profile records exist. Do not convert an environment, custody, matrix, or timing failure into an empirical result.</action>
  <verify>
    <automated>pnpm v1.38:foundation-contract:check &amp;&amp; pnpm turbo test --concurrency=1</automated>
  </verify>
  <acceptance_criteria>All focused and serialized checks pass, exact regeneration is clean, the forbidden inventory is empty, and the aggregate root resolves every required artifact.</acceptance_criteria>
  <done>Phase 262 satisfies its validation contract and can hand one immutable authority root to Phase 263.</done>
</task>

</tasks>

## Artifacts this phase produces

Complete Phase 262 created-symbol/path inventory:

- `scripts/evaluate-v1-38-foundation-contract.test.ts`
- `evaluateV138FoundationAdmission` in `scripts/lib/v1-38-foundation-admission.ts`
- `reproduceV138CurrentMatrix` in `scripts/lib/v1-38-current-matrix-reproduction.ts`
- `V138StudyContractSchema`, `calibrateV138StudyContract`, and `validateV138AccountingClosure` in `scripts/lib/v1-38-study-contract.ts`
- `evaluateV138FrozenGates`, `validateV138ReportState`, `lintV138Claim`, `canonicalizeV138OpeningCluster`, and `evaluateV138Classifiers` in `scripts/lib/v1-38-measurement.ts`
- `analyzeV138PreFormationContainment` in `scripts/lib/v1-38-containment.ts`
- `createV138CustodyCommitment`, `executeV138CustodyCommand`, and `projectV138SafeCustodyReceipt` in `scripts/lib/v1-38-custody.ts`
- `generateV138FoundationContractRoot` and `checkV138FoundationContract` in `scripts/evaluate-v1-38-foundation-contract.ts`
- `.planning/artifacts/v1.38-foundation-admission.json`
- `.planning/artifacts/v1.38-current-matrix-reproduction.json`
- `.planning/artifacts/v1.38-pre-search-contract.json`
- `.planning/artifacts/v1.38-custody-public-reference.json`
- `.planning/artifacts/v1.38-pre-formation-containment.json`
- `.planning/artifacts/v1.38-foundation-contract-root.json`
- `v1.38:foundation-contract:write` and `v1.38:foundation-contract:check` in `package.json`

## Multi-Source Coverage Audit

| Source | ID/range | Plan(s) | Status | Notes |
|---|---|---|---|---|
| GOAL | Phase 262 goal | 01-07 | COVERED | Exact admission through immutable scientific/custody/containment root. |
| REQ | ADMIT-01, ADMIT-02, ADMIT-04 | 01 | COVERED | Exact predecessor join and typed stop. |
| REQ | ADMIT-03 | 02 | COVERED | Supervised 540-Match regression reproduction. |
| REQ | MEAS-01..MEAS-04 | 03 | COVERED | Study, structural opportunity, and two-ledger contract. |
| REQ | MEAS-05..MEAS-09 | 04 | COVERED | Calibrated gates, reports, and claims. |
| REQ | MEAS-10, DECI-02 | 05 | COVERED | Protocol-only profiles, classifiers, and containment. |
| REQ | SEAL-01 | 06-07 | COVERED | Synthetic custody, real authorization checkpoint, safe reference, and root. |
| RESEARCH | Admission join + safe matrix reproduction | 01-02 | COVERED | Exact authorities and canonical runtime path. |
| RESEARCH | Calibration + frozen contract | 03-04 | COVERED | Profile-neutral calibration precedes freeze. |
| RESEARCH | Classifiers + non-materialization | 05 | COVERED | Pure fixtures and adversarial monitor. |
| RESEARCH | Custody + aggregate root | 06-07 | COVERED | Separate real authority and bounded projection. |
| CONTEXT | D-01..D-06 | 01-07 | COVERED | Immutable/fail-closed evidence, canonical boundaries, privacy, charging, and scope fences. |
| CONTEXT | D-07..D-10 | 01-02 | COVERED | Exact predecessor and regression reproduction. |
| CONTEXT | D-11..D-18 | 03-04 | COVERED | Estimands, structural vectors, calibration, gates, and reports. |
| CONTEXT | D-19..D-22 | 05-07 | COVERED | Custody, contamination, protocol-only profiles, and non-materialization. |

Deferred ideas from CONTEXT.md are excluded: planner/factory/league/freeze/materialization/retraining/opening/decision/certification/release work remains in Phases 263–270, and later rules experiments are not planned here.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved custody handoff → repository | Only opaque approved reference data may enter Git. |
| Prerequisite artifacts → aggregate root | Every identity must be exact, complete, uncontaminated, and reproducible. |
| Phase 262 root → downstream phases | Later work must bind this root without mutating or bypassing it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-25 | Spoofing | custody public reference | high | mitigate | Verify approved opaque authorization/store identities and reject fixtures/unknown controls. |
| T-262-26 | Tampering | aggregate root | high | mitigate | Exact path/digest/source joins, deterministic rendering, and mutation tests for every prerequisite. |
| T-262-27 | Information Disclosure | CLI/reference/root | high | mitigate | Closed schemas, safe stdout, privacy scan, and forbidden-key mutations. |
| T-262-28 | Repudiation | phase closure | high | mitigate | Complete output inventory, source bindings, immutable summaries, and exact check command. |
| T-262-29 | Elevation of Privilege | downstream consumption | high | mitigate | Root binds containment proof and cannot exist on unauthorized/contaminated/partial input. |
</threat_model>

<verification>
Run `pnpm v1.38:foundation-contract:check`, the full focused Vitest entrypoint, containment/admission checks, and `pnpm turbo test --concurrency=1`. Confirm exact artifact regeneration and absence of forbidden executable formation/candidate material.
</verification>

<success_criteria>
- One content-addressed root joins every Phase 262 authority and output.
- Real custody is represented by approved opaque controls, never simulated.
- All 16 requirement IDs and D-01 through D-22 are covered with no deferred feature leakage.
- Downstream work can bind the root, but cannot bypass the canonical kernel/runtime, privacy, accounting, or pre-formation fences.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-07-SUMMARY.md` when done.
</output>
