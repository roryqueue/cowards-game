---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "201"
type: tdd
wave: 168
depends_on: [262-200]
files_modified:
  - scripts/lib/v1-38-lean-container-match-session.ts
  - scripts/lib/v1-38-lean-container-match-session.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json
  - .planning/debug/phase-262-probe-failed.md
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-201-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "The broker no longer rejects a valid completion solely because natural Worker exit is observed before its correlated MessagePort completion receipt."
    - "Success still requires exactly one valid request-bound completion receipt, exactly one port close, exactly one natural Worker exit with code 0, no Worker error, no duplicate/missing/inconsistent event, and completion inside the unchanged overall deadline."
    - "No acknowledgement handshake, grace extension, deadline reset, retry, fallback, worker reuse, relaxed correlation, or weakened frame/output/resource validation is introduced."
    - "Legacy and v1.17 Advanced rapid-exit stress each pass at least 50 consecutive iterations, while invariant tests reject every invalid count, identity, error, exit-code, timeout, missing-event, duplicate-event, and inconsistent-outcome case."
    - "Attempt 8 diagnostic-v5 and its incomplete-cleanup terminal remain immutable; the v14 writer is retired before source changes and fresh v15 custody/tooling bind the separately committed repair plus all prior history."
    - "Attempt 9 of 10 runs exactly one diagnostic-v6 over the unchanged public fixture order through the real persistent broker, with zero preflights, zero Matches, no effects, exact cleanup, privacy-safe evidence, and 36 locks."
  artifacts:
    - path: scripts/lib/v1-38-lean-container-match-session.ts
      provides: "Minimal order-insensitive three-event completion reconciliation"
    - path: scripts/lib/v1-38-lean-container-match-session.test.ts
      provides: "Legacy/v1.17 rapid-exit stress and strict lifecycle-invariant regressions"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Fresh v15 custody/toolchain, retired v14 writer, and diagnostic-v6 writer/checker"
    - path: .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json
      provides: "Single immutable attempt-9 public-fixture confirmation"
  key_links:
    - from: scripts/lib/v1-38-lean-container-match-session.test.ts
      to: scripts/lib/v1-38-lean-container-match-session.ts
      via: "At least 50 legacy and 50 v1.17 Advanced rapid-exit executions prove event-order independence without relaxing the final three-event invariant"
      pattern: "completion lifecycle"
    - from: .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json
      to: scripts/check-v1-38-lean-admission.ts
      via: "Exact attempt-8 root is immutable v15 history and the spent writer is non-executable"
      pattern: "diagnosticV5Root"
    - from: scripts/lib/v1-38-lean-container-match-session.ts
      to: .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json
      via: "Fresh custody binds the separately committed minimal repair before the sole exact-boundary confirmation"
      pattern: "exitCount"
---

<objective>
Remove only the invalid exit-before-receipt ordering rejection, prove the full completion invariant under rapid exits, and consume attempt 9 with one actual-fixture confirmation diagnostic.

Purpose: Accept valid Node event delivery permutations without weakening correlation, completion, natural-exit, deadline, containment, or cleanup requirements.
Output: TDD minimal broker repair, fresh v15 custody/toolchain, one diagnostic-v6 artifact, privacy-safe history, and summary; zero preflights and zero Matches.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/phase-262-probe-failed.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-EXECUTION-CLOSURE-RESEARCH.md
@.planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-200-SUMMARY.md
@scripts/lib/v1-38-lean-container-match-session.ts
@scripts/lib/v1-38-lean-container-match-session.test.ts
@scripts/check-v1-38-lean-admission.ts
@scripts/check-v1-38-lean-admission.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Accept valid completion events in any observed order</name>
  <files>scripts/lib/v1-38-lean-container-match-session.ts, scripts/lib/v1-38-lean-container-match-session.test.ts, scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/debug/phase-262-probe-failed.md</files>
  <behavior>
    - "RED reproduces the valid order exit(code 0) before one correlated completion receipt and one port close, and expects the receipt value to be accepted only after all three observations arrive within the original deadline."
    - "RED runs the real Advanced source selectActivations rapid-exit path at least 50 consecutive times through the legacy harness and at least 50 through v1.17, with every execution accepted and no exit-73 broker failure."
    - "RED accepts all valid permutations of exactly one correct receipt, exactly one close, and exactly one code-0 exit; arrival order alone never decides validity."
    - "RED rejects wrong requestId/kind/shape, Worker error, nonzero or null exit, duplicate receipt/close/exit, missing receipt/close/exit, conflicting receipt, post-terminal surplus, and any permutation that does not complete before the unchanged overall deadline."
    - "RED proves the wait budget is derived once from the existing deadline and is neither reset nor extended after early exit or any later event."
    - "RED proves no ACK/acknowledgement message or handshake, new timer/grace, retry, Worker reuse, fallback, relaxed serializer/frame/output cap, environment capability, or resource change appears."
    - "RED binds the read-only reproduction: Advanced selectActivations old source exit 73 with receipt=1, close=1, exit=1, exitCode=0, exit-before-receipt observed; removing only the ordering rejection passed 50/50 in memory; cleanup failure was secondary to broker poison."
    - "RED retires the spent diagnostic-v5 writer/CLI before runtime changes and authenticates its root sha256:3c2c3954c5a4aaedf3ad06b30c264aa9eec0849892579c1f8569c266a9abce07, Plan200 commit 975a5ed88ad336da63f77b49d56894e2e4927107, and summary root sha256:5080a1ab6b89914ba35233f644d75b063006f9982765b6ac491e2ca0e84cc39a without allowing another attempt-8 session."
    - "RED covers complete v15 diagnostic/preflight/authorization/review/live/post-run/adjudication/eligibility/final-tracking schemas, writers, checkers, selectors, mutation rejection, collision freedom, and exact history binding; no v15 effect is created source-only."
  </behavior>
  <action>Record the already-completed read-only reproduction in debug history using only the bounded public fixture/method, event counts, exit code, order flag, exit-73 class, 50/50 in-memory control, secondary-cleanup conclusion, zero preflight/Match/effect accounting, and no raw source/input/output/memory/diagnostic. Write RED tests first. In `supervise`, remove only the `exitBeforeReceipt` state, assignments, and final order rejection. Continue collecting events after an early code-0 exit until exactly one valid request-bound completion receipt, one port close, and one natural exit have all arrived or the single pre-existing deadline expires. Keep every existing failure and duplicate check, request correlation, receipt shape, output/frame/schema cap, fresh-per-request Worker, serialized broker correlation, poison behavior, Worker resource limit, empty environment, no fallback, startup/method/cancellation/overall timing, and container control unchanged. Do not add an ACK or any worker/port acknowledgement, new wait, deadline reset, grace, retry, fallback, or reuse. Add deterministic event-permutation/invariant tests and at least 50 consecutive real Advanced-source rapid-exit executions for each legacy and v1.17 harness. Before touching shared runtime code, permanently retire diagnostic-v5's write function and CLI selector so it fails before session creation and cannot rewrite attempt 8; preserve diagnostic-v4 retirement too. Then add fresh v15 source custody and the complete versioned toolchain needed by Plans202/175/176: diagnostic-v6; preflight-v12; authorization/review-v15; reviewed-ready, invocation/terminal/post-run-v15; adjudication/eligibility-v15; and final-tracking-v15. Bind exact Plan200 terminal/diagnostic-v5 plus all earlier immutable roots, attempts 1-8, one remaining corrective 24-Match opportunity, unchanged rules/formation/privacy/resources, and 36 locks. Do not invoke Docker, diagnostic, preflight, or Match in this task. Commit source/tests separately before Task2.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36</automated>
  </verify>
  <done>The separately committed broker accepts valid completion/close/code-0-exit permutations, rejects every invalid lifecycle, passes at least 50 legacy and 50 v1.17 Advanced rapid-exit iterations, and exposes a complete fresh zero-effect v15 toolchain.</done>
</task>

<task type="auto">
  <name>Task 2: Consume attempt 9 with exactly one diagnostic-v6 confirmation</name>
  <files>.planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json, .planning/debug/phase-262-probe-failed.md, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-201-SUMMARY.md</files>
  <action>After Task1's separate source commit, run fresh v15 source-only custody and invoke diagnostic-v6 exactly once from the main orchestrator. Exercise the exact digest-pinned image, controls, public fixture/input builders, method requests, warm/sample counts, and deterministic starter-then-advanced/selectActivations-then-soldierBrain order through the real persistent broker. Preserve the closed diagnostic projection: separately committed source/tree/closure and history roots, bounded first fixture/method/ordinal or lifecycle stage, coarse allowlisted result/session class, exact attempted/successful/timing aggregates, cleanup, attemptOrdinal 9, attemptLimit 10, attemptsRemaining 1, preflightInvocations 0, matchInvocations 0, and false authority. Retain no Strategy source/input/output/memory/objective, raw response/sample, stdout/stderr, error/stack, container identity, or host diagnostic. Always clean Workers, ports, stream, and owned container and verify absence. Commit and validate diagnostic-v6, then append only its privacy-safe bounded result and accounting to debug history and summary. On exact pass, Plan202 becomes eligible; on any failure/ambiguity/cleanup defect, Plan202 remains ineligible and one bounded attempt remains for a new human-approved plan. Do not invoke either preflight entry point, construct/invoke a Match, create any v15 operational effect, or modify prior history.</action>
  <verify>
    <automated>node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-actual-fixture-stage-diagnostic-v6 &amp;&amp; test "$(jq -r '.attemptOrdinal' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json)" = 9 &amp;&amp; test "$(jq -r '.attemptsRemaining' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json)" = 1 &amp;&amp; test "$(jq -r '.preflightInvocations' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json)" = 0 &amp;&amp; test "$(jq -r '.matchInvocations' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v6.json)" = 0 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated>
  </verify>
  <done>One immutable attempt-9 diagnostic-v6 truthfully confirms or denies the minimal repair with zero preflights, zero Matches, no effects, exact cleanup/accounting, and one attempt remaining.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Repair the proven broker event-order defect without weakening lifecycle integrity | 262-201 | COVERED | One-line semantic repair plus exhaustive proof.
REQ | ADMIT-01..ADMIT-04 | Exact source/runtime identity and fail-closed admission | 262-201 | COVERED | v15 custody; diagnostic grants no authority.
REQ | MEAS-01..MEAS-10 | Preserve schedule, limits, accounting, privacy, cleanup, and claims | 262-201 | COVERED | Exact bounds and closed diagnostic.
REQ | SEAL-01 | Preserve holdout and formation absence | 262-201 | COVERED | No Match/candidate/formation/holdout action.
REQ | DECI-02 | Preserve hostile container and fresh Worker boundary | 262-201 | COVERED | No boundary or resource relaxation.
RESEARCH | — | Require correlated completion, port close, and natural exit | 262-201 | COVERED | Order-independent conjunction.
CONTEXT | D-01..D-22 | Determinism, immutability, privacy, accounting, fail-closed execution | 262-201 | COVERED | Prior roots and strict failures preserved.
CONTEXT | D-34L | Preserve frozen current-rules fixture gate | 262-201 | COVERED | Confirmation only; no gate execution.
CONTEXT | D-34L.1 | No Match before passing final preflight/review | 262-201 | COVERED | Attempt 9 has zero preflight/Match.
OPERATOR | attempt 9/10 | Minimal ordering repair plus one confirmation | 262-201 | COVERED | One diagnostic-v6 only.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Worker events -> broker result | Three authenticated terminal observations may arrive in any order but all remain mandatory. |
| prior diagnostic -> repaired source | Attempt-8 root and spent writer retirement prevent replay or reinterpretation. |
| committed repair -> diagnostic-v6 | Fresh v15 custody binds exact source and history before the sole run. |
| diagnostic-v6 -> Plan202 | Only exact pass makes final preflight eligible; diagnostic grants no authority itself. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-201-01 | Spoofing | completion receipt | critical | mitigate | Exact request-bound shape/correlation and exactly-one count remain mandatory. |
| T-262-201-02 | Tampering | lifecycle reconciliation | critical | mitigate | Remove only order rejection; retain close/exit/code/error/duplicate/deadline invariants. |
| T-262-201-03 | Elevation of Privilege | proposed handshake | critical | mitigate | Tests forbid ACK, extra grace, retry, fallback, reuse, and relaxed limits. |
| T-262-201-04 | Repudiation | attempt history | high | mitigate | Retire diagnostic-v5 writer and bind immutable attempt-8 root before attempt 9. |
| T-262-201-05 | Information Disclosure | diagnostic-v6 | high | mitigate | Closed aggregate projection excludes all Strategy and raw runtime data. |
| T-262-201-06 | Denial of Service | early-exit wait | high | mitigate | Single unchanged overall deadline; no reset or extension after exit. |
| T-262-201-SC | Tampering | package supply chain | low | accept | No package installation occurs. |
</threat_model>

<verification>Legacy and v1.17 rapid-exit stress pass at least 50 iterations each; invalid lifecycle matrix remains fail-closed; v15 source custody binds the separate repair; diagnostic-v6 validates attempt 9 with zero preflights/Matches/effects, privacy-safe aggregates, cleanup, history, and 36 locks.</verification>

<success_criteria>
- Two tasks; source/tests commit precedes the sole diagnostic.
- Only exit-before-receipt ordering rejection is removed.
- Exactly one valid receipt, close, and code-0 exit remain mandatory within one unchanged deadline.
- No ACK, retry, grace extension, fallback, reuse, limit change, or scope expansion.
- Attempt 9 runs diagnostic-v6 once with zero preflights and zero Matches.
- Exact pass alone enables Plan202; otherwise one attempt remains and human direction is required.
</success_criteria>

<output>Create `262-201-SUMMARY.md`; dispatch Plan202 only on exact diagnostic-v6 pass, otherwise stop with one bounded attempt remaining.</output>
