---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "191"
type: tdd
wave: 160
depends_on: [262-190]
files_modified:
  - scripts/lib/v1-38-lean-container-match-session.ts
  - scripts/lib/v1-38-lean-container-match-session.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-191-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "Per D-34L.1, every Strategy method request executes in a fresh stateless Worker inside the existing digest-pinned container broker, while the broker and its Docker exec stream remain Match-scoped."
    - "The broker never launches a per-method Node child process, never executes Strategy source directly, never retains a loaded Strategy global between requests, and never falls back to an unconfined adapter."
    - "Strict request ordering, request-ID correlation, canonical framing, byte/schema limits, startup and method deadlines, bounded termination, poison-and-remove behavior, and both legacy and v1.17 result envelopes remain fail closed."
    - "The Docker boundary, image, network/read-only/tmpfs/memory/CPU/pids/capability controls, method ceilings, 45-second cell deadline, 900-second outer deadline, cleanup, privacy, current rules, and formation absence remain unchanged."
    - "A separately committed source-only v9 custody path binds the repair and fresh preflight-v8, authorization-v9, review-v9, and effect-v9 destinations without invoking Docker, preflight, or Match."
  artifacts:
    - path: scripts/lib/v1-38-lean-container-match-session.ts
      provides: "Persistent container broker with one fresh bounded guest Worker per method"
    - path: scripts/lib/v1-38-lean-container-match-session.test.ts
      provides: "Isolation, framing, timeout, cleanup, and no-child-process regression coverage"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Fresh v9 custody, schema, writer, and selector path"
  key_links:
    - from: scripts/lib/v1-38-lean-container-match-session.ts
      to: packages/runtime-js/src/worker-bridge.ts
      via: "Fresh-isolate and bounded synchronous host-controller pattern reused inside the container broker"
      pattern: "Worker"
    - from: scripts/lib/v1-38-lean-container-match-session.ts
      to: packages/runtime-js/src/subprocess-harness.ts
      via: "Exact legacy and v1.17 protocol/result semantics"
      pattern: "SUBPROCESS_HARNESS"
    - from: scripts/check-v1-38-lean-admission.ts
      to: .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v8.json
      via: "Exact separately committed Plan191 source identity"
      pattern: "sourceCommit"
---

<objective>
Replace the diagnosed per-method guest-process bottleneck with a fresh bounded guest Worker per request inside the existing persistent container broker, then bind the repair to a fresh source-only v9 custody path.

Purpose: Make the frozen 45-second-per-cell and 15-minute total feasibility projection realistic without weakening hostile-code containment, stateless request semantics, protocol validation, cleanup, privacy, gameplay, or formation boundaries.
Output: TDD-protected broker repair, fresh v9 source custody/checkers, and a source summary; no Docker, preflight, authorization, review, marker, Match, tracking, archive, or tag effect.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/phase-262-probe-failed.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-ADMISSION-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-EXECUTION-CLOSURE-RESEARCH.md
@.planning/artifacts/v1.38-lean-runner-direct-container-preflight-v7.json
@.planning/artifacts/v1.38-lean-runner-direct-validity-review-v8.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-190-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-190-SUMMARY.md
@packages/runtime-js/src/worker-bridge.ts
@packages/runtime-js/src/worker-harness.ts
@packages/runtime-js/src/subprocess-harness.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute each broker request in a fresh bounded guest Worker</name>
  <files>scripts/lib/v1-38-lean-container-match-session.ts, scripts/lib/v1-38-lean-container-match-session.test.ts</files>
  <behavior>
    - "RED proves the Match-scoped broker performs no per-method child-process launch and creates exactly one fresh guest Worker for every accepted method request."
    - "RED proves source-defined globals, module state, prototype mutations, and StrategyMemory cannot cross request boundaries, including two requests for the same fixture and alternating fixtures."
    - "RED proves both legacy and v1.17 requests retain their exact existing success, player-violation, system-failure, runtime-accounting, and byte-capped envelopes."
    - "RED proves forbidden filesystem, network, process, dynamic-code, and nested-worker capabilities remain unavailable to Strategy source inside the Docker boundary."
    - "RED proves wrong, duplicate, missing, out-of-order, malformed, oversized, or surplus-key request/response frames poison the session and remove the owned container."
    - "RED proves startup timeout, method timeout, worker crash, ambiguous completion, output-cap breach, and termination-grace breach terminate the fresh Worker before any response is accepted, poison the stream, and complete owned-container cleanup without fallback."
    - "RED proves requests are serialized and the next response cannot be emitted until the current Worker has terminated and its ports are closed."
  </behavior>
  <action>Write the failing tests first, then replace only the per-request execution mechanism inside `BROKER_SOURCE`. Keep the existing Match-scoped container and long-lived Docker exec stream, but have the broker create a fresh `node:worker_threads` Worker for each validated request. Adapt the existing worker-bridge, worker-harness, and subprocess-harness controller patterns so each Worker receives only one exact request through a private channel, uses `env: {}`, `execArgv: []`, and the existing bounded `resourceLimits`, signals startup and completion through a SharedArrayBuffer/MessageChannel or an equivalently bounded protocol, and is terminated before the broker writes the correlated response. Serialize the broker queue; increment the expected request ID only after the request frame validates; reject any ambiguous state and exit or poison so the host removes the owned container. Preserve the exact legacy and v1.17 ABI envelopes, response signing boundary, runtime observations, byte ceilings, method/startup/cancellation timeouts, deterministic inputs, and canonical failure classifications. The Worker is defense-in-depth inside the existing Docker hostile-code boundary and must not be represented as a standalone security boundary. Do not load or execute Strategy source in the broker global scope, retain compiled/user globals, launch another process per method, relax any container control or deadline, add a fallback adapter, invoke Docker, run a preflight, or run a Match.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false</automated>
  </verify>
  <done>The persistent container broker uses exactly one fresh terminated Worker per accepted request, satisfies all isolation/protocol/deadline/cleanup regressions, and has no per-method process or fallback path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the separately committed worker repair to fresh v9 custody</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-191-SUMMARY.md</files>
  <behavior>
    - "Fresh canonical paths are preflight-v8, authorization-v9, validity-review-v9, invocation-v9, terminal-v9, adjudication-v9, and phase-262 eligibility-v9, with no alias to prior destinations."
    - "The source-only checker binds the separately committed runnable Plan191 commit/tree/executable closure, worker-broker source and tests, immutable Plan190 roots and diagnosis, exactly 36 locks, and absence of all fresh operational destinations."
    - "Structural checks reject per-method child-process launch, persistent loaded Strategy state, broker-direct Strategy execution, absent Worker termination, missing resource limits, omitted request correlation, and any runtime fallback."
    - "Fresh renderers, validators, writers, and selectors preserve exactly one non-consuming preflight-v8, conditional authorization-v9, exactly seven validity categories, zero preflight Matches, pass-only Plan175 admission, and v9 downstream no-authority semantics."
    - "All prior preflights, reviews, diagnoses, authorizations, and effect paths remain immutable and ineligible."
  </behavior>
  <action>After Task1 source and tests are committed separately, extend `scripts/check-v1-38-lean-admission.ts` and its tests additively for the fresh v9 lineage. Include the broker/session source and every transitive runtime input in the executable closure. Add a source-only selector that authenticates the exact Plan191 commit/tree/blobs, proves current tracked-byte equality, preserves Plan190 and earlier roots, enforces the worker-broker structural invariants, counts exactly 36 successor locks, and requires every preflight-v8/auth-v9/review-v9/effect-v9 destination absent. Add fresh preflight/auth/review/live/adjudication/tracking schemas and selectors consumed by Plans192/175/176; do not alias old artifacts, reinterpret a prior refusal, broaden review beyond the seven D-34L.1 categories, or create any fresh operational artifact. Commit production and tests before the source-only closure check so the summary can name the exact runnable source commit. Do not invoke Docker, preflight, authorization, review, live, recovery, or Match selectors.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/check-v1-38-lean-admission.test.ts scripts/lib/v1-38-lean-container-match-session.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-guest-worker-source-only-v9 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated>
  </verify>
  <done>The separately committed Plan191 repair has a passing source-only v9 custody check, all fresh operational paths are absent, no runtime effect occurred, and the source summary records exact commits/tests/closures.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Make the reviewed lean feasibility path executable under frozen limits | 262-191 | COVERED | Removes measured per-call process overhead without changing the gate.
REQ | ADMIT-01..ADMIT-04 | Exact source/runtime custody and pass-only admission | 262-191 | COVERED | Fresh v9 source lineage.
REQ | MEAS-01..MEAS-10 | Preserve schedule, budgets, failure accounting, cleanup, privacy, and claims | 262-191 | COVERED | Runtime behavior and checker invariants.
REQ | SEAL-01 | Preserve sealed-holdout state | 262-191 | COVERED | No holdout or evidence effect.
REQ | DECI-02 | Hostile-code containment | 262-191 | COVERED | Fresh Worker remains inside Docker boundary.
RESEARCH | — | Frozen lean gate, one Match-scoped container, bounded supervised execution | 262-191 | COVERED | Only guest execution mechanism changes.
CONTEXT | D-01..D-22 | Determinism, hostile input, exact accounting, privacy, no product authority | 262-191 | COVERED | Explicitly preserved.
CONTEXT | D-34L | Exact 12-cell/two-pass contract | 262-191 | COVERED | Schedule and acceptance unchanged.
CONTEXT | D-34L.1 | Seven-category, no-recovery, pass-only closure | 262-191 | COVERED | Fresh v9 custody only.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| host -> Docker exec broker | Exact framed requests cross into the existing digest-pinned hostile-code container. |
| broker -> fresh guest Worker | One untrusted Strategy method runs in one resource-bounded Worker and leaves no reusable state. |
| guest Worker -> broker response | Only one correlated, byte-capped, schema-valid terminal envelope may cross back. |
| repaired source -> future preflight | Exact committed bytes must authenticate before any runtime measurement. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-191-01 | Elevation of Privilege | guest Worker | critical | mitigate | Keep Worker inside unchanged Docker controls and deny forbidden capabilities. |
| T-262-191-02 | Tampering | request/response protocol | critical | mitigate | Exact IDs, canonical frames, schema/byte checks, and poison on ambiguity. |
| T-262-191-03 | Information Disclosure | cross-request state | high | mitigate | Fresh Worker and closed ports for every request; no broker-loaded Strategy. |
| T-262-191-04 | Denial of Service | hung/crashed Worker | high | mitigate | Startup/method/grace limits, termination-before-response, poison-and-remove cleanup. |
| T-262-191-05 | Spoofing | source custody | high | mitigate | Exact commit/tree/blob/closure checks before preflight eligibility. |
| T-262-191-SC | Tampering | package supply chain | low | accept | No package or image installation occurs. |
</threat_model>

<verification>The focused broker and admission suites pass; TypeScript passes; source-only v9 custody authenticates separately committed bytes, no fresh artifacts exist, no Docker/preflight/Match ran, and all 36 locks remain.</verification>

<success_criteria>
- Two TDD tasks across five files.
- No per-method child process or persistent Strategy state remains in the broker.
- Every request uses a fresh bounded Worker inside the unchanged container boundary.
- Legacy and v1.17 envelopes, timeouts, ceilings, cleanup, privacy, gameplay, and formation absence remain unchanged.
- Fresh v9 custody passes with every operational destination absent.
- Zero Docker, preflight, authorization, review, marker, Match, recovery, tracking, archive, or tag effects.
</success_criteria>

<output>Create `262-191-SUMMARY.md`; dispatch `262-192-PLAN.md` only after the exact separately committed source-only v9 check passes.</output>
