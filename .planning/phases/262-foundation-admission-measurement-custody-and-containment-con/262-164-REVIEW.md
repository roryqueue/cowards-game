---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "164"
reviewed: 2026-09-01T20:11:02Z
depth: deep
source_commit: 2c2101c58dc5c8c9fa87e97c5adc79c2e0c0150a
source_tree: 32068718914d4ef03150eabb5ab8a3e3233d38a0
files_reviewed: 29
files_reviewed_list:
  - scripts/lib/v1-38-lean-runner-feasibility.ts
  - scripts/lib/v1-38-lean-runner-feasibility.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - apps/runtime-service/src
  - packages/engine/src
  - packages/persistence/src
  - packages/replay/src
  - packages/runtime-js/src
  - packages/runtime-python/src
  - packages/runtime-supervisor/src
  - packages/runtime-wasm-wasi/src
  - packages/spec/src
  - apps/runtime-service/package.json
  - packages/engine/package.json
  - packages/persistence/package.json
  - packages/replay/package.json
  - packages/runtime-js/package.json
  - packages/runtime-python/package.json
  - packages/runtime-supervisor/package.json
  - packages/runtime-wasm-wasi/package.json
  - packages/spec/package.json
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-163-SUMMARY.md
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-157-REVIEW.md
findings:
  critical: 1
  warning: 1
  info: 0
  total: 2
status: issues_found
---

# Phase 262 Plan 164: Code Review Report

**Reviewed:** 2026-09-01T20:11:02Z
**Depth:** deep
**Files Reviewed:** 29 recursively bound closure entries
**Status:** issues_found

## Summary

The exact committed source closure at `2c2101c58dc5c8c9fa87e97c5adc79c2e0c0150a` is not ready for Plan 158. The focused 62-test suite, TypeScript, legacy recovery structural check, source/tree/blob custody, immutable evidence roots, effect absence, and exact 36-lock inventory all passed. Deterministic runtime projection, current-formation containment, privacy-safe terminal evidence, charge identities, active-orphan authentication, process-group rejection, stale-process rejection, bounded TERM/KILL cleanup, and unchanged production deadlines were also rechecked.

However, the executable checker has no Plan 164 v2 manifest/review/readiness contract at all, and the recovery-only structural proof still misses a prohibited transitive schedule-construction path. The complete literal finding set is one critical and one warning. Readiness v2 must remain absent; this review admits no execution.

## Narrative Findings (AI reviewer)

## Critical Issues

### CR-164-01: Plan 164 v2 review and readiness artifacts have no executable trust path

**Classification:** BLOCKER

**File:** `scripts/check-v1-38-lean-admission.ts:19-27`

**Also:** `scripts/check-v1-38-lean-admission.ts:153-173`, `scripts/check-v1-38-lean-admission.ts:409-460`, `scripts/check-v1-38-lean-admission.ts:931-945`, `scripts/run-v1-38-lean-runner-feasibility.ts:641-658`

**Issue:** The only corrective path map still points manifest, source review, and readiness to the v1 artifacts. The only corrective review/readiness types and validators require v1 schemas, the CLI exposes only `--check-corrective-source-review` and `--check-corrective-readiness`, and the runner obtains authority through that v1 loader. There is no implementation of either command prescribed by Plan 164: `--check-corrective-source-review-v2` or `--check-corrective-reviewed-ready-v2`. Both commands fail with `LEAN_CHECK_SELECTOR_INVALID`. Moreover, the committed v2 manifest adds `reviewClosure`, `immutableRoots`, `freshCorrectiveEffects`, and `successorLockCount`, so the existing exact-key v1 manifest validator cannot accept it.

This is not merely missing convenience tooling. A hand-written readiness-v2 artifact would be ignored by the runner, while the runner would continue looking for the failed v1 review/readiness chain. Plan 158 therefore has no authenticated route from the exact v2 manifest and independent review to its one-wrapper authority.

**Fix:** Add distinct immutable v2 manifest/review/readiness paths and schemas; validate every v2 closure field and root against Git and historical evidence; add the two exact Plan 164 selectors; make the corrective invocation loader consume only readiness v2; add negative tests proving v1 artifacts cannot satisfy v2 readiness and that any v2 field/root drift fails. Rebind and independently review the resulting new source commit before creating readiness.

## Warnings

### WR-01: Recovery-only structural proof omits its transitive schedule-construction call chain

**Classification:** WARNING

**File:** `scripts/check-v1-38-lean-admission.ts:266-285`

**Also:** `scripts/run-v1-38-lean-runner-feasibility.ts:673-684`, `scripts/check-v1-38-lean-admission.ts:534-543`, `scripts/check-v1-38-lean-admission.ts:832-840`

**Issue:** The structural checker extracts only `runLeanCorrectiveRecoveryOnlyInjected` and the CLI selector block. The selector calls `terminalizeLeanCorrectiveInterruption`, which calls `createLeanInterruptedTerminal`, which calls `buildLeanSchedule()` and enumerates all 24 cells. None of those transitive functions is inspected. Consequently, the legacy structural check reports success even though Plan 163 explicitly requires the actual recovery call graph to have no schedule construction or enumeration path. It would also fail to detect a future launch primitive added inside either transitive checker function.

**Fix:** Parse and traverse the complete reachable call graph across the runner and checker modules, or redesign recovery terminalization around a dedicated interruption-tombstone schema that requires no schedule construction. Add a regression that fails on the current `terminalizeLeanCorrectiveInterruption -> createLeanInterruptedTerminal -> buildLeanSchedule` chain and on launch primitives introduced in any reachable callee.

## Plan 157 Finding Recheck

- `CR-01`: closed. Parent and child admission reject tracked-byte drift and non-enumerated residue.
- `CR-02`: closed. Both marker and terminal are rejected before marker creation, and terminal absence is rechecked before invocation.
- `CR-03`: closed for the expressly scoped authenticated active-orphan state. Ownership binds invocation root, PID, process group, selector, and token; recovery rejects stale or mismatched identity before signaling and proves bounded exit.
- `WR-01`: open. The executable selector is now inspected, but its transitive call graph is not.
- `WR-02`: closed. The expensive fixture test alone has a 30-second test timeout; the 15-minute outer, 45-second cell, and 2-second cleanup limits remain unchanged.

## Verification Evidence

- Exact prescribed serial Vitest command: 62/62 passed in 47.35 seconds.
- `pnpm exec tsc --noEmit --pretty false`: passed.
- Legacy `--check-corrective-recovery-only-structure`: passed, but is incomplete per `WR-01`.
- Both prescribed v2 selectors: failed read-only with `LEAN_CHECK_SELECTOR_INVALID`.
- Every source OID in manifest v2 matched source commit `2c2101c5`; tree, Plan 163 summary blob, and Plan 157 review blob matched.
- First invocation, first terminal, diagnostic custody, failed v1 manifest, and failed v1 review canonical roots matched the v2 manifest.
- Fresh corrective invocation, terminal, adjudication, eligibility, readiness v2, and child-ownership effects remained absent.
- No live or recovery-only selector was invoked and no Match/effect was created.
- The exact 36 successor lockfiles remained present and untouched.

## Authority

This review is non-authorizing. Plan 158, Phase 263, candidate search, formation materialization, holdout opening, public/product/production exposure, counted play, gameplay change, archive, release, and tag authority all remain false.

---

_Reviewed: 2026-09-01T20:11:02Z_
_Reviewer: fresh independent gsd-code-reviewer_
_Depth: deep_
