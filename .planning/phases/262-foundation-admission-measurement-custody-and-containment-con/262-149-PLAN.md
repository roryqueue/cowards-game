---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "149"
type: tdd
wave: 124
depends_on: [262-148]
files_modified:
  - scripts/lib/v1-38-lean-runner-feasibility.ts
  - scripts/lib/v1-38-lean-runner-feasibility.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/artifacts/v1.38-lean-runner-manifest.json
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-149-SUMMARY.md
autonomous: true
requirements: [ADMIT-03, ADMIT-04, MEAS-04, MEAS-08, MEAS-09, DECI-02]
must_haves:
  truths:
    - "The private runner pre-enumerates exactly 12 current-formation fixture cells and executes pass:a then pass:b serially with no per-cell retry."
    - "The reducer requires 24/24 supervised successes, complete cleanup, exact coverage, board realism, and four byte-identical normalized roots per corresponding cell."
    - "The manifest binds committed source and the exact selected tuple while every public/product/formation/downstream authority remains false."
  artifacts:
    - path: scripts/lib/v1-38-lean-runner-feasibility.ts
      provides: "Strict manifest, schedule, normalization, accounting, and reducer contracts"
    - path: scripts/run-v1-38-lean-runner-feasibility.ts
      provides: "15-minute serial supervised private gate adapter"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Read-only source/result/authority checker and manifest writer"
    - path: .planning/artifacts/v1.38-lean-runner-manifest.json
      provides: "Committed exact-source lean gate manifest"
  key_links:
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: apps/runtime-service/src/execute-match.ts
      via: "existing supervised prepared runtime-service request path"
      pattern: "executePreparedRuntimeServiceRequestV118"
    - from: scripts/lib/v1-38-lean-runner-feasibility.ts
      to: .planning/artifacts/v1.38-lean-runner-manifest.json
      via: "strict canonical schema and source-bound render"
      pattern: "lean_runner_feasibility_v1"
---

<objective>
Build the complete tested lean feasibility gate without running its live selector.

Purpose: Prove the source, normalization, runtime supervision, timeout, cleanup, privacy, and authority behavior before effects.
Output: TDD-tested pure contracts/reducer, bounded execution adapter, checker, committed source-bound manifest, and summary.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/artifacts/v1.38-lean-admission-contract-v1.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-ADMISSION-RESEARCH.md
@scripts/lib/v1-38-current-matrix-reproduction.ts
@apps/runtime-service/src/execute-match.ts
@packages/persistence/src/starter-strategies.ts
@packages/persistence/src/advanced-strategies.ts
@packages/engine/src/kernel/create-initial-state.ts
@packages/spec/src/arena-catalog-v1-37.ts
</context>

<interfaces>
Use existing `findStarterStrategy`/`buildStarterStrategyRevision` for `starter:aggro-chaser`, `findAdvancedStrategy`/`buildAdvancedStrategyRevision` for `advanced:vanguard-pressure`, the selected V1.18 prepared runtime-service path and its existing terminal/final-state/transition trace identities, and canonical SHA-256 helpers. Do not copy fixture source or rules. Define exported strict types for `LeanManifest`, `LeanCell`, `LeanExecutionRecord`, `LeanTerminal`, `LeanAuthority`, and injected execution/clock/abort dependencies so all non-live tests use fakes while the live CLI alone selects real dependencies.
</interfaces>

<feature>
  <name>lean_runner_feasibility_v1</name>
  <files>scripts/lib/v1-38-lean-runner-feasibility.ts, scripts/lib/v1-38-lean-runner-feasibility.test.ts, scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts, scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts</files>
  <behavior>
    RED: tests fail for the exact 3 arenas x 2 side assignments x 2 initiative parities schedule, pass identities, 24 charges, strict schemas, current edge-rank realism, and source/tuple custody.
    RED: tests fail for missing/duplicate/extra cells; every non-success class; unlaunched work; incomplete cleanup/orphan; deadline; any of outcome/final-state/ordered-transition-event/runtime-accounting mismatch; unstable-field over- or under-normalization; dirty/stale source; and forbidden formation/private/public/product authority.
    GREEN: the implementation passes every positive and negative case while the live selector remains uncalled and live terminal absent.
    REFACTOR: preserve strict exact-key parsing, immutable canonical ordering, injected effects, and separately committed source then manifest lineage.
  </behavior>
  <implementation>Per D-03 and D-34L, implement the exact research contract. Build canonical cell IDs from arena label, bottom/top fixture IDs, and initiative player; execute all pass:a cells before pass:b with concurrency one and no retry. Use the existing supervised runtime-service adapter and canonical engine, preserving canonical per-Match limits. Before the first Match, atomically create and fsync `.planning/artifacts/v1.38-lean-runner-invocation-v1.json` with exclusive no-follow creation, exact readiness/manifest/source roots, invocation ordinal one, and every authority false. Any existing marker blocks live entry forever; recovery may run postcheck/terminalization only. Start the 15-minute deadline at first fixture launch; on expiry abort the active supervised request, await bounded shutdown, record all remaining cells unlaunched, and still emit one safe terminal. Normalize only pass/invocation identity and wall-clock fields; compare outcome, final-state, ordered transition/event, and failure-free runtime-accounting roots separately. The committed terminal schema must expose safe aggregate counts, historical `0/540` with `reinterpreted:false`, `fixture_feasibility_only`, complete cleanup, determinism counts, formationMaterialized:false, and exhaustive authority booleans. Implement distinct non-effect selectors for source checks, manifest rendering/checking, synthetic execution, and terminal checking. The real live selector must require a committed zero-finding Plan150 readiness artifact and reject prior invocation-marker or terminal presence.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED then GREEN the pure schedule and reducer</name>
  <files>scripts/lib/v1-38-lean-runner-feasibility.ts, scripts/lib/v1-38-lean-runner-feasibility.test.ts</files>
  <behavior>
    - "12 unique canonical cells and two ordered passes yield exactly 24 charged identities."
    - "Only exact 24-success, cleanup-complete, four-root-equal evidence passes."
    - "Historical evidence and authority fields cannot be omitted or reinterpreted."
  </behavior>
  <action>Commit failing tests first, implement the strict pure contract/reducer second, and refactor only after green. Include exhaustive mutation tests for every schedule dimension, count, result class, cleanup flag, normalized root, claim, historical field, and authority flag.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-runner-feasibility.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>The pure layer deterministically distinguishes pass, non_pass, and invalid without gameplay-result thresholds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED then GREEN the bounded supervised adapter</name>
  <files>scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts</files>
  <behavior>
    - "Injected execution proves serial order, no retries, one outer deadline, and unconditional cleanup."
    - "The real adapter reaches existing runtime-service supervision and never evaluates Strategy source directly."
    - "Synthetic tests cannot select the live effect mode or create the canonical terminal."
    - "An exclusive fsynced invocation marker is durable before first effect and blocks a second entry after interruption."
  </behavior>
  <action>Write failing fake-clock/fake-runtime tests before the adapter. Add separate synthetic and real dependency constructors; only the closed live CLI selector may construct real runtime-service dependencies. Exercise success, each failure classification, hung active request, late cleanup, unlaunched suffix behavior, atomic marker creation, existing-marker refusal, crash-after-marker recovery, and postcheck-only terminalization. Add a real-path integration test using existing fixture builders and supervised test support without invoking all 24 live Matches.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-lean-runner-feasibility.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>The adapter is bounded, serial, supervised, cleanup-safe, and unreachable from synthetic selectors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED then GREEN custody checks and publish the manifest</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/artifacts/v1.38-lean-runner-manifest.json, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-149-SUMMARY.md</files>
  <behavior>
    - "Manifest source commit precedes the manifest commit and binds every executable source/test blob plus selected tuple and fixture hashes."
    - "Dirty tracked worktree, tuple/arena/formation drift, forbidden artifacts, or existing invocation marker/terminal fail before effects."
    - "All checks and synthetic modes leave live invocation count zero."
  </behavior>
  <action>Commit the complete source/tests before rendering the manifest to avoid commit self-reference. Then render a manifest binding that exact source commit, selected tuple, fixture revisions/source hashes, arena labels plus semantic geometry hashes, current formation root, schedule root, canonical runtime limits, deadline, normalization schema, claim class, and all-false authority. Commit manifest and summary as a descendant. Test exact Git blob custody, safe working-tree policy that permits only the pre-existing authenticated successor-lock residue, forbidden lab/holdout/candidate/public paths, and absence of the terminal. Do not expose or invoke the live selector.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-runner-feasibility.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-manifest</automated></verify>
  <done>A separately committed exact-source manifest is ready for independent review, with no live effect or downstream authority.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Exact-authority private pre-search feasibility | 262-149 | COVERED | Source only, no live run.
REQ | ADMIT-03 | Fixture-only deterministic gate | 262-149 | COVERED | Complete source and tests.
REQ | ADMIT-04 | Exact source/tuple drift stop | 262-149 | COVERED | Pre-effect checker.
REQ | MEAS-04 | 24/24 success and cleanup | 262-149 | COVERED | Reducer hard gate.
REQ | MEAS-08 | Advanced regression-only | 262-149 | COVERED | Fixture-only claim.
REQ | MEAS-09 | Separate historical/lean states | 262-149 | COVERED | Terminal schema.
REQ | DECI-02 | No product authorization | 262-149 | COVERED | Static/runtime negative gates.
RESEARCH | — | Exact schedule/resource/pass/failure contract | 262-149 | COVERED | All recommendations implemented.
RESEARCH | — | Validation Wave 0 gaps | 262-149 | COVERED | Pure, adapter, checker, integration tests.
CONTEXT | D-01..D-06 | Canonical/private/fail-closed execution | 262-149 | COVERED | Existing engine/runtime only.
CONTEXT | D-34L | Lean gate and bounded claim | 262-149 | COVERED | Exact implementation.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| committed manifest -> private runner | Untrusted/stale bytes must fail before hostile Strategy execution. |
| runner -> runtime service -> canonical kernel | Strategy effects cross supervision; transitions remain engine-owned. |
| raw execution -> committed terminal | Private observations must reduce to bounded safe evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-149-01 | Tampering | manifest and task inventory | critical | mitigate | Strict schemas, exact source/tuple hashes, canonical schedule root. |
| T-262-149-02 | Elevation of Privilege | Strategy execution | critical | mitigate | Existing supervised runtime-service boundary only. |
| T-262-149-03 | Information Disclosure | terminal publication | high | mitigate | Aggregate allowlist and forbidden-key tests. |
| T-262-149-04 | Denial of Service | hung Match | high | mitigate | 15-minute outer abort plus bounded cleanup barrier. |
| T-262-149-SC | Tampering | package supply chain | low | accept | Existing dependencies only; no install. |
</threat_model>

<verification>
All new tests, affected Phase262 tests, TypeScript, manifest custody, privacy/formation boundary scans, and `git diff --check` pass from committed bytes; no live terminal exists.
</verification>

<success_criteria>
- Source implements exactly the approved 24-Match gate through canonical supervision.
- Every failure and boundary has a focused negative test.
- Manifest binds a prior exact source commit without circular identity.
- No live execution occurs.
</success_criteria>

<output>Create `262-149-SUMMARY.md`; dispatch only `262-150-PLAN.md`.</output>
