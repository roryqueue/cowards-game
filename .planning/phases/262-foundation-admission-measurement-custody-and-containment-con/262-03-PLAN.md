---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "03"
type: execute
wave: 6
depends_on: ["262-10"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-study-contract.ts
  - .planning/artifacts/v1.38-calibration-freeze-policy.json
autonomous: true
requirements: [MEAS-01, MEAS-02, MEAS-03, MEAS-04]
must_haves:
  truths:
    - "The pre-search study contract fixes estimands, contrasts, complete cells, splits, opponents, seeds, structural opportunity dimensions, stopping, selection, and permitted claims before candidate output per D-11 through D-13."
    - "Accepted-cell evidence remains separate from the charged attempt/allocation ledger; missing, duplicate, conflicting, invalid, or system-failed work fails closed per D-05 and D-16."
    - "Contained calibration consumes only historical regression fixtures and synthetic profile-neutral inputs and records proof that candidate output was unavailable per D-14."
  artifacts:
    - path: "scripts/lib/v1-38-study-contract.ts"
      provides: "Closed schemas for study design, two-ledger accounting, calibration evidence, and content roots"
      exports: ["V138StudyContractSchema", "V138CalibrationFreezePolicySchema", "deriveV138CalibrationFreezePolicy", "calibrateV138StudyContract", "validateV138AccountingClosure"]
    - path: ".planning/artifacts/v1.38-calibration-freeze-policy.json"
      provides: "Preregistered content-addressed starting values, admissibility bounds, denominators, and parameter-specific selectors fixed before calibration"
  key_links:
    - from: "scripts/lib/v1-38-study-contract.ts"
      to: ".planning/artifacts/v1.38-current-matrix-reproduction.json"
      via: "regression-only calibration input root"
      pattern: "currentMatrixReproductionRoot"
---

<objective>
Define and calibrate the immutable scientific and structural accounting contract, implementing D-01, D-05, and D-11 through D-17 without inspecting candidate output.

Purpose: Freeze what is measured and what work is charged before search can influence the definitions.
Output: Exact study/accounting schemas, profile-neutral calibration receipts, and closure tests.

Execution gate: Do not execute any task unless `262-10-SUMMARY.md` records a successful outcome and `.planning/artifacts/v1.38-current-matrix-reproduction.json` passes its exact checker with `status: passed_exact` and 540 accepted cells. A stopped or missing receipt leaves this plan and Plans 262-04 through 262-07 blocked.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-10-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-02-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md
@.planning/milestone-proposals/v1.38-competitive-strategy-factory-and-adversarial-league/ACTIVATION-PROMPT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exact estimands, cells, selection, and claim boundaries</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-10-SUMMARY.md</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md</file>
    <file>scripts/generate-v1-37-strategy-foundation-handoff.ts</file>
    <file>packages/spec/src/canonical-json.ts</file>
    <file>packages/spec/src/canonical-identity-domains.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-study-contract.ts, scripts/evaluate-v1-38-foundation-contract.test.ts, .planning/artifacts/v1.38-calibration-freeze-policy.json</files>
  <behavior>
    - The primary estimand is separately adapted fixed-factory metagames; the three paired contrasts are explicit and fixed-policy transfer is secondary only.
    - Complete cell identity enumerates every split, opponent field, semantic arena, side, entrant-level initiative, seed block, replicate, runtime, and metric root.
    - Response, stopping, finalist, portfolio, robust-pure selection, and permitted claims are exact-keyed and immutable.
    - Every calibration starting value, denominator, admissibility bound, benchmark/fixture root, and pure parameter-specific selector is frozen and content-addressed before calibration executes.
  </behavior>
  <action>Before creating or changing any contract artifact, run the Plan 262-10 authoritative receipt checker and require its Summary to record a successful `passed_exact` 540-cell outcome; stop this plan without implementation if either condition is absent. Then add the `contract` tests first. Implement readonly exact-key schemas and canonical identity builders for D-11/D-12/D-17. Require equality of the declared and accepted cell-ID sets rather than count equality. Bind response admission, stopping, finalist cardinality/eligibility, portfolio selection, robust-pure selection, metric/fixture code roots, and an oracle-relative claim grammar. Before any calibration code runs, define `V138CalibrationFreezePolicySchema` and `deriveV138CalibrationFreezePolicy` with every activation-prompt starting value, exact denominator, fixed benchmark/fixture root, admissibility bound, and pure parameter-specific selector. Render `.planning/artifacts/v1.38-calibration-freeze-policy.json` canonically and bind its policy/code roots. Tests must mutate selector source, ordering, bounds, denominator, or input roots and prove a new policy root or rejection. No candidate, finalist, result, profile state, holdout preimage, or calibration observation may be an input to policy construction.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t contract --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The schema rejects missing/extra fields, incomplete cell inventories, mutable aliases, post-result values, and stronger-than-oracle-relative claims.</acceptance_criteria>
  <done>MEAS-01 and MEAS-03 have executable exact schemas and domain-separated roots, and the complete calibration selector policy is immutable before calibration evidence can exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement two-ledger structural opportunity accounting</name>
  <read_first>
    <file>apps/runtime-service/src/execute-match.ts</file>
    <file>scripts/lib/v1-37-integrated-proof-manifest.ts</file>
    <file>scripts/lib/v1-38-study-contract.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-study-contract.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - Candidate, response, search, teacher, distillation, Match, model, human, replay, cache, retry, hardware/runtime, and artifact-limit dimensions close independently.
    - Rejected, invalid, duplicate, failed, system-failed, retried, unfilled, and unused allocations remain charged.
    - No failed/system attempt can be converted into an accepted cell.
  </behavior>
  <action>Add property and mutation coverage under `accounting`, then implement `validateV138AccountingClosure`. Model the multi-resource opportunity vector per D-13 without fungible conversion between dimensions. Require immutable parent/child retry lineage, exact preallocation, explicit unfilled/unused dispositions, and a separate accepted-cell root. Reject missing attempt IDs, retry overwrite, conflict, imputation, or accepted system failure per D-05/D-16.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t accounting --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Synthetic task graphs close exactly, and every seeded omission, free retry, cross-dimension conversion, or system-failure promotion is detected.</acceptance_criteria>
  <done>MEAS-02 and MEAS-04 are enforced by independent charged-work and accepted-evidence ledgers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run contained profile-neutral calibration</name>
  <read_first>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/artifacts/v1.38-calibration-freeze-policy.json</file>
    <file>.planning/milestone-proposals/v1.38-competitive-strategy-factory-and-adversarial-league/ACTIVATION-PROMPT.md</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md</file>
  </read_first>
  <files>scripts/lib/v1-38-study-contract.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - Calibration measures structural work units, accounting closure, benchmark denominators, and retention sizing using only admitted regression/synthetic inputs.
    - Direct Strategy work is separated from provider, orchestration, and infrastructure overhead.
    - Every replacement proposal records old/new value, denominator, input/code roots, reason, and proof candidate output was unavailable.
    - Calibration records the preregistered freeze-policy root and cannot supply, replace, or reinterpret selector/admissibility logic.
  </behavior>
  <action>Implement `calibrateV138StudyContract` as a deterministic/profile-neutral calibration receipt builder that requires the exact `.planning/artifacts/v1.38-calibration-freeze-policy.json` root created by Task 1. Use only the admitted historical reproduction receipt, policy-declared fixed benchmark inputs, synthetic task graphs, and synthetic storage/classifier payloads per D-14/D-15. Record environment identities only where behavior varies, never candidate outcomes. Calibration reports observations and exact denominators under the already-frozen policy; it cannot create, modify, select, or reinterpret selectors or admissibility bounds. Reject a missing, changed, late-created, or mismatched policy root.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t 'contract|accounting|calibration' --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The calibration receipt is reproducible on fixed inputs, distinguishes overhead classes, and fails if any candidate/profile/holdout-result field is present.</acceptance_criteria>
  <done>The evidence needed to freeze MEAS-01 through MEAS-04 and later numeric gates exists without candidate inspection.</done>
</task>

</tasks>

## Artifacts this phase produces

- `V138StudyContractSchema`, `calibrateV138StudyContract`, and `validateV138AccountingClosure` in `scripts/lib/v1-38-study-contract.ts`
- `V138CalibrationFreezePolicySchema` and `deriveV138CalibrationFreezePolicy` in `scripts/lib/v1-38-study-contract.ts`
- `.planning/artifacts/v1.38-calibration-freeze-policy.json`
- Profile-neutral calibration and accounting evidence embedded in the later pre-search contract root

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Calibration inputs → frozen contract | Candidate-conditioned information must not influence definitions or budgets. |
| Attempt ledger → accepted evidence | Failed work must stay charged and excluded from gameplay cells. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-08 | Tampering | study contract | high | mitigate | Exact schemas, domain roots, candidate-unavailable proof, and immutable branch semantics. |
| T-262-09 | Repudiation | opportunity ledger | high | mitigate | Preallocated IDs, append-only outcomes, retry lineage, and exact closure checks. |
| T-262-10 | Elevation of Privilege | accepted-cell reducer | high | mitigate | Separate schemas/roots and a zero-tolerance promotion mutation suite. |
| T-262-11 | Information Disclosure | calibration receipt | medium | mitigate | Profile-neutral allowlist and rejection of candidate/holdout fields. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
Confirm all starting inputs remain explicit and every proposed replacement carries its denominator and no-candidate proof.
</verification>

<success_criteria>
- The estimand and complete-cell universe are immutable before search.
- Every structural resource dimension closes independently.
- Calibration is contained, profile-neutral, and cannot inspect candidate output.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-03-SUMMARY.md` when done.
</output>
