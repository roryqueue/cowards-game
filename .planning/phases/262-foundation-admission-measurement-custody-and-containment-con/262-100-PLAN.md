---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "100"
type: execute
wave: 82
depends_on: [262-99]
files_modified:
  - scripts/lib/v1-38-bounded-retry-v3-native-custody-v1.ts
  - scripts/run-v1-38-bounded-retry-envelope-v3.ts
  - scripts/run-v1-38-bounded-retry-envelope-v3.test.ts
autonomous: true
requirements: [ADMIT-03, ADMIT-04, MEAS-02, MEAS-04, MEAS-09, MEAS-10, SEAL-01]
must_haves:
  truths:
    - "Per D-01, D-02, and D-24R, every Plan-98/99 producer, test, checker, provisional JSON/REVIEW, summary, finding, root, invalidation, and closeout byte remains immutable protected failed history."
    - "Every fixed committed source path is authenticated as exactly one Git tree blob with mode 100644 or 100755, then read as the exact raw blob Buffer and compared to a no-follow working-file Buffer with Buffer.equals."
    - "Final-newline, no-final-newline, empty, binary, invalid-UTF-8, embedded-NUL, regular/executable mode, missing, malformed, duplicate, symlink, gitlink, tree, and byte/mode-drift fixtures fail or pass exactly according to raw Git custody."
    - "The strict successor review input is only the new Plan-101 v5 path with schemaVersion `v1.38-plan-262-101-git-object-byte-custody-rereview-v5`, protocol `git-object-byte-custody-v1`, portable domain `v1.38:plan-262-101:git-object-byte-custody:portable:v5`, result-root domain `v1.38:plan-262-101:git-object-byte-custody:root:v5`, review-root domain `v1.38:plan-262-101:git-object-byte-custody:review:v5`, and finding-root domain `v1.38:plan-262-101:git-object-byte-custody:finding:v5`; portable members remain cross-checkout invariants and the complete environment-bound local closure remains freshly equal before and after."
    - "Plan 100 is source/synthetic only: it publishes no review verdict, seal, envelope, live evidence, charge, accepted cell, lifecycle mutation, or downstream authority."
  artifacts:
    - path: scripts/lib/v1-38-bounded-retry-v3-native-custody-v1.ts
      provides: "Hardened isolated raw-Buffer Git helper and exact tree-entry/blob custody"
    - path: scripts/run-v1-38-bounded-retry-envelope-v3.ts
      provides: "Raw committed-byte consumer plus strict Plan-101 v5 review schema"
    - path: scripts/run-v1-38-bounded-retry-envelope-v3.test.ts
      provides: "Adversarial byte, mode, tree-entry, closure, history, and zero-publication tests"
  key_links:
    - from: scripts/lib/v1-38-bounded-retry-v3-native-custody-v1.ts
      to: scripts/run-v1-38-bounded-retry-envelope-v3.ts
      via: "ls-tree -z exact entry authentication followed by cat-file blob raw stdout Buffer"
      pattern: "cat-file"
    - from: scripts/run-v1-38-bounded-retry-envelope-v3.ts
      to: .planning/artifacts/v1.38-plan-262-101-bounded-retry-source-rereview-v5.json
      via: "one strict v5 schema/path and portable-to-full closure join"
      pattern: "plan26292Eligible"
  prohibitions:
    - "Do not edit, delete, amend, regenerate, or reinterpret any Plan-98/99 source, checker, test, provisional pair, REVIEW, SUMMARY, finding, root, invalidation, or closeout byte; Plan-99 plan26292Eligible:true is invalid historical data."
    - "Do not use git show for committed payload custody, decode committed payloads as text, trim output, normalize line endings, re-encode text into bytes, hash instead of final Buffer equality, or accept filters/textconv."
    - "Do not accept absent/duplicate/unexpected tree records, mode outside 100644/100755, symlink 120000, gitlink 160000, tree, malformed metadata, path mismatch, or a working non-regular/symlink path."
    - "Do not weaken the portable/full closure separation, frozen D-29R/D-31R policy, existing seal-v13/envelope-v3 destinations, runtime/kernel semantics, privacy boundary, or reduced-assurance claim."
    - "Do not publish Plan-101 review files, seal/envelope/live/capacity/reproduction/adjudication/lifecycle artifacts, access a secret, run live work, or grant Plan-92/Phase-263/downstream authority."
---

<objective>
Correct committed source custody to preserve exact Git blob bytes and regular-file modes while retaining every frozen policy, closure, history, and non-authority semantic per D-01 through D-18, D-19R, D-20R, D-21, D-22, and D-23R through D-32R.

Purpose: Remove the `GIT_SHOW_BYTES_TRIMMED` integrity defect additively without modifying the immutable Plan-98/99 branch or broadening the v3 retry route.
Output: Three corrected source/test files and a normal `262-100-SUMMARY.md`; no review verdict or evidence artifact.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-100-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-98-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-99-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-99-SUMMARY.md
@.planning/artifacts/v1.38-plan-262-99-bounded-retry-source-rereview-v4.json

<interfaces>
From `scripts/lib/v1-38-bounded-retry-v3-native-custody-v1.ts`:
- Keep `runV138RetryV3IsolatedGit(...): string` for trimmed textual metadata callers; introduce a byte-returning sibling that uses the identical authenticated `/usr/bin/git`, hardened args, clean environment, isolated temporary config, and 64 MiB bound with no encoding.
- Tree custody must parse one NUL-terminated `ls-tree -z` record and `cat-file blob <oid>` must return the raw uncompressed blob bytes.

From `scripts/run-v1-38-bounded-retry-envelope-v3.ts`:
- `deriveV138V3SealedInactiveEnvelope` is the actual consumer boundary. Its custody loop must compare raw committed blob bytes to `readNoFollow` bytes and authenticate exact modes before any derivation.
- `authenticateReviewedExecutionClosure` must accept only the future Plan-101 v5 path/schema and retain portable-member equality plus the unchanged complete local before/after root.

From immutable Plan 99:
- The provisional v4 JSON/REVIEW pair and its zero array/root are historical bytes only. `262-99-SUMMARY.md` plus STATE carry `GIT_SHOW_BYTES_TRIMMED`, Plan-92 ineligibility, 0/0, and total downstream denial.

Resolved Plan-101 v5 contract (exact literals):
- schemaVersion: `v1.38-plan-262-101-git-object-byte-custody-rereview-v5`
- protocol: `git-object-byte-custody-v1`
- portable reviewed-closure hash domain: `v1.38:plan-262-101:git-object-byte-custody:portable:v5`
- canonical result-root hash domain: `v1.38:plan-262-101:git-object-byte-custody:root:v5`
- Markdown review-root hash domain: `v1.38:plan-262-101:git-object-byte-custody:review:v5`
- ordered finding-root hash domain: `v1.38:plan-262-101:git-object-byte-custody:finding:v5`
- disposable committed checkout: `umask 077`, a `0700` temporary directory, and `git clone --no-local --no-checkout` into an isolated object store; detach at the exact Plan-100 source-completion commit, disable hooks/signing, commit only candidate JSON/REVIEW bytes with fixed non-secret identity/timestamps, run the actual consumer there, remove the clone on every exit, and verify canonical refs/destinations unchanged. Linked worktrees are forbidden.
</interfaces>
</context>

## Artifacts this phase produces

- `scripts/lib/v1-38-bounded-retry-v3-native-custody-v1.ts` — adds a raw isolated Git helper returning stdout as `Buffer` without text conversion. It authenticates `/usr/bin/git`, preserves the existing hardened configuration/environment and cleanup behavior, and supports NUL-delimited tree metadata and arbitrary blob bytes without changing text-helper callers.
- `scripts/run-v1-38-bounded-retry-envelope-v3.ts` — replaces only committed-payload custody: exact one-record `ls-tree -z` validation for path/type/OID/mode, raw `cat-file blob` retrieval, working regular-file executable projection, and final `Buffer.equals`. It also replaces the invalid v4 review input with the strict Plan-101 v5 path/schema/protocol/root while preserving the portable/full closure contract and all seal/envelope/live behavior.
- `scripts/run-v1-38-bounded-retry-envelope-v3.test.ts` — extends the focused suite with synthetic Git repositories and byte-exact fixtures: final newline, no final newline, empty file, CRLF bytes, arbitrary binary/invalid UTF-8, embedded NUL, 100644, 100755, symlink, gitlink, tree, missing, malformed, duplicate/unexpected record, wrong OID/path/type, byte drift, mode drift, decode/trim/re-encode bypasses, portable-member drift, before/after full-root drift, protected Plan-98/99 history, and destination absence.
- `262-100-SUMMARY.md` — records only exact source completion commit/tree/parent/blobs/modes, focused verification, immutable Plan-98/99 invalidation lineage, and Plan-101-only review eligibility. It is not a review verdict.
- Canonical evidence artifacts: none. The v5 JSON/REVIEW pair belongs only to Plan 101; seal-v13 and the inactive envelope remain Plan-92 outputs.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact raw Git blob and mode custody</name>
  <read_first>
    - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-100-RESEARCH.md
    - scripts/lib/v1-38-bounded-retry-v3-native-custody-v1.ts
    - scripts/run-v1-38-bounded-retry-envelope-v3.ts
    - scripts/run-v1-38-bounded-retry-envelope-v3.test.ts
    - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-99-SUMMARY.md
  </read_first>
  <files>scripts/lib/v1-38-bounded-retry-v3-native-custody-v1.ts, scripts/run-v1-38-bounded-retry-envelope-v3.ts, scripts/run-v1-38-bounded-retry-envelope-v3.test.ts</files>
  <behavior>
    - "Text metadata callers preserve current trimmed-string behavior, while the new raw helper returns stdout bytes unchanged for ls-tree -z and cat-file blob."
    - "Exactly one NUL-terminated entry with the requested path, type blob, 40-hex OID, and mode 100644 or 100755 is required before payload reading."
    - "Committed and no-follow working Buffers compare exactly for final-newline, no-final-newline, empty, CRLF, binary/invalid-UTF-8, and embedded-NUL fixtures."
    - "Missing, duplicate, malformed, unexpected-path, symlink, gitlink, tree, byte-drift, and executable-mode-drift cases fail before authority-sensitive work."
  </behavior>
  <action>Write the synthetic-repository fixture matrix first. Add `runV138RetryV3IsolatedGitBytes` as a sibling to the text helper, sharing executable digest validation, hardened arguments, clean environment, isolated temporary directory, maxBuffer, and cleanup, but omitting `encoding` and returning the raw stdout `Buffer`. In one closed custody function, validate the repository-relative path, invoke `ls-tree -z <commit> -- <path>`, require exactly one fully consumed NUL record with ASCII metadata grammar and exact raw path, require `blob` and mode `100644` or `100755`, then invoke `cat-file blob <oid>` through the raw helper. Compare the returned Buffer to the no-follow working regular-file Buffer using `Buffer.equals`; separately require Git's executable projection to match whether any working executable bit is set. Preserve the existing text helper for metadata and never route blob payloads through it. Cover empty bytes, retained final newline, no final newline, CRLF, arbitrary invalid UTF-8, embedded NUL, both regular modes, and every rejection family named in behavior per D-02, D-04, D-16, and D-17.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-bounded-retry-envelope-v3.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=180000 --hookTimeout=180000 --bail=1</automated></verify>
  <acceptance_criteria>
    - No committed payload path decodes, trims, normalizes, re-encodes, filters, or substitutes a digest for final byte equality.
    - Exact blob bytes and tracked executable mode are jointly authenticated for every fixed custody member.
    - Every non-regular or ambiguous tree/path/mode case fails deterministically before derivation or publication.
  </acceptance_criteria>
  <done>The production custody boundary preserves arbitrary committed Git blob bytes and exact regular-file mode semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the corrected consumer to the future v5 review without semantic drift</name>
  <read_first>
    - scripts/run-v1-38-bounded-retry-envelope-v3.ts
    - scripts/run-v1-38-bounded-retry-envelope-v3.test.ts
    - .planning/artifacts/v1.38-plan-262-99-bounded-retry-source-rereview-v4.json
    - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-99-REVIEW.md
    - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-99-SUMMARY.md
  </read_first>
  <files>scripts/run-v1-38-bounded-retry-envelope-v3.ts, scripts/run-v1-38-bounded-retry-envelope-v3.test.ts</files>
  <behavior>
    - "Only `.planning/artifacts/v1.38-plan-262-101-bounded-retry-source-rereview-v5.json` with the exact v5 schema/protocol/root and literal zero findings can make Plan 92 eligible."
    - "The Plan-99 provisional pair is rejected as current authority and retained only through explicit protected-history/invalidation fields with provisionalPairReinterpreted:false."
    - "Portable fields equal the freshly derived local closure member-by-member; gitObjectRoot stays local and the complete executionClosureRoot is identical before and after the no-publish consumer."
    - "Synthetic successful derivation preserves the existing seal-v13/envelope-v3 bytes, D-29R bounds, inactive 0/0 status, frozen runtime/kernel/privacy semantics, and total downstream denial."
  </behavior>
  <action>Write strict v5 schema, invalid-v4-history, portable/full closure, raw-custody bypass, before/after, and destination-absence tests before changing the consumer contract. Advance only the current review path to Plan 101 and require exact schemaVersion `v1.38-plan-262-101-git-object-byte-custody-rereview-v5`, protocol `git-object-byte-custody-v1`, portable domain `v1.38:plan-262-101:git-object-byte-custody:portable:v5`, result-root domain `v1.38:plan-262-101:git-object-byte-custody:root:v5`, review-root domain `v1.38:plan-262-101:git-object-byte-custody:review:v5`, and finding-root domain `v1.38:plan-262-101:git-object-byte-custody:finding:v5`. Require the v5 pair to bind the exact Plan-100 source completion commit/tree/parent and all three corrected file blobs/modes, literal zero findings, successful pre-publication actual-consumer observation, Plan-92-only eligibility, fresh 0/0, exhaustive false broader authority, and explicit immutable Plan-98/99 protected history including the provisional roots, later blocked roots/finding, and `provisionalPairReinterpreted:false`. Preserve the portable tuple members and independently derive the complete local closure; require field equality and the same full root before/after. Mutation-test every attempt to accept the v4 pair as authority, omit invalidation, decode/trim/re-encode bytes, bypass mode/type/path checks, alias root domains, or change frozen seal/envelope/controller semantics. Exercise only source and synthetic/disposable paths; create no canonical review or evidence file per D-01, D-24R, D-29R, and D-32R.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-bounded-retry-envelope-v3.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=180000 --hookTimeout=180000 --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; pnpm exec tsx scripts/run-v1-38-bounded-retry-envelope-v3.ts --check-source-only &amp;&amp; test ! -e .planning/artifacts/v1.38-plan-262-101-bounded-retry-source-rereview-v5.json &amp;&amp; test ! -e .planning/artifacts/v1.38-successor-source-seal-v13.json &amp;&amp; test ! -e .planning/artifacts/v1.38-plan-262-90-retry-envelope-v3.json &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - The corrected producer accepts only a future literal-zero v5 review and cannot consume Plan-99 provisional eligibility.
    - Exact portable/full closure separation and full-root before/after equality remain mandatory.
    - Existing seal/envelope/live semantics are byte-for-behavior unchanged and every canonical/live/downstream destination remains absent.
  </acceptance_criteria>
  <done>The raw-byte-correct consumer is ready for a fresh independent Plan-101 review and grants no authority itself.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Exact-authority matrix reproduction before candidate work | 262-100 | COVERED | Repairs only the source-custody gate; no empirical credit.
REQ | ADMIT-03 | Reviewed exact source remains required before sealing/live reproduction | 262-100 | COVERED | Source work grants only Plan-101 review eligibility.
REQ | ADMIT-04 | Missing, malformed, ambiguous, drifting, or invalid authority fails closed | 262-100 | COVERED | Exact tree record, raw blob, mode, v5 schema, and history checks.
REQ | MEAS-02 | Frozen policy, identity, runtime, and source bounds remain exact | 262-100 | COVERED | Only byte retrieval/mode authentication and successor review path change.
REQ | MEAS-04 | Accepted evidence requires zero identity/system/privacy defects | 262-100 | COVERED | Adversarial byte/mode/tree/root fixture matrix.
REQ | MEAS-09 | Integrity failure stays distinct from empirical pass/non-pass | 262-100 | COVERED | Plan-99 GIT_SHOW_BYTES_TRIMMED remains immutable 0/0 blocked history.
REQ | MEAS-10 | Private profile-neutral non-authorizing procedure | 262-100 | COVERED | Synthetic/source-only; no Strategy, secret, formation, holdout, or live input.
REQ | SEAL-01 | Exact single_operator_local_seal_v1 claim boundary | 262-100 | COVERED | No seal or custody claim is created or changed.
RESEARCH | raw Git byte custody | ls-tree -z plus cat-file blob Buffer equality and exact regular modes | 262-100 | COVERED | Task 1 owns helper, consumer, and fixture matrix.
RESEARCH | successor consumer contract | Strict v5 review, protected v4 invalidation, portable/full closure preservation | 262-100 | COVERED | Task 2 owns schema and regression boundary.
CONTEXT | D-01..D-18, D-19R, D-20R, D-21, D-22, D-23R..D-32R | Immutable, finite, private correction with no downstream authority | 262-100 | COVERED | Deferred Phase-263/later and gameplay work excluded.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Git tree metadata -> committed payload | Untrusted/ambiguous mode, type, OID, or path metadata must resolve to exactly one regular blob before bytes are read. |
| Git blob stdout -> no-follow working file | Arbitrary binary bytes cross without decoding, trimming, normalization, filters, or re-encoding and must compare exactly. |
| immutable Plan-98/99 history -> corrected consumer | Provisional eligibility and later invalidation remain exact non-authorizing history. |
| future Plan-101 portable review -> full local closure | Cross-checkout members must equal fresh local members while environment-bound gitObjectRoot stays local. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-100-01 | Spoofing | Git tree path/type/mode/OID | critical | mitigate | Exact one-record NUL parsing, exact path, blob type, 100644/100755 allowlist, OID grammar, and rejection fixtures. |
| T-262-100-02 | Tampering | committed blob bytes | critical | mitigate | Raw `cat-file blob` Buffer and final `Buffer.equals` against no-follow working bytes, including binary/NUL/newline fixtures. |
| T-262-100-03 | Repudiation | Plan-99 invalidation | high | mitigate | Preserve exact provisional and blocked roots/finding/0/0 with `provisionalPairReinterpreted:false`. |
| T-262-100-04 | Information Disclosure | synthetic/source correction | high | mitigate | No Strategy, receipt, secret, holdout, private runtime payload, or host path enters output. |
| T-262-100-05 | Elevation of Privilege | v5 review and closure gate | critical | mitigate | Strict new path/schema, literal zero, actual-consumer observation requirement, member equality, full-root equality, exhaustive false authority. |
| T-262-100-06 | Denial of Service | Git subprocess/fixture parsing | medium | mitigate | Existing 64 MiB bound, finite fixed paths, exact one-record parser, deterministic cleanup, serialized tests. |
| T-262-100-SC | Tampering | package supply chain | low | accept | No dependency installation occurs; existing toolchain members remain authenticated. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/run-v1-38-bounded-retry-envelope-v3.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=180000 --hookTimeout=180000 --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; pnpm exec tsx scripts/run-v1-38-bounded-retry-envelope-v3.ts --check-source-only &amp;&amp; git diff --check</automated>
</verification>

<success_criteria>
- Exact raw committed bytes and regular-file Git modes are authenticated for every custody path with the full adversarial fixture matrix.
- The immutable Plan-98/99 branch remains non-authorizing history and only a future Plan-101 v5 zero review may make Plan 92 eligible.
- No canonical evidence, live action, charge, acceptance, lifecycle mutation, or downstream authority occurs.
</success_criteria>

<output>Create `262-100-SUMMARY.md` with exact source commit/tree/parent/blobs/modes, focused verification, Plan-98/99 immutable invalidation custody, and Plan-101-only handoff/non-authority.</output>
