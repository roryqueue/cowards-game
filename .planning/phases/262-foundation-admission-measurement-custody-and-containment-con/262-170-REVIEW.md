---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "170"
reviewed: 2026-09-01
depth: deep
source_commit: c1bda8cf8a42c1e4a26ccb1a44129f48f826c00e
source_tree: bce72f68971e896fef585c609da4279d6fe743e0
manifest_root: sha256:194a64af7c9436e1ccf2eab91553ecdb63368216092d091d0533d71c74c1e857
findings:
  critical: 3
  warning: 1
  info: 0
  total: 4
status: issues_found
---

# Phase 262 Plan 170: Corrective Source Review v5

The exact v5-bound Plan 169 source closure is not ready for Plan 158. The sole independent reviewer authenticated source commit `c1bda8cf8a42c1e4a26ccb1a44129f48f826c00e`, tree `bce72f68971e896fef585c609da4279d6fe743e0`, and the recursive executable closure, but returned three critical findings and one warning. Readiness v5 remains absent, this review admits no execution, and no live or recovery selector was invoked.

## Critical Findings

### CR-170-01: The v5 manifest omits the committed Plan 169 summary identity

Plan 170 requires the manifest to bind the separately committed Plan 169 summary. The exact summary identity is commit `c33111d5`, blob `8b4bab02e1e3262e32d0c5efcde9c58f509073ef`, and content root `sha256:b24af58afb224715eb2e40bedef0150f761e1b7b04687c9346e256e77b79a65b`. The v5 manifest schema, renderer, checker, and recovery-lineage checker instead retain only `plan165Summary`. The missing binding violates the v5 trust-closure must-have.

### CR-170-02: Security-relevant capability arguments are not exactly proven

The structural proof accepts capability-bearing calls by partial syntax and identifier spelling rather than exact argument values. Adversarial mutations using `git(repoRoot, ["clean", "-fdx"])`, a parameter-smuggled `SIGUSR1`, `unlinkSync(path.resolve(repoRoot, "package.json"))`, and an extra `ps -ww` argument all passed. Primitive argument values, exact arity/value vectors, and exact canonical filesystem targets must be propagated and validated through the reachable call graph.

### CR-170-03: Name-based allowlists accept shadowed or rebound symbols

Allowed calls and constructors are selected by spelling rather than authenticated symbol provenance. Adversarial mutations using a local `.map()` method, shadowed `JSON.parse`, shadowed `new TypeError`, and `readFileSync` from an unrelated external module all passed. Each allowed operation must resolve to an authenticated global, exact module export, or proven receiver type.

## Warning

### WR-170-01: Binding collisions can be silently resolved

Function, alias, import, and re-export indexing uses unchecked `Map.set` operations and resolution priority. Some colliding bindings therefore select a function instead of failing as ambiguous. A single collision-aware binding registrar is required across all declaration kinds.

## Verified Coverage

- The exact `unsafeRecoveryHop` function-declaration mutation is now rejected.
- Parenthesized and single-parameter arrows, function expressions, local aliases, default/named/aliased/namespace relative imports, named/default re-exports, multi-hop resolution, computed/dynamic calls, parse failures, unresolved bare calls, property hiding, and disallowed `NewExpression` were exercised.
- Unsafe dataflow through `cleanup`, `terminalizeInvalid`, and `postcheck` was rejected.
- The focused serial suite passed **71/71** tests and TypeScript compilation passed.
- V2, v3, and v4 predecessor checks, current recovery structure, and the v5 manifest checker passed.
- Readiness, invocation, terminal, adjudication, eligibility, and child-ownership effects remain absent; exactly 36 successor locks remain untouched.

## Authority

This review is non-authorizing. Plan 158 eligibility, corrective invocation, recovery invocation, Phase 263, candidate search, formation materialization, holdout opening, public/product/production exposure, counted play, gameplay change, archive, release, and tag authority all remain false.

---

_Reviewer: fresh independent gsd-code-reviewer `/root/execute_262_170/review_262_170_v5`_
_Disposition: issues_found_
