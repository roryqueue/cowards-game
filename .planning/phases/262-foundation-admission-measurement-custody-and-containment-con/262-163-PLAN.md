---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "163"
type: execute
wave: 134
depends_on: [262-157]
files_modified:
  - scripts/lib/v1-38-lean-runner-feasibility.ts
  - scripts/lib/v1-38-lean-runner-feasibility.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-163-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-03, ADMIT-04, MEAS-04, MEAS-08, MEAS-09, DECI-02]
must_haves:
  truths:
    - "Corrective admission authenticates the exact tracked working-tree bytes immediately before marker creation and again in the child before any Match can launch."
    - "Either pre-existing fresh corrective effect destination makes the normal wrapper fail before marker creation or Match execution."
    - "A narrowly scoped durable operational ownership record lets recovery-only authenticate and terminate an active orphan after parent death, while recovery-only has no route to enumerate, launch, resume, or relaunch a Match."
    - "Structural and behavioral tests inspect the actual executable recovery-only CLI selector as well as its helper and prove zero launch capability."
    - "The expensive focused fixture test is reliable without extending or weakening any production Match, cell, child-cleanup, or outer gate deadline."
  artifacts:
    - path: scripts/run-v1-38-lean-runner-feasibility.ts
      provides: "Fail-closed corrective wrapper and authenticated recovery-only selector"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Tracked-byte admission and actual-selector zero-launch structural proof"
    - path: scripts/run-v1-38-lean-runner-feasibility.test.ts
      provides: "Regression coverage for all five Plan157 findings"
  key_links:
    - from: scripts/check-v1-38-lean-admission.ts
      to: scripts/run-v1-38-lean-runner-feasibility.ts
      via: "shared exact clean-status policy at parent preflight and child admission"
      pattern: "corrective.*status|working.*tree"
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: .planning/artifacts/v1.38-lean-runner-corrective-terminal-v2.json
      via: "fresh-destination rejection before marker and bounded orphan terminalization after marker"
      pattern: "recovery-only|terminal"
---

<objective>
Close exactly the three critical and two warning findings from Plan262-157 without running the corrective gate or creating any live effect.

Purpose: Make the sole corrective route safe enough for a fresh exact-source independent review while preserving D-34L's one-rerun limit and immutable failed history.
Output: Focused source/test fixes and a source-only summary; no manifest, readiness, marker, terminal, Match, adjudication, archive, or tag.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-ADMISSION-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-157-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-157-SUMMARY.md
@scripts/lib/v1-38-lean-runner-feasibility.ts
@scripts/run-v1-38-lean-runner-feasibility.ts
@scripts/check-v1-38-lean-admission.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed on tracked-byte drift and either pre-existing corrective effect</name>
  <files>scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts, scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts</files>
  <behavior>
    - A modified tracked executable file fails normal corrective preflight before marker creation, and the child repeats the same authentication before its first Match.
    - The exact 36 successor locks are the only tolerated pre-existing untracked operational residue; no tracked drift is tolerated.
    - A pre-existing fresh v2 marker or fresh v2 terminal independently blocks the normal wrapper before any Match, and terminal absence is rechecked immediately before launch.
    - The immutable first marker/terminal and Plan157 manifest/review remain unchanged and cannot satisfy fresh readiness.
  </behavior>
  <action>Write failing regression tests first for CR-01 and CR-02, then implement one shared, exact corrective clean-status policy used immediately before the parent creates the fresh marker and repeated inside child admission before schedule execution. Authenticate tracked bytes against the reviewed commit rather than merely recognizing paths. Permit only the exact 36 existing `.v138-successor-*.lock` records plus the narrowly enumerated current corrective operational/effect paths appropriate to the admission stage; reject every other status entry. Check both fresh v2 effect destinations before marker creation and atomically re-check terminal absence before launch so neither stale terminal nor marker can cause charged work. Preserve the first attempt, diagnostic observations, Plan157 nonzero review, all historical routes, canonical gameplay, current formation, and every false authority field per D-34L. Do not create fresh manifest/readiness/effect artifacts and do not run any Match.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>CR-01 and CR-02 have deterministic negative tests and both parent and child fail before launch on unauthorized bytes or either pre-existing effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authenticated orphan termination with zero recovery launch surface</name>
  <files>scripts/lib/v1-38-lean-runner-feasibility.ts, scripts/lib/v1-38-lean-runner-feasibility.test.ts, scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts, scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts</files>
  <behavior>
    - Before an admitted child can execute, the parent durably writes a local operational ownership record bound to the fresh invocation root, child PID/process group, exact child selector, and a per-invocation opaque token visible in the child command identity.
    - Recovery-only rejects missing, malformed, stale, lineage-mismatched, PID-reused, or command-identity-mismatched ownership records without signalling a process.
    - For an authenticated active orphan, recovery-only performs bounded TERM/wait/KILL cleanup, proves exit, removes the operational record, and writes only a terminalized-invalid terminal for the existing marker.
    - Recovery-only with marker absent or terminal present rejects; every recovery-only branch has zero schedule enumeration, worker creation, Match launch, resume, or relaunch calls.
  </behavior>
  <action>Close CR-03 and WR-01 with a narrowly scoped durable operational child-ownership record that is not canonical evidence, is never committed, contains no Strategy/private payload, and is deleted after verified cleanup. Bind it to the fresh marker/invocation root and an unguessable per-invocation token included in the exact child command identity; record the child PID and process group only after spawn, fsync the record before permitting child admission, and require recovery to re-authenticate the live command identity plus lineage before signalling. Use bounded termination and explicit exit proof; reject PID reuse or ambiguity instead of signalling. Recovery-only may consume an existing marker and ownership record solely to clean and terminalize invalid, and must expose no imported/callable path to schedule construction, worker creation, fixture execution, Match execution, or retry. Replace the prior helper-only text slice with structural inspection of both the helper and the actual executable CLI selector branch, using AST/call-graph analysis or an equivalently explicit parsed boundary; retain behavioral injected-spy tests. Preserve the normal wrapper's `finally` cleanup. Do not run either live selector.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-runner-feasibility.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-corrective-recovery-only-structure</automated></verify>
  <done>CR-03 and WR-01 are closed by authenticated bounded orphan cleanup and structural plus behavioral proof over the executable selector that recovery cannot launch work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stabilize focused verification and bind the source-only closure</name>
  <files>scripts/lib/v1-38-lean-runner-feasibility.test.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-163-SUMMARY.md</files>
  <behavior>
    - The 24-cell canonical-request fixture test passes reliably under its exact prescribed focused command.
    - Any increased timeout is scoped only to that expensive test or test command; production per-cell, child cleanup, and 15-minute outer deadlines remain byte-for-byte unchanged.
    - All five Plan157 finding regressions pass without live effects.
  </behavior>
  <action>Close WR-02 by assigning the expensive canonical-request test an explicit stable test-only timeout derived from the independently observed 30-second passing bound, or by reducing fixture-construction cost without changing runtime behavior. Add a regression that authenticates unchanged production deadline constants before and after the test change. Run the full focused suite serially, TypeScript, structural recovery proof, source-only historical custody checks, effect-absence checks, and `git diff --check`. Create the summary with the exact source commit, test counts, the five closed finding IDs, no live effects, all broader authority false, and the preserved 36-lock inventory. Do not create Plan164 artifacts.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-runner-feasibility.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-corrective-recovery-only-structure &amp;&amp; git diff --check</automated></verify>
  <done>All five findings are covered by passing source-only tests, production deadlines are unchanged, and no live/readiness effect exists.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Make the sole corrective route safe for exact-source re-review | 262-163 | COVERED | Five findings only; no live effect.
REQ | ADMIT-03 | Preserve one reviewed whole-gate corrective opportunity | 262-163 | COVERED | Admission safety fixes only.
REQ | ADMIT-04 | Reject source/effect drift before execution | 262-163 | COVERED | Parent and child checks.
REQ | MEAS-04 | Keep invalid/recovered work inadmissible | 262-163 | COVERED | Orphan terminalizes invalid.
REQ | MEAS-08 | Preserve fixture-only meaning | 262-163 | COVERED | No result claim.
REQ | MEAS-09 | Keep history, correction, and recovery distinct | 262-163 | COVERED | No reuse or relaunch.
REQ | DECI-02 | Preserve least authority and production isolation | 262-163 | COVERED | Recovery has zero launch capability.
RESEARCH | — | One whole corrective rerun only after a specific committed fix | 262-163 | COVERED | Source fix precedes fresh review.
CONTEXT | D-01..D-22 | Integrity, privacy, accounting, runtime, and containment bounds | 262-163 | COVERED | Unchanged.
CONTEXT | D-23R..D-33R | Retired full-matrix routes | EXCLUDED | Immutable history only.
CONTEXT | D-34L | At most one full corrective rerun; no resource/gameplay retry | 262-163 | COVERED | No Match or authority effect.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| reviewed commit -> executable working tree | Imported bytes must equal reviewed tracked bytes at both parent and child admission. |
| marker -> child ownership | Local operational identity may authorize termination only, never execution. |
| recovery CLI -> process table | Recovery must prove exact child identity before bounded signalling. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-163-01 | Tampering | tracked working tree | critical | mitigate | Exact parent and child clean-status/authentication tests. |
| T-262-163-02 | Elevation of Privilege | stale effect destinations | critical | mitigate | Marker and terminal absence before marker and immediately before launch. |
| T-262-163-03 | Spoofing | orphan identity | critical | mitigate | Invocation-root, PID/group, opaque-token, and command-identity binding. |
| T-262-163-04 | Denial of Service | parent death | high | mitigate | Durable ownership record plus bounded TERM/wait/KILL and exit proof. |
| T-262-163-05 | Elevation of Privilege | recovery selector | critical | mitigate | Parsed actual-selector call graph and behavioral zero-launch tests. |
| T-262-163-06 | Repudiation | timeout adjustment | medium | mitigate | Test-only scope plus production deadline byte assertions. |
| T-262-163-SC | Tampering | package supply chain | low | accept | No package installation occurs. |
</threat_model>

<verification>
All five Plan157 findings have focused failing-then-passing regressions, the exact default focused command is stable, TypeScript and structural checks pass, no live artifacts or Matches exist, and the 36 locks remain untouched.
</verification>

<success_criteria>
- CR-01 through CR-03 and WR-01 through WR-02 each map to a passing regression.
- Clean tracked bytes and both fresh destinations are checked before effects.
- Recovery can safely terminate an authenticated active orphan but cannot launch or relaunch work.
- Structural inspection covers the executable CLI branch.
- Production deadlines and frozen D-34L bounds remain unchanged.
- No live, readiness, adjudication, archive, or tag effect occurs.
</success_criteria>

<output>Create `262-163-SUMMARY.md`; dispatch Plan262-164 for fresh exact-source independent review.</output>
