---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "183"
type: tdd
wave: 152
depends_on: [262-182]
files_modified:
  - scripts/lib/v1-38-lean-container-match-session.ts
  - scripts/lib/v1-38-lean-container-match-session.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-183-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "Per D-34L and D-34L.1, Docker's tagless RepoDigest identity and the frozen tag-bearing invocation reference compare equal only when both repository and sha256 digest are byte-identical."
    - "The exact tag-bearing digest-pinned LEAN_CONTAINER_IMAGE remains the only container invocation reference; identity normalization never permits a floating tag, digest-only value, alternate repository, or alternate digest."
    - "Every container has a caller-chosen deterministic unique name and immutable ownership label before `docker create`; ambiguous create outcomes always enter bounded cleanup-by-name, but force-removal occurs only after exact ownership confirmation, while collisions and cleanup ambiguity fail closed with no fallback."
    - "The Plan182 v3 refusal remains immutable history; a fresh v5 trust/effect family reserves preflight v4, authorization/review v5, and invocation/terminal/adjudication/eligibility v5 destinations."
    - "Source-only checks bind exact separately committed Plan183 bytes, preserve all earlier history and 36 locks, and invoke no Docker preflight, Match, marker, authorization, review, or effect."
  artifacts:
    - path: scripts/run-v1-38-lean-runner-feasibility.ts
      provides: "Exact repository-plus-digest identity parser/comparator used by the non-consuming container preflight"
    - path: scripts/lib/v1-38-lean-container-match-session.ts
      provides: "Pre-create name/label ownership plus collision-safe cleanup-by-name for every ambiguous create branch"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Collision-free v5 trust/effect paths and source-only/readiness/effect validators"
  key_links:
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: docker image inspect RepoDigests
      via: "Parse both references into exact repository and sha256 digest components before equality comparison"
      pattern: "repository|digest"
    - from: scripts/check-v1-38-lean-admission.ts
      to: .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v4.json
      via: "Fresh exclusive LEAN_DIRECT_V5_ARTIFACT_PATHS family"
      pattern: "LEAN_DIRECT_V5_ARTIFACT_PATHS"
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: scripts/lib/v1-38-lean-container-match-session.ts
      via: "Caller derives a deterministic collision-resistant name and ownership label from immutable invocation/source and Match identity before session creation"
      pattern: "containerName"
---

<objective>
Repair the two Plan182 result-validity blockers—false Docker image-identity rejection and missing cleanup ownership after ambiguous create—and establish a fresh collision-free v5 trust/effect path.

Purpose: Permit one new non-consuming preflight to recognize Docker's canonical tagless RepoDigest without weakening the frozen digest-pinned invocation or spending the sole corrective Match opportunity.
Output: Separately committed tested image/session source and source-only summary; no Docker preflight, authorization, review, marker, Match, terminal, or tracking effect.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/artifacts/v1.38-lean-runner-direct-container-preflight-v3.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-182-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-182-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-EXECUTION-CLOSURE-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Normalize exact Docker repository and digest identity without weakening invocation pinning</name>
  <files>scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts</files>
  <behavior>
    - "RED proves the current comparator rejects Docker's observed `node@sha256:2bdb...bf14` even though the frozen invocation constant is `node:24-alpine@sha256:2bdb...bf14`; GREEN accepts this exact repository-plus-digest identity."
    - "The parser accepts only a nonempty repository followed by `@sha256:` and exactly 64 lowercase hexadecimal digest characters; it strips a tag only from the final repository path component and never changes the repository namespace."
    - "The comparator rejects a different repository with the same digest, the same repository with a different digest, floating or tag-only references, digest-only values, uppercase/malformed/truncated digests, and entries that merely contain the expected text."
    - "`LEAN_CONTAINER_IMAGE` remains byte-identical and is still passed unchanged to Docker create/inspect and recorded as `imageReference`; only local RepoDigest membership uses the parsed repository-plus-digest comparison."
  </behavior>
  <action>First commit RED tests that reproduce the observed representation mismatch and prove all fail-closed mismatch and malformed cases. Then implement a small exported pure parser and equality helper for Docker image identity. Parse the frozen tag-bearing reference and every inspected RepoDigest into `{ repository, digest }`; remove a tag only when a colon occurs after the last slash and before `@`, retain ports and namespaces correctly, and require exact repository plus full lowercase sha256 equality. Keep `LEAN_CONTAINER_IMAGE` unchanged as the invocation and inspection reference and keep the pass artifact's `imageReference` unchanged; replace only the literal RepoDigest membership predicate with `some` over exact normalized identities. Do not accept digest equality alone. Update every session caller to pass a deterministic collision-resistant container name derived from immutable source or invocation identity plus Match identity, without time or randomness. Do not invoke Docker or the live selector.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-lean-runner-feasibility.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>RED fails for the exact Docker representation mismatch; GREEN accepts only byte-identical repository plus digest while preserving the exact tag-bearing invocation reference and deterministic caller-owned session names.</done>
</task>

<task type="auto" tdd="true">
  <name>Own and clean ambiguous container creation, then close fresh v5 source custody</name>
  <files>scripts/lib/v1-38-lean-container-match-session.ts, scripts/lib/v1-38-lean-container-match-session.test.ts, scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-183-SUMMARY.md</files>
  <behavior>
    - "RED proves a create timeout or error after actual creation, status-zero output loss, malformed returned ID, or unexpected stderr cannot return or throw before a bounded ownership-and-cleanup procedure addresses the exact caller name."
    - "A caller-chosen deterministic unique name and immutable ownership label are validated and included through `docker create --name <caller-name> --label <ownership>` before the image; later start, exec, close, and poison cleanup address that exact name and verify that label rather than trusting create stdout."
    - "A pre-existing exact-name collision fails before create and does not delete or reuse the occupant; a post-precheck collision or label mismatch also retains the foreign occupant and fails closed."
    - "Ambiguous create with matching ownership is force-removed by exact name and confirmed absent; ownership-inspection ambiguity, matching-owned removal failure, or absence-verification ambiguity fails as incomplete cleanup with no adapter, Match evidence, or fallback."
    - "Successful create with normal or lost stdout proceeds only after exact name and ownership confirmation; every branch has a bounded disposition without deleting an unowned container."
    - "Fresh paths are exactly direct-container-preflight-v4, direct-authorization-v5, direct-validity-review-v5, direct-invocation-v5, direct-terminal-v5, direct-adjudication-v5, and phase-262-lean-direct-eligibility-v5."
    - "Fresh v5 validators preserve exact Plan183 Git commit/tree/executable closure, pass-only authorization, exactly seven D-34L.1 review categories, branch-aware denial/admission, later v5 effect checks, immutable Plan182 refusal history, all 36 locks, and zero consumed corrective Match invocations."
  </behavior>
  <action>First commit RED fault-injection tests for ambiguous create success with output loss, timeout/error after creation, malformed output, unexpected stderr, pre-create name collision, post-precheck collision with a foreign ownership label, cleanup failure, ownership/absence-verification ambiguity, and no fallback. The post-precheck collision test must prove the foreign occupant is retained. Extend `LeanContainerMatchSessionOptions` with a required caller-chosen `containerName` and immutable `ownershipLabel`; validate both before any transport call and include exact `--name` and `--label` arguments in create. Establish the exact cleanup target and bounded ownership-aware cleanup function before invoking create. Perform a bounded pre-create exact-name absence check so a known collision refuses without deleting or reusing the occupant. For every create timeout/error/ambiguous-output branch, enter the bounded cleanup-by-name procedure: inspect the exact name's immutable label; if absent, cleanup is complete; if the label matches, force-remove by exact name and require bounded absence confirmation; if there is an explicit conflict or label mismatch, retain the occupant and fail closed; if ownership inspection, owned removal, or absence confirmation is ambiguous, throw a cleanup-specific system failure and expose no usable session. Do not trust Docker's returned container ID as cleanup ownership; successful create with normal or lost stdout may proceed only after exact name/label confirmation, and start, exec, close, and poison cleanup use that verified exact name. Never fall back to worker threads or subprocess execution. Keep image, resource controls, IPC, limits, deadlines, canonical host engine/runtime, and Match semantics unchanged. Add an additive `LEAN_DIRECT_V5_ARTIFACT_PATHS` family and the source-only, preflight-v4, authorization-v5, exactly-seven-category review-v5, reviewed-ready-v5, and later invocation/terminal/adjudication/eligibility-v5 render/write/check functions and CLI selectors by following the existing v4 shape. Bind authorization to exact separately committed Plan183 runnable bytes, not HEAD or its summary child; preserve Plan182 and older bytes as immutable non-current history; rewire the direct live selector to v5 readiness/effects without calling it. Commit all runnable source/tests separately before the summary, then record exact commit/tree/closure. Run only focused tests, TypeScript, source-only checking, lock counting, and diff checks. Do not invoke Docker, write operational artifacts, create a marker, run a Match, edit tracking, or expand certification.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-container-session-source-only-v5 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated></verify>
  <done>All ambiguous create branches either prove the caller-owned name absent after bounded removal or fail closed as incomplete cleanup; exact v5 source custody and effect absence pass with immutable history and 36 locks.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Repair the false image-identity refusal without consuming the run | 262-183 | COVERED | TDD source-only repair.
REQ | ADMIT-01..ADMIT-04 | Exact current-runtime/source admission and fail-closed drift | 262-183 | COVERED | Exact repo plus digest and fresh custody.
REQ | MEAS-01..MEAS-10 | Preserve tuple, schedule, accounting, cleanup, claims, and privacy | 262-183 | COVERED | No empirical or semantic change.
REQ | SEAL-01 | Preserve holdout/local-seal truth | 262-183 | COVERED | No holdout action.
REQ | DECI-02 | Preserve hostile containment | 262-183 | COVERED | Invocation image and controls unchanged.
RESEARCH | — | Bounded result-validity proof, not production certification | 262-183 | COVERED | Two narrow Plan182 validity repairs only.
CONTEXT | D-01..D-22 | Integrity/runtime/privacy/accounting | 262-183 | COVERED | Additive fail-closed identity and custody checks.
CONTEXT | D-34L | One corrected 24-Match opportunity remains | 262-183 | COVERED | No Match effect.
CONTEXT | D-34L.1 | Seven-category no-recovery closure | 262-183 | COVERED | Fresh v5 trust/effect path.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Docker inspect output -> preflight | A tagless canonical RepoDigest must match only the same repository and digest. |
| frozen invocation reference -> Docker | The exact tag-bearing digest pin must remain unchanged. |
| caller-owned name/label -> Docker create/cleanup | Ownership identity exists before create; only a matching owned container may be removed. |
| prior refusal -> fresh trust path | Plan182 non-pass bytes cannot become current evidence or authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-183-01 | Spoofing | image identity parser | critical | mitigate | Strict repository plus full sha256 parsing/equality and hostile mismatch tests. |
| T-262-183-02 | Tampering | invocation image | critical | mitigate | Byte-identical frozen tag-bearing constant remains the sole Docker reference. |
| T-262-183-03 | Denial of Service | ambiguous create cleanup | critical | mitigate | Pre-create unique name/label, ownership check, matching-only remove-by-name, and absence confirmation. |
| T-262-183-04 | Elevation of Privilege | v5 readiness | critical | mitigate | Fresh exact-source preflight pass, authorization, seven passes, and zero findings. |
| T-262-183-05 | Repudiation | Plan182 refusal | high | mitigate | Immutable roots and collision-free fresh destinations. |
| T-262-183-SC | Tampering | package supply chain | low | accept | No package install or image pull occurs. |
</threat_model>

<verification>Focused RED/GREEN tests prove exact image identity and pre-create cleanup ownership; TypeScript and source-only v5 custody pass; Plan182 and earlier history plus 36 locks are unchanged; all v5 operational destinations remain absent; Docker and Match invocation counts remain zero.</verification>

<success_criteria>
- Two focused TDD tasks across seven files, within the plan context budget.
- Docker's canonical tagless RepoDigest matches only the exact frozen repository and digest.
- The tag-bearing digest-pinned invocation reference and all hostile controls remain unchanged.
- Every ambiguous create outcome enters ownership-aware cleanup; only a matching owned container is removed, while foreign collisions are retained and all ambiguity fails closed.
- Fresh v5 custody/effect paths cannot consume Plan182 or older refusal artifacts.
- No Docker preflight, authorization, review, marker, Match, terminal, archive, or tag effect occurs.
</success_criteria>

<output>Create `262-183-SUMMARY.md` after the separately committed runnable fix; dispatch Plan184 for one fresh non-consuming preflight and independent review.</output>
