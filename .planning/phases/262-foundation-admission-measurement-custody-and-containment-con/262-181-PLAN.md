---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "181"
type: tdd
wave: 150
depends_on: [262-180]
files_modified:
  - scripts/lib/v1-38-lean-container-match-session.ts
  - scripts/lib/v1-38-lean-container-match-session.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-181-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "Per D-34L and D-34L.1, the private runner creates exactly one digest-pinned hostile container for one Match, serves only that Match's bounded Strategy calls, and proves destruction before another Match can begin."
    - "The session preserves the existing canonical transition engine, selected runtime ABI, Strategy observations, accounting, image, containment controls, fixture tuple, 12-cell/two-pass schedule, 45-second cell deadline, and 15-minute outer deadline."
    - "Timeout, stream overflow, malformed or desynchronized frames, unexpected process state, hostile output, and cleanup ambiguity fail the Match closed, permanently poison the session, and cannot select a fallback runtime."
    - "Plans178 and180 remain immutable non-consuming refusal history; source-only checks leave every fresh preflight, authorization, review, marker, Match, terminal, adjudication, and eligibility destination absent and preserve exactly 36 locks."
  artifacts:
    - path: scripts/lib/v1-38-lean-container-match-session.ts
      provides: "Private per-Match container lifecycle and strict request/response transport"
    - path: scripts/run-v1-38-lean-runner-feasibility.ts
      provides: "Canonical Match execution wired to one bounded container session"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Fresh v4 trust/effect paths and exact Plan181 source-only custody gates"
  key_links:
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: scripts/lib/v1-38-lean-container-match-session.ts
      via: "Open before executePreparedRuntimeServiceRequestV118 and destroy in a finally path before returning the cell result"
      pattern: "createLeanContainerMatchSession"
    - from: scripts/lib/v1-38-lean-container-match-session.ts
      to: packages/runtime-js/src/subprocess-ipc.ts
      via: "Existing strict request encoder, response parser, byte caps, and classified system failures"
      pattern: "encodeSubprocessIpcRequest|parseSubprocessIpcResponse"
    - from: scripts/check-v1-38-lean-admission.ts
      to: .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v3.json
      via: "Fresh exclusive source/preflight/authorization/review/effect family"
      pattern: "LEAN_DIRECT_V4_ARTIFACT_PATHS"
---

<objective>
Replace the infeasible fresh-container-per-method topology with one fail-closed private container session per Match, then establish a collision-free v4 trust path.

Purpose: Close Plan180 CR-01 without relaxing a frozen scientific, runtime, containment, semantic, privacy, formation-absence, schedule, or deadline bound and without spending the sole corrective Match opportunity.
Output: Separately committed tested session source and exact source-only custody proof; no Docker preflight, authorization, review, marker, Match, terminal, adjudication, eligibility, tracking, archive, or tag effect.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-180-REVIEW.md
@.planning/artifacts/v1.38-lean-runner-direct-container-preflight-v2.json
@.planning/artifacts/v1.38-lean-runner-direct-validity-review-v3.json
@scripts/run-v1-38-lean-runner-feasibility.ts
@packages/runtime-js/src/container-subprocess-adapter.ts
@packages/runtime-js/src/subprocess-ipc.ts
@packages/runtime-js/src/abi-bridge.ts
@apps/runtime-service/src/runtime-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the fail-closed per-Match container session</name>
  <files>scripts/lib/v1-38-lean-container-match-session.ts, scripts/lib/v1-38-lean-container-match-session.test.ts</files>
  <behavior>
    - "A session creates and starts one container with the exact digest-pinned image, network none, read-only root, noexec/nosuid 16 MiB tmpfs, 64 MiB memory, 0.5 CPU, 64 PIDs, all capabilities dropped, no-new-privileges, minimal environment, shell disabled, and bounded stdout/stderr; it binds one immutable Match identity."
    - "Each request runs through the existing runtime ABI bridge and strict JSON IPC encoder/parser inside the already-started container, with exact per-method timeout and byte limits; one complete response is accepted and missing, extra, malformed, out-of-order, or oversized framing is rejected."
    - "A timeout, signal, nonzero exit, stream overflow, hostile schema output, frame desynchronization, duplicate response, or runtime-accounting ambiguity poisons the session, triggers bounded forced container removal, and makes every later call fail closed."
    - "Normal completion removes the container exactly once; close is idempotent only for cleanup, cannot reopen the same session, and proves no reusable container crosses a Match boundary. A different Match always receives a distinct container identity."
    - "The adapter metadata remains container-subprocess/runtime-js-container-subprocess with fallback false, and injected fake Docker transports cover lifecycle, timeout, cleanup failure, cross-Match isolation, hostile validation, and no-fallback behavior without contacting Docker."
  </behavior>
  <action>Commit RED tests first for one-container-per-Match lifecycle, exact control arguments, strict framed request/response ordering, byte caps, timeout destruction, protocol poisoning, forced cleanup, cleanup ambiguity, distinct cross-Match container identities, hostile output validation, and unreachable fallback. Then implement a private runner-only session module. Keep the canonical engine and runtime-service on the host; only hostile Strategy method execution belongs inside the digest-pinned container. Reuse the existing ABI bridge, subprocess IPC encoder/parser, candidate observation/accounting, output limits, and classified system-failure semantics instead of inventing alternate validation. Start one inert shell-free container for an exact Match identity, invoke each bounded method through that container using an argument-vector Docker operation and piped framed JSON, and force-remove the container after the Match or on the first fault. Never reuse a poisoned or closed session, never accept stderr or surplus stdout as evidence, never mount the repository or host paths, never enable network, and never select worker-thread, host subprocess, or any fallback. All unit tests use dependency-injected transports and must run no Docker command.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>The RED suite fails for the missing session behavior, then GREEN proves exact containment, strict framing, hostile validation, timeout/fault cleanup, no fallback, and one-container-per-Match isolation without invoking Docker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire canonical Match execution and fresh source custody</name>
  <files>scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts, scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-181-SUMMARY.md</files>
  <behavior>
    - "executePreparedLeanCell opens one session for the cell's stable canonical Match identity, injects its adapter into the otherwise unchanged runtime-service configuration, runs the unchanged prepared request, and closes in finally before projecting evidence."
    - "Session lifecycle or cleanup ambiguity becomes system_failure with cleanupComplete false or orphanedChild true and never gains semantic roots; success/player-violation projections retain the existing six roots and current-formation checks."
    - "The preflight sampler separately measures container create/start/destroy cost and in-session selectActivations/soldierBrain cost, then conservatively projects the unchanged method ceilings, 24 Matches, 45-second cell deadline, and 15-minute outer deadline without executing a Match."
    - "Fresh paths are exactly direct-container-preflight-v3, direct-authorization-v4, direct-validity-review-v4, direct-invocation-v4, direct-terminal-v4, direct-adjudication-v4, and phase-262-lean-direct-eligibility-v4."
    - "Source-only validation binds the exact separately committed Plan181 source commit/tree/executable closure, rejects any changed executable byte or occupied fresh destination, preserves every v1-v3 artifact and review byte, requires exactly 36 locks, and proves zero preflight, authorization, review, Match, marker, terminal, adjudication, or eligibility effects."
    - "A branch-aware v4 disposition checker validates exact admission through reviewed readiness and validates denial through admitsPlan175 false, absent Match/effects, and false broader authority."
  </behavior>
  <action>Extend the RED set before runner/checker production edits. Wire the private session adapter into only the prepared lean fixture path by replacing the adapter object on a normally constructed runtime-service configuration; do not modify production runtime-config selection or any engine, rules, replay, fixture, arena, schedule, deadline, acceptance, privacy, or formation code. Scope one session around exactly one executePreparedRuntimeServiceRequestV118 call and make the close result part of the existing cleanup truth. Replace only the throughput model for the fresh trust family: separately account for one container lifecycle per Match and the unchanged per-method ceilings inside that session, using conservative maxima and the existing startup/cleanup margin. Add LEAN_DIRECT_V4_ARTIFACT_PATHS plus additive render/write/check functions for preflight v3, authorization/review v4, reviewed-ready v4, branch-aware review disposition v4, and the later v4 invocation/terminal/adjudication/eligibility chain. The disposition checker always validates the review and its exact source/preflight relationship; on admission it also requires reviewed-ready, while on denial it requires admitsPlan175 false, absent v4 Match/effects, and false broader authority without incorrectly requiring authorization absent when a passing preflight preceded a reviewer finding. Authorization remains impossible in this plan. Commit all runnable source and tests separately, then record its exact commit/tree/executable closure in the summary. Run only focused tests, TypeScript, source-only custody, absence, and lock checks; do not invoke Docker or any operational selector.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-container-session-source-only-v4 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated></verify>
  <done>Canonical prepared Matches use exactly one fail-closed hostile container session each; fresh v4 source custody and all no-effect checks pass over a separately committed closure; every frozen bound and all history remain unchanged.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Close Plan180 CR-01 without consuming the corrective run | 262-181 | COVERED | TDD and source only.
REQ | ADMIT-01..ADMIT-04 | Exact current-runtime admission and fail-closed source/runtime drift | 262-181 | COVERED | Exact source closure and fresh pass-only gates.
REQ | MEAS-01..MEAS-10 | Preserve tuple, schedule, budgets, accounting, cleanup, claims, and privacy | 262-181 | COVERED | Only execution topology changes.
REQ | SEAL-01 | Preserve local-seal/holdout boundary | 262-181 | COVERED | No opening or access.
REQ | DECI-02 | Preserve containment and classifier contract | 262-181 | COVERED | Same image/controls; no fallback.
RESEARCH | — | Private result-validity essentials rather than production certification ceremony | 262-181 | COVERED | Narrow source repair and bounded proof.
CONTEXT | D-01..D-22 | Integrity, canonical runtime, privacy, accounting, and formation absence | 262-181 | COVERED | History immutable; host engine unchanged.
CONTEXT | D-34L | One exact 24-Match corrected opportunity remains | 262-181 | COVERED | No Match effect.
CONTEXT | D-34L.1 | Seven-category, no-recovery, pass-only closure | 262-181 | COVERED | New v4 trust family and no fallback.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| host canonical engine -> hostile container session | Untrusted Strategy source/input crosses into one Match-scoped container and only a bounded validated result returns. |
| framed response -> runtime accounting | Malformed, surplus, delayed, or hostile output must never become semantic evidence. |
| one Match -> next Match | Container state and processes must not cross the Match boundary. |
| reviewed source -> future authority | Only exact separately committed Plan181 bytes may enter fresh preflight/review. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-181-01 | Elevation of Privilege | session adapter | critical | mitigate | Exact Docker controls, no mounts/network/shell/capabilities, and no fallback. |
| T-262-181-02 | Tampering | framed IPC | critical | mitigate | Existing encoder/parser, exact response cardinality, byte caps, and poison-on-desync tests. |
| T-262-181-03 | Denial of Service | timeout/cleanup | critical | mitigate | Per-method limits, whole-session forced removal, bounded cleanup, and unclean-evidence rejection. |
| T-262-181-04 | Information Disclosure | diagnostics/output | high | mitigate | Aggregate-only errors, bounded streams, and no raw source/memory/environment/host paths in artifacts. |
| T-262-181-05 | Repudiation | cross-Match reuse | high | mitigate | Immutable Match identity, terminal session state, distinct container identity, and lifecycle tests. |
| T-262-181-SC | Tampering | package/image supply chain | low | accept | No install or image pull occurs; the existing exact local digest remains frozen. |
</threat_model>

<verification>RED/GREEN tests prove the hardest constraint first: one Match-scoped hostile session meets strict protocol, cleanup, timeout, isolation, and no-fallback contracts. TypeScript and source-only custody pass, all operational destinations are absent, no Docker/Match runs, and exactly 36 locks remain.</verification>

<success_criteria>
- Two focused TDD tasks and seven total files, with the private session isolated from production configuration.
- One container lifecycle per Match, strict method IPC inside the session, and destruction before the next Match.
- Canonical engine/runtime semantics, image/controls, tuple/schedule/deadlines, privacy, accounting, and current formation are unchanged.
- Fault, timeout, desync, hostile output, or uncertain cleanup cannot produce reusable evidence or fallback execution.
- Fresh v4 trust/effect paths are collision-free and absent; no opportunity is consumed.
</success_criteria>

<output>Create `262-181-SUMMARY.md` after the separately committed runnable repair; dispatch Plan182 for exactly one fresh non-consuming preflight and independent seven-category review.</output>
