# Phase 262 Corrective Successor: Source Completeness Before Fresh Authority - Research

**Researched:** 2026-08-14
**Domain:** Immutable evidence routing, offline CLI/source-completeness proof, and one-shot current-matrix reproduction
**Confidence:** HIGH

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

### Milestone-wide integrity charter
- **D-01:** Evidence is immutable and content-addressed. There is no mutable `latest`; a changed input, policy, implementation, or result creates a new branch with a new root.
- **D-02:** Missing, stale, incompatible, contaminated, incomplete, mismatched, or non-reproducible evidence fails closed. Process/integrity failure is distinct from a process-valid empirical failure and blocks authoritative progress.
- **D-03:** The exact selected canonical `MATCH_KERNEL` remains the only transition authority, and hostile Strategy source remains behind the supervised runtime-service / Runtime Broker boundary. No copied rules, alternate scheduler, or Strategy execution in the coordinator, web, API, or Go is permitted.
- **D-04:** Lab evidence remains private and unreachable from production registration, persistence, scheduling, Chronicle, replay, standings, and public/default surfaces. Only exact eligible pre-formation current-league source hashes may later enter ordinary certification.
- **D-05:** Every attempt, retry, rejection, invalid output, duplicate, failure, system failure, and unused allocation remains charged and visible in its proper evidence ledger; accepted evidence cannot omit inconvenient work.
- **D-06:** Cycle-cap, MOVE/reversal, Backstab geometry/timing, scan timing, arena, runtime, product/public, and combined-rule changes remain unavailable in v1.38.

### Exact predecessor admission
- **D-07:** Authoritative v1.38 work begins only after a machine-checked join resolves the v1.37 audit, exact archive commit, annotated `v1.37` tag, independent post-tag result, and exact selected semantic/runtime authority tuple. Copied labels are not authority.
- **D-08:** The immutable v1.37 archive commit remains the release authority. Later non-semantic corrections are recorded as separate lineage and may inform current correctness checks, but they do not move, recreate, or silently reinterpret the tag or archived evidence.
- **D-09:** Any failed join, stale certificate, incompatible identity, semantic drift, or unexplained reproduction mismatch emits an explicit stop disposition back to the integrity foundation. Phase 262 must not normalize or repair the predecessor inside v1.38.
- **D-10:** Reproduce the persisted current-rules matrix's declared shape and expected results through the selected canonical kernel and supervised execution path. The audit script's `new Function` loader is historical reproduction material, not an execution mechanism to reuse. Starter and Advanced Strategies remain smoke, regression, and throughput fixtures only.

### Frozen measurement and budget contract
- **D-11:** Before inspecting candidate output, freeze one immutable contract for the primary estimand—separately adapted formation-specific metagames under the fixed factory—and the bracket/current, inward/current, and bracket/inward contrasts. Fixed-policy transfer is explicitly secondary screening.
- **D-12:** The contract fixes complete cells, scoring, draw value, both sides, both entrant-level initiative states, semantically distinct design arenas, all splits and opponent fields, matched root-seed blocks, stopping and response admission, finalist eligibility/cardinality, portfolio selection, robust-pure selection, and permitted claims.
- **D-13:** Freeze a multi-resource opportunity vector rather than a single fungible compute scalar: attempted candidates, accepted response slots and unfilled-slot disposition, response rounds, search/teacher/distillation work, Matches, model attempts/tokens, human effort/submissions, replay review, cache policy, retries, hardware class, runtime, source, objective, memory, and output limits.
- **D-14:** Contained profile-neutral calibration spikes may refine structural work units, denominators, retry/burn rules, and starting numeric gates before search. Direct Strategy work must be distinguished from provider, orchestration, and infrastructure overhead. Candidate outcomes may not influence calibration.
- **D-15:** The activation prompt's 64 KB hard source cap, preferred under-48 KB target, below-5 ms p99 starting target, population/core/finalist counts, response thresholds, probe threshold, red-team threshold, and Advanced-library regression threshold are calibration inputs. Any replacement value and its exact denominator must be justified and frozen before candidate output is inspected.
- **D-16:** Zero accepted runtime violations, system failures, legal-information violations, private-data leaks, missing cells, duplicate/conflicting results, or unproved identity joins is a hard evidence condition. System-failed work remains charged but can never be converted into gameplay or an accepted cell.
- **D-17:** Metric code, canonicalization, normalization, denominators, replication treatment, materiality thresholds, hard versus compensating gates, classifier fixtures, response/finalist rules, and report language are hashed with the contract. A composite result cannot override a mandatory integrity or rejection gate.
- **D-18:** Reports maintain separate fields for `process_status`, current-rules outcome, formation rejection/pass, and holdout contamination. A valid empirical failure is publishable evidence; threshold softening, selective failure omission, or stronger-than-oracle-relative claims are not.

### Holdout custody and pre-formation containment
- **D-19:** A separately permissioned custodian and private store control the holdout preimages and commitment material. The iterative experiment coordinator receives only a profile-agnostic commitment before opening and one bounded safe receipt after the authorized batch.
- **D-20:** Holdout lineage must prove that source data, training data, prompts, caches, opponent construction, and schedule construction contain no profile-conditioned or current-trained input. Custody records the named role, authorized opening actor, access/query ledger, storage identity, safe projection, contamination response, retention, and retirement.
- **D-21:** Premature access, unauthorized query, commitment mismatch, uncertain evaluator state, or disclosure outside the frozen safe projection is contamination, not a diagnostic opportunity. It follows the precommitted invalidation/reporting path and cannot be repaired with a replacement or second holdout.
- **D-22:** Precommit the literal current-edge, full-inward, and edge-anchored-bracket coordinates, unchanged inward facings, equal-compute dimensions, telemetry, causal classifiers, and hard rejection logic now. Only profile-agnostic schemas, metric code, and synthetic positive/negative/mirrored/obfuscated fixtures may exist before the valid Phase 266 current-league freeze.

### Decision Revision: single-operator local seal (2026-08-12)
- **D-19R:** For future routing, D-19 is superseded by the operator-approved `single_operator_local_seal_v1` assurance class. One named `repository_operator` controls a restricted out-of-repository local holdout store and one closed opening command. The coordinator receives only a profile-agnostic commitment before opening and one bounded safe receipt afterward. This is process separation, not independent custody, and it makes no independent/third-party custody, separate-permissioning, non-collusion, comprehensive-host-monitoring, cryptographic-erasure, forensic-deletion, or malicious-owner-resistance claim.
- **D-20R:** For future routing, D-20 retains the profile-neutral lineage, frozen source/training/prompt/cache/opponent/schedule exclusions, tool-mediated access/query ledger, safe projection, terminal contamination, retention, and retirement requirements while naming the role `repository_operator`. Commitment-secret ingress is only `<absolute-local-seal-root>/input/commitment-secret.bin`, under owner-only `0700` ancestors as an effective-UID-owned regular non-symlink `0600` file of 32..4096 bytes. It is opened no-follow, validated, read once during commitment, zero-filled in process, unlinked, and followed by parent fsync before success; uncertainty fails closed and bytes never enter CLI, environment, logs, Git, tests, receipts, artifacts, or output.
- D-19 and D-20 remain visible above as truthful historical requirements and continue to describe the terminal Plan 262-40/42/43 branch. D-19R and D-20R supersede them only for successor Plans 262-44 through 262-48; they grant no ADMIT-03, SEAL-01, candidate-search, Phase 263, formation, holdout-opening, public, activation, or production authority.
- ADMIT-03 remains blocked and revised SEAL-01 remains pending until independently verified local-seal mechanics and one fresh literal 540/540 reproduction pass are joined exactly.

### the agent's Discretion
- Exact schema, module, command, storage, and typed-reason names are left to research and planning within the locked evidence and privacy boundaries.
- Exact budget values, materiality thresholds, classifier cutoffs, commitment primitive, encrypted-storage mechanism, and retention sampling are chosen only after the required contained Phase 262 spikes, then frozen before candidate output is inspected.
- A managed signing identity may be used if one already exists; Phase 262 must not create an ad hoc signing trust system to simulate custody.

### Deferred Ideas (OUT OF SCOPE)
- Planner and deterministic runner implementation belongs to Phase 263.
- Candidate factory, independent oracles, and quarantined intake belong to Phase 264.
- League execution and current-league freeze belong to Phases 265–266.
- Executable formation materialization, equal retraining, sealed opening, decision, certification, and release closure belong to Phases 267–270.
- Cap, MOVE, Backstab, scan-timing, arena, runtime, product, and combined-rule experiments require separately approved later work.
</user_constraints>

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| ADMIT-01 | Maintainers can begin authoritative v1.38 Strategy evaluation only when the v1.37 audit, archive commit, annotated tag, and independent post-tag checker all resolve and pass. | Preserve the already admitted predecessor roots in every new source seal, route context, terminal, and activation join. [VERIFIED: `.planning/REQUIREMENTS.md`] |
| ADMIT-02 | Every v1.38 research root resolves and records the exact selected rules, engine, runtime ABI, Chronicle, arena-catalog, Set-policy, canonical-JSON, provider, runtime, and conformance identities instead of trusting copied labels. | Reuse the route-5 complete writer/checker architecture and bind the exact existing tuple; do not reduce the new route to schema labels. [VERIFIED: codebase inspection] |
| ADMIT-03 | Researchers can reproduce the persisted current-rules audit matrix before candidate search and can use Starter and Advanced Strategies only as smoke, regression, and throughput fixtures rather than balance evidence. | Implement and independently prove a real CLI-reachable context → preflight → calibration → conditional 540-cell reproduction → terminal path before seeking fresh authority. [VERIFIED: `.planning/REQUIREMENTS.md`] |
| ADMIT-04 | A missing, stale, incompatible, or semantically drifting predecessor authority stops authoritative v1.38 work and returns the defect to the integrity foundation without repairing, normalizing, or changing canonical behavior inside this milestone. | Record Plan 262-47 as a pre-execution source-completeness failure, expire it, preserve all bytes, and create no fabricated route terminal. [VERIFIED: codebase inspection] |
| MEAS-10 | Before tuning, maintainers precommit the literal three-profile protocol, profile-agnostic cold common root, matched execution order, unchanged-rule comparison, equal-compute dimensions, common profile-agnostic holdout opponent/schedule construction, telemetry, rejection gates, holdout commitment and single-operator local sealing procedure, access/query policy, safe receipt, contamination response, and non-authorization semantics; the holdout lineage proves its source, training data, prompts, caches, and selection use no profile-conditioned or current-trained input, and profile-agnostic metric code and synthetic classifier fixtures may exist while no executable formation state or evidence is materialized. The procedure identifies `single_operator_local_seal_v1` and remains pending until its mechanics and claim boundary are implemented and independently verified. | Require the same formation-absence and policy roots in the new route and its source-completeness review. [VERIFIED: `.planning/REQUIREMENTS.md`] |
| SEAL-01 | A named repository operator controls a restricted out-of-repository local holdout store and one closed opening command. The immutable protocol binds secret-salted commitment material, pre-open freeze checks, application-level access/query events, one consumed opening before evaluation launch, bounded safe aggregate projection, terminal contamination, retention, and retirement. The evidence explicitly identifies `single_operator_local_seal_v1` and makes no independent-custody, third-party-custody, separate-permissioning, non-collusion, comprehensive-host-monitoring, cryptographic-erasure, forensic-deletion, or malicious-owner-resistance claim. | Keep the already proven v3 local-seal latch noncompensating and unchanged; the corrective work concerns only ADMIT-03 source and routing. [VERIFIED: `.planning/ROADMAP.md`] |
</phase_requirements>

## Summary

Plan 262-47 must not execute. Its A6/B6 custody is structurally valid, but A6 does not contain the live route that Plan 262-47 says Task 3 will call. At exact A6 `600c7770867e6090147914dc090780f5b63930ec`, `scripts/lib/v1-38-current-matrix-reproduction.ts` ends the route-6 implementation with a frozen contract and `checkV138Plan26247SyntheticRoute`; there are no v10 context/preflight/calibration writers, no v11 reproduction writer, no Plan-262-47 terminal builder/writer/checker, no v10/v11 direct commands, and no matching branches in `runReceiptCli`. The current tests therefore prove schema isolation and a synthetic acceptance predicate, not executable route completeness. [VERIFIED: Git/codebase inspection]

B6 `e2166736c2a1a3f1decbb1d6b3722f87945a47ea` is a sole-parent child of A6 and changes exactly the authorization-v6 and seal-v6 paths. All eight live destinations remain absent, so no preflight, calibration, reproduction, consumption marker, or authorized route terminal exists and fresh charges remain 0. The correct disposition is a distinct pre-execution source failure, not any terminal disposition reserved for a started/consumed route. The authorization-v6 and seal-v6 bytes remain immutable historical evidence, but the authorization is expired and unusable by instruction and must never be retried, repaired in place, or used to authorize replacement source. [VERIFIED: Git/artifact inspection]

The corrective successor should use a new route ordinal, new schemas, new authorization/seal bytes, and wholly exclusive destinations. Before asking the operator for those bytes, it should implement the complete route offline with TDD and pass an independent source-completeness review that proves the actual exported writers and direct CLI dispatch are reachable in a disposable fixture without calling live RSS observation, Strategy execution, or canonical repository writers. Only after that review may a fresh authorization be rendered, accepted byte-for-byte, sealed as a direct child, and consumed once from the main orchestrator. [VERIFIED: established route-5 implementation pattern]

**Primary recommendation:** Archive and terminally disqualify Plan 262-47 without forging a route terminal; implement and independently prove a complete route-7 source branch before creating any new authority; then run exactly one main-only successor and rewire Plan 262-48 to its new terminal/reproduction artifacts. [VERIFIED: codebase and locked decisions]

## Project Constraints (from AGENTS.md)

- Keep the engine pure, deterministic, serializable, and side-effect free. [VERIFIED: `AGENTS.md`]
- Do not put game rules in React components. [VERIFIED: `AGENTS.md`]
- Do not execute user Strategy code in the web/API process. [VERIFIED: `AGENTS.md`]
- Do not use `Math.random`, `Date.now`, system time, filesystem, network, or database access inside engine logic. [VERIFIED: `AGENTS.md`]
- Do not use Node `vm` as a security boundary for untrusted code. [VERIFIED: `AGENTS.md`]
- Treat Strategy code as hostile and validate every runtime boundary with schemas. [VERIFIED: `AGENTS.md`]
- Preserve canonical terminology: Soldier, Match, Phase, Round, Activation, Cycle, Action, Advance, STONE, FALLEN, Chronicle. [VERIFIED: `AGENTS.md`]
- Strategy Revisions are immutable once submitted for Match or MatchSet play. [VERIFIED: `AGENTS.md`]
- Public replay output must not expose Strategy source, StrategyMemory, SoldierMemory, or objective payloads by default. [VERIFIED: `AGENTS.md`]
- Runtime work must test invalid outputs, timeout behavior, forbidden capabilities, memory/source limits, schema validation, and the distinction between strategy failure and system failure. [VERIFIED: `AGENTS.md`]
- Keep planning documents committed when updated. [VERIFIED: `AGENTS.md`]

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Historical failure disposition and archives | Offline planning/evidence control plane | Git object store | It records immutable process truth and grants no runtime capability. [VERIFIED: codebase pattern] |
| Authorization and source sealing | Offline evidence control plane | Git object store | Exact commit/tree/parent/blob identities and sole-parent topology own authority custody. [VERIFIED: `v1-38-successor-source-seal.ts`] |
| Route CLI and terminalizer | Offline main orchestrator | Runtime-service child boundary | The orchestrator owns ordered receipts/accounting; hostile Strategy execution stays behind the existing supervised runtime boundary. [VERIFIED: `AGENTS.md`, route-5 code] |
| Matrix transitions | Strategy Execution Service / canonical engine | Offline orchestrator | The existing `MATCH_KERNEL` remains the sole transition authority; the coordinator only schedules and validates. [VERIFIED: `262-CONTEXT.md`] |
| Activation join | Offline evidence control plane | Planning status carriers | It joins two exact noncompensating latches and grants only Phase 263 planning authority. [VERIFIED: `262-48-PLAN.md`] |

## Standard Stack

### Core

| Library/tool | Workspace version | Purpose | Why Standard |
|--------------|-------------------|---------|--------------|
| Node.js | 24.15.0 installed | Direct TypeScript CLI host and durable filesystem primitives | Existing route tooling runs with Node and `tsx`; no new runtime is required. [VERIFIED: local environment] |
| TypeScript | `^6.0.3` | Typed schemas, receipts, writers, and terminal branches | Existing implementation language and monorepo typecheck contract. [VERIFIED: `package.json`] |
| `tsx` | `^4.22.0` | Execute TypeScript CLI entry points | Existing commands use `node --import tsx` or `pnpm exec tsx`. [VERIFIED: plans/codebase] |
| Vitest | `^4.1.6` | TDD, mutation tests, subprocess/fixture proof | Existing phase tests and supported bounded execution flags. [VERIFIED: `package.json`] |
| Git | 2.50.1 installed | Commit/tree/parent/blob custody and byte-preserving archive proof | Existing seals derive authority from full Git identities. [VERIFIED: local environment] |

### Supporting

| Library/tool | Version | Purpose | When to Use |
|--------------|---------|---------|-------------|
| Existing canonical JSON and identity helpers | repository-owned | Byte-stable receipts and domain-separated roots | Every new artifact and review manifest. [VERIFIED: codebase inspection] |
| Existing `writeV138ImmutableReceipt` / `writeV138Plan26219Immutable` | repository-owned | Exclusive, fsynced, collision-refusing publication | Every disposable test write and eventual authorized route write. [VERIFIED: codebase inspection] |
| Existing protocol-v2 child boundary | repository-owned | Bounded closed child result frames and privacy-safe classification | Calibration and reproduction child execution. [VERIFIED: codebase inspection] |

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Complete real writers plus CLI registration | Expand `checkV138Plan26247SyntheticRoute` | Rejected: it can validate fabricated objects but cannot prove any live command reaches a writer or terminalizer. [VERIFIED: codebase inspection] |
| Fresh route ordinal and destinations | Repair A6/B6 or reuse absent v10/v11 paths | Rejected: source changed after authorization and the prior authority is expired/unusable; reuse would violate immutable-branch and no-retry rules. [VERIFIED: locked task constraint and D-01/D-02] |
| Distinct pre-execution failure record | Write `v1.38-plan-262-47-terminal-v1.json` | Rejected: no authorized route stage was consumed, so a terminal would claim a state transition that never occurred. [VERIFIED: artifact absence and route-5 terminal semantics] |

**Installation:** none. This corrective sequence must install no package and must use the frozen workspace lockfile. [VERIFIED: existing stack suffices]

## Package Legitimacy Audit

No external package is added or installed by the recommended correction, so the package-legitimacy gate is not applicable. [VERIFIED: recommended scope]

## Exact Failure Diagnosis

| Expected Plan-262-47 capability | Present in A6? | Evidence |
|---------------------------------|----------------|----------|
| v10 execution-context builder/checker/writer | No | No matching symbol or direct command exists; route 5 has `build/check/writeV138ExecutionContextV9Receipt`. [VERIFIED: codebase inspection] |
| v10 headroom-preflight builder/checker/writer | No | No matching symbol or CLI branch exists; route 5 has the full V9 equivalents. [VERIFIED: codebase inspection] |
| v10 calibration builder/checker/writer | No | Only the synthetic validator accepts an already-built calibration object. [VERIFIED: codebase inspection] |
| v11 reproduction builder/checker/writer | No | Only a 540-object synthetic predicate exists. [VERIFIED: codebase inspection] |
| Consumption-marker writer/checker | No | Route-6 contract names a schema, but implements no marker lifecycle. [VERIFIED: codebase inspection] |
| Plan-262-47 disposition union and terminal evidence | No | No source symbols implement its failure/pass branches. [VERIFIED: codebase inspection] |
| Plan-262-47 terminal writer/checker CLI | No | `V138_RECEIPT_DIRECT_COMMANDS` stops at Plan 262-30 and `runReceiptCli` has no 262-47 branch. [VERIFIED: codebase inspection] |
| Real CLI reachability test | No | The five tests cover constants, Git identity, rendering, mutation, and synthetic acceptance only. [VERIFIED: `evaluate-v1-38-successor-route.test.ts`] |

The earlier source review's statement “PASS — zero findings in the additive v6/v10/v11 pre-authorization source” is therefore overbroad. It truthfully checked the files it named and no live destinations were written, but its checklist did not require a symbol-by-symbol writer/terminalizer inventory or a subprocess/direct-entry reachability proof. Preserve the review bytes as historical evidence; do not rewrite its verdict. [VERIFIED: `262-47-REVIEW.md` and code comparison]

## Architecture Patterns

### System Architecture Diagram

```text
Plan 262-47 A6/B6 + zero live destinations
  -> pre-execution source-failure disposition (NOT route terminal)
  -> byte-preserving archives + expired/unusable authority root
  -> offline route-7 RED tests
  -> complete context/preflight/calibration/reproduction/terminal source
  -> independent source-completeness review
       | failure -> no authorization; return to offline implementation
       ` zero findings + real CLI reachability, no live writes
  -> fresh exact operator literal
  -> direct-child B7 authorization/seal only
  -> main-orchestrator exclusive execution checkpoint
       -> context:v11
       -> preflight:v11
       -> conditional calibration:v11
       -> conditional reproduction:v12 (540 cells)
       -> exactly one Plan-262-57 terminal
  -> rewired Plan 262-48 two-latch join
       | ADMIT-03 not literal pass -> gaps_found, no activation root
       ` ADMIT-03 pass + local-seal v3 pass -> Phase 263 planning only
```

### Recommended Project Structure

```text
scripts/
├── lib/v1-38-current-matrix-reproduction.ts    # additive route-7 live path and terminalizer
├── lib/v1-38-successor-source-seal.ts           # additive authorization-v7 / seal-v7 custody
├── evaluate-v1-38-successor-route.test.ts       # contracts, synthetic mutation coverage
└── evaluate-v1-38-successor-source-complete.test.ts # disposable-repo CLI reachability proof
.planning/phases/.../
├── archived/262-47-HISTORICAL.md                # exact original bytes
├── archived/262-48-HISTORICAL.md                # preserve pre-rewire bytes
├── 262-53-PLAN.md                               # disposition/archive/tracking
├── 262-54-PLAN.md                               # offline TDD implementation
├── 262-55-PLAN.md                               # independent source review
├── 262-56-PLAN.md                               # fresh authorization/seal only
├── 262-57-PLAN.md                               # one main-only live route
└── 262-48-PLAN.md                               # rewired activation join
```

### Pattern 1: Separate Process Failure From Route Terminal

**What:** Publish an additive, non-authorizing pre-execution disposition that binds A6, B6, authorization/seal roots, exact absence of all eight live destinations, `routeStarted:false`, fresh charged/accepted counts `0/0`, `authorityExpired:true`, `noRetry:true`, and a closed reason such as `sealed_source_incomplete`. It must explicitly state `isRouteTerminal:false` and `satisfiesAdmit03:false`. [VERIFIED: locked evidence model]

**When to use:** A fatal problem is discovered after authority/seal creation but before any execution-context or consumption marker is written. [VERIFIED: current state]

**Do not:** Call any Plan-262-47 terminal builder, infer a terminal disposition, or create the canonical terminal path. The absence of the terminal is itself part of the truthful record. [VERIFIED: current artifact inventory]

### Pattern 2: Complete Additive Route Mirroring

**What:** Mirror route 5's complete capability surface under new names and schemas, then bind it to new authorization/seal custody. The minimum implementation surface is: route contract/dispositions/paths; authority and B7 custody resolution; Pattern-C ownership; context build/check/write; preflight build/check/write; marker build/check/write; calibration build/check/write; reproduction build/check/write; publication-route/prerequisite checks; obstruction/interruption/pre-observation proofs; terminal evidence/build/check/write/branch; direct-command allowlist entries; and `runReceiptCli` parsers for every command. [VERIFIED: route-5 code inventory]

**When to use:** The successor has a fresh route ordinal and must be executable from the same main entry point as earlier successful route attempts. [VERIFIED: codebase pattern]

### Pattern 3: Source Completeness Is a Reachability Proof

**What:** Build a closed source-completeness manifest listing every command, exported writer/checker, target path, prerequisite, side-effect class, and terminal branch. Test it in a disposable Git repository by invoking the real direct entry/CLI parsing with injected fake headroom and child-runner dependencies. The proof may write only disposable fixture destinations and must assert the real workspace's canonical destinations remain absent byte-for-byte. [VERIFIED: internal architecture; injection is recommended from official Vitest guidance]

**When to use:** Before review completion and before rendering any authorization literal. [VERIFIED: corrective ordering]

**Example:**

```typescript
// Source: established route-5 pattern in scripts/lib/v1-38-current-matrix-reproduction.ts
const ROUTE_7_COMMANDS = Object.freeze({
  context: "--write-execution-context-v11-receipt",
  preflight: "--write-headroom-preflight-v11-receipt",
  calibration: "--calibrate-parallel-v11-receipt",
  reproduction: "--write-authoritative-v12-receipt",
  terminalWrite: "--write-plan-262-57-terminal-v1",
  terminalCheck: "--check-plan-262-57-terminal-v1",
})

// Review must prove every value is registered and reaches its real handler.
```

### Pattern 4: Exclusive Durable Publication

**What:** Continue using the existing immutable receipt publisher. It creates an exclusive temporary file, fsyncs bytes, verifies the persisted bytes, links to an absent destination, fsyncs the directory, and refuses collision/replacement. [VERIFIED: `writeV138ImmutableReceipt`]

Node's official filesystem documentation also recommends opening directly rather than an `access()`-then-open race, defines `wx`/`O_EXCL` as fail-if-present, and documents flush/fsync behavior. [CITED: https://nodejs.org/api/fs.html]

### Anti-Patterns to Avoid

- **Schema-only implementation:** A route constant plus synthetic predicate is not a writer/terminalizer. [VERIFIED: current failure]
- **Review by assertion count:** Passing tests are irrelevant if no test invokes the real command-to-handler path. [VERIFIED: current failure]
- **Repairing sealed source:** Adding writers to A6 changes source bytes after the operator authorized them. Create A7/B7 instead. [VERIFIED: D-01 and artifact custody]
- **Reusing empty destinations:** Absence does not make v10/v11 paths unclaimed; authorization-v6 already bound them. New authority requires new exclusive destinations. [VERIFIED: authorization-v6]
- **Forged terminal:** A canonical terminal cannot truthfully describe a route that never started. [VERIFIED: current artifact inventory]
- **Automatic execution after code review:** Source review and authority/seal remain separate gates; live work stays a main-only checkpoint. [VERIFIED: established plan pattern]

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Canonical JSON and roots | New serializer/hash format | Existing canonical JSON and domain-separated identity helpers | Prevents identity drift. [VERIFIED: codebase] |
| Durable exclusive publication | `existsSync` then `writeFileSync` | Existing immutable receipt publisher | Avoids replacement and TOCTOU behavior. [VERIFIED: codebase; Node docs] |
| Child failure protocol | Free-form stdout/stderr JSON | Existing protocol-v2 decoder/reducer | Preserves closed taxonomy and privacy bounds. [VERIFIED: codebase] |
| Runtime execution | Direct Strategy import or `new Function` | Existing supervised runtime-service path | Required by D-03/D-10 and AGENTS.md. [VERIFIED: project constraints] |
| Source-completeness evidence | Reviewer prose alone | Machine-readable command/handler/path manifest plus subprocess/fixture tests | Makes omissions detectable before authority. [VERIFIED: lesson from A6] |
| Git custody | Caller-supplied abbreviated OIDs | Full derived commit/tree/parent/blob equality | Prevents spoofing and ambiguity. [VERIFIED: existing seal code] |

**Key insight:** This correction is not primarily a game/runtime repair. It is a capability-completeness and evidence-ordering repair: executable source must exist and be independently reachable before irreversible authority is requested. [VERIFIED: codebase diagnosis]

## Runtime State Inventory

| Category | Items Found | Action Required |
|----------|-------------|-----------------|
| Stored data | None — the route artifacts are Git-tracked files; no database row was created by Plan 262-47. [VERIFIED: plan/artifact inventory] | No data migration. |
| Live service config | None — no route command, Strategy, Match, provider, or service was invoked. [VERIFIED: all live destinations absent] | No service mutation. |
| OS-registered state | None — no process registration or daemon is part of this route. [VERIFIED: code/plan inspection] | No OS action. |
| Secrets/env vars | None for this correction; local-seal secret ingress belongs to the already proven separate SEAL-01 path and must not be opened. [VERIFIED: context and scope] | Do not access local seal input. |
| Build artifacts / installed packages | Existing workspace dependencies only; no new package is required. [VERIFIED: package/config inspection] | Use frozen lockfile; disposable fixture/worktree cleanup only. |

## Common Pitfalls

### Pitfall 1: Treating B6 Custody PASS as Executability

**What goes wrong:** The checker proves exact A6/B6 topology and bytes, so execution is assumed safe even though the sealed source lacks handlers. [VERIFIED: current checker result]

**Why it happens:** Custody proof answers “what bytes were authorized?”; it does not answer “do those bytes implement every promised capability?” [VERIFIED: architecture distinction]

**How to avoid:** Require the separate source-completeness manifest and real CLI-reachability proof before authorization rendering. [VERIFIED: recommendation]

**Warning signs:** Route schemas are present but searches find no writer/checker symbols or direct commands. [VERIFIED: current source]

### Pitfall 2: Expiring Authority by Inventing a Terminal

**What goes wrong:** A terminal file is created merely to represent the pre-execution discovery, falsely implying the authorized route entered its state machine. [VERIFIED: risk analysis]

**How to avoid:** Use a separate non-terminal disposition and archive; bind destination absence and 0/0 charges. [VERIFIED: recommendation]

### Pitfall 3: Reusing v10/v11 Names Because Files Are Absent

**What goes wrong:** A new implementation writes paths already named in authorization-v6, creating ambiguous lineage or apparent reuse. [VERIFIED: authorization-v6 destinations]

**How to avoid:** Route ordinal 7 uses context/preflight/calibration v11, reproduction v12, authorization/seal v7, and Plan-262-57 markers/terminal. [VERIFIED: recommended branch design]

### Pitfall 4: “Dry Run” That Bypasses the CLI

**What goes wrong:** Unit tests call builders directly, while the command allowlist/parser still omits them. [VERIFIED: A6 failure mode]

**How to avoid:** Spawn the real entry point in a disposable fixture and assert each exact command reaches its injected real handler and target resolver. Vitest documents module mocking, but also warns that same-file internal calls cannot be replaced from outside; design explicit dependency injection at the CLI seam rather than depending on a fragile spy. [CITED: https://main.vitest.dev/guide/mocking/modules]

### Pitfall 5: Review Fixes After Authorization

**What goes wrong:** A review finds a missing branch after the operator literal is created, tempting an in-place source repair. [VERIFIED: current history]

**How to avoid:** Every finding returns to 262-54, creates a new candidate/A7, and invalidates the previous review result. Authorization is rendered only after zero findings. [VERIFIED: recommended order]

## Code Examples

### Source-Completeness Manifest Check

```typescript
// Source: recommended additive pattern based on route-5 command dispatch
type RouteCommandProof = Readonly<{
  command: string
  handler: string
  canonicalDestination: string
  liveSideEffect: "none" | "headroom" | "child-execution"
}>

export const checkRoute7SourceCompleteness = (
  manifest: readonly RouteCommandProof[],
) => {
  const expected = Object.values(ROUTE_7_COMMANDS)
  if (manifest.length !== expected.length) throw new TypeError("ROUTE_7_SOURCE_INCOMPLETE")
  for (const command of expected) {
    if (!manifest.some((entry) => entry.command === command)) {
      throw new TypeError("ROUTE_7_COMMAND_UNREACHABLE")
    }
  }
  return true
}
```

### Safe Pre-Execution Disposition Shape

```typescript
// Source: recommended evidence shape; distinct from every route terminal schema
const disposition = {
  schemaVersion: "v1.38-plan-262-47-pre-execution-source-failure-v1",
  reason: "sealed_source_incomplete",
  sourceA6,
  sourceB6,
  authorizationRoot,
  sealRoot,
  routeStarted: false,
  isRouteTerminal: false,
  chargedAttemptCount: 0,
  acceptedCellCount: 0,
  authorityExpired: true,
  noRetry: true,
  satisfiesAdmit03: false,
}
```

### Bounded Sequential Validation

```bash
pnpm exec vitest run \
  scripts/evaluate-v1-38-successor-route.test.ts \
  scripts/evaluate-v1-38-successor-source-complete.test.ts \
  --pool=forks --maxWorkers=1 --no-file-parallelism \
  --testTimeout=120000 --bail=1
```

Vitest documents `--no-file-parallelism`, `maxWorkers`, and bounded `testTimeout`; the repository's earlier route reviews also use serialized runs because these tests are resource-sensitive. [CITED: https://vitest.dev/config/fileparallelism.html] [CITED: https://main.vitest.dev/config/maxworkers]

## Exact Recommended Plan Split

### Plan 262-53 — Dispose and Archive the Unexecutable 262-47 Branch

- Create a non-terminal pre-execution source-failure artifact binding exact A6/B6 custody, authorization/seal roots, reason `sealed_source_incomplete`, eight live-destination absences, 0 charged, 0 accepted, `routeStarted:false`, `authorityExpired:true`, `noRetry:true`, and every downstream authority false. [VERIFIED: recommended evidence contract]
- Copy `262-47-PLAN.md` byte-for-byte to `archived/262-47-HISTORICAL.md`, record SHA-256, remove it from active execution, and create no `262-47-SUMMARY.md`. [VERIFIED: existing archive convention]
- Preserve authorization-v6, seal-v6, source review, A6/B6 commits, all prior v5-v9 charges/terminals, policy/local-seal roots, gameplay/privacy/formation-absence facts, and all absent v10/v11 destinations. [VERIFIED: locked scope]
- Before editing active Plan 262-48, preserve its current bytes as `archived/262-48-HISTORICAL.md` with SHA-256. Rewire the active 262-48 dependency/input contract to the future Plan-262-57 route without granting authority. [VERIFIED: byte-preservation requirement]
- Update ROADMAP/STATE/validation/verification truth to `source_incomplete_pre_execution`, ADMIT-03 blocked, revised SEAL-01 still passed reduced-assurance, Phase 262 incomplete. [VERIFIED: current two-latch status]

### Plan 262-54 — Implement the Complete Offline Route-7 Source Through TDD

- Add failing tests first for every route capability listed in the diagnosis table, including all terminal branches, markers, exclusive collisions, prior-history mutations, cleanup, privacy, and formation absence. [VERIFIED: AGENTS/testing pattern]
- Implement additive authorization-v7/seal-v7 and route-7 schemas. Recommended destinations are authorization-v7, seal-v7, context:v11, preflight:v11, calibration:v11, reproduction:v12, and Plan-262-57 consumption/terminal paths; none may overlap Plan 262-47 destinations. [VERIFIED: D-01 and authorization-v6]
- Mirror the complete route-5 implementation surface and register every command in both `V138_RECEIPT_DIRECT_COMMANDS` and `runReceiptCli`. [VERIFIED: route-5 implementation]
- Use injected fake observer/runner and a disposable Git repository to execute all CLI branches; never invoke live RSS sampling, provider/runtime Strategy execution, or the canonical workspace writer. [VERIFIED: corrective scope]
- Produce a machine-readable source-completeness manifest/checker, but no review verdict, authorization, seal, or live artifact. [VERIFIED: ordering requirement]

### Plan 262-55 — Independent Source-Completeness Review and Freeze A7

- Independently inventory and verify each actual export, command allowlist entry, parser branch, writer, checker, marker, terminal disposition, and exclusive destination. [VERIFIED: lesson from A6]
- Run the real direct CLI in a disposable fixture for every command and prove handler reachability, argument rejection, collision refusal, terminal branch selection, bounded stdout, and zero canonical/live writes. [VERIFIED: recommended proof]
- Require exact source base, commit/tree/parents, changed-path allowlist, blobs, full protected history including A6/B6 and the pre-execution disposition, and exact zero findings. [VERIFIED: prior deep-review pattern]
- Any finding returns to 262-54 and creates a new candidate; only a zero-finding review commit becomes A7. [VERIFIED: immutable review convergence]

### Plan 262-56 — Obtain Fresh Authority and Seal Direct-Child B7 Only

- Render the exact complete Plan-262-57 authorization-v7 literal only from zero-finding A7. It must bind the new source-completeness review root, A6/B6 failure disposition, all 40 historical charged calibration identities, every prior authorization byte including v6, unchanged 200 ms / 2,500-bp / 8-attempt / 4-shard / conditional-540 policy, exact semantic/runtime/privacy/gameplay/formation roots, and the new exclusive destinations. [VERIFIED: existing authorization pattern]
- Stop at a blocking human checkpoint for the complete byte-for-byte literal. Hashes, approval words, reconstructed values, and the Plan-262-47 literal are invalid. [VERIFIED: prior checkpoint pattern]
- In a detached worktree at A7, write exactly authorization-v7 and seal-v7, commit them as sole-parent B7, prove exact two-path topology and working/blob equality, and perform no live work. [VERIFIED: B4/B5/B6 pattern]

### Plan 262-57 — Consume Once From the Main Orchestrator and Terminalize

- Require clean main, no active helpers/executors, exact checked A7/B7, fresh authority bytes, and absence of every route-7 live destination immediately before execution. [VERIFIED: Pattern-C plans]
- Write context:v11, consume preflight marker then one 200 ms preflight, conditionally consume calibration marker then one 8/4 calibration, and only on exact admission consume reproduction marker then one 540-cell reproduction:v12. [VERIFIED: frozen policy]
- Terminalize every reached branch exactly once, including pre-observation failure, fresh-destination obstruction, consumed-stage interruption, preflight unavailable/refused, calibration stopped, reproduction stopped, or literal reproduction pass. All reached work remains charged; authority expires permanently; no retry and no partial evidence reuse. [VERIFIED: route-5 terminal model]
- Create a summary only after the read-only terminal checker passes. [VERIFIED: existing execution pattern]

### Rewired Plan 262-48 — Exact Two-Latch Join

- Depend on Plan 262-57 (and retain the exact Plan-262-52 local-seal v3 proof) rather than Plan 262-47. [VERIFIED: corrective dependency]
- Read only reproduction:v12 plus Plan-262-57 terminal for ADMIT-03; explicitly reject v11/Plan-262-47 terminal absence or pre-execution disposition as an admission latch. [VERIFIED: noncompensation rule]
- If Plan 262-57 is anything other than literal 540/540 pass, keep the activation artifact absent and record `gaps_found`. If both exact latches pass, grant only Phase 263/candidate-factory planning authority; formation, holdout opening, public, activation beyond the named capability, and production remain false. [VERIFIED: `262-48-PLAN.md` intent]

## State of the Art

| Old Approach | Corrective Approach | When Changed | Impact |
|--------------|---------------------|--------------|--------|
| Schema/constants plus synthetic route validator accepted as source readiness | Command-handler-path-terminal completeness manifest plus disposable real CLI reachability | Corrective successor after A6 inspection | Prevents authority from sealing non-executable source. [VERIFIED: current diagnosis] |
| One source review combined structural and executability claims | Separate code review and source-completeness review | Corrective successor | Review must prove real capabilities, not infer them. [VERIFIED: recommendation] |
| Plan 262-47 owns v10/v11 route | Archived failed pre-execution branch; fresh route 7 owns new destinations | Corrective successor | Preserves old bytes and eliminates ambiguous reuse. [VERIFIED: D-01/D-02] |

**Deprecated/outdated:**

- Plan 262-47's execution Task 3 is unusable and must be archived without summary or terminal. [VERIFIED: source inspection]
- Authorization-v6 must never be accepted by a future writer/checker. [VERIFIED: locked task constraint]
- `checkV138Plan26247SyntheticRoute` may remain as historical unit coverage but cannot count as route completeness or ADMIT-03 evidence. [VERIFIED: function semantics]

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | The recommended successor uses route ordinal 7 with v11/v12 receipt generations and Plan 262-57 terminal names. [ASSUMED] | Plan split | Names are discretionary; planner may choose different fresh names if all exclusivity and lineage requirements remain exact. |
| A2 | The pre-execution disposition reason is named `sealed_source_incomplete`. [ASSUMED] | Failure disposition | Naming is discretionary; semantics must remain non-terminal, 0/0, expired, and non-authorizing. |

## Open Questions

1. **Should active 262-48 retain its number after archival?**
   - What we know: the user explicitly requested rewiring 262-48, and its current bytes must be preserved. [VERIFIED: task request]
   - What's unclear: whether the planner prefers an active rewritten 262-48 or a new activation successor number.
   - Recommendation: archive current bytes as `archived/262-48-HISTORICAL.md`, then retain active `262-48-PLAN.md` as the rewired two-latch join to honor the requested name. [ASSUMED]

2. **Exact fresh schema/version names**
   - What we know: v10/v11 and authorization/seal v6 are already bound by the unusable branch. [VERIFIED: authorization-v6]
   - What's unclear: naming is explicitly agent discretion.
   - Recommendation: route ordinal 7, authorization/seal v7, context/preflight/calibration v11, reproduction v12, and Plan-262-57 markers/terminal. [ASSUMED]

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|-------------|-----------|---------|----------|
| Node.js | TypeScript CLI/tests | ✓ | 24.15.0 | — |
| pnpm | Workspace test/typecheck | ✓ | 11.1.2 | — |
| Git | A7/B7 custody and archives | ✓ | 2.50.1 (Apple Git-155) | — |
| Vitest | Source-completeness TDD | ✓ | workspace `^4.1.6` | — |
| PostgreSQL/Docker | Not required for source-completeness proof | not probed | — | Keep this correction offline; run broader boundary monitors only if their existing contract requires an owned disposable DB. |

**Missing dependencies with no fallback:** none. [VERIFIED: environment audit]

**Missing dependencies with fallback:** none. [VERIFIED: environment audit]

## Validation Architecture

### Test Framework

| Property | Value |
|----------|-------|
| Framework | Vitest `^4.1.6` [VERIFIED: `package.json`] |
| Config file | workspace/package configuration; no new framework config required [VERIFIED: repository inspection] |
| Quick run command | `pnpm exec vitest run scripts/evaluate-v1-38-successor-route.test.ts scripts/evaluate-v1-38-successor-source-complete.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=120000 --bail=1` |
| Full suite command | focused route/source-completeness/protocol tests, `pnpm turbo typecheck --concurrency=1`, boundary monitors when required, and `git diff --check` |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| ADMIT-03 | Every new route command reaches a real writer/checker/terminal branch without live effects in review | subprocess + integration | quick run command above | ❌ Wave 0 |
| ADMIT-03 | Exact admitted 8/4 calibration gates exact 540 unique accepted cells | unit + integration | successor route and source-completeness suites | partial — synthetic test exists; real route test ❌ |
| ADMIT-04 | Pre-execution failure records 0/0, expiry/no-retry, destination absence, and no terminal | unit + artifact checker | `pnpm exec vitest run scripts/evaluate-v1-38-successor-source-complete.test.ts --maxWorkers=1` | ❌ Wave 0 |
| ADMIT-01/02 | New route binds exact predecessor and semantic/runtime tuple | mutation + custody integration | successor route suite | ❌ route-7 cases |
| MEAS-10 | Formation artifacts remain absent and policy/classifier roots unchanged | boundary mutation | successor route suite + dependency boundary checker | partial — existing monitor; route-7 binding ❌ |
| SEAL-01 | Existing local-seal v3 proof remains exact and noncompensating | regression | `pnpm exec vitest run scripts/verify-v1-38-local-seal.test.ts --maxWorkers=1` | ✅ |

### Required Source-Completeness Assertions

1. The expected command set equals the registered direct-command set for route 7. [VERIFIED: recommended invariant]
2. Every command reaches the correct `runReceiptCli` parser branch. [VERIFIED: A6 gap]
3. Every parser branch calls the named real exported writer/checker, not a synthetic validator. [VERIFIED: A6 gap]
4. Every writer resolves exactly one exclusive canonical destination and refuses presence/symlink/path alias. [VERIFIED: existing publication contract]
5. Every stage writes its consumption marker before invoking the injected live dependency. [VERIFIED: D-05]
6. Every possible reached state maps to exactly one checked terminal disposition. [VERIFIED: terminal model]
7. The disposable proof observes no canonical workspace byte changes and invokes no real headroom/child/runtime dependency. [VERIFIED: corrective boundary]
8. Mutation of any A6/B6/history/policy/runtime/privacy/gameplay/formation root fails before any sink is invoked. [VERIFIED: D-01/D-02]

### Sampling Rate

- **Per task commit:** quick serialized source-completeness suite plus typecheck for affected package. [VERIFIED: recommended workflow]
- **Per wave merge:** full focused route/protocol/source-completeness suite and monorepo typecheck. [VERIFIED: prior route proof pattern]
- **Phase gate:** fresh authority may be rendered only after independent zero-finding source-completeness review; live execution may begin only after checked B7 in the main orchestrator. [VERIFIED: corrective ordering]

### Wave 0 Gaps

- [ ] `scripts/evaluate-v1-38-successor-source-complete.test.ts` — real CLI/handler/writer/terminal reachability in a disposable Git fixture.
- [ ] Add route-7 writer/checker/terminal mutation cases to `scripts/evaluate-v1-38-successor-route.test.ts`.
- [ ] Add pre-execution Plan-262-47 disposition checker tests.
- [ ] Add exact active-route destination non-overlap test against all v5-v11 historical paths.
- [ ] Add activation regression proving the pre-execution disposition and authorization-v6 cannot satisfy ADMIT-03.

## Security Domain

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | no | No user authentication is changed; operator authority is evidence custody, not product auth. [VERIFIED: scope] |
| V3 Session Management | no | No session state is introduced. [VERIFIED: scope] |
| V4 Access Control | yes | Exact capability-specific authorization, full Git custody, one-use expiry, main-only execution, and downstream authority denials. [VERIFIED: plan architecture] |
| V5 Input Validation | yes | Closed schemas, exact records, canonical paths, full OIDs, bounded CLI args/stdin/stdout, and mutation tests. [VERIFIED: existing patterns] |
| V6 Cryptography | yes | Existing SHA-256 domain-separated identity helpers only; do not invent a signing or hashing system. [VERIFIED: D-01 and discretion limits] |

### Known Threat Patterns for the Route Tooling

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Sealed-but-incomplete source | Elevation of Privilege | Source-completeness manifest plus actual CLI reachability before authority. [VERIFIED: current finding] |
| Old authorization accepted by new writer | Spoofing | New schema/route ordinal/destination set and explicit rejection of authorization-v6. [VERIFIED: D-01/D-02] |
| Destination overwrite or alias | Tampering | Canonical path equality, no-follow checks, exclusive durable publisher, collision mutations. [VERIFIED: existing writer] |
| Uncharged work after marker omission | Repudiation | Marker-before-effect ordering and interruption terminal tests. [VERIFIED: D-05] |
| Raw child/runtime output escape | Information Disclosure | Existing protocol-v2 bounded projection and privacy scans. [VERIFIED: project constraints] |
| Helper/concurrent live execution | Elevation of Privilege | Main-only Pattern-C ownership check immediately before first write. [VERIFIED: established plan pattern] |

## Sources

### Primary (HIGH confidence)

- `AGENTS.md` — project constraints and testing requirements.
- `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `262-CONTEXT.md` — scope, locked decisions, requirement status, and two-latch dependency.
- `262-47-PLAN.md`, `262-48-PLAN.md`, and `262-47-REVIEW.md` — promised route, activation join, and prior review claims.
- Authorization-v6, seal-v6, Git commits A6/B6, and live-destination inventory — exact custody and zero-live-work state.
- `scripts/lib/v1-38-current-matrix-reproduction.ts` — route-5 complete implementation versus route-6 synthetic-only source.
- `scripts/lib/v1-38-successor-source-seal.ts` — v6 authorization/seal and custody behavior.
- `scripts/evaluate-v1-38-successor-route.test.ts` — actual A6 test coverage.
- Plans/reviews 262-24, 262-25, 262-28, 262-29, 262-30, and 262-31 — earlier route implementation, review, seal, execution, and terminal patterns.

### Secondary (MEDIUM confidence)

- [Node.js filesystem documentation](https://nodejs.org/api/fs.html) — exclusive creation, fsync/flush, and race avoidance.
- [Vitest module mocking documentation](https://main.vitest.dev/guide/mocking/modules) — injection/mocking behavior and same-file limitations.
- [Vitest file parallelism](https://vitest.dev/config/fileparallelism.html) and [maxWorkers](https://main.vitest.dev/config/maxworkers) — serialized bounded test controls.

### Tertiary (LOW confidence)

- None.

## Metadata

**Confidence breakdown:**
- Failure diagnosis: HIGH — exact source, Git objects, artifacts, and destination absence were inspected.
- Architecture: HIGH — mirrors the repository's complete route-5 implementation and immutable evidence rules.
- Plan split: HIGH for ordering and boundaries; MEDIUM for discretionary fresh schema/file names.
- Pitfalls: HIGH — each major pitfall is demonstrated by the A6/B6 sequence or an existing successful route pattern.

**Research date:** 2026-08-14
**Valid until:** 2026-09-13, or immediately stale if any A6/B6 byte, Plan 262-47/48 byte, route source, or planning status changes.
