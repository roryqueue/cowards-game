---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "06"
type: execute
wave: 11
depends_on: ["262-05"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-custody.ts
autonomous: false
requirements: [SEAL-01]
user_setup:
  - service: sealed-holdout-custody
    why: "A genuinely separate custodian, authorized opening actor, and approved encrypted private store cannot be inferred or created by repository code."
    dashboard_config:
      - task: "Provide opaque IDs and authorization evidence for the named custodian role, authorized one-open actor/command, approved encrypted external store, key identifier, retention policy, and retirement authority; do not provide secrets or holdout preimages."
        location: "Organization-approved custody and encrypted-storage control plane"
must_haves:
  truths:
    - "Synthetic custody mechanics enforce an outside-repository restricted store, secret-keyed commitment, append-only access/query ledger, one-open state machine, bounded safe projection, terminal contamination, and authorized retirement per D-19 through D-21."
    - "Repository code never invents a custodian, opening actor, encrypted store, managed signer, secret, salt, or preimage."
    - "The real operational identities are supplied only after admission, reproduction, calibration, classifiers, containment, and synthetic custody mechanics pass."
    - "The authorized route produces one exact bounded and authenticated handoff carrying every public field Plan 07 consumes; the stop route carries only a terminal typed disposition."
  artifacts:
    - path: "scripts/lib/v1-38-custody.ts"
      provides: "Closed custody command/state machine and safe projection"
      exports: ["createV138CustodyCommitment", "executeV138CustodyCommand", "projectV138SafeCustodyReceipt", "V138AuthorizedCustodyHandoffSchema", "verifyV138AuthorizedCustodyHandoff"]
  key_links:
    - from: "scripts/lib/v1-38-custody.ts"
      to: "scripts/lib/v1-37-restricted-evidence-store.ts"
      via: "adapted outside-repo, exclusive-write, no-follow, append-only mechanics"
      pattern: "O_NOFOLLOW"
---

<objective>
Prove the sealed-holdout custody mechanics synthetically, then pause for the genuine separately permissioned operational inputs required by D-19 through D-21.

Purpose: Establish the security and lifecycle machinery without pretending repository-local fixtures constitute real custody.
Output: A tested custody state machine plus an explicit approved-input handoff for final root construction.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-05-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and adversarially drill the closed custody state machine</name>
  <read_first>
    <file>scripts/lib/v1-37-restricted-evidence-store.ts</file>
    <file>scripts/lib/v1-37-restricted-evidence-store.test.ts</file>
    <file>packages/spec/src/public-projection-privacy.ts</file>
    <file>.planning/artifacts/v1.38-pre-search-contract.json</file>
  </read_first>
  <files>scripts/lib/v1-38-custody.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - Commit, authorize_open, open_once, safe_projection, verify, contaminate, and retire are closed commands; no generic read/query/getter exists for the coordinator.
    - Store roots inside the repo, traversal, symlinks, overwrite, oversized reads, commitment mismatch, unauthorized/premature/repeated opens, and forbidden projection keys fail closed and are logged.
    - Contamination is terminal and cannot authorize a replacement/diagnostic holdout.
    - The authorized handoff schema is exact, bounded, canonically digested, and contains no secret or private path.
  </behavior>
  <action>Add the `custody` integration/mutation suite using a restrictive external temporary directory and synthetic non-holdout bytes. Adapt v1.37 storage mechanics: outside-repo root, 0700/0600 modes, exclusive writes, bounded no-follow reads, append-only fsynced events, and content verification. Implement HMAC-SHA-256 with custodian-held secret material and secret random salt, length-checked timing-safe comparison, monotone one-open/contamination/retirement states, and a holdout-specific exact safe-projection allowlist. Define `V138AuthorizedCustodyHandoffSchema` with exact bounded fields: schema/profile ID; authorization mechanism; opaque authorization receipt ID and digest; canonical handoff digest; verifier policy ID; commitment profile and digest; opaque custodian role/store/key/trust-domain/opening-actor/opening-command/retention-policy/retirement-authority IDs; encrypted-object digest and byte length; `profileNeutralProtocolRoot`; safe-projection schema ID, maximum bytes, and maximum cardinality; and optional existing-managed-signer key/trust-domain/signature fields. Commitment creation must require and domain-bind the exact Plan 05 `profileNeutralProtocolRoot`; the authenticated handoff must carry that same value, not a separately invented lineage root. `verifyV138AuthorizedCustodyHandoff` must authenticate the canonical handoff digest against the approved control-plane receipt or an already-authorized managed signature, reject mixed/unknown mechanisms, and never create signing trust. Per D-19/D-21, expose no generic read/query path and never return secret, salt, preimage, hidden identities, raw access detail, evaluator state, decryption material, or private store path.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t custody --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Every synthetic happy-path state transition is deterministic; every access, path, commitment, projection, replay-open, and contamination mutation fails with a stable reason and durable ledger event.</acceptance_criteria>
  <done>The custody/store mechanics satisfy SEAL-01 synthetically without claiming a real custodian or store.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize the real custody identities and encrypted store</name>
  <read_first>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md</file>
    <file>.planning/artifacts/v1.38-pre-search-contract.json</file>
    <file>scripts/lib/v1-38-containment.ts</file>
  </read_first>
  <files>None — operational authorization input only; no secret or private-store material is written by this checkpoint.</files>
  <decision>Identify and authorize every field of `V138AuthorizedCustodyHandoffSchema`: the genuine separately permissioned custodian role, one-open actor/command, approved external encrypted store, key/trust domain, commitment digest/profile bound to the exact Plan 05 `profileNeutralProtocolRoot`, encrypted-object digest/length, retention/retirement authorities, safe-projection bounds, and canonical handoff authorization evidence.</decision>
  <context>These identities do not exist in repository authority. Fixture people, local temp directories, ephemeral keys, and invented encryption would violate SEAL-01 and D-19. All autonomous admission, reproduction, calibration, classifier, containment, and synthetic custody work must be green before this checkpoint is presented.</context>
  <action>Present the two fail-closed choices only after the automated preflight passes. For `authorized-existing-controls`, obtain the exact bounded schema fields from the authorized control plane, verify their canonical handoff digest against the opaque authorization receipt/verifier policy, and record the authenticated handoff verbatim in `262-06-SUMMARY.md` under `authorized_custody_handoff`; this is the sole Plan 06→07 data contract. Managed-signature fields are permitted only when the same evidence proves an existing authorized signer and trust domain; otherwise omit them and make no signature claim. Explicitly reject secrets, salts, preimages, decryption material, raw access/query logs, hidden opponent/schedule identities, evaluator state, or private paths. For `stop-no-authority`, record only `stop_no_custody_authority` plus a public-safe reason and no handoff fields. Plan 07 must validate this summary contract before consuming any field.</action>
  <options>
    <option id="authorized-existing-controls">
      <name>Use approved existing controls</name>
      <pros>Supplies real separately permissioned identities and an approved encrypted store without creating an ad hoc trust system.</pros>
      <cons>Requires opaque authorization receipts and store/control-plane evidence from outside the repository.</cons>
    </option>
    <option id="stop-no-authority">
      <name>Stop without authority</name>
      <pros>Preserves the fail-closed contract honestly when custody is unavailable.</pros>
      <cons>Blocks the Phase 262 aggregate root and all authoritative downstream work.</cons>
    </option>
  </options>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t 'custody|containment|calibration' --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The authorized response supplies and authenticates commitment digest, encrypted-object digest/length, the exact Plan 05 `profileNeutralProtocolRoot`, every opaque authority/policy ID, and safe-projection bounds without any forbidden private field; or explicitly selects the fail-closed stop.</acceptance_criteria>
  <done>The execution summary records either one schema-valid authenticated handoff sufficient for Plan 07 or the terminal stop disposition; no field, identity, control, or trust root is invented.</done>
  <resume-signal>Select `authorized-existing-controls` and provide the approved opaque identifiers/receipt references, or select `stop-no-authority`.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `createV138CustodyCommitment`, `executeV138CustodyCommand`, and `projectV138SafeCustodyReceipt` in `scripts/lib/v1-38-custody.ts`
- Synthetic external-directory custody, contamination, projection, and retirement fixtures in the shared Phase 262 test
- An approved operational-input handoff or a typed `stop_no_custody_authority` disposition; never fabricated authorization

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Coordinator → custodian command surface | The coordinator must not gain generic read/query/open authority. |
| External encrypted store → safe repository reference | Secrets, preimages, hidden identities, and evaluator internals must remain private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-20 | Spoofing | custodian/opening identity | high | mitigate | Blocking human authorization of existing separately permissioned controls; reject fixtures and ephemeral identities. |
| T-262-21 | Information Disclosure | safe projection | high | mitigate | Exact allowlist, byte/cardinality bounds, forbidden-key mutation tests, and no generic read/query API. |
| T-262-22 | Tampering | private store | high | mitigate | Outside-repo root, no-follow bounded reads, exclusive writes, digest/HMAC verification, restrictive modes. |
| T-262-23 | Repudiation | access/open lifecycle | high | mitigate | Append-only fsynced ledger, actor/command IDs, one-open ordinal, terminal contamination. |
| T-262-24 | Denial of Service | store/parser | medium | mitigate | Bounded bytes/cardinality and predeclared commands. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
Run this wave gate before presenting the checkpoint. Never continue to aggregate-root construction without approved real operational identities.
</verification>

<success_criteria>
- Synthetic custody mechanics pass every adversarial mutation.
- The real custody inputs are explicitly authorized or the phase stops honestly.
- No secret, holdout preimage, private store path, or evaluator detail enters Git.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-06-SUMMARY.md` when done.
</output>
