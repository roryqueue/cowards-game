---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "08"
type: execute
wave: 3
depends_on: ["262-02"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-current-matrix-reproduction.ts
  - .planning/artifacts/v1.38-historical-matrix-expectation.json
autonomous: true
gap_closure: true
requirements: [ADMIT-03]
must_haves:
  truths:
    - "The historical expected predicate is independently derived from immutable pre-v1.38 Git evidence and exact declared results, never from the new supervised run being checked, per D-01, D-02, and D-10."
    - "The expected predicate is non-placeholder, content-addressed, exact-keyed, mutation-detecting, and fails closed when its source commit, blob, bytes, declared results, or derivation code changes."
    - "Observed matrix outcomes cannot write, replace, normalize, or otherwise influence the persisted expectation; mismatch remains an ADMIT-03 process failure."
  artifacts:
    - path: ".planning/artifacts/v1.38-historical-matrix-expectation.json"
      provides: "Immutable independently sourced historical matrix expectation and exact declared predicate"
    - path: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      provides: "Expectation loader, source-binding validator, and independent aggregate predicate evaluator"
      exports: ["loadV138HistoricalMatrixExpectation", "evaluateV138HistoricalMatrixPredicate"]
  key_links:
    - from: ".planning/artifacts/v1.38-historical-matrix-expectation.json"
      to: ".planning/artifacts/v2.0-core-rules-audit/README.md"
      via: "pre-v1.38 Git commit, blob object, byte digest, exact declared-result extraction, and derivation-code root"
      pattern: "sourceBlobOid"
    - from: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      to: ".planning/artifacts/v1.38-historical-matrix-expectation.json"
      via: "closed-schema validation before any observed result is accepted"
      pattern: "historicalExpectationRoot"
---

<objective>
Replace the impossible all-zero historical aggregate constant with an independently bound persisted expectation/predicate, closing the non-circular expectation portion of ADMIT-03 and implementing D-01, D-02, D-05, and D-10.

Purpose: Ensure the new supervised 540-Match run is checked against immutable pre-v1.38 evidence rather than against a value learned from itself.
Output: A persisted historical expectation artifact, source/derivation bindings, mutation tests, and an independent predicate evaluator.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-02-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md
@.planning/artifacts/v2.0-core-rules-audit/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist the independently sourced historical expectation</name>
  <read_first>
    <file>.planning/artifacts/v2.0-core-rules-audit/README.md</file>
    <file>.planning/artifacts/v2.0-core-rules-audit/run-current-meta-matrix.ts</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md</file>
    <file>packages/spec/src/canonical-json.ts</file>
    <file>packages/spec/src/canonical-identity-domains.ts</file>
  </read_first>
  <files>scripts/evaluate-v1-38-foundation-contract.test.ts, scripts/lib/v1-38-current-matrix-reproduction.ts, .planning/artifacts/v1.38-historical-matrix-expectation.json</files>
  <behavior>
    - The expectation binds the admitted immutable v1.37 archive commit, the pre-v1.38 README Git blob object, its exact SHA-256 bytes, the historical runner blob/source digest, and the expectation-derivation source root.
    - Exact declared results include the 540-cell inventory, both 62-44-2 leaders, the 57-51-0 third-place record, nine majority-edge cycles, and per-Strategy Smoke/Open Field equality.
    - Missing, extra, ambiguous, duplicated, malformed, mutated, working-tree-only, or observed-run-derived expectation input is rejected.
  </behavior>
  <action>Add focused `matrix expectation` tests first. Implement a closed expectation schema and generator that resolves the historical README and runner from the already admitted immutable v1.37 archive commit via Git object identity, not from a copied release label or the current working-tree bytes alone. Parse only the exact declared historical results identified in the source evidence, preserve their literal denominators, bind the source commit/blob/byte digests plus derivation source root, and canonically write `.planning/artifacts/v1.38-historical-matrix-expectation.json`. The generator must not execute the historical loader, call the current supervised runner, accept observed outcomes, or accept an override root. Fail closed when the source declaration is ambiguous or does not match its exact expected shape per D-01/D-02/D-10.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix expectation" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The committed expectation is non-zero, canonical, reproducible from immutable pre-v1.38 Git evidence, and its root changes or validation rejects for every source, blob, declaration, ordering, or derivation mutation.</acceptance_criteria>
  <done>The historical expectation exists independently before any new full supervised result and cannot be rewritten by the observed run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the independent predicate during canonical reduction</name>
  <read_first>
    <file>.planning/artifacts/v1.38-historical-matrix-expectation.json</file>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
    <file>scripts/evaluate-v1-38-foundation-contract.test.ts</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - The reducer validates the expectation source/root before evaluating observed outcomes.
    - The complete canonical aggregate must satisfy every exact declared historical predicate; a matching subset or recomputed observed expectation is rejected.
    - Receipt construction binds both the independent expectation root and the exact observed aggregate/accepted-ledger roots.
  </behavior>
  <action>Replace `HISTORICAL_EXPECTED_AGGREGATE_ROOT` with `loadV138HistoricalMatrixExpectation` and `evaluateV138HistoricalMatrixPredicate`. Keep the observed aggregate root as a separately computed output, never an input to expectation construction. Require exact 540-ID inventory and all declared leader, record, cycle, and arena-equality predicates before a pass; bind the expectation root, source bindings, predicate version, observed aggregate root, accepted ledger root, and reducer source root into the receipt. Add mutation cases for source bytes, source object identity, expectation fields/root, leaders, record totals, cycle count, Smoke/Open Field equality, observed-derived rewrite attempts, and extra/missing keys. Every ambiguity or mismatch must remain `MATRIX_REPRODUCTION_MISMATCH` and publish zero accepted cells per D-02/D-05/D-10.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix expectation|matrix reduction" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Reduction can pass only against the independently persisted predicate, and deterministic tests prove expectation independence, mutation detection, exact predicate enforcement, and separate exact observed-root binding.</acceptance_criteria>
  <done>The all-zero placeholder is gone and no real or synthetic observed aggregate can define its own expected result.</done>
</task>

</tasks>

## Artifacts this plan produces

- `.planning/artifacts/v1.38-historical-matrix-expectation.json`
- `loadV138HistoricalMatrixExpectation` and `evaluateV138HistoricalMatrixPredicate` in `scripts/lib/v1-38-current-matrix-reproduction.ts`
- Focused historical expectation independence and mutation coverage in `scripts/evaluate-v1-38-foundation-contract.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pre-v1.38 Git evidence → expectation artifact | Historical declarations become a new immutable predicate only after exact object and byte verification. |
| Observed supervised outcomes → expected predicate | New results are untrusted evidence and must never influence their own expectation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-30 | Spoofing | historical source identity | high | mitigate | Resolve the admitted archive commit and exact Git blob objects; bind object IDs and byte digests. |
| T-262-31 | Tampering | expectation artifact | high | mitigate | Closed schema, canonical root, derivation-code root, exact declarations, and mutation tests. |
| T-262-32 | Repudiation | expectation provenance | high | mitigate | Persist source commit/blob/digest lineage and forbid observed-run inputs or override roots. |
| T-262-33 | Elevation of Privilege | historical runner | high | mitigate | Treat the runner as hashed evidence only; never execute its loader or direct-engine path. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix expectation|matrix reduction" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
</verification>

<success_criteria>
- The expectation is independently reconstructible from immutable pre-v1.38 Git evidence.
- Every source, predicate, root, ambiguity, and observed-derived mutation fails closed.
- Canonical reduction binds an exact observed aggregate root while evaluating the separate historical predicate.
- No historical unsafe execution mechanism or direct-engine shortcut is introduced.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-08-SUMMARY.md` when done.
</output>
