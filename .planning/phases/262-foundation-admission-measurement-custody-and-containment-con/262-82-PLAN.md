---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "82"
type: execute
wave: 64
depends_on: [262-77]
files_modified:
  - scripts/lib/v1-38-bounded-retry-envelope.ts
  - scripts/run-v1-38-bounded-retry-envelope.ts
  - scripts/run-v1-38-bounded-retry-envelope.test.ts
autonomous: true
gap_closure: true
requirements: [ADMIT-03, ADMIT-04, MEAS-02, MEAS-04, MEAS-09, MEAS-10]
must_haves:
  truths:
    - "At the inclusive four-hour boundary, the controller durably appends one immutable expiry terminal journal record before returning an exhausted result."
    - "A crash at either side of the expiry append cannot leave reusable capacity: restart derives the same terminal or appends it once from the still-active predecessor."
    - "Repeated or concurrent invocation cannot duplicate the expiry terminal, advance another identity, or reopen the envelope."
    - "Every frozen 3-start, 12-observation, 5-minute, 15-minute, 8-attempt/4-shard, 200 ms, inclusive 2,500bp, and one-540-cell bound remains unchanged."
    - "Plan 77 remains immutable blocked history; this correction performs no live work and grants no sealing, admission, lifecycle, or downstream authority."
  artifacts:
    - path: scripts/lib/v1-38-bounded-retry-envelope.ts
      provides: "Replay-valid immutable expiry-terminal event and exhausted derived state"
    - path: scripts/run-v1-38-bounded-retry-envelope.ts
      provides: "Controller-side inclusive deadline terminalization before any wait, observation, or return"
    - path: scripts/run-v1-38-bounded-retry-envelope.test.ts
      provides: "Boundary, crash, idempotence, concurrency, no-reuse, and regression proofs"
  key_links:
    - from: scripts/run-v1-38-bounded-retry-envelope.ts
      to: scripts/lib/v1-38-bounded-retry-envelope.ts
      via: "controller appends the model-owned expiry terminal through the existing durable journal boundary"
      pattern: "time_window_expired"
    - from: scripts/lib/v1-38-bounded-retry-envelope.ts
      to: scripts/run-v1-38-bounded-retry-envelope.test.ts
      via: "replay derives exhausted state and zero remaining reusable work from the immutable terminal"
      pattern: "disposition.*exhausted"
  prohibitions:
    - "Do not mutate, delete, supersede, republish, or reinterpret the Plan-77 review JSON, report, summary, finding, or roots."
    - "Do not run live preflight, calibration, reproduction, local-seal access, or canonical artifact publication."
    - "Do not change any frozen retry bound, canonical runtime/kernel path, gameplay rule, privacy projection, or historical charge."
    - "Do not grant Plan-78 eligibility, ADMIT-03, Phase 263, candidate, formation, holdout, public, product, activation, production, counted-play, or gameplay-change authority."
---

<objective>
Correct `TIME_WINDOW_EXPIRY_NOT_TERMINALIZED` by making inclusive four-hour expiry a durable, immutable terminal transition before controller return, per D-01, D-02, D-05, D-23R through D-27R.

Purpose: Close the exact Plan-77 source finding without altering the frozen envelope or rewriting the review that discovered it.
Output: Corrected source/controller and focused synthetic tests only; no live work or authority.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-75-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-76-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-77-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-77-REVIEW.md
@.planning/artifacts/v1.38-plan-262-77-bounded-retry-source-review-v1.json
@scripts/lib/v1-38-bounded-retry-envelope.ts
@scripts/run-v1-38-bounded-retry-envelope.ts
@scripts/run-v1-38-bounded-retry-envelope.test.ts

<interfaces>
From scripts/lib/v1-38-bounded-retry-envelope.ts:
- `V138RetryJournalEvent` is the closed journal-event union.
- `appendV138RetryJournalRecord(records, event, atMilliseconds, envelopeRoot)` validates replay and returns a previous-root-linked immutable record list.
- `deriveV138RetryState(envelopeValue, records)` derives counters, remaining capacity, disposition, and state root only from envelope plus journal.

From scripts/run-v1-38-bounded-retry-envelope.ts:
- `V138BoundedRetryControllerEffects.appendDurableRecord(record)` is the sole injected durability boundary.
- `runV138BoundedRetryController({ envelope, owner, records, effects })` owns deterministic reconciliation, waits, observation, calibration, reproduction, and return.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD the immutable inclusive-expiry transition</name>
  <read_first>
    - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-77-REVIEW.md
    - .planning/artifacts/v1.38-plan-262-77-bounded-retry-source-review-v1.json
    - scripts/lib/v1-38-bounded-retry-envelope.ts
    - scripts/run-v1-38-bounded-retry-envelope.test.ts
  </read_first>
  <files>scripts/lib/v1-38-bounded-retry-envelope.ts, scripts/run-v1-38-bounded-retry-envelope.test.ts</files>
  <behavior>
    - Exact boundary: first observation at t0 and terminal event at t0 + 14,400,000 ms yields `exhausted` with reason `time_window_expired`.
    - Before boundary: the expiry event is rejected and the journal remains unchanged.
    - After boundary: no ordinary reservation, observation, calibration, or reproduction event is accepted; only the single expiry terminal may close an otherwise active envelope.
    - Replay/idempotence: a committed expiry terminal remains terminal and a second terminal or any later identity transition is rejected.
    - Capacity: terminal state exposes no reusable preflight, route, calibration, or reproduction opportunity while preserving all previously charged identities.
  </behavior>
  <action>Write failing tests first, then extend the closed journal schema and replay state with one explicit immutable terminal event whose sole bounded reason is `time_window_expired`, per D-01, D-05, D-24R, and D-25R. Make the deadline inclusive: at or after `firstObservationMilliseconds + 14_400_000`, ordinary work transitions fail and the expiry transition is valid exactly once. Derive `exhausted` and the terminal reason from the journal rather than wall-clock inspection or caller input, preserve every existing counter and root-chain rule, and ensure terminal replay makes all remaining identities unavailable. Keep D-03, D-06, D-10, and D-25R's exact numeric and runtime/kernel predicates unchanged; do not edit Plan-77 evidence.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-bounded-retry-envelope.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=180000 --bail=1</automated></verify>
  <acceptance_criteria>
    - Tests prove pre-boundary rejection, exact-boundary closure, post-boundary closure, replayed terminality, and zero identity reuse.
    - The terminal is a previous-root-linked journal fact and cannot be inferred from mutable clock state.
    - All prior finite-envelope tests retain their exact expectations except the formerly blocked expiry case now expects durable exhaustion.
  </acceptance_criteria>
  <done>The pure model can represent, validate, replay, and permanently enforce inclusive time-window expiry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminalize expiry durably before controller return</name>
  <read_first>
    - scripts/lib/v1-38-bounded-retry-envelope.ts
    - scripts/run-v1-38-bounded-retry-envelope.ts
    - scripts/run-v1-38-bounded-retry-envelope.test.ts
    - scripts/check-v1-38-plan-262-77-bounded-retry-source-review.test.ts
  </read_first>
  <files>scripts/run-v1-38-bounded-retry-envelope.ts, scripts/run-v1-38-bounded-retry-envelope.test.ts</files>
  <behavior>
    - Controller at the exact deadline appends the expiry terminal through `appendDurableRecord` before returning and performs no later observation or work.
    - The production wrapper derives and exclusive-writes the immutable terminal result from that exhausted journal state when live mode is later invoked; tests prove this only in owned temporary fixtures.
    - Crash before durable append leaves the prior journal active but reusable only by a restart that immediately terminalizes; crash after the durable write replays exhausted without a duplicate append.
    - Two owners or stale concurrent journals cannot create two terminals or consume a next identity; the production lock and previous-root chain fail closed.
    - A wait target that reaches or crosses the deadline terminalizes instead of sampling or reserving work.
    - Repeated invocation over terminal bytes returns the same closed state and never relaunches calibration or reproduction.
    - Sealing and live CLI contracts require the future Plan-83 re-review and corrected Plan-82 summary; Plan-77 remains a protected historical input rather than an eligibility input.
  </behavior>
  <action>Write controller-level failing tests before implementation. Add a single deadline guard used on entry, after reconciliation, before and after each wait, and before every irreversible work transition. When the inclusive deadline is reached while journal state is active, append the model-owned expiry terminal through the existing `appendDurableRecord` durability boundary, update the in-memory record list only after that call succeeds, derive the exhausted state, and return without observation, calibration, reproduction, or another identity reservation, per D-02 and D-25R. Preserve reserve-before-work ordering and the owner-only production lock. Keep the production wrapper's terminal-result publication downstream of the returned journal-derived state; add an owned-temporary-fixture test proving expiry produces both the durable journal terminal and one exclusive immutable terminal result with bounded reason, exact roots/counters, and false downstream authority. Exercise injected durable-writer failures on both sides of the append, restart from captured durable bytes, stale-root/concurrent-owner rejection, repeated invocation, and no-reuse of every remaining identity. Update the fixed sealing/live source-custody inputs to the corrected Plan-82 summary and future Plan-83 re-review pair, while retaining the exact Plan-77 blocked pair/root/finding only in protected history per D-24R and D-27R. Keep all tests synthetic/fake-process only and assert Plan-77 artifacts plus every canonical seal/live/reproduction/activation destination are byte-identical or absent; per D-26R, no eligibility or authority is created by this source change.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-bounded-retry-envelope.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=180000 --bail=1 &amp;&amp; pnpm turbo typecheck --concurrency=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>
    - Inclusive expiry always reaches durable terminal journal evidence before a normal controller return, and the production wrapper can derive exactly one immutable terminal result from it.
    - Crash, restart, repetition, and concurrency cannot duplicate the terminal or reuse any identity.
    - No live handler, canonical destination, Plan-77 evidence, lifecycle carrier, or downstream authority changes.
  </acceptance_criteria>
  <done>The corrected committed source is ready for a fresh independent re-review and nothing else.</done>
</task>

</tasks>

## Artifacts this phase produces

- Corrected bounded-retry model/controller and focused synthetic tests.
- Normal `262-82-SUMMARY.md` recording exact corrected source custody after execution.
- No new review verdict, seal, envelope, live evidence, reproduction, activation, lifecycle mutation, or authority.

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Exact released-authority admission before candidate work | 262-82 | COVERED | Fixes the blocker without opening the route.
REQ | ADMIT-03, ADMIT-04 | Reproducible matrix path and fail-closed admission | 262-82 | COVERED | Expiry becomes durable non-pass evidence.
REQ | MEAS-02, MEAS-04, MEAS-09, MEAS-10 | Frozen bounds, failure accounting, branch-distinct reporting, containment | 262-82 | COVERED | Tests preserve every bound and denial.
RESEARCH | — | Inclusive four-hour terminal, crash recovery, concurrency, no reuse | 262-82 | COVERED | Implements the exact missing terminalization capability.
CONTEXT | D-01..D-18, D-19R, D-20R, D-21, D-22, D-23R..D-27R | All locked evidence, runtime, privacy, local-seal, formation, retry, and authority boundaries | 262-82 | COVERED | Exact behavior is preserved; Plan 77 remains historical and live work remains absent.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| monotonic receipt -> journal | Clock-derived expiry must become durable evidence before it affects return behavior. |
| active journal -> terminal journal | Only one exact boundary transition may consume terminality; no identity may be reused. |
| durable append -> restart | Crash ambiguity must replay from committed bytes without guessing or relaunching work. |
| corrected source -> later review | Source changes cannot attest to their own correctness or authorize sealing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-82-01 | Spoofing | expiry receipt | critical | mitigate | Injected monotonic clock, first-observation-derived inclusive deadline, journal-owned reason. |
| T-262-82-02 | Tampering | expiry terminal | critical | mitigate | Canonical previous-root-linked record, single transition, immutable replay. |
| T-262-82-03 | Repudiation | crash boundary | high | mitigate | Durable append ordering and restart tests for captured committed bytes. |
| T-262-82-04 | Information Disclosure | journal/result | high | mitigate | Bounded reason and safe counters only; no runtime diagnostics or Strategy data. |
| T-262-82-05 | Denial of Service | repeated/concurrent controller | high | mitigate | Terminal idempotence, owner lock, stale-root rejection, no later waits/work. |
| T-262-82-06 | Elevation of Privilege | corrected source | critical | mitigate | No live mode invocation or eligibility; Plan 83 independently reviews committed source. |
| T-262-82-SC | Tampering | package supply chain | low | accept | No package installation occurs. |

ASVS L1: any ambiguous clock, append, replay, ownership, root, or destination state fails closed with no live work or authority.
</threat_model>

<verification>
- <automated>pnpm exec vitest run scripts/run-v1-38-bounded-retry-envelope.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --testTimeout=180000 --bail=1 &amp;&amp; pnpm turbo typecheck --concurrency=1 &amp;&amp; git diff --check</automated>
</verification>

<success_criteria>
- Inclusive four-hour expiry is durably journaled exactly once before normal return.
- Crash/restart, idempotence, concurrency, and no-reuse tests pass without changing any frozen policy.
- Plan 77 remains immutable blocked history and Plan 83 is the sole next action; no live work or authority exists.
</success_criteria>

<output>Create `262-82-SUMMARY.md` with exact corrected source commit/tree/path custody, test evidence, Plan-77 preservation, and explicit non-authorization; then dispatch Plan 83 only.</output>
