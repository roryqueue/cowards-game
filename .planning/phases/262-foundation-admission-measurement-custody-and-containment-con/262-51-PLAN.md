---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "51"
type: tdd
wave: 39
depends_on: [262-49]
files_modified:
  - scripts/verify-v1-38-local-seal.ts
  - scripts/verify-v1-38-local-seal.test.ts
  - scripts/check-v1-38-dependency-revision-boundaries.ts
  - scripts/evaluate-v1-38-dependency-revision.test.ts
autonomous: true
requirements: [MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "The exact Plan 262-50 finding V2_VERIFIER_MODE_MISSING has a failing-before and passing-after regression without rewriting v1 or v2 evidence."
    - "The verifier exposes deterministic read-only --check-v2 and --check-v3 modes; v2 validates the canonical FAIL artifact exactly, while v3 validates an exclusive versioned verdict without writing or upgrading it."
    - "The boundary checker accepts only the declared 42-plan lifecycle states for repair, fresh v3 review, ADMIT route, and activation, including a no-summary Plan-52 FAIL branch."
    - "Green author tests grant no revised SEAL-01 credit and every downstream authority remains false."
  artifacts:
    - path: scripts/verify-v1-38-local-seal.ts
      provides: "Versioned read-only verifier modes with exact schema/root validation"
    - path: scripts/evaluate-v1-38-dependency-revision.test.ts
      provides: "42-plan successor lifecycle and drift regression coverage"
  key_links:
    - from: .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-50-REVIEW.md
      to: scripts/verify-v1-38-local-seal.ts
      via: "Exact V2_VERIFIER_MODE_MISSING red/green regression"
      pattern: "V2_VERIFIER_MODE_MISSING|--check-v2"
    - from: scripts/check-v1-38-dependency-revision-boundaries.ts
      to: .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-52-PLAN.md
      via: "Exact lifecycle state permits v3 review only after Plan 262-51 summary"
      pattern: "262-51|262-52"
---

<objective>
Repair the sole Plan 262-50 verifier-mode finding and extend exact lifecycle tracking for the additive v3 review route.

Purpose: Make the already-required v2 verification command real without altering its immutable FAIL evidence or broadening the verifier into a writer.
Output: A tested read-only `--check-v2` mode and exact 42-plan lifecycle checks, with no requirement credit.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md

Use RED -> GREEN with separate commits. Preserve protocol-v1/v2, verification-v1/v2, and Plan-262-46/50 reviews byte-for-byte. Plan 262-52 alone owns the fresh verdict.
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-50-REVIEW.md
@.planning/artifacts/v1.38-local-seal-independent-verification-v2.json
@scripts/verify-v1-38-local-seal.ts
@scripts/verify-v1-38-local-seal.test.ts
@scripts/check-v1-38-dependency-revision-boundaries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact read-only v2 and v3 verification modes</name>
  <files>scripts/verify-v1-38-local-seal.ts, scripts/verify-v1-38-local-seal.test.ts</files>
  <behavior>
    - `--check-v2` validates the canonical v2 artifact schema, source/root joins, preserved evidence hashes, finding count, false SEAL-01 verdict, reduced assurance, and all denied authorities.
    - Missing, malformed, noncanonical, mutated, wrong-domain, wrong-root, or verdict-upgraded v2 bytes fail with bounded stable codes.
    - `--check-v2` performs no write, accepts no alternate path or caller identity, and does not reinterpret the immutable finding.
    - `--check-v3` validates the exclusive canonical v3 schema/domain/root and either zero-finding PASS or bounded FAIL disposition; tests use disposable canonical fixtures before the live v3 artifact exists.
    - Existing `--write`/`--check` behavior and v1 bytes remain exact.
  </behavior>
  <action>Commit a RED test that runs the exact v2 command and reproduces `V138_LOCAL_SEAL_REVIEW_CLI_USAGE`, plus v2/v3 mutation and no-write fixtures. Add closed `--check-v2` and `--check-v3` branches sharing strict versioned validation. V2 reads only the canonical v2 path, validates exact keys/types/domains/root and required false-authority disposition, and preserves the FAIL verdict. V3 accepts no caller-selected path in live CLI mode and validates the exclusive canonical v3 path once present; unit tests exercise an injected disposable fixture through the closed library seam without creating repository evidence. Do not create v3 evidence or repair any reviewed source.</action>
  <verify><automated>pnpm exec vitest run scripts/verify-v1-38-local-seal.test.ts --maxWorkers=1 &amp;&amp; pnpm exec tsx scripts/verify-v1-38-local-seal.ts --check-v2 &amp;&amp; test ! -e .planning/artifacts/v1.38-local-seal-independent-verification-v3.json &amp;&amp; git diff --check</automated></verify>
  <done>The formerly missing v2 command passes only for immutable canonical v2 FAIL evidence, and the read-only v3 mode is mutation-tested before its exclusive artifact exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend exact lifecycle tracking to the v3 review branch</name>
  <files>scripts/check-v1-38-dependency-revision-boundaries.ts, scripts/evaluate-v1-38-dependency-revision.test.ts</files>
  <behavior>
    - Pre-51 is 42/38 with incomplete 47,48,51,52; post-51 is 42/39 with incomplete 47,48,52.
    - Plan-52 PASS is 42/40 incomplete 47,48; Plan-52 FAIL remains 42/39 incomplete 47,48,52 with canonical v3 FAIL evidence and no summary.
    - Post-47 is 42/41 incomplete 48; post-48 is 42/42 incomplete none.
    - Any skipped, out-of-order, compensating, added, removed, or unexpectedly summarized state produces PLAN_DISCOVERY_DRIFT.
  </behavior>
  <action>Write all six v3-branch lifecycle fixtures and one-input drift mutations before changing the checker. Replace the 41-plan v2-review lifecycle with the exact 42-plan additive branch while preserving archived 262-46/50 hashes and all prior FAIL artifacts/reviews. Keep incomplete ordering canonical and do not allow a FAIL artifact to substitute for a summary.</action>
  <verify><automated>pnpm exec vitest run scripts/evaluate-v1-38-dependency-revision.test.ts --maxWorkers=1 &amp;&amp; node /Users/roryquinlan/.codex/gsd-core/bin/gsd-tools.cjs query phase-plan-index 262 | jq -e '(.plans | length) == 42 and .incomplete == ["262-47","262-48","262-51","262-52"]' &amp;&amp; pnpm exec tsx scripts/check-v1-38-dependency-revision-boundaries.ts --check &amp;&amp; pnpm turbo typecheck --concurrency=1 &amp;&amp; git diff --check</automated></verify>
  <done>The exact v3 successor lifecycle is accepted and all invalid transitions fail closed.</done>
</task>

</tasks>

<verification>
- The exact Plan 262-50 failing command has RED/GREEN proof.
- All v1/v2 evidence and both failed reviews remain byte-identical.
- Exact 42-plan lifecycle, boundary checker, focused tests, and typecheck pass.
- Revised SEAL-01, ADMIT-03, and downstream authority remain false.
</verification>

<success_criteria>
- `--check-v2` and `--check-v3` are deterministic, canonical, read-only, and verdict-preserving.
- Plan 262-52 is the sole route to a fresh v3 verdict.
- No candidate, formation, holdout opening, public/default, activation, or production work occurs.
</success_criteria>

<output>Create `262-51-SUMMARY.md` after both repairs pass. Grant no requirement or authority credit.</output>
