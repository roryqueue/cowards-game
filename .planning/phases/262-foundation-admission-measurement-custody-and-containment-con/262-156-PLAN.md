---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "156"
type: execute
wave: 131
depends_on: [262-152]
files_modified:
  - scripts/lib/v1-38-lean-runner-feasibility.ts
  - scripts/lib/v1-38-lean-runner-feasibility.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - packages/replay/src/reconstruct.ts
  - packages/replay/src/reconstruct.test.ts
  - packages/replay/src/index.ts
  - apps/runtime-service/src/execute-match.ts
  - apps/runtime-service/src/execute-match-v1-18.test.ts
  - .planning/artifacts/v1.38-lean-runner-diagnostic-custody-v1.json
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-156-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-03, ADMIT-04, MEAS-04, MEAS-08, MEAS-09, DECI-02]
must_haves:
  truths:
    - "Per D-34L, the frozen Open Field label remains a distinct schedule and evidence cell while its historical alias executes only through the active Smoke arena identity."
    - "Per D-01, D-02, D-05, and D-34L, evidence binds declared label identity, resolved execution identity, and semantic geometry identity without merging charges or results."
    - "Canonical terrain is projected in the same y-then-x order by replay reconstruction and the runtime terminal comparison, so Standard Cross cannot fail solely because equivalent terrain arrays are ordered differently."
    - "The six diagnosis-only Matches remain non-live, non-charged, unpersisted, and inadmissible as D-34L evidence."
  artifacts:
    - path: scripts/lib/v1-38-lean-runner-feasibility.ts
      provides: "Dual declared-label and resolved-execution arena identity in schedule/evidence contracts"
    - path: packages/replay/src/reconstruct.ts
      provides: "Shared canonical replay-terrain projection"
    - path: apps/runtime-service/src/execute-match.ts
      provides: "Runtime terminal projection using the replay canonical terrain order"
    - path: .planning/artifacts/v1.38-lean-runner-diagnostic-custody-v1.json
      provides: "Safe aggregate custody for six inadmissible diagnosis-only Matches"
  key_links:
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: packages/spec/src/arena-catalog-v1-37.ts
      via: "historical alias resolution to an active schedulable target before request construction"
      pattern: "aliasOf"
    - from: scripts/lib/v1-38-lean-runner-feasibility.ts
      to: .planning/artifacts/v1.38-lean-runner-corrective-terminal-v2.json
      via: "each evidence cell preserves declared and execution arena identities"
      pattern: "executionArenaId"
    - from: packages/replay/src/reconstruct.ts
      to: apps/runtime-service/src/execute-match.ts
      via: "one canonical y-then-x terrain projection used on both sides of terminal equality"
      pattern: "canonical.*Terrain"
---

<objective>
Correct the two independently diagnosed implementation defects that caused the first lean gate's arena-specific non-pass, without running or crediting any new Match.

Purpose: Make the unchanged D-34L schedule executable through valid active arena authority and make terminal replay comparison order-stable while preserving canonical rules, geometry, charges, and historical evidence.
Output: Separately committed focused source fixes and tests, source-only corrective selectors, a safe diagnostic-custody receipt, and summary.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-ADMISSION-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-152-SUMMARY.md
@.planning/artifacts/v1.38-lean-runner-terminal.json
@packages/spec/src/arena-catalog-v1-37.ts
@packages/replay/src/reconstruct.ts
@apps/runtime-service/src/execute-match.ts
@scripts/lib/v1-38-lean-runner-feasibility.ts
@scripts/run-v1-38-lean-runner-feasibility.ts
@scripts/check-v1-38-lean-admission.ts
</context>

<interfaces>
The corrective schedule keeps `baseCellId`, `cellId`, `chargedIdentity`, `arenaId`, and `arenaLabel` bound to the frozen declared label cell. Add explicit resolved execution fields such as `executionArenaId` and `executionArenaLabel`; Open Field must bind `arena:open-field:v1`/`Open Field` as the declared identity and `arena:smoke:v1`/`Smoke` as execution identity, while Smoke and Standard Cross resolve to themselves. Semantic geometry remains bound independently.

The diagnostic-custody artifact must record exactly six prior diagnosis-only Matches with `persisted:false`, `liveInvocation:false`, `charged:false`, `evidenceAdmissible:false`, no per-Match outcomes or private diagnostics, formation materialization false, and every authority false.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve label cells while resolving active execution arenas</name>
  <files>scripts/lib/v1-38-lean-runner-feasibility.ts, scripts/lib/v1-38-lean-runner-feasibility.test.ts, scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts, .planning/artifacts/v1.38-lean-runner-diagnostic-custody-v1.json</files>
  <behavior>
    - "Open Field retains its frozen declared base-cell and charge identities but prepares a request whose arena id/name are the active Smoke target."
    - "Smoke and Standard Cross declared and execution identities are equal."
    - "Evidence and request-realism roots bind declared id/label, resolved execution id/label, alias relation, and semantic geometry hash."
    - "A missing, chained, inactive, unschedulable, geometry-mismatched, or cyclic alias fails before any child execution."
  </behavior>
  <action>Write RED tests for the exact alias and failure cases, then implement a pure catalog resolver used by schedule validation, prepared-request construction, evidence schemas, reduction, and realism hashing. Per D-34L, never replace the Open Field label cell with Smoke in `baseCellId`, `cellId`, `chargedIdentity`, ordinal, coverage, or comparison keys; only the runtime request uses the resolved active target. Require the historical alias and active target to share the exact semantic geometry hash and require the target to be active and schedulable. Add the corrective source/checker selectors and fresh artifact-path constants needed by Plans157-159, but make every effect selector unreachable in this plan. Separately write the safe aggregate diagnostic-custody receipt for the six already completed diagnosis-only Matches; state explicitly that no raw result was persisted, no execution is charged, no result is admissible, and no authority follows. Commit RED tests, GREEN source, and diagnostic custody as separate focused commits. Do not run a real Match or write any corrective invocation, terminal, review, readiness, adjudication, or eligibility artifact.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-runner-feasibility.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>The frozen 12-cell schedule remains exact, Open Field executes through active Smoke authority, both identities are authenticated in evidence, and six diagnostics are recorded only as inadmissible custody.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonicalize terrain ordering at replay and runtime terminal joins</name>
  <files>packages/replay/src/reconstruct.ts, packages/replay/src/reconstruct.test.ts, packages/replay/src/index.ts, apps/runtime-service/src/execute-match.ts, apps/runtime-service/src/execute-match-v1-18.test.ts</files>
  <behavior>
    - "Equivalent terrain sets project in canonical y-then-x order in replay reconstruction and runtime terminal comparison."
    - "Standard Cross completes semantic reconstruction when source arrays contain the same valid terrain in a different order."
    - "Coordinate changes, duplicates, missing stones, and out-of-bounds terrain still fail integrity checks."
    - "Smoke behavior and canonical hashes remain unchanged."
  </behavior>
  <action>Write RED replay and runtime-service regressions that reproduce the diagnosed Standard Cross terminal-binding failure using equivalent differently ordered terrain. Export one side-effect-free canonical terrain projection from the replay package, ordering cloned positions by y then x to match the canonical arena contract, and consume it in both replay final-state reconstruction and runtime `projectFinalStateForReplay`. Preserve validation of coordinates, multiplicity, bounds, rules, events, outcomes, and transition hashes; ordering normalization must not turn a changed terrain set into equality. Commit RED and GREEN separately and run the focused replay/runtime tests. Do not alter engine transitions, arena geometry, starting formation, Strategy inputs, gameplay rules, or public behavior.</action>
  <verify><automated>pnpm exec vitest run packages/replay/src/reconstruct.test.ts apps/runtime-service/src/execute-match-v1-18.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>Replay and runtime compare canonically ordered terrain while every semantic terrain mutation still fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind the corrective closure and prove source-only containment</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, scripts/lib/v1-38-lean-runner-feasibility.test.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-156-SUMMARY.md</files>
  <behavior>
    - "The checker accepts only the two focused fixes plus the safe diagnostic-custody contract."
    - "The first invocation marker and terminal remain byte-identical and Git-identical."
    - "All fresh corrective review, readiness, invocation, terminal, adjudication, and eligibility destinations remain absent."
    - "All 36 successor lockfiles remain untouched."
  </behavior>
  <action>Extend the checker and focused tests to authenticate the source-only corrective closure, the exact immutable roots/bytes of the first marker and terminal, the six-count inadmissible diagnostic-custody artifact, and absence of every later corrective artifact. Include negative tests for collapsing Open Field into Smoke coverage, using the historical alias as the runtime arena, trusting diagnostic results, normalizing terrain outside the projection boundary, or changing any authority flag. Run the complete focused suite and TypeScript, record exact commits/test counts and unchanged lock inventory in the summary, and stop with Plan157 as the sole next action.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-runner-feasibility.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts packages/replay/src/reconstruct.test.ts apps/runtime-service/src/execute-match-v1-18.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-corrective-source-only &amp;&amp; git diff --check</automated></verify>
  <done>The two fixes are committed and fully tested, old live evidence is immutable, fresh effect artifacts are absent, and only Plan157 review is eligible.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Restore the reviewed D-34L feasibility path without changing its scientific claim | 262-156 | COVERED | Two specific implementation defects only.
REQ | ADMIT-03 | Correct the diagnosed causes of the 8/24 non-pass | 262-156 | COVERED | Alias execution and terrain projection.
REQ | ADMIT-04 | Fail closed on invalid arena or source authority | 262-156 | COVERED | Exact active-target and historical-byte checks.
REQ | MEAS-04 | Do not credit invalid/system-failed or diagnostic work | 262-156 | COVERED | Six diagnostics are explicitly non-charged and inadmissible.
REQ | MEAS-08 | Advanced remains fixture-only | 262-156 | COVERED | No strength or balance claim.
REQ | MEAS-09 | Keep diagnosis, correction, and empirical evidence distinct | 262-156 | COVERED | Separate custody and no live terminal.
REQ | DECI-02 | Preserve non-production containment | 262-156 | COVERED | No product or downstream authority.
RESEARCH | — | At most one whole corrective rerun after a specific committed fix | 262-156 | COVERED | This plan fixes only; it does not rerun.
CONTEXT | D-01..D-22 | Immutable evidence, canonical runtime, privacy, accounting, and containment | 262-156 | COVERED | Old evidence and rule semantics remain unchanged.
CONTEXT | D-23R..D-33R | Retired full-matrix routes | EXCLUDED | Historical only and not revived.
CONTEXT | D-34L | Full corrective rerun permitted only after diagnosed code defects are fixed | 262-156 | COVERED | Both diagnosed defects receive focused tests and commits.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| declared arena label -> runtime arena authority | A historical label must resolve to one exact active target without changing cell identity. |
| engine final state -> replay/runtime projection | Equivalent terrain order must compare consistently without erasing semantic differences. |
| diagnosis-only execution -> committed evidence | Unpersisted diagnostics must never become charged or admissible evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-156-01 | Tampering | arena identity resolution | critical | mitigate | Bind declared id/label, target id/label, alias edge, and geometry hash; reject every non-exact target. |
| T-262-156-02 | Elevation of Privilege | diagnostic custody | critical | mitigate | Closed schema fixes count six and all evidence/charge/authority flags false. |
| T-262-156-03 | Tampering | terrain normalization | high | mitigate | Normalize order only; tests retain coordinate, multiplicity, and bounds failures. |
| T-262-156-04 | Repudiation | first marker/terminal | high | mitigate | Checker authenticates exact bytes, blobs, and roots before any successor readiness. |
| T-262-156-05 | Information Disclosure | diagnostic receipt | high | mitigate | Safe aggregate only; no per-Match outcome, source, memory, objective, host, or raw diagnostic. |
| T-262-156-SC | Tampering | package supply chain | low | accept | No package installation occurs. |
</threat_model>

<verification>
Focused RED/GREEN tests reproduce and close both defects; all existing lean source tests pass serially; TypeScript passes; the source-only checker authenticates old evidence, fresh-path absence, diagnostic inadmissibility, and 36 untouched locks; no Match executes.
</verification>

<success_criteria>
- Open Field is still one of 12 declared label cells and executes through active Smoke identity.
- Standard Cross replay/runtime terminal comparison is order-stable without weakening terrain integrity.
- Six diagnosis-only Matches are recorded as unpersisted, non-live, non-charged, and inadmissible.
- The first marker/terminal and all 36 locks are unchanged.
- No corrective live artifact or downstream authority exists.
</success_criteria>

<output>Create `262-156-SUMMARY.md`; dispatch `262-157-PLAN.md` only after the exact focused source commits and all source-only checks pass.</output>
