---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "45"
type: tdd
wave: 37
depends_on: [262-44]
files_modified:
  - scripts/lib/v1-38-local-seal.ts
  - scripts/evaluate-v1-38-local-seal.test.ts
  - scripts/evaluate-v1-38-local-seal.ts
  - .planning/artifacts/v1.38-local-seal-protocol-v1.json
autonomous: true
requirements: [MEAS-10, SEAL-01]
must_haves:
  truths:
    - "The local tool creates one restricted out-of-repository sealed store and exposes no generic read, get, query, list, debug, or preimage API per D-04, D-19R, and D-20R."
    - "The sole opening is durably consumed before evaluation launch; crash, system failure, mismatch, repeated opening, or uncertain state terminalizes the claim without retry or replacement per D-02, D-05, and D-21."
    - "A hash-chained canonical event ledger detects deletion, reorder, fork, mutation, and missing/extra events while repository evidence reveals only bounded roots and coarse states."
    - "Commitment, request, freeze, metric, classifier, threshold, schedule, finalist, policy, semantic, runtime, and receipt roots join exactly before a verified receipt is accepted."
    - "The schema reports assuranceClass single_operator_local_seal_v1 and cannot render any independent-custody or malicious-owner-resistance claim."
  artifacts:
    - path: scripts/lib/v1-38-local-seal.ts
      provides: "Closed local-seal state machine, commitment, ledger, projection, contamination, and retirement interfaces"
    - path: scripts/evaluate-v1-38-local-seal.test.ts
      provides: "Red/green adversarial lifecycle, crash, tamper, privacy, and claim-boundary tests"
    - path: .planning/artifacts/v1.38-local-seal-protocol-v1.json
      provides: "Synthetic-input protocol capability root with no real holdout material"
  key_links:
    - from: .planning/artifacts/v1.38-pre-search-policy-root.json
      to: .planning/artifacts/v1.38-local-seal-protocol-v1.json
      via: "Exact non-authorizing policy root bound into the protocol commitment"
      pattern: "pre_search_policy_root"
    - from: scripts/lib/v1-38-local-seal.ts
      to: packages/spec/src/public-output-privacy.ts
      via: "Approved privacy seam validates every repository-safe projection"
      pattern: "assertPublicOutputLeakSafe"
---

<objective>
Implement and freeze the single-operator local-seal mechanics under the revised contract, satisfying the mechanical portion of D-19R/D-20R without claiming independent custody.

Purpose: Preserve one-shot procedural blindness, precommitment, tamper evidence, fail-closed contamination, privacy, and no-post-result tuning using only the existing repository cryptography/filesystem stack.
Output: A new compatibility-free local-seal module, adversarial tests, deterministic write/check CLI, and synthetic protocol artifact.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LOCAL-SEALED-HOLDOUT-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-44-SUMMARY.md
@scripts/lib/v1-38-custody.ts
@scripts/evaluate-v1-38-custody.test.ts
@scripts/lib/v1-38-policy-authority.ts
@packages/spec/src/canonical-json-encode.ts
@packages/spec/src/canonical-identity-domains.ts
@packages/spec/src/public-output-privacy.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the single-operator local sealed holdout protocol</name>
  <files>scripts/lib/v1-38-local-seal.ts, scripts/evaluate-v1-38-local-seal.test.ts, scripts/evaluate-v1-38-local-seal.ts, .planning/artifacts/v1.38-local-seal-protocol-v1.json</files>
  <behavior>
    RED:
    - Exact-key schemas accept only assuranceClass `single_operator_local_seal_v1`, repositoryOperator, toolMediatedLedger, operatorNoPrematureAccessDeclaration, explicit assurance limitations, and approved root types.
    - Commit accepts secret bytes only from `<absolute-local-seal-root>/input/commitment-secret.bin`: 0700 owner-only ancestors, effective-UID-owned regular non-symlink 0600 file, bounded 32..4096 bytes, O_NOFOLLOW, read once during commitment, close and zero-fill in `finally`, unlink plus parent fsync before success, and no secret/path in returned values.
    - Wrong owner, group/other permissions, wrong type, symlink, out-of-bounds size, short read, cleanup failure, or uncertain unlink/fsync fails closed without emitting commitment evidence; secret bytes never enter CLI, environment, Git, tests, logs, receipts, artifacts, or public/default output.
    - State proceeds empty -> committed -> open_armed -> open_consumed -> projected -> verified -> retired, with contaminated -> retired as the sole failure branch; no transition can reopen or replace a holdout.
    - `open_consumed` and its fsynced hash-chain event are durable before the evaluation callback launches; a thrown/crashed callback records a charged terminal system failure and leaves reopening impossible.
    - Sequence, previous-event hash, command, outcome, stable reason, state, request root, and resulting state root detect ledger deletion, reorder, fork, mutation, duplication, or truncation.
    - The exact request binds all three future freeze roots, preselected finalists, canonical kernel/runtime/semantic tuple, policy, metric, classifier, threshold, opponent/schedule, receipt allowlist, contamination, and retirement roots before opening.
    - Safe projection accepts only opaque roots, counts, coarse status, and approved aggregate metrics within exact field/cardinality/byte bounds; any leak or unexpected key contaminates terminally.
    - Generic read/query/list/debug exports, independent-custody fields, separatelyPermissioned, external authentication, host-monitoring proof, and malicious-owner claims are absent or rejected.
    GREEN:
    - Implement the smallest closed module satisfying every behavior with existing Node crypto/fs and canonical identity/privacy helpers; do not modify or reinterpret the historical synthetic custody module/artifact.
    REFACTOR:
    - Centralize exact-key parsing, stable reason codes, durable exclusive append/fsync helpers, and domain-separated identities without widening exports or accepted inputs.
  </behavior>
  <action>Create the new local-seal module and CLI rather than renaming the historical synthetic module in place. Run the RED behaviors first, then implement GREEN and perform the stated REFACTOR. Implement one secret ingress only: resolve `input/commitment-secret.bin` beneath the already validated absolute out-of-repository store root; validate each ancestor and the file without following symlinks; require current effective UID ownership, exact 0700 directory and 0600 regular-file modes, and 32..4096 bytes; open with O_NOFOLLOW, read once during commitment, derive the domain-separated HMAC, then close and zero-fill all owned secret Buffers in `finally`, unlink the file, and fsync `input/` before returning success. Any ownership, permission, type, path, size, read, close, unlink, or fsync uncertainty fails closed and emits no accepted commitment. Do not accept secret bytes, encodings, or secret paths as CLI flags, environment values, configuration, logs, artifacts, receipts, or output. Document that zero-fill/unlink are best-effort hygiene and cannot prove removal from OS caches, swap, backups, copies, or a malicious owner. Tests must cover success plus wrong UID when safely fixtureable, wrong modes, symlink, directory/device, undersize/oversize, short/truncated file, unlink/fsync failure injection, Buffer cleanup, stdout/stderr/artifact leak scans, and absence of CLI/env secret seams. The protocol artifact uses synthetic non-holdout fixture bytes only and states `realHoldoutMaterialPresent: false`, `satisfiesSeal01Mechanics: true`, `independentCustodyClaimed: false`, and `downstreamAuthority: denied`. Use import.meta.url for repository root, deterministic canonical JSON, exclusive output, and byte-identical --write/--check. Preserve D-01 through D-06, D-11 through D-22, D-19R, and D-20R. No package, service, engine, Strategy, formation, product, or live matrix work is permitted.</action>
  <verify><automated>pnpm exec vitest run scripts/evaluate-v1-38-local-seal.test.ts --maxWorkers=1 &amp;&amp; pnpm exec tsx scripts/evaluate-v1-38-local-seal.ts --check</automated></verify>
  <done>The closed state machine, durable burn, tamper-evident ledger, safe projection, honest claims, and deterministic protocol artifact pass all adversarial tests.</done>
</task>
</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Achievable local-seal mechanics before candidate search | 262-45 | COVERED | Synthetic protocol proof only.
REQ | MEAS-10 | Commitment, roots, query policy, safe receipt, contamination | 262-45 | COVERED | Exact state machine and artifact.
REQ | SEAL-01 | Named operator, restricted store, one opening, retirement | 262-45/46 | COVERED | Mechanics here; independent proof next.
RESEARCH | State machine, event chain, crash burn, privacy, honest claims | 262-45 | COVERED | All behaviors explicit.
CONTEXT | D-01..D-22, D-19R/D-20R | Integrity and revised role boundary | 262-45 | COVERED | Historical module remains unchanged.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| operator input -> restricted store | Secrets and preimages cross only into bounded local files outside Git. |
| committed store -> one opening | Root equality and durable consumption precede evaluation. |
| private ledger/result -> repository receipt | Only the allowlisted aggregate projection may cross. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-45-01 | Spoofing | assurance identity and secret-file owner | critical | mitigate | Exact schema/domain, effective-UID ownership/mode/type checks, and explicit excluded claims. |
| T-262-45-02 | Tampering | store/event chain | critical | mitigate | No-follow restricted files, exclusive writes, hashes, sequence/previous-root checks. |
| T-262-45-03 | Repudiation | opening/failure | critical | mitigate | Durable pre-launch consumption and append-only stable-reason events. |
| T-262-45-04 | Information Disclosure | secret ingress and safe receipt | critical | mitigate | Fixed local secret-file interface, no CLI/env/log seam, Buffer cleanup, exact projection bounds, privacy tests, and terminal contamination. |
| T-262-45-05 | Denial of Service | crash/store loss | high | accept | Terminal no-retry failure preserves integrity over availability. |
| T-262-45-06 | Elevation of Privilege | reopen/debug API | critical | mitigate | Closed command surface and adversarial export/transition tests. |
| T-262-45-SC | Tampering | package installs | low | accept | Existing Node and repository helpers only; no install. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-local-seal.test.ts --maxWorkers=1 &amp;&amp; pnpm exec tsx scripts/evaluate-v1-38-local-seal.ts --check &amp;&amp; pnpm turbo typecheck --concurrency=1 &amp;&amp; git diff --check</automated>
</verification>

<success_criteria>
- Every required local-seal transition and adversarial failure is tested and deterministic.
- The opening burns before evaluation launch and no tool-mediated retry is possible.
- Repository-visible output contains no secret, path, raw event, private Strategy data, or inflated assurance claim.
- No historical evidence, engine rule, candidate, formation, live route, or downstream authority changes.
</success_criteria>

<output>Create `262-45-SUMMARY.md` with safe roots and explicit assurance limits; never include real holdout material or local paths.</output>
