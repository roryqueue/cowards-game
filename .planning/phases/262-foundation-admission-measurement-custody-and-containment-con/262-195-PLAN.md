---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "195"
type: tdd
wave: 163
depends_on: [262-193]
files_modified:
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v2.json
  - .planning/debug/phase-262-probe-failed.md
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-195-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "The diagnostic absence predicate admits Docker 29.4 bare-inspect absence only for status 1, null signal, no transport error, stdout exactly `[]\n`, and stderr exactly `Error: No such object: <requested-name>\n`."
    - "The new exact tuple is additive to the already accepted exact absence tuples; every near miss, surplus byte, wrong name, wrong status/signal, daemon failure, permission failure, transport error, or other diagnostic remains unknown and blocks container creation."
    - "Diagnostic-v1 remains byte-immutable `docker_unavailable` at root sha256:e849dd83d14888f2361ec830bf139ef2cddd7f67fd615aad1bfcd1fe4e2587a4 and is never retried, replaced, reinterpreted, or credited."
    - "One fresh exact-image/exact-controls broker-only diagnostic-v2 authenticates the repaired Plan193 Worker lifecycle, invokes zero preflights and zero Matches, and counts as bounded attempt 3 of 10."
    - "Fresh v11 custody and a complete tested v11 operational toolchain bind the separately committed tuple repair, diagnostic-v2, preflight-v10, authorization/review-v11, live/post-run-v11, adjudication/eligibility-v11, and final-tracking-v11 paths while preserving exactly 36 locks."
  artifacts:
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Exact Docker 29.4 tuple predicate plus v11 custody and operational toolchain"
    - path: scripts/check-v1-38-lean-admission.test.ts
      provides: "Positive and near-miss tuple tests plus v11 mutation coverage"
    - path: .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v2.json
      provides: "Fresh immutable attempt-3 broker-only diagnostic"
  key_links:
    - from: scripts/check-v1-38-lean-admission.test.ts
      to: scripts/check-v1-38-lean-admission.ts
      via: "Exact byte tuple table accepts only the observed Docker 29.4 absent-object response"
      pattern: "No such object"
    - from: .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v2.json
      to: .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v10.json
      via: "Exact committed source/tree/closure, image, controls, success lifecycle, cleanup, and diagnostic-v2 root"
      pattern: "workerLifecycleDiagnostic"
---

<objective>
Admit the exact Docker 29.4 bare-inspect absence tuple, bind the additive repair to fresh v11 custody, and rerun only the broker lifecycle diagnostic.

Purpose: Let the diagnostic reach the already repaired Worker lifecycle without broadening absence detection into a permissive stderr heuristic or spending a preflight or Match.
Output: TDD-protected exact tuple repair, complete v11 toolchain, one committed broker-only diagnostic-v2, and summary; zero preflights and zero Matches.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/phase-262-probe-failed.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-EXECUTION-CLOSURE-RESEARCH.md
@.planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v1.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-193-SUMMARY.md
@scripts/check-v1-38-lean-admission.ts
@scripts/check-v1-38-lean-admission.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit only the exact Docker 29.4 bare-inspect absence tuple</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts</files>
  <behavior>
    - "RED proves status 1, signal null, no transport error, stdout bytes exactly `[]\n`, and stderr bytes exactly `Error: No such object: requested-name\n` is accepted as absent."
    - "RED proves the existing exact empty-stdout uppercase tuple and single-newline lowercase Docker 29.4 tuple remain accepted without normalization."
    - "RED rejects empty, `[]`, `[]\n\n`, whitespace-prefixed/suffixed, non-UTF8, or otherwise surplus stdout bytes for the new tuple."
    - "RED rejects stderr case, prefix, suffix, newline, target-name, and surplus-byte deviations, including a valid message naming another container."
    - "RED rejects wrong status, non-null signal, transport error, daemon unavailable, permission denied, timeout, and any combined stdout/stderr response outside the exact allowlist."
    - "RED proves both initial and final diagnostic absence checks use the same exact predicate and fail closed before or after owned-container cleanup."
  </behavior>
  <action>Write the complete positive and negative byte-table tests first. Then modify only the diagnostic's exact absence predicate so the observed Docker 29.4 bare `docker inspect` response is admitted when and only when status is 1, signal is null, transport error is absent, stdout is the three bytes `[]` plus LF, and stderr is the exact uppercase absent-object sentence naming the requested container plus LF. Keep the previously admitted exact tuples as separate alternatives; do not trim, lowercase, parse loosely, use substring or regex matching, ignore stdout, or accept any daemon/permission/transport failure. Route both pre-create and post-remove checks through this one predicate. Preserve Plan193's Worker lifecycle source, image and controls, diagnostic-v1 bytes, all frozen timing and resource bounds, privacy, current rules, and formation absence. Do not invoke Docker, diagnostic, preflight, or Match in this task.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false</automated>
  </verify>
  <done>The exact observed tuple and only the existing exact alternatives classify the requested container as absent; all near misses remain fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind v11 custody and run fresh broker lifecycle diagnostic-v2</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v2.json, .planning/debug/phase-262-probe-failed.md, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-195-SUMMARY.md</files>
  <behavior>
    - "Source-only v11 custody authenticates the separately committed tuple repair commit/tree/executable closure, the exact Plan193 source and diagnostic-v1 denial root, 36 locks, and absence of every fresh v11 operational destination."
    - "Fresh schemas, canonical renderers, validators, writers, selectors, and mutation tests implement diagnostic-v2, preflight-v10, authorization-v11, validity-review-v11, reviewed-ready/live/post-run-v11, adjudication-v11, eligibility-v11, and final-tracking-v11."
    - "Authorization-v11 binds the immutable first lean attempt; all diagnosis, source-fix, diagnostic, and preflight history through Plan196; Plan172's root and five certification-only dispositions; exactly one unconsumed corrective invocation; recoveryAuthorized:false; and all broader authority false."
    - "Diagnostic-v2 uses the exact installed image and frozen controls, exercises one legacy and one v1.17 request through the real persistent Docker exec broker, proves completion/port-close/natural-exit and cleanup, and never constructs or invokes a Match."
    - "The diagnostic-v2 artifact contains only exact source/image/control identities, stage, aggregate counts/timing, cleanup, attempt ordinal 3 of 10, zero preflight invocations, zero Match invocations, and false authority."
  </behavior>
  <action>After Task1 is committed separately, add fresh v11 paths and implement the complete operational toolchain required by Plans196/175/176: source-only-v11 and diagnostic-v2 checks; preflight-v10 and authorization-v11 schemas/renderers/validators/writers/selectors; validity-review-v11 validation/selector; reviewed-ready, live, and post-run-v11 selectors; invocation/terminal-v11 schemas/writers/validators; adjudication/eligibility-v11 schemas/renderers/writers/validators/selectors; and final-tracking-v11 validation. Mutation-test every pass-only edge and fresh destination. Bind diagnostic-v1 by its exact committed root and require it to remain non-pass history; bind Plan193's repair, Plan172's five certification-only findings, all prior attempts, one unconsumed corrective invocation, `recoveryAuthorized:false`, every broader authority false, and exactly 36 locks. Commit all source/test changes before any Docker operation, then pass the source-only-v11 selector with diagnostic-v2 and every v11 operational destination absent. Invoke exactly one uniquely named broker-only diagnostic-v2 under the unchanged digest-pinned image and exact controls. It may exercise only the real broker's legacy and v1.17 public probes and must always remove its owned container; it must not call the preflight evaluator, feasibility runner, Match kernel, Match orchestrator, or any Match selector. Persist and commit only the privacy-safe aggregate artifact, count it as attempt 3 of 10, and update the debug record. If diagnostic-v2 is non-pass, Plan196 remains ineligible and seven bounded attempts remain. Do not create preflight-v10, authorization/review-v11, invocation, marker, terminal, adjudication, eligibility, tracking, archive, or tag effects.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/check-v1-38-lean-admission.test.ts scripts/lib/v1-38-lean-container-match-session.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-worker-lifecycle-source-only-v11 &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-worker-lifecycle-diagnostic-v2 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated>
  </verify>
  <done>The exact tuple repair has passing v11 custody and a complete tested v11 toolchain; diagnostic-v2 truthfully passes or denies with zero preflights, zero Matches, complete cleanup, and immutable prior history.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Make the reviewed lean gate reachable under exact Docker behavior | 262-195 | COVERED | Narrow tuple repair plus real-boundary diagnostic.
REQ | ADMIT-01..ADMIT-04 | Exact source/runtime custody and pass-only admission | 262-195 | COVERED | Fresh v11 custody; no admission effect.
REQ | MEAS-01..MEAS-10 | Preserve schedule, budgets, accounting, cleanup, privacy, and claims | 262-195 | COVERED | Diagnostic-v2 is aggregate-only and non-consuming.
REQ | SEAL-01 | Preserve sealed-holdout state | 262-195 | COVERED | No holdout operation.
REQ | DECI-02 | Hostile-code containment | 262-195 | COVERED | Real broker remains inside unchanged Docker controls.
RESEARCH | — | Fail-closed container ownership and bounded supervised execution | 262-195 | COVERED | Exact allowlist, no heuristic absence.
CONTEXT | D-01..D-22 | Determinism, immutable evidence, hostile input, accounting, privacy, no product authority | 262-195 | COVERED | Explicitly preserved.
CONTEXT | D-34L | Exact 12-cell/two-pass contract | 262-195 | COVERED | No Match schedule or acceptance change.
CONTEXT | D-34L.1 | Seven-category, no-recovery, pass-only closure | 262-195 | COVERED | Source and diagnostic only.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Docker inspect bytes -> ownership state | Only exact absent tuples may permit container creation. |
| host -> Docker exec broker | Two bounded public probes cross into the digest-pinned hostile-code container. |
| committed source -> diagnostic-v2 | Exact tuple repair and Worker lifecycle bytes must authenticate before launch. |
| diagnostic-v2 -> preflight-v10 | Only exact pass may make Plan196 eligible. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-195-01 | Spoofing | container absence predicate | critical | mitigate | Exact status/signal/error/stdout/stderr/name tuple allowlist; reject every near miss. |
| T-262-195-02 | Tampering | diagnostic lineage | critical | mitigate | Exact Git closure and immutable diagnostic-v1 root binding. |
| T-262-195-03 | Elevation of Privilege | guest runtime | critical | mitigate | Preserve digest-pinned Docker controls, fresh Workers, and no fallback. |
| T-262-195-04 | Information Disclosure | diagnostic-v2 | high | mitigate | Aggregate allowlist excludes source, payloads, outputs, memory, and raw diagnostics. |
| T-262-195-05 | Denial of Service | cleanup | high | mitigate | Existing bounded lifecycle plus exact final absence verification. |
| T-262-195-SC | Tampering | package supply chain | low | accept | No package or image installation occurs. |
</threat_model>

<verification>Focused TDD and TypeScript pass; exact positive and near-miss tuple tables pass; source-only v11 authenticates committed bytes and immutable diagnostic-v1; one diagnostic-v2 records attempt 3 with zero preflights/Matches; cleanup, 36 locks, frozen boundaries, and fresh destination absence pass.</verification>

<success_criteria>
- Two TDD tasks across five files.
- Only the exact Docker 29.4 bare-inspect tuple is added to the absence allowlist.
- Diagnostic-v1 remains immutable non-pass history.
- The full tested v11 operational toolchain exists before downstream execution.
- Diagnostic-v2 is attempt 3 of 10 and invokes zero preflights and zero Matches.
- No gameplay, formation, privacy, product, public, archive, or tag change occurs.
</success_criteria>

<output>Create `262-195-SUMMARY.md`; dispatch `262-196-PLAN.md` only after passing source-only v11 custody and committed diagnostic-v2 pass.</output>
