---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "185"
type: tdd
wave: 154
depends_on: [262-184]
files_modified:
  - scripts/lib/v1-38-lean-container-match-session.ts
  - scripts/lib/v1-38-lean-container-match-session.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-185-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "Per D-34L and D-34L.1, one Match creates one hostile container and one long-lived Docker attach or `docker exec -i` stream; all Strategy method calls multiplex over that stream and never start another Docker CLI lifecycle."
    - "The persistent protocol is sequential, request-id bound, byte-capped, deadline-bounded, and fail-closed: malformed, duplicate, missing, stale, out-of-order, surplus, oversized, timed-out, stderr-bearing, or post-poison frames can never become Match evidence."
    - "Host-side coordination may bridge the synchronous adapter to the persistent stream, but no Strategy source executes in a host worker, subprocess, web, API, Go, or fallback path; the unchanged digest-pinned container and hostile controls remain the only execution boundary."
    - "A container name is absent only when Docker returns the exact bounded no-such-object result for that exact validated name; every other status-1 or nonzero outcome is unknown and fails closed both before create and after removal."
    - "Plan184 remains immutable zero-Match refusal history; fresh v6 trust/effect paths are source-only and preserve the sole 24-Match opportunity, all 36 locks, and every frozen gameplay, semantic, runtime, accounting, privacy, formation-absence, 45-second cell, and 900-second outer bound."
  artifacts:
    - path: scripts/lib/v1-38-lean-container-match-session.ts
      provides: "One persistent hostile runtime stream per Match, strict request-id framing, bounded failure handling, and exact absence classification"
    - path: scripts/run-v1-38-lean-runner-feasibility.ts
      provides: "Actual persistent-session preflight and unchanged prepared Match wiring"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Fresh preflight-v5 and v6 authorization/review/effect custody"
  key_links:
    - from: scripts/lib/v1-38-lean-container-match-session.ts
      to: Docker container runtime
      via: "Exactly one long-lived `docker exec -i` or attach process per Match, with every method request carried by the same framed stream"
      pattern: "requestId|stdin|stdout"
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: scripts/lib/v1-38-lean-container-match-session.ts
      via: "One session is opened and closed around each Match, never around each method"
      pattern: "createLeanContainerMatchSession"
    - from: scripts/check-v1-38-lean-admission.ts
      to: .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v5.json
      via: "Fresh collision-free LEAN_DIRECT_V6_ARTIFACT_PATHS family"
      pattern: "LEAN_DIRECT_V6_ARTIFACT_PATHS"
---

<objective>
Remove the two Plan184 result-validity blockers by replacing per-method Docker execution with one persistent Match-scoped stream and making container-name absence exact and fail-closed.

Purpose: Bring the already-frozen 24-Match feasibility gate inside its unchanged deadlines without weakening hostile containment, evidence validity, or cleanup.
Output: Separately committed TDD source and fresh v6 source custody; no Docker preflight, authorization, review, marker, Match, terminal, tracking, archive, or tag effect.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/artifacts/v1.38-lean-runner-direct-container-preflight-v4.json
@.planning/artifacts/v1.38-lean-runner-direct-validity-review-v5.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-184-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-184-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-EXECUTION-CLOSURE-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multiplex every Match method over one persistent hostile stream</name>
  <files>scripts/lib/v1-38-lean-container-match-session.ts, scripts/lib/v1-38-lean-container-match-session.test.ts, scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts</files>
  <behavior>
    - "RED proves that opening one Match and invoking multiple legacy and v1.17 ABI methods causes exactly one container create/start and exactly one long-lived Docker attach or exec process, with no Docker exec/create/start per method."
    - "Each request carries the next contiguous positive request ID and one exact mode/payload/deadline/cap envelope; each response must echo that ID and use the exact response shape. Missing, duplicate, stale, future, reordered, malformed, surplus, noncanonical-base64, oversized, truncated, extra, or unsolicited frames poison the session."
    - "The persistent in-container broker invokes the existing legacy or v1.17 harness behavior for each request in a fresh method execution context, preserves exact input/output bytes and failure classification, and retains no Strategy globals, module cache, output, memory, or request data across method calls beyond explicit canonical request state."
    - "Only one request may be in flight. The existing per-method startup, wall, cancellation-grace, stdout, stderr, and output limits remain authoritative; timeout, broker exit/signal, transport error, stderr, cap overflow, or protocol desynchronization terminates the persistent stream, poisons the Match session, performs bounded owned-container cleanup, and permits no retry or fallback."
    - "A graceful close terminates the broker/stream, observes its bounded terminal receipt, removes only the exact owned container, proves exact absence, and is idempotent; forced close after any failure owns both the long-lived Docker process and container cleanup and never reports complete while either may remain."
    - "The measured method samples used by preflight are round trips on one already-established persistent stream. One-time container and stream establishment/cleanup are measured separately once per Match, so the unchanged projection is one lifecycle plus 20 select and 240 brain calls, not 260 Docker lifecycles."
  </behavior>
  <action>First commit RED tests using an injectable persistent-stream factory and control transport. Require exact call inventories, multiple sequential requests on one stream, mixed legacy/v1.17 byte payloads, contiguous request IDs, one-in-flight ordering, all frame corruptions, method and stream deadline failures, broker exit/signal/stderr, cleanup after every poison branch, idempotent close, and a regression that fails if a method issues `docker exec`, `docker create`, or `docker start`. Then implement one Match-scoped persistent hostile runtime channel. Prefer one long-lived `docker exec -i` process or one attach process after the existing controlled create/start sequence; do not issue a separate Docker command per method. Because the canonical adapter is synchronous, a trusted host coordination worker may own the asynchronous Docker stream and expose a bounded synchronous request/response bridge, but it must never evaluate Strategy source or become an execution fallback. Inside the container, keep one trusted broker alive for the Match and run each request through the exact existing legacy or v1.17 harness semantics in a fresh per-request execution context; transient in-container method children are acceptable when needed to preserve fresh-process semantics, but the host must retain exactly one Docker stream lifecycle for the Match. Use canonical newline-delimited exact-key JSON envelopes with contiguous positive integer request IDs and canonical base64 for opaque input/output bytes, or an equivalently strict length-delimited JSON framing proven by the same tests. Bound encoded and decoded frame sizes before allocation/use; validate exact keys, types, IDs, base64 canonicality, EOF, stderr, and no trailing/unsolicited bytes. Keep the existing digest-pinned image, network none, read-only root, noexec tmpfs, memory/CPU/PID caps, dropped capabilities, no-new-privileges, minimal environment, adapter metadata, runtime ABI, fixture construction, schedule, method ceilings, 45-second cell limit, 900-second outer limit, and no-fallback behavior unchanged. Update the actual non-consuming preflight to warm and measure multiple method requests on the same established stream, while measuring lifecycle once per Match and using the unchanged conservative 20/240 projection. Emit only privacy-safe aggregate timings and counts. Do not invoke Docker or either preflight/live selector while executing this plan.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1</automated></verify>
  <done>Every method in a Match uses one persistent Docker stream with strict framed correlation and unchanged semantics/limits; no per-method Docker lifecycle or fallback remains reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove exact absence and establish fresh v6 custody</name>
  <files>scripts/lib/v1-38-lean-container-match-session.ts, scripts/lib/v1-38-lean-container-match-session.test.ts, scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-185-SUMMARY.md</files>
  <behavior>
    - "RED proves status 1 with daemon, permission, format, timeout, signal, malformed, empty, wrong-target, extra-line, or other stderr is `unknown`, never `absent`, at both the pre-create gate and post-remove confirmation."
    - "Only the exact bounded Docker no-such-object diagnostic naming the exact validated container name classifies absent; status, signal, stdout, stderr, and transport error must otherwise match the closed absence predicate exactly."
    - "Owned and foreign present names retain existing exact-label behavior; an unknown precheck prevents create, and an unknown post-remove check reports incomplete cleanup/orphan without retry, evidence, or fallback."
    - "Fresh paths are exactly direct-container-preflight-v5, direct-authorization-v6, direct-validity-review-v6, direct-invocation-v6, direct-terminal-v6, direct-adjudication-v6, and phase-262-lean-direct-eligibility-v6."
    - "Fresh v6 source-only/readiness/effect validators bind the separately committed Plan185 runnable commit/tree/executable closure, one persistent stream per Match, exact absence classification, unchanged frozen bounds, immutable Plan184 and earlier history, 36 locks, and zero consumed corrective Match invocations."
  </behavior>
  <action>Add RED faults for exact no-such-object acceptance and every non-absence status-1/error variant at precheck and post-remove. Replace the broad `status === 1` predicate with one closed parser for Docker's bounded exact-name no-such-object response; require exact target equality and reject all extra or ambiguous output. Keep ownership-label inspection and matching-only removal unchanged. Add source tests that fail if the runnable session contains a per-method Docker exec path, permits more than one persistent Docker stream per Match, loses request-ID correlation, or weakens timeout/cleanup ownership. Add an additive `LEAN_DIRECT_V6_ARTIFACT_PATHS` family and preflight-v5, authorization-v6, exactly-seven-category review-v6, reviewed-ready-v6, invocation/terminal/adjudication/eligibility-v6, post-run, and final-tracking schemas/renderers/writers/checkers/CLI selectors following the v5 shape. The source-only checker must bind the exact separately committed Plan185 runnable commit—not HEAD or a docs child—verify current executable blobs, fresh destination absence, immutable v5 refusal roots, all 36 locks, persistent-stream and exact-absence source invariants, one unconsumed invocation, no recovery/relaunch/partial reuse, and all authority false. Rewire the dormant live selector to v6 readiness and effects without invoking it. Commit all runnable source/tests before the summary, record exact commit/tree/closure in the summary, and run only focused tests, TypeScript, source-only checking, lock counting, and diff checks. Do not invoke Docker, write any operational v6 artifact, create a marker, run a Match, edit tracking, or expand the seven-category result-validity scope into certification.</action>
  <verify><automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-container-stream-source-only-v6 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated></verify>
  <done>Exact absence is proven fail-closed at both lifecycle gates and fresh v6 custody authenticates the persistent-stream source with no operational or Match effect.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Remove the final measured runner blockers without consuming the run | 262-185 | COVERED | TDD source-only repair.
REQ | ADMIT-01..ADMIT-04 | Exact current source/runtime admission and fail-closed drift | 262-185 | COVERED | Persistent stream plus fresh custody.
REQ | MEAS-01..MEAS-10 | Preserve schedule, limits, accounting, cleanup, claims, and privacy | 262-185 | COVERED | No empirical/gameplay change.
REQ | SEAL-01 | Preserve holdout/local-seal truth | 262-185 | COVERED | No holdout action.
REQ | DECI-02 | Preserve hostile containment | 262-185 | COVERED | Container remains sole hostile boundary.
RESEARCH | — | Bounded result-validity proof rather than production certification | 262-185 | COVERED | Exactly two measured blockers.
CONTEXT | D-01..D-22 | Integrity/runtime/privacy/accounting | 262-185 | COVERED | Strict IPC and fail-closed cleanup.
CONTEXT | D-34L | One corrected 24-Match opportunity and fixed deadlines | 262-185 | COVERED | No Match effect; performance repair only.
CONTEXT | D-34L.1 | Seven-category no-recovery closure | 262-185 | COVERED | Fresh v6 trust/effect path.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| synchronous host adapter -> persistent stream coordinator | Only framed opaque requests cross; no Strategy execution occurs in the coordinator. |
| persistent Docker stream -> in-container broker | IDs, lengths, modes, deadlines, and exact keys must agree for every request/response. |
| in-container broker -> hostile Strategy harness | Existing fresh-request semantics and resource limits remain authoritative. |
| Docker inspect/remove -> cleanup result | Only exact-name absence or exact ownership can close cleanup. |
| Plan184 refusal -> v6 source | Historical non-pass cannot become current evidence or authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-185-01 | Spoofing | persistent response frames | critical | mitigate | Contiguous request IDs, exact envelopes, one in-flight request, and poison on mismatch. |
| T-262-185-02 | Tampering | method bytes/semantics | critical | mitigate | Canonical base64/length validation and exact existing harness behavior per request. |
| T-262-185-03 | Elevation of Privilege | host coordinator/fallback | critical | mitigate | Coordinator never evaluates Strategy; container-only execution and no fallback are source-tested. |
| T-262-185-04 | Denial of Service | stream timeout/exit/cleanup | critical | mitigate | Per-request deadlines, bounded stream termination, owned removal, and exact absence proof. |
| T-262-185-05 | Information Disclosure | IPC/preflight evidence | high | mitigate | Raw source/frames/stderr remain transient; artifacts expose safe aggregates only. |
| T-262-185-06 | Repudiation | v6 custody | high | mitigate | Separate source commit/tree/closure and immutable v5 roots. |
| T-262-185-SC | Tampering | package/image supply chain | low | accept | No package install or image pull; exact existing digest remains frozen. |
</threat_model>

<verification>Focused RED/GREEN tests prove exactly one persistent Docker stream per Match, strict request correlation, unchanged method semantics and limits, poison/cleanup on every fault, and exact absence classification. TypeScript and source-only v6 custody pass; Plan184 history and 36 locks remain unchanged; all v6 operational paths remain absent.</verification>

<success_criteria>
- Two focused TDD tasks across seven files.
- Multiple method calls produce one Docker stream lifecycle per Match and zero per-method Docker lifecycle.
- Strict framing, request IDs, deadlines, byte caps, fresh method semantics, and cleanup are fully fault-tested.
- Only exact no-such-object for the exact name means absent; every other status-1 branch fails closed.
- Fresh v6 custody cannot consume or reinterpret Plan184 or older artifacts.
- No Docker preflight, authorization, review, marker, Match, terminal, tracking, archive, or tag effect occurs.
</success_criteria>

<output>Create `262-185-SUMMARY.md` after the separately committed runnable fix; dispatch Plan186 for one fresh non-consuming preflight and independent review.</output>
