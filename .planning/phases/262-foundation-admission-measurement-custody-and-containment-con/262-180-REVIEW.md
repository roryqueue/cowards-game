---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "180"
reviewed: 2026-09-08T17:42:00Z
depth: deep
files_reviewed: 8
files_reviewed_list:
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - packages/runtime-js/src/container-subprocess-adapter.ts
  - apps/runtime-service/src/execute-match.ts
  - .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v2.json
  - .planning/artifacts/v1.38-lean-runner-direct-validity-review-v3.json
findings:
  critical: 1
  warning: 0
  info: 0
  total: 1
status: issues_found
blockingFindingCount: 1
admitsPlan175: false
---

# Phase 262 Plan 180: Direct Result-Validity Review v3

**Outcome:** denied with one active blocker. The immutable preflight is `non_pass`, authorization v3 is absent, and the current fresh-container-per-method topology cannot complete the frozen schedule within its unchanged deadlines. Plan 175 is not admitted.

## Summary

This was one fresh independent deep review of exact runnable source commit `4261dcd0998f81dbd822f1c435ff92d8d41483bb`, tree `6c97c1075aacb89f1da6688e275caeb007c68f97`, executable closure root `sha256:a126ceda28183d64afd3fff950f55eea72ab8be6619262504855a063fede6462`, and the separately committed v2 preflight at commit `20cf27e6f1efef9c72ce013bca7d8d08a149b596`.

The preflight is a complete, schema-valid `non_pass/container_preflight_refused` outcome with one non-consuming preflight invocation and zero Match invocations. Read-only lifecycle evidence narrows the refusal to throughput rather than container failure: all 16 container lifecycles reached exit `0`, but fresh-container startup on every Strategy method creates a conservative lower-bound projection of `63,790 ms` per cell and `1,530,960 ms` for the run. Those figures already exceed the frozen `45,000 ms` cell and `900,000 ms` outer deadlines, and adapter wall time can only be greater. The pass-only authorization path correctly left authorization v3 absent, so `admitsPlan175:false` is mandatory.

The review did not invoke a Docker probe, preflight, live or recovery selector, Match, marker, terminal, adjudication, eligibility, requirements/tracking, archive, or tag path. The executable closure remains byte-identical to the reviewed source; current descendants change only committed planning artifacts. Exactly 36 authenticated successor locks remain, and all v3 runtime/effect destinations are absent.

## Narrative Findings (AI reviewer)

### CR-01: Fresh-container-per-method execution cannot meet the frozen schedule deadlines

**Classification:** BLOCKER

**Category:** `tuple_or_schedule_drift`

**Files:** `scripts/run-v1-38-lean-runner-feasibility.ts:460-475`; `packages/runtime-js/src/container-subprocess-adapter.ts:106-139`

**Issue:** Each preflight Strategy method call enters `adapter.execute`, which performs a fresh `docker run`. The one permitted preflight completed 16 create/start/die/destroy lifecycles with exit `0`, so this is not an image, harness, or containment failure. With warm probes excluded, measured start-to-die lower-bound maxima were `204.305 ms` for `selectActivations` and `105.455 ms` for `soldierBrain`. Applying those lower bounds to the frozen method ceilings yields at least `63,790 ms` per cell and `1,530,960 ms` for 24 Matches, exceeding the fixed `45,000 ms` and `900,000 ms` limits. Because the adapter adds time outside the observed lifecycle, the actual total cannot rescue the result.

**Fix:** Add a source-only private-lab container session that remains alive for one Match and accepts the Match's bounded Strategy method requests through strict framed JSON IPC, then destroys the container before the next Match. Keep the exact digest-pinned image, no-network/read-only/tmpfs/memory/CPU/PID/capability/no-new-privileges controls, hostile output/schema validation, runtime semantics, tuple, schedule, and both deadlines unchanged. Test cleanup, timeout, protocol desynchronization, cross-Match isolation, and no fallback; then run a newly planned non-consuming preflight. Do not change a frozen bound or consume the sole Match opportunity during the repair.

## Seven-Category Outcome

| # | Category | Status | Independent conclusion |
|---:|---|---|---|
| 1 | `source_or_dirty_byte_drift` | PASS | Commit, tree, executable closure, latest runnable-source commit, tracked executable bytes, and 36-lock inventory all agree. |
| 2 | `multiple_launch` | PASS | Reviewed readiness and the non-consuming runtime preflight precede exclusive marker creation; absent authorization makes the selector unreachable before launch. All v3 effects are absent. |
| 3 | `tuple_or_schedule_drift` | **BLOCKER** | The tuple is unchanged, but fresh container startup per method projects to at least 63.8 seconds per cell and 25.5 minutes per run, so the existing execution topology cannot meet the frozen schedule deadlines. |
| 4 | `supervised_runtime_escape` | PASS | The reachable path binds `container-subprocess` / `runtime-js-container-subprocess`, the digest-pinned image, restrictive Docker controls, strict IPC, and `fallback:false`. Non-pass grants no fallback. |
| 5 | `partial_interrupted_or_unclean_evidence` | PASS | The committed outcome is a complete refusal with zero Matches and false authority, not a partial pass. No downstream effect exists. |
| 6 | `private_data_disclosure` | PASS | New artifacts are aggregate-only and contain no Strategy source or memory, objective payload, streams, environment, host paths, credentials, or raw diagnostics. |
| 7 | `non_pass_authority` | PASS | Exact non-pass produced no authorization. Reviewed readiness requires exact pass plus exact authorization; this review denies Plan 175 and keeps every authority false. |

## Denied-Branch Integrity

- Preflight root: `sha256:46a4c2cd629edffbba99bee8797761a0e434fa7972a88e71aee7389ae1df7a1e`.
- Authorization v3 is absent and this review claims `authorizationRoot:null`.
- Invocation v3, terminal v3, adjudication v3, and eligibility v3 are absent.
- The sole corrective Match opportunity remains unconsumed: the preflight records `matchInvocations:0`.
- Plan 174 remains denied history, and Plan 178 remains immutable non-pass history.
- Plan 172 findings `CR-V6-01` through `CR-V6-04` and `WR-V6-01` remain open certification-only, nonblocking history under D-34L.1; they are not erased or counted as active result-validity findings.

---

_Reviewed: 2026-09-08T17:42:00Z_

_Reviewer: independent gsd-code-reviewer `/root/execute_262_180/review_262_180`_

_Depth: deep_

_Disposition: denied with one active result-validity blocker; Plan 175 not admitted_
