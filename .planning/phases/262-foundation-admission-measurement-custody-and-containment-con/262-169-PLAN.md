---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "169"
type: tdd
wave: 140
depends_on: [262-168]
files_modified:
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-169-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-03, ADMIT-04, MEAS-04, MEAS-08, MEAS-09, DECI-02]
must_haves:
  truths:
    - "The exact CR-168-01 function-declaration bypass fails before implementation and passes only after the recovery proof traverses its call to buildLeanSchedule."
    - "Recovery reachability is derived from parsed TypeScript syntax and sound relative-module resolution across top-level function declarations, arrow functions, function expressions, imports, aliases, and re-exports."
    - "Every reachable bare callee that is neither resolved nor present in an explicit audited inert allowlist fails closed, while higher-order callback properties and capability-bearing Node member calls are proven at their exact call sites."
    - "The failed v4 manifest/review and all older history remain immutable and non-authorizing; no readiness, invocation, recovery, Match, or other corrective effect is created."
  artifacts:
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "AST and module-resolution-sound recovery capability proof plus additive v5 trust contracts"
    - path: scripts/check-v1-38-lean-admission.test.ts
      provides: "Exact CR-168-01 regression and exhaustive reachable-callee call-shape mutations"
    - path: scripts/run-v1-38-lean-runner-feasibility.ts
      provides: "Fresh-v5-only corrective admission with unchanged inert recovery route"
  key_links:
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: scripts/check-v1-38-lean-admission.ts
      via: "actual recovery selector parsed and traversed from its real entry point"
      pattern: "checkLeanCorrectiveRecoveryOnlyStructure"
    - from: .planning/artifacts/v1.38-lean-runner-corrective-source-review-v4.json
      to: scripts/check-v1-38-lean-admission.test.ts
      via: "CR-168-01 exact adversarial regression"
      pattern: "unsafeRecoveryHop"
---

<objective>
Close only CR-168-01 by replacing the fail-open regex reachability proof with parsed TypeScript/module traversal and preparing a fresh v5-only trust path without effects.

Purpose: Make recovery incapability structurally sound before the one remaining complete corrective rerun can be reviewed again.
Output: RED/GREEN source and tests plus summary; no manifest, review, readiness, invocation, recovery, or Match effect.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-ADMISSION-RESEARCH.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-167-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-168-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-168-SUMMARY.md
@.planning/artifacts/v1.38-lean-runner-corrective-source-manifest-v4.json
@.planning/artifacts/v1.38-lean-runner-corrective-source-review-v4.json
</context>

<feature>
  <name>Fail-closed recovery call-graph proof</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts</files>
  <behavior>
    - "RED exact regression: inserting `function unsafeRecoveryHop(): void { buildLeanSchedule() }` and calling it from `terminalizeLeanCorrectiveInterruption` must initially demonstrate the reviewed bypass, then GREEN must reject it as launch capability."
    - "Top-level function declarations, parenthesized and single-parameter arrows, and function expressions assigned through const/let bindings are indexed and traversed when reachable."
    - "Default, named, aliased, and namespace relative imports, local aliases, and named/default re-exports resolve to their actual local declarations across multiple hops."
    - "Every reachable bare callee unresolved after lexical and relative-module resolution fails closed unless its exact symbol is in a small explicit audited inert-global allowlist."
    - "The actual selector's object-literal callbacks are bound through `runLeanCorrectiveRecoveryOnlyInjected` parameter/property dataflow; mutations behind cleanup, terminalizeInvalid, or postcheck remain reachable and rejected."
    - "Required `execFileSync` and `process.kill` calls pass only under exact callee-and-argument policies for the authenticated recovery operations; every other property call, NewExpression, computed call, dynamic import, non-relative import, ambiguous binding, parse error, or unsupported reachable shape fails closed."
    - "The current actual recovery route passes and remains unable to enumerate a schedule, build a request/Match, create a marker, launch a child, send execute IPC, or invoke the normal wrapper."
  </behavior>
  <implementation>Per D-34L, first commit one RED regression containing the exact `unsafeRecoveryHop` function-declaration bypass from CR-168-01; do not substitute an arrow-function approximation. Replace the definition/call regexes with the repository's installed TypeScript compiler API, parsing each supplied runner/checker/local relative module into a SourceFile and building a lexical declaration/import/export index. Resolve every reachable CallExpression from the actual recovery selector and its helpers through function declarations, variable-bound arrows/functions, local aliases, default/named/namespace imports, and re-exports. Resolve relative specifiers using the supplied module map and the same `.js` to TypeScript source identity convention used by the runner. Model the real `runLeanCorrectiveRecoveryOnlyInjected({...})` call: bind the object-literal callback properties to the helper's `dependencies` parameter and follow `dependencies.cleanup()`, `dependencies.terminalizeInvalid()`, and `dependencies.postcheck()` into their exact callback bodies. Maintain an explicit, narrowly audited set of inert language/global calls actually required by the valid recovery graph; reject every other unresolved bare callee. Do not globally allowlist member calls: validate the reachable `execFileSync` and `process.kill` sites by exact imported/builtin identity and their fixed command/signal/argument forms, and fail closed for every other property call, NewExpression, computed/dynamic/external/ambiguous binding, or parse failure. Preserve the forbidden capability set and producer-terminal checks. Add mutations covering every required declaration/import shape, unresolved bare calls at first and later hops, each of the three callback-property edges, forbidden code hidden behind a property/NewExpression, and altered child-process/process-signal arguments. Add v5 manifest/review/readiness schemas, selectors, exact paths, an outcome-aware `--check-corrective-review-outcome-v5` selector accepting exactly nonzero-plus-readiness-absent or literal-zero-plus-valid-readiness, and runner consumption by mechanically extending the v4 contract while binding the immutable nonzero v4 review and all older roots; only v5 may later authorize Plan158. Preserve all existing artifacts byte-for-byte, all corrective counters at zero, the single remaining rerun, and exactly 36 locks. Commit GREEN separately. Do not render v5 artifacts or invoke live/recovery selectors.</implementation>
</feature>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Close the sole reviewed recovery-proof blocker before rerun | 262-169 | COVERED | Source/tests only.
REQ | ADMIT-03 | Preserve one reviewed whole corrective rerun | 262-169 | COVERED | Fresh v5 path, zero consumed.
REQ | ADMIT-04 | Reject unresolved or drifting recovery capability | 262-169 | COVERED | Parsed fail-closed graph.
REQ | MEAS-04 | Recovery cannot construct evidence-bearing work | 262-169 | COVERED | Structural proof only.
REQ | MEAS-08 | Preserve fixture-only claim | 262-169 | COVERED | No Match or inference.
REQ | MEAS-09 | Separate failed review, repair, review, and execution | 262-169 | COVERED | Additive v5 only.
REQ | DECI-02 | Preserve containment and downstream denial | 262-169 | COVERED | All authority false.
RESEARCH | — | Correct only a specific diagnosed implementation defect before one full rerun | 262-169 | COVERED | CR-168-01 only.
CONTEXT | D-01..D-22 | Immutable evidence, privacy, accounting, runtime, containment | 262-169 | COVERED | No effects; exact history.
CONTEXT | D-23R..D-33R | Retired full-matrix routes | 262-169 | EXCLUDED | Immutable history only.
CONTEXT | D-34L | At most one complete corrective rerun after reviewed fix | 262-169 | COVERED | No invocation or readiness emitted.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| TypeScript source -> structural graph | Every reachable call form must be parsed and resolved without regex omissions. |
| relative module edge -> declaration | Imports, aliases, and re-exports must resolve uniquely or fail closed. |
| failed v4 history -> v5 trust path | Nonzero v4 review stays immutable and cannot authorize execution. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-169-01 | Elevation of Privilege | reachable bare callee | critical | mitigate | AST traversal rejects every unresolved non-allowlisted call. |
| T-262-169-02 | Tampering | module resolution | critical | mitigate | Exact relative-module mapping with ambiguity and parse failure rejection. |
| T-262-169-03 | Spoofing | declaration shape | critical | mitigate | Index all required function/arrow/expression and import/export shapes. |
| T-262-169-04 | Repudiation | failed v4 review | high | mitigate | Root-bind it as immutable non-authorizing predecessor. |
| T-262-169-05 | Information Disclosure | tests/source artifacts | high | mitigate | Synthetic source mutations and safe aggregates only. |
| T-262-169-SC | Tampering | package supply chain | low | accept | Use the installed TypeScript compiler API; no package installation. |
</threat_model>

<verification>
  <automated>pnpm exec vitest run scripts/lib/v1-38-lean-runner-feasibility.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-corrective-recovery-only-structure &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-corrective-source-review-v4 &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-corrective-source-only &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; test ! -e .planning/artifacts/v1.38-lean-runner-corrective-source-manifest-v5.json &amp;&amp; test ! -e .planning/artifacts/v1.38-lean-runner-corrective-source-review-v5.json &amp;&amp; test ! -e .planning/artifacts/v1.38-lean-runner-corrective-readiness-v5.json &amp;&amp; git diff --check</automated>
</verification>

<success_criteria>
- The exact unsafeRecoveryHop function-declaration bypass is RED then rejected after GREEN.
- Every required reachable local/relative call shape resolves; every unresolved bare reachable callee fails closed.
- Current recovery-only structure passes without weakening any launch-capability prohibition.
- v5 trust support is source-only; no v5 artifact or corrective effect exists.
- All predecessor history, the one remaining rerun, and 36 locks are preserved.
</success_criteria>

<output>Create `262-169-SUMMARY.md`; dispatch `262-170-PLAN.md` only after source/tests and summary are separately committed with no effects.</output>
