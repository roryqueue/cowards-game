---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "177"
type: execute
wave: 146
depends_on: [262-174]
files_modified:
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-177-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "Per D-34L.1, both active Plan174 result-validity findings are closed in separately committed runnable source without rewriting the denied v1 authorization or review."
    - "The fixed TypeScript fixtures are rebuilt for runtime-js-container-subprocess and execute only through container-subprocess using node:24-alpine@sha256:2bdb65ed1dab192432bc31c95f94155ca5ad7fc1392fb7eb7526ab682fa5bf14 with the existing hostile limits."
    - "A non-consuming readiness/performance preflight checks Docker, the exact local image, the actual container harness, both fixture methods, and conservative per-method throughput before any invocation marker can be created."
    - "The direct authorization v2 writer accepts an explicit separately committed runnable source commit/tree/closure and rejects HEAD or a documentation-only descendant as a default source identity."
    - "No authorization, review, preflight effect, Match, marker, terminal, adjudication, or authority is created in this plan."
  artifacts:
    - path: scripts/run-v1-38-lean-runner-feasibility.ts
      provides: "Container-confined fixture revision construction plus pre-marker readiness/performance preflight"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Exact direct authorization v2 renderer/writer/checker over explicit runnable Git bytes"
  key_links:
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: packages/runtime-js/src/container-subprocess-adapter.ts
      via: "runtime-js-container-subprocess revisions and container-subprocess service selection using the exact digest"
      pattern: "runtime-js-container-subprocess"
    - from: scripts/check-v1-38-lean-admission.ts
      to: .planning/artifacts/v1.38-lean-runner-direct-authorization-v2.json
      via: "Explicit separately committed source commit/tree/closure; never inferred from current HEAD"
      pattern: "LEAN_DIRECT_AUTHORIZATION_V2"
---

<objective>
Close exactly the two active Plan174 validity findings in a separately committed container-confined runnable closure.

Purpose: Make the existing one-shot 24-Match gate reviewable against exact source custody and a real hostile-Strategy boundary before any effect is authorized.
Output: Tested runner/checker source and exact source-only summary; no authorization, review, preflight artifact, Match, or marker.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/milestone-proposals/v1.38-competitive-strategy-factory-and-adversarial-league/ACTIVATION-PROMPT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/artifacts/v1.38-lean-runner-direct-authorization-v1.json
@.planning/artifacts/v1.38-lean-runner-direct-validity-review-v1.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-174-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-174-SUMMARY.md
@packages/runtime-js/src/container-subprocess-adapter.ts
@packages/runtime-js/src/executor.ts
@packages/runtime-js/src/revision.ts
@apps/runtime-service/src/runtime-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the container-boundary and pre-marker feasibility findings</name>
  <files>scripts/run-v1-38-lean-runner-feasibility.ts, scripts/run-v1-38-lean-runner-feasibility.test.ts</files>
  <behavior>
    - "Starter and Advanced fixture revisions retain their fixed source, library lineage, and Strategy identity inputs while being rebuilt with the registry record for runtime-js-container-subprocess, a matching source artifact, provider validation, and runtime compatibility identity."
    - "The prepared Match service selects container-subprocess and the exact digest-pinned node image; worker-thread, tag-only image fallback, host subprocess fallback, and silent adapter substitution are rejected."
    - "The existing no-network, read-only-root, no-new-privileges, dropped-capability, tmpfs, 64m memory, 0.5 CPU, 64 PID, minimal-environment, shell-disabled, stdout/stderr, and method-timeout controls remain active."
    - "The preflight runs before marker creation, invokes no Match, writes no charged evidence, proves Docker server and exact local RepoDigest identity, validates both compiled fixture methods through the actual container harness, and measures method latency."
    - "The preflight fails before marker when either method fails, isolation/image identity drifts, or conservative projected cell/run duration cannot fit both the unchanged 45000 ms cell deadline and 900000 ms outer deadline."
  </behavior>
  <action>Per D-01 through D-06, D-16, D-34L, and D-34L.1, first add RED cases for both Plan174 findings and every preflight fail-closed branch. Rebuild each fixed library fixture with `buildStrategyRevision` using the exact `runtime-js-container-subprocess` registry record while preserving its source, system Strategy ID, Starter/Advanced lineage metadata, and deterministic artifact/provenance derivation. Configure `createRuntimeServiceConfig` with `strategyExecutionAdapter: "container-subprocess"` and `containerImage: "node:24-alpine@sha256:2bdb65ed1dab192432bc31c95f94155ca5ad7fc1392fb7eb7526ab682fa5bf14"`; do not add a worker, subprocess, floating-tag, or in-process fallback. Add an injectable preflight that checks a responsive Docker 29.4.0-compatible server, requires the exact digest to be present locally without pulling, verifies the actual locked container arguments/resource controls, and sends valid compiled fixture probes for both `selectActivations` and `soldierBrain` through the same harness. Measure multiple warmed invocations per method with monotonic time, record sample counts and safe aggregate latency statistics, and compute conservative projected duration from the frozen per-Match method ceilings (`selectActivations` 20 and `soldierBrain` 240), two entrants, 24 cells, the unchanged 45000 ms per-cell limit, and 900000 ms outer limit. Include fixed startup/cleanup margin and require both ceilings to fit; missing observations, non-finite values, command/image drift, probe failure, or insufficient headroom fails before marker. Wire the live selector as check reviewed-ready, then rerun the same non-consuming preflight, then exclusively create the marker; no preflight branch may create the marker or Match. Emit only allowlisted Docker/image/adapter/limits/latency/count/feasibility fields, never raw source, IPC, stderr, environment, host paths, or diagnostics.</action>
  <verify><automated>pnpm exec vitest run scripts/run-v1-38-lean-runner-feasibility.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false</automated></verify>
  <done>The runnable path uses the exact digest-pinned hostile container boundary, and infeasible container-per-method throughput is rejected before marker creation without consuming the sole invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit source custody and the direct authorization v2 trust path</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-177-SUMMARY.md</files>
  <behavior>
    - "The new authorization binds an explicitly supplied, already committed runnable commit/tree/blob closure plus the exact preflight root, image digest, adapter identity, frozen schedule, immutable history, and exhaustive false authority."
    - "Supplying current HEAD, the Plan177 summary commit, another documentation-only descendant, an ancestor with stale runnable blobs, or a dirty tracked closure is rejected."
    - "The denied v1 authorization and review remain byte-identical historical inputs and are never overwritten, deleted, accepted as current, or relabeled."
  </behavior>
  <action>Implement additive exact-key paths and schemas for `v1.38-lean-runner-direct-authorization-v2`, its container preflight, and its fresh review. The renderer/writer must require an explicit source commit and independently derive its tree and executable blob closure with raw Git-object reads. It must prove that the supplied commit is the separately committed Plan177 runnable source commit, that the commit changes the authorized source/test set rather than documentation only, that its closure equals current tracked runnable bytes, and that later documentation commits are descendants only. Reject omission, `HEAD` as a sentinel/default, current summary/docs-child identity, stale tree/blob closure, dirty tracked bytes, non-ancestor source, missing preflight, or a preflight that is not exact-pass. Bind the exact digest-pinned image, container adapter/controls, performance formula and measured root, Plan174 denied authorization/review roots and both findings as preserved resolved-by-new-source history, Plan172's five open certification-only findings unchanged, one unconsumed invocation, recovery/partial reuse/relaunch false, and all broader authority false. Add source-only, preflight, authorization, review-outcome, reviewed-ready, post-run, adjudication, and final-tracking checks on only the additive v2 trust path. Commit source/tests before writing the summary, run the source-only checker against that exact commit, and record commit/tree/blob closure in the summary. Do not invoke the preflight writer or authorization writer and do not create any effect artifact.</action>
  <verify><automated>pnpm exec vitest run scripts/check-v1-38-lean-admission.test.ts scripts/run-v1-38-lean-runner-feasibility.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-container-source-only-v2 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated></verify>
  <done>One separately committed runnable source/tree/closure is available for Plan178; v1 history is untouched and every preflight/authorization/review/live destination remains absent.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Restore a reviewable hostile-runtime fixture gate | 262-177 | COVERED | Source and tests only.
REQ | ADMIT-01..ADMIT-04 | Exact current runtime admission under released authority | 262-177 | COVERED | Explicit Git custody and fail-closed preflight.
REQ | MEAS-01..MEAS-10 | Preserve tuple, schedule, budgets, accounting, cleanup, claims, and privacy | 262-177 | COVERED | Frozen values and safe aggregates.
REQ | SEAL-01 | Preserve local-seal/holdout boundary | 262-177 | COVERED | No holdout access or authority.
REQ | DECI-02 | Preserve containment | 262-177 | COVERED | Exact container boundary and no fallback.
RESEARCH | — | Close source identity and hostile-runtime findings before review | 262-177 | COVERED | Exact two-finding closure.
CONTEXT | D-01..D-22 | Integrity, runtime, accounting, privacy, and containment | 262-177 | COVERED | Tests and explicit closure.
CONTEXT | D-34L | One unchanged 24-Match corrective gate | 262-177 | COVERED | No charge or invocation yet.
CONTEXT | D-34L.1 | Seven-category direct no-recovery closure | 262-177 | COVERED | Active trust path corrected.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| fixed library source -> container fixture revision | Runtime/artifact identities must be rebuilt consistently for the selected adapter. |
| host -> container harness | Hostile Strategy crosses only the digest-pinned restricted container boundary. |
| preflight -> marker | Readiness/performance evidence must pass before any charged effect is possible. |
| Git history -> authorization writer | Explicit runnable source bytes cannot be replaced by a documentation descendant. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-177-01 | Spoofing | image/source identity | critical | mitigate | Exact local RepoDigest and raw Git commit/tree/blob authentication. |
| T-262-177-02 | Elevation of Privilege | runtime selection | critical | mitigate | Container-only adapter and no fallback. |
| T-262-177-03 | Denial of Service | per-method container startup | high | mitigate | Non-consuming measured feasibility gate before marker. |
| T-262-177-04 | Information Disclosure | preflight output | high | mitigate | Exact aggregate allowlist and negative privacy tests. |
| T-262-177-SC | Tampering | package supply chain | low | accept | No package installation occurs; the existing image is digest-pinned and locally inspected. |
</threat_model>

<verification>Focused runner/checker tests and TypeScript pass; source-only checker authenticates the separately committed runnable closure; exact v1 artifacts and 36 locks remain unchanged; no preflight, authorization, review, Match, or marker exists.</verification>

<success_criteria>
- Five-file plan, below ten files.
- Both Plan174 active findings have direct test-backed source fixes.
- Exact digest-pinned container confinement has no fallback.
- Performance infeasibility fails before marker under unchanged 45-second/cell and 15-minute bounds.
- Explicit authorization source custody rejects HEAD and documentation-only descendants.
- No authorization, review, Match, marker, terminal, archive, or tag occurs.
</success_criteria>

<output>Create `262-177-SUMMARY.md`; dispatch `262-178-PLAN.md` to run the non-consuming preflight, bind exact Plan177 bytes, and independently review them.</output>
