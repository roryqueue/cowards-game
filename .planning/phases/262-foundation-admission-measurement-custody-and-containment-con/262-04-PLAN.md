---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "04"
type: execute
wave: 11
depends_on: ["262-03"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-measurement.ts
  - .planning/artifacts/v1.38-pre-search-contract.json
autonomous: true
requirements: [MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09]
must_haves:
  truths:
    - "Numeric budgets and gates are frozen only from contained profile-neutral calibration, with exact denominators and explicit justification for every replacement per D-14 and D-15."
    - "Advanced results remain regression-only and every empirical claim is bounded to the named oracle, budgets, roots, and conditions."
    - "Process, current-rules, formation, and contamination states remain orthogonal; no composite score or threshold softening can override a hard gate per D-17 and D-18."
  artifacts:
    - path: "scripts/lib/v1-38-measurement.ts"
      provides: "Pure gate, report-state, denominator, and claim-lint logic"
      exports: ["freezeV138CalibratedContract", "evaluateV138FrozenGates", "validateV138ReportState", "lintV138Claim"]
    - path: ".planning/artifacts/v1.38-pre-search-contract.json"
      provides: "Immutable calibrated study, budget, gate, report, and claim contract"
  key_links:
    - from: ".planning/artifacts/v1.38-pre-search-contract.json"
      to: "scripts/lib/v1-38-study-contract.ts"
      via: "canonical exact-schema renderer"
      pattern: "calibration"
    - from: "scripts/lib/v1-38-measurement.ts"
      to: ".planning/artifacts/v1.38-pre-search-contract.json"
      via: "bound threshold, denominator, and report grammar"
      pattern: "denominator"
---

<objective>
Freeze calibrated numeric gates, report semantics, and bounded claim grammar before candidate inspection, implementing D-14 through D-18.

Purpose: Ensure later empirical results are interpreted against immutable, denominator-complete, non-compensating rules.
Output: Pure measurement/report evaluators and the content-addressed pre-search contract.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-03-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md
@.planning/milestone-proposals/v1.38-competitive-strategy-factory-and-adversarial-league/ACTIVATION-PROMPT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze calibrated feasibility and structural gates</name>
  <read_first>
    <file>scripts/lib/v1-38-study-contract.ts</file>
    <file>.planning/artifacts/v1.38-calibration-freeze-policy.json</file>
    <file>.planning/milestone-proposals/v1.38-competitive-strategy-factory-and-adversarial-league/ACTIVATION-PROMPT.md</file>
    <file>packages/spec/src/constants.ts</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-RESEARCH.md</file>
  </read_first>
  <files>scripts/lib/v1-38-measurement.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - The 64 KB hard cap, preferred under-48 KB target, under-5 ms p99 benchmark, population/core/finalist counts, 55/60/70 percent gates, and red-team gate enter calibration as named starting values.
    - Every frozen value binds denominator, benchmark/hardware identity, calibration roots, and replacement justification when changed.
    - Advanced above-70 percent remains regression-only and cannot establish balance or robustness.
  </behavior>
  <action>Add `gates` tests, then implement only `freezeV138CalibratedContract` as the consumer of Plan 03's already-content-addressed `.planning/artifacts/v1.38-calibration-freeze-policy.json` and profile-neutral calibration receipt. Verify both receipts bind the identical preregistered policy root, then execute the frozen selectors in parameter-ID order without changing selector code, admissibility bounds, denominators, ordering, benchmark roots, or fallback rules: select exactly the admissible derived replacement prescribed by the policy, otherwise retain the starting value; missing, ambiguous, non-finite, late-policy, or root-mismatched evidence stops. Record old/new values, denominator, input/code roots, reason, and no-candidate proof per D-14/D-15. Freeze population, independent-core, finalist, response, probe, red-team, source/runtime, and zero-integrity gates automatically with no post-calibration checkpoint or discretion, and reject candidate/profile/holdout outcome fields.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t gates --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Every frozen number is traceable to a starting value or contained replacement receipt, and missing denominators fail closed.</acceptance_criteria>
  <done>MEAS-05, MEAS-06, MEAS-07, and MEAS-08 have calibrated, immutable, exact-denominator gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce orthogonal report states and bounded claims</name>
  <read_first>
    <file>scripts/lib/v1-38-study-contract.ts</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <files>scripts/lib/v1-38-measurement.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - Process status, current-rules outcome, formation outcome, and holdout status are separate fields with an explicit valid-combination table.
    - Process failure blocks authority while a process-valid current failure, no robust pure finalist, or later bracket rejection remains publishable evidence.
    - Hard integrity/rejection gates run before empirical conjunctions; diagnostic/composite scores never reverse them.
  </behavior>
  <action>Add table and mutation coverage under `reporting`, then implement `validateV138ReportState`, `evaluateV138FrozenGates`, and `lintV138Claim`. Encode the ordered non-compensating decision logic from D-16 through D-18. Allow only oracle/budget/conditions/root-bounded claims; reject solved-game, optimality, permanent-balance, unexploitable, production-authorization, threshold-softening, and selective-omission formulations.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t reporting --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>All valid state combinations are enumerated, invalid combinations and stronger claims are rejected, and empirical failure remains distinct from process failure.</acceptance_criteria>
  <done>MEAS-09 and bounded claim semantics are executable and mutation-proved.</done>
</task>

<task type="auto">
  <name>Task 3: Render and lock the pre-search contract</name>
  <read_first>
    <file>scripts/lib/v1-38-study-contract.ts</file>
    <file>scripts/lib/v1-38-measurement.ts</file>
    <file>.planning/artifacts/v1.38-foundation-admission.json</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
  </read_first>
  <files>.planning/artifacts/v1.38-pre-search-contract.json, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <action>Render the exact canonical pre-search contract from the admitted authority, supervised reproduction, calibration, study design, structural vector, gates, report grammar, and claim linter. Bind implementation/fixture roots and prohibit mutable `latest` discovery per D-01. The artifact is private research control-plane metadata, never a product API or game-rule authority.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t 'contract|gates|reporting' --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The artifact regenerates byte-identically from declared roots and changes identity when any contract/code/fixture/calibration input changes.</acceptance_criteria>
  <done>The calibrated scientific, budget, gate, reporting, and claim contract is frozen before candidate output.</done>
</task>

</tasks>

## Artifacts this phase produces

- `evaluateV138FrozenGates`, `validateV138ReportState`, and `lintV138Claim` in `scripts/lib/v1-38-measurement.ts`
- `.planning/artifacts/v1.38-pre-search-contract.json`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Calibration receipt → numeric freeze | Unsupported replacements or missing denominators could bias later conclusions. |
| Empirical outcomes → report/claims | Results must not soften hard gates or expand permitted claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-12 | Tampering | gate freeze | high | mitigate | Bind starting values/replacements, denominators, code/input roots, and no-candidate proof. |
| T-262-13 | Repudiation | report state | high | mitigate | Orthogonal exact enums and exhaustive valid-combination tests. |
| T-262-14 | Elevation of Privilege | composite decision logic | high | mitigate | Ordered non-compensating evaluator; mutation tests prevent score override. |
| T-262-15 | Information Disclosure | pre-search artifact | medium | mitigate | Closed research-control-plane schema and privacy-safe projection checks. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
Regenerate the pre-search contract twice to prove byte stability and content-root sensitivity.
</verification>

<success_criteria>
- Every gate has an exact denominator and frozen interpretation.
- Advanced evidence is mechanically regression-only.
- Process, empirical, formation, and contamination states cannot collapse into one status.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-04-SUMMARY.md` when done.
</output>
