---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "200"
type: tdd
wave: 167
depends_on: [262-199]
files_modified:
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json
  - .planning/debug/phase-262-probe-failed.md
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-200-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "The only source repair records exactly one finite non-negative elapsed request timing in a finally path for every attempted adapter call, including adapter throws, while retaining aggregateTimings.requestCount === requestCounts.attempted."
    - "An adapter throw fails closed at the first public fixture/method/warm-or-sample stage and projects only a coarse session-failure class; later cleanup failure is recorded separately and cannot erase that first failure."
    - "Attempt 7 remains consumed terminal history at Plan199 commit 97241136e4279c57c5734a896a886afe31fa327a, diagnostic-v4 remains absent, and its old writer/selector are permanently retired before shared diagnostic code changes."
    - "Fresh v14 diagnostic-only custody authenticates the exact Plan197 source/image/controls and immutable diagnostics-v1/v2/v3, preflight-v11, review-v12, and Plan199 terminal history before one collision-free diagnostic-v5 run."
    - "Attempt 8 of 10 is exactly one actual-fixture stage diagnostic-v5 in the unchanged deterministic public fixture/method/warm/sample order through the real persistent broker, with zero preflights, zero Matches, no effects, complete owned cleanup, and exactly 36 locks."
    - "The route stops after diagnostic-v5 regardless of result; runtime repair and fresh-preflight planning are deferred until the committed stage result is known, leaving two authorized attempts."
  artifacts:
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Throw-safe exact timing accounting, coarse session-failure projection, and fresh v14 diagnostic-only custody/writer/checker"
    - path: scripts/check-v1-38-lean-admission.test.ts
      provides: "RED/GREEN proof for adapter-throw timing equality, first-stage retention, privacy, history, and zero-effect isolation"
    - path: .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json
      provides: "Single immutable attempt-8 actual-fixture stage result"
    - path: .planning/debug/phase-262-probe-failed.md
      provides: "Privacy-safe attempt-8 result and next-action boundary"
  key_links:
    - from: scripts/check-v1-38-lean-admission.test.ts
      to: scripts/check-v1-38-lean-admission.ts
      via: "A thrown adapter call executes the timing finally block before the bounded failure projection is validated"
      pattern: "requestCount"
    - from: .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-199-SUMMARY.md
      to: .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json
      via: "Exact immutable attempt-7 terminal root and required diagnostic-v4 absence"
      pattern: "attempt7History"
    - from: scripts/run-v1-38-lean-runner-feasibility.ts
      to: .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json
      via: "Exact Plan197 public fixture builders, persistent broker, image, controls, order, request caps, and lifecycle"
      pattern: "advanced:vanguard-pressure"
---

<objective>
Repair only the diagnostic throw-path timing projection, preserve attempt 7 as terminal history, and consume attempt 8 with one fresh actual-fixture stage diagnostic.

Purpose: Obtain an admissible first-failure stage and coarse class even when the real adapter throws, without changing runtime behavior or spending a preflight or Match.
Output: TDD projection-accounting repair, fresh v14 diagnostic-only custody, one diagnostic-v5 artifact, privacy-safe history, and summary; zero preflights and zero Matches.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/phase-262-probe-failed.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-EXECUTION-CLOSURE-RESEARCH.md
@.planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v1.json
@.planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v2.json
@.planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v3.json
@.planning/artifacts/v1.38-lean-runner-direct-container-preflight-v11.json
@.planning/artifacts/v1.38-lean-runner-direct-validity-review-v12.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-199-PLAN.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-199-SUMMARY.md
@scripts/run-v1-38-lean-runner-feasibility.ts
@scripts/check-v1-38-lean-admission.ts
@scripts/check-v1-38-lean-admission.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make diagnostic timing accounting total across adapter throws</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts</files>
  <behavior>
    - "RED injects a throw on the first adapter call and expects terminalStage starter:aggro-chaser:selectActivations:warm, resultClass session_failure, violationType null, attempted 1, successful 0, and aggregate requestCount 1."
    - "RED injects a throw at each later public warm/sample ordinal and expects the first failing fixture/method/ordinal, exactly one timing per attempted call, prior successes only, and no continued adapter calls."
    - "RED proves the elapsed observation is appended from a finally block for success, ok:false, and throw branches and remains finite and non-negative."
    - "RED preserves the exact schema invariants: planned 16, 0 <= successful <= attempted <= 16, request timing count exactly equals attempted, complete requires 16/16 plus two clean sessions, and player_violation requires one allowlisted violation type."
    - "RED permits session_failure only with null violationType, rejects unknown/coerced exception classes and all surplus/raw fields, and keeps the first failing public stage unchanged through all later lifecycle work."
    - "RED injects adapter throw followed by close failure and requires the adapter fixture/method/ordinal and session_failure class to remain unchanged while cleanup.complete is false; session:close is terminal only when cleanup is the first failure."
    - "RED authenticates Plan199 terminal commit 97241136e4279c57c5734a896a886afe31fa327a and summary root sha256:16f76622d37a6ad882936377cf81cecf3022067680b8dd45ba68dbb82ea7e5a5, requires diagnostic-v4 absence, and rejects any attempt-7 mutation or artifact appearance."
    - "RED proves the retired v13 diagnostic-v4 write selector fails before createSession, broker execution, or artifact creation after Plan199 terminal history exists; its check selector may verify historical presence only and cannot launch work."
    - "RED reserves only collision-free diagnostic-v5 and fresh v14 diagnostic-only selectors; no v14 preflight, authorization, review, live, post-run, adjudication, eligibility, tracking, recovery, archive, tag, public, production, formation, or Match path is introduced."
  </behavior>
  <action>Write the adapter-throw regressions first, including first-call and later-stage tables, exact attempted/successful/timing counts, first-stage retention, coarse session-failure projection, adapter-throw-plus-close-failure, cleanup-first failure, and privacy mutations. Before modifying shared runner behavior, permanently retire the v13 diagnostic-v4 write function and CLI write selector against exact Plan199 terminal commit/summary history: any call must fail before createSession, broker execution, or destination creation. Retain only non-executing historical validation/check behavior if needed to prove immutability. Repair only the request-observation accounting inside the diagnostic runner: set the bounded public stage, start the monotonic timer, increment attempted once, invoke `adapter.execute` inside a try/catch, and append exactly one finite non-negative elapsed observation in a finally block whether the call succeeds, returns ok:false, or throws. Preserve the validator's exact equality between aggregate timing count and attempted count; do not weaken it to completed calls. Map an adapter throw only to the closed `session_failure` result class with null violationType, retaining no error object/message/stack and making no inference about runtime root cause. Once a request failure is set, keep its terminalStage/resultClass immutable through cleanup; record cleanup incompleteness only in the existing closed cleanup projection. Use session:close as terminalStage only when cleanup is the first failure after otherwise successful requests. Preserve the existing player-violation projection, exact order, 16-call schedule, public fixture/input builders, persistent session behavior, image, controls, timing/resource/deadline bounds, and all structural preflight/Match/effect exclusions. Add fresh v14 diagnostic-only source custody, schema, renderer, exclusive writer, checker, and selectors for collision-free diagnostic-v5. Bind exact Plan197 runnable source/tree/closure and all prior roots, plus exact Plan199 commit/summary and diagnostic-v4 absence. Do not modify the runtime broker/session implementation, call Docker, run a diagnostic/preflight/Match, or create any artifact/effect in this task. Commit source/tests separately before Task2.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/check-v1-38-lean-admission.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; test ! -e .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v4.json &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36</automated>
  </verify>
  <done>The committed diagnostic code records one timing per attempted call, preserves the first adapter-failure stage across cleanup, permanently disables the consumed attempt-7 writer, and exposes only fresh v14 diagnostic-v5 with zero operational authority.</done>
</task>

<task type="auto">
  <name>Task 2: Consume attempt 8 with exactly one diagnostic-v5 run</name>
  <files>.planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json, .planning/debug/phase-262-probe-failed.md, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-200-SUMMARY.md</files>
  <action>After Task1's separate source commit, run the fresh v14 source-only checker and invoke the diagnostic-v5 writer exactly once from the main orchestrator. Use the exact Plan197 source/image/controls and real public fixtures through the persistent broker in the unchanged order: starter:aggro-chaser selectActivations warm then samples 1-3, soldierBrain warm then samples 1-3; then advanced:vanguard-pressure in the same method/order. On success, ok:false, or adapter throw, record exactly one aggregate timing for each attempted call. On the first request failure, persist only its bounded public fixture/method/ordinal stage and either allowlisted player violation or coarse session_failure class; do not retain Strategy source/input/output/memory/objective, raw samples, stdout/stderr, error text/stack, container identity, or host diagnostics. Always close the session and verify owned cleanup/absence. Cleanup incompleteness sets cleanup.complete false without replacing a prior request-failure stage/class; use the bounded session:close stage only if cleanup is the first failure. Write only diagnostic-v5 with attemptOrdinal 8, attemptLimit 10, attemptsRemaining 2, preflightInvocations 0, matchInvocations 0, and exact false authority. Validate and commit it, then append only the privacy-safe projection, immutable roots, aggregate counts/timings, cleanup, and accounting to debug history and summary. Do not create diagnostic-v4, invoke its retired writer, invoke either preflight entry point, construct/invoke a Match, create any operational effect, repair runtime behavior, or plan/run a fresh preflight. Stop after the committed diagnostic-v5 result is known.</action>
  <verify>
    <automated>node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-actual-fixture-stage-diagnostic-v5 &amp;&amp; test "$(jq -r '.attemptOrdinal' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json)" = 8 &amp;&amp; test "$(jq -r '.attemptsRemaining' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json)" = 2 &amp;&amp; test "$(jq -r '.aggregateTimings.requestCount' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json)" = "$(jq -r '.requestCounts.attempted' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json)" &amp;&amp; test "$(jq -r '.preflightInvocations' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json)" = 0 &amp;&amp; test "$(jq -r '.matchInvocations' .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v5.json)" = 0 &amp;&amp; test ! -e .planning/artifacts/v1.38-lean-runner-direct-actual-fixture-stage-diagnostic-v4.json &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated>
  </verify>
  <done>Exactly one diagnostic-v5 consumes attempt 8 and commits an admissible first-failure stage/coarse class with exact aggregate accounting, cleanup, zero preflights, zero Matches, no effects, and two attempts remaining.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Obtain admissible first-failure evidence after Plan199 projection rejection | 262-200 | COVERED | Narrow accounting repair plus one diagnostic.
REQ | ADMIT-01..ADMIT-04 | Exact source/runtime identity and fail-closed authority | 262-200 | COVERED | Diagnostic grants no admission.
REQ | MEAS-01..MEAS-10 | Exact counts, aggregate timing, privacy, cleanup, resources, and claim bounds | 262-200 | COVERED | One timing per attempt; closed projection.
REQ | SEAL-01 | Preserve holdout and formation absence | 262-200 | COVERED | No candidate, formation, holdout, or Match activity.
REQ | DECI-02 | Preserve hostile Strategy containment | 262-200 | COVERED | Exact Plan197 container/broker path unchanged.
RESEARCH | — | Diagnose exact public-fixture failure before runtime repair | 262-200 | COVERED | Runtime repair remains deferred.
CONTEXT | D-01..D-22 | Immutable, deterministic, private, accountable, fail-closed evidence | 262-200 | COVERED | Plan199 history bound; exact schema.
CONTEXT | D-34L | Preserve current-rules gate and frozen bounds | 262-200 | COVERED | Diagnostic only mirrors public probe order.
CONTEXT | D-34L.1 | No Match before passing fresh preflight | 262-200 | COVERED | Zero preflight/Match/effect reachability.
OPERATOR | attempt 8/10 | Repair projection only, then one diagnostic-v5 and stop | 262-200 | COVERED | Two attempts remain afterward.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| attempted adapter call -> aggregate evidence | Finally-path timing makes every attempted call accountable even when hostile execution throws. |
| thrown error -> persisted projection | Only a bounded stage and coarse session_failure class cross; error contents are discarded. |
| Plan199 history -> v14 custody | Exact terminal commit/summary and diagnostic-v4 absence prevent retry or reinterpretation. |
| diagnostic-v5 -> downstream workflow | Structural exclusions keep preflight, Match, and all effect authority unreachable. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-200-01 | Repudiation | attempted/timing counts | critical | mitigate | One finally-path timing per attempted call and exact validator equality. |
| T-262-200-02 | Information Disclosure | adapter throw projection | high | mitigate | Closed session_failure class; discard exception, raw diagnostics, payloads, and identifiers. |
| T-262-200-03 | Tampering | attempt-7 history | critical | mitigate | Bind Plan199 commit/summary root, require diagnostic-v4 absence, and retire the v13 writer before changing shared code. |
| T-262-200-04 | Elevation of Privilege | v14 selectors | critical | mitigate | Diagnostic-only paths plus structural exclusion of preflight, Match, and effect writers. |
| T-262-200-05 | Denial of Service | session lifecycle | high | mitigate | Preserve exact deadlines/resource controls, first-failure stop, bounded timings, and owned cleanup. |
| T-262-200-06 | Spoofing | public stage identity | high | mitigate | Closed fixture/method/ordinal and session-stage enums with exact-order tests. |
| T-262-200-SC | Tampering | package supply chain | low | accept | No package installation occurs. |
</threat_model>

<verification>Focused TDD and TypeScript pass; exact Plan199 terminal history and missing diagnostic-v4 are authenticated; diagnostic-v5 validates first-stage/coarse-class evidence with timingCount === attempted, attempt 8/10, cleanup, zero preflights, zero Matches, false authority, immutable prior roots, and 36 locks.</verification>

<success_criteria>
- Two tasks across five files.
- The only repair is finally-path request timing plus coarse adapter-throw projection.
- Task1 is committed before the sole attempt-8 diagnostic-v5 run.
- Diagnostic-v5 persists the first failing public stage even on adapter throw.
- Attempt 7 remains consumed, diagnostic-v4 remains absent, and no history is rewritten.
- Preflight and Match counts are zero; all authority/effects remain false or absent.
- Runtime repair and fresh-preflight planning wait for diagnostic-v5; two attempts remain.
</success_criteria>

<output>Create `262-200-SUMMARY.md`, stop after diagnostic-v5, and plan runtime repair or a fresh preflight only as a new additive successor informed by the committed result.</output>
