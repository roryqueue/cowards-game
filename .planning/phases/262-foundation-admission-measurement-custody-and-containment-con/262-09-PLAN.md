---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "09"
type: execute
wave: 4
depends_on: ["262-08"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-current-matrix-reproduction.ts
autonomous: true
gap_closure: true
requirements: [ADMIT-03]
must_haves:
  truths:
    - "All 540 unchanged exact requests are deterministically assigned before execution and run only through the v1.18 prepared runtime service, supervised worker-thread adapter, v1.19 ABI, and selected MATCH_KERNEL per D-03 and D-10."
    - "A resource-bounded asynchronous scheduler enforces four concurrent shards as a ceiling, four attempts per shard, per-child and aggregate RSS limits, 25% host headroom, per-shard timeout, total-run timeout, and fail-closed cancellation."
    - "Completion order cannot alter canonical outcome, charged-ledger, accepted-ledger, shard-receipt, progress-receipt, or aggregate roots."
    - "Every allocation, launch, attempt, retry, terminal outcome, cancellation, failure, and never-launched ID remains visible and charged; no partial or calibration cell is reusable or published."
  artifacts:
    - path: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      provides: "Precommitted calibration policy/projector, deterministic sharder, injected scheduler, asynchronous subprocess supervisor, resource monitor, cleanup barrier, and canonical charged reduction"
      exports: ["V138ParallelCalibrationPolicySchema", "deriveV138ParallelCalibrationPolicy", "projectV138ParallelMatrix", "planV138MatrixShards", "calibrateV138ParallelMatrix", "executeV138ParallelMatrix"]
    - path: "scripts/evaluate-v1-38-foundation-contract.test.ts"
      provides: "Determinism, resource refusal, cleanup, cancellation, accounting, and no-partial-reuse coverage"
  key_links:
    - from: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      to: "apps/runtime-service/src/execute-match.ts"
      via: "unchanged exact requests executed by executePreparedRuntimeServiceRequestV118 inside supervised child shards"
      pattern: "executePreparedRuntimeServiceRequestV118"
    - from: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      to: "packages/engine/src/kernel/driver.ts"
      via: "existing v1.19 runtime path reaches the selected MATCH_KERNEL without a coordinator transition loop"
      pattern: "engine-kernel-v1.37-candidate-1"
---

<objective>
Replace serial shard execution with deterministic, resource-bounded, asynchronously supervised parallel orchestration without changing Match requests, semantics, evidence strength, or accounting, implementing D-02, D-03, D-05, D-10, D-14, and D-16.

Purpose: Make a safe sub-90-minute authoritative attempt possible while preserving the exact canonical runtime and fail-closed evidence contract.
Output: A deterministic sharding/scheduling core, asynchronous child supervision, complete charged/progress receipts, cleanup proofs, and focused injected-runner tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-08-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-PATTERNS.md
@scripts/lib/v1-38-current-matrix-reproduction.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic sharding, progress, and charged reduction</name>
  <read_first>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
    <file>scripts/evaluate-v1-38-foundation-contract.test.ts</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/artifacts/v1.38-historical-matrix-expectation.json</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md</file>
    <file>packages/spec/src/canonical-json.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - Canonical 540 attempt IDs are preallocated exactly once into stable shards and lanes with at most four attempts per shard and at most four active shards.
    - Before any calibration executes, one immutable policy fixes eight calibration-only attempts as four concurrent two-attempt shards, exact aggregation/denominators, a 7.5% margin, 60,000 ms fixed overhead, integer rounding, and inclusive 5,400,000 ms admission.
    - Injected completion orders, lane counts, and valid shard interleavings produce byte-identical canonical receipts and roots.
    - Monotonic terminal progress accounts exactly for launched, terminal, successful-but-unaccepted, failed, cancelled, and unlaunched IDs while acceptedCellsPublished remains zero.
    - Missing, duplicate, reordered, conflicting, unknown, prior-partial, calibration, retry-alias, player-violation, system-failure, timeout, or cancelled cells remain charged and block accepted reduction.
  </behavior>
  <action>Add injected-runner `matrix calibration policy`, `matrix scheduler`, and `matrix accounting` tests first. Before exposing any executable calibration function, implement closed `V138ParallelCalibrationPolicySchema` plus pure `deriveV138ParallelCalibrationPolicy` and `projectV138ParallelMatrix`. Freeze the calibration inventory to the first eight canonical attempt templates in canonical order, renamed to distinct `calibration:v1:{0..7}:{templateAttemptId}` identities that can never equal a matrix cell ID, assigned as four stable shards/lanes with exactly two sequential attempts per shard and all four shards launched together. Freeze `sampleAttemptCount: 8`, `sampleShardCount: 4`, `attemptsPerShard: 2`, `concurrency: 4`, `authoritativeAttemptDenominator: 540`, `marginBasisPoints: 750`, `fixedOverheadMilliseconds: 60000`, and `maxProjectedTotalMilliseconds: 5400000`. Define observed batch wall time as the ceiling to integer milliseconds of parent monotonic elapsed time from the first child spawn through the cleanup barrier for all four shards; define per-child RSS as each child's maximum sample, aggregate RSS as the maximum over sampling ticks of the sum for all active children, and host headroom as the minimum sampled free/total ratio converted by floor to integer basis points. Compute `baseProjectedMilliseconds = ceil(calibrationBatchWallMilliseconds * 540 / 8)`, `marginMilliseconds = ceil(baseProjectedMilliseconds * 750 / 10000)`, and `projectedTotalMilliseconds = baseProjectedMilliseconds + marginMilliseconds + 60000`; admit time only when `projectedTotalMilliseconds &lt;= 5400000`. Canonicalize and bind the exact policy, inventory, projection-function source, aggregation rules, denominator, and rounding-mode roots before execution. Mutation-test every inventory ID/order/count/shard assignment, concurrency, denominator, margin, fixed overhead, formula/source root, aggregation rule, rounding rule, and exact-boundary comparator; 5,400,000 passes and 5,400,001 stops. Then implement pure `planV138MatrixShards` from the existing canonical inventory, with stable shard/lane IDs and no authoritative request mutation. Define closed exact-key schemas for allocation, launch, attempt, retry, shard terminal, progress, cancellation, cleanup, and final publication records. The reducer must canonicalize by predeclared attempt ID after all terminals, not completion order; it must reject any missing/extra/duplicate/conflicting/reordered or aliasing identity before consulting outcomes. Preserve the prior unbounded failure and one-attempt calibration as immutable charged lineage, give every new calibration a distinct non-cell identity, and make seeded prior/calibration reuse a typed hard stop per D-05/D-16. Atomic publication remains either all 540 exact successful cells or the canonical empty accepted root.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix calibration policy|matrix scheduler|matrix accounting" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The complete calibration inventory and integer projection contract are rooted before execution; mutation tests cover every policy/projection input and exact threshold behavior, while property/table tests prove no duplicate, missing, conflicting, reordered, reused, or uncharged allocation can reach accepted reduction and all valid completion orders produce identical roots.</acceptance_criteria>
  <done>The calibration policy/projector, scheduler, and reducer have deterministic rooted identities and exact accounting before any real parallel subprocess is launched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supervise asynchronous shards with bounded resources and cleanup</name>
  <read_first>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
    <file>apps/runtime-service/src/execute-match.ts</file>
    <file>packages/runtime-js/src/worker-host.ts</file>
    <file>packages/runtime-js/src/subprocess-host.ts</file>
    <file>scripts/evaluate-v1-38-foundation-contract.test.ts</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts</files>
  <behavior>
    - Async child orchestration never exceeds four shards, four attempts per shard, 2 GiB per child, 4 GiB aggregate child RSS, ten minutes per shard, 90 minutes total, or the 25% host free-memory floor.
    - A starting width of four is admitted only after the precommitted eight-attempt/four-shard calibration and deterministic 7.5%-plus-60,000-ms projection pass on the recorded hardware/OS/Node identity.
    - First hard failure or parent exception/interrupt stops launches, terminates every active process group gracefully then forcibly within bounded deadlines, awaits all exits, and proves no orphan remains.
    - Every terminal shard emits exactly one complete progress receipt with IDs, elapsed time, max RSS, cumulative counts, zero published cells, and non-reusable partial status.
  </behavior>
  <action>Replace serial `spawnSync` orchestration with injected asynchronous child-process supervision while keeping each child’s existing in-process `executePreparedRuntimeServiceRequestV118` path and exact authoritative request bytes unchanged. Require the exact rooted calibration policy and projection-function source from Task 1; no CLI or exported calibration executor may accept sample size, inventory, margin, overhead, aggregation, denominator, rounding, comparator, or formula overrides. Freeze `maxConcurrentShards: 4`, `maxAggregateChildRssKilobytes: 4194304`, `minHostFreeMemoryPercent: 25`, the existing per-child/per-shard limits, and `maxProjectedTotalMilliseconds/maxTotalRunMilliseconds: 5400000`. Sample portable host memory and child RSS through an explicit adapter whose identity and observations enter calibration/receipts using the precommitted aggregation rules; refuse launch when measurement is unavailable or ambiguous. Put shards in controllable process groups, stop scheduling on the first hard failure, send bounded graceful termination then forced termination, await exit/close for every child and supervised worker, and record cleanup proof. Bind the calibration policy root, projection source root, inventory root, raw observations, derived integer fields, and admitted/stopped decision into every calibration and final receipt. Add fake-clock/process/resource tests for exact-boundary and one-over cases, aggregate/per-child RSS, headroom, timeouts, spawn/output failure, cancellation, parent exception/interrupt, worker cleanup, and no-orphan proof. Do not add a direct-engine path, unsafe loader, copied rule logic, changed runtime limits, retry that erases charge, or partial reuse per D-02/D-03/D-05/D-10.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix resources|matrix cleanup|matrix cancellation" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>Injected tests prove resource refusal, deterministic cancellation, complete cleanup, exact charged accounting, and zero partial publication for every launch, runtime, timeout, signal, parse, memory, and headroom failure path.</acceptance_criteria>
  <done>The parallel scheduler is safe to calibrate against the real supervised runtime without weakening authority or evidence.</done>
</task>

</tasks>

## Artifacts this plan produces

- `V138ParallelCalibrationPolicySchema`, `deriveV138ParallelCalibrationPolicy`, `projectV138ParallelMatrix`, `planV138MatrixShards`, `calibrateV138ParallelMatrix`, and `executeV138ParallelMatrix` in `scripts/lib/v1-38-current-matrix-reproduction.ts`
- Deterministic scheduler, accounting, resource, cleanup, and cancellation tests in `scripts/evaluate-v1-38-foundation-contract.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Coordinator → supervised shard processes | The coordinator assigns immutable requests but cannot execute Strategy code or transitions. |
| Child resource/progress reports → scheduler | Child output is untrusted until schema, identity, resource, and terminal-accounting validation pass. |
| Partial execution → accepted evidence | Completed partial work remains charged but cannot become accepted or reusable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-34 | Denial of Service | parallel child scheduler | high | mitigate | Fixed concurrency/shard/time/RSS/headroom limits, precommitted deterministic projection, total deadline, and fail-closed launch admission. |
| T-262-35 | Tampering | shard/progress reduction | high | mitigate | Preallocated exact IDs, closed receipt schemas, canonical order-independent reduction, and conflict detection. |
| T-262-36 | Repudiation | charged execution ledger | high | mitigate | Record allocations, launches, attempts, retries, failures, cancellations, unlaunched IDs, and prior charged lineage. |
| T-262-37 | Elevation of Privilege | Strategy/runtime execution path | critical | mitigate | Keep execution solely in v1.18 prepared runtime-service requests with supervised worker-thread v1.19 authority and selected MATCH_KERNEL. |
| T-262-38 | Denial of Service | process/worker cleanup | high | mitigate | Process groups, bounded graceful/forced termination, awaited exits, worker cleanup proof, and parent-interrupt handling. |
| T-262-39 | Information Disclosure | child diagnostics/progress | medium | mitigate | Closed public-safe progress fields; omit Strategy source, memory, objective payloads, stacks, and host-private paths. |
</threat_model>

<verification>
<automated>pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix calibration policy|matrix scheduler|matrix accounting|matrix resources|matrix cleanup|matrix cancellation" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
</verification>

<success_criteria>
- Scheduler width, shard size, memory, headroom, per-shard timeout, and total timeout are all frozen and enforced.
- The exact eight-attempt/four-shard calibration inventory and deterministic projection formula are rooted and mutation-tested before execution.
- Exact requests and the v1.18/v1.19/MATCH_KERNEL execution lineage are unchanged.
- Results and roots are invariant to completion order.
- Every failure cancels safely, charges all work, proves cleanup, and publishes zero accepted cells.
- Calibration, prior partials, and failed-shard successes are never reusable.
</success_criteria>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-09-SUMMARY.md` when done.
</output>
