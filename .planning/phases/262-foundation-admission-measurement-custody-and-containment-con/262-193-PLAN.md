---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "193"
type: tdd
wave: 162
depends_on: [262-192]
files_modified:
  - scripts/lib/v1-38-lean-container-match-session.ts
  - scripts/lib/v1-38-lean-container-match-session.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v1.json
  - .planning/debug/phase-262-probe-failed.md
  - .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-193-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [ADMIT-01, ADMIT-02, ADMIT-03, ADMIT-04, MEAS-01, MEAS-02, MEAS-03, MEAS-04, MEAS-05, MEAS-06, MEAS-07, MEAS-08, MEAS-09, MEAS-10, SEAL-01, DECI-02]
must_haves:
  truths:
    - "Per D-34L.1, a valid guest result is accepted only after an explicit completion and natural-exit receipt from that request's fresh Worker; successful requests do not depend on the fragile 100 ms terminate race diagnosed after Plan192."
    - "Startup, method, cancellation, request, cell, and outer deadlines remain unchanged; a missing, late, duplicate, malformed, or inconsistent completion/exit receipt fails closed, terminates the Worker, poisons the broker session, and removes the owned container."
    - "Every method still receives one fresh resource-bounded Worker inside the existing digest-pinned Match-scoped container, with serialized request IDs, strict frames and byte/schema caps, no persistent Strategy state, no broker-direct Strategy execution, no per-method child process, and no fallback."
    - "One exact-image, exact-controls, broker-only diagnostic exercises the repaired success lifecycle, records only aggregate stage/timing/cleanup evidence, invokes zero preflights and zero Matches, and counts as operator-authorized bounded attempt 2 of 10."
    - "Fresh v10 custody binds the separately committed repair, diagnostic, and all preflight-v9, authorization-v10, review-v10, and effect-v10 destinations while preserving every earlier artifact and exactly 36 locks."
  artifacts:
    - path: scripts/lib/v1-38-lean-container-match-session.ts
      provides: "Event-friendly completion-plus-exit Worker lifecycle inside the persistent broker"
    - path: scripts/lib/v1-38-lean-container-match-session.test.ts
      provides: "TDD coverage for success exit, timeout, ambiguity, poisoning, and cleanup"
    - path: .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v1.json
      provides: "Privacy-safe exact-container proof of the repaired broker lifecycle"
    - path: scripts/check-v1-38-lean-admission.ts
      provides: "Fresh v10 source, diagnostic, preflight, review, and effect custody"
  key_links:
    - from: scripts/lib/v1-38-lean-container-match-session.ts
      to: scripts/lib/v1-38-lean-container-match-session.test.ts
      via: "Explicit completion and exit receipts tested before valid response acceptance"
      pattern: "exit"
    - from: .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v1.json
      to: .planning/artifacts/v1.38-lean-runner-direct-container-preflight-v9.json
      via: "Exact committed source, image, controls, broker protocol, cleanup, and passing diagnostic root"
      pattern: "workerLifecycleDiagnostic"
---

<objective>
Repair the constrained-container Worker success lifecycle and prove it through one bounded broker-only diagnostic before spending another preflight.

Purpose: Remove the source/runtime integration defect that can turn valid guest completions into broker exit 73 under the frozen 0.5-CPU and 64-MB controls, without weakening containment, statelessness, protocol validation, or any frozen deadline.
Output: TDD-protected lifecycle repair, fresh v10 custody, one privacy-safe exact-container diagnostic, and a source summary; zero preflights and zero Matches.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/phase-262-probe-failed.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-LEAN-EXECUTION-CLOSURE-RESEARCH.md
@.planning/artifacts/v1.38-lean-runner-direct-container-preflight-v8.json
@.planning/artifacts/v1.38-lean-runner-direct-validity-review-v9.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-191-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-192-REVIEW.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-192-SUMMARY.md
@scripts/lib/v1-38-lean-container-match-session.ts
@scripts/lib/v1-38-lean-container-match-session.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace success-path forced termination with an authenticated Worker exit handshake</name>
  <files>scripts/lib/v1-38-lean-container-match-session.ts, scripts/lib/v1-38-lean-container-match-session.test.ts</files>
  <behavior>
    - "RED proves a valid legacy or v1.17 result is not accepted merely because the result port signaled; the same request's Worker must also close its port and reach a successful natural exit."
    - "RED proves delayed successful exit under constrained scheduling no longer fails solely because it exceeds 100 ms, provided completion remains within the unchanged request budget."
    - "RED proves missing exit, nonzero exit, result-after-exit, duplicate result, duplicate exit, inconsistent request identity, and message/exit races fail closed without emitting a valid response."
    - "RED proves startup and method timeout paths retain the unchanged cancellation-grace termination ceiling and canonical legacy/v1.17 timeout envelopes."
    - "RED proves every accepted request still uses a fresh Worker with env {}, execArgv [], existing resourceLimits, closed ports, serialized correlation, and no state reuse, child-process launch, or fallback."
    - "RED proves a lifecycle failure poisons the broker stream and leads to bounded owned-container cleanup before another request can execute."
  </behavior>
  <action>Write the failing lifecycle and race tests first. Then change only the fresh-Worker controller embedded in `LEAN_CONTAINER_BROKER_SOURCE`: have each guest Worker emit one request-bound completion receipt, close its transferred port, and exit naturally; have the broker await and reconcile the result, port closure, and Worker exit through asynchronous event delivery rather than immediately awaiting `worker.terminate()` on success. Charge the whole success handshake to the existing request budget and never extend the frozen startup, method, cancellation, 45-second cell, or 900-second outer limits. Reserve forced termination for timeout, crash, malformed, ambiguous, or cancellation branches and retain the existing cancellation-grace ceiling there. Accept a success only when exactly one schema-valid byte-capped result and one successful exit belong to the current request. Poison and exit the broker on any ambiguity so the host removes the owned container. Preserve both exact ABI envelopes, deterministic inputs, runtime accounting, resource limits, one fresh Worker per request, serialized correlation, and the Docker hostile-code boundary. Do not execute Strategy source in broker scope, introduce persistent user state, spawn a process per method, add fallback execution, relax controls, invoke Docker, run a preflight, or run a Match.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/lib/v1-38-lean-container-match-session.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false</automated>
  </verify>
  <done>Both protocol modes accept valid results only after an authenticated natural Worker exit, constrained-scheduling success no longer depends on a 100 ms termination race, and every failure remains bounded and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind v10 custody and run one exact-controls broker lifecycle diagnostic</name>
  <files>scripts/check-v1-38-lean-admission.ts, scripts/check-v1-38-lean-admission.test.ts, .planning/artifacts/v1.38-lean-runner-direct-worker-lifecycle-diagnostic-v1.json, .planning/debug/phase-262-probe-failed.md, .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-193-SUMMARY.md</files>
  <behavior>
    - "Fresh canonical paths are preflight-v9, authorization-v10, validity-review-v10, invocation-v10, terminal-v10, adjudication-v10, and phase-262 eligibility-v10, with no alias to earlier paths."
    - "Source-only v10 custody authenticates the separately committed repair commit/tree/executable closure, Plan192 roots, exact 36 locks, the lifecycle invariants, and absence of every fresh operational destination."
    - "Fresh schemas, canonical renderers, validators, writers, and selectors implement preflight-v9, authorization-v10, review-v10, reviewed-ready/live/post-run-v10, adjudication-v10, eligibility-v10, and final-tracking-v10 before any downstream plan invokes them."
    - "Authorization-v10 binds the immutable first lean attempt, all non-consuming diagnosis/repair/preflight history through Plan194, the Plan172 review root and five certification-only dispositions, exactly one unconsumed corrective invocation, recoveryAuthorized:false, and every broader authority false."
    - "The diagnostic launches one uniquely owned broker-only container from the unchanged digest-pinned image with the exact read-only/network/memory/CPU/pids/capability/tmpfs controls, sends representative legacy and v1.17 requests through the real Docker exec stream, and never constructs or invokes a Match."
    - "Diagnostic pass requires every request's completion/exit receipt, response correlation, caps, timing, Worker cleanup, stream close, and container removal to succeed; any ambiguity records a bounded non-pass stage and grants no preflight or Match authority."
    - "The committed diagnostic contains only source/image/control identities, bounded stage code, aggregate counts/timings, cleanup status, attempt ordinal 2 of 10, zero preflight invocations, zero Match invocations, and false authority; no Strategy source, input, output, memory, or private runtime data is retained."
  </behavior>
  <action>After Task1 is committed separately, extend the admission checker and tests additively for v10 source and effect custody plus a one-shot worker-lifecycle diagnostic writer/checker. Implement and mutation-test the complete fresh operational toolchain consumed downstream: preflight-v9 and authorization-v10 schemas/canonical renderers/validators/writers/selectors; validity-review-v10 schema/rendering/validation/selector; reviewed-ready, live, and post-run-v10 selectors; invocation-v10 and terminal-v10 schemas/writers/validators; adjudication-v10 and eligibility-v10 schemas/renderers/writers/validators/selectors; and final-tracking-v10 validation across requirements, roadmap, state, status, audit, validation, and UAT. Require authorization-v10 to bind the immutable first lean attempt; all diagnosis, committed fixes, diagnostics, and non-consuming preflights through Plan194; the exact Plan172 review root and all five certification-only dispositions; one unconsumed corrective invocation; `recoveryAuthorized:false`; and all broader authority false. Require the pass-only lineage `diagnostic-v1 -> preflight-v9 -> authorization-v10 -> review-v10 -> invocation/terminal-v10 -> adjudication/eligibility-v10 -> final-tracking-v10`, with every failure or missing/ambiguous input denying the next edge. Bind the exact repair commit/tree and every transitive runtime input; reject the old success-path termination race structurally and require explicit result/port/exit reconciliation with unchanged failure termination bounds. Commit the checker/test source before running any container. Run the source-only v10 selector with all fresh destinations absent. Then invoke exactly one uniquely named broker-only diagnostic using the exact installed digest-pinned image and exact controls already frozen for the lean session. Exercise both legacy and v1.17 success lifecycles through the real persistent Docker exec stream with deterministic public fixtures sufficient to expose completion/exit races, but do not call the canonical preflight evaluator, feasibility runner, Match kernel, Match orchestrator, or any Match selector. Always remove the owned container. Persist only the bounded aggregate artifact described above, commit it, and verify its canonical root and exact source binding. Count this repair/diagnostic route as authorized attempt 2 of 10; it does not consume the corrective 24-Match opportunity. Update the debug record with the exact result and evidence root. Preserve preflight-v8/review-v9 and all earlier artifacts byte-for-byte. Do not create preflight-v9, authorization-v10, review-v10, marker, terminal, adjudication, eligibility, tracking, archive, or tag effects.</action>
  <verify>
    <automated>pnpm exec vitest run scripts/check-v1-38-lean-admission.test.ts scripts/lib/v1-38-lean-container-match-session.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism --bail=1 &amp;&amp; pnpm exec tsc --noEmit --pretty false &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-worker-lifecycle-source-only-v10 &amp;&amp; node --import tsx scripts/check-v1-38-lean-admission.ts --check-direct-worker-lifecycle-diagnostic-v1 &amp;&amp; test "$(find . -maxdepth 1 -name '.v138-successor-*.lock' | wc -l | tr -d ' ')" = 36 &amp;&amp; git diff --check</automated>
  </verify>
  <done>The separately committed repair has passing v10 custody, the full tested v10 downstream operational toolchain exists, and one immutable exact-controls broker-only diagnostic records complete cleanup, zero preflights, zero Matches, all authority false, and preserved history.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Make the reviewed lean gate executable under frozen limits | 262-193 | COVERED | Repairs the observed Worker lifecycle defect and proves the real boundary.
REQ | ADMIT-01..ADMIT-04 | Exact source/runtime custody and pass-only admission | 262-193 | COVERED | Fresh v10 custody; no admission effect.
REQ | MEAS-01..MEAS-10 | Preserve schedule, budgets, accounting, cleanup, privacy, and claims | 262-193 | COVERED | Diagnostic is aggregate-only and non-consuming.
REQ | SEAL-01 | Preserve sealed-holdout state | 262-193 | COVERED | No holdout operation.
REQ | DECI-02 | Hostile-code containment | 262-193 | COVERED | Fresh Worker remains inside unchanged Docker controls.
RESEARCH | — | Match-scoped container, request-scoped Worker, bounded supervised execution | 262-193 | COVERED | Only the success lifecycle handshake changes.
CONTEXT | D-01..D-22 | Determinism, immutable evidence, hostile input, exact accounting, privacy, no product authority | 262-193 | COVERED | Explicitly preserved.
CONTEXT | D-34L | Exact 12-cell/two-pass contract | 262-193 | COVERED | No schedule or acceptance change.
CONTEXT | D-34L.1 | Seven-category, no-recovery, pass-only closure | 262-193 | COVERED | Source and diagnostic only.
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| host -> Docker exec broker | Exact framed diagnostic and later runtime requests cross into the digest-pinned hostile-code container. |
| broker -> fresh guest Worker | One untrusted Strategy method runs in one resource-bounded Worker. |
| Worker completion/exit -> broker response | A result is valid only after exact receipt and successful natural exit agree. |
| committed source -> diagnostic | Exact source/tree/closure must authenticate before the one diagnostic launch. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-193-01 | Tampering | completion and exit receipts | critical | mitigate | Bind both receipts to one request and reject duplicates, races, or disagreement. |
| T-262-193-02 | Elevation of Privilege | guest Worker | critical | mitigate | Preserve fresh Worker isolation inside unchanged Docker controls and forbid fallback. |
| T-262-193-03 | Denial of Service | Worker lifecycle | high | mitigate | Existing request/method/cancellation budgets, forced termination on failure, broker poison, container removal. |
| T-262-193-04 | Information Disclosure | diagnostic artifact | high | mitigate | Aggregate allowlist excludes source, payloads, outputs, memory, and raw diagnostics. |
| T-262-193-05 | Spoofing | source and image custody | high | mitigate | Exact Git closure, image digest, controls, unique ownership, and canonical artifact root. |
| T-262-193-SC | Tampering | package supply chain | low | accept | No package or image installation occurs. |
</threat_model>

<verification>Focused TDD and TypeScript pass; v10 source custody authenticates exact committed bytes; one exact-image/exact-controls broker-only diagnostic passes or records a bounded denial; zero preflight and Match invocations occur; all 36 locks and prior artifacts remain.</verification>

<success_criteria>
- Two TDD tasks across seven files.
- Success acceptance requires exact completion plus natural exit and no 100 ms success termination race.
- Failure paths retain unchanged startup, method, cancellation, cell, and outer bounds.
- One broker-only exact-controls diagnostic records attempt 2 of 10 and invokes zero Matches.
- Fresh v10 custody passes with preflight-v9 and all v10 operational destinations absent.
- No gameplay, formation, privacy, production, public, archive, or tag change occurs.
</success_criteria>

<output>Create `262-193-SUMMARY.md`; dispatch `262-194-PLAN.md` only after exact v10 source custody and the committed broker lifecycle diagnostic pass.</output>
