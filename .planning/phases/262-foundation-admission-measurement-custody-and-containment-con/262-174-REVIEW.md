---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "174"
reviewed: 2026-09-08T14:59:13Z
depth: deep
files_reviewed: 28
files_reviewed_list:
  - scripts/lib/v1-38-lean-runner-feasibility.ts
  - scripts/lib/v1-38-lean-runner-feasibility.test.ts
  - scripts/run-v1-38-lean-runner-feasibility.ts
  - scripts/run-v1-38-lean-runner-feasibility.test.ts
  - scripts/check-v1-38-lean-admission.ts
  - scripts/check-v1-38-lean-admission.test.ts
  - apps/runtime-service/src
  - packages/engine/src
  - packages/persistence/src
  - packages/replay/src
  - packages/runtime-js/src
  - packages/runtime-python/src
  - packages/runtime-supervisor/src
  - packages/runtime-wasm-wasi/src
  - packages/spec/src
  - apps/runtime-service/package.json
  - packages/engine/package.json
  - packages/persistence/package.json
  - packages/replay/package.json
  - packages/runtime-js/package.json
  - packages/runtime-python/package.json
  - packages/runtime-supervisor/package.json
  - packages/runtime-wasm-wasi/package.json
  - packages/spec/package.json
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - .planning/artifacts/v1.38-lean-runner-direct-authorization-v1.json
findings:
  critical: 2
  warning: 0
  info: 0
  total: 2
status: issues_found
blockingFindingCount: 2
admitsPlan175: false
---

# Phase 262 Plan 174: Direct Validity Review

**Outcome:** blocked. Two result-validity findings exist, so Plan 175 is not admitted and every authority remains false.

## Summary

The review traced the committed authorization through the actual direct selector, child admission, canonical request construction, runtime adapter, terminal reduction, post-run validation, and exact-pass eligibility branch. It also compared the authorization against the raw committed Plan 173 summary rather than treating the checker as sufficient evidence.

At review time, tracked bytes were clean; exactly 36 authenticated `.v138-successor-<sha256>.lock` files were the only worktree residue; and direct invocation, terminal, adjudication, and eligibility effects were absent. The authorization preserves the exact Plan 172 review root and all five open findings as nonblocking certification-only history.

## Critical Issues

### CR-01: Authorization binds the wrong source commit and tree

**Classification:** BLOCKER  
**Category:** `source_or_dirty_byte_drift`  
**File:** `.planning/artifacts/v1.38-lean-runner-direct-authorization-v1.json:1`

**Issue:** The committed Plan 173 summary requires commit `c11e9173cc29149b16460944676a6c2b11829e9b` and tree `15edf9a9916403e72a7d30af55d2a0183c8990ca`. The authorization instead records the later documentation commit `3105b0453b3d8cbd285ea0ddc9d82ab319e3529a` and tree `ca11d4825099875a843388e4aac440bcad0ad283`. The later commit only adds the summary and all 27 executable closure object IDs still match, but Plan 174 explicitly requires the exact commit and tree named by that summary. The checker at `scripts/check-v1-38-lean-admission.ts:1665-1677` reconstructs expectations from the authorization's own commit and therefore cannot detect this contract mismatch.

**Fix:** Publish a new authorization that binds the exact Plan 173 summary values (`c11e9173...` / `15edf9a...`) and obtain a new independent review. Do not reinterpret matching executable blobs as matching exact commit/tree custody.

### CR-02: Reachable direct execution selects a non-hostile worker-thread boundary

**Classification:** BLOCKER  
**Category:** `supervised_runtime_escape`  
**File:** `scripts/run-v1-38-lean-runner-feasibility.ts:352-369`

**Issue:** The live direct selector reaches `executePreparedLeanRequest`, which explicitly configures `strategyExecutionAdapter: "worker-thread"`. The selected adapter declares itself a `local-dev-fallback` and states that it is “not a final sandbox for hostile Strategy code” because worker threads share a host process (`packages/runtime-js/src/adapter.ts:88-99`). The outer per-cell child supervision at `scripts/run-v1-38-lean-runner-feasibility.ts:430-575` provides timeout and process cleanup, but it does not convert the inner in-process worker-thread execution into the required hostile-Strategy containment boundary.

**Fix:** Route the runnable fixture path through a reviewed hostile-code boundary, such as the project's supervised container/subprocess implementation, and bind that adapter identity and its required runtime inputs in the authorization before re-review.

## Seven-Category Outcome

1. `source_or_dirty_byte_drift` — **finding**. Exact authorization commit/tree do not match the committed Plan 173 requirement; closure blobs and current tracked bytes otherwise match.
2. `multiple_launch` — **pass**. The only reachable live selector performs reviewed-ready checks and creates an `O_EXCL` marker before Match one; the marker prevents concurrent or later launch.
3. `tuple_or_schedule_drift` — **pass**. Fixture hashes, arena mappings, side/initiative matrix, tuple, runtime limits, 15-minute deadline, 12-cell/two-pass schedule, formation, and normalization roots are fixed and revalidated.
4. `supervised_runtime_escape` — **finding**. The reachable Match path selects the explicitly non-hostile worker-thread adapter.
5. `partial_interrupted_or_unclean_evidence` — **pass**. Recovery is unreachable from active dispatch; marker-only is invalid; all 24 ordered records and cleanup are re-derived before acceptance.
6. `private_data_disclosure` — **pass**. Exact-key aggregate projections and privacy validation exclude source, memories, objectives, diagnostics, streams, keys, and host paths.
7. `non_pass_authority` — **pass**. Only exact `pass` can admit eligibility; this review is blocked and has exhaustive false authority.

## Selector, Recovery, Lock, and Effect Evidence

- Actual launch reachability: `main` recognizes `--run-reviewed-direct-gate`, loads authorization/review, creates the exclusive marker, runs the 24-cell supervisor, and writes one terminal (`scripts/run-v1-38-lean-runner-feasibility.ts:609-661`).
- Recovery unreachability: the active `main` has no recovery branch. The old recovery routine is retained as an uncalled historical fixture (`scripts/run-v1-38-lean-runner-feasibility.ts:591-607`).
- Lock inventory: exactly 36 root-level authenticated successor locks were present and were the only pre-review worktree residue.
- Fresh-effect absence: direct invocation, terminal, adjudication, and eligibility artifacts were absent. The review artifact did not exist before this independent review.

## Certification-Only History

These five Plan 172 findings remain open and are preserved without being marked fixed or counted as active Plan 174 findings:

- `CR-V6-01` — critical, open, `certification_only_nonblocking_under_D_34L_1`
- `CR-V6-02` — critical, open, `certification_only_nonblocking_under_D_34L_1`
- `CR-V6-03` — critical, open, `certification_only_nonblocking_under_D_34L_1`
- `CR-V6-04` — critical, open, `certification_only_nonblocking_under_D_34L_1`
- `WR-V6-01` — warning, open, `certification_only_nonblocking_under_D_34L_1`

---

_Reviewer: gsd-code-reviewer (fresh independent Plan 262-174 review)_
_Disposition: blocked; no repair, repeat, launch, recovery, marker, Match, terminal, or downstream authority invoked_
