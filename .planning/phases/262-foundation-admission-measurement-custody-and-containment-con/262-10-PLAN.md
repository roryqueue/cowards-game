---
phase: 262-foundation-admission-measurement-custody-and-containment-con
plan: "10"
type: execute
wave: 5
depends_on: ["262-09"]
files_modified:
  - scripts/evaluate-v1-38-foundation-contract.test.ts
  - scripts/lib/v1-38-current-matrix-reproduction.ts
  - .planning/artifacts/v1.38-current-matrix-reproduction.json
autonomous: true
gap_closure: true
requirements: [ADMIT-03]
must_haves:
  truths:
    - "A small real supervised parallel calibration proves unchanged v1.18 service, supervised worker-thread runtime, v1.19 ABI/kernel, exact requests, fixture-only labels, measured width, memory/headroom, and conservative 90-minute projection before the full launch."
    - "All 540 authoritative cells run from scratch under the frozen envelope; calibration, the initial resource-pressure attempt, and every prior partial cell remain separately charged and excluded."
    - "A pass publishes exactly 540 canonical accepted cells only after exact inventory, independent historical predicate, aggregate, resource, cleanup, and final-root validation all succeed."
    - "Any calibration or full-run failure emits a new immutable stopped_process_failure successor with zero accepted cells and leaves ADMIT-03 and Plans 262-03 through 262-07 blocked; the 90-minute gate is never raised or softened."
  artifacts:
    - path: ".planning/artifacts/v1.38-current-matrix-reproduction.json"
      provides: "Immutable passing exact reproduction receipt or honest stopped process-failure successor with full charged lineage"
    - path: "scripts/lib/v1-38-current-matrix-reproduction.ts"
      provides: "Calibration, authoritative write, and exact receipt-check commands"
      exports: ["runV138ParallelMatrixCalibration", "writeV138AuthoritativeMatrixReceipt", "checkV138AuthoritativeMatrixReceipt"]
  key_links:
    - from: ".planning/artifacts/v1.38-current-matrix-reproduction.json"
      to: ".planning/artifacts/v1.38-historical-matrix-expectation.json"
      via: "independent expected predicate root and exact source bindings"
      pattern: "historicalExpectationRoot"
    - from: ".planning/artifacts/v1.38-current-matrix-reproduction.json"
      to: ".planning/artifacts/v1.38-foundation-admission.json"
      via: "exact admitted v1.37 authority and unchanged selected runtime tuple"
      pattern: "admissionRoot"
---

<objective>
Calibrate and execute the complete authoritative parallel historical matrix under the unchanged 90-minute gate, closing ADMIT-03 only on an exact 540/540 pass and otherwise preserving an honest stopped process failure.

Purpose: Produce the passing supervised regression receipt required before any candidate-sensitive Phase 262 contract work may resume.
Output: Real supervised calibration proof, a complete authoritative run, exact final-root checks, and the immutable successor reproduction receipt.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-09-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-02-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md
@.planning/artifacts/v1.38-foundation-admission.json
@.planning/artifacts/v1.38-historical-matrix-expectation.json
@.planning/artifacts/v1.38-current-matrix-reproduction.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibrate the real four-slot supervised execution envelope</name>
  <read_first>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
    <file>scripts/evaluate-v1-38-foundation-contract.test.ts</file>
    <file>.planning/artifacts/v1.38-foundation-admission.json</file>
    <file>.planning/artifacts/v1.38-historical-matrix-expectation.json</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>apps/runtime-service/src/execute-match.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts, .planning/artifacts/v1.38-current-matrix-reproduction.json</files>
  <behavior>
    - The exact precommitted eight-attempt/four-shard/two-attempt-per-shard calibration inventory executes through the exact prepared v1.18 service and supervised v1.19 worker-thread/MATCH_KERNEL path.
    - Calibration records hardware, OS, Node, policy/source roots, concurrency, wall time, per-child/aggregate RSS, host headroom, cleanup, and every integer input/output of the frozen 7.5%-plus-60,000-ms projection.
    - Exactly 5,400,000 projected milliseconds passes the time boundary; one millisecond over, any resource ambiguity, or any non-success/cleanup failure stops before the full run.
    - Calibration attempts are separately charged and cannot alias or populate matrix cells.
  </behavior>
  <action>Add one focused real `matrix supervised parallel calibration` integration and supporting injected branch tests. Implement and invoke exactly `node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --calibrate-parallel-receipt .planning/artifacts/v1.38-current-matrix-reproduction.json`; the CLI accepts no inventory, sample-size, concurrency, formula, margin, overhead, denominator, aggregation, rounding, comparator, or time-threshold override. It must load and verify the exact precommitted Plan 262-09 calibration policy/source roots, execute its eight calibration-only identities as four concurrent two-attempt shards, and apply only `projectV138ParallelMatrix`. Keep exact Strategy/runtime request construction and `regression_throughput_only` labeling; bind the admitted runtime tuple, predecessor receipt/root, calibration policy/inventory/projection source roots, raw observations, derived fields, scheduler/resource roots, and cleanup proof. On admission, atomically write a closed `calibration_admitted` successor receipt with zero accepted cells and explicit non-reuse; on projection, memory, headroom, classification, cleanup, authority, policy, or source failure, atomically write a newly rooted `stopped_process_failure` successor with zero accepted cells, the exact predecessor lineage, full charged calibration dispositions, and no full-run launch, then exit nonzero. Add exact `--check-calibration-receipt`, `--require-calibration-admitted`, and `--require-stopped-process-failure` modes so tests verify both branches and every policy/projection mutation. The calibration can never launch the 540-cell run or reuse calibration outcomes per D-03/D-05/D-10/D-14.</action>
  <verify>
    <automated>sh -c 'node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --calibrate-parallel-receipt .planning/artifacts/v1.38-current-matrix-reproduction.json; rc=$?; node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --check-calibration-receipt .planning/artifacts/v1.38-current-matrix-reproduction.json || exit $?; if [ "$rc" -eq 0 ]; then node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --require-calibration-admitted .planning/artifacts/v1.38-current-matrix-reproduction.json; else node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --require-stopped-process-failure .planning/artifacts/v1.38-current-matrix-reproduction.json; fi' &amp;&amp; pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix supervised parallel calibration|matrix calibration receipt branches" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The exact calibration CLI produces and the checker validates either a rooted `calibration_admitted` receipt under the frozen policy or a newly rooted `stopped_process_failure` receipt with zero accepted cells, exact predecessor lineage, full charged calibration evidence, cleanup proof, and no full-run launch.</acceptance_criteria>
  <done>A four-slot full run is authorized only by measured headroom and a conservative projection at or below the frozen 90-minute threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Run all 540 cells and seal the exact authoritative receipt</name>
  <read_first>
    <file>scripts/lib/v1-38-current-matrix-reproduction.ts</file>
    <file>.planning/artifacts/v1.38-historical-matrix-expectation.json</file>
    <file>.planning/artifacts/v1.38-current-matrix-reproduction.json</file>
    <file>.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md</file>
    <file>scripts/evaluate-v1-38-foundation-contract.test.ts</file>
  </read_first>
  <files>scripts/lib/v1-38-current-matrix-reproduction.ts, scripts/evaluate-v1-38-foundation-contract.test.ts, .planning/artifacts/v1.38-current-matrix-reproduction.json</files>
  <action>Require a checked `calibration_admitted` receipt from Task 1, then expose and invoke `--write-authoritative-receipt` to preallocate and run all 540 exact attempt IDs from scratch under the identical rooted calibration/resource policy. Preserve the initial 14,390-second resource-pressure attempt and the earlier 35,812-ms calibration as charged predecessor lineage, then record every new calibration/shard/attempt/retry/failure/cancellation/unlaunched allocation and progress receipt in the closed public-safe ledger. Stop on the first hard failure, complete cleanup/accounting, and atomically write a new `stopped_process_failure` receipt with zero accepted cells; never overwrite history without binding the immediate calibration receipt and original predecessor roots. Only after all 540 exact successes terminate, cleanup is proven, actual wall time is at most 5,400,000 ms, resources stayed within bounds, IDs/inventory are exact, and the independent historical predicate matches may the command atomically publish 540 accepted cells and `status: passed_exact`. Bind exact charged, accepted, shard, progress, calibration policy/inventory/projection source, calibration receipt, expectation, observed aggregate, reducer/scheduler source, authority, and final receipt roots. Add `--check-authoritative-receipt` that recomputes every derived/root field and fails on byte, order, count, source, policy, predicate, lineage, resource, cleanup, or final-root mutation. If the run stops, create `262-10-SUMMARY.md` with `outcome: stopped_process_failure`, do not claim plan success, and keep Plan 262-03 blocked; do not raise or reinterpret the 90-minute threshold per D-02/D-05/D-10/D-16.</action>
  <verify>
    <automated>node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --write-authoritative-receipt .planning/artifacts/v1.38-current-matrix-reproduction.json &amp;&amp; node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --check-authoritative-receipt .planning/artifacts/v1.38-current-matrix-reproduction.json &amp;&amp; pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix authoritative receipt|matrix final root" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>The command passes only for a public-safe `passed_exact` receipt with 540/540 accepted cells, zero violations/failures/retries/cancellations/missing/duplicate/conflicting IDs, complete cleanup, exact independent predicate match, actual wall time at or below 90 minutes, and an exactly recomputed final root. Otherwise it preserves a stopped receipt and returns failure.</acceptance_criteria>
  <done>ADMIT-03 is closed only by the exact passing receipt; any bounded inability to meet the gate remains an honest charged process failure with zero reusable or accepted cells.</done>
</task>

</tasks>

## Artifacts this plan produces

- A successor `.planning/artifacts/v1.38-current-matrix-reproduction.json` with either `passed_exact` or truthful `stopped_process_failure`
- Real supervised parallel calibration, authoritative write, and exact check commands in `scripts/lib/v1-38-current-matrix-reproduction.ts`
- Calibration independence, full receipt, and exact final-root tests in `scripts/evaluate-v1-38-foundation-contract.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Calibration → full-run admission | Calibration is resource evidence only and cannot become an accepted matrix cell. |
| Supervised shard outputs → authoritative receipt | All outcomes remain untrusted until complete identity, resource, cleanup, expectation, and reduction checks pass. |
| Stopped predecessor → successor branch | Prior charged failures remain immutable lineage and cannot be erased by a retry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-262-40 | Tampering | calibration admission | high | mitigate | Precommit and bind exact inventory, sample size, aggregation, denominator, formula, margin, fixed overhead, rounding, comparator, policy/source roots, measured resources, and both receipt branches. |
| T-262-41 | Repudiation | authoritative execution ledger | high | mitigate | Persist every allocation, attempt, retry, failure, cancellation, unlaunched ID, shard/progress receipt, and predecessor root. |
| T-262-42 | Denial of Service | 540-cell run | high | mitigate | Enforce frozen per-child, aggregate, headroom, shard, concurrency, and 90-minute total limits with fail-closed cancellation. |
| T-262-43 | Tampering | accepted publication/final root | critical | mitigate | Atomic 0-or-540 publication, independent predicate, exact inventory, canonical roots, and a recomputing check command. |
| T-262-44 | Information Disclosure | persisted receipt | medium | mitigate | Closed public-safe fields; exclude Strategy source, memories, objective payloads, raw diagnostics, stacks, and private host paths. |
</threat_model>

<verification>
<automated>node --import tsx scripts/lib/v1-38-current-matrix-reproduction.ts --check-authoritative-receipt .planning/artifacts/v1.38-current-matrix-reproduction.json &amp;&amp; pnpm exec vitest run scripts/evaluate-v1-38-foundation-contract.test.ts -t "matrix supervised parallel calibration|matrix authoritative receipt|matrix final root" --maxWorkers=1 &amp;&amp; pnpm typecheck</automated>
</verification>

<success_criteria>
- Real parallel calibration passes unchanged runtime authority, memory/headroom, cleanup, and conservative sub-90-minute projection.
- All 540 exact requests run from scratch and terminate exactly once under the frozen envelope.
- Every prior/current allocation and outcome is charged and visible; no calibration or partial cell is reused.
- Accepted publication is atomic and occurs only at 540/540 exact success with independent historical predicate match.
- The exact final receipt root recomputes and mutation tests fail closed.
- A stopped outcome remains stopped_process_failure and does not unblock Plan 262-03.
</success_criteria>

<source_audit>

| Source | ID | Gap requirement/constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Begin research only under exact released authority and immutable pre-search contract | 262-08 through 262-10 | COVERED — ADMIT-03 gate restored before downstream work |
| REQ | ADMIT-03 | Reproduce persisted current-rules matrix; Starter/Advanced remain fixture-only | 262-08 through 262-10 | COVERED |
| RESEARCH | — | Exact 540 inventory, supervised v1.18/v1.19/MATCH_KERNEL path, canonical reduction, immutable receipt | 262-08 through 262-10 | COVERED |
| CONTEXT | D-01 | Immutable content-addressed successor, no mutable latest | 262-08, 262-10 | COVERED |
| CONTEXT | D-02 | Missing/ambiguous/mismatched/non-reproducible evidence fails closed | 262-08 through 262-10 | COVERED |
| CONTEXT | D-03 | Selected MATCH_KERNEL and supervised runtime are sole authorities | 262-09, 262-10 | COVERED |
| CONTEXT | D-05 | Every allocation/attempt/retry/failure remains charged | 262-09, 262-10 | COVERED |
| CONTEXT | D-10 | Exact historical shape/results through safe selected path; fixtures only | 262-08 through 262-10 | COVERED |
| CONTEXT | Deferred | Planner/factory/league/formations/product/rule changes | none | EXCLUDED — explicitly deferred |

</source_audit>

<output>
Create `.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-10-SUMMARY.md` when done. Mark it successful only when the authoritative receipt is `passed_exact`; otherwise record `outcome: stopped_process_failure` and leave all downstream plans blocked.
</output>
