---
phase: 60
plan: 01
slug: public-ladder-service-read-follow-up
status: completed
created: 2026-05-22
---

# Phase 60-01 Plan — Public Ladder Service Read Follow-Up

## Tasks

1. [x] Extend service contracts for the public ladder read.
   - Add public ladder season schemas and a public ladder page service envelope.
   - Add `getPublicLadderSeason` route metadata and fixture example.
   - Regenerate the public service OpenAPI artifact.

2. [x] Add service-owned public ladder read.
   - Inject and call `buildTrialLadderSeasonDto` from `@cowards/service`.
   - Validate the envelope with `@cowards/spec` schemas.
   - Run the canonical public DTO leak guard before returning.

3. [x] Rewire the web ladder page.
   - Read the ladder season through `apps/web/lib/public-service-boundary.ts`.
   - Read the current user affordance through the account read boundary.
   - Preserve not-found behavior, standings, entries, MatchSet result links, counted-state copy, and no-permanent-ratings copy.

4. [x] Remove the old competitive public read.
   - Remove `competitiveServer.getTrialLadderSeason`.
   - Keep ladder writes and admin/governance flows in their existing competitive path.

5. [x] Verify and update monitors.
   - Add service tests for ladder envelope, missing seasons, and private-field rejection.
   - Add the ladder page to strict import enforcement.
   - Update boundary-monitor baselines and confirm Go route manifest stays unchanged.
