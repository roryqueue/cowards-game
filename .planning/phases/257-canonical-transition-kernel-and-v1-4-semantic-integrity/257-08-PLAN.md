---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "08"
type: execute
wave: 4
depends_on: ["257-01", "257-05", "257-06", "257-07"]
files_modified:
  - packages/engine/src/kernel/types.ts
  - packages/engine/src/kernel/validate.ts
  - packages/engine/src/kernel/step.ts
  - packages/engine/src/kernel/driver.ts
  - packages/engine/src/kernel/kernel-contract.test.ts
  - packages/engine/src/kernel/semantic-boundaries.test.ts
  - packages/engine/src/types.ts
  - packages/engine/src/index.ts
autonomous: true
requirements: [KERN-01, KERN-02, KERN-03, KERN-07, KERN-10, KERN-11]
must_haves:
  truths:
    - "A pure resumable step and candidate-branded driver implement one validated lifecycle/runtime-response authority without replacing the active runMatch pointer before Plan 19 (D-01 through D-04)."
    - "Transition records bind tuple, coordinates, classification, ordered events, hashes, and terminal/failure status while private state stays controlled (D-02)."
    - "Every input/output edge validates semantically; failure returns unchanged prestate, no canonical partial stream, and no player penalty (D-05 through D-08)."
  artifacts:
    - path: "packages/engine/src/kernel/step.ts"
      provides: "Pure one-edge transition authority"
    - path: "packages/engine/src/kernel/driver.ts"
      provides: "Inactive candidate Match loop and runtime effect interpreter"
  key_links:
    - from: "packages/engine/src/kernel/driver.ts"
      to: "packages/engine/src/kernel/step.ts"
      via: "driver loops only over validated step results"
      pattern: "stepMatch"
---

<objective>
Implement the pure transition kernel, effects-as-data, and inactive candidate engine driver.

Purpose: Replace distributed imperative scheduling with the D-01 through D-08 authority contract without changing preserved v1.4 observations.
Output: Private machine/cursor types, validated step, public driver, transition stream, and passing RED contracts.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-01-SUMMARY.md
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-05-SUMMARY.md
@packages/engine/src/match.ts
@packages/engine/src/activation.ts
@packages/engine/src/types.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define private machine effects resumes records and hash projection</name>
  <files>packages/engine/src/kernel/types.ts, packages/engine/src/kernel/validate.ts, packages/engine/src/types.ts, packages/engine/src/kernel/kernel-contract.test.ts</files>
  <behavior>
    - Mutually exclusive transition/effect/completed/failure results carry exact cursor and pending-effect identity.
    - Runtime resumes distinguish success, player violation, and system failure; stale/duplicate/mismatched resumes are integrity failures with unchanged state.
    - Controlled state projection uses fixed field order, code-point ordering, domain separation, and exact tuple; it excludes source, artifacts, memories, objectives, diagnostics, and host/security data.
  </behavior>
  <action>Drive types and validators from the Wave-0 test. Keep cursor/private recorder material out of the package barrel. Use Node SHA-256 only over a finite validated domain projection, not a general Phase-258 canonical JSON encoder. Model system failure separately from Strategy violation and cap restricted issue detail.</action>
  <verify><automated>pnpm --filter @cowards/engine exec vitest run src/kernel/kernel-contract.test.ts -t "effect|resume|record|hash"</automated></verify>
  <done>The kernel contract cannot confuse effects, resumes, private state, or failure ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one-edge step with validate-before-commit discipline</name>
  <files>packages/engine/src/kernel/step.ts, packages/engine/src/kernel/validate.ts, packages/engine/src/kernel/kernel-contract.test.ts, packages/engine/src/kernel/semantic-boundaries.test.ts</files>
  <behavior>
    - Each call crosses exactly one Phase/Round/Cycle/Activation/contraction/runtime-response boundary.
    - Incoming machine/command/resume and outgoing state/events are semantically validated on every edge in every environment.
    - Candidate failure returns the original machine and no record; valid success returns exactly one record and next machine/effect/completion.
    - Existing movement/Backstab/contraction primitives retain D-12 behavior and Cycle-start scans remain.
  </behavior>
  <action>Extract lifecycle decisions from the engine loop into a pure reducer that calls existing action primitives. Validate before candidate reduction and after candidate creation; expose state only after successful validation. Do not invoke runtime, persistence, replay, time, randomness, filesystem, network, or database. If any compatibility fixture changes outside D-09 through D-15, stop for approval.</action>
  <verify><automated>pnpm --filter @cowards/engine exec vitest run src/kernel/kernel-contract.test.ts src/kernel/semantic-boundaries.test.ts</automated></verify>
  <done>One pure step owns every lifecycle edge and invalid candidates cannot escape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete an inactive candidate driver and integration seam</name>
  <files>packages/engine/src/kernel/driver.ts, packages/engine/src/index.ts, packages/engine/src/kernel/kernel-contract.test.ts, packages/engine/src/types.ts</files>
  <behavior>
    - Candidate driver alone loops, invokes injected runtime effects, resumes exact requests, and returns result plus ordered transition stream and controlled recorder material.
    - System failure returns restricted attempt evidence and unchanged gameplay, never a partial canonical Chronicle or player penalty.
    - Repeated execution is byte-deterministic with one MATCH_ENDED and no scheduling after terminal state.
  </behavior>
  <action>Export the driver only through an explicitly candidate-branded, non-current test/integration seam; keep active `runMatch`, current authority registry, tuple, and current Match behavior byte-equivalent until Plan 19. The driver interprets only yielded effects and appends records only after validated success. Run the compatibility corpus against both active-old and candidate paths, allowing only approved defect REDs until Plan 09.</action>
  <verify><automated>pnpm --filter @cowards/engine exec vitest run src/kernel/kernel-contract.test.ts src/match.test.ts src/compatibility-fixtures.test.ts</automated></verify>
  <done>The candidate authority is complete and tested while active current execution remains unchanged.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Hostile runtime response to pure engine machine | Forged/stale responses or invalid candidate state could mutate gameplay or blame a player. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-08 | Tampering/Replay/DoS | kernel and driver | high | mitigate | Exact pending-effect identity, every-edge semantic validation, commit-after-validation, unchanged failure state, bounded evidence. |
| T-257-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>
<verification>Run focused kernel/engine/compatibility suites and inspect imports to confirm engine purity.</verification>
<success_criteria>The future direct Match path is deterministic and complete but remains inactive until atomic activation.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-08-SUMMARY.md` when done.</output>
