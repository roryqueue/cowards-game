---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/artifacts/v1.37-phase-257-red-baseline.json
  - packages/engine/src/kernel/kernel-contract.test.ts
  - scripts/check-v1-37-integrity-boundaries.test.ts
autonomous: true
requirements: [KERN-01, KERN-02, KERN-07, KERN-11]
must_haves:
  truths:
    - "Before refactoring, executable RED evidence identifies the engine/replay duplicate Match loops, replay-owned runtime entry, and contiguous Activation bypass without changing their behavior."
    - "The target contract proves one explicit transition step, exact runtime-effect yield/resume identity, ordered transition evidence, and one public engine Match driver (D-01 through D-04)."
    - "Any valid-v1.4 semantic delta outside D-09 through D-15 remains a blocking compatibility failure rather than a golden update (KERN-11)."
  artifacts:
    - path: ".planning/artifacts/v1.37-phase-257-red-baseline.json"
      provides: "Immutable pre-refactor source/probe/tuple identity for Phase 257"
    - path: "packages/engine/src/kernel/kernel-contract.test.ts"
      provides: "Permanent one-step/effect/driver behavioral contract"
  key_links:
    - from: "packages/engine/src/kernel/kernel-contract.test.ts"
      to: "packages/engine/src/match.ts"
      via: "future public runMatch and private step contract"
      pattern: "runMatch|stepMatch"
---

<objective>
Freeze the pre-refactor authority baseline and add permanent RED contracts for the one kernel and driver.

Purpose: Make D-01 through D-04, D-13, D-16, and KERN-11 executable before authority moves, so refactoring cannot erase evidence or normalize an unapproved semantic change.
Output: A deterministic before artifact, kernel contract tests, and structural-debt fixtures.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-CONTEXT.md
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-RESEARCH.md
@.planning/artifacts/v1.37-core-rules-audit-baseline.json
@packages/engine/src/match.ts
@packages/replay/src/build.ts
@apps/runtime-service/src/execute-match.ts
@scripts/check-v1-37-integrity-boundaries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist exact pre-refactor authority and reproduction identity</name>
  <files>.planning/artifacts/v1.37-phase-257-red-baseline.json</files>
  <action>Record deterministic schema version, HEAD, current tuple ID/expansion, hashes of engine Match loop, replay Match loop, runtime-service entry, stale contiguous wrapper, Phase-256 audit baseline, and exact seven-probe output. Name the four Phase-257 defects, the two later-phase defects, and the preserved RIGHT history separately. Do not modify `.planning/artifacts/v1.37-core-rules-audit-baseline.*`, historical v1.4/v1.36 evidence, immutable Strategy Revisions, `.planning/config.json`, or `CowardsGameSpec_Full_Consolidated_v1.md`. Read the consolidated spec only with `git show HEAD:CowardsGameSpec_Full_Consolidated_v1.md` if exact committed bytes are needed.</action>
  <verify><automated>pnpm exec tsx .planning/artifacts/v2.0-core-rules-audit/reproduce-core-rule-gaps.ts &amp;&amp; pnpm v1.37:integrity-boundaries:check</automated></verify>
  <done>The new artifact reproduces the exact Phase-256 observations and source identities without altering any protected or historical byte.</done>
</task>

<task type="auto">
  <name>Task 2: Add permanent RED contracts for kernel authority and bypass removal</name>
  <files>packages/engine/src/kernel/kernel-contract.test.ts, scripts/check-v1-37-integrity-boundaries.test.ts</files>
  <behavior>
    - One step advances exactly one lifecycle or runtime-response boundary and never invokes runtime I/O (D-01, D-03).
    - Each record includes transition kind, exact semantic tuple, lifecycle coordinates, classification, ordered events, before/after hashes, and terminal/failure status without private state (D-02).
    - The driver alone interprets effects, resumes only the matching pending request, and returns result plus the identical ordered stream consumed by Chronicle (D-03, D-04).
    - Structural fixtures reject a replay scheduler, runtime-service replay-owned execution, a contiguous Activation wrapper/export, or a second lifecycle loop (D-13, D-16).
  </behavior>
  <action>Write behavior-first tests against the intended public driver and controlled test seam. Include mismatched, duplicate, stale, and wrong-kind resume cases, deterministic repeated execution, unchanged prestate on failure, exactly one terminal event, and a private-data scan. The exact named contract test must reach the absent-authority assertion and emit `[EXPECTED_RED:MISSING_KERNEL_AUTHORITY]`; no other failure may count. Extend synthetic structural fixtures so enforce mode reports the current known debt by stable finding code. The RED command must reject missing-test, import-resolution, compilation, configuration, and timeout failures. Do not implement production code in this plan.</action>
  <verify><automated>zsh -c 'set -eu; set +e; OUT=$(pnpm --filter @cowards/engine exec vitest run src/kernel/kernel-contract.test.ts -t "missing-kernel-authority: one driver owns transitions" 2&gt;&amp;1); RC=$?; set -e; print -r -- "$OUT"; test $RC -ne 0; print -r -- "$OUT" | rg -F "[EXPECTED_RED:MISSING_KERNEL_AUTHORITY]"; if print -r -- "$OUT" | rg -qi "no test files|no tests found|cannot find module|failed to load|compile error|type error|configuration|timed out|timeout"; then exit 1; fi' &amp;&amp; pnpm exec vitest run scripts/check-v1-37-integrity-boundaries.test.ts -t "Phase 257 RED"</automated></verify>
  <done>RED fails for the missing single-authority behavior and structural tests name every current bypass precisely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Current source/probes to compatibility evidence | Refactor edits could erase the before-state or disguise unapproved drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-01 | Tampering/Repudiation | RED baseline and authority fixtures | high | mitigate | Exact source/probe/tuple hashes, immutable Phase-256 reference, stable finding codes, and KERN-11 stop gate. |
| T-257-SC | Tampering | package supply chain | low | accept | No package install; existing workspace tools only. |
</threat_model>

<verification>Rerun the exact audit reproduction, confirm the before artifact is deterministic, and confirm RED fails only for intended missing target behavior.</verification>
<success_criteria>One immutable before-state and permanent executable contract exist before any production authority moves.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-01-SUMMARY.md` when done.</output>
