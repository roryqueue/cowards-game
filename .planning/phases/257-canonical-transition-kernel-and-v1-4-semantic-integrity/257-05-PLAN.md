---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "05"
type: execute
wave: 2
depends_on: ["257-04"]
files_modified:
  - packages/spec/src/semantic-integrity.ts
  - packages/spec/src/semantic-integrity.test.ts
  - packages/spec/src/fixtures/semantic-integrity-vectors.json
  - packages/spec/src/index.ts
autonomous: true
requirements: [KERN-03, KERN-11]
must_haves:
  truths:
    - "One lowest-layer contract defines bounded stable semantic issue codes, canonical ordering, public category, and restricted metadata (D-05 through D-08)."
    - "Arena, GameState, lifecycle, version, event, and outcome validators reject every KERN-03 invariant without performing gameplay or I/O."
    - "Valid canonical fixtures remain admitted and invalid values never become player violations unless the violation is illegal Strategy output at the legality boundary (D-06)."
  artifacts:
    - path: "packages/spec/src/semantic-integrity.ts"
      provides: "Pure semantic contracts, stable code vocabulary, and canonical arena/state validators"
    - path: "packages/spec/src/fixtures/semantic-integrity-vectors.json"
      provides: "Shared TypeScript/Go expected meanings and order"
  key_links:
    - from: "packages/spec/src/semantic-integrity.ts"
      to: "packages/spec/src/types.ts"
      via: "typed shape-first semantic validation"
      pattern: "SemanticIntegrity|validateCanonical"
---

<objective>
Implement the shared semantic-integrity contract and pure validators.

Purpose: Every later kernel/service/replay/persistence gate needs one deterministic KERN-03 vocabulary and ownership classification.
Output: Pure spec-layer validators, bounded results, exports, and passing shared vectors.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-04-SUMMARY.md
@packages/spec/src/types.ts
@packages/spec/src/schemas.ts
@packages/replay/src/validate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded stable issues and deterministic ordering</name>
  <files>packages/spec/src/semantic-integrity.ts, packages/spec/src/semantic-integrity.test.ts, packages/spec/src/fixtures/semantic-integrity-vectors.json</files>
  <behavior>
    - Fixed TUPLE, ARENA, PLAYER, SOLDIER, POSITION, LIFECYCLE, OUTCOME, and TRANSITION code families return in explicit family/code/path order.
    - Issue count, path segments, path length, and metadata keys/value bytes are capped; overflow returns a stable truncation disposition without recursion or unbounded diagnostics.
    - Public projection contains only a safe top-level system/integrity category; restricted projection contains only stable codes, bounded paths, transition kind, hashes, and safe metadata.
    - Permuting input object/array construction where semantics are equivalent cannot change issue order.
  </behavior>
  <action>Drive implementation from the Wave-0 vectors. Define readonly unions/interfaces and fixed precedence arrays; use code-point comparison, never locale-sensitive sort or incidental Map/Set insertion order. Keep shape parsing separate: these functions accept already typed/parsed values or return a stable shape-owned system failure. Do not build Phase-258's general recursive canonical JSON validator and do not include source, artifacts, memories, objectives, diagnostics, host paths, or security internals.</action>
  <verify><automated>pnpm --filter @cowards/spec exec vitest run src/semantic-integrity.test.ts -t "codes|bounds|order|projection"</automated></verify>
  <done>Stable issue meaning/order/bounds and safe projections pass every vector and permutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement arena, state, lifecycle, tuple, and outcome semantics</name>
  <files>packages/spec/src/semantic-integrity.ts, packages/spec/src/semantic-integrity.test.ts, packages/spec/src/index.ts</files>
  <behavior>
    - Arena validation rejects inverted bounds, duplicate/out-of-bounds terrain, terrain/start overlap, duplicate starts, and starts not admitted by the declared arena.
    - State validation rejects duplicate identities/occupancy, unknown owners, invalid status-position-facing, out-of-bounds/terrain occupancy, bad initiative, mixed versions, and incoherent outcome.
    - Transition context validation rejects illegal lifecycle coordinates, quota/cursor/pending-effect mismatch, event/state delta mismatch, terminal continuation, and before/after hash mismatch.
    - Valid current canonical starts and preserved v1.4 states pass without normalization or mutation.
  </behavior>
  <action>Implement pure validators over explicit projections. Accumulate applicable issues in fixed rule order and return immutable/cloned results. Require exact current semantic tuple where the caller requests current validation, but keep historical routing outside this current validator. Export contracts from the spec barrel for engine/replay/services/Go-vector generation; do not add a scheduler or alter Strategy legality.</action>
  <verify><automated>pnpm --filter @cowards/spec exec vitest run src/semantic-integrity.test.ts</automated></verify>
  <done>The shared KERN-03 semantic contract admits valid starts and rejects every vector deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parsed typed value to canonical semantic meaning | Type correctness can hide ownership, occupancy, geometry, version, or outcome incoherence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-05 | Tampering/Denial of Service/Information Disclosure | semantic-integrity contract | high | mitigate | Fixed rules/order, explicit caps, immutable results, safe public projection, adversarial shared vectors. |
| T-257-SC | Tampering | package supply chain | low | accept | Existing TypeScript/Zod/Vitest only; no install. |
</threat_model>

<verification>Run the focused spec suite and confirm vector generation/input bytes remain deterministic.</verification>
<success_criteria>All consumers can share one pure, bounded, version-aware semantic vocabulary without sharing gameplay execution.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-05-SUMMARY.md` when done.</output>
