---
phase: 266-content-addressed-current-league-freeze
plan: "02"
status: source-review-gap
empirical_authority: false
absence_receipt_issued: false
---

# Plan 266-02 source review gap

The isolated `codex/phase266-integration` inventory repair remains unmerged and
non-authorizing. Focused synthetic tests and strict TypeScript checks passed,
but independent review did not clear the all-scope absence claim. No actual
absence receipt, current-league freeze, or Phase 267 parent was produced.

The latest repair fails closed for unknown files in derived materialization
scopes and authenticates a bounded subset of league/factory composed carriers.
It still cannot prove absence for an alternate-named opaque file placed
directly at the repository or `.planning` root: those broad scopes hash such
files without decoding or rejecting their contents. A neutral `opaque.payload`
there can conceal a formation candidate. The carrier reader also retains a
512-artifact/32-MiB bound and restrictive typed-payload admission, below the
size and format diversity of the retained Phase 264/265 stores. These limits
fail closed, but prevent the positive full-store proof required by Plan 266-02.

Before promoting the isolated source or marking FRZE-02 complete, revise the
inventory design to close the direct-root alternate-name path without treating
ordinary committed policy prose as formation materialization, and establish a
bounded authenticated decoder/closure at the actual store envelope. Seed
direct repository and `.planning` opaque-file canaries, real-format benign
controls, malformed/unknown/orphan DAG canaries, and projected-scale tests.
Independently review the frozen diff and run the full source suite. A synthetic
fixture or a hash-only file observation is not a real absence receipt.

This source gap is downstream of the separate Phase 265 empirical blocker:
its one-shot allocation ended `process_invalid`, so no real Phase 266 freeze
preflight can pass even if this scanner is repaired. The original unopened
operator-local holdout seal location remains unresolved as another separate
prerequisite. Do not run a new Match, open the holdout, materialize a formation,
or publish a freeze root on the strength of this document.
