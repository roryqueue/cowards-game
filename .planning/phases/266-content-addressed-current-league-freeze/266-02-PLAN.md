---
phase: 266-content-addressed-current-league-freeze
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/v1-38-current-freeze-inventory.ts
  - scripts/lib/v1-38-current-freeze-inventory.test.ts
autonomous: true
requirements: [FRZE-02]
must_haves:
  truths:
    - "A bounded executable inventory independently derives and sees every mandatory repository, generated, artifact, task/lineage, cache, prompt/model, trace/replay, and configured private-store scope."
    - "Every forbidden pre-freeze formation artifact class and naming or embedded-identity path is detected; ordinary contract prose is allowed."
    - "Missing or unreadable scopes and changed snapshots cannot produce an absence receipt."
  artifacts:
    - path: scripts/lib/v1-38-current-freeze-inventory.ts
      provides: "Read-only all-scope formation absence inventory and rooted receipt"
    - path: scripts/lib/v1-38-current-freeze-inventory.test.ts
      provides: "Per-class and per-scope seeded canaries"
  key_links:
    - "Authenticated scope inventory -> forbidden-class findings -> absence receipt"
    - "Receipt snapshot root -> publication-window recheck"
---

<objective>
Make pre-formation absence an executable, all-scope and independently testable precondition (D-10, D-11, D-12).

Purpose: The existing source boundary scanner intentionally omits private/generated stores. Output: a read-only inventory and canary suite; no actual freeze receipt is published in this plan.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md
@.planning/phases/266-content-addressed-current-league-freeze/266-RESEARCH.md
@.planning/phases/266-content-addressed-current-league-freeze/266-PATTERNS.md
@.planning/phases/266-content-addressed-current-league-freeze/266-VALIDATION.md
</context>

<read_first>
Shared execution sources for both tasks: .planning/research/SUMMARY.md; CowardsGameSpec_Full_Consolidated_v1.md; CowardsGame_Technical_Architecture_Spec_V1.md.
</read_first>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Scan every authenticated materialization scope</name>
<files>scripts/lib/v1-38-current-freeze-inventory.ts, scripts/lib/v1-38-current-freeze-inventory.test.ts</files>
<read_first>scripts/lib/v1-38-current-freeze-inventory.ts (if present); scripts/lib/v1-38-current-freeze-inventory.test.ts (if present); scripts/check-v1-38-lab-boundaries.ts; scripts/check-v1-38-lab-boundaries.test.ts; packages/strategy-lab/src/league/repository.ts; scripts/lib/v1-38-local-seal.ts; .planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md; .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md</read_first>
<behavior>- The scanner independently derives mandatory roles from fixed repository/.planning paths plus authenticated Phase 265 allocation, run journal/head, factory producer configuration, actual local-seal configuration and path-bearing typed retained evidence; caller input cannot define or shrink that set.
- Supplied scan roots must exactly reconcile by canonical realpath and role with independently derived roots; missing, extra, unknown, inaccessible, symlinked, unbounded, or changed scope is failure, never interpreted as empty.
- Receipt commits scope identifiers, bounded observation digests, forbidden-class coverage and zero findings without disclosing private paths.</behavior>
<action>Per D-10, create `deriveMandatoryPreFormationScopes`, `inventoryPreFormationAbsence` and `validatePreFormationAbsenceReceipt` with an independently derived required-scope contract and injected filesystem roots for tests. Derive the closed role set from fixed repository/.planning generated/artifact/cache/prompt/trace/replay locations; authenticated Phase 265 allocation `outputDirectories.league` and `outputDirectories.responseFactory`; run journal/head `factoryDirectory` and `responseFactoryDirectory`; retained authoring config `stateDirectory`, `disclosedDirectory`, `promptRoot` and `contextRoot`; other typed path-bearing retained records; and actual operator-local seal-store configuration. The supplied `--inputs` descriptor is only a locator: reconcile its paths one-to-one against that derived set by canonical realpath, role, owner/type and exact object identity; reject missing, extra, unknown, inaccessible, symlinked or aliased roots, including configured roots outside the repository. Re-derive the set during the locked publication check. Walk full data-relevant scopes with bounded lstat/regular-file/no-symlink reads and canonical object identities where available. Inspect typed contents and identity paths as well as filenames, while classifying policy/spec prose as non-materialized. Scan only holdout store metadata/state/ledger and paths, never preimage bytes. Return a versioned private receipt committing the exact observed scopes and zero forbidden objects. Do not use the old source scanner's exclusions.</action>
<verify><automated>pnpm exec vitest run --maxWorkers=1 scripts/lib/v1-38-current-freeze-inventory.test.ts &amp;&amp; pnpm exec tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --types node --skipLibCheck scripts/lib/v1-38-current-freeze-inventory.ts</automated></verify>
<acceptance_criteria>Tests cover omitted, extra, aliased, inaccessible, symlinked, unknown, changed, and over-limit roots, including omission of an authenticated configured root outside the repository while `--inputs` claims no such root; a same-input scan is deterministic and a check-only scan writes no file.</acceptance_criteria>
<done>Every configured scan scope is accounted for before an absence receipt can be valid.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Reject every pre-freeze formation artifact class with seeded canaries</name>
<files>scripts/lib/v1-38-current-freeze-inventory.ts, scripts/lib/v1-38-current-freeze-inventory.test.ts</files>
<read_first>scripts/lib/v1-38-current-freeze-inventory.ts; scripts/lib/v1-38-current-freeze-inventory.test.ts; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; .planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md; scripts/check-v1-38-serious-league-boundaries.ts</read_first>
<behavior>- Current-edge experiment, inward, or bracket materialization is rejected when represented as a namespace, cold-root branch, manifest, initial state, candidate, score, prompt, cache, trace, replay, or result.
- Each class is detected through canonical filename, alternate filename, directory identity, or embedded typed identity across the relevant roots; future-profile policy text remains a negative control.
- A changed scope between scan and publication is detectable by receipt validation.</behavior>
<action>Per D-10 and D-11, build a table-driven forbidden-class classifier over typed artifacts and namespace/identity paths; include seeded canaries for every class across repository, generated, private artifact, task/lineage, cache, prompt/model, trace/replay and private-storage roots. Require a positive canary for every supported naming/identity path and a prose-only control for the approved precommitted formation contract. Root the complete independently derived scope and class coverage plus findings; include snapshot identity that the publisher must rederive under its exclusive observation window. No text-only grep pass or caller-declared scope map may issue an absence receipt.</action>
<verify><automated>pnpm exec vitest run --maxWorkers=1 scripts/lib/v1-38-current-freeze-inventory.test.ts &amp;&amp; pnpm exec tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --types node --skipLibCheck scripts/lib/v1-38-current-freeze-inventory.ts &amp;&amp; pnpm exec tsx scripts/check-v1-38-lab-boundaries.ts</automated></verify>
<acceptance_criteria>All seeded forbidden classes reject from every applicable scope; prose-only fixtures pass; missing canary-class coverage fails the test suite.</acceptance_criteria>
<done>FRZE-02 has an executable pre-root absence proof rather than a narrative assertion.</done>
</task>
</tasks>

## Artifacts this phase produces

- `scripts/lib/v1-38-current-freeze-inventory.ts`: `deriveMandatoryPreFormationScopes`, `inventoryPreFormationAbsence`, `validatePreFormationAbsenceReceipt`, typed scan-scope and absence-receipt contracts.
- `scripts/lib/v1-38-current-freeze-inventory.test.ts`: all-scope, all-class, alternate-path, and prose-control canaries.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository/configured private roots -> absence receipt | A hidden, unreadable, or race-added formation artifact would make a false absence claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-266-05 | Tampering | scope selection | high | mitigate | Authenticated required-role scope map; missing/unknown/unreadable/symlink roots fail. |
| T-266-06 | Spoofing/Repudiation | forbidden artifact classifier | high | mitigate | Typed identity and path checks with per-class/per-scope canaries, not grep-only evidence. |
| T-266-07 | Elevation of privilege | stale absence receipt | high | mitigate | Receipt commits exact scope snapshot; publication rechecks it while holding the same exclusive gate. |
| T-266-08 | Information disclosure | receipt metadata | high | mitigate | Private full findings; safe output has opaque roots and approved aggregates only. |
| T-266-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>

<verification>Focused canary suite and existing lab boundary scanner pass; tests use only injected temporary roots and never materialize a real formation namespace.</verification>
<success_criteria>FRZE-02 absence is proven across all declared scopes with class-complete canaries and a recheckable private receipt.</success_criteria>
<output>Create `.planning/phases/266-content-addressed-current-league-freeze/266-02-SUMMARY.md` when done.</output>
