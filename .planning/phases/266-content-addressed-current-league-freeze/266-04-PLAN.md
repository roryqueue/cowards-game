---
phase: 266-content-addressed-current-league-freeze
plan: 04
type: execute
wave: 3
depends_on: [266-01, 266-02, 266-03]
files_modified:
  - packages/strategy-lab/src/league/freeze.ts
  - packages/strategy-lab/src/league/freeze.test.ts
  - scripts/lib/v1-38-current-freeze-publication.ts
  - scripts/derive-v1-38-current-freeze-expected-root.ts
  - scripts/freeze-v1-38-current-league.ts
  - scripts/freeze-v1-38-current-league.test.ts
autonomous: true
requirements: [FRZE-01, FRZE-02, FRZE-03, FRZE-04]
must_haves:
  truths:
    - "Read-only raw-graph/custody admission binds an actual Phase 265 head, complete charges, independent empirical verification, and unopened seal metadata before any positive freeze decision; a separate reviewer record supplies the expected final root for write/check."
    - "An exclusive all-leaf publication window emits one validated final root or no valid root, even on interruption or a concurrent formation attempt."
    - "Only a privacy-safe roots/approved-aggregate projection can leave the restricted store."
  artifacts:
    - path: packages/strategy-lab/src/league/freeze.ts
      provides: "Authenticated empirical eligibility and exact-parent acceptance gated on the raw-graph/custody adapter"
    - path: packages/strategy-lab/src/league/freeze.test.ts
      provides: "Injected complete process-valid non-pass, empty allowlist, exact-parent acceptance, and fixture-denial tests"
    - path: scripts/lib/v1-38-current-freeze-publication.ts
      provides: "Staged immutable all-leaf transaction and data-only checker"
    - path: scripts/freeze-v1-38-current-league.ts
      provides: "Explicit offline preflight/write/check CLI"
    - path: scripts/derive-v1-38-current-freeze-expected-root.ts
      provides: "Separate read-only raw-evidence root derivation and immutable private reviewer record"
    - path: scripts/freeze-v1-38-current-league.test.ts
      provides: "Synthetic input, privacy, crash/race, read-only and mutation regressions"
  key_links:
    - "Authenticated Phase 265 raw graph/custody + independent empirical closure + original unopened seal + all-scope absence -> positive eligibility -> final manifest assembly"
    - "Staged leaf/receipt byte verification + exclusive snapshot -> no-replace final-root commit marker"
---

<objective>
Implement the authenticated raw-graph/custody adapter, positive empirical/parent gates, offline read-only gate, and atomic immutable publisher, but exercise them only on synthetic injected evidence in this plan (D-01 through D-16).

Purpose: The final root must be mechanically impossible to publish from incomplete Phase 265 evidence, protocol-only seal data, or a stale absence scan. Output: authenticated adapter, positive injected non-pass/parent tests, publisher, CLI, and fault tests; no actual freeze root.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md
@.planning/phases/266-content-addressed-current-league-freeze/266-RESEARCH.md
@.planning/phases/266-content-addressed-current-league-freeze/266-PATTERNS.md
@.planning/phases/266-content-addressed-current-league-freeze/266-VALIDATION.md
@.planning/phases/266-content-addressed-current-league-freeze/266-01-SUMMARY.md
@.planning/phases/266-content-addressed-current-league-freeze/266-02-SUMMARY.md
@.planning/phases/266-content-addressed-current-league-freeze/266-03-SUMMARY.md
</context>

<read_first>
Shared execution sources for both tasks: .planning/research/SUMMARY.md; CowardsGameSpec_Full_Consolidated_v1.md; CowardsGame_Technical_Architecture_Spec_V1.md.
</read_first>

<publication_epoch_contract>
`--preflight` is read-only and non-authorizing and does not require an expected root. Plan 05 records its roots-only report and commits that report before fixing the immutable prepublication Git commit/tree/dirty-declaration epoch, preserving and binding any pre-existing user changes without deleting them. The separate `derive-v1-38-current-freeze-expected-root.ts` route then reopens authenticated raw Phase 265/seal/inventory inputs at that epoch, constructs the complete typed leaf set without reading a candidate freeze manifest, publisher response, or safe receipt, applies the canonical domain-root primitive, and writes only one no-replace private reviewer record containing expected root, epoch identity, input roots and derivation-source root. `--write` and `--check` require both independently supplied `--freeze-root` and `--expected-record`, rederive the root themselves, and require all three to agree. The private reviewer record grants no authority on its own.

The baseline formation-absence/source snapshot is the committed prepublication epoch. Preflight report is inside it. Only these later phase-owned outputs may be excluded from baseline absence/dirty comparison: exact private expected-root reviewer record, content-addressed Phase 266 private leaves/absence receipt/manifest/commit marker, `.planning/artifacts/v1.38-phase-266-current-league-freeze.json`, and `266-FREEZE-VERIFICATION.md` / `266-05-SUMMARY.md` after they exist. Each allowed output is separately schema/role/byte-hash/privacy validated and forbidden-formation-class scanned; it cannot redefine baseline source, scopes, or evidence. `--check` reconstructs the baseline from the committed epoch, validates the exact allowed output delta, and rejects any unrelated added/changed/deleted path or private-store object. Phase 267 rechecks the immutable root and baseline receipt under its own later source epoch, not by pretending its future checkout is the old Phase 266 source.
</publication_epoch_contract>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Build the all-leaf immutable transaction and safe projection</name>
<files>packages/strategy-lab/src/league/freeze.ts, packages/strategy-lab/src/league/freeze.test.ts, scripts/lib/v1-38-current-freeze-publication.ts, scripts/derive-v1-38-current-freeze-expected-root.ts, scripts/freeze-v1-38-current-league.test.ts</files>
<read_first>scripts/lib/v1-38-current-freeze-publication.ts (if present); scripts/derive-v1-38-current-freeze-expected-root.ts (if present); scripts/freeze-v1-38-current-league.test.ts (if present); packages/strategy-lab/src/league/freeze.ts; packages/strategy-lab/src/league/freeze.test.ts; packages/strategy-lab/src/league/freeze-finalists.ts; scripts/run-v1-38-serious-league.ts; packages/strategy-lab/src/league/allocation.ts; .planning/phases/265-serious-current-rules-league-and-development-red-team/265-VERIFICATION.md; scripts/lib/v1-38-current-freeze-inventory.ts; scripts/lib/v1-38-local-seal.ts; packages/strategy-lab/src/league/repository.ts; scripts/lib/v1-38-durable-publication-successor-v1.ts; packages/spec/src/public-output-privacy.ts; .planning/artifacts/v1.37-restricted-evidence-policy.md</read_first>
<behavior>- Private required leaves and absence receipt are staged, re-resolved/rehashed under one exclusive snapshot, and only then a no-replace root commit marker makes the manifest authoritative.
- A crash before commit marker, missing staged leaf, changed inventory, conflicting target, or concurrent formation materialization produces no valid root; same bytes may be checked repeatedly without mutation.
- Safe receipt is exact-schema roots/approved aggregates and recursively privacy-scanned; source, memory, objective, prompt, trace, holdout bytes, private path, and internal error payload cannot leak.
- An injected but fully authenticated retained-graph/custody fixture can positively classify a complete process-valid non-pass with an explicitly empty exact-source allowlist and accept only its independently expected exact parent after reopening the committed final-root marker; the same leaf set without the adapter's authenticated result remains non-authorizing.</behavior>
<action>Per D-01, D-02, D-03, D-06, D-08, D-09 and D-12, implement `authenticateRetainedCurrentLeagueFreezeInput` as the read-only raw-graph/custody adapter over explicit Phase 265 repositories and head: boundedly reopen the graph and all referenced raw bytes; admit the unchanged v2 allocation and reconcile its exact root with run-start, head, result and independent LEAG-01–09 empirical verification bound to that head; join factory/response candidate closures, starts/terminals/unused charges, matrices, analysis/probes/red-team/report, historical producer source, and the Plan 03 original unopened seal receipt. Never treat `verifyRetainedSeriousLeague`, a caller `passed` flag, source-only manifest, or a synthetic fixture label as empirical authority. Extend `freeze.ts` eligibility and `requireExactCurrentLeagueFreezeParent` so only this authenticated complete result, an independently reopened committed final-root marker, and the separately supplied expected root can yield positive process-valid non-pass and exact-parent acceptance, with an explicit empty finalist/source allowlist when warranted; leave source-only candidates denied. Do not expose a deserializable trusted flag or caller-constructible authority token; recheck the authenticated evidence/marker at the positive-use boundary. Test the positive path using injected persisted graph/custody readers and independently rooted verification fixtures only, not a live empirical claim. Then implement `stageCurrentLeagueFreeze`, `publishCurrentLeagueFreeze`, and `checkCurrentLeagueFreeze` over the plan-01/02/03 contracts, plus the distinct `deriveIndependentCurrentFreezeExpectedRoot` raw-evidence route in `scripts/derive-v1-38-current-freeze-expected-root.ts`. After the roots-only preflight report is committed as the source epoch, this route independently reads authenticated typed raw leaves, never a candidate freeze manifest/publisher response/safe receipt, applies the canonical domain identity, and persists one no-replace operator-private reviewer record with expected root, epoch, input roots and derivation identity. Write/check each take independently supplied expected `freezeRoot` and immutable reviewer record and refuse any derived or stored root mismatch, even when the substituted branch is internally valid. Use an exclusive private-store observation/publication lock and bounded authenticated reads; under that gate, reconstruct the committed prepublication source/absence epoch and separately validate only the exact named phase-owned reviewer record, private freeze objects, safe receipt, verification and summary output delta; reject every unrelated change; independently rederive and reconcile the mandatory scope set from fixed repository/.planning paths, authenticated unchanged Phase 265 prospective allocation `outputDirectories.league`/`outputDirectories.responseFactory`, run journal/head `factoryDirectory`/`responseFactoryDirectory`, retained authoring config `stateDirectory`/`disclosedDirectory`/`promptRoot`/`contextRoot`, other path-bearing typed evidence and actual seal-store config; then recheck exact source/dirty declaration, all required object bytes, Phase 265 charge/completion graph and unopened seal metadata/ledger. Stage canonical manifest and receipt as private content-addressed objects, fsync them, then no-replace publish one final-root commit marker only after all members and safe projection validate. A marker is valid only if an independent checker reopens every reference; a partial stage is non-authorizing and never repaired by `--check`. Reject symlinks, stale scope digests, conflicting same-root bytes, rollback/rewrite and any descendant parent that is not the independently expected final root. Keep previous roots as immutable branches; changed bytes produce a new root and former descendants fail exact-parent validation. Do not copy the predecessor pair transaction's private-byte-containing intent. Use `assertPublicOutputLeakSafe` on the exact safe receipt and sanitized errors. Do not edit engine, runtime, canonical Strategy registration, public DTOs or ORM schema.</action>
<verify><automated>pnpm exec vitest run --maxWorkers=1 packages/strategy-lab/src/league/freeze.test.ts scripts/freeze-v1-38-current-league.test.ts &amp;&amp; pnpm exec tsc -b packages/strategy-lab/tsconfig.json --pretty false &amp;&amp; pnpm exec tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --types node --skipLibCheck scripts/lib/v1-38-current-freeze-publication.ts scripts/derive-v1-38-current-freeze-expected-root.ts</automated></verify>
<acceptance_criteria>Injected positive tests cover a complete process-valid non-pass path using persisted graph/custody and separate verification fixtures, explicitly empty exact-source allowlist, matching independently expected final root, and accepted exact parent only after authenticated adapter output and independently reopened commit marker; the identical typed leaves without the adapter remain denied. Fault tests cover each stage/commit boundary, missing or mutated leaf, changed or omitted outside-repository scope, pre-commit race, alternate formation identity, conflicting root, stale source, incomplete charge, bad seal, safe-output leak and exact-parent invalidation. Two valid synthetic freeze branches prove that expected `freezeRoot=A` rejects substitution of B in check and Phase 267 handoff; no private data appears in emitted receipt/error.</acceptance_criteria>
<done>Only an authenticated complete raw graph/custody result can pass positive process-valid non-pass and exact-parent tests; one root marker is authoritative only when complete immutable members and absence receipt are independently resolvable and revalidated.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Wire explicit read-only preflight/check and gated write CLI</name>
<files>scripts/freeze-v1-38-current-league.ts, scripts/freeze-v1-38-current-league.test.ts</files>
<read_first>scripts/freeze-v1-38-current-league.ts (if present); scripts/freeze-v1-38-current-league.test.ts; scripts/lib/v1-38-current-freeze-publication.ts; scripts/derive-v1-38-current-freeze-expected-root.ts; scripts/run-v1-38-serious-league.ts; scripts/evaluate-v1-37-prearchive-proof.ts; .planning/phases/265-serious-current-rules-league-and-development-red-team/265-07-PLAN.md; .planning/phases/265-serious-current-rules-league-and-development-red-team/265-VERIFICATION.md; .planning/artifacts/v1.38-local-seal-protocol-v2.json; .planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md</read_first>
<behavior>- `--preflight` is strictly read-only and non-authorizing without a root; `--check` is strictly read-only and requires expected root/reviewer record; `--write` requires the independently supplied `--freeze-root` and immutable private expected-root record, Phase 265 immutable head and the unchanged prospective allocation whose exact root is bound by that head, independent empirical verification, actual seal store/receipt, reconciled mandatory inventory scopes, and private freeze store.
- Source-only fixtures and an unrun prospective allocation alone never satisfy the authenticated adapter or production CLI gate; `verifyRetainedSeriousLeague` is a supporting read-only graph check, never the empirical-completion verdict.
- CLI has no provider, Strategy, Match, model, holdout opening/query, formation constructor, or product mutation mode.</behavior>
<action>Per D-02 through D-04, D-10 through D-12 and D-16, expose only explicit `--preflight`, `--write`, `--check` modes with `--freeze-root` and `--expected-record` required only for write/check, plus exact `--inputs`, `--phase-265-allocation`, `--phase-265-result`, `--phase-265-verification`, `--seal-store`, `--freeze-store` inputs and `--receipt-path` for checking the safe artifact; no defaults to HEAD, latest, timestamp or directory order. A separate raw-evidence reviewer route derives and records the expected root after the preflight report is committed as the source epoch; `--freeze-root` is supplied from that immutable private record, never the candidate receipt/manifest, and compared to the derived root in write/check and later Phase 267 handoff. Preflight has no root and grants no authority. `--inputs` is an operator-local restricted path locator only: independently derive the mandatory scope set from fixed repository/.planning paths, authenticated Phase 265 allocation/run journal/head, factory producer config, actual seal-store config and path-bearing typed evidence, then reconcile one-to-one by canonical realpath and role; its own scope claims carry no authority. Require the unchanged `.planning/artifacts/v1.38-phase-265-allocation-v2.json` as the approved prospective allocation; admit it to freeze only when its exact `allocation.root` equals the admitted root of the allocation embedded in the actual `run-start`, the retained head's `allocationRoot`, and the `v1.38-phase-265-run-result.json` allocation root, with independently reopened graph and separately completed empirical verification. Reject the older allocation path, altered v2 bytes, missing run, or any root mismatch; no successor allocation artifact exists. Resolve separately completed Phase 265 SUMMARY/VERIFICATION with all LEAG-01–09 and roadmap empirical truths independently verified; invoke the Plan 04 raw-graph/custody adapter to confirm immutable head/allocation and independent verdict match retained repositories, rederive complete charge/leaf graph, and deny the source-only Plan 01 candidate path. Reconstruct the historical Phase 265 producer source inventory and implementation/source roots from its frozen commit in a read-only detached checkout or retained exact inventory; never recompute that historical identity using `leagueCurrentSourceIdentity()` over the current Phase 266 checkout, whose new scripts alter `factoryAssessmentImplementationManifest()`. Bind separate Phase 266 freezer source identity. Require a real operator-configured restricted commitment store and read-only committed-state inspector; checked-in protocol-v2 is not a substitute. Resolve every inventory scope, run preliminary finalist proof and versioned non-circular seal bridge, then call publication. `--preflight` renders a private non-authorizing decision without writing; `--check` reconstructs the committed prepublication source/absence epoch plus only named, separately verified phase-owned output deltas and reopens an existing exact root/receipt byte-for-byte without write or seal event append. Source tests use synthetic injected stores only; never call the production `--write` before the Plan 05 gate.</action>
<verify><automated>pnpm exec vitest run --maxWorkers=1 scripts/freeze-v1-38-current-league.test.ts packages/strategy-lab/src/league/freeze.test.ts packages/strategy-lab/src/league/freeze-finalists.test.ts scripts/lib/v1-38-current-freeze-inventory.test.ts &amp;&amp; pnpm exec tsc -b packages/strategy-lab/tsconfig.json --pretty false &amp;&amp; pnpm exec tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --types node --skipLibCheck scripts/lib/v1-38-current-freeze-inventory.ts scripts/lib/v1-38-current-freeze-publication.ts scripts/derive-v1-38-current-freeze-expected-root.ts scripts/freeze-v1-38-current-league.ts &amp;&amp; pnpm exec tsx scripts/check-v1-38-lab-boundaries.ts &amp;&amp; pnpm exec tsx scripts/check-v1-38-serious-league-boundaries.ts</automated></verify>
<acceptance_criteria>Missing Phase 265 result/SUMMARY/independent verification, `gaps_found`, old or altered prospective allocation, allocation/run-start/head/result root mismatch, source-only or unrun prospective input, missing actual seal, noncommitted ledger, omitted outside-repository mandatory scope, wrong independently supplied `--freeze-root` or private reviewer record, and partial preliminary proof all fail before publication. Injected integration tests positively admit a complete process-valid non-pass with empty exact-source allowlist and accept its exact independently expected parent only through the authenticated adapter plus reopened final-root marker; mismatched or source-only variants reject. Synthetic end-to-end transition tests cover non-authorizing preflight → committed report epoch → separate expected-root record → write → safe receipt → read-only check, including alternate valid branch substitution and unrelated file/store mutation rejection. Adding Phase 266 source scripts after the frozen Phase 265 producer commit does not falsify historical producer verification or substitute current freezer source identity. Snapshot tests prove `--preflight`/`--check` do not write.</acceptance_criteria>
<done>All source-only mechanics pass; the real CLI still emits no Phase 266 root until Plan 05 supplies authenticated empirical and seal inputs.</done>
</task>
</tasks>

## Artifacts this phase produces

- `packages/strategy-lab/src/league/freeze.ts`: authenticated eligibility and exact-parent acceptance wired to Plan 01 mechanics.
- `packages/strategy-lab/src/league/freeze.test.ts`: injected authenticated non-pass/empty-allowlist/exact-parent acceptance and source-only denial tests.
- `scripts/lib/v1-38-current-freeze-publication.ts`: `authenticateRetainedCurrentLeagueFreezeInput`, `stageCurrentLeagueFreeze`, `publishCurrentLeagueFreeze`, `checkCurrentLeagueFreeze`.
- `scripts/derive-v1-38-current-freeze-expected-root.ts`: `deriveIndependentCurrentFreezeExpectedRoot` and private immutable reviewer record.
- `scripts/freeze-v1-38-current-league.ts`: explicit offline `--preflight`, `--write`, `--check` CLI and exact input contract.
- `scripts/freeze-v1-38-current-league.test.ts`: synthetic publication, privacy, read-only, crash, and race tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Producer inputs -> offline publisher | A plausible source-only graph or stale seal could be laundered into empirical authority. |
| Private store -> safe receipt | Private evidence and host paths must not escape through metadata or errors. |
| Absence scan -> root publication | Concurrent materialization could invalidate a stale negative scan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-266-13 | Spoofing/Tampering | CLI input gate | high | mitigate | Read-only authenticated raw-graph/custody adapter joins exact head/allocation, independent Phase 265 closure, charges and original seal receipt before positive eligibility or write. |
| T-266-14 | Repudiation | all-leaf publication | high | mitigate | Reconcile complete charges and reopen every typed object before exclusive no-replace marker. |
| T-266-15 | Elevation of privilege | publication race | high | mitigate | Locked same-snapshot absence recheck, staged fsynced members, atomic marker; Phase 267 must recheck exact root before construction. |
| T-266-16 | Information disclosure | safe projection/errors | high | mitigate | Exact roots-only schema, recursive privacy scan and sanitized diagnostics; no private-byte intent. |
| T-266-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>

<verification>All source suites, typecheck and existing private boundaries pass on synthetic data. Confirm absence of `.planning/artifacts/v1.38-phase-266-current-league-freeze.json` and of real root publication after this plan.</verification>
<success_criteria>Authenticated adapter plus pure mechanics prove positive complete process-valid non-pass and exact-parent behavior in injected tests; real publication still requires Plan 05's actual Phase 265 and seal inputs.</success_criteria>
<output>Create `.planning/phases/266-content-addressed-current-league-freeze/266-04-SUMMARY.md` when done.</output>
