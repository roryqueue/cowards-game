---
phase: 266-content-addressed-current-league-freeze
plan: 03
type: execute
wave: 2
depends_on: [266-01]
files_modified:
  - scripts/lib/v1-38-local-seal.ts
  - scripts/verify-v1-38-local-seal.test.ts
  - packages/strategy-lab/src/league/freeze-finalists.ts
  - packages/strategy-lab/src/league/freeze-finalists.test.ts
autonomous: true
requirements: [FRZE-01, FRZE-03, FRZE-04]
must_haves:
  truths:
    - "The original operator-local commitment and unopened ledger are read without an event append, preimage read, opening, or query."
    - "The source-only seal precursor is distinguishable from and non-circularly joined to the complete final manifest root."
    - "Only preliminarily proven, exact-source-hash current finalists enter the frozen eligibility list; empty eligibility is explicit."
  artifacts:
    - path: scripts/lib/v1-38-local-seal.ts
      provides: "Read-only committed/unopened receipt inspection over existing validated state and ledger"
    - path: packages/strategy-lab/src/league/freeze-finalists.ts
      provides: "Preliminary finalist proof and exact-hash allowlist contracts"
  key_links:
    - "Original seal commitment/ledger + precursor checkout identity -> versioned final-root bridge"
    - "Actual finalist source hash + all preliminary subreceipts -> eligibility allowlist leaf"
---

<objective>
Bind the actual unopened local seal and prove exact current-finalist feasibility without granting ordinary product certification (D-05, D-07, D-13, D-14, D-15, D-16).

Purpose: A source-only predecessor identity and seal-mechanics demonstration must not be mistaken for a real holdout commitment. Output: read-only receipt inspection, non-circular bridge, and preliminary finalist/allowlist gates with fake-store tests only.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md
@.planning/phases/266-content-addressed-current-league-freeze/266-RESEARCH.md
@.planning/phases/266-content-addressed-current-league-freeze/266-PATTERNS.md
@.planning/phases/266-content-addressed-current-league-freeze/266-01-SUMMARY.md
@.planning/artifacts/v1.38-local-seal-protocol-v2.json
</context>

<read_first>
Shared execution sources for both tasks: .planning/research/SUMMARY.md; CowardsGameSpec_Full_Consolidated_v1.md; CowardsGame_Technical_Architecture_Spec_V1.md.
</read_first>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Inspect the original seal commitment without mutation and define the final-root bridge</name>
<files>scripts/lib/v1-38-local-seal.ts, scripts/verify-v1-38-local-seal.test.ts</files>
<read_first>scripts/lib/v1-38-local-seal.ts; scripts/verify-v1-38-local-seal.test.ts; packages/strategy-lab/src/league/freeze.ts; .planning/artifacts/v1.38-local-seal-protocol-v2.json; .planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-CONTEXT.md; .planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md; packages/spec/src/canonical-identity-domains.ts</read_first>
<behavior>- A read-only inspector validates original commitment bytes/root, sourceCommit/sourceTree/freezeCarrierIdentity/currentLeagueFreezeRoot precursor, hash-chained event ledger, state file, store ownership and pre-open `committed` state without appending any event.
- Any arm, consume, evaluation, project, verify, contaminate, retire, access/query, corrupt chain, absent actual commitment, or protocol-only fixture blocks; a safe roots-only receipt identifies `single_operator_local_seal_v1` and its assurance limits.
- The versioned bridge binds the immutable precursor/commitment/ledger to a separately derived league-evidence-set root; the final manifest then binds that bridge, so no leaf refers to its own final root or changes original seal history.</behavior>
<action>Per D-01, D-05, D-07 and D-16, add `inspectV138LocalSealUnopened` beside existing private `loadCommitment`/`loadLedger` so it reuses owner-only bounded reads and validates existing state without the event-appending `verifyV138LocalSealReceipt` API. Return only commitment root, request root, state/ledger root, original Git commit/tree/carrier, and precursor `currentLeagueFreezeRoot`; never read `input/` or secret bytes. Reject any state beyond exactly committed or any access/query evidence. Define `V138CurrentFreezeSealBridge` as a canonical final-manifest leaf over the original precursor/commitment/ledger, an independently derived league-evidence-set root that excludes this bridge, and distinct authenticated Phase 265 producer and Phase 266 freezer source/tuple declarations. Derive the final manifest root only after adding this bridge as a leaf; neither the bridge nor the original commitment contains the final root. Independently rederive the precursor from its recorded original commit/tree, validate those Git objects and declared ancestry to the Phase 265 producer and Phase 266 freezer sources, and compare the seal request's kernel/runtime/semantic tuple to the final manifest. If that relation cannot be established, block; do not invent a new seal authority or sign a hash as if it were a signature. The checked-in protocol artifact reports no real holdout material and cannot satisfy this gate.</action>
<verify><automated>pnpm exec vitest run --maxWorkers=1 scripts/verify-v1-38-local-seal.test.ts &amp;&amp; pnpm exec tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --types node --skipLibCheck scripts/lib/v1-38-local-seal.ts</automated></verify>
<acceptance_criteria>Fake restricted-store tests show byte-identical state before/after valid and invalid inspection, reject opened/queried/contaminated/corrupt/missing stores, reject source/tuple/ancestry mismatches, and prove precursor and final root remain distinct without a hash cycle.</acceptance_criteria>
<done>Only an actual unchanged pre-open local commitment/ledger can supply the final freeze's seal leaf.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Prove preliminary current-finalist feasibility and freeze exact-source eligibility</name>
<files>packages/strategy-lab/src/league/freeze-finalists.ts, packages/strategy-lab/src/league/freeze-finalists.test.ts</files>
<read_first>packages/strategy-lab/src/league/freeze-finalists.ts (if present); packages/strategy-lab/src/league/freeze-finalists.test.ts (if present); packages/strategy-lab/src/league/freeze.ts; packages/strategy-lab/src/league/selection.ts; packages/strategy-lab/src/league/report.ts; packages/strategy-lab/src/factory/admission.ts; packages/strategy-lab/src/factory/repository.ts; .planning/REQUIREMENTS.md; .planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md; .planning/phases/266-content-addressed-current-league-freeze/266-CONTEXT.md</read_first>
<behavior>- Each actual Phase 265 finalist has exact source bytes/hash and independent rooted preliminary legal-information, deterministic-repeat, source-size, memory/objective/output, declared runtime profile, replay-review, and compatibility subresults.
- Eligibility is the subset with all subresults passing; a missing or failing proof cannot be silently dropped or promoted, and a no-finalist or all-failed set yields an explicit empty allowlist.
- Ordinary promotion later requires exact hash membership and canonical immutable Strategy Revision re-admission; formation-control candidates never inherit membership.</behavior>
<action>Per D-13 through D-15, implement `deriveCurrentFinalistPreliminaryReceipts` and `deriveCurrentFinalistSourceAllowlist` with exact finalist-set reconciliation to the verified Phase 265 disposition. Reopen source and evidence roots and require all seven preliminary categories with declared runtime lane/profile; derive deterministic-repeat and replay-review only from authenticated retained current-rules evidence, never a fresh Match or holdout query. Bind each accepted hash to its evidence roots, source bytes and candidate lineage, then emit a sorted exact-hash allowlist and explicit ordinary Strategy Revision re-admission rule. Do not perform Phase 269 product-path certification or claim readiness from a missing finalist. Test no-finalist, all-failed and partial-failed branches; a failing finalist stays recorded with its reason but not eligible.</action>
<verify><automated>pnpm exec vitest run --maxWorkers=1 packages/strategy-lab/src/league/freeze-finalists.test.ts &amp;&amp; pnpm exec tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>Injected exact-source passing finalist is listed; changed source, missing category, mismatched runtime/replay root or formation-derived candidate rejects eligibility; empty result remains an empty allowlist with no promotion claim.</acceptance_criteria>
<done>FRZE-03 has source-hash-specific preliminary proof and an immutable, possibly empty ordinary-promotion design.</done>
</task>
</tasks>

## Artifacts this phase produces

- `scripts/lib/v1-38-local-seal.ts`: `inspectV138LocalSealUnopened`, `V138CurrentFreezeSealBridge` and read-only precursor/final-root join.
- `scripts/verify-v1-38-local-seal.test.ts`: fake restricted-store nonmutation and seal-bridge regressions.
- `packages/strategy-lab/src/league/freeze-finalists.ts`: `deriveCurrentFinalistPreliminaryReceipts`, `deriveCurrentFinalistSourceAllowlist`.
- `packages/strategy-lab/src/league/freeze-finalists.test.ts`: exact-hash, category, and empty-eligibility tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator-local restricted seal -> freeze | Commitment/state metadata may be observed; preimage/access/query/opening remains forbidden. |
| Current finalist evidence -> later canonical Strategy Revision | A label or formation candidate cannot substitute for exact allowed source bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-266-09 | Information disclosure | seal inspector | high | mitigate | Read only commitment/state/ledger; no preimage path, opening command, query or raw safe-output payload; snapshot nonmutation tests. |
| T-266-10 | Spoofing/Tampering | precursor-to-final bridge | high | mitigate | Recompute original precursor from immutable commit/tree, verify ledger and exact tuple/source ancestry, bind separate final root. |
| T-266-11 | Elevation of privilege | preliminary eligibility | high | mitigate | Seven rooted category receipts and exact source hash; no product certification and explicit canonical re-admission. |
| T-266-12 | Repudiation | failed/no finalists | medium | mitigate | Retain every finalist disposition and explicit empty allowlist; no fabricated readiness. |
| T-266-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>

<verification>Run fake-store seal tests, preliminary finalist tests, and TypeScript checks. Inspect test fixtures to confirm no real holdout preimage or empirical claim is present.</verification>
<success_criteria>FRZE-03 is executable and read-only; the original commitment, not a protocol demo or source precursor renamed as a final root, is the only seal input.</success_criteria>
<output>Create `.planning/phases/266-content-addressed-current-league-freeze/266-03-SUMMARY.md` when done.</output>
