---
phase: 264-immutable-factory-independent-oracles-and-quarantined-intake
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/strategy-lab/src/factory/contracts.ts
  - packages/strategy-lab/src/factory/contracts.test.ts
  - packages/strategy-lab/src/factory/identity.ts
  - packages/strategy-lab/src/factory/identity.test.ts
  - packages/strategy-lab/src/factory/ledger.ts
  - packages/strategy-lab/src/factory/ledger.test.ts
  - packages/strategy-lab/src/factory/repository.ts
  - packages/strategy-lab/src/factory/repository.test.ts
  - packages/strategy-lab/src/factory/admission.ts
  - packages/strategy-lab/src/factory/admission.test.ts
  - packages/strategy-lab/src/index.ts
  - packages/strategy-lab/package.json
autonomous: true
requirements: [FACT-05, FACT-06, FACT-07]
must_haves:
  truths:
    - "A private candidate, correction, retry, and every terminal attempt have distinct canonical roots and retained lineage."
    - "Factory admission preserves exact native source/lane and can reach only injected existing supervised execution evidence."
    - "No candidate attempt can become gameplay when validation or supervision reports a failure."
  artifacts:
    - path: packages/strategy-lab/src/factory/contracts.ts
      provides: strict private factory artifact, attempt, and oracle-packet schemas
    - path: packages/strategy-lab/src/factory/ledger.ts
      provides: append-only charged attempt ledger validation
    - path: packages/strategy-lab/src/factory/repository.ts
      provides: private content-addressed publish-once/read/resume repository and terminal-coverage guard
    - path: packages/strategy-lab/src/factory/admission.ts
      provides: immutable private candidate admission and supervised-result mapping
  key_links:
    - from: packages/strategy-lab/src/factory/admission.ts
      to: packages/strategy-lab/src/runtime-bridge.ts
      via: injected LabSupervisedProvider and runCanonicalLabMatch only
      pattern: runCanonicalLabMatch
  prohibitions:
    - "No mutable latest address, overwrite, production revision/persistence/DTO projection, or direct candidate-source execution."
    - "No Phase 263 allocation, selector, custody, Match, or retry reuse; failed and blocked records remain private charged evidence."
---

<objective>
Create the shared private, immutable candidate factory foundation.
Purpose: make every later oracle and intake path preserve evidence without becoming a production Strategy path.
Output: strict factory contracts, identity constructors, private durable repository, append-only ledger, and hostile-source admission API.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-RESEARCH.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-07-SUMMARY.md
@packages/strategy-lab/src/contracts.ts
@packages/strategy-lab/src/runtime-bridge.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define private immutable factory contracts and roots</name>
  <files>packages/strategy-lab/src/factory/contracts.ts, packages/strategy-lab/src/factory/contracts.test.ts, packages/strategy-lab/src/factory/identity.ts, packages/strategy-lab/src/factory/identity.test.ts</files>
  <read_first>packages/strategy-lab/src/contracts.ts; packages/strategy-lab/src/identity.ts; packages/spec/src/runtime.ts; .planning/artifacts/v1.38-pre-search-study-policy.json; .planning/artifacts/v1.38-pre-search-measurement-policy.json</read_first>
  <behavior>- Same canonical input has the same domain-separated root; source, provider/prompt/validation/build correction, or lineage change has a new root.\n- A candidate requires source bytes/hash, build/toolchain and compatibility identities, factory/algorithm/schema versions, parent lineage, doctrine/oracle family, split, validation, exact native lane/runtime profile, and six fingerprint roots.\n- Unknown keys, mutable addresses, translated lanes, missing predecessor roots, or malformed identities are rejected.</behavior>
  <action>Create strict bounded canonical schemas and factory-specific identity functions under `strategy-lab/src/factory`; reuse `admitCanonicalJsonValue`, `labRoot`, `freezeLabValue`, and the existing LAB_ADMITTED_ROOTS as non-consuming predecessor references per D-01, D-02, D-06, D-07, D-08, and D-10. Define a typed `FactoryOraclePacket` data-only seam that the three leaf packages can emit; do not import any oracle from the factory. Require immutable content roots and lineage edges rather than mutation or mutable aliases. Keep all values private/offline and exclude production revision IDs, production persistence, Chronicle, schedules, standings, and DTO shapes per D-05.</action>
  <verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/contracts.test.ts packages/strategy-lab/src/factory/identity.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab</automated></verify>
  <acceptance_criteria>Canonical candidate roots bind every D-07 field, corrections create a new lineage child, and malformed/mutable/native-lane-changing envelopes fail before storage.</acceptance_criteria>
  <done>FACT-05 private artifact identity is testable without widening a production schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement charged append-only attempt ledger and hostile admission</name>
  <files>packages/strategy-lab/src/factory/ledger.ts, packages/strategy-lab/src/factory/ledger.test.ts, packages/strategy-lab/src/factory/repository.ts, packages/strategy-lab/src/factory/repository.test.ts, packages/strategy-lab/src/factory/admission.ts, packages/strategy-lab/src/factory/admission.test.ts</files>
  <read_first>packages/strategy-lab/src/runtime-bridge.ts; packages/strategy-lab/src/contracts.ts; apps/runtime-service/src/revalidate-strategy-revision-v1-19.ts; packages/runtime-js/src/supervised-subprocess-adapter.ts</read_first>
  <behavior>- Charging occurs before validation/execution and exactly one retained terminal record exists for every attempted allocation.\n- accepted, rejected, invalid, duplicate, legal-but-weak, retried, player violation, and system failure are distinguishable and never scored as gameplay except actual supervised success.\n- Admission accepts source as bytes/data and routes Match validation only through injected existing supervision.\n- Repository publication is non-overwriting and root-only; resume treats unreadable or start-only work as uncertain/system-failed and rejects missing, duplicate, or incomparable terminal coverage.</behavior>
  <action>Implement an append-only ledger verifier and immutable record constructors that bind task/budget roots, authoring mechanism, input/output roots, validation, duplicate/clone evidence, retry parent, resource accounting, and final disposition per D-04 and D-09. This task owns the bounded repository handoff: define the `FactoryRepository` API first, then connect admission to that API, with no unrelated orchestration or producer implementation added to this foundation. Add the private `FactoryRepository` using the safe content-addressed publication and conservative resume pattern from `shards.ts`: `publishFactoryArtifact` publishes candidate/packet/evidence bytes once, `readFactoryArtifact(root)` retrieves only a declared root, `recordFactoryAttemptStart` durably charges the allocation before validation or supervision, `publishFactoryAttemptTerminal` requires that start and one readable immutable terminal, and `resumeFactoryAttemptInventory` refuses missing, duplicate, or incomparable coverage. Wire `admitFactory` to publish canonical proposal/validation evidence and return repository roots; callers, including the later calibration runner, must persist every final disposition through this repository. Implement factory admission as data validation plus an injected `LabSupervisedProvider`/`runCanonicalLabMatch` bridge; retain exact success/player-violation/system-failure evidence and preserve failure as unscored evidence per D-03. Do not import, dynamically load, evaluate, or execute candidate source in the coordinator; prohibit fallback result conversion and preserve exact native lane per D-03, D-04, and D-10.</action>
  <verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/ledger.test.ts packages/strategy-lab/src/factory/repository.test.ts packages/strategy-lab/src/factory/admission.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab</automated></verify>
  <acceptance_criteria>All declared dispositions are retained and charged in root-only private storage; duplicate/retry/correction records remain addressable by distinct roots; a supervision failure remains private unscored failure evidence.</acceptance_criteria>
  <done>FACT-06 and FACT-07 have focused failing-path and three-way-semantics coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Export the non-strategic factory API</name>
  <files>packages/strategy-lab/src/index.ts, packages/strategy-lab/package.json</files>
  <read_first>packages/strategy-lab/src/index.ts; packages/strategy-lab/package.json; packages/strategy-lab/src/factory/contracts.ts; packages/strategy-lab/src/factory/admission.ts</read_first>
  <action>Complete the bounded handoff by exporting only data contracts, identity/admission/ledger/repository functions, and types needed by private oracle leaves and the roots-only calibration CLI. Add a narrow factory subpath package export so leaves import only the non-strategic contract surface rather than the root barrel, which may export planner code. Do not export strategic selectors, source execution, production adapters, or mutable stores; this maintains the D-05 and D-18 one-way factory boundary and leaves producer orchestration to the later private CLI.</action>
  <verify><automated>./node_modules/.bin/tsc -b packages/strategy-lab</automated></verify>
  <acceptance_criteria>Leaf packages can compile against the factory packet contract while the factory retains no dependency on a strategic core.</acceptance_criteria>
  <done>The next-wave leaves have a narrow non-strategic interface.</done>
</task>
</tasks>

## Artifacts this phase produces

- `factory/contracts.ts`: private `FactoryOraclePacket`, candidate, proposal, validation-evidence, and attempt schemas.
- `factory/identity.ts`: domain-separated factory roots; `factory/repository.ts`: `FactoryRepository`, `publishFactoryArtifact`, `readFactoryArtifact`, `recordFactoryAttemptStart`, `publishFactoryAttemptTerminal`, and `resumeFactoryAttemptInventory` (private, root-only).
- `factory/ledger.ts` and `factory/admission.ts`: charged terminal validation and `admitFactory`/`LabSupervisedProvider` non-strategic admission seam.
- `strategy-lab/package.json`: narrow private factory subpath export; `strategy-lab/src/index.ts`: no new public or production API surface.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| oracle/intake bytes → factory | Hostile provenance and source enter private admission. |
| factory → supervised runtime | Candidate source must not execute in the coordinator. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-264-01 | Tampering | factory identity/ledger | high | mitigate | Canonical strict schemas, roots, immutable lineage, and terminal-ledger tests. |
| T-264-02 | Elevation | admission bridge | high | mitigate | Data-only admission plus existing supervised provider; no direct source execution. |
| T-264-03 | Repudiation | charged attempts | medium | mitigate | One root-bound terminal disposition per allocation and retry linkage. |
| T-264-SC | Tampering | package installs | high | accept | No package installation is planned. |
</threat_model>

<verification><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/contracts.test.ts packages/strategy-lab/src/factory/identity.test.ts packages/strategy-lab/src/factory/ledger.test.ts packages/strategy-lab/src/factory/repository.test.ts packages/strategy-lab/src/factory/admission.test.ts</automated></verification>
<success_criteria>Private immutable candidate and attempt evidence is available to leaves, production-unreachable, and hostile source has only the existing supervised path.</success_criteria>
<output>Create `.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-01-SUMMARY.md` when done</output>
