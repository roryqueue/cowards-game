---
phase: 264-immutable-factory-independent-oracles-and-quarantined-intake
plan: "06"
type: execute
wave: 2
depends_on: [264-01]
files_modified: [packages/strategy-lab/src/factory/intake.ts, packages/strategy-lab/src/factory/intake.test.ts, packages/strategy-lab/src/factory/intake-protocol.ts, packages/strategy-lab/src/factory/intake-protocol.test.ts]
autonomous: true
requirements: [ORCL-01, ORCL-05]
must_haves:
  truths: ["A separately controlled private intake channel accepts only complete explicit deterministic source and provenance.", "Disclosure/conflict/budget/reviewer details are frozen and rejected work is retained in the common ledger root."]
  artifacts:
    - path: packages/strategy-lab/src/factory/intake-protocol.ts
      provides: strict frozen protocol and disclosure schema
    - path: packages/strategy-lab/src/factory/intake.ts
      provides: quarantined packet admission and ledger linkage
  key_links:
    - from: packages/strategy-lab/src/factory/intake.ts
      to: packages/strategy-lab/src/factory/admission.ts
      via: explicit source packet then common hostile admission
      pattern: admitFactory
  prohibitions:
    - "No default participant/protocol, contact, UI, live agent, direct source execution, or invented human/external provenance."
    - "No holdout/other-candidate/memory/objective/host disclosure, Phase 263 reuse, public/production path, or rules work."
---
<objective>Implement the private human/external quarantine channel. Purpose: make external attack evidence accountable without a web UI, live agent, or disclosure leak. Output: frozen protocol validator, intake admission, and rejection/charge tests.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md
@packages/strategy-lab/src/factory/contracts.ts
@packages/strategy-lab/src/factory/ledger.ts
@packages/strategy-lab/src/factory/admission.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Define the exact frozen intake protocol and disclosure classes</name><files>packages/strategy-lab/src/factory/intake-protocol.ts, packages/strategy-lab/src/factory/intake-protocol.test.ts</files><read_first>.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md; packages/strategy-lab/src/factory/contracts.ts; packages/strategy-lab/src/factory/ledger.ts</read_first><behavior>- A configured protocol requires disclosure class, count/time/reviewer/reuse budgets, independence and conflict declarations, confidentiality/provenance, validation, and acceptance/rejection rules.\n- Omitted participant authorization or disclosure fields cannot become an active protocol.\n- Prohibited data classes are refused before reviewer projection.</behavior><action>Create strict private protocol and reviewer-view schemas per D-21 and D-23. Require an actual frozen protocol to identify allowed disclosure, submission/time limits, reviewer budget/reuse, conflicts, confidentiality, provenance, validation, and disposition policy. There is no default participant/protocol and no actual participant claim; incomplete configuration is an immutable blocked record. Explicitly exclude holdout, other candidate source, memories, objectives, evaluator/host data, credentials, and security internals from all intake/reviewer projections.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/intake-protocol.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab</automated></verify><acceptance_criteria>Only a fully specified, root-bound protocol can authorize intake; reviewer projections are bounded to its disclosure class.</acceptance_criteria><done>ORCL-05 has a fail-closed channel contract.</done></task>
<task type="auto" tdd="true"><name>Task 2: Admit explicit hostile submissions into the common evidence root</name><files>packages/strategy-lab/src/factory/intake.ts, packages/strategy-lab/src/factory/intake.test.ts</files><read_first>packages/strategy-lab/src/factory/intake-protocol.ts; packages/strategy-lab/src/factory/admission.ts; packages/strategy-lab/src/factory/ledger.ts</read_first><behavior>- Explicit deterministic source with complete provenance is forwarded as data to common admission.\n- Advice, prose, opaque binary, live-agent, incomplete provenance, conflict, validation failure, and rejection create retained charged dispositions.\n- Reviewers cannot conceal an unfavorable submission or resubmit outside a declared budget.</behavior><action>Implement intake packet validation and immutable ledger linkage. Admit only explicit deterministic source plus protocol-authorized provenance to the common factory admission seam; never directly run it. Preserve failed/rejected/invalid/conflicted/duplicate/retried packets in the same evidence root with charged resource fields and no gameplay conversion per D-03, D-04, D-22, and D-24. Do not create UI, database persistence, contacts, or automatic reviewer/model actions; production remains unreachable per D-05.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/intake.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab</automated></verify><acceptance_criteria>All rejected attack forms remain private retained evidence and only an exact validated source packet can proceed toward supervised admission.</acceptance_criteria><done>The documented separate intake route is complete without fabricated human/external participation.</done></task>
</tasks>
## Artifacts this phase produces

- `factory/intake-protocol.ts`: private `FrozenIntakeProtocol`, disclosure/reviewer projection schemas, and blocked configuration disposition.
- `factory/intake.ts`: `admitQuarantinedIntakePacket` data-only common-admission/ledger linkage.
- Both outputs retain private protocol and evidence roots only; neither emits a UI, public DTO, or production persistence API.
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| human/external packet → quarantine | Hostile submission and claimed provenance enter. |
| protocol → reviewer projection | Disclosure must remain bounded. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-264-12 | Information disclosure | reviewer projection | high | mitigate | Exact disclosure schema and prohibited-class rejection tests. |
| T-264-13 | Repudiation | intake disposition | high | mitigate | Root-bound protocol, charged terminal ledger, conflict/retry retention. |
| T-264-SC | Tampering | package installs | high | accept | No install action. |
</threat_model>
<verification><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/intake-protocol.test.ts packages/strategy-lab/src/factory/intake.test.ts</automated></verification>
<success_criteria>External attacks have a genuine gated private intake path but no invented participant/provenance or production projection.</success_criteria>
<output>Create `.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-06-SUMMARY.md` when done</output>
