---
phase: 264-immutable-factory-independent-oracles-and-quarantined-intake
plan: "02"
type: execute
wave: 2
depends_on: [264-01]
files_modified:
  - packages/strategy-oracle-tactical/package.json
  - packages/strategy-oracle-tactical/tsconfig.json
  - packages/strategy-oracle-tactical/src/index.ts
  - packages/strategy-oracle-tactical/src/selector.ts
  - packages/strategy-oracle-tactical/src/scoring.ts
  - packages/strategy-oracle-tactical/src/search.ts
  - packages/strategy-oracle-tactical/src/emit.ts
  - packages/strategy-oracle-tactical/src/tactical.test.ts
autonomous: true
requirements: [ORCL-01, ORCL-02]
must_haves:
  truths:
    - "The tactical oracle owns substantive selector, mission/action scoring, search state, response logic, and explicit legal deterministic source emission."
    - "The leaf emits a factory packet as data and has no cross-oracle strategic import."
  artifacts:
    - path: packages/strategy-oracle-tactical/src/selector.ts
      provides: tactical activation/mission selector
    - path: packages/strategy-oracle-tactical/src/emit.ts
      provides: self-contained explicit candidate source and packet
  key_links:
    - from: packages/strategy-oracle-tactical/src/emit.ts
      to: packages/strategy-lab/src/factory/contracts.ts
      via: FactoryOraclePacket data contract
      pattern: FactoryOraclePacket
  prohibitions:
    - "No reuse of a Phase 263 strategic selector/scorer/search core, direct source execution, or cross-oracle strategic sharing."
    - "No production/public registration, league/holdout/formation/rules work, or claim that test packets are empirical evidence."
---
<objective>Build the physically separate structured tactical optimizer leaf. Purpose: furnish a genuine strategic core, not a renamed existing planner. Output: private package with owned tactical selection, scoring, search, and explicit source emission.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md
@packages/strategy-lab/src/planner/emit.ts
@packages/strategy-lab/src/factory/contracts.ts</context>
<tasks>
<task type="auto">
<name>Task 1: Establish the tactical leaf package and owned strategic core</name>
<files>packages/strategy-oracle-tactical/package.json, packages/strategy-oracle-tactical/tsconfig.json, packages/strategy-oracle-tactical/src/index.ts, packages/strategy-oracle-tactical/src/selector.ts, packages/strategy-oracle-tactical/src/scoring.ts, packages/strategy-oracle-tactical/src/search.ts</files>
<read_first>packages/strategy-lab/package.json; packages/strategy-lab/src/planner/assign.ts; packages/strategy-lab/src/planner/brain.ts; packages/spec/src/schemas.ts</read_first>
<action>Create a private composite TypeScript leaf whose own source implements ordered tactical candidate selection, mission score, Action score, bounded search-state expansion, and response generation per D-14 and D-15. Consume only canonical legal input types and allowlisted spec/engine/lab contracts; it must not import an existing planner selector, scorer, search tree, learned parameter, prompt, or response helper per D-18. Use the canonical engine only for rules/legality rather than writing a transition implementation, preserving D-03 and D-19.</action>
<verify><automated>./node_modules/.bin/tsc -b packages/strategy-oracle-tactical</automated></verify>
<acceptance_criteria>The package compiles independently and source ownership makes tactical selector, both scoring layers, search state, and response generation explicit.</acceptance_criteria><done>ORCL-02 has a distinct tactical-core implementation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Emit and prove legal deterministic tactical candidate source</name>
<files>packages/strategy-oracle-tactical/src/emit.ts, packages/strategy-oracle-tactical/src/index.ts, packages/strategy-oracle-tactical/src/tactical.test.ts</files>
<read_first>packages/strategy-lab/src/planner/emit.ts; packages/strategy-lab/src/planner/information-boundary.test.ts; packages/strategy-lab/src/factory/contracts.ts</read_first>
<behavior>- Different legal tactical inputs alter the emitted policy's selections; a constant action source does not satisfy the tests.\n- Hidden/counterfactual changes with identical legal inputs do not alter student choices.\n- Emission yields self-contained deterministic source plus a root-bound factory packet without executing it.</behavior>
<action>Export `emitTacticalFactoryPacket` from `src/emit.ts` and re-export it from the private `src/index.ts`; it accepts the tactical leaf's owned controller and canonical factory request and returns the strict data-only `FactoryOraclePacket` consumed by Plan05 ingestion. In `src/tactical.test.ts`, import this exact named export from the leaf entrypoint, assert it is callable, invoke it with a valid bounded request, and verify its returned canonical source/provenance/packet roots through the factory schema. A wrong export name, mismatched source root, malformed input, or missing model provenance must fail the corresponding assertion before any supervision. Implement an AST/lexical checked emitter that compiles the tactical leaf's owned controller into explicit deterministic source and constructs its FactoryOraclePacket. Reuse only the source-closure and runtime validation pattern, not existing strategic modules. Prove emitted choices depend on legal inputs and exercise distinct selector/scoring/search paths; source is data for factory admission and not executed by this package per D-03, D-14, D-15, D-18, and D-19.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-tactical/src/tactical.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-oracle-tactical</automated></verify>
<acceptance_criteria>Tests demonstrate substantive legal-input tactical variation and packet provenance without claiming synthetic fixtures are actual candidates.</acceptance_criteria><done>The first independent automated mechanism is implementation-complete and ready for factory admission.</done>
</task>
</tasks>
## Artifacts this phase produces

- `strategy-oracle-tactical/src/{selector,scoring,search}.ts`: private owned tactical selector, mission/Action scorers, bounded state, and response logic.
- `strategy-oracle-tactical/src/emit.ts`: exports `emitTacticalFactoryPacket`; data-only `FactoryOraclePacket` producer through the narrow factory-contract subpath.
- `strategy-oracle-tactical/src/index.ts`: private leaf entrypoint; no runtime, web, API, Go, or public export.
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| legal strategy input → tactical source | Source must use only permitted observations. |
| tactical packet → factory | Source remains data until existing supervision. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-264-04 | Tampering | tactical strategic core | high | mitigate | Own selector/scorers/search plus source and static-boundary tests. |
| T-264-05 | Information disclosure | emitted source | high | mitigate | Legal-input regression and no counterfactual data in emitted closure. |
| T-264-SC | Tampering | package installs | high | accept | Workspace-only package files; no installation. |
</threat_model>
<verification><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-tactical/src/tactical.test.ts</automated></verification>
<success_criteria>A private tactical optimizer is materially strategic and physically isolated, with no new engine or source-execution lane.</success_criteria>
<output>Create `.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-02-SUMMARY.md` when done</output>
