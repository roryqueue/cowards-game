---
phase: 264-immutable-factory-independent-oracles-and-quarantined-intake
plan: "05"
type: execute
wave: 3
depends_on: [264-01, 264-02, 264-03, 264-04, 264-06]
files_modified: [packages/strategy-lab/src/factory/supervision-artifacts.ts, packages/strategy-lab/src/factory/supervision-artifacts.test.ts, packages/strategy-lab/src/factory/fingerprint.ts, packages/strategy-lab/src/factory/fingerprint.test.ts, packages/strategy-lab/src/factory/admission.ts, packages/strategy-lab/src/factory/admission.test.ts, packages/strategy-lab/src/factory/calibration.ts, packages/strategy-lab/src/factory/calibration.test.ts, scripts/check-v1-38-factory-boundaries.ts, scripts/check-v1-38-factory-boundaries.test.ts, scripts/check-v1-38-lab-boundaries.ts, scripts/check-v1-38-lab-boundaries.test.ts, scripts/ingest-v1-38-factory-packet.ts, scripts/ingest-v1-38-factory-packet.test.ts, scripts/prepare-v1-38-factory-calibration.ts, scripts/run-v1-38-factory-calibration.ts, scripts/lib/v1-38-factory-supervised-runtime.ts, scripts/lib/v1-38-factory-supervised-runtime.test.ts, packages/strategy-lab/src/factory/contracts.ts, packages/strategy-lab/src/factory/contracts.test.ts, packages/strategy-lab/src/factory/ledger.ts, packages/strategy-lab/src/factory/ledger.test.ts, packages/strategy-lab/src/factory/packet.ts, packages/strategy-lab/src/factory/index.ts, packages/strategy-lab/src/factory/intake.ts, packages/strategy-lab/src/factory/intake.test.ts, packages/strategy-lab/package.json, packages/strategy-oracle-tactical/src/emit.ts, packages/strategy-oracle-teacher/src/emit.ts, packages/strategy-oracle-model/src/emit.ts]
autonomous: true
requirements: [FACT-08, ORCL-06, ORCL-07]
must_haves:
  truths: ["Every candidate has frozen source-structure, lineage, dependency, legal-input decision, Chronicle-behavior, and matchup-response fingerprints.", "Borderline clone/correlation/independence is unresolved and quarantined.", "Static boundary checks reject shared strategic cores and private-factory production reachability."]
  artifacts:
    - path: packages/strategy-lab/src/factory/fingerprint.ts
      provides: six-dimension fingerprint and independence receipt
    - path: packages/strategy-lab/src/factory/calibration.ts
      provides: development-only corpus and threshold-free calibration report
    - path: scripts/prepare-v1-38-factory-calibration.ts
      provides: fail-closed fresh calibration manifest preparation
    - path: scripts/ingest-v1-38-factory-packet.ts
      provides: private named-producer packet validation and one-time FactoryRepository publication
    - path: scripts/run-v1-38-factory-calibration.ts
      provides: bounded manifest-driven calibration runner
  key_links:
    - from: scripts/prepare-v1-38-factory-calibration.ts
      to: packages/strategy-lab/src/factory/calibration.ts
      via: validated policy roots and development-only corpus
      pattern: calibration
  prohibitions:
    - "No arbitrary caller-supplied fingerprint root, fixture, label, or hash is empirical independence evidence."
    - "No live provider call, direct source execution, Phase 263 reuse, holdout/league/formation/rules work, or public/production reachability."
---
<objective>Implement clone/correlation and independence evidence, including a prepared but unrun bounded development-calibration path. Purpose: prevent names, hashes, or package facades from counting as diversity. Output: six derived fingerprints, quarantine result, static monitor, and roots-only calibration producer runner.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-RESEARCH.md
@.planning/artifacts/v1.38-pre-search-study-policy.json
@.planning/artifacts/v1.38-pre-search-measurement-policy.json
@scripts/check-v1-38-lab-boundaries.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Compute and root all six independence dimensions</name><files>packages/strategy-lab/src/factory/fingerprint.ts, packages/strategy-lab/src/factory/fingerprint.test.ts</files><read_first>packages/strategy-lab/src/factory/contracts.ts; packages/strategy-lab/src/factory/repository.ts; packages/strategy-lab/src/runtime-bridge.ts; packages/strategy-lab/src/planner/information-boundary.test.ts</read_first><behavior>- Cosmetic source rewrite or doctrine label alone cannot pass clone review.\n- Source-structure, lineage, dependency, legal-decision, Chronicle-behavior, and matchup-response roots are derived from canonical immutable inputs, never accepted as a caller assertion.\n- A supplied dimension root that disagrees with the rederived root, missing trace, or borderline comparison returns unresolved/quarantined.</behavior><action>Implement canonical, domain-separated fingerprint algorithms and an independence receipt requiring all six D-11 dimensions plus dependency, authorship, counterfactual-correlation, clone, and failure-mode evidence from D-19. Derive `sourceStructureRoot` from canonical parsed source/normalized AST structure including semantic identifiers and control flow; `lineageRoot` from the ordered immutable candidate/parent DAG; `dependencyRoot` from the recursively resolved locked import/package dependency graph; `legalDecisionRoot` from the canonical legal-observation/action projection of selected supervision records; `chronicleBehaviorRoot` from the redacted canonical Chronicle event/behavior projection; and `matchupResponseRoot` from predeclared paired-matchup response/correlation projections. Require the packet/proposal root, validation-evidence root, and accepted-candidate root as distinct stages so candidate identity cannot depend circularly on its own fingerprint. Recompute all six roots from repository-read immutable inputs and reject caller-supplied mismatches. Feed decision/behavior/matchup dimensions only from admitted supervision records, never raw objective, memory, host, or private opponent data. Test label and cosmetic-source failures, source/trace/root mismatch rejection, absent traces, and unresolved quarantine; mark absence, conflict, or materiality ambiguity `unresolved` rather than count it per D-13.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/fingerprint.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab</automated></verify><acceptance_criteria>All six derived dimensions and supporting receipts are mandatory; labels, hashes, caller roots, and cosmetic rewrites do not satisfy independence.</acceptance_criteria><done>FACT-08 and ORCL-07 evidence has strict executable representation.</done></task>
<task type="auto" tdd="true"><name>Task 2: Ingest named producer packets, then build the development-only calibration runner</name><files>packages/strategy-lab/src/factory/calibration.ts, packages/strategy-lab/src/factory/calibration.test.ts, scripts/ingest-v1-38-factory-packet.ts, scripts/ingest-v1-38-factory-packet.test.ts, scripts/prepare-v1-38-factory-calibration.ts, scripts/run-v1-38-factory-calibration.ts</files><read_first>packages/strategy-lab/src/factory/fingerprint.ts; packages/strategy-lab/src/factory/repository.ts; packages/strategy-lab/src/factory/admission.ts; packages/strategy-oracle-tactical/src/emit.ts; packages/strategy-oracle-teacher/src/emit.ts; packages/strategy-oracle-model/src/emit.ts; packages/strategy-lab/src/factory/intake.ts; .planning/artifacts/v1.38-pre-search-study-policy.json; .planning/artifacts/v1.38-pre-search-measurement-policy.json; .planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-TIMING-CALIBRATION.md</read_first><behavior>- Corpus includes semantic rewrites, shared-selector variants, symmetry/opaque-ID variants, near-identical behavior, latent divergence, and expected false positives.\n- `ingest-v1-38-factory-packet.ts` accepts only the exact named producer APIs `emitTacticalFactoryPacket`, `emitTeacherFactoryPacket`, `emitModelFactoryPacket`, or `admitQuarantinedIntakePacket`; it verifies producer identity, origin, evidence class, packet root, source root, native lane, runtime profile, and source-root linkage before one non-overwriting `publishFactoryArtifact` call.\n- Tactical/teacher packets are materialized through their real leaf emitters; model packets pass only the frozen-bundle validator/emitter; human/external packets pass only the quarantine validator; no provider/model call or generated-source execution occurs.\n- Preparation validates current policy roots and requires a newly supplied allocation/protocol root plus packet roots returned by ingestion; it does not reuse a Phase263 selector, run, or budget.\n- The runner records a start before packet validation, calls admission and selected supervision, then retains every terminal disposition and a roots-only readiness input; synthetic fixtures and manually preseeded/arbitrary roots are rejected as empirical evidence.\n- Numeric thresholds remain absent until a newly authorized calibration result is frozen.</behavior><action>Create the labeled development-only calibration corpus and threshold-free reporting algorithm required by D-12; fixture cases prove mechanics only and must be labelled mechanics rather than candidates, model evidence, human provenance, or independence. Add the private outer-orchestrator ingestion script (outside `packages/strategy-lab`) that dispatches only the exact named functions from Plans 02/03/04/06, validates the declared `producerIdentity`, `origin`, `evidenceClass`, `packetRoot`, `sourceRoot`, `nativeLane`, `runtimeProfile`, and source-root linkage against the data-only packet, records a blocked absent-channel disposition when a model or human/external channel is unavailable, and publishes exactly once through `FactoryRepository`. It must reject arbitrary manually preseeded roots, fixture roots marked empirical, missing or mismatched provenance, and duplicate publication; it never imports factory code into an oracle, calls a provider/source, or executes `GENERATED` source. Define one private roots-only `FactoryCalibrationManifest` containing current policy roots, the new finite allocation/protocol root, selected supervision identity, and the packet roots returned by ingestion with their exact origin/evidence class. The prepare CLI accepts only those repository-published roots and emits a new content-addressed manifest only when Plan 07 supplies exact authorization. The runner opens that repository, records each `recordFactoryAttemptStart` before packet validation, obtains proposal then validation evidence, invokes only `admitFactory` and injected selected supervision, derives six dimensions from returned immutable records, publishes the accepted immutable candidate only after that evidence, persists every success/player-violation/system-failure/invalid/rejected/duplicate/weak/retried/unresolved terminal, and writes a roots-only readiness input/report. It retains blocked producer dispositions when Plan 07 facts are absent and labels injected tests as mechanics, not empirical evidence. Neither command may modify gameplay, open holdout, use historic selectors, or create an unapproved allocation.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 scripts/ingest-v1-38-factory-packet.test.ts packages/strategy-lab/src/factory/calibration.test.ts packages/strategy-lab/src/factory/repository.test.ts &amp;&amp; ./node_modules/.bin/tsx scripts/ingest-v1-38-factory-packet.ts --help &amp;&amp; ./node_modules/.bin/tsx scripts/prepare-v1-38-factory-calibration.ts --help &amp;&amp; ./node_modules/.bin/tsx scripts/run-v1-38-factory-calibration.ts --help</automated></verify><acceptance_criteria>A real tactical or teacher emitter packet (and validator-gated model/intake packet) is ingested, published once, read by preparation, and reaches injected supervision, derived fingerprints, and readiness mechanics; arbitrary preseeded roots and fixture evidence are rejected and every blocked/failure disposition remains retained.</acceptance_criteria><done>The prospective calibration path has a bounded real-input ingestion boundary, is callable, and remains fail-closed.</done></task>
<task type="auto" tdd="true"><name>Task 3: Extend the recursive private-factory boundary monitor</name><files>scripts/check-v1-38-factory-boundaries.ts, scripts/check-v1-38-factory-boundaries.test.ts</files><read_first>scripts/check-v1-38-lab-boundaries.ts; scripts/check-v1-38-lab-boundaries.test.ts; packages/strategy-lab/package.json</read_first><behavior>- Production, web, API, Go, deployment, and public/generated roots cannot transitively reach factory/oracle packages.\n- Oracles cannot directly, through a re-export/barrel, package-manifest dependency, or dynamic loader reach another strategic core or shared selector/scorer/tree/parameter/prompt implementation.\n- Factory can consume the narrow packet-contract subpath but cannot import strategic leaf code.</behavior><action>Extend the existing AST/import graph monitor for `strategy-lab` factory and the three oracle leaf package roots. Recursively resolve static imports, export-from/barrel re-exports, package-manifest workspace/dependency edges, and every dynamic `import()`/`require`/loader expression; inspect the closure from each oracle root and every production, web, API, Go, deployment, public/default, and generated entrypoint. Maintain the D-18 allowlist only for literal legality/geometry, strict schemas, canonical identity/artifact creation, supervised adapters, reporting, and the narrow factory contract subpath; fail any route to another oracle strategic core or shared selector/scorer/tree/parameter/prompt/response implementation, factory-to-oracle reachability, direct hostile execution construct, or production/public reachability. Add injected graph tests for direct, transitive, barrel, dynamic, package-manifest, and production-root denials plus one allowed non-strategic contract path; this monitor is evidence of the declared graph, not sandbox certification.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 scripts/check-v1-38-factory-boundaries.test.ts &amp;&amp; ./node_modules/.bin/tsx scripts/check-v1-38-factory-boundaries.ts</automated></verify><acceptance_criteria>Audited recursive separation detects forbidden strategic sharing while the narrow non-strategic contract path remains allowed and all new private packages remain production-unreachable.</acceptance_criteria><done>ORCL-06 has an executable recursive import/deployment boundary audit.</done></task>
</tasks>
## Artifacts this phase produces

### Same-plan private integration seams — 2026-09-14

Read-only preparation identified three necessary implementation seams, not new product or execution authority:

- Task2 also retains complete issued supervision through `factory/supervision-artifacts.ts` and its focused tests. Canonical JSONL streams across ordered byte chunks capped at262144bytes; per-record canonical limits remain unchanged. Reopen verifies all receipt/execution/trace commitments under explicit read bounds and returns data only, never renewed execution authority. This prevents long-run evidence from being reduced to unrecoverable hashes or oversized aggregate artifacts.

- Tasks1/2 may also update `factory/contracts.ts`, `factory/ledger.ts` and their focused tests to represent an explicit retained `unresolved` disposition. Final publication must retain the issued derivation receipt and candidate together through a rooted descriptor; a valid private candidate remains distinct from an affirmative independence or counted-league claim. No circular candidate-root dependency is introduced.
- Task3 owns a narrow `factory/packet.ts` entry, the private `strategy-lab/package.json` subpath export, and only the corresponding factory-import lines in the three oracle `emit.ts` files. The current broad factory barrel reaches admission/runtime code. Route emitters through the packet schema/type/identity-only seam, without changing strategic controller bytes or broadly allowing factory internals. These additional files are part of Plan05's existing boundary task.
- Tasks1/2 may make a minimal tested intake reader integration change if mixed producer/calibration records in the common repository expose a budget-scope mismatch. Preserve protocol-specific charging, every retained record, all incomplete-accounting refusals and the non-authorizing `blocked_configuration` union. Never solve integration by deleting evidence or silently treating unknown usage as zero.

Plan07's Markdown decision may contain one exact machine-readable canonical JSON record; preparation must parse and validate it without treating prose or historical policy-wrapper flags as fresh authorization. No such record is created until the real decision supplies its facts and finite bounds. Factory packet `nativeLane.providerId` identifies the packet producer and is cross-bound to `packet.provider`; selected runtime adapter identity is separately checked on the validated StrategyRevision (`runtime-js-container-subprocess`, authored TypeScript, exact ABI/image/limits). Do not conflate oracle/model provider identity with container adapter identity and thereby block all real producer packets.

These file-list additions implement the existing three tasks. They do not add a numbered plan, dependency, experiment, allocation, install, public surface or human checkpoint. Main owns this integration clarification and lifecycle tracking.

### Same-plan runtime integration clarification — 2026-09-13

Read-only adapter inspection found that the existing selected provider is `createPlannerSupervisedRuntime` in `scripts/lib/v1-38-planner-supervised-runtime.ts`, called through `runCanonicalLabMatch` and the factory's issued-admission `superviseFactory` seam. It supports authored TypeScript revisions, the admitted JavaScript container subprocess, the existing v1.19 ABI and exact resource/image bounds. Other language labels allowed by the generic packet schema are not proof of an available native provider.

Task 2 therefore also owns `scripts/lib/v1-38-factory-supervised-runtime.ts` and its focused test. Build the missing packet/source-to-validated-StrategyRevision adapter, retain the selected supervisor and wrap its identity with exact factory packet/proposal/validation/native-lane roots. Only the actually supported TypeScript native lane may proceed; unsupported lanes remain explicitly blocked, never silently translated or assigned invented adapters. Tests use injected providers and must exercise identity mismatches, unsupported lanes, fresh budget binding and cleanup without launching any guest. Do not invoke or reuse Phase 263 prepare/run selectors, receipts or allocations. This is implementation of the existing selected-supervision requirement, not new runtime, execution or resource authority.

Task 3 may extend `scripts/check-v1-38-lab-boundaries.ts` and its tests to reuse the existing AST resolver and production/Docker checks rather than duplicate or replace them with a regex facade. Add path-scoped oracle dependency policy, manifest/entrypoint edges and fail-closed unresolved private loader handling; expose only the needed non-strategic scanner seam for the factory wrapper. Preserve all existing boundary regressions, including Docker stage-copy and ignore re-inclusion checks. Permit TypeScript AST tooling only at explicitly reviewed emitter/fingerprint paths; do not broadly permit arbitrary private dependencies or strategic sharing. The historical Phase 263 receipt validator is unrelated and must remain unchanged.

Tasks 1 and 2 also integrate `factory/admission.ts` and its tests: final candidate publication must require the issued, six-dimension derivation receipt bound to the exact proposal, validation and supervision receipt. Matching-looking caller fingerprint roots must not bypass derivation through the generic finalizer. The adapter captures canonical legal request projections and associates them with issued invocation/input roots before private publication; the existing runtime accounting alone stores input hashes, not those legal inputs. Strip memories, objectives and host/evaluator details from fingerprint projections. Retain the existing non-circular packet → proposal → validation → supervised evidence → fingerprints → candidate order and all source/participant/result-attribution regressions.

- `factory/fingerprint.ts`: `deriveFactoryFingerprints`, six named derived roots, and `FactoryIndependenceReceipt` from repository-read private evidence.
- `factory/calibration.ts`: development-corpus classifier, `FactoryCalibrationManifest`, threshold-free report, and unresolved quarantine result.
- `scripts/ingest-v1-38-factory-packet.ts` and `.test.ts`: private named-producer ingestion/publication operation and real-emitter-to-repository mechanics test.
- `scripts/prepare-v1-38-factory-calibration.ts`: private `prepare` command requiring policy, allocation/protocol, producer-packet, and repository roots; it rejects fixture/arbitrary-root empirical flags.
- `scripts/run-v1-38-factory-calibration.ts`: private `run` command consuming only a prepared manifest and emitting roots-only readiness input/report; no provider or source-execution flag.
- `scripts/check-v1-38-factory-boundaries.ts`: recursive static/re-export/manifest/dynamic-loader boundary report with production-root denial status.
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| supervision traces → fingerprints | Private evidence is summarized into roots. |
| package graph → production | Strategic packages must remain non-production leaves. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-264-10 | Tampering | clone evidence | high | mitigate | Six rooted dimensions, unresolved quarantine, development-only calibration. |
| T-264-11 | Elevation | package graph | high | mitigate | AST graph monitor and deny-path tests. |
| T-264-SC | Tampering | package installs | high | accept | No install action. |
</threat_model>
<verification><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/factory/fingerprint.test.ts packages/strategy-lab/src/factory/calibration.test.ts scripts/check-v1-38-factory-boundaries.test.ts</automated></verification>
<success_criteria>Only complete multi-axis evidence can advance an independence claim, and a fresh calibration can be prepared without spending an old allocation.</success_criteria>
<output>Create `.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-05-SUMMARY.md` when done</output>
