---
phase: 264-immutable-factory-independent-oracles-and-quarantined-intake
plan: "07"
type: execute
wave: 4
depends_on: [264-05, 264-06]
files_modified: [packages/strategy-oracle-model/src/bundle.ts, packages/strategy-oracle-model/src/emit.ts, packages/strategy-oracle-model/src/model.test.ts, packages/strategy-lab/src/factory/contracts.ts, packages/strategy-lab/src/factory/contracts.test.ts, scripts/ingest-v1-38-factory-packet.ts, scripts/ingest-v1-38-factory-packet.test.ts, scripts/prepare-v1-38-factory-calibration.ts, scripts/prepare-v1-38-factory-calibration.test.ts, scripts/author-v1-38-factory-model-source.ts, scripts/author-v1-38-factory-model-source.test.ts]
autonomous: true
requirements: [FACT-05, FACT-06, FACT-07, ORCL-04, ORCL-06, ORCL-07]
must_haves:
  truths:
    - "Before authoring output, a private root fixes authoring attempts A-01 through A-04 including corrections/30 minutes, a 50,000 input-plus-output-token accounting ceiling per attempt and 200,000 total, 48 workloads/90 minutes, all source slots, and human/external zero."
    - "A captured model result binds request/response, requested/reported model IDs, client version/settings, actual usage, emitted source, and explicit unavailable serving snapshot without inventing a model revision."
    - "The isolated authoring command receives only a disclosed rules/ABI packet, never executes output or reads private core/holdout/history data."
  artifacts:
    - path: scripts/author-v1-38-factory-model-source.ts
      provides: pre-authoring allocation validation and isolated JSON capture
    - path: packages/strategy-oracle-model/src/bundle.ts
      provides: successor frozen provenance schema
    - path: scripts/prepare-v1-38-factory-calibration.ts
      provides: complete 48-cell source/workload manifest validation
  key_links:
    - from: scripts/author-v1-38-factory-model-source.ts
      to: packages/strategy-oracle-model/src/bundle.ts
      via: canonical private JSON capture
      pattern: FrozenModelBundle
    - from: scripts/prepare-v1-38-factory-calibration.ts
      to: scripts/run-v1-38-factory-calibration.ts
      via: immutable manifest root
      pattern: factory-calibration-manifest
  prohibitions:
    - "No generation, probe, emitted-source execution, guest intake, Match, or workload is run by this plan."
    - "No production/counted/public/formation/holdout/rules output or consumed Phase 262/263 selector is used."
---

<objective>
Implement the approved fresh-model route without consuming it. Freeze the actual source/workload allocation before output and make model provenance truthful about an undisclosed serving snapshot.

Purpose: satisfy D-17/D-20 while retaining the hostile-source and private-factory boundaries.
Output: tested source-only authoring, ingestion, and manifest code ready for independent source review.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-READINESS-DECISION.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-FRESH-ROUTE-RESEARCH.md
@packages/strategy-oracle-model/src/bundle.ts
@packages/strategy-lab/src/factory/calibration.ts
@scripts/ingest-v1-38-factory-packet.ts
@scripts/prepare-v1-38-factory-calibration.ts
</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add immutable successor model provenance</name>
<files>packages/strategy-oracle-model/src/bundle.ts, packages/strategy-oracle-model/src/emit.ts, packages/strategy-oracle-model/src/model.test.ts, packages/strategy-lab/src/factory/contracts.ts, packages/strategy-lab/src/factory/contracts.test.ts, scripts/ingest-v1-38-factory-packet.ts, scripts/ingest-v1-38-factory-packet.test.ts</files>
<read_first>packages/strategy-oracle-model/src/bundle.ts; packages/strategy-oracle-model/src/emit.ts; packages/strategy-oracle-model/src/model.test.ts; packages/strategy-lab/src/factory/contracts.ts; scripts/ingest-v1-38-factory-packet.ts</read_first>
<behavior>- A successor bundle/packet roots exact request and response records, requested/reported model IDs, client version/settings, actual returned usage, source, and `servingSnapshot.availability: unavailable`.
- Missing provenance, drift, a fabricated snapshot, or a client version offered as a model revision is rejected as a new retained blocked attempt.
- Existing v1 bundles reload under their original semantics and no mutable latest alias selects the successor.</behavior>
<action>Write RED tests, then implement a versioned successor bundle/packet and root domain per D-02, D-07, D-17, D-20, and ORCL-04. Re-admit/rederive the exact companion during factory ingestion and reload. Require the explicit approved `unavailable` serving-snapshot state; do not equate client version with a serving snapshot or silently replace any identity. Preserve original v1 records untouched. Keep output as inert data: no model call and no source execution occurs in this task, per D-03 and D-05.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-model/src/model.test.ts packages/strategy-lab/src/factory/contracts.test.ts scripts/ingest-v1-38-factory-packet.test.ts</automated></verify>
<acceptance_criteria>Only an exact private provenance companion can reload a successor packet, and unavailable snapshot disclosure never becomes invented identity.</acceptance_criteria>
<done>Model source has a truthful immutable provenance path.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Freeze exact authoring and workload allocation before output</name>
<files>scripts/author-v1-38-factory-model-source.ts, scripts/author-v1-38-factory-model-source.test.ts, scripts/prepare-v1-38-factory-calibration.ts, scripts/prepare-v1-38-factory-calibration.test.ts</files>
<read_first>scripts/prepare-v1-38-factory-calibration.ts; scripts/ingest-v1-38-factory-packet.ts; packages/strategy-lab/src/factory/calibration.ts; packages/strategy-lab/src/factory/ledger.ts</read_first>
<behavior>- Authorization fixes exactly A-01..A-04, corrections included, in one 30-minute window, with a 50,000-token input-plus-output accounting ceiling for each charged attempt and 200,000 total. Actual usage records input, output, cached-input, and total fields; a missing/over-ceiling record is a charged terminal stop with no replacement attempt. This is an experiment accounting ceiling, not a provider hard cap. Requested provider token-cap availability is separately `unavailable` when the authenticated CLI does not expose it.
- It freezes this complete twelve-slot AST transformation graph before output. S01 is one exact tactical base emission; S02 is S01 comment-only rewrite; S03 is one exact teacher base from the frozen existing-legal-spine training record set at maximum depth 3 and 128 nodes total; S04 is S03 with retained same-selector source plus a pure delegating wrapper; S05 is M-BASE from the first valid A slot; S06 is S05 with reversible opaque Soldier-ID/geometry mapping and exact inverse before the entrypoint, an identity-roundtrip control explicitly limited to this purpose; S07 is S01 with only `soldierBrain` result action changed to TURN_TO_STONE when canonical ABI predicate `input.self.position !== null && input.self.position.x === 2`, requiring a schema-admitted reachable x=2 witness and an observed non-STONE-to-STONE trace difference; S08 is S01 with every `soldierBrain` action TURN_TO_STONE; S09 is S03 comment-only rewrite; S10 is S03 x=2 mutation; S11 is S05 with a guard constant 0 delegating wrapper; S12 is the same wrapper with guard constant 1 returning TURN_TO_STONE for every action. Every transform preserves `selectActivations` and result memory unless an explicit wrapper roundtrip is required, uses frozen AST-safe rewrite recipes without imports/evaluation, and strictly validates its output. Each control retains true base origin and `calibration_only`; none becomes a fake model producer, candidate, or independent mechanism. A-02..A-04 are correction slots only and may remain unused.
- It derives 48 roots: each slot in Block A `arena:smoke:v1`, candidate bottom, and Block B `arena:standard-cross:v1`, candidate top, both with seed `factory-264-control-v1`, both candidate/opponent initiatives, `maxPhases: 1`, and the existing runtime invocation/lifetime limits unchanged. It declares this a two-geometry/side-confounded pilot, never a factorial or competitive result.</behavior>
<action>After RED tests, add canonical allocation/authorization validation per D-01, D-09, D-12, D-14 through D-18, D-21, and D-24. Bind the fixed token accounting ceiling, A-01..A-04/30 minutes, exactly one tactical emission S01, exactly one teacher search/distillation S03 (depth 3/128 nodes total), up to four model attempts for S05, nine control materializations S02/S04/S06..S12, 48 workloads/90 minutes, `maxInvocations: 256`, `maxLifetimeMs: 120000`, existing 1s per-method runtime limit unchanged, zero replay-review allocation, and intake zero. Encode the S01..S12 recipes and roots before output, materialize roots after transformations, and permit deliberately correlated controls. The six fit edges are S01/S02, S03/S04, S05/S06 (positive); S01/S07 (near); S01/S08 (latent); S11/S12 (false-positive high-structure/behavior-conflict). S09/S10 are secondary checks only. Retain concrete source witnesses and these trace predicates: positives have matched decisions; S07 differs on reachable x=2 legal input; S08 observes non-stone→stone; S11/S12 prove guard-only source difference plus real behavior contrast. A failed expectation is unresolved, never relabeled. Predeclare actual base edges S01/S03, S01/S05, S03/S05 separately from fit. Charge every attempt/workload terminal and stop on missing/over-ceiling usage or system failure. Implement the source-only command builder in a newly created disclosed-packet-only directory with sanitized environment/cwd and denied repository/core/holdout/history paths. Require read-only-help/documented tool-disabled/no-read capability; otherwise emit `authoring_context_capability_unavailable` and launch nothing. Retain private event/identity/usage/source roots. Do not invoke it.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 scripts/author-v1-38-factory-model-source.test.ts scripts/prepare-v1-38-factory-calibration.test.ts &amp;&amp; ./node_modules/.bin/tsc --ignoreConfig --types node --module nodenext --moduleResolution nodenext --target es2022 --noEmit scripts/author-v1-38-factory-model-source.ts scripts/prepare-v1-38-factory-calibration.ts</automated></verify>
<acceptance_criteria>Only a complete rooted allocation exposes an author command; it contains the exact case mapping/48 cells before source output.</acceptance_criteria>
<done>The approved route is source-ready and no resource is consumed.</done>
</task>
<task type="auto">
<name>Task 3: Stop at independent-source-review handoff</name>
<files>packages/strategy-oracle-model/src/bundle.ts, packages/strategy-oracle-model/src/emit.ts, packages/strategy-lab/src/factory/contracts.ts, scripts/author-v1-38-factory-model-source.ts, scripts/ingest-v1-38-factory-packet.ts, scripts/prepare-v1-38-factory-calibration.ts</files>
<read_first>scripts/check-v1-38-factory-boundaries.ts; packages/strategy-lab/src/factory/fingerprint.ts; scripts/run-v1-38-factory-calibration.ts</read_first>
<action>Run focused pure/injected tests, standalone TypeScript checks, and the private boundary monitor. Inspect D-03, D-05, D-17 through D-20, and D-23: output is inert, no prohibited input/import exists, and no private prompt/source payload reaches projections. End at a source-ready handoff to independent source review; this is not a human checkpoint and it must not issue a model command, run a workload, or claim independence/readiness.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-model/src/model.test.ts packages/strategy-lab/src/factory/contracts.test.ts scripts/author-v1-38-factory-model-source.test.ts scripts/ingest-v1-38-factory-packet.test.ts scripts/prepare-v1-38-factory-calibration.test.ts &amp;&amp; ./node_modules/.bin/tsx scripts/check-v1-38-factory-boundaries.ts</automated></verify>
<acceptance_criteria>Source checks pass and the executor reports the exact review handoff without empirical action.</acceptance_criteria>
<done>Only independent source review can release the bounded empirical path.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| disclosed ABI packet → authoring process | Generated response must not obtain repository, core, holdout, or host access. |
| authoring event stream → frozen bundle | Provenance, usage, or model identity can be omitted, forged, or drifted. |
| frozen bundle → factory packet | Data admission must not become execution or production registration. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-264-14 | Spoofing | model identity/snapshot | high | mitigate | Versioned requested/reported/client/usage binding and explicit unavailable snapshot. |
| T-264-15 | Information disclosure | prompt/context | high | mitigate | Generate disclosed ABI packet only and retain private event records. |
| T-264-16 | Tampering | allocation manifest | high | mitigate | Canonical root binds slots/cases/cells/windows before authoring. |
| T-264-17 | Elevation | emitted source | critical | mitigate | Capture data only; later execution uses existing supervised runtime. |
| T-264-SC | Tampering | package installs | low | accept | No install action. |
</threat_model>
<verification>Run named focused tests, TypeScript 6 standalone checks, and the factory boundary monitor only. Do not run generation, calibration, Match, broad Turbo, installation, or historical selectors.</verification>
<success_criteria>Exact allocation and honest provenance are source-ready, independent review gates consumption, and all runtime/rules/privacy/formation/holdout/public bounds remain unchanged.</success_criteria>
<output>Create `.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-07-SUMMARY.md` when done.</output>
