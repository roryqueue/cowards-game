---
phase: 264-immutable-factory-independent-oracles-and-quarantined-intake
plan: "04"
type: execute
wave: 2
depends_on: [264-01]
files_modified: [packages/strategy-oracle-model/package.json, packages/strategy-oracle-model/tsconfig.json, packages/strategy-oracle-model/src/index.ts, packages/strategy-oracle-model/src/bundle.ts, packages/strategy-oracle-model/src/emit.ts, packages/strategy-oracle-model/src/model.test.ts]
autonomous: true
requirements: [ORCL-01, ORCL-04]
must_haves:
  truths: ["A model channel admits exact immutable request-response bundles and explicit source only.", "Unavailable or drifting provider identity creates a charged block, never substitution or live runner invocation."]
  artifacts:
    - path: packages/strategy-oracle-model/src/bundle.ts
      provides: provider-neutral frozen-bundle admission
    - path: packages/strategy-oracle-model/src/emit.ts
      provides: explicit source packet conversion
  key_links:
    - from: packages/strategy-oracle-model/src/emit.ts
      to: packages/strategy-lab/src/factory/contracts.ts
      via: rooted data packet
      pattern: FactoryOraclePacket
  prohibitions:
    - "No provider SDK, network/credential use, live model invocation, direct source execution, or provider identity substitution."
    - "No Phase 263 reuse, public/production projection, or fixture represented as genuine provider evidence."
---
<objective>Build the provider-neutral frozen-bundle program-synthesis leaf. Purpose: preserve actual model provenance while keeping live calls outside Match/search execution. Output: strict bundle validator, exact-drift block, and explicit source emitter.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-AI-SPEC.md
@packages/strategy-lab/src/factory/contracts.ts
@packages/strategy-lab/src/runtime-bridge.ts</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Define exact frozen model bundles and drift disposition</name><files>packages/strategy-oracle-model/package.json, packages/strategy-oracle-model/tsconfig.json, packages/strategy-oracle-model/src/index.ts, packages/strategy-oracle-model/src/bundle.ts, packages/strategy-oracle-model/src/model.test.ts</files><read_first>packages/strategy-lab/src/contracts.ts; packages/strategy-lab/src/factory/contracts.ts; packages/spec/src/canonical-json.ts</read_first><behavior>- A bundle binds provider/model/version/settings, request/prompt/context bytes or roots, token/resource accounting, response bytes, attempt/budget root, and source candidate.\n- Missing accounting, unavailable identity, or any identity drift returns a charged terminal block with a new root.\n- Parsing a bundle cannot invoke a provider.</behavior><action>Create the private model leaf and strict canonical frozen bundle schema/identity constructors per D-02, D-07, D-17, and D-20. Require exact provider/model/version/settings/prompt/context/token/request/response/attempt provenance; model producer interaction is an externally supplied immutable input, so no network client, provider SDK, environment credential, or live invocation is introduced. Make unavailable or drifted identity an immutable charged block/new attempt under D-04 and D-20.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-model/src/model.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-oracle-model</automated></verify><acceptance_criteria>Bundle validation is fail-closed, root-stable, and records unavailability/drift without invented provider metadata or retry allocation.</acceptance_criteria><done>ORCL-04 has provenance infrastructure without a live provider call.</done></task>
<task type="auto"><name>Task 2: Convert a validated bundle into explicit hostile source data</name><files>packages/strategy-oracle-model/src/emit.ts, packages/strategy-oracle-model/src/index.ts, packages/strategy-oracle-model/src/model.test.ts</files><read_first>packages/strategy-oracle-model/src/bundle.ts; packages/strategy-lab/src/factory/admission.ts; packages/strategy-lab/src/factory/contracts.ts</read_first><action>Export `emitModelFactoryPacket` from `src/emit.ts` and re-export it from the private `src/index.ts`; it accepts only a validated FrozenModelBundle and canonical factory request and returns the strict data-only `FactoryOraclePacket` consumed by Plan05 ingestion. In `src/model.test.ts`, import this exact named export from the leaf entrypoint, assert it is callable, invoke it with a valid bounded request, and verify its returned canonical source/provenance/packet roots through the factory schema. A wrong export name, mismatched source root, malformed input, or missing model provenance must fail the corresponding assertion before any supervision. Implement a data-only conversion from an admitted bundle's exact explicit source to FactoryOraclePacket, preserving native language/provider/runtime lane and all bundle roots. Reject opaque binaries, advice, non-source responses, and source that fails the existing factory closure/validation contract. Do not execute, dynamically import, or score generated source; only later factory admission may use existing supervised execution per D-03, D-10, and D-17.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-model/src/model.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-oracle-model</automated></verify><acceptance_criteria>Only explicit validated source can reach the common packet API, and no code path can substitute a provider or invoke a model from a runner.</acceptance_criteria><done>The third automated mechanism is frozen-bundle based and private.</done></task>
</tasks>
## Artifacts this phase produces

- `strategy-oracle-model/src/bundle.ts`: private `FrozenModelBundle` schema, root constructor, and charged unavailable/drift disposition.
- `strategy-oracle-model/src/emit.ts`: exports `emitModelFactoryPacket`; validated explicit-source-to-`FactoryOraclePacket` data conversion.
- `strategy-oracle-model/src/index.ts`: private frozen-bundle leaf entrypoint; no live-provider command or public export.
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| external model record → frozen bundle | Untrusted provenance and response bytes enter. |
| bundle → factory | Explicit hostile source is data only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-264-08 | Spoofing | provider identity | high | mitigate | Exact version/settings/request-response roots and drift block. |
| T-264-09 | Elevation | model source | high | mitigate | No network/evaluation path; factory supervision only. |
| T-264-SC | Tampering | package installs | high | accept | No install action. |
</threat_model>
<verification><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-model/src/model.test.ts</automated></verification>
<success_criteria>Frozen-bundle synthesis is exact, charged, and incapable of becoming live Match/search inference.</success_criteria>
<output>Create `.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-04-SUMMARY.md` when done</output>
