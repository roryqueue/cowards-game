---
phase: 264-immutable-factory-independent-oracles-and-quarantined-intake
plan: "03"
type: execute
wave: 2
depends_on: [264-01]
files_modified: [packages/strategy-oracle-teacher/package.json, packages/strategy-oracle-teacher/tsconfig.json, packages/strategy-oracle-teacher/src/index.ts, packages/strategy-oracle-teacher/src/teacher.ts, packages/strategy-oracle-teacher/src/distill.ts, packages/strategy-oracle-teacher/src/controller.ts, packages/strategy-oracle-teacher/src/emit.ts, packages/strategy-oracle-teacher/src/teacher.test.ts]
autonomous: true
requirements: [ORCL-01, ORCL-03]
must_haves:
  truths: ["A separate teacher uses canonical transitions offline and emits a legal-information-only deterministic student.", "Teacher counterfactuals and search state cannot enter deployed source or factory admission."]
  artifacts:
    - path: packages/strategy-oracle-teacher/src/teacher.ts
      provides: offline canonical-transition counterfactual search
    - path: packages/strategy-oracle-teacher/src/distill.ts
      provides: explicit legal-observation student policy
  key_links:
    - from: packages/strategy-oracle-teacher/src/emit.ts
      to: packages/strategy-lab/src/factory/contracts.ts
      via: FactoryOraclePacket data emission
      pattern: FactoryOraclePacket
  prohibitions:
    - "No teacher-only counterfactual data in emitted student source, shared tactical core, direct source execution, or Phase 263 reuse."
    - "No public/production path, league/holdout/formation/rules work, or fixture treated as actual candidate evidence."
---
<objective>Build a distinct search-teacher/distiller leaf. Purpose: make privileged offline exploration useful only by distilling a deterministic legal-input student. Output: isolated canonical-engine teacher, student emitter, and proof.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-CONTEXT.md
@packages/strategy-lab/src/runtime-bridge.ts
@packages/strategy-lab/src/planner/information-boundary.test.ts
@packages/strategy-lab/src/factory/contracts.ts</context>
<tasks>
<task type="auto"><name>Task 1: Create the independent offline teacher and distillation package</name><files>packages/strategy-oracle-teacher/package.json, packages/strategy-oracle-teacher/tsconfig.json, packages/strategy-oracle-teacher/src/index.ts, packages/strategy-oracle-teacher/src/teacher.ts, packages/strategy-oracle-teacher/src/distill.ts</files><read_first>packages/strategy-lab/package.json; packages/strategy-lab/src/runtime-bridge.ts; packages/engine/src/index.ts; packages/spec/src/schemas.ts</read_first><action>Create the private teacher leaf with its own counterfactual search-state and distillation policy. The teacher may call the exported canonical MATCH_KERNEL offline for counterfactual transition evaluation, but may not copy rules and may not share the tactical selector, scorer, search tree, parameters, or response logic per D-14, D-16, and D-18. Define an explicit student representation whose inputs are canonical legal observation, allowed objective, and permitted memory only; do not place privileged state, opponent internals, host data, or evaluator data in that representation per D-03 and D-16.</action><verify><automated>./node_modules/.bin/tsc -b packages/strategy-oracle-teacher</automated></verify><acceptance_criteria>Teacher-only state is structurally separate from the student and only the canonical engine supplies transitions.</acceptance_criteria><done>The second strategic core has independent teacher and distillation ownership.</done></task>
<task type="auto" tdd="true"><name>Task 2: Emit and test the distilled legal student</name><files>packages/strategy-oracle-teacher/src/emit.ts, packages/strategy-oracle-teacher/src/index.ts, packages/strategy-oracle-teacher/src/teacher.test.ts</files><read_first>packages/strategy-lab/src/planner/emit.ts; packages/strategy-lab/src/planner/information-boundary.test.ts; packages/strategy-lab/src/factory/contracts.ts</read_first><behavior>- Identical canonical legal observations produce the same student result despite altered teacher-only counterfactual data.\n- Distillation changes emitted student behavior only from recorded legal-observation training records.\n- The package emits source as a rooted packet without executing it.</behavior><action>Export `emitTeacherFactoryPacket` from `src/emit.ts` and re-export it from the private `src/index.ts`; it accepts the leaf's explicit distilled legal-observation student and canonical factory request and returns the strict data-only `FactoryOraclePacket` consumed by Plan05 ingestion. In `src/teacher.test.ts`, import this exact named export from the leaf entrypoint, assert it is callable, invoke it with a valid bounded request, and verify its returned canonical source/provenance/packet roots through the factory schema. A wrong export name, mismatched source root, malformed input, or missing model provenance must fail the corresponding assertion before any supervision. Emit self-contained deterministic student source from the leaf's explicit distilled policy, assert closure and legal-input-only behavior with paired cases, and construct a rooted FactoryOraclePacket. Test that counterfactual teacher state influences offline selection but cannot reach deployed student choices; source never crosses a direct execution path per D-03, D-16, and D-19.</action><verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-teacher/src/teacher.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-oracle-teacher</automated></verify><acceptance_criteria>Tests demonstrate teacher/student separation, canonical legal replay, and no synthetic fixture claim of actual candidate evidence.</acceptance_criteria><done>ORCL-03 is implemented with a verifiable information boundary.</done></task>
</tasks>
## Artifacts this phase produces

### Same-plan readiness correction clarification — 2026-09-13

The teacher must compare actual mission/Action alternatives through current canonical `MATCH_KERNEL` runtime resumes, with one global node budget and actual reached-depth accounting. Varying only seeds, stopping at the first effect or scoring event counts does not establish response search. Its declared opponent hypothesis must affect modeled responses and canonical outcome-based selection. Selected effects must yield schema-admitted legal training targets that feed distillation; unrelated caller labels alone do not connect the teacher to the student. These are source/unit mechanics, not new empirical Match authority.

Add the leaf-owned pure `controller.ts` used by trusted student wrappers and statically bundled by the emitter. Both emitted entrypoints must use that exact tested owned controller, with source-manifest correspondence and changed-controller-byte/root tests. Do not duplicate a handwritten approximation, import another strategic core or execute generated source. Regression tests must cover low global budgets, early termination, outcome/hypothesis variation, search-to-training-to-packet linkage, unknown-field rejection and useful unseen legal inputs.

- `strategy-oracle-teacher/src/teacher.ts`: private offline counterfactual teacher over canonical transitions.
- `strategy-oracle-teacher/src/distill.ts` and `emit.ts`: legal-observation student policy and exact `emitTeacherFactoryPacket` data-only producer.
- `strategy-oracle-teacher/src/index.ts`: private leaf entrypoint with no public or production export.
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| offline counterfactual state → student | Privileged state must be stripped before deployment. |
| teacher packet → factory | Student source remains hostile data. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-264-06 | Information disclosure | teacher/distiller | high | mitigate | Separate representations and paired legal-input tests. |
| T-264-07 | Tampering | transition authority | high | mitigate | Use MATCH_KERNEL, prohibit copied resolver. |
| T-264-SC | Tampering | package installs | high | accept | No install action. |
</threat_model>
<verification><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-oracle-teacher/src/teacher.test.ts</automated></verification>
<success_criteria>The teacher is an independent offline mechanism and deployed student requires no privileged input.</success_criteria>
<output>Create `.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-03-SUMMARY.md` when done</output>
