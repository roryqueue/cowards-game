---
phase: 58
plan: 01
slug: public-player-profile-service-read
status: completed
created: 2026-05-22
---

# Phase 58-01 Plan — Public Player Profile Service Read

## Tasks

1. [x] Add the public player service method.
   - Add `PublicPlayerPageServiceDto`.
   - Add `getPublicPlayerPage(handle)` to `CowardsService`.
   - Validate and leak-check public player DTO envelopes.

2. [x] Rewire the web page through the public service boundary.
   - Add `getPublicPlayerProfile(handle)` to `apps/web/lib/public-service-boundary.ts`.
   - Replace `competitiveServer.getPublicPlayerProfile` in the player page.
   - Remove the obsolete competitive-server profile facade and direct profile persistence import.

3. [x] Widen strict import enforcement.
   - Add the player page to `strictMigratedFiles`.
   - Update import-boundary tests and boundary monitor baseline fingerprints.

4. [x] Verify behavior and boundary shrinkage.
   - Add service tests for envelope, missing profile, and private-field rejection.
   - Run service tests, web tests, import-boundary tests, boundary monitors, and typecheck.
