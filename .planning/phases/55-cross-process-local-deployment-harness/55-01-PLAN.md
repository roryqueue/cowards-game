---
phase: 55
plan: 01
slug: cross-process-local-deployment-harness
status: completed
created: 2026-05-22
---

# Phase 55-01 Plan — Cross-Process Local Deployment Harness

## Tasks

1. [x] Add a root `topology:check` command.
   - Validate package scripts, fixture files, route manifest, service contract version, and runtime adapter metadata.
   - Print exact local commands for starting web, worker/runtime, TS service path, and Go read-only service.

2. [x] Add optional live smoke checks.
   - `--web-url` checks `/api/service/health`.
   - `--go-url` checks `/health`, public MatchSet summary, public replay metadata, and unauthenticated owner analytics failure.
   - `--require-web` and `--require-go` make missing live processes fail.

3. [x] Add privacy and failure diagnostics.
   - Scan topology outputs for private markers.
   - Keep details concise and free of source, memory, objective payloads, owner debug, stack traces, stderr, tokens, host paths, and private runtime internals.

4. [x] Verify.
   - Add focused tests for static topology and live failure semantics.
   - Run topology check, tests, typecheck, review, and commit.
