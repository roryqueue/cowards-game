# Phase 30 UI Spec

## Surface

The Strategy Workshop remains the first screen at `/`.

## Requirements

- Advanced Library appears as a distinct section from Starter Library.
- Revision history includes a compact comparison of current vs previous immutable Revision.
- Test output is framed as profile-scoped Gauntlet results.
- Replay links remain public-safe, with owner-debug links only where already authorized.

## Verification

Browser check passed at `http://localhost:3000/`.
