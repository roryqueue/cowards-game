---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "44"
type: execute
wave: 27
depends_on: ["260-43"]
files_modified:
  - packages/persistence/src/complete-match.test.ts
autonomous: true
requirements: [STRAT-04]
must_haves:
  truths:
    - "The valid v1.18 semantic-receipt admission assertion remains exact under the full concurrent persistence gate."
    - "Only the test's bounded timeout changes; production source, semantic expectations, selectors, database state, gameplay, and public output remain unchanged."
  artifacts:
    - path: packages/persistence/src/complete-match.test.ts
      provides: Load-tolerant bounded receipt/deep-clone integration assertion
---
<objective>Stabilize the real activation persistence gate after the same cryptographic receipt/deep-clone assertion exceeded Vitest's generic five-second default twice under concurrent package load.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-43-SUMMARY.md @.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-14-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bound the valid receipt/deep-clone integration assertion</name>
  <files>packages/persistence/src/complete-match.test.ts</files>
  <action>Apply the existing 15-second integration-test timeout used by neighboring receipt-drift and PostgreSQL completion assertions to the single valid v1.18 semantic-receipt/deep-clone test. Do not change fixtures, assertions, verification, cloning, production source, or package-wide defaults. Run the focused assertion repeatedly, the exact full persistence package command, typecheck, lint, and protected-baseline check before retrying coordinator prepare.</action>
  <verify><automated>export DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game; pnpm exec vitest run packages/persistence/src/complete-match.test.ts -t "verifies the exact spec receipt and returns an immutable deep clone" --maxWorkers=1 --no-file-parallelism --no-cache &amp;&amp; pnpm --filter @cowards/persistence test</automated></verify>
  <done>The exact coordinator persistence gate passes without weakening or skipping the valid receipt assertion.</done>
</task>
</tasks>
<verification>Require focused repeated success, all 328 persistence tests, workspace static gates, exact protected baseline, and unchanged v1.17 database head.</verification>
<success_criteria>Coordinator prepare no longer fails because an integration-strength cryptographic assertion inherits Vitest's generic five-second unit timeout.</success_criteria>
<output>Create 260-44-SUMMARY.md.</output>
