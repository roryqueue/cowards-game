---
context: phase
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-07-19T12:49:57.317Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through failed activation prepares. Acknowledge them before resuming.

- [ ] CONSTRAINT: Coordinator exclusivity — Never mutate a current selector, the proof path, Git index, activation commit, or semantic-authority database head manually. Use the Plan-31 coordinator for every transition and recovery step.
- [ ] CONSTRAINT: Protected user changes — `.planning/config.json` and `CowardsGameSpec_Full_Consolidated_v1.md` are user-owned dirt. Never edit, stage, restore, or commit either file; verify their captured baseline before activation.
- [ ] CONSTRAINT: Exact isolated validation — Main-workspace success is insufficient. Activation readiness must pass in the coordinator's exact-parent disposable clone with its isolated dependency materialization.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Current selection in historical generation | Historical v1.17 trace generation followed current registries and failed when the v1.19 candidate was selected in the disposable clone. | blocking | Keep the generator on `MATCH_KERNEL.runMatchV117`, exact v1.17 tuple metadata, and direct immutable trace-v3 manifest/root paths. |
| Main-workspace-only type proof | The ordinary workspace passed while exact isolated activation materialization exposed TS2883 exported React element types. | blocking | Preserve explicit exported `JSX.Element` / `Promise<JSX.Element>` annotations and require the coordinator clone gate. |
| Raw proof-output identity assumptions | Gate stdout contains timing/cache variability, so raw proof JSON may change even when normalized semantic receipts are equal. | advisory | Trust the evaluator's normalized receipt checks and exact semantic validator; refresh committed receipts through their owning writer only. |

<current_state>
Milestone v1.37 is not complete. Phase 260 Plan 14, Task 1 is paused immediately before a fresh full coordinator `prepare` attempt. No live selector or database activation has occurred. Every failed prepare compensated safely. The database head is exactly `active-v1.17-bootstrap|0|sha256:fd2cc24a345c0cb94dde9966262f128c663a4430022574729eb4a902177c4b5a|null|null|null`; there is no pending intent, finalization, or compensation. The protected baseline verifies as `sha256:c0e1c2a6319f01377df74a2d6e5c493d26382f2882c059116c5ba467e5e81707`. The latest proof process completed as commit `9a6a248` and no background process remains.
</current_state>

<completed_work>

Completed within the active Plan-14 task:

- Pinned historical v1.17 trace generation to explicit v1.17 transition and released trace-v3 authority (`b16009a`, `dfe06f1`).
- Proved selected-current budget and trace generation are selector-neutral and widened the realistic selector test timeout (`a7cbf2d`).
- Typed Workshop fixtures by exact selected ABI and removed stale current-ABI/runtime fixture imports (`b87120f`, `14adc74`, `fbaa048`).
- Refreshed executable proof evidence (`07c1f9d`).
- Added explicit exported React return types so exact isolated candidate typecheck succeeds (`e7d851d`).
- Refreshed the activation seam inventory after the preactivation writer/check (`9a6a248`).
- Verified the web package typecheck/lint and 42 test files / 234 tests; verified the exact isolated activation-candidate typecheck exits zero.
- Reverified the protected working-tree baseline and exact v1.17 database head after all failed prepares.
</completed_work>

<remaining_work>

- Re-run the full coordinator `prepare` after `e7d851d` and `9a6a248`.
- If prepare succeeds, use the same coordinator for `validate`, `rollback-drill`, `stage`, `commit`, `finalize`, and `smoke`.
- Run `evaluate-v1-37-observation-v1-19-postactivation.ts --check` for the production activation ID.
- Create `260-14-SUMMARY.md`, run the phase review/validation/verification closure, and continue the remaining milestone phase and audit/archive work.
- If any coordinator stage fails, follow its deterministic recovery/compensation result. A compensated activation is safe but does not complete Plan 14.
</remaining_work>

<decisions_made>

- Historical v1.17 generators must never resolve through selected-current registries because current activation cannot reinterpret immutable evidence.
- Exact-parent disposable-clone validation is the activation readiness authority because it reproduces the dependency graph used by the coordinator.
- The Plan-31 coordinator alone owns the five selector files, proof output, staging/commit, and singleton database transition.
</decisions_made>

<blockers>
- None requiring human input. Work is paused only because the user requested a status check and pause.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — exact machine-readable state, hashes, and next action.
2. `.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-14-PLAN.md` — activation contract and allowed mutation set.
3. `.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-31-SUMMARY.md` — coordinator transaction and recovery design.
4. `.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-49-SUMMARY.md` — last completed selected-current service/replay proof.
5. `.planning/STATE.md` — milestone position and durable decisions.
6. `.planning/METHODOLOGY.md` (if present) — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- Do not run a historical v1.17 generator through a selected-current registry; use closed historical dispatch and immutable released manifests.
- Do not infer activation readiness from the main checkout alone; let the coordinator validate the exact-parent disposable clone.
- Do not hand-edit selectors or the database head to recover a failed prepare; invoke the coordinator's owned recovery path.
- Do not stage the two protected user-owned files.

## Infrastructure State

- PostgreSQL URL for all three required variables: `postgresql://cowards:cowards@localhost:5432/cowards_game`.
- Required Go path prefix: `/usr/local/go/bin`.
- Semantic authority selection head: exact v1.17 bootstrap, revision 0, no pending/finalization/compensation.
- Working tree: only `.planning/config.json` and `CowardsGameSpec_Full_Consolidated_v1.md` are modified; both are protected user changes.
- Background processes/watchers: none.
- Outstanding async external jobs: none.

<context>
The repeated full prepares were intentionally allowed to fail closed because each exposed a later real candidate-state gate. Those findings have been repaired in small commits, and the final exact isolated candidate typecheck now passes. The immediate goal on resume is not more speculative hardening: it is one fresh canonical prepare to determine whether the complete real gate stack is now green, followed by the coordinator's remaining atomic activation stages only if it is.
</context>

<next_action>
Start with: verify the protected baseline and database head, export `DATABASE_URL`, `COWARDS_GO_BACKEND_TEST_DATABASE_URL`, and `COWARDS_V1_37_SIGNED_CONFORMANCE_TEST_DATABASE_URL` to the Coward's Game PostgreSQL URL with `/usr/local/go/bin` prepended to `PATH`, then run `pnpm exec tsx scripts/activate-v1-37-observation-v1-19.ts --mode prepare --activation-id activation:phase260:plan14:production`.
</next_action>
