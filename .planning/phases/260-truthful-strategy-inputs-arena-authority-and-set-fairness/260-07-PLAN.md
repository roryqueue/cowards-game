---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "07"
type: execute
wave: 6
depends_on: ["260-05"]
files_modified:
  - packages/persistence/src/complete-match.ts
  - packages/persistence/src/complete-match.test.ts
  - packages/persistence/src/matchset-status.ts
  - packages/persistence/src/matchset-status.test.ts
  - packages/persistence/src/scoring.ts
  - packages/persistence/src/scoring.test.ts
autonomous: true
requirements: [SET-03, SET-04, SET-05]
must_haves:
  truths:
    - "An explicitly addressed successor scenario counts only when each exact canonical condition has valid terminal evidence; Phase-259 current completion/status/scoring remains exact before Plan 14."
    - "Player violations are valid canonical terminal evidence; system failures do not count and retry only with identical frozen identity."
    - "Insertion and completion order cannot affect scoring or counted bytes."
    - "A current runtime-v1.19 condition can complete or count only if each frozen Strategy Revision still matches the exact D-04 revalidation evidence bound at scheduling; old or cross-revision evidence never upgrades eligibility."
  artifacts:
    - path: packages/persistence/src/matchset-status.ts
      provides: Exact full-matrix pending/degraded/countable state derivation
    - path: packages/persistence/src/scoring.test.ts
      provides: Permutation-independent canonical condition scoring proof
  key_links:
    - from: packages/persistence/src/complete-match.ts
      to: packages/persistence/src/matchset-status.ts
      via: validated terminal condition evidence triggers exact scenario recomputation
---
<objective>Stage versioned TypeScript successor completion/status/scoring while preserving every Phase-259 current behavior before activation.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-05-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-18-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bind terminal completion and retry to one frozen condition</name>
  <files>packages/persistence/src/complete-match.ts, packages/persistence/src/complete-match.test.ts</files>
  <read_first>complete-match.ts semantic receipt admission and rollback; 0026 condition columns; Phase-258 three-way failure/retry rules; D-16.</read_first>
  <behavior>Success/player-violation terminal evidence must match scenario, condition, sides, initiative, seed, revisions, each revision's frozen runtime-v1.19 revalidation root, arena/catalog/hash, tuple, and signed request. System failure commits no gameplay/condition terminal evidence and a retry keeps every identity byte unchanged within bounded policy.</behavior>
  <action>Add a versioned runtime-v1.19 completion branch selected only by exact tuple/current-semantic dispatch; keep the generated current selector on Phase 259 and preserve its completion/retry bytes and behavior. In the successor branch, extend D-14 through D-16 admission after receipt and Chronicle validation but before mutation. Recheck D-04 revision ID/source/artifact/ABI/tuple/evidence bindings against the scheduling snapshot; do not infer compatibility from source shape or a lane certificate. Add missing/revoked/cross-revision/old-tuple evidence, wrong-condition, stale catalog, swapped side/initiative, changed request hash, player violation, retryable system failure, exhausted retry, fault-after-each-write rollback, and Phase-259 snapshot tests.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/complete-match.test.ts --maxWorkers=1</automated></verify>
  <acceptance_criteria>Only exact valid terminal evidence occupies a condition; every retry is identity-equal and system failure remains no-mutation.</acceptance_criteria>
  <done>Condition evidence cannot be forged by completion or retry drift.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Gate status and scoring on exact complete matrices</name>
  <files>packages/persistence/src/matchset-status.ts, packages/persistence/src/matchset-status.test.ts, packages/persistence/src/scoring.ts, packages/persistence/src/scoring.test.ts</files>
  <read_first>matchset-status.ts determineMatchSetStatus; scoring.ts scoreMatchSet; Set policy expected membership; competition counted-state projection.</read_first>
  <behavior>Missing/running/retryable conditions or any condition whose frozen Strategy Revision lacks its exact D-04 v1.19 evidence are pending/non-counted; exhausted system-failed matrices are degraded and non-counted; exact four valid terminals with exact revision evidence are countable. Player-violation terminals score canonically. Sort by scenarioId then condition ordinal before scoring.</behavior>
  <action>Implement D-15 and D-16 only in the exact runtime-v1.19 status/scoring branch, with membership and D-04 evidence validation before any score or counted status. Write all insertion/completion permutations, absent/substituted/revoked revision evidence, duplicate terminal, omitted condition, generic-four-terminal counterfeit, player violation, system failure, retry exhaustion, and exact Phase-259 current output tests. Keep historical and current Phase-259 MatchSets on their explicit old scoring dispatch without relabeling old evidence.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/matchset-status.test.ts packages/persistence/src/scoring.test.ts</automated></verify>
  <acceptance_criteria>Every permutation yields identical output bytes, and partial/degraded matrices never affect counted standings.</acceptance_criteria>
  <done>TypeScript completion and scoring are full-matrix aware.</done>
</task>
</tasks>

## Artifacts this phase produces

- Exact condition completion/retry admission.
- Full-matrix status and permutation-independent scoring.

<threat_model>
## Trust Boundaries
| Boundary | Untrusted input |
|---|---|
| runtime completion to persistence | receipts, Chronicle, condition identity, failure class |
| persisted rows to standings | terminal membership, order, retry state |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260-07A | Spoofing | terminal condition | critical | mitigate | Full frozen-field and signed-request equality before mutation. |
| T-260-07B | Tampering | scoring order | high | mitigate | Canonical scenario/ordinal sort and permutation tests. |
| T-260-07C | Elevation of Privilege | system failure | critical | mitigate | No terminal evidence, no player penalty, no counted status. |
| T-260-07D | Repudiation | retry | high | mitigate | Stable condition/request identity and append-only attempts. |
| T-260-07E | Spoofing | revision evidence at completion | critical | mitigate | Recheck exact frozen revision/source/artifact/ABI/tuple/evidence roots before countability. |
</threat_model>
<verification>Run completion rollback/retry and status/scoring permutation suites.</verification>
<success_criteria>Only a complete exact explicitly dispatched successor matrix with exact revision evidence counts, independent of runtime order, while Phase-259 current behavior remains unchanged until activation.</success_criteria>
<output>Create 260-07-SUMMARY.md.</output>
