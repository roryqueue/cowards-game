---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "08"
type: execute
wave: 2
depends_on: ["259-05", "259-06"]
files_modified:
  - packages/replay/src/validate.ts
  - packages/replay/src/validate.test.ts
  - packages/replay/src/index.ts
autonomous: true
requirements: [CHRN-02, CHRN-03, CHRN-06]
must_haves:
  truths:
    - "D-13 per-slot lifecycle state is enforced together with global Phase/Round/initiative and semantic subject/state agreement."
    - "D-15 exact persisted tuple/authoritative resolution selects a parser before parsing; unresolved evidence remains immutable and is never guessed."
    - "D-16 current and historical vocabularies/boundaries stay disjoint, and the exported shared validator never migrates or relabels history."
  artifacts:
    - path: packages/replay/src/validate.ts
      provides: Version-first dispatcher, non-reconstructing semantic core, and public current Chronicle validator
  key_links:
    - from: packages/replay/src/grammar.ts
      to: packages/replay/src/validate.ts
      via: validateCurrentChronicleSemantics consumes per-slot grammar and all subject/state/postcondition checks without reconstruction
    - from: packages/replay/src/validate.ts
      to: packages/replay/src/reconstruct.ts
      via: validateCurrentReplayReconstruction calls validateCurrentChronicleSemantics exactly once; validateCurrentChronicle never calls reconstruction
    - from: packages/replay/src/index.ts
      to: apps/runtime-service/src/execute-match.ts
      via: validateCurrentChronicle is the shared public semantic admission API with bounded privacy-safe failures
---
<objective>Make Chronicle admission version-strict, per-slot, semantic, and stable-code exact under D-13, D-15, and D-16.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-05-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-06-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route exact versions before schema, grammar, or replay projection</name>
  <files>packages/replay/src/validate.ts, packages/replay/src/validate.test.ts</files>
  <read_first>259-05 per-slot grammar API; 259-06 frozen historical APIs/manifest; packages/replay/src/validate.ts compatibility resolver; Phase-256 exact tuple resolver.</read_first>
  <behavior>
    - Exact current tuples invoke only current schema/vocabulary/per-slot grammar; exact historical tuples invoke only frozen historical modules.
    - Current rejects historical-only event literals/boundaries and historical rejects current-only ones.
    - Ambiguous/unknown tuple evidence returns typed unresolved/unsupported results with original bytes untouched; no newest-first probing or migration exists.
  </behavior>
  <action>Write cross-version confusion and no-write RED vectors first. Refactor dispatch so D-15 resolution happens before any schema/grammar helper and D-16 routes to disjoint modules. Keep the current shared API name and backward-compatible result envelope.</action>
  <verify><automated>pnpm exec vitest run packages/replay/src/validate.test.ts packages/replay/src/historical-v1-4.test.ts</automated></verify>
  <acceptance_criteria>Every tuple follows one explicit parser path, ambiguous bytes remain raw, and no current helper is reachable from historical interpretation.</acceptance_criteria>
  <done>Chronicle parser selection is exact and historically safe.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Extract an acyclic semantic core and enforce complete state agreement</name>
  <files>packages/replay/src/validate.ts, packages/replay/src/validate.test.ts, packages/replay/src/index.ts</files>
  <read_first>Task-1 dispatch; 259-05 stable per-slot grammar codes; packages/spec/src/semantic-integrity.ts; packages/engine/src/kernel/types.ts; current Chronicle fixtures.</read_first>
  <behavior>
    - Actor/player/Soldier/activation subject, bounds, occupancy, ownership, status/position, initiative, lifecycle, version, outcome, and transition postconditions agree with the canonical state.
    - One mutation per semantic family fails at the first stable bounded code and never changes state, memory, outcome, or Chronicle.
    - `validateCurrentChronicleSemantics` performs schema, version, grammar, state, outcome, and postcondition validation but never imports or calls reconstruction.
    - `validateCurrentChronicle` calls the semantic core exactly once and never calls `validateCurrentReplayReconstruction`; runtime-service and persistence share this public API.
  </behavior>
  <action>Expand the adversarial mutation table and add call-count/no-recursion RED tests before implementation. Extract `validateCurrentChronicleSemantics` as the non-reconstructing package-internal core, then make `validateCurrentChronicle` a single call into it. Remove every reverse import/call from validation to reconstruction. Preserve valid current v1.4 decisions; if a proposed clarification changes any valid state, legality, event order, outcome, or Strategy observation, stop for explicit approval.</action>
  <verify><automated>pnpm --filter @cowards/replay test &amp;&amp; pnpm --filter @cowards/replay typecheck</automated></verify>
  <acceptance_criteria>All required semantic families reject deterministically, the semantic core is invoked once, validation has no reconstruction dependency, and service/persistence import one identical API.</acceptance_criteria>
  <done>Current Chronicle admission is version-strict, semantically complete, and acyclic.</done>
</task>
</tasks>

## Artifacts this phase produces

- `validateCurrentChronicleSemantics` non-reconstructing core and `validateCurrentChronicle` public wrapper with per-slot semantic enforcement.
- Exact `resolveReplayCompatibilityIdentity` current/historical/unresolved dispatch with no migrations.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Version spoofing invokes the wrong semantics | high | Tuple-first closed dispatch before parsing. |
| Subject/event payload disagrees with canonical state | high | Full semantic agreement and stable first failure. |
| Diagnostic echoes attacker/private values | high | Bounded closed codes and approved safe coordinates only. |
</threat_model>
<verification>Run the full replay suite, typecheck, cross-version mutations, and historical immutable proof.</verification>
<success_criteria>Current and historical Chronicles validate under exactly one correct semantic path with no valid-v1.4 drift.</success_criteria>
<output>Create `259-08-SUMMARY.md`.</output>
