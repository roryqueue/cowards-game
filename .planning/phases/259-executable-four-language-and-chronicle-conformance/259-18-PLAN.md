---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "18"
type: execute
wave: 6
depends_on: ["259-08", "259-14", "259-30"]
files_modified:
  - packages/persistence/src/complete-match.ts
  - packages/persistence/src/complete-match.test.ts
  - packages/persistence/src/semantic-integrity.test.ts
autonomous: true
requirements: [CHRN-04, CHRN-05]
must_haves:
  truths:
    - "D-14 persistence compares exact per-transition and final trace roots before opening or mutating the completion transaction."
    - "D-16 persistence invokes the same exported version-strict semantic validator as runtime-service and never parses events to derive gameplay."
    - "Invalid, stale, unresolved, or mismatched evidence causes full rollback/no rows and remains non-penalizing system failure."
    - "Persistence imports the pure v1.18 parser/verifier from `@cowards/spec`, never runtime-service, and requires exact bottom/top certificate references plus common-meter roots."
  artifacts:
    - path: packages/persistence/src/complete-match.ts
      provides: Pre-transaction shared Chronicle/reconstruction plus authenticated v1.18 receipt admission
  key_links:
    - from: packages/replay/src/validate.ts
      to: packages/persistence/src/complete-match.ts
      via: persistence invokes the same validateCurrentChronicle public wrapper before transaction start
    - from: packages/replay/src/reconstruct.ts
      to: packages/persistence/src/complete-match.ts
      via: exact transition, final state, outcome, terminal, and trace root must match the service receipt
    - from: packages/spec/src/runtime-semantic-receipt-v1-18.ts
      to: packages/persistence/src/complete-match.ts
      via: shared pure verifier authenticates signature, trace/common-meter roots, distinct bottom/top certificate/source/freshness references, ownership, and no-mutation before transaction
    - from: packages/persistence/src/complete-match.ts
      to: packages/persistence/src/runtime-evidence-authority-publisher.ts
      via: completion rechecks current certificate ID, hash, generation, roots, and freshness before durable use
---
<objective>Make durable Match completion consume only the same fully validated reconstruction-equivalent evidence accepted by runtime-service.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-08-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-14-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-30-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Admit complete Match evidence before any persistence mutation</name>
  <files>packages/persistence/src/complete-match.ts, packages/persistence/src/complete-match.test.ts</files>
  <read_first>packages/persistence/src/complete-match.ts pre-transaction admission; 259-08 validator; 259-14 reconstruction result; 259-30 public spec-owned v1.18 parser/verifier.</read_first>
  <behavior>
    - Shared validation, exact reconstruction, terminal/final state/outcome/trace/common-meter roots, tuple, and distinct bottom/top certificate ID/hash/generation/lane/freshness/source identities all agree before transaction start.
    - Persistence verifies with `@cowards/spec` and a trusted public key; it has no dependency on an app issuer/parser.
    - Invalid/unresolved/historical-confused/stale/mismatched evidence throws the stable semantic system failure and writes zero rows.
    - Successful admission returns a branded deep clone so later caller mutation cannot change durable meaning.
  </behavior>
  <action>Write pre-transaction spy and one mutation per semantic/reconstruction/signature/bottom/top certificate/source/freshness/common-meter/v1.18 receipt family plus an app-import prohibition before implementation. Verify through `@cowards/spec`, compose only `@cowards/replay`, and do not implement Chronicle grammar or Match rules in persistence. Preserve explicit prior-version dispatch.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/complete-match.test.ts</automated></verify>
  <acceptance_criteria>Every invalid two-sided input fails before BEGIN/write and successful input is spec-verified, exact, cloned, and root-bound without app dependency.</acceptance_criteria>
  <done>Persistence cannot durably use a semantically invalid Chronicle or divergent replay.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove transactional rollback and public-safe system ownership</name>
  <files>packages/persistence/src/complete-match.test.ts, packages/persistence/src/semantic-integrity.test.ts</files>
  <read_first>Task-1 admitted snapshot; completion transaction fault seams; semantic-integrity DB fixture; privacy-safe failure contract.</read_first>
  <behavior>
    - Faults after each insert/update boundary roll back Match, Chronicle, result, receipt, standings/claims, and all partial rows.
    - Replay/validation/certificate/system failures never become player violations or gameplay terminal events.
    - Persisted/public failure receipts contain only approved safe identity/status/root fields.
  </behavior>
  <action>Add real PostgreSQL fault injection and row-count/state snapshot checks at every transactional boundary. Reuse the stable system-failure projection and poison-string scans; never persist private trace preimages or diagnostics.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/complete-match.test.ts packages/persistence/src/semantic-integrity.test.ts --testTimeout=15000</automated></verify>
  <acceptance_criteria>All injected failures leave the database exactly unchanged and emit only non-penalizing public-safe system evidence.</acceptance_criteria>
  <done>Durable Chronicle admission is atomic, semantically shared, and failure-safe.</done>
</task>
</tasks>

## Artifacts this phase produces

- Trace-root-bound `admitCurrentMatchCompletion` result.
- PostgreSQL-backed rollback/no-partial-write semantic integrity proof.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Invalid evidence reaches SQL writes | high | Complete shared admission before transaction. |
| Partial completion survives a later fault | high | Real PostgreSQL rollback matrix and row snapshots. |
| Durable/public rows expose private trace data | high | Closed safe schema and poison-string assertions. |
</threat_model>
<verification>Run focused persistence tests against PostgreSQL with transaction fault injection and semantic privacy checks.</verification>
<success_criteria>Only reconstruction-equivalent evidence persists, and all failures leave zero partial durable state.</success_criteria>
<output>Create `259-18-SUMMARY.md`.</output>
