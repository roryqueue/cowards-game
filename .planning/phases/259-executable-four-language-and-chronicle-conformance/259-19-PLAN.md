---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "19"
type: execute
wave: 7
depends_on: ["259-09", "259-16", "259-18"]
files_modified:
  - packages/persistence/migrations/0023_runtime_conformance_certificates.sql
  - packages/persistence/src/schema.ts
  - packages/persistence/src/migrations.test.ts
  - packages/persistence/src/runtime-evidence-authority-publisher.ts
  - packages/persistence/src/runtime-evidence-authority-publisher.test.ts
autonomous: true
requirements: [CONF-04, CONF-05]
must_haves:
  truths:
    - "D-09/D-10 only complete reviewed real three-run certificates can enter append-only authority; unavailable, partial, skipped, unsupported, fallback, or synthetic evidence writes nothing."
    - "D-11 publication rechecks exact current certificate, source graph, tuple, corpus, policy, artifact, runtime/toolchain, run roots, and freshness."
    - "D-12 each complete lane can publish independently while incomplete lanes remain absent and all-four phase closure remains false."
    - "Migration 0023 extends the existing migration-0012 certificate/evidence ledger after 0021/0022 and creates no parallel registry."
    - "Certificate import extends the existing plural `RuntimeEvidenceAuthorityImportTrustRoot[]` API; protected descriptor loading/bootstrap remains Plan 28 and no singular conformance root is introduced."
  artifacts:
    - path: packages/persistence/migrations/0023_runtime_conformance_certificates.sql
      provides: Additive append-only v1.17 certificate/run/source provenance on the existing ledger
    - path: packages/persistence/src/runtime-evidence-authority-publisher.ts
      provides: Serializable verified certificate import/publication and safe receipt API
  key_links:
    - from: packages/spec/src/runtime-conformance-certificate-v1-17.ts
      to: packages/persistence/migrations/0023_runtime_conformance_certificates.sql
      via: exact certificate bytes/hash, three run roots, lane/corpus/tuple identity, freshness, revocation, and supersession fields extend existing ledger rows
    - from: packages/spec/src/runtime-evidence-attestation-v1-17.ts
      to: packages/persistence/src/runtime-evidence-authority-publisher.ts
      via: only managed-producer verified snapshots and complete certificate bindings may enter the serializable publication transaction
    - from: packages/persistence/src/runtime-evidence-authority-publisher.ts
      to: scripts/sign-v1-37-language-conformance-certificate.ts
      via: signing command imports verified signed bytes and receives only a public-safe installed/rejected receipt
---
<objective>Extend the existing append-only evidence ledger and publisher for exact reviewed Phase-259 certificates without creating a parallel authority.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-09-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-16-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-18-SUMMARY.md @.planning/phases/256-counted-safety-and-canonical-authority/256-18-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the existing certificate ledger with exact v1.17 run and source provenance</name>
  <files>packages/persistence/migrations/0023_runtime_conformance_certificates.sql, packages/persistence/src/schema.ts, packages/persistence/src/migrations.test.ts</files>
  <read_first>migrations 0012, 0020, 0021, and 0022; 259-07 certificate contract; 259-09 evidence binding; current persistence schema/migration tests.</read_first>
  <behavior>
    - Migration 0023 follows existing 0021/0022 ordering and alters/adds only versioned provenance on the migration-0012 certificate/evidence ledger.
    - Rows retain exact signed certificate bytes/hash, three run roots, source IDs/hashes, lane, corpus/tuple/policy, issued/fresh-until, status, revocation, and supersession.
    - Duplicate/conflicting identities, mutation/update/delete, incomplete roots, open shapes, and cross-version references reject.
  </behavior>
  <action>Write migration-order, existing-ledger extension, immutable-row, cardinality, foreign-key, conflict, and rollback RED tests first. Add `0023_runtime_conformance_certificates.sql`; do not reuse 0021/0022, rewrite history, or create a second certificate registry.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/migrations.test.ts --testTimeout=15000</automated></verify>
  <acceptance_criteria>Migration ordering is unique, old rows remain valid, exact Phase-259 provenance is append-only, and no parallel status/registry authority exists.</acceptance_criteria>
  <done>The existing database authority can store exact signed Phase-259 evidence safely.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reverify and publish each complete lane independently under the existing authority lock</name>
  <files>packages/persistence/src/runtime-evidence-authority-publisher.ts, packages/persistence/src/runtime-evidence-authority-publisher.test.ts</files>
  <read_first>Task-1 ledger fields; Phase-256 serializable publisher/advisory-lock/source-manifest pattern; 259-09 branded evidence snapshot; 259-16 reviewed per-lane candidate shape; existing RuntimeEvidenceAuthorityImportTrustRoot plural verifier API.</read_first>
  <behavior>
    - Import/publication recomputes exact certificate bytes/signature, managed attestation, run roots, graph/source closure, freshness, status, revocation, and supersession inside one serializable locked transaction.
    - Import verification accepts only the existing closed `readonly RuntimeEvidenceAuthorityImportTrustRoot[]` value after Plan-28 bootstrap; it has no environment/file loader and never accepts runtime producer private-key configuration.
    - A complete lane publishes independently; an incomplete/stale/forged/fixture/self-signed/caller-supplied lane inserts or promotes zero authoritative rows.
    - Public/operator receipts expose only approved IDs, hashes, generation, status, and stable reason code.
  </behavior>
  <action>Write plural-root identity/substitution, import rejection, source closure, concurrency, stale/revoked, independent-lane, rollback, runtime-producer-key poison, and safe-receipt RED tests. Extend the existing publisher/import verifier to accept only the exact bootstrapped `RuntimeEvidenceAuthorityImportTrustRoot[]` input and reverify it inside the transaction. File loading, permissions, and high-water bootstrap belong only to Plan 28. No caller-supplied pass flag, singular root, new registry, or certificate-shaped request trust is allowed.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/runtime-evidence-authority-publisher.test.ts --testTimeout=15000</automated></verify>
  <acceptance_criteria>Only exact current managed signed certificates publish, each lane is independent, failures write nothing, and receipts remain private-data-free.</acceptance_criteria>
  <done>The existing authority publisher is ready for the managed signing/import plan.</done>
</task>
</tasks>

## Artifacts this phase produces

- `0023_runtime_conformance_certificates.sql` additive existing-ledger provenance.
- Verified per-lane signed certificate import/publication and safe receipt API.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Duplicate migration version rewrites activation history | high | Unique 0023 ordering after immutable 0021/0022. |
| New table becomes parallel authority | high | Foreign-keyed extension of migration-0012 ledger and existing publisher only. |
| Partial or forged certificate enters SQL | high | Full canonical/signature/graph/freshness verification before insert. |
</threat_model>
<verification>Run PostgreSQL migration, append-only, publication, concurrency, rollback, stale, and privacy tests.</verification>
<success_criteria>Exact signed Phase-259 certificates can be stored and published only through the existing append-only authority.</success_criteria>
<output>Create `259-19-SUMMARY.md`.</output>
