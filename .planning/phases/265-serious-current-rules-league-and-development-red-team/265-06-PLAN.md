---
phase: 265-serious-current-rules-league-and-development-red-team
plan: "06"
type: execute
wave: 4
depends_on: [265-03, 265-04, 265-05]
files_modified: [packages/strategy-lab/src/league/red-team.ts, packages/strategy-lab/src/league/red-team.test.ts, scripts/run-v1-38-serious-league.ts, scripts/run-v1-38-serious-league.test.ts]
autonomous: true
requirements: [LEAG-04, LEAG-09]
must_haves:
  truths: ["Every automated, model, human, and external red-team channel has a frozen allocation row, charged terminal outcome, and required invariance/probe receipt.", "A usable private CLI accepts only a complete Phase 265 allocation root and executes the connected runner/response loop through a fresh host-issued factory provider, without direct source evaluation or hidden default budgets."]
  artifacts:
    - path: packages/strategy-lab/src/league/red-team.ts
      provides: all-channel charged attempts, probe receipts, and response return
    - path: scripts/run-v1-38-serious-league.ts
      provides: allocation-gated private league prepare/run/reopen CLI
  key_links:
    - from: red-team.ts
      to: league/psro.ts
      via: successful counter returns a required response admission
      pattern: admitLeagueResponse
    - from: run-v1-38-serious-league.ts
      to: league/connected-runner.ts
      via: allocation-gated persisted-candidate host issuance followed by runLeagueCell dispatch
      pattern: issueLeagueProviderFromFactoryCandidate
  prohibitions: [No absent channel treated as zero without authorization, empirical action from fixtures, direct source evaluation, production/public registration, formation/holdout action, or fallback allocation.]
---

<objective>
Implement complete multi-channel development-red-team accounting and the allocation-gated private command that can run the fully connected league once a human supplies the missing empirical vector.

Purpose: make live execution usable and auditable without inventing the still-unallocated resource ceilings or participant policy.
Output: red-team coordinator, exact CLI, and injected source-only tests.
</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-RESEARCH.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-AI-SPEC.md
@.planning/artifacts/v1.38-pre-search-study-policy.json
@.planning/artifacts/v1.38-pre-search-measurement-policy.json
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-03-PLAN.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-04-PLAN.md
@packages/strategy-lab/src/factory/ledger.ts
@packages/strategy-lab/src/factory/intake.ts

<interfaces>Plan 04 exposes `declareLeagueRound`, `admitLeagueResponse`, and `advanceLeagueRound`; Plan 03 exposes `issueLeagueProviderFromFactoryCandidate`, `runLeagueCell`, `LeagueRepository`, and data-only reopen. The issuer reuses the existing factory admission plus `createFactorySupervisedRuntime` machinery: factory reopen data is `issued: false` and cannot supply a provider. Phase 264 quarantine intake supplies provenance/participant checks but neither its zero-intake outcome nor its allocation is Phase 265 authority.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Account for all red-team channels and invariance probes</name>
<files>packages/strategy-lab/src/league/red-team.ts, packages/strategy-lab/src/league/red-team.test.ts</files>
<read_first>packages/strategy-lab/src/league/contracts.ts; packages/strategy-lab/src/league/psro.ts; packages/strategy-lab/src/league/repository.ts; packages/strategy-lab/src/factory/ledger.ts; packages/strategy-lab/src/factory/intake.ts</read_first>
<behavior>- An immutable allocation contains automated, model, human, and external rows; each row has explicit opportunity/resource/review ceilings or an explicit authorized zero disposition.
- Starts precede all work; success, rejected, weak, invalid, duplicate, player failure, system failure, retry, unfilled, and unused capacity all retain charge/accounting evidence.
- Side, initiative, horizontal symmetry, opaque IDs, Soldier order, source order, semantic arena identity, deterministic repeat/restart, and worker/shard/completion-layout probes each emit a receipt. Repeat/layout/normalized semantic identity require byte equality; valid altered game conditions use paired contrasts under the frozen material-dependence bound.
- A positive admitted counter is returned to Plan 04's next-round path; a process-valid metagame failure is not conflated with integrity failure.</behavior>
<action>Implement `declareRedTeamAllocation`, `startRedTeamAttempt`, `terminalizeRedTeamAttempt`, `recordLeagueProbe`, and `reenterAcceptedCounter`. Require one rooted channel row for automated/model/human/external work; a zero row is accepted only when its Phase 265 allocation explicitly authorizes it. Bind every attempt to resource accounting, retry parent, disclosure/provenance/reviewer policy where applicable, and the candidate/round target. Implement all nine probe families and their identity-versus-paired-contrast criteria; do not demand identical results from genuinely changed valid game conditions, and do not create a material-dependence numeric bound before the allocation supplies it. Route accepted counters through `admitLeagueResponse`; retain every other outcome and unused ceiling in the root per D-02, D-05, D-10, D-14, D-15, and D-20.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/red-team.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>All four channels and nine probes are represented explicitly, and a favorable leader cannot omit failed/unused work or evade counter re-entry.</acceptance_criteria>
<done>LEAG-09 development red-team mechanics are complete before actual participants or budgets are authorized.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Deliver the complete allocation-gated private league CLI</name>
<files>scripts/run-v1-38-serious-league.ts, scripts/run-v1-38-serious-league.test.ts</files>
<read_first>packages/strategy-lab/src/league/repository.ts; packages/strategy-lab/src/league/connected-runner.ts; packages/strategy-lab/src/league/matrix.ts; packages/strategy-lab/src/league/solver.ts; packages/strategy-lab/src/league/psro.ts; packages/strategy-lab/src/league/red-team.ts; .planning/artifacts/v1.38-pre-search-study-policy.json; .planning/artifacts/v1.38-pre-search-measurement-policy.json</read_first>
<behavior>- `prepare`, `run`, and `verify-retained` modes accept exact repository and immutable root inputs; the run mode refuses every candidate/Match/model/human/external dispatch unless an allocation fully supplies candidate, Match, response, model, human, external, review, resource, retry/burn, material-dependence, and participant/provenance values.
- The CLI obtains each runtime capability itself from persisted candidate closure through Plan 03's host issuer, then wires population → matrix → supervised connected cells → complete snapshot → frozen solver → PSRO/red-team → portfolio/report; fixture mode remains explicitly injected mechanics rather than empirical evidence.
- Retained verification reopens and validates all linked allocation, population, terminal, snapshot, solver, round, red-team, selection, and report roots without new work.</behavior>
<action>Implement `scripts/run-v1-38-serious-league.ts` with `prepare`, `run`, and `verify-retained` commands. `prepare` parses and roots a complete human-authored allocation document only when every exact opportunity, operational/review, participant/provenance, retry/burn, resource ceiling, and material-dependence value is present; it may validate but must not dispatch. `run` accepts the allocated root, the LeagueRepository path, the FactoryRepository path, and published Phase 264 candidate roots; for every cell it calls only Plan 03's `issueLeagueProviderFromFactoryCandidate` to re-admit source closure and construct the host-issued provider before `runLeagueCell`. It must not accept a serialized or caller-injected provider, treat `issued: false` reopen records as capability, or bypass the existing factory admission/runtime adapter. Refuse partial allocation, stale policy/tuple/runtime, candidate/source/executable/provider identity mismatch, absent channel handling, unsafe provenance, or any fixture marked empirical. `verify-retained` performs a bounded data-only root graph reopen and reports process-valid result versus process failure. Expose help and injected host-issuer seams for tests. Do not import strategic leaf code, evaluate source in the coordinator, issue provider calls from a fixture, reuse Phase 263/264 budgets, open holdout, materialize formations, or write public/counted/production data per D-03 through D-06 and D-20.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 scripts/run-v1-38-serious-league.test.ts packages/strategy-lab/src/league/red-team.test.ts &amp;&amp; ./node_modules/.bin/tsx scripts/run-v1-38-serious-league.ts --help &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>The command is runnable and complete, but no real dispatch can happen from an incomplete or inherited allocation, source data cannot masquerade as an issued provider, and reopen cannot cause a second execution.</acceptance_criteria>
<done>The fully connected private Phase 265 runner has an allocation-gated persisted-candidate-to-host-issued-provider handoff before the single later human allocation decision.</done>
</task>
</tasks>

## Artifacts this phase produces

- `league/red-team.ts`: immutable all-channel allocation/attempt ledger, probe receipts, and `reenterAcceptedCounter`.
- `scripts/run-v1-38-serious-league.ts`: private `prepare`, `run`, and `verify-retained` commands.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| human/model/external opportunity to red-team ledger | Participant/provenance, capacity, retries, and unfavorable outcomes must remain visible. |
| CLI allocation input to dispatch | Missing ceilings could turn an operator command into unbounded or unauthorized empirical work. |
| counter result to PSRO | A successful attack must alter declared future evaluation rather than report narrative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-265-16 | Repudiation | channel allocation/attempts | high | mitigate | Explicit all-channel rows, start-before-work terminals, unused and failed charge retention. |
| T-265-17 | Elevation | league CLI dispatch | high | mitigate | Complete root-bound allocation validator plus host issuer require exact candidate closure and fresh factory-issued provider identity; injected command tests deny incomplete, inherited, reopened, and caller-provider authority. |
| T-265-18 | Tampering | invariance/counter evidence | high | mitigate | Rooted nine-probe receipts and mandatory PSRO re-entry transition. |
| T-265-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Exact red-team and CLI tests, CLI help, and strategy-lab typecheck; no empirical selector, provider call, participant intake, or Match is run during implementation.</verification>
<success_criteria>A future authorized operator can run/reopen the whole private league using only exact roots and host-issued provider capabilities, while all missing current allocations remain a hard stop.</success_criteria>
<output>Create `.planning/phases/265-serious-current-rules-league-and-development-red-team/265-06-SUMMARY.md` when done.</output>
