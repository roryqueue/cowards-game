---
phase: 265-serious-current-rules-league-and-development-red-team
plan: "02"
type: tdd
wave: 2
depends_on: [265-01]
files_modified: [packages/strategy-lab/src/league/matrix.ts, packages/strategy-lab/src/league/matrix.test.ts]
autonomous: true
requirements: [LEAG-01, LEAG-02]
must_haves:
  truths: ["Every population snapshot enumerates exactly 8 × C(n,2) cells across two semantic arenas and four explicit conditions.", "Sparse, duplicate, conflicting, identity-mismatched, invalid, and system-failed evidence blocks reduction rather than changing a payoff.", "Solver-facing payoff bytes consist only of the Plan 01 rooted canonical-kernel entrant half-point projections in canonical entrant order."]
  artifacts:
    - path: packages/strategy-lab/src/league/matrix.ts
      provides: canonical matrix enumeration and complete-snapshot admission
  key_links:
    - from: league/matrix.ts
      to: spec set-condition and arena catalogs
      via: explicit condition rows and semantic geometry hash
      pattern: CANONICAL_SET_CONDITION_ROWS_V1_37
    - from: league/matrix.ts
      to: league/contracts.ts
      via: canonical-kernel outcome-to-entrant half-point projection per admitted terminal
      pattern: projectCanonicalKernelOutcomeToEntrantHalfPoints
  prohibitions: [No display-arena duplication, inferred seed parity, direct-source runner, or payoff imputation.]
---

<objective>
Build the complete empirical-game matrix and its fail-closed snapshot reducer.

Purpose: only exact, condition-balanced, supervised evidence can reach a meta-solver.
Output: tested matrix enumeration, canonical cell identities, and complete-snapshot admission.
</objective>

<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-PATTERNS.md
@packages/spec/src/arena-catalog-v1-37.ts
@packages/spec/src/set-condition-policy-v1-37.ts
@packages/strategy-lab/src/tasks.ts
@packages/strategy-lab/src/reduce.ts
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-01-PLAN.md

<interfaces>`CANONICAL_SET_CONDITION_ROWS_V1_37` supplies four explicit side and entrant-level initial-initiative rows. `CANONICAL_ARENA_CATALOG_V1_37` supplies active records keyed by `semanticGeometryHash`; its parser rejects duplicate active semantic geometry. Plan 01 supplies rooted population/cell/terminal/snapshot contracts.</interfaces>
</context>

<feature>
<name>Complete semantic empirical-game snapshot</name>
<files>packages/strategy-lab/src/league/matrix.ts, packages/strategy-lab/src/league/matrix.test.ts</files>
<behavior>
- A sorted population of n immutable candidates creates exactly `8 × C(n,2)` expected cells: one unordered pair, four explicit condition rows, and two active semantic geometry hashes.
- Smoke and Open Field collapse to one semantic empty geometry; a duplicate active semantic hash causes a matrix integrity failure.
- Matrix admission rejects missing, duplicate-identical, duplicate-conflicting, stale, tuple/request/runtime mismatch, invalid, player-violation, and system-failure cells without assigning a payoff; complete rows reduce in canonical order to stable bytes and a root across layouts.
- Complete rows expose only rooted entrant-relative `0|1|2` half-point values derived from the canonical kernel outcome; mirrored sides and initiative rows do not invert or alter an asymmetric winner's entrant payoff.</behavior>
<implementation>After RED, implement `enumerateLeagueCells`, `admitCompletePayoffSnapshot`, and canonical reduction using Plan 01 identities, the arena catalog, exact condition policy, and Plan 01's canonical outcome projection. Require the full terminal record for a cell and preserve every rejected terminal as process evidence. Build solver payoff bytes only from the projection root and entrant half points, ordered by immutable entrant candidate root, never from `bottomScore`, seed spelling, or an unverified raw winner field. Test the actual maximum supported 262144-byte artifact shape through bounded stream/chunk descriptors rather than constructing one oversized monolithic manifest, while full-scale expected identity coverage is computed from the complete population from the outset. Do not call the historical matrix script, dispatch a Match, or derive a condition from a PRNG.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Red-test exact semantic matrix coverage and failure admission</name>
<files>packages/strategy-lab/src/league/matrix.test.ts</files>
<read_first>packages/strategy-lab/src/tasks.test.ts; packages/strategy-lab/src/runner-invariance.test.ts; packages/spec/src/arena-catalog-v1-37.ts; packages/spec/src/set-condition-policy-v1-37.ts</read_first>
<behavior>- Assert the 8 × C(n,2) count, stable ordinal ordering, and semantic alias collapse for injected candidate populations.
- Assert every incomplete or non-success terminal family blocks solver admission and remains visible in the returned process disposition.
- Assert mirrored-side and initial-initiative cells with one asymmetric canonical winner retain the same entrant-relative half-point result, while a draw contributes one half-point; assert snapshot payoff bytes contain only the rooted Plan 01 projections.
- Assert reordered cells and distinct worker/shard/restart operational metadata retain identical semantic snapshot bytes.</behavior>
<action>Write the failing focused fixtures before implementation. Cover all matrix fault families listed above, including the distinction that a valid disappointing success outcome is retained while invalid process evidence blocks. Add mirrored-side, initiative, asymmetric winner-for-each-entrant, and draw cases before the solver seam exists; require each expected half-point value to originate from the Plan 01 projection root rather than a bottom/top score. Include a maximal bounded chunk/stream identity case to guard against the Phase 264 oversized-manifest regression without reducing full population coverage per D-02, D-07, D-08, D-09, and D-10.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/matrix.test.ts</automated></verify>
<acceptance_criteria>The new test fails only because `league/matrix.ts` has not yet implemented its required API.</acceptance_criteria>
<done>RED coverage defines every LEAG-01/02 matrix invariant.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Implement canonical full-cell enumeration and snapshot admission</name>
<files>packages/strategy-lab/src/league/matrix.ts</files>
<read_first>packages/strategy-lab/src/league/contracts.ts; packages/strategy-lab/src/league/identity.ts; packages/strategy-lab/src/reduce.ts; packages/strategy-lab/src/shards.ts</read_first>
<behavior>- The RED fixtures pass with a rooted complete snapshot only when coverage, all evidence joins, and every canonical entrant payoff projection are exact.
- A reduction never observes a synthetic win/loss/draw for a missing or process-failed cell.
</behavior>
<action>Implement `enumerateLeagueCells` and `admitCompletePayoffSnapshot` against Plan 01 contracts. Canonically order pairs by immutable candidate identity and cells by pair/condition/semantic geometry root; require exactly one terminal whose candidate, population, condition, semantic geometry, tuple, runtime, request, trace, and root joins agree. Invoke `projectCanonicalKernelOutcomeToEntrantHalfPoints` for each successful terminal and emit only its rooted `0|1|2` entrant-relative integer value into the solver-facing payoff bytes, in canonical entrant order. Use chunked, content-addressed descriptors for retained full-scale identities and stable canonical semantic bytes for reductions. Reject aliases and all incomplete/process-invalid coverage before any solver-facing payoff matrix is emitted per D-01, D-02, D-07, D-08, D-09, and D-10.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/matrix.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>A complete two-semantic-arena matrix is the sole solver-admissible result and its bytes are layout-independent.</acceptance_criteria>
<done>LEAG-01 and LEAG-02 have a real, complete, fail-closed matrix seam.</done>
</task>
</tasks>

## Artifacts this phase produces

- `league/matrix.ts`: `enumerateLeagueCells` and `admitCompletePayoffSnapshot` exports.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| retained cell terminals to snapshot reducer | A hostile or broken worker may submit incomplete, duplicate, stale, or mismatched evidence. |
| arena/condition authority to task identity | Display aliases and inferred conditions could falsify replication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-265-04 | Tampering | cell-to-snapshot joins | high | mitigate | Exact identity joins, one-cell coverage, canonical reduction, fault fixtures. |
| T-265-05 | Integrity | arena and condition enumeration | high | mitigate | Use active semantic hashes and explicit v1.37 condition rows; alias tests. |
| T-265-06 | Denial of service | full-scale manifest retention | medium | mitigate | Bounded chunk descriptors plus actual maximum-shape test. |
| T-265-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Named matrix test and strategy-lab TypeScript build only.</verification>
<success_criteria>Every candidate pair receives all eight semantic condition cells, and evidence defects stop rather than bias the empirical game.</success_criteria>
<output>Create `.planning/phases/265-serious-current-rules-league-and-development-red-team/265-02-SUMMARY.md` when done.</output>
