# Plan 265-08 pre-execution check — BLOCK

**Checked:** 2026-09-23. **Scope:** only the uncommitted diagnostic-pilot plan, against the operator-approved four-Match/240-second/30-minute/zero-retry envelope, committed 265-08 research, Phase 265 context and the consumed Plan 07 evidence. This is source/plan inspection only. No Strategy, provider, Match, model, allocation or result was invoked or written.

The plan correctly states that the old allocation-v2 is consumed, keeps the pilot private and non-LEAG, chooses the canonical S01/S03 Smoke four-condition identities, retains charged failures and unused slots, and requires independent review before its conditional dispatch. Its three tasks have files/action/verify/done fields; `verify.plan-structure` reports valid. Those intentions do not establish an executable, safe path. Resolve the blockers below before execution.

## Blockers

1. **BLOCKER — key_links_planned / execution path: the advertised host-issued provider cannot reach `runCanonicalLabMatch`.** Task 2 says issue through `issueLeagueProviderFromFactoryCandidate` and execute directly through `runCanonicalLabMatch`, but the issuer returns `LeagueIssuedProvider`, an identity-only opaque capability. The real `FactorySupervisionProvider` is held in `connected-runner.ts`'s private `WeakMap`; only `runLeagueCell` calls `requireIssued` to unwrap it. `runCanonicalLabMatch` requires actual providers with `invoke`, `verify`, and `close`. Plan 08 does not modify `connected-runner.ts`, and `runLeagueCell` itself uses the legacy `LeagueCellStart`/`LeagueRepository` charge and terminal domains, not the demanded distinct diagnostic ledger. An executor cannot implement the stated link by only writing the listed pilot module and CLI without bypassing the trusted host issuance or borrowing the old league ledger. **Fix:** explicitly plan and test a narrow pilot-owned adapter/composition point in `connected-runner.ts` (include it in `files_modified`) that accepts an already durable diagnostic precharge, authenticates exact pilot cell/allocation/issuer bindings, internally obtains the hidden providers, calls `runCanonicalLabMatch`, and returns only bounded execution/cleanup evidence. Alternatively specify a compatible refactor of `runLeagueCell` that preserves its legacy behavior and uses separate pilot record domains. Add injected tests proving precharge precedes both provider issuances and no old full-league route is reachable. Evidence: `connected-runner.ts` issuer, `privateProviders`, `requireIssued`, and `runLeagueCell`; `runtime-bridge.ts` provider type.

2. **BLOCKER — scope/feasibility admission: the proposed historical reader can consume the pilot clock before useful dispatch.** `readLeagueInitialCandidates` is described as a selection-only reader, but its `indexFactory` reads and parses *every* `factory-artifact-*.bin` before selecting S01/S03, and it also traverses the retained ledger/assessment. The prior data-only three-base import took **1,394.27 seconds** (`265-08-DIAGNOSTIC-RESEARCH.md`), leaving **405.73 seconds** of the 1,800-second command budget even before Docker/host/capacity admission, repository reservation, retention or cleanup. Four worst-case 240-second cells cannot start on that path; the first cell has at most 165.73 seconds of non-Match margin, unmeasured and potentially exhausted. The plan permits zero starts, but zero starts cannot learn the intended full-Match duration or artifact cost. It also never identifies the exact `factoryAssessmentArtifactRoots` and `operations.maxArtifactBytes/maxArtifactRecords` required by the selection-only reader; the old allocation cannot supply them as authority. **Fix:** freeze the new pilot's exact independently authenticated assessment root and bounded reader limits, then plan a targeted S01/S03 closure/assessment lookup (or source-reviewed selective index) that avoids the entire-store traversal, with a source-only measured admission latency and a conservative first-start reserve. Keep all of that work inside the 30-minute `run` clock; stop safely without a start if the measured bound fails. Do not silently move the historical traversal outside the clock or reuse allocation-v2 as admission authority. Evidence: `scripts/run-v1-38-serious-league.ts` `LeagueInitialCandidateSelection`, `indexFactory`, `readLeagueInitialCandidates`; diagnostic research Summary/Open Question 1.

3. **BLOCKER — deadline enforcement: checks plus an AbortSignal do not prove a hard 240-second cell or 1,800-second run bound in this source.** Task 2 promises an abort/kill signal and before/after monotonic checks, but `runCanonicalLabMatch` accepts no deadline/signal and loops to 1,010,000 kernel steps. The existing container stream uses synchronous `Atomics.wait` and Docker controls use `spawnSync`; a same-thread timer cannot interrupt those waits or synchronous publication/fsync. After-the-fact checks classify an overrun but do not prevent it. The plan also requires retention and cleanup after timeout while claiming both are inside the cap, without fixing an in-cell reserve or external watchdog. **Fix:** specify the actual preemptive boundary: a separate supervising process/thread with monotonic watchdog and owned-container termination, or source-level remaining-deadline propagation to each bounded host/stream/control/publication step plus guaranteed worst-case cleanup/terminal reserve *inside* each cell and overall cap. Test a blocked host exchange, blocked publication, and cleanup timeout with a fake clock/watchdog to prove elapsed caps are not merely reported after overrun. Preserve 1,000-ms method timeout and 120,000-ms ordinary provider lifetime. Evidence: `runtime-bridge.ts` bridge signature; `v1-38-lean-container-match-session.ts` synchronous transport/stream and cleanup; both supervisor sources.

4. **BLOCKER — pre-dispatch source-review gate is procedural but not bound to the authorizing route.** Task 3 explicitly requests an agent other than the implementer and says re-review after repair, which is the right order. Yet Task 3's sole automated `<verify>` is post-run `verify-retained`; no stated `prepare`/`run` admission check requires a zero-finding independent-review disposition and passing full-gate receipt for the *exact* source root it executes. The allocation's source root and the review document can each be valid but unrelated. This leaves the must-have “only source-reviewed, fully tested code ... may make a charged start” unverifiable from the executable route. **Fix:** make the independent reviewer a distinct named role/output before allocation, freeze executable/test/boundary source hashes after any repair, and require a pre-dispatch comparison of the reviewed zero-finding root plus recorded exact gate exits to the run's implementation/source root. Reject missing/stale/mismatched review before repository reservation/provider issuance; inject denial tests. Avoid circularly hashing the review/result files into their own executable-source root.

5. **BLOCKER — requirement metadata asserts LEAG-02 while the plan disclaims all LEAG evidence.** Frontmatter says `requirements: [LEAG-02]`, but the objective, truths, coverage audit and success criteria repeatedly say this four-cell diagnostic satisfies **no** LEAG-01–09 requirement. LEAG-02 concerns failed cells in complete league payoff/meta-solving; this pilot produces no payoff matrix or solver input. An automated GSD coverage check could incorrectly credit the failed phase's requirement from this plan. **Fix:** remove LEAG-02 from Plan 08's `requirements` field (or use an explicit diagnostic/non-requirement marker supported by GSD), retain the non-imputation guard in action/threat model, and keep LEAG-01–09 pending on the complete reviewed league. Evidence: `REQUIREMENTS.md` LEAG-02 and Plan 08 frontmatter/objective/audit.

## Warnings

1. **WARNING — dependency_correctness:** `wave: 6` with sole `depends_on: [265-06]` is inconsistent with the recorded 265-06 Wave 4 → Wave 5 dependency rule. Plan 07's source and failed historical artifacts are prerequisites, but Plan 07 is incomplete and must not become an execution-success dependency. Correct the wave metadata or explicitly document the historical-source dependency without making the diagnostic inherit Plan 07's consumed authority. The roadmap still lists seven plans and the failed Plan 07, so update planning inventory without implying Phase 265 completion.

2. **WARNING — scope_sanity:** 12 `files_modified` in one three-task plan exceeds the normal 5–8-file target and 10-file warning threshold, with one task combining independent review, full 29-suite gate, allocation and live dispatch. Splitting source/gate from conditional dispatch would reduce review drift risk, provided the same operator envelope and zero-retry rule remain fixed.

3. **WARNING — verification_derivation:** Task 3's `verify-retained` command cannot by itself distinguish a safe no-start admission denial, a missing result, or a valid retained diagnostic prefix, and it does not check old JSON/tree digest equality or review/gate status. Name an executable source-only preflight verification for the prestart branch and explicit digest/receipt checks for the run branch; retain Task 3 incomplete when any required gate fails.

## Overall disposition

**BLOCK.** Do not execute Task 3, create the pilot allocation, or dispatch the pilot from this plan revision. The plan must first close the opaque-provider execution seam, demonstrate prospective clock/capacity feasibility for at least one useful start without weakening any bound, make hard deadlines enforceable, and bind the independent exact-source review to admission. The old 265-07 allocation/result/repository remain immutable; this review gives no LEAG, freeze, formation, holdout, counted or public authority.
