---
phase: 265-serious-current-rules-league-and-development-red-team
plan: "09"
type: execute
wave: 6
depends_on: [265-08]
files_modified:
  - .planning/artifacts/v1.38-phase-265-diagnostic-pilot-allocation.json
  - .planning/artifacts/v1.38-phase-265-diagnostic-pilot-result.json
autonomous: true
must_haves:
  truths:
    - "Exactly one new diagnostic-only allocation is admitted from the independently reviewed Plan 08 source/gate receipt; old allocation-v2 is never run, retried, refunded, replaced or treated as authorization."
    - "At most one `run` command may start zero to four private S01/S03 Smoke current-rules Matches; its monotonic 1800000-ms clock begins at command entry and covers targeted Phase 264 admission, fresh host/capacity checks, Match cells, publication and owned cleanup."
    - "Each cell is charged before either provider issuance, lasts at most 240000 ms including reserved cleanup/terminal work, and no next cell starts unless remaining overall time covers its full cap plus cleanup reserve; first non-success stops all further starts with zero retries."
    - "An independent read-only verifier distinguishes safe no-start denial, a verified diagnostic prefix and a missing/invalid result, proves old JSON/tree digest equality, and grants no LEAG-01–09, Phase 266 freeze, formation, holdout, counted, public or production authority."
  artifacts:
    - path: .planning/artifacts/v1.38-phase-265-diagnostic-pilot-allocation.json
      provides: one exact-key content-addressed private diagnostic allocation bound to Plan 08 source gate
    - path: .planning/artifacts/v1.38-phase-265-diagnostic-pilot-result.json
      provides: immutable no-start or charged-prefix result plus read-only verifier disposition
  key_links:
    - from: .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json
      to: scripts/run-v1-38-diagnostic-pilot.ts
      via: prepare/preflight/run recompute source closure and require independent zero-finding review plus every recorded gate exit 0
    - from: .planning/artifacts/v1.38-phase-265-diagnostic-pilot-allocation.json
      to: .strategy-lab/league-265-diagnostic-pilot-20260923-a
      via: one-shot marker, distinct durable precharge/terminal ledger and parent watchdog
    - from: .planning/artifacts/v1.38-phase-265-diagnostic-pilot-result.json
      to: .planning/artifacts/v1.38-phase-265-run-result.json
      via: read-only old-result digest comparison only; no linkage as payoff or continuation
---

<objective>
Only if Plan 08's exact-source independent review and all source gates pass, prepare one new immutable diagnostic allocation and attempt at most one private pilot run. An admission denial or bounded partial prefix is a truthful diagnostic outcome, not a complete league.

Purpose: measure actual current-rules full-Match duration and artifact cost under a preemptive deadline while preserving consumed allocation-v2 and all downstream denial gates.
Output: new pilot allocation, a rooted run/no-start result, and read-only retained verification.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-07-POSTFAILURE-ROUTE-ASSESSMENT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-DIAGNOSTIC-RESEARCH.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PLAN.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-SUMMARY.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-SOURCE-REVIEW.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json
@scripts/run-v1-38-diagnostic-pilot.ts

<interfaces>
The exact pilot is S01/S03 on Smoke, seed `league-265-postfailure-pilot-20260923-a`, condition order `a-bottom-a-first`, `a-bottom-b-first`, `a-top-a-first`, `a-top-b-first` rederived by `createSetScenarioV137`. It covers one of two active semantic arenas, so even four successes are not an arena-balanced matrix. The separately rooted output paths are `.planning/artifacts/v1.38-phase-265-diagnostic-pilot-allocation.json`, `.planning/artifacts/v1.38-phase-265-diagnostic-pilot-result.json`, and `.strategy-lab/league-265-diagnostic-pilot-20260923-a` (mode 0700). The authenticated read-only source repository is `.strategy-lab/factory-264-fresh-20260914-approved-two`. The old allocation-v2 `.planning/artifacts/v1.38-phase-265-allocation-v2.json`, stale unversioned historical allocation `.planning/artifacts/v1.38-phase-265-allocation.json`, old result `.planning/artifacts/v1.38-phase-265-run-result.json`, old league repository `.strategy-lab/league-265-current-rules-20260922`, and old response-factory repository `.strategy-lab/factory-265-current-rules-20260922` are immutable and never destinations. Plan 265-07's attempted empirical branch is terminal process-invalid history, not a prerequisite that may be re-executed. This plan satisfies no LEAG requirement. No CLI branch in this plan invokes `runSeriousLeague` or the old full-league command.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admit one distinct allocation and perform read-only feasibility checks</name>
  <files>.planning/artifacts/v1.38-phase-265-diagnostic-pilot-allocation.json</files>
  <action>Before creating the allocation, verify Plan 08 SUMMARY, named independent reviewer, zero actionable findings, `265-08-PILOT-GATE.json` canonical root, all exact-source gate exits and current source closure using `check-gate`; reject any stale/partial receipt. Confirm ROADMAP inventory identifies Plan 07 as consumed process-invalid and Plan 08/09 as diagnostic supplements, not Phase 265 completion. Compute and retain SHA-256 of the three old JSON files and a sorted no-follow content+mode+relative-path digest of both old league and response-factory repositories; read them only and include all five baseline digests in the new pilot allocation. Use the new CLI `prepare` once with the exact S01/S03/assessment/closure/condition/tuple/runtime/image roots from Plan 08, four reserved starts, `diagnostic_only`, 240000-ms per-cell including cleanup, 1800000-ms total, zero retries, source-derived physical-byte/record/inode and terminal reserves, privacy class and explicit false flags for LEAG, freeze, formation, counted and public authority. The allocation binds the exact Plan 08 source-gate root and old-evidence digest baseline but not any old allocation as authority. Use exclusive-create on the new allocation path; a pre-existing mismatched or prior attempted pilot allocation is a stop, never a new retry. Run the CLI's read-only `preflight` against this allocation and the Phase 264 store: it checks exact targeted assessment/closure, recorded source-only reader latency bound, current gate, Docker digest/daemon, 2-CPU/256-MB isolation, fresh host memory and free bytes/inodes on the intended pilot filesystem, source-derived worst-case artifact and terminal/cleanup reserves, empty new repository path/no owned-container collision and safe first-start time margin. This standalone preflight creates no run authority, reservation, provider or Match and is repeated entirely inside the eventual `run` clock. If any bound is unverifiable or first useful start cannot fit, do not invoke `run`; record the no-dispatch reason in the Plan 09 summary, leave the run task incomplete and seek a new technical review rather than weakening limits.</action>
  <verify><automated>./node_modules/.bin/tsx scripts/run-v1-38-diagnostic-pilot.ts preflight --gate .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json --allocation .planning/artifacts/v1.38-phase-265-diagnostic-pilot-allocation.json --factory-repository .strategy-lab/factory-264-fresh-20260914-approved-two --repository .strategy-lab/league-265-diagnostic-pilot-20260923-a</automated></verify>
  <done>One new pilot allocation is exact-key, private, rooted and source-gate-bound; read-only preflight proves a safe prospective first start on fresh host/capacity observations or Task 2 remains unrun. The old empirical allocation/result/repository bytes are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Attempt one bounded run, stop at first non-success and verify retained diagnostic evidence</name>
  <files>.planning/artifacts/v1.38-phase-265-diagnostic-pilot-result.json</files>
  <action>Execute only if Task 1's read-only preflight passes and the exact-source gate still matches; `preflight` is never substituted for the run's own checks. Invoke `scripts/run-v1-38-diagnostic-pilot.ts run` at most once with the one new allocation, gate, exact new repository/result and Phase 264 factory path. The parent watchdog captures its monotonic start at command entry before any targeted Phase 264 reader, host check, filesystem check, directory creation or child spawn. Reserve the one-shot result path by exclusive create; any interruption leaves an unmistakable attempted/uncertain marker, so the same allocation cannot be started again. Within that same 1800000-ms clock, revalidate the independent review/gate source closure and old-evidence digest, directly reauthenticate the exact Phase 264 assessed pair without whole-store index or historical 48-workload replay, and obtain fresh Docker/image/isolation, memory, bytes and inodes admission before a pilot repository reservation or provider issuance. For each next ordered cell, require a fresh capacity margin and remaining time at least its 240000-ms cap plus the separately reserved safe parent cleanup/terminal time. Begin the cell timer before the distinct durable pilot precharge; only after that precharge may the private connected-runner adapter issue both host providers and call `runCanonicalLabMatch`/`MATCH_KERNEL`. Parent watchdog preemptively kills a blocked child before the cell/overall caps, verifies exact `v1.38-lean-owner` container labels before force-removing only owned names, enforces bounded terminal publication, and denies all further starts on unknown ownership, timeout, incomplete cleanup, retention failure or any other non-success. Do not use the old `runLeagueCell` ledger, derive a payoff, invoke a solver or treat `benchmarkLifetimeMs` as Match time. The new result must be one of exact private statuses: `prestart_denied` with zero starts/no provider, or `diagnostic_prefix` with at most four charged cells, precise success/failure/uncertain/unused slots, monotonic durations, invocation and byte/record/inode costs, cleanup and allowlisted failure codes. If a crash prevents a complete result, the read-only verifier must classify the attempted marker/start-only evidence as `process_invalid`, not missing success or a retry opportunity. A prefix after one to three starts is explicitly unbalanced; even four successes are one-geometry diagnostics only. Run only the new CLI's `verify-retained`, which validates the result, gate/source closure, separate ledger/repository, every charged/unused cell and the pre/post old JSON/tree digests. A missing result, stale gate, changed old byte, wrong ownership, unknown charge or verifier mismatch fails. Report actual durations and retained costs only; never claim LEAG-01–09, finalist, Phase 266 freeze, formation, holdout, counted/public/production eligibility, or replace/refund/retry/reinterpret the consumed allocation-v2.</action>
  <verify><automated>./node_modules/.bin/tsx scripts/run-v1-38-diagnostic-pilot.ts verify-retained --gate .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json --allocation .planning/artifacts/v1.38-phase-265-diagnostic-pilot-allocation.json --result .planning/artifacts/v1.38-phase-265-diagnostic-pilot-result.json --repository .strategy-lab/league-265-diagnostic-pilot-20260923-a --factory-repository .strategy-lab/factory-264-fresh-20260914-approved-two</automated></verify>
  <done>At most one run has a read-only verified `prestart_denied` or diagnostic-prefix disposition, or its missing/uncertain result is explicitly process-invalid; zero to four starts and zero retries are proven; all three old JSON hashes and both old repository tree digests are identical. Any failed review, preflight, source, host, capacity or verifier gate leaves Plan 09 incomplete and creates no new empirical authority.</done>
</task>

</tasks>

## Source coverage audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Complete current-rules league | Still requires a separately approved and independently verified full process-valid league; this is only a diagnostic prefix. | no pilot claim |
| REQ | LEAG-01–09 | Remain pending; no requirement ID is credited by Plan 09. | no pilot claim |
| RESEARCH | one new allocation, at-most-four S01/S03 Smoke conditions, distinct store/result/verifier | Tasks 1–2 | covered |
| RESEARCH | fresh exact source/host/capacity, hard 240-second/30-minute caps and zero retry | Tasks 1–2 | covered |
| RESEARCH | old-artifact immutability, stop-first-failure, privacy and limited interpretation | Task 2 | covered |
| CONTEXT | D-01–D-06 | rooted fail-closed canonical private supervision, charged failures and no rule/formation/production changes | covered |
| CONTEXT | D-07–D-10 | canonical ordered one-geometry conditions, no matrix admission or retry | covered within pilot scope |
| CONTEXT | D-11–D-21 | Solver/PSRO/red-team/finalist/full-report obligations remain with complete league plans, never this pilot. | excluded from pilot |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| reviewed source to pilot admission | Gate receipt must match current executable/test/boundary bytes before prepare, preflight and run. |
| old allocation/result/repository to diagnostic allocation | Old paths are hashed read-only comparison inputs, never destinations or authority. |
| child Match to parent watchdog and Docker | Parent can preempt a synchronously blocked child and remove only exactly labeled pilot-owned containers within reserved time. |
| private diagnostic result to league/public | Verifier cannot promote a prefix into payoff/LEAG/freeze or reveal source/memory/objectives/raw diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-265-09-01 | Spoofing/Tampering | gate/allocation source | high | mitigate | CLI rehashes exact reviewed source and canonical zero-finding gate on prepare/preflight/run/reopen. |
| T-265-09-02 | Tampering | consumed allocation-v2 | high | mitigate | Exclusive new paths plus pre/post SHA-256 and no-follow old tree digest; old roots never enter pilot authority. |
| T-265-09-03 | Denial of service | cell/run deadline | high | mitigate | Separate monotonic parent watchdog, full-cap admission before each cell, preemption and reserved bounded owned cleanup/terminal writer. |
| T-265-09-04 | Repudiation | start/failure accounting | high | mitigate | One-shot attempted marker, durable pre-issuance charge, append-only terminals/unused slots and independent read-only reopen. |
| T-265-09-05 | Information disclosure | result/private graph | high | mitigate | Mode-0700 store, allowlisted codes and data-only diagnostic metrics, no public/production integration. |
| T-265-SC | Tampering | package installs | low | accept | No package installation is planned. |
</threat_model>

<verification>
Task 1's standalone `preflight` is read-only and grants no authority; it must positively distinguish insufficient first-cell margin from a passing observation. The sole Task 2 `run` repeats every gate and read inside its 1800000-ms command-entry clock. `verify-retained` must return a typed no-start or charged-prefix classification and fail on a missing/uncertain result or any old digest change. If Task 1 cannot show a prospective useful first start, do not invoke `run`; no extra allocation, second attempt, Match, provider, Strategy or model is authorized.
</verification>

<success_criteria>
One separately rooted diagnostic pilot allocation and at most one conditional run are truthfully accounted. Every start and failure is charged and reopens, or a zero-start preflight stop is explicit. Old evidence is byte-identical and no LEAG/current-freeze/formation/holdout/public/counted claim follows. Future full-league feasibility and a valid Phase 266 freeze remain separate operator decisions.
</success_criteria>

<output>
Create `.planning/phases/265-serious-current-rules-league-and-development-red-team/265-09-SUMMARY.md` with gate, allocation, result/verifier and old-digest roots when done.
</output>
