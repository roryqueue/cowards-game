---
phase: 265-serious-current-rules-league-and-development-red-team
plan: "04"
type: tdd
wave: 3
depends_on: [265-01, 265-02, 265-03]
files_modified: [packages/strategy-lab/src/league/solver.ts, packages/strategy-lab/src/league/solver.test.ts, packages/strategy-lab/src/league/psro.ts, packages/strategy-lab/src/league/psro.test.ts]
autonomous: true
requirements: [LEAG-03, LEAG-04]
must_haves:
  truths: ["A solver is chosen only after a synthetic numerical comparison and emits byte-identical exact canonical output for a complete immutable snapshot.", "Each PSRO round freezes mixture and named pure targets, charges every response disposition, and puts accepted late counters into the next snapshot."]
  artifacts:
    - path: packages/strategy-lab/src/league/solver.ts
      provides: frozen deterministic restricted-game solver
    - path: packages/strategy-lab/src/league/psro.ts
      provides: immutable response-round state transitions
  key_links:
    - from: league/solver.ts
      to: league/matrix.ts
      via: CompletePayoffSnapshot root only
      pattern: CompletePayoffSnapshot
    - from: league/psro.ts
      to: league/solver.ts
      via: frozen solver output and named target roots
      pattern: LeagueSolverOutput
  prohibitions: [No float tolerance policy, live partial-cell access, convenient early exit, or human solver-selection checkpoint.]
---

<objective>
Run the required synthetic numeric spike, freeze the selected deterministic solver, and use its immutable result to drive a bounded response loop.

Purpose: resolve algorithm choice with evidence while making the actual league response policy reproducible and complete.
Output: golden-tested exact solver and immutable PSRO/double-oracle transitions.
</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-RESEARCH.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-AI-SPEC.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-02-PLAN.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-03-PLAN.md
@packages/strategy-lab/src/factory/numeric-calibration.ts
@packages/strategy-lab/src/runner.ts

<interfaces>Plan 02 provides an immutable `CompletePayoffSnapshot` with canonical payoff bytes. Plan 01 provides solver manifest/output and round contracts. Plan 03 supplies exact charged terminal/repository semantics for response evidence.</interfaces>
</context>
<feature>
<name>Frozen exact solver and response loop</name>
<files>packages/strategy-lab/src/league/solver.ts, packages/strategy-lab/src/league/solver.test.ts, packages/strategy-lab/src/league/psro.ts, packages/strategy-lab/src/league/psro.test.ts</files>
<behavior>
- Synthetic degenerate, tie, asymmetric, and adversarial numeric matrices compare candidate algorithms under a declared bounded measurement corpus; the accepted one commits algorithm/version, BigInt rational representation, normalization, tie-break, iteration/convergence schedule, and failure codes.
- Golden outputs remain byte-identical through entrant permutation, repeat, worker/shard order, restart, and source-order/tie-break probes.
- A round names the exact snapshot/mixture and strongest/vulnerable pures; every response starts charged and only complete legal, independent, novel, positive evidence creates the next population snapshot.
</behavior>
<implementation>Implement the numeric spike first, select the engineering-delegated solver from its committed result, then persist a manifest and output that consume a complete snapshot only. Implement PSRO transitions after solver outputs exist. Do not claim Nash, exact exploitability, optimality, or convergence beyond the frozen bounded oracle-relative protocol.</implementation>
</feature>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Compare and freeze the deterministic empirical-game solver</name>
<files>packages/strategy-lab/src/league/solver.ts, packages/strategy-lab/src/league/solver.test.ts</files>
<read_first>packages/strategy-lab/src/league/contracts.ts; packages/strategy-lab/src/league/matrix.ts; packages/strategy-lab/src/factory/numeric-calibration.ts; .planning/phases/265-serious-current-rules-league-and-development-red-team/265-AI-SPEC.md</read_first>
<behavior>- A committed synthetic corpus compares at least exact-rational bounded-regret/fictitious-play and exact-rational pivoted restricted-game candidates; selection records observed golden/permutation/boundary results and rejects an inconclusive comparator.
- The selected implementation uses integer half-point payoffs and `bigint` rational arithmetic, canonical entrant ordering, non-negative normalized weights summing to one, frozen tie-breaks, and explicit bounded failure result.
- Identical complete snapshots yield byte-identical output through all listed restart/layout/probe cases, while incomplete snapshots are rejected before solving.</behavior>
<action>Write RED golden vectors first, then implement `runLeagueSolverSpike`, the exact candidate comparison, and `solveLeagueSnapshot`. Select the solver only from the synthetic numeric result and embed its observed algorithm/version/representation/normalization/iteration schedule/tie-break/failure code in a rooted `LeagueSolverManifest`; this is the D-11 delegated engineering decision, not a human checkpoint. Persist golden vectors for degenerate/tied, permutation, adversarial numeric boundary, repeat, worker count, shard order, restart, and source-order/tie-break cases. Freeze exact rational `bigint` arithmetic if the spike selects it; if another compared candidate wins under the committed corpus, freeze that measured choice with the same byte-identity obligations. The solver accepts one complete immutable snapshot root and never reads live cells, changes prior outputs, uses floating tolerance, or makes an optimality claim per D-11, D-12, and D-13.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/solver.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>One measurement-backed exact solver manifest and byte-identical golden output exist before any live league solver invocation.</acceptance_criteria>
<done>LEAG-03 has a tested, frozen solver selection and implementation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Implement immutable PSRO target, admission, and re-entry transitions</name>
<files>packages/strategy-lab/src/league/psro.ts, packages/strategy-lab/src/league/psro.test.ts</files>
<read_first>packages/strategy-lab/src/league/solver.ts; packages/strategy-lab/src/league/contracts.ts; packages/strategy-lab/src/league/repository.ts; packages/strategy-lab/src/factory/fingerprint.ts</read_first>
<behavior>- Each round roots the complete input snapshot, selected mixture, strongest/vulnerable pure targets, solver manifest/output, response allocation, and declared closure rule.
- Accepted legal, independent, non-clone, novel, positive responses create the next population and require a fresh full snapshot; rejected/weak/invalid/duplicate/player/system/unfilled/retried responses remain charged terminal evidence.
- A successful late counter cannot be omitted or used to end the loop before the frozen closure condition.</behavior>
<action>Implement `declareLeagueRound`, `admitLeagueResponse`, and `advanceLeagueRound` against the solver output, factory fingerprint receipts, repository terminals, and required allocation roots. Require both mixture and named strongest/vulnerable pure targets in every round; record a charge-before-work event; admit only complete legal/runtime/provenance, independence/clone, novelty, and positive-response evidence; route every accepted counter to the next population/snapshot identity. Reject an early closure or any attempt to rewrite an earlier snapshot/distribution. Preserve all disallowed or unavailable outcomes as terminal ledger rows per D-05 and D-13 through D-15.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/psro.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>Rounds prove their exact frozen targets and every accepted counter forces the declared response branch.</acceptance_criteria>
<done>LEAG-04 has a complete bounded response-loop state machine.</done>
</task>
</tasks>

## Artifacts this phase produces

- `league/solver.ts`: `runLeagueSolverSpike`, `solveLeagueSnapshot`, and frozen solver-manifest generation.
- `league/psro.ts`: `declareLeagueRound`, `admitLeagueResponse`, and `advanceLeagueRound`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| complete snapshot to solver | Partial or changed evidence could manufacture a distribution. |
| response evidence to next round | A weak, cloned, invalid, or omitted counter could bias loop closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-265-10 | Tampering | solver selection/output | high | mitigate | Synthetic comparator, rooted manifest, exact arithmetic, and byte-golden invariance suite. |
| T-265-11 | Repudiation | response loop | high | mitigate | Round/target/admission roots and charge-first terminals preserve accepted and failed responses. |
| T-265-12 | Integrity | counter re-entry | high | mitigate | Transition tests reject early closure and require fresh next population/snapshot. |
| T-265-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Run exact solver and PSRO Vitest paths and the affected TypeScript build; all inputs are synthetic or injected.</verification>
<success_criteria>The solver decision is measured and frozen before use, and the response loop cannot hide a successful counter or reinterpret an old snapshot.</success_criteria>
<output>Create `.planning/phases/265-serious-current-rules-league-and-development-red-team/265-04-SUMMARY.md` when done.</output>
