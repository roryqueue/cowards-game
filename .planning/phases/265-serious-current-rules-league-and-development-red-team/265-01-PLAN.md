---
phase: 265-serious-current-rules-league-and-development-red-team
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [packages/strategy-lab/src/league/contracts.ts, packages/strategy-lab/src/league/contracts.test.ts, packages/strategy-lab/src/league/identity.ts, packages/strategy-lab/src/league/identity.test.ts]
autonomous: true
requirements: [LEAG-01, LEAG-02]
must_haves:
  truths: ["League evidence has strict private immutable contracts rather than caller-trusted roots or a mutable active result.", "Every league identity is domain-separated and canonical across candidate, population, cell, snapshot, solver, round, allocation, portfolio, and report artifacts.", "Every solver payoff is a rooted entrant-relative integer half-point projection of one completed canonical kernel outcome, never a side-relative score inferred by the solver."]
  artifacts:
    - path: packages/strategy-lab/src/league/contracts.ts
      provides: strict private league schemas, terminal dispositions, and canonical entrant payoff projection
    - path: packages/strategy-lab/src/league/identity.ts
      provides: domain-separated league root derivation
  key_links:
    - from: league/contracts.ts
      to: factory/contracts.ts
      via: immutable Phase 264 candidate and receipt roots
      pattern: FactoryCandidate
    - from: league/contracts.ts
      to: runtime-bridge.ts
      via: completed MATCH_KERNEL outcome plus exact entrant and condition bindings
      pattern: LabMatchExecution
  prohibitions: [No public DTO, production Strategy identity, mutable latest selector, formation artifact, or empirical dispatch is introduced.]
---

<objective>
Define the immutable private league vocabulary and every canonical identity needed by later matrix, solver, runner, response, selection, and report code.

Purpose: make provenance and failure accounting fail closed before any league work can be accepted.
Output: `league/contracts.ts` and `league/identity.ts` with focused contract tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-RESEARCH.md
@packages/strategy-lab/src/contracts.ts
@packages/strategy-lab/src/factory/contracts.ts
@packages/strategy-lab/src/factory/ledger.ts
@packages/strategy-lab/src/factory/identity.ts

<interfaces>
Use `LabRoot`, `labRoot(domain, value)`, and `freezeLabValue` from `@cowards/strategy-lab`; consume Phase 264 `FactoryCandidate`, `FactoryFingerprintEvidence`, `FactoryAttemptStart`, and `FactoryAttemptTerminal` as evidence data, never as an asserted league outcome. `FactoryDisposition` already distinguishes accepted, rejected, invalid, duplicate, legal-but-weak, retried, unresolved, player violation, and system failure.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Establish strict rooted league contracts</name>
<files>packages/strategy-lab/src/league/contracts.ts, packages/strategy-lab/src/league/contracts.test.ts</files>
<read_first>packages/strategy-lab/src/contracts.ts; packages/strategy-lab/src/factory/contracts.ts; packages/strategy-lab/src/factory/ledger.ts; .planning/artifacts/v1.38-pre-search-study-policy.json; .planning/artifacts/v1.38-pre-search-measurement-policy.json</read_first>
  <behavior>- A league population, cell terminal, complete snapshot, solver manifest/output, round, response admission, allocation/attempt, mixture, portfolio, finalist disposition, and report all require exact private fields and a self-consistent root.
  - A candidate is admissible only through Phase 264 candidate, supervision, fingerprint, lineage, tuple, runtime, and provenance evidence; a bare boolean or caller-selected root is rejected.
  - Every charged result retains success, player violation, system failure, rejection, duplicate, weak, invalid, retry, unfilled, and unused disposition evidence; process-invalid and process-valid metagame outcomes remain distinct.</behavior>
<action>Create strict `privacy: "private_offline"` canonical schemas and constructors for immutable `LeaguePopulation`, `LeagueCell`, `LeagueCellTerminal`, `CompletePayoffSnapshot`, `LeagueSolverManifest`, `LeagueSolverOutput`, `LeagueRound`, `LeagueResponseAdmission`, `LeagueAllocation`, `LeagueAttempt`, `LeagueMixture`, `LeaguePortfolio`, `RobustPureDisposition`, and `LeagueReportDescriptor`. Define `projectCanonicalKernelOutcomeToEntrantHalfPoints` as the sole solver-input projection: accept only a completed `LabMatchExecution` whose canonical final outcome and exact Match player IDs agree with the rooted cell entrant, candidate side, condition, and result-event commitment; emit entrant-relative integer half points `2`, `1`, or `0` for win, draw, or loss and root the projection with the cell/outcome/entrant bindings. Test mirrored entrant sides and initial-initiative rows, an asymmetric win for each entrant, and a draw, proving the same canonical outcome maps by entrant identity rather than bottom/top side. Bind each artifact to exact candidate/factory supervision/fingerprint roots, admitted tuple/runtime/request roots, pre-search policy roots, and charged accounting roots; parse unknown data at every boundary and reject missing, stale, contaminated, conflicting, unfilled, or identity-mismatched input per D-01, D-02, D-04, D-05, D-07, D-09, D-10, D-13, D-16, and D-21. Model every policy ceiling and material-dependence bound as a required value in a future immutable Phase 265 allocation, never as a default, and keep process-valid no-finalist distinct from a process failure. Do not create a production schema, public projection, formation namespace, or live candidate/Match/model/human/external action.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/contracts.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>Malformed or caller-claimed provenance, omitted resources, mutable selection, nonterminal charged work, and a side-relative or unrooted payoff cannot produce an admissible league artifact.</acceptance_criteria>
<done>Private league contracts represent all required evidence, rooted entrant-relative half-point payoffs, and three-way process semantics.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Derive domain-separated immutable league identities</name>
<files>packages/strategy-lab/src/league/identity.ts, packages/strategy-lab/src/league/identity.test.ts</files>
<read_first>packages/strategy-lab/src/factory/identity.ts; packages/strategy-lab/src/identity.ts; packages/spec/src/canonical-identity-domains.ts; packages/strategy-lab/src/league/contracts.ts</read_first>
<behavior>- Equal canonical values derive equal roots while a changed semantic field or identity domain changes the root.
- Unordered pairs normalize by immutable candidate identity; display IDs, completion order, worker layout, directory ordering, and seed spelling cannot change identity.
- Root rederivation rejects a mismatched supplied root for each league artifact class.</behavior>
<action>Implement self-root-omitting, domain-separated derivations for population, canonical unordered pair, four-condition row, semantic-arena cell, terminal, snapshot, solver manifest/output, round/target/admission, allocation/attempt, invariance receipt, mixture, portfolio, finalist disposition, and report descriptor. Canonically sort only immutable candidate roots and semantic geometry hashes; preserve a display arena label only as non-identity metadata per D-01, D-07, D-08, D-10, D-11, and D-20. Expose validators that rederive every supplied root and freeze admitted values. Keep exact condition/request identity values rather than inferring initiative from a seed.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/identity.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>All later artifacts have reproducible identities independent of execution layout and reject root substitution.</acceptance_criteria>
<done>League root functions establish the immutable identity spine for Phase 265.</done>
</task>
</tasks>

## Artifacts this phase produces

- `league/contracts.ts`: strict private schemas, constructors, and process-valid/process-failure dispositions.
- `league/identity.ts`: `deriveLeaguePopulationRoot`, `deriveLeagueSnapshotRoot`, `deriveLeagueSolverManifestRoot`, `deriveLeagueRoundRoot`, `deriveLeagueAllocationRoot`, and related canonical identity exports.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 264 evidence to league admission | Immutable candidate and supervision evidence is untrusted until root/schema joined. |
| Future allocation input to private league | Operator-supplied ceilings and channel dispositions must be complete and rooted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-265-01 | Tampering | league contracts and roots | high | mitigate | Exact-key canonical schemas plus rederived domain roots; focused contract/identity tests. |
| T-265-02 | Repudiation | charged ledger input | high | mitigate | Start/terminal and allocation roots are mandatory for every disposition; omitted capacity is represented. |
| T-265-03 | Elevation | candidate provenance | high | mitigate | Admission requires authenticated Phase 264 candidate/supervision evidence rather than a boolean or source claim. |
| T-265-SC | Tampering | package installs | high | mitigate | No package installation is planned; preserve lockfile and workspace-only stack. |
</threat_model>

<verification>Run only the two named contract/identity tests and the strategy-lab TypeScript build.</verification>
<success_criteria>LEAG-01/02 have typed immutable evidence primitives, all D-01–D-05 and D-07–D-10 identity requirements are enforceable, and unallocated empirical capacity cannot be invented.</success_criteria>
<output>Create `.planning/phases/265-serious-current-rules-league-and-development-red-team/265-01-SUMMARY.md` when done.</output>
