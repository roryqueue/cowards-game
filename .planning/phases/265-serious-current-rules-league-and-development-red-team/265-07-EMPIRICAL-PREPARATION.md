# Phase 265 Plan 07 — Prospective empirical preparation

Status: data-only packet preparation, builder historical recheck, normal `prepare-prospective` recheck, and capacity projection complete. Both independent preparation paths returned the same prospective allocation root `sha256:4d8f01f9d9d4a3a04d6e5445fc335a3564a96d31449a88f611d99ddb6cc4c5b5` (canonical raw SHA-256 `17a3a7b9ea45ad2c6b1bbe2f335810e499bf0f592d596aae2662beb148cdda96`), now published at `.planning/artifacts/v1.38-phase-265-allocation.json`. This note is not a capacity receipt, a host-issued provider, a Match terminal, an accepted Strategy, or a frozen league result.

## Reviewed source and historical inputs

- Independently reviewed Task 2 source commit: `25ba6a10cb11ca74bf53738aca5e2396fd974a24`.
- Current implementation root: `sha256:67d8f60e2691582d2d4e7f8d5f7ba52deb5a1ec65784111161f3adeb85ba7049`; source root: `sha256:32465649b2c9727c116a6bb2e661315f0e7eea256db58b7afa421a4acf836fe9`.
- The real data-only historical reader affirmed S01/S03/S05 as three distinct assessed bases. Private canonical base manifest raw SHA-256: `1e0e2b0ba47637be0304b629b93f14b8fafd0bd6f4d1656db33d08a38fabfac8`. The prospective allocation builder and normal preparation command must independently repeat that full reader before run.
- One source/build disclosure dependency is present in the separate Phase 265 response factory repository at `sha256:68d4223c8125c80bab57336e8aacc3b5620b401e54509426481ea1229a8c4854`. It binds reviewed source, implementation, toolchain, exact model settings template, and declared environment, but does not invent unavailable external serving snapshots or independent custody.

## Draft and packet trail

- Eleven target-free request drafts follow the approved 3 tactical / 3 teacher / 5 model schedule. Final v5 draft raw SHA-256: `6eae8d888e70e350d5e1f425b00a1a159aae65b2e1d99252b4350ca2a966519b`.
- Separate reviewer `/root/265_draft_reviewer` accepted every v5 draft with actual per-job clock records in `265-07-PACKET-REVIEW-v5.md`. The v3 review is historical. The v4 review rejected six unintended tactical/teacher prompt-root changes; v4 was not compiled. v5 changes only the five model prompt bytes and corresponding authoring prompt roots, and gives the model the exact v1.19 initiative-field and awareness-grid shapes.
- The final v5 reviewer log is a committed single-operator local review record (raw SHA-256 `e3961a20331271c0c0da928dc89466de7501777b1362e05b9006177d40dcd2ba`). The conversion CLI binds those exact bytes and the exact v5 draft hash before constructing review/role inputs. It does not prove external or cryptographically independent reviewer custody; prior-exposure/conflict fields remain operator assertions.
- Private reviewed packet input raw SHA-256: `2459946f8380b96331d553da4df335e29eb54a29bc41f1ddf3dfbb457545fd05`; participant-role map raw SHA-256: `a84d7de87e4c52785335bc1c59e22a856d71d36510e56b657861a16ae7c51f24`.
- The packet compiler published exactly four content-addressed request/disclosure/provenance/review artifacts per job (44), after validating all eleven jobs and their shared dependency bytes. Private packet-summary raw SHA-256: `cad308333d6d46d2c8b16279eba6b2e8034b7949f1bc5410601e4e52f73390e5`.
- A separate read-only run-preflight risk review found no current deterministic path blocker. It noted that the generic model-authoring preflight does not type-check `settingsRoot` before the model turn. The exact five immutable current requests were checked separately: all have the expected `sha256:` settings root, approved model/provider, executable regular file with execute bit, existing regular 0600 auth file, and absent state/disclosure directories. No auth bytes were read and no model turn was made. This is route-specific validation, not a claim that the generic source path is universally hardened.

## Technical gates still required

1. The builder independently reverified historical S01/S03/S05 bytes, authenticated every packet/role/authoring preflight, and produced the rooted prospective allocation above. This is data-only; it did not reserve the allocation.
2. Normal `prepare-prospective` historical admission independently returned the exact same allocation object/root, then the canonical allocation artifact was published byte-identically to the private builder output. The CLI accepts an unrooted input document, not its already-rooted output. The first invocation with the rooted file refused immediately with `LEAGUE_ALLOCATION_PROSPECTIVE_DOCUMENT` and no evidence read or live work. The exact unrooted body was then derived by removing only `root` and `schemaVersion` (private raw SHA-256 `a130188deeb2d24d96141a027e68eadcd789b76a2fb88d480d8ecb2c9513816e`), and the corrected normal admission passed.
3. The six-category data-only capacity plan was constructed from fully reverified retained trace, diagnostic storage, and historical factory witnesses. Private raw SHA-256: `f3d222f84acbc40064c1749a7407203a2dac3b0f62ca22c987a11b7d945b14e4`. Its logical projection is 127,582,231,362 bytes / 7,237,174 records, with 167,418,829,480 bytes of physical disk projection; the ordinary-pool margins are 12,004,205,758 bytes / 1,562,826 records. This is a projection, not a fresh host observation or a receipt. A preliminary read-only host snapshot showed a positive disk margin but only ~1.9 GiB available memory; it must not be substituted for the fresh same-process preflight.
4. In one `run --capacity-input` process, finish all static checks, observe fresh host memory/disk, admit the capacity receipt, and reserve the allocation only if every frozen margin passes. The result path is exclusively reserved outside all run repositories before invoking that process. Never retry a consumed allocation.
5. Verify the retained result graph without dispatch. A process-valid disappointing league result remains truthful; process/integrity failure cannot become a finalist or unlock Phase 266.

No model call, teacher/tactical producer, Strategy execution, Match, formation materialization, holdout opening, public/counted/production operation, or canonical rules change has been performed by this preparation.

## Capacity preflight refusal and source-lineage repair (2026-09-23)

The first standalone read-only `preflight` completed its full static historical check but refused with `LEAGUE_ALLOCATION_CAPACITY_OBSERVATION`. It emitted no receipt, did not reserve the allocation, and did not dispatch a producer or Match. During the historical scan, macOS reported very few immediately free pages while its separate `memory_pressure -Q` memorystatus reading showed substantial reclaimable headroom. The old CLI used Node `freemem()` for the prospective available-memory observation; treating those immediately free pages as available memory was unsuitable for this Darwin host. No threshold was lowered to pass the gate.

The repaired CLI uses a strict, fail-closed C-locale Darwin memorystatus observation, conservatively floors its whole-percentage estimate to bytes, and retains the selected 1 GiB process-headroom minimum at plan admission, receipt admission, and live dispatch. The 200 ms command uses `SIGKILL` on timeout. The private import boundary permits that host probe only when the league CLI itself is the graph origin; transitive imports from another restricted helper reject. Source review findings and their closures are preserved in the versioned capacity-memory and capacity-boundary review reports. The reviewed final source commit is `4eb48e4d0070551cde3e0f7cb86ba7b46c0ed53a`, with implementation root `sha256:92b40fc585ac087928a477924fa1bc560d309bda29e5fe762151a5a0152bf564` and source root `sha256:945321708ba5e15489fd3de0e2fb90d80425e7b8ba8e89d1ef61569ee99fb3e4`. This is a conservative technical measurement repair under the existing capacity envelope, not a new gameplay/rules or producer budget decision.

The published `v1.38-phase-265-allocation.json` and its private source-bound disclosure, drafts, packets, and capacity plan remain immutable historical preparation for the earlier source root. They are now stale and must not be used for Task 3. A separately versioned disclosure, independently reviewed drafts, packets, capacity witnesses, amendment, allocation, and fresh preflight are required. No old receipt may be refreshed and no consumed allocation may be retried.

The complete final-source named Vitest gate passed on the repaired source: 29 test files and 350 tests, duration 2,067.79 seconds. The strict script typecheck, strategy-lab build, tactical-corpus tests (3/3), and serious-league, lab, factory, and service import boundary checks also passed; the latter three reported no strict or ownership offenses (19 existing report-only service findings). The private v6 source/build disclosure now binds the final reviewed source/implementation roots at `sha256:fa6a3a65ffcc9c653b8b228e2f3ca3b791e0ac21d1c513ed3cc7da0d1e5a3c14`. This is only a source and draft-preparation gate: no new allocation, receipt, provider, or Match exists yet.
