---
phase: 265-serious-current-rules-league-and-development-red-team
plan: "03"
type: tdd
wave: 2
depends_on: [265-01]
files_modified: [packages/strategy-lab/src/league/repository.ts, packages/strategy-lab/src/league/repository.test.ts, packages/strategy-lab/src/league/connected-runner.ts, packages/strategy-lab/src/league/connected-runner.test.ts]
autonomous: true
requirements: [LEAG-01, LEAG-02, LEAG-09]
must_haves:
  truths: ["The connected private runner can execute every admitted cell through the full existing MATCH_KERNEL and a host-issued factory-supervised provider boundary.", "Retained league data can be reopened with exact roots and data-only bounds without renewing execution authority."]
  artifacts:
    - path: packages/strategy-lab/src/league/connected-runner.ts
      provides: supervised full-Match cell execution and terminal retention
    - path: packages/strategy-lab/src/league/repository.ts
      provides: immutable private artifact publication and bounded reopen
  key_links:
    - from: league/connected-runner.ts
      to: scripts/lib/v1-38-factory-supervised-runtime.ts
      via: persisted candidate publication re-admission and host-issued FactorySupervisionProvider
      pattern: createFactorySupervisedRuntime
    - from: league/connected-runner.ts
      to: runtime-bridge.ts
      via: issued provider only, then runCanonicalLabMatch
      pattern: runCanonicalLabMatch
  prohibitions: [No coordinator source evaluation, copied transition loop, public output, formation artifact, or direct historical runner use.]
---

<objective>
Provide the executable private retention and connected-runner path which consumes Phase 264 candidates as data and uses the admitted kernel/runtime boundary for every Match.

Purpose: a complete source implementation can be operated after allocation without bypassing hostile-source, ledger, or reopen protections.
Output: immutable league repository and tested supervised connected runner.
</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-08-SUMMARY.md
@.planning/phases/264-immutable-factory-independent-oracles-and-quarantined-intake/264-VERIFICATION.md
@packages/strategy-lab/src/runtime-bridge.ts
@packages/strategy-lab/src/factory/admission.ts
@packages/strategy-lab/src/factory/repository.ts
@packages/strategy-lab/src/factory/supervision-artifacts.ts
@packages/engine/src/kernel/driver.ts

<interfaces>`runCanonicalLabMatch({ match, providers })` is the sole implementation-facing Match executor; it validates provider revision/tuple/image/runtime identity and drives `MATCH_KERNEL.createMachineV119` and `stepMatch`. A reopened factory supervision artifact is explicitly `issued: false`; it is evidence only. The trusted host must recover exact candidate publication/source/packet/proposal/validation records, invoke the existing `admitFactory` plus `authorizeFactorySupervision` path, and create the nonserializable `FactorySupervisionProvider` through `createFactorySupervisedRuntime` before `runCanonicalLabMatch` receives it.</interfaces>
</context>
<feature>
<name>Supervised private league execution and reopening</name>
<files>packages/strategy-lab/src/league/repository.ts, packages/strategy-lab/src/league/repository.test.ts, packages/strategy-lab/src/league/connected-runner.ts, packages/strategy-lab/src/league/connected-runner.test.ts</files>
<behavior>
- A cell begins with a charged root before validation or runtime work; all success, player violation, system failure, cleanup failure, retry, and non-dispatch disposition paths publish one immutable terminal.
- The host, not a caller, re-admits a persisted candidate publication and uses the existing factory-supervised runtime adapter to issue a nonserializable provider whose candidate/source/executable/runtime/identity bindings are exact before the runner maps `runCanonicalLabMatch` evidence into a league terminal.
- Reopen validates roots and bounds, returns private data only, and cannot issue another Match.
</behavior>
<implementation>Use factory publication patterns for bounded canonical chunks/descriptors. Reuse the existing host-owned `scripts/lib/v1-38-factory-supervised-runtime.ts` machinery and factory admission functions; a persisted candidate root is source data for a fresh host issuance, never a serializable provider capability. Tests use an injected host runtime constructor behind that existing adapter, rather than accepting a caller-created provider, and do not consume empirical allocation or produce a candidate claim.</implementation>
</feature>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Implement immutable league evidence storage and read-only reopening</name>
<files>packages/strategy-lab/src/league/repository.ts, packages/strategy-lab/src/league/repository.test.ts</files>
<read_first>packages/strategy-lab/src/factory/repository.ts; packages/strategy-lab/src/factory/supervision-artifacts.ts; packages/strategy-lab/src/league/contracts.ts; packages/strategy-lab/src/league/identity.ts</read_first>
<behavior>- Publishing the same bytes is idempotent only when its digest agrees; tampering, overwrite, missing chunks, or descriptor/root mismatch fails.
- Start records precede terminal records and uncertain/nonterminal work reopens as charged process failure rather than disappearing.
- Bounded reopen returns descriptors/records with `issued: false` and no dispatch capability.</behavior>
<action>Create `LeagueRepository`, publish-once artifact/chunk/descriptor methods, charge-first start/terminal methods, and bounded `reopenLeagueEvidence` using content-addressed canonical bytes. Retain every allocation row and terminal state including unused capacity, retries, invalid output, rejected or weak response, player failure, system failure, and cleanup failure per D-01, D-02, D-05, D-09, and D-10. Copy neither source nor memory/objective payload into safe records; source-private evidence remains in existing private factory artifacts referenced only by roots.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/repository.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>All retained private league evidence is immutable, completeness-aware, bounded on reopen, and lacks a path that can start work.</acceptance_criteria>
<done>Connected execution has a durable charge and retention substrate.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Execute a complete cell only through the supervised full-kernel bridge</name>
<files>packages/strategy-lab/src/league/connected-runner.ts, packages/strategy-lab/src/league/connected-runner.test.ts</files>
<read_first>packages/strategy-lab/src/runtime-bridge.ts; packages/strategy-lab/src/factory/admission.ts; packages/strategy-lab/src/factory/repository.ts; packages/strategy-lab/src/factory/supervision-artifacts.ts; scripts/lib/v1-38-factory-supervised-runtime.ts; packages/strategy-lab/src/league/repository.ts; packages/engine/src/kernel/driver.ts</read_first>
<behavior>- A persisted candidate publication root, exact factory source closure, and complete Phase 265 start are re-admitted by the trusted host into an issued `FactorySupervisionProvider`; only that issued capability reaches `runCanonicalLabMatch`, retains canonical transition/accounting/trace commitments, and terminalizes one exact cell.
- Provider identity mismatch, malformed runtime evidence, player violation, system failure, and cleanup failure each remain a charged terminal and cannot enter a payoff snapshot.
- Reopened factory records report `issued: false` and cannot be passed to `runLeagueCell`; forged source closure, executable, runtime, provider-identity, or allocation/start bindings fail before any kernel step.</behavior>
<action>Implement `issueLeagueProviderFromFactoryCandidate` and `runLeagueCell`. The issuer runs only in the host-owned CLI/runtime process: read the persisted candidate-publication descriptor and exact source/packet/proposal/validation artifacts from `FactoryRepository`; verify the candidate root, publication roots, source bytes hash, selected revision/executable identity, tuple/image/runtime limits, Phase 265 allocation root, and charged league-start root; re-run `admitFactory` and `authorizeFactorySupervision`; then call the existing `createFactorySupervisedRuntime` with the exact admitted source and start/budget roots. Mark the resulting `LeagueIssuedProvider` in a module-private `WeakSet` and bind its factory authorization, candidate root, source root, executable root, runtime identity, allocation, and start before allowing `runLeagueCell` to unwrap it. `runLeagueCell` must require two such host-issued capabilities, validate the rooted population/cell/condition/arena/request tuple, call only `runCanonicalLabMatch`, bind canonical full-Match outcome, trace, accounting, and cleanup commitments to the precharged terminal, and persist through `LeagueRepository`. Reopen stays data-only and never calls the issuer. Tests must prove a persisted candidate-to-host-issued-provider handoff, source-closure and identity rejection, `issued: false` reopen rejection, cleanup behavior, and no caller-created `LabSupervisedProvider` acceptance. Do not reconstruct legality, schedule activations, dynamically load source in the coordinator, or translate a system failure into a game score per D-03, D-04, D-05, D-06, D-09, and D-20.</action>
<verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/connected-runner.test.ts packages/strategy-lab/src/runtime-bridge.test.ts packages/strategy-lab/src/factory/admission.test.ts packages/strategy-lab/src/factory/supervision-artifacts.test.ts scripts/lib/v1-38-factory-supervised-runtime.test.ts &amp;&amp; ./node_modules/.bin/tsc -b packages/strategy-lab/tsconfig.json --pretty false</automated></verify>
<acceptance_criteria>Every executable cell has a persisted-candidate-to-host-issued-provider-to-full-kernel path, while reopened data and caller-created provider claims cannot grant execution; every failure stays charged and non-scorable.</acceptance_criteria>
<done>The Phase 265 private runner has a source-closed, host-issued factory supervision handoff, is retainable, and is reopenable without authority renewal before any live allocation is approved.</done>
</task>
</tasks>

## Artifacts this phase produces

- `league/repository.ts`: `createLeagueRepository`, immutable publications, charged terminals, and `reopenLeagueEvidence`.
- `league/connected-runner.ts`: `issueLeagueProviderFromFactoryCandidate` host issuance and `runLeagueCell` full-Match supervision adapter.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted candidate publication to host provider issuance | Reopened data must be source-closed and re-admitted by the host, but must not become caller-held execution authority. |
| runtime result to league terminal | Malformed or incomplete runtime evidence must not become a payoff. |
| artifact reopen caller to retained records | Reopening is data inspection, not execution authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-265-07 | Elevation | connected runner | high | mitigate | Reuse the factory admission and `createFactorySupervisedRuntime` issuer; WeakSet-issued capability, source closure, provider identity, and reopen-denial tests prevent caller-provider authority. |
| T-265-08 | Tampering | retained terminals | high | mitigate | Charge-first roots, immutable publication, digest verification, tamper/reopen tests. |
| T-265-09 | Information disclosure | reopen projection | high | mitigate | Bounded data-only reopen and root-only links to private source/memory/objective evidence. |
| T-265-SC | Tampering | package installs | high | mitigate | No package installation is planned. |
</threat_model>
<verification>Run the exact repository, connected-runner, runtime-bridge, factory admission/supervision-artifact, and existing factory-supervised-runtime test paths plus strategy-lab typecheck.</verification>
<success_criteria>Phase 264 evidence is consumed without being relabeled as Phase 265 runtime evidence; only a fresh host issuance from exact persisted candidate closure can reach the allocation-gated CLI.</success_criteria>
<output>Create `.planning/phases/265-serious-current-rules-league-and-development-red-team/265-03-SUMMARY.md` when done.</output>
