# Phase 265 diagnostic pilot plan check — iteration 2: BLOCK

**Checked:** 2026-09-23. **Plans:** revised 265-08 and new 265-09. This is plan/source inspection only; no Strategy, provider, Match, model, allocation, result or old evidence was invoked or written. The exact operator ceiling remains four private current-rules Matches, 240000 ms per Match including cleanup, 1800000 ms from `run` command entry, and zero retries.

## Prior findings disposition

| Prior finding | Iteration-2 status | Evidence |
|---|---|---|
| Opaque provider cannot reach canonical bridge | **Still BLOCKED, narrowed below** | 08 now owns `connected-runner.ts` and plans `runDiagnosticPilotCell`, but does not plan the matching pilot-specific *issuance* API needed to populate its private WeakMap from a diagnostic start. |
| Whole-store historical import may consume clock | Addressed in plan | 08 Task 1 freezes exact Phase 264 assessment, threshold, S01/S03 publication/source/supervision/packet/proposal/validation and charged attempt roots, explicitly rejects `indexFactory`/48-workload replay; Task 2 measures targeted real-store latency and 09 repeats it inside the command-entry clock. The named roots were found in the retained Phase 264 store and committed allocation-v2 as historical locators. Actual latency remains an execution gate, not an assumption of success. |
| Same-thread timeout cannot preempt blocked work | Addressed in plan | 08 Task 2 puts synchronous transport/kernel/fsync in a child while an independent parent sets absolute monotonic deadlines, kills its process group, verifies exact Docker owner label/name and uses bounded cleanup/terminal reserve. If finite reserve cannot be proved, dispatch is denied. This is architecturally capable of enforcing the ceiling, subject to source tests/review. |
| Review/gates not bound to executable route | Addressed in plan | 08 Task 3 requires a different named reviewer, zero findings, exact reviewed executable/test/boundary source closure and all source-gate exits in `265-08-PILOT-GATE.json`; `check-gate`, `prepare`, `preflight` and `run` rehash it. The source closure excludes review/gate/allocation/result outputs, so no self-root is introduced. 09 depends on 08 before any allocation or run. |
| False LEAG-02 metadata | Fixed | Neither diagnostic plan claims a LEAG requirement; both explicitly preserve all LEAG-01–09 and Phase 266 gates as pending. |
| Wave/dependency mismatch | Fixed | 08 is Wave 5 after completed 06; 09 is Wave 6 and depends on 08. The process-invalid 07 route is historical input, not a success dependency. |
| Oversized mixed source-and-live plan | Fixed | 08 is source/test/review only; 09 alone creates a new allocation and conditionally runs once. |
| Post-run-only verification | Fixed | 08 has source-only `check-gate`; 09 has read-only `preflight` and distinct `verify-retained` classifications for a no-start denial, charged prefix and invalid/missing evidence, including three old JSON hashes and both old repository tree digests. |

## Remaining blocker

**BLOCKER — key_links_planned / cross-plan data contract: no pilot-specific issuer is planned for the new WeakMap-backed adapter.** 08 Task 1 creates a distinct `diagnostic_only` cell/start/terminal ledger and says `runDiagnosticPilotCell` will consume issued handles from `connected-runner.ts`'s `privateProviders` WeakMap without writing any legacy `LeagueCellStart` or `LeagueCellTerminal`. But the only existing function that populates that WeakMap, `issueLeagueProviderFromFactoryCandidate`, requires a `LeagueCell` parsed by `LeagueCellSchema` and a `LeagueCellStart` whose `cellRoot`/`allocationRoot` match. Its host request binds `attemptRoot` to that legacy start root. The plan does not name a pilot-specific issuer, overload or discriminated shared issuance contract. As written, the executor must either synthesize a legacy league cell/start (contradicting the distinct-domain requirement), bypass the trusted issuance/WeakMap boundary, or leave `runDiagnosticPilotCell` with no provider to unwrap. Any of those defeats the stated plan-to-source link.

**Precise fix:** In 08 Task 1, name and specify a narrow `issueDiagnosticPilotProviderFromFactoryCandidate` (or equivalent discriminated pilot branch) in `connected-runner.ts`. It must accept only an admitted pilot allocation, exact diagnostic cell and *already durable* diagnostic start/precharge plus authenticated Phase 264 closure; derive `attemptRoot` from that diagnostic start, bind `budgetRoot` to the pilot allocation, authorize the factory supervisor, insert the actual provider into the same private WeakMap under a non-serializable pilot-issued handle, and reject legacy/old allocation or forged handles. `runDiagnosticPilotCell` must accept only those pilot handles, require both issued handles to match the precharge/cell/condition/request/tuple/runtime/revision identities, then call `runCanonicalLabMatch` without `runLeagueCell` or legacy journal writes. Add an injected test showing both pilot issuances happen strictly after the new-domain durable charge and that legacy-issued handles cannot enter the pilot adapter. Include the new issuer/adapter behavior in the independent source review and exact-source gate. This stays inside 08's existing `connected-runner.ts` ownership and does not increase the approved empirical envelope.

## Residual execution conditions, not new plan blockers

- The measured targeted reader latency, physical-byte/record/inode formula, finite cleanup reserve and fresh Docker/host admission must actually pass before 09 starts. The plan now fails closed and seeks technical review if they do not; it does not assert that the current host can complete four Matches.
- The parent must set each cell's absolute deadline **before** allowing the child to enter precharge/issuance and derive both provider container names/owner label independently, so a blocked child cannot postpone the 240-second clock or hide an owned container. 08's “parent absolute deadlines” and precharge-before-issuance contract are sufficient plan hooks; the independent review and injected blocked-path tests must verify this exact ordering.
- The currently edited ROADMAP inventories 07 as consumed and 08/09 as diagnostic supplements. Per `AGENTS.md`, updated planning docs must be committed at the normal handoff; the review itself commits nothing.

## Disposition

**BLOCK — one blocker remains.** Revise 08 Task 1 to specify the pilot-specific issuance contract, then recheck both plans before source execution. All five old-artifact paths stay read-only; neither plan grants LEAG, freeze, formation, holdout, counted, public or production authority.
