---
phase: 265-serious-current-rules-league-and-development-red-team
plan: "08"
type: execute
wave: 5
depends_on: [265-06]
files_modified:
  - packages/strategy-lab/src/league/diagnostic-pilot.ts
  - packages/strategy-lab/src/league/diagnostic-pilot.test.ts
  - packages/strategy-lab/src/league/connected-runner.ts
  - scripts/lib/v1-38-factory-supervised-runtime.ts
  - scripts/lib/v1-38-planner-supervised-runtime.ts
  - scripts/run-v1-38-diagnostic-pilot.ts
  - scripts/run-v1-38-diagnostic-pilot.test.ts
  - scripts/check-v1-38-diagnostic-pilot-boundaries.ts
  - .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-SOURCE-REVIEW.md
  - .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json
autonomous: true
must_haves:
  truths:
    - "A new diagnostic-only allocation/ledger/verifier and narrowly scoped connected-runner adapter are source-complete, but no allocation, provider, Strategy or Match is created by this plan."
    - "The authenticated S01/S03 Phase 264 assessment and closure can be reopened by exact roots without whole-store indexing or 48-workload historical replay; measured source-only latency leaves a conservative first-cell admission margin or fails closed."
    - "An independent parent-process monotonic watchdog can preempt a blocked child Match/transport/publication and boundedly clean only containers bearing the pilot's exact ownership label before either 240000-ms cell or 1800000-ms overall deadline; same-thread timers and after-checks are not the deadline boundary."
    - "A machine-readable zero-finding review and full-gate receipt binds the exact executable/test/boundary source bytes; the pilot command rejects an absent or stale receipt before any reservation or provider issuance."
  artifacts:
    - path: packages/strategy-lab/src/league/diagnostic-pilot.ts
      provides: pilot-only contracts, targeted historical authentication, capacity formula, ledger and verifier
    - path: packages/strategy-lab/src/league/connected-runner.ts
      provides: private WeakMap-backed pilot adapter to canonical Match execution after distinct durable precharge
    - path: scripts/run-v1-38-diagnostic-pilot.ts
      provides: parent watchdog and private child route, pure preparation, source-only reader probe and read-only checks
    - path: .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json
      provides: exact-source independent review and automated gate receipt, with no empirical authority by itself
  key_links:
    - from: packages/strategy-lab/src/league/diagnostic-pilot.ts
      to: packages/strategy-lab/src/league/connected-runner.ts
      via: authenticated diagnostic precharge and immutable cell/allocation identity accepted only by the private issued-provider adapter
    - from: packages/strategy-lab/src/league/connected-runner.ts
      to: packages/strategy-lab/src/runtime-bridge.ts
      via: privateProviders WeakMap unwrap inside connected-runner and runCanonicalLabMatch/MATCH_KERNEL only
    - from: scripts/run-v1-38-diagnostic-pilot.ts
      to: .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json
      via: executable exact-source/reviewer/exit-code admission before preparation or run
---

<objective>
Build and independently source-review the isolated post-failure diagnostic pilot route. It cannot dispatch in this plan. Plan 265-09 alone may create one new allocation and conditionally invoke the route under the operator's four-Match, 240-second-per-Match, 30-minute-overall, zero-retry envelope.

Purpose: close the opaque-provider, historical-reader, hard-deadline and exact-source gate gaps without using or amending consumed allocation-v2.
Output: source, focused injected tests, source-only admission latency, independent review and a machine-verifiable gate receipt.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-CONTEXT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-07-POSTFAILURE-ROUTE-ASSESSMENT.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-DIAGNOSTIC-RESEARCH.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PLAN-CHECK.md
@.planning/phases/265-serious-current-rules-league-and-development-red-team/265-07-PLAN.md
@.github/workflows/ci.yml
@packages/strategy-lab/src/league/connected-runner.ts
@packages/strategy-lab/src/league/contracts.ts
@packages/strategy-lab/src/factory/repository.ts
@packages/strategy-lab/src/factory/supervision-artifacts.ts
@packages/strategy-lab/src/runtime-bridge.ts
@scripts/assess-v1-38-factory-independence.ts
@scripts/run-v1-38-serious-league.ts
@scripts/lib/v1-38-lean-container-match-session.ts

<interfaces>
Plan 265-07 Task 2 source and its retained process-invalid failure are historical prerequisites, not a successful execution dependency or fresh authority. Plan 08 has no files_modified overlap with Plan 07; Wave 5 follows completed Plan 06 Wave 4. The operator-approved pilot satisfies no LEAG-01–09 requirement. Neither Plan 08 nor its source-only commands may create a pilot allocation, repository reservation, provider, Strategy, Match, model call, formation or holdout artifact. The old allocation `.planning/artifacts/v1.38-phase-265-allocation-v2.json` has root `sha256:5ac794324ed031ced8ff3a2c09b6389daa65029ed126ee595c37cda433d2585b`; old result `.planning/artifacts/v1.38-phase-265-run-result.json` has process-invalid head `sha256:67d9b53e300a95893931b8457678bf500a6724b713334e823ee5fa0b36594534`; old repository is `.strategy-lab/league-265-current-rules-20260922`. They are immutable historical comparison inputs, never pilot authority or write targets. The new pilot store and result are reserved for Plan 09 at `.strategy-lab/league-265-diagnostic-pilot-20260923-a` and `.planning/artifacts/v1.38-phase-265-diagnostic-pilot-result.json`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authenticate the exact assessed pair and connect a separate precharged pilot adapter</name>
  <files>packages/strategy-lab/src/league/diagnostic-pilot.ts, packages/strategy-lab/src/league/diagnostic-pilot.test.ts, packages/strategy-lab/src/league/connected-runner.ts</files>
  <behavior>
    - New strict `diagnostic_only` allocation admits only S01/S03, Smoke semantic hash `sha256:39aecc22c184660c1c08ab810fbfa3066da1a650b20e91d72a838ed7fb70a0e1`, seed `league-265-postfailure-pilot-20260923-a`, and `createSetScenarioV137`'s four ordered IDs recorded in 265-08-DIAGNOSTIC-RESEARCH.md; old allocation/head cannot parse as pilot.
    - Targeted admission reads exact authenticated Phase 264 assessment/threshold, two publication/source/supervision/packet/proposal/validation artifacts and two named attempt start/terminal pairs. It does not call `indexFactory`, `readLeagueInitialCandidates`, `readRetainedFactoryLedger`, `verifyHistoricalFactoryAssessmentForLeague`, `importAssessedFactoryCandidate`, or a whole-store replay.
    - The pilot-specific issuer accepts only the admitted pilot allocation, exact diagnostic cell, an independently reopened already durable diagnostic start and authenticated Phase 264 candidate closure. It binds runtime `attemptRoot` to that pilot start and `budgetRoot` to that pilot allocation; precharge precedes each of the two provider issuances.
    - The pilot adapter accepts only the two matching nonserializable pilot-issued handles, never legacy-issued or forged handles. Only `connected-runner.ts` resolves real providers through its private WeakMap, validates candidate/revision/runtime/tuple/condition/request bindings, and calls `runCanonicalLabMatch`. It neither writes legacy `LeagueCellStart`/`LeagueCellTerminal` nor projects a payoff.
  </behavior>
  <action>Implement exact-key `createDiagnosticPilotAllocation`/`admitDiagnosticPilotAllocation`, a distinct pilot cell/start/terminal ledger and read-only verifier under new domain tags, and `readDiagnosticPilotAssessedPair` in `diagnostic-pilot.ts` per D-01–D-05 and D-07–D-10. Freeze the independent Phase 264 assessment artifact root `sha256:25913b26fa81fa15177774fbdcf9c0d1ef244ad13910bc664bfde4ea8c2e43f8`, assessment root `sha256:0446fef49598ef425c883774adb23159ade4b1e44f630463a72562777a9ecea1`, threshold artifact root `sha256:f6098c9e14ed868e162a9374557e518678996b619f3f8912fb8723113328fa72`, producer implementation root `sha256:5baaeb677327a6102fd3dc719543686b14448122a91a4bca320cd0836cf5040b` and assessment implementation root `sha256:6a6094089e6714def26c427f60dfc0fae15e534cc685ad7a76dc883c4911b97c`. For S01 bind publication `sha256:248a48e285a6f15da53ade90b1d8a66200fd35a33c46ce716017209eefd0ea9b`, source `sha256:3a49f15d3b0164e25106e44bd27f1e33c11a13bf0bfd6410c85e494dead823e2`, supervision `sha256:80d17a2c7beebb758bb14eacd2dc9318d7d7b884363754e67510823bdda9ecf7`, packet `sha256:ec8efeae5c37bfe5faf426dc02f04804a7f793148ddd55c45071f9edc74b6aa2`, proposal `sha256:1457f102aa1c0c724ff095397d11e7d566aed85e0b49dff8d87407b4afbde305`, validation `sha256:5f3d9d3868ffe17dd0cc75fe9ac132bb9b9ff608d508ffaf10211806f02a341c`, charged start `sha256:3fd1884a7c9a06f60cfce5d650890d97fcadcc502581cab2abf2b506cba98cb5` and terminal `sha256:d05e72e01e96c050d346042b234f8b2b8c77b54ba09d69a80a359c4d438667a1`; for S03 bind publication `sha256:b53b00a5e92f0f696f40b20453a2821596f65be6fdc561d2b8bc44f351cf8de8`, source `sha256:19126911caf193808c53de111576986e80b26d9c2109dcb3d04edd3344b5e39f`, supervision `sha256:de70fc054600a04d26f7278811f8996d666d36a0b51233b19d7535137198abd6`, packet `sha256:0fd00718595ddd325aa622a02ad5cc0a28d59735b9e8e0f9fc1f2ef78b6a760e`, proposal `sha256:d749328e2d42c9020725d5d0af191dead719a2c03e10371080bfc06eaf210513`, validation `sha256:4b89fea76498adafd18b1cf032099d94879dfdce371803bddda6c032f03b8c9a`, charged start `sha256:eab3be22248697ea9ad5721184289fc2bb8ed8ff63fe51a1e8e323c14f88be22` and terminal `sha256:72b7d680827447a4ec3f761980dc7714976cdda0751df234510d66a5b0a9c5e6`. The old run-start provided locator values only; authenticate every Phase 264 file by direct byte hash, canonical schema/domain root, affirmative assessment and threshold membership, exact start/terminal/supervision joins and candidate admission root (S01 `sha256:850d8c03d00b6dd8791a68403801e55c37fcaf6f6f2f9c31105b782cbf9f26f5`; S03 `sha256:f5cd002a1cece02fb4f9a63ea1d952354dd57558307be2304326cd806274a774`). Never accept the old allocation as admission authority. Freeze pilot read caps from exact targeted descriptor and source-enforced bounds, not old full-league limits; reject unknown extra reads, symlinks, mismatches or unbounded traversal. In `connected-runner.ts`, add `runDiagnosticPilotCell` as the sole private composition point: it checks a durable pilot precharge against new ledger, exact pilot allocation/cell/start/request roots, both issued-provider WeakMap entries and revision identities, invokes `runCanonicalLabMatch` with hidden providers, returns bounded private execution/cleanup status, and creates no legacy cell journal, payoff or solver input. Add injected tests for both issuance orders, precharge-before-first-and-second-issuance, forged issued handles, changed closure/root, old allocation rejection, start-only uncertainty, failure privacy and no full-league dispatch.</action>
  <pilot_issuer_contract>Implement `issueDiagnosticPilotProviderFromFactoryCandidate` in `connected-runner.ts` as the only pilot issuance entry point. Require an admitted diagnostic pilot allocation, the exact allocation-bound diagnostic cell and request, an already durable diagnostic start authenticated by read-only reopen of the new ledger, and the exact Phase 264 assessed-candidate closure from `readDiagnosticPilotAssessedPair`. Validate allocation/cell/start/closure/revision/runtime/tuple/condition/request joins before calling `authorizeFactorySupervision` or `host.createFactorySupervisedRuntime`; reject the consumed legacy allocation and any legacy `LeagueCell`/`LeagueCellStart` as authority. Pass `attemptRoot` equal to the authenticated pilot start root and `budgetRoot` equal to the admitted pilot allocation root. Store each real `FactorySupervisionProvider` only in `connected-runner.ts`'s private `privateProviders` WeakMap beneath a fresh opaque nonserializable pilot handle branded by a pilot-only private WeakSet and binding record; never expose the provider or accept a structurally imitated handle. `runDiagnosticPilotCell` must require two matching pilot-branded handles whose binding records join the exact current allocation/cell/start and opposite seats; reject `issueLeagueProviderFromFactoryCandidate` handles even when their visible fields match, plus forged, stale, cross-cell and swapped-seat handles before canonical Match entry. In injected tests, log durable precharge before the first and second issuer calls in either issuance order; prove neither issuer or adapter accepts absent/uncertain precharge, legacy-issued handles or forged handles, and prove provider creation never occurs on those rejections. These tests execute no real provider, Strategy or Match.</pilot_issuer_contract>
  <verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 packages/strategy-lab/src/league/diagnostic-pilot.test.ts packages/strategy-lab/src/league/connected-runner.test.ts</automated></verify>
  <done>The targeted reader authenticates S01/S03 from Phase 264 without the 48-workload replay; only a distinct durable pilot charge opens the WeakMap-backed canonical Match adapter; all output remains diagnostic-only and no test dispatches a real provider or Match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scoped supervisor lifetime and a preemptive parent watchdog without live execution</name>
  <files>scripts/lib/v1-38-factory-supervised-runtime.ts, scripts/lib/v1-38-planner-supervised-runtime.ts, scripts/run-v1-38-diagnostic-pilot.ts, scripts/run-v1-38-diagnostic-pilot.test.ts, scripts/check-v1-38-diagnostic-pilot-boundaries.ts</files>
  <behavior>
    - Both supervised lifetime gates accept at most 240000 ms only with a non-serializable authenticated grant bound to the exact diagnostic pilot allocation/cell. Ordinary lifetime remains 120000 ms, benchmark mode unchanged and the per-method executor timeout 1000 ms.
    - A parent process starts the monotonic 1800000-ms clock at the `run` command entry, watches a separate child which owns all synchronous historical reading, Docker stream/control, canonical Match and fsync/publication work, and preemptively terminates blocked work before cell and overall limits minus reserved cleanup/terminal time.
    - The parent removes only exact-name containers whose `v1.38-lean-owner` label equals the pilot cell's independently derived owner label. Failed/unknown ownership or cleanup is process-invalid and bars another start. A safe no-start admission stop differs from a missing result.
  </behavior>
  <action>Per D-03, D-04 and D-06, add a pilot-only non-serializable lifetime grant passed through `createFactorySupervisedRuntime` to `createPlannerSupervisedRuntime`; both independently validate the grant's exact allocation/cell binding and ceiling, never reuse `benchmarkLifetimeMs`, and preserve the 24800-invocation cap, ABI, 2-CPU/256-MB image/isolation profile, memory/source/output limits and 1000-ms method timeout. Build the new CLI with pure `prepare`, read-only `measure-targeted-reader`, `check-gate`, `preflight`, `verify-retained`, and one guarded `run`; it must have no import/call edge to `runSeriousLeague` or old allocation admission. `preflight` is a non-authorizing observation which `run` repeats inside its own clock. `run` is a nonblocking parent watchdog: capture `performance.now()` before spawning the private worker; pass only exact pilot identifiers/IPC and keep the parent event loop free of synchronous transport, assessment replay, publication and fsync. The worker performs static/gate/provenance checks and each cell under the parent's absolute deadlines. Begin each 240000-ms cell window before its distinct durable precharge and provider issuance; require remaining global time to cover the full next-cell cap plus separately reserved cleanup/terminal time. Parent watchdog kills the worker process group at cell execution deadline minus a frozen cleanup/publication reserve and at overall deadline minus its own reserve; then asynchronously inspect exact container name and `v1.38-lean-owner` label, kill/remove only matching owned containers with bounded child-process timeouts, verify absence, and publish an allowlisted diagnostic timeout/uncertain terminal in a bounded separate writer. If publication cannot be completed within its reserve, preserve the durable precharge/start-only state and classify the result process-invalid on read-only reopen; never call it a success. Source-enforce the reserve from both providers' existing 2000-ms stream-close/remove/inspect bounds plus parent kill, terminal-writer and filesystem-timeout bounds; if no finite safe reserve can be established, deny dispatch. The watchdog—not an AbortSignal or after-the-fact check—must remain able to fire during blocked `Atomics.wait`, `spawnSync`, kernel loop or fsync. Use the existing `runCanonicalLabMatch` unchanged inside Task 1's private adapter; do not modify engine/rules, runtime ABI or general container session. Derive pilot-only artifact byte/record/inode ceilings from source-enforced 24800 invocations per provider, 262144-byte output limit, 1010000-step kernel maximum, bounded canonical record/chunk/descriptor and atomic temporary-file costs; add an explicit pilot-local failure cap wherever an upper bound is absent, and reserve terminal and cleanup space before a charge. Expand the new diagnostic boundary checker from the existing AST/import graph to scan the new CLI/module/adapter and all product/deployment roots; forbid old full-league dispatch, direct hostile source, public/counted/formation/holdout edges and unsafe projection fields. Inject fake clocks/processes/Docker/FS for blocked host exchange, blocked graph publication, cleanup timeout, reviewer-gate mismatch, capacity/inode failure and zero real-provider/Match execution. Run `measure-targeted-reader` source-only against the actual Phase 264 store in separate fresh processes and repeated reads; record elapsed measurements without claiming a cold-cache guarantee, the maximum plus explicit conservative margin, and proof that the actual `run` repeats this work inside its 30-minute clock. A measurement exceeding the first-cell 240000-ms-plus-cleanup feasibility window is a no-dispatch finding, not permission to move reading outside the clock.</action>
  <verify><automated>./node_modules/.bin/vitest run --maxWorkers=1 scripts/run-v1-38-diagnostic-pilot.test.ts scripts/lib/v1-38-factory-supervised-runtime.test.ts scripts/lib/v1-38-planner-supervised-runtime.test.ts</automated></verify>
  <done>Injected tests show parent-process preemption and owned-container cleanup for blocked synchronous paths; both 240-second pilot lifetime gates and ordinary 120-second/one-second limits are proved; the targeted real-store source-only latency is measured without an allocation, provider or Match.</done>
</task>

<task type="auto">
  <name>Task 3: Obtain independent exact-source review and produce an executable source-gate receipt</name>
  <files>.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-SOURCE-REVIEW.md, .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json</files>
  <action>Freeze a canonical ordered hash list for all eight production/test/boundary files modified above plus the exact relevant workspace lockfile and CI gate definition; exclude this review, gate receipt and any later allocation/result to avoid a self-hash. Request a phase-level source-only review by a named independent agent other than the source implementer; record reviewer ID, author ID, exact source-closure root, threat/decision checks, findings and zero-actionable disposition in `265-08-PILOT-SOURCE-REVIEW.md`. The review must inspect the hidden-provider adapter, targeted assessment proof without 48-workload replay, preemptive watchdog and Docker ownership cleanup, both scoped lifetime gates, capacity/inode formula, privacy/no-old-run boundary, and tests. Repair any finding, re-freeze source and obtain a new independent review. On the reviewed exact bytes, run Task 1–2 focused tests and the complete named 29-suite Phase 265 CI source command, strict package/script TypeScript, package build, serious/lab/factory/service/diagnostic boundary checks and privacy-marker tests; capture exact commands, exit codes and source root. Emit one canonical `diagnostic-pilot-source-gate-v1` JSON receipt binding source closure, review file hash/reviewer/zero findings, every gate command and exit 0, targeted reader latency samples/ceiling, the pilot-only time/capacity formula versions and explicit `empiricalAuthority:false`. Implement `check-gate`/`prepare`/`run` admission in Task 2's CLI so each recomputes the current source closure and validates the review/gate receipt root and zero-exit list before allocation, repository reservation or provider issuance; a stale, fabricated, missing or mismatched receipt is rejected. Run `check-gate` source-only after writing the receipt. Do not create a pilot allocation or invoke `run` in this plan.</action>
  <review_contract>The independent reviewer must trace `issueDiagnosticPilotProviderFromFactoryCandidate` through admitted pilot allocation, exact cell, durable start and authenticated Phase 264 closure into the private WeakMap, verify `attemptRoot = pilotStart.root` and `budgetRoot = pilotAllocation.root`, and verify `runDiagnosticPilotCell` rejects legacy-issued and forged handles. Record explicit passing evidence for injected durable-precharge-before-both-issuances, either issuance order, and no-legacy-handle tests. Include these issuer/adapter source bytes and tests in the canonical reviewed source closure, 29-suite/focused test run and exact-source gate receipt; an omitted issuer or test, stale source root or unresolved finding blocks `check-gate`, `prepare` and `run`.</review_contract>
  <verify><automated>./node_modules/.bin/tsx scripts/run-v1-38-diagnostic-pilot.ts check-gate --gate .planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-PILOT-GATE.json</automated></verify>
  <done>Independent zero-finding review and every source gate bind identical executable/test/boundary bytes; the CLI revalidates their rooted receipt before any future authorization. This plan ends with zero pilot allocations, reservations, providers and Matches.</done>
</task>

</tasks>

## Source coverage audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Full Phase 265 current-rules league | Existing 265-01–07 retain the complete-league plan; 265-08–09 are separately approved diagnostic response, not goal completion. | no pilot claim |
| REQ | LEAG-01–09 | Remain pending on a separately approved complete league; no requirement ID is credited by this diagnostic plan. | no pilot claim |
| RESEARCH | S01/S03 Smoke four canonical conditions, separate root/ledger/verifier | Task 1 | covered |
| RESEARCH | targeted historical reader and measured admission latency | Tasks 1–2 | covered |
| RESEARCH | both supervisor layers, source-enforced capacity and truly preemptive deadlines | Task 2 | covered |
| RESEARCH | source-only independent review, exact gate, privacy/no-old-route proof | Tasks 2–3 | covered |
| CONTEXT | D-01–D-06 | immutable fail-closed private canonical/supervised and no-rule/no-production boundary | covered |
| CONTEXT | D-07–D-10 | canonical condition/semantic geometry, no incomplete matrix admission or retry | covered within diagnostic scope |
| CONTEXT | D-11–D-21 | Solver, PSRO, red-team, portfolio and claims remain with 265-04–07 and cannot be produced or claimed by this pilot. | excluded from pilot; retained by prior plans |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| old Phase 265 evidence to pilot | Historical run-start is a locator only; exact Phase 264 assessment/candidate bytes are authenticated separately. |
| pilot ledger to issued provider | Durable diagnostic charge must precede both host issuances; only connected-runner's private WeakMap unwraps providers. |
| child Match to parent watchdog | Parent's monotonic timer remains runnable while child blocks; Docker cleanup is restricted to exact pilot-owned labels. |
| source review to execution | Exact reviewed source/gate receipt is checked by prepare/run, not merely filed as prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-265-08-01 | Spoofing | historical candidate/assessment | high | mitigate | Direct byte/domain/root/member/attempt joins for frozen Phase 264 artifacts; old allocation/head denied as pilot authority. |
| T-265-08-02 | Elevation | opaque issued provider and lifetime option | high | mitigate | Private WeakMap adapter, durable pilot precharge, non-serializable two-layer 240-second grant; ordinary 120-second limit unchanged. |
| T-265-08-03 | Denial of service | blocked child/Docker/filesystem | high | mitigate | Separate parent watchdog, finite reserved cleanup/terminal budget, exact ownership-label cleanup and source-derived byte/record/inode admission. |
| T-265-08-04 | Tampering | review/gate source drift | high | mitigate | Independent zero-finding reviewer and canonical source/command receipt rehashed by CLI before any allocation or dispatch. |
| T-265-08-05 | Information disclosure | private pilot diagnostics | high | mitigate | Dedicated AST/import boundary scan, private-only result fields and tests that deny source/memory/objectives/raw diagnostics/host paths. |
| T-265-SC | Tampering | package installs | low | accept | No package installation is planned. |
</threat_model>

<verification>
This plan's checks are source-only. The exact full Phase 265 CI test command in `.github/workflows/ci.yml` plus pilot-focused tests, strict `tsc` over changed package/scripts, `tsc -b packages/strategy-lab/tsconfig.json --pretty false`, serious/lab/factory/diagnostic AST boundary commands and `pnpm exec tsx scripts/check-service-boundary-imports.ts` must all exit zero on the source root signed off in the independent review. `check-gate` must reject a mutated source file, test, gate command, reviewer disposition or latency receipt in injected tests. A read-only targeted Phase 264 latency measurement is permitted; no provider, Strategy, Match, model, allocation or repository reservation is permitted.
</verification>

<success_criteria>
The checked source provides a feasible, fail-closed targeted reader, private precharged canonical-Match adapter, pilot-only two-layer lifetime exception, preemptive watchdog/owned cleanup, source-derived capacity admission and executable exact-source review gate. Plan 09 remains conditional; this plan itself creates no empirical authority or LEAG evidence.
</success_criteria>

<output>
Create `.planning/phases/265-serious-current-rules-league-and-development-red-team/265-08-SUMMARY.md` with exact source/review/gate roots and the measured reader ceiling when done.
</output>
