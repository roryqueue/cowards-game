---
phase: 61
plan: 01
slug: runtime-isolation-readiness-guardrails
status: completed
created: 2026-05-22
---

# Phase 61-01 Plan — Runtime Isolation Readiness Guardrails

## Tasks

1. [x] Add promotion-readiness criteria to sandbox evidence.
   - Record required live container probes, resource limits, filesystem and network denial, image provenance, deployment preflight, failure taxonomy, redacted diagnostics, and local ergonomics.
   - Keep `noCandidatePromoted: true` and counted defaults unchanged.

2. [x] Add fail-loud required container mode.
   - Add `--require-container` support to the sandbox evaluator.
   - Add `pnpm sandbox:evaluate:container` as an explicit Docker/container evidence command.
   - Fail when container evidence is skipped.

3. [x] Add spec-owned runtime isolation promotion metadata.
   - Add adapter `isolationPromotionState`.
   - Add adapter-specific criteria without changing counted eligibility.
   - Keep container subprocess at `evidence-only` despite its production-candidate readiness label.

4. [x] Extend topology and monitors.
   - Add a runtime-isolation topology layer.
   - Add `--require-runtime-container`.
   - Add boundary-monitor checks for readiness criteria, no-promotion state, and public-safe diagnostics.

5. [x] Verify guardrails.
   - Add runtime-js tests for readiness criteria and required-container failure.
   - Add topology and monitor tests.
   - Run sandbox, topology, boundary monitor, spec, runtime-js, formatting, and typecheck commands.
