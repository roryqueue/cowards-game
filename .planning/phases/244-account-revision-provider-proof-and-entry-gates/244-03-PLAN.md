---
phase: 244-account-revision-provider-proof-and-entry-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 244-02
files_modified:
  - apps/go-backend/live_backend.go
  - apps/go-backend/main_test.go
  - apps/web/app/strategies/[strategyId]/page.tsx
  - apps/web/app/matchsets/result-view-model.ts
  - apps/web/app/matchsets/[matchSetId]/page.tsx
  - apps/web/lib/public-go-read-client.test.ts
  - apps/web/app/matchsets/result-view-model.test.ts
  - packages/spec/src/competition.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/service.ts
  - packages/persistence/src/competition.test.ts
  - packages/persistence/src/ladder.test.ts
autonomous: true
requirements:
  - ENTRY-01
  - ENTRY-02
  - ENTRY-03
  - ENTRY-04
requirements_addressed:
  - ENTRY-01
  - ENTRY-02
  - ENTRY-03
  - ENTRY-04
must_haves:
  truths:
    - "Counted Go exhibition entry requires current provider-grade proof for TypeScript, Python, Rust, and Zig."
    - "Non-counted exhibition entry still rejects unsupported providers, hidden TinyGo, invalid rows, stale/missing/mismatched proof, incompatible runtime metadata, package mode other than none, required host capabilities, and silent fallback."
    - "Go and persistence eligibility tests agree across eligible, draft, invalid, stale-proof, missing-proof, mismatched-proof, unsupported-provider, package-declared, unavailable-runtime, and TinyGo cases."
    - "Entry labels and public categories derive from provider proof and registry policy."
    - "Public Strategy, result, and replay-facing labels use proof-aware runtime semantics and do not re-promote TypeScript missing-proof rows through local runtime metadata."
  artifacts:
    - path: "apps/go-backend/live_backend.go"
      provides: "Provider-proof-backed counted and non-counted Go entry gates"
    - path: "apps/go-backend/main_test.go"
      provides: "Go entry eligibility matrix"
    - path: "apps/web/app/strategies/[strategyId]/page.tsx"
      provides: "Public Strategy page consumes proof-aware semantics from the public read DTO instead of recomputing local runtime-only labels"
    - path: "apps/web/app/matchsets/result-view-model.ts"
      provides: "Public result/replay-facing runtime summaries consume proof-aware entrant labels instead of raw runtime-only semantics"
    - path: "apps/web/app/matchsets/[matchSetId]/page.tsx"
      provides: "MatchSet result page passes proof-aware entrant runtime labels into evidence and workbench view-models"
    - path: "apps/web/app/matchsets/result-view-model.test.ts"
      provides: "Public result/replay-facing labels remain proof-derived and privacy-safe"
    - path: "packages/spec/src/schemas.ts"
      provides: "Public Strategy DTO schema carries proof-aware runtime semantics if the field is added to public reads"
    - path: "packages/persistence/src/competition.test.ts"
      provides: "Persistence counted-entry parity tests"
    - path: "packages/persistence/src/ladder.test.ts"
      provides: "Persistence ladder-entry parity tests"
  key_links:
    - from: "apps/go-backend/live_backend.go#loadOwnedEntrants"
      to: "apps/go-backend/provider_readiness.go"
      via: "metadata-aware entry eligibility"
      pattern: "runtimeAllows.*Entry|classify.*Readiness"
    - from: "packages/persistence/src/competition.test.ts"
      to: "apps/go-backend/main_test.go"
      via: "mirrored eligibility matrix names"
      pattern: "stale.*proof|missing.*proof|TinyGo"
    - from: "apps/go-backend/live_backend.go#runtimeSemanticsForRevision"
      to: "apps/web/app/strategies/[strategyId]/page.tsx"
      via: "public Strategy card runtimeSemantics"
      pattern: "runtimeSemantics"
    - from: "apps/web/app/matchsets/[matchSetId]/page.tsx"
      to: "apps/web/app/matchsets/result-view-model.ts"
      via: "proof-aware entrant runtime labels"
      pattern: "entrantRuntimeLabels"
---

<objective>
Bring Go exhibition entry gates into parity with the stricter persistence competition and ladder provider-proof semantics.

Purpose: Account-save readiness is not enough if entry paths can still treat TypeScript, Python, Rust, or Zig metadata as eligible without current proof. Counted and non-counted gates must agree on proof-backed eligibility.
Output: Go entry-gate implementation changes plus Go and TypeScript tests proving parity across positive and negative readiness states.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-CONTEXT.md
@.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-RESEARCH.md
@.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-PATTERNS.md
@.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-02-SUMMARY.md
@.planning/artifacts/v1.35-boundary-surface-inventory.md
@apps/go-backend/live_backend.go
@apps/go-backend/main_test.go
@packages/persistence/src/competition.ts
@packages/persistence/src/competition.test.ts
@packages/persistence/src/ladder.ts
@packages/persistence/src/ladder.test.ts
@apps/web/app/strategies/[strategyId]/page.tsx
@apps/web/app/matchsets/result-view-model.ts
@apps/web/app/matchsets/[matchSetId]/page.tsx
@apps/web/lib/public-go-read-client.test.ts
@apps/web/app/matchsets/result-view-model.test.ts
@packages/spec/src/schemas.ts
@packages/spec/src/competition.ts
@packages/spec/src/service.ts

<interfaces>
Existing helpers that must converge:

```go
func runtimeAllowsCountedPlay(runtime map[string]any, metadata map[string]any, sourceHash string, sourceBytes int) bool
func runtimeAllowsNonCountedExhibition(runtime map[string]any) bool
func loadOwnedEntrants(ctx context.Context, userID string, revisionIDs []string, lockedAt time.Time, counted bool) ([]map[string]any, error)
```

```typescript
export const runtimeAllowsCountedPlay = (
  runtime: unknown,
  provenance?: { metadata?: unknown; sourceHash?: string; sourceBytes?: number },
) => StrategyRuntimeMetadata

export const assertLadderEligibleRuntime = (
  runtime: unknown,
  provenance?: { metadata?: unknown; sourceHash?: string; sourceBytes?: number },
) => StrategyRuntimeMetadata
```

Go non-counted eligibility must become metadata-aware; do not keep adapter-id-only eligibility for provider languages.

Public/read label surfaces that must consume proof-aware semantics:

```go
func runtimeSemanticsForRevision(runtime map[string]any, metadata map[string]any, sourceHash string, sourceBytes int) map[string]any
func (row revisionRow) publicCard() map[string]any
```

```typescript
getPublicStrategyCard(strategyId)
// public Strategy page should display strategy.runtimeSemantics when present,
// not recompute from raw runtime alone.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mirrored Go and persistence eligibility matrix tests</name>
  <files>apps/go-backend/main_test.go, packages/persistence/src/competition.test.ts, packages/persistence/src/ladder.test.ts</files>
  <behavior>
    - TypeScript counted entry requires source artifact provider validation matching source hash/bytes, artifact hash/bytes, provider id, contract version, engine/runtime compatibility, and package mode `none`.
    - Python counted entry requires source-bundle provider validation; Rust/Zig counted entry requires compiled artifact provider validation.
    - Non-counted Go exhibition rejects draft, invalid, stale-proof, missing-proof, mismatched-proof, unsupported-provider, package-declared, unavailable-runtime, TinyGo-hidden, required-capability, incompatible ABI, and silent-fallback cases.
    - Entry creation or `loadOwnedEntrants` rejects invalid owner/revision state: missing revision id, revision owned by another user, and invalid revision validation state remain blocked while Phase 245 auth cleanup stays out of scope.
    - Go and persistence test case names align for eligible, draft, invalid, stale-proof, missing-proof, mismatched-proof, unsupported-provider, package-declared, unavailable-runtime, and TinyGo cases per ENTRY-03.
  </behavior>
  <action>
    Add failing tests first. In Go, extend `main_test.go` or add focused table sections there for TypeScript provider proof, metadata-aware non-counted entry, and invalid owner/revision state. The invalid owner/revision coverage must include at least one wrong-user or missing-revision case and one invalid-validation case against `loadOwnedEntrants`/entry creation using existing test harnesses or a deterministic substitute. In TypeScript persistence tests, add explicit parity cases for TypeScript counted proof and the named negative matrix if not already covered. Reference D-05, D-06, D-07, D-08, D-11, and ENTRY-02 in test names or comments. Keep tests metadata/fixture-based and fast where possible; do not require runtime-service, browser, or Strategy execution for this task. If DB is needed for ownership regression, start existing project services or document and run the deterministic substitute that proves the ownership/revision checks remain enforced.
  </action>
  <verify>
    <automated>cd apps/go-backend && go test ./... -run 'Test.*Runtime.*Play|Test.*Entry|Test.*LoadOwnedEntrants|Test.*Ownership' && pnpm --filter @cowards/persistence test -- competition.test.ts ladder.test.ts</automated>
  </verify>
  <done>The eligibility matrix fails before Go non-counted/counting parity changes and proves proof negatives plus invalid owner/revision states cannot enter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Go counted and non-counted entry gates provider-proof-backed</name>
  <files>apps/go-backend/live_backend.go</files>
  <behavior>
    - `runtimeAllowsCountedPlay` rejects TypeScript without JS/TS source artifact provider validation, matching persistence reference semantics per D-08.
    - `runtimeAllowsNonCountedExhibition` accepts metadata/source identity arguments and rejects all provider languages when proof is missing, stale, mismatched, invalid, package-declared, incompatible, or unsupported per D-07.
    - `loadOwnedEntrants` passes revision metadata, source hash, and source bytes into both counted and non-counted entry checks.
    - Public entry errors use stable public-safe categories or calm messages; they do not expose source, artifact bytes/base64, raw diagnostics, host paths, env values, package paths, tokens, DB details, provider signing material, private runtime internals, StrategyMemory, SoldierMemory, or objective payloads.
  </behavior>
  <action>
    Update the Go entry helper signatures and call sites narrowly. Bring TypeScript counted checks into `runtimeAllowsCountedPlay` by calling `sourceArtifactProviderValidationMatches(metadata, sourceHash, sourceBytes, "strategy-language-provider-js-ts", "typescript")`. Change non-counted exhibition eligibility so provider languages still require current proof and package mode `none`; non-counted changes counted status only, not proof requirements. Preserve JavaScript behavior unless a test proves it must be adjusted for existing registry compatibility; Phase 244 requirements target TypeScript, Python, Rust, and Zig. Do not add local execution, runtime fallback, package support, TinyGo visibility, sandbox certification claims, or ownership/alias changes.
  </action>
  <verify>
    <automated>cd apps/go-backend && go test ./... -run 'Test.*Runtime.*Play|Test.*Entry'</automated>
  </verify>
  <done>Go counted and non-counted entry gates reject the Phase 244 negative matrix and accept only proof-backed eligible provider-language revisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Propagate proof-aware public/read labels</name>
  <files>apps/go-backend/live_backend.go, apps/go-backend/main_test.go, apps/web/app/strategies/[strategyId]/page.tsx, apps/web/app/matchsets/result-view-model.ts, apps/web/app/matchsets/[matchSetId]/page.tsx, apps/web/lib/public-go-read-client.test.ts, apps/web/app/matchsets/result-view-model.test.ts, packages/spec/src/competition.ts, packages/spec/src/schemas.ts, packages/spec/src/service.ts</files>
  <behavior>
    - Public Strategy card DTOs include or preserve proof-aware `runtimeSemantics` derived by `runtimeSemanticsForRevision(runtime, metadata, sourceHash, sourceBytes)`.
    - `apps/web/app/strategies/[strategyId]/page.tsx` displays proof-aware semantics from the public Strategy DTO instead of recomputing `describeStrategyRuntimeProductSemantics(strategy.runtime)` from raw runtime alone when `runtimeSemantics` is available.
    - TypeScript rows missing provider proof no longer show `local-dev-fallback`, `Local/dev fallback`, or `Counted eligible` on public Strategy labels; they show a proof-required/not-counted state and public-safe reason.
    - Result and replay-facing production code in `result-view-model.ts` and `matchsets/[matchSetId]/page.tsx` consumes proof-derived entrant labels or semantics instead of recomputing counted readiness from raw runtime alone.
    - Result and replay-facing view-model tests assert labels/evidence consume stored proof-derived runtime semantics or entrant snapshots and do not re-promote missing-proof rows through local language/runtime assumptions.
    - Public/default tests scan these label paths for D-10 forbidden markers.
  </behavior>
  <action>
    Add failing tests first. In Go, add a public Strategy card or `runtimeSemanticsForRevision` regression proving TypeScript without JS/TS provider validation downgrades to not counted and no longer emits `local-dev-fallback` as a readiness claim. In web tests, assert the public Strategy page/client path accepts and displays DTO-provided proof-aware runtime semantics, and add or tighten result/replay-facing view-model/page coverage so public labels are proof-derived and privacy-safe. Update `publicCard`, `PublicStrategyCardDto`/schema/service DTO types, `apps/web/app/strategies/[strategyId]/page.tsx`, `apps/web/app/matchsets/result-view-model.ts`, and `apps/web/app/matchsets/[matchSetId]/page.tsx` as needed so public Strategy/result/replay labels do not derive counted readiness from raw runtime alone. Avoid changing frozen Match execution contracts unless an existing public read DTO already supports the field. Keep Phase 248 final privacy scan work out of scope.
  </action>
  <verify>
    <automated>cd apps/go-backend && go test ./... -run 'Test.*Runtime.*Semantics|Test.*PublicStrategy|Test.*Runtime.*Play|Test.*Entry' && pnpm --filter @cowards/spec test -- schemas.test.ts service.test.ts && pnpm --filter @cowards/web test -- public-go-read-client.test.ts result-view-model.test.ts</automated>
  </verify>
  <done>Public Strategy/result/replay-facing labels derive readiness from provider proof and registry policy, and TypeScript missing-proof rows cannot appear counted-ready through public/default UI labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Prove Go and persistence entry parity</name>
  <files>apps/go-backend/main_test.go, packages/persistence/src/competition.test.ts, packages/persistence/src/ladder.test.ts</files>
  <behavior>
    - The same named readiness/proof cases pass or fail consistently in Go and TypeScript persistence tests.
    - Persistence tests remain the stricter reference for TypeScript, Python, Rust, and Zig counted entry.
    - Test output makes unsupported-provider, TinyGo-hidden, package-declared, draft/non-execution, invalid, unavailable-runtime, stale-proof, missing-proof, and mismatched-proof failures distinguishable without raw private diagnostics.
  </behavior>
  <action>
    Tighten mirrored tests after implementation so the parity contract is durable. If an existing generated Go parity fixture is relevant, run `pnpm go:parity`; otherwise keep this task to direct Go and persistence test files. Do not change production persistence semantics unless a test reveals an actual gap against Phase 244 requirements; current research identifies persistence as the stricter reference.
  </action>
  <verify>
    <automated>pnpm go:parity && pnpm --filter @cowards/persistence test -- competition.test.ts ladder.test.ts</automated>
  </verify>
  <done>Go and persistence eligibility checks agree on all Phase 244 matrix cases, and entry labels/categories remain proof-derived and public-safe.</done>
</task>

</tasks>

<source_coverage_audit>
GOAL: Covers the entry half of "Users can save and enter account-owned Strategy Revisions only under honest provider-proof-backed readiness states."
REQ: Covers ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04.
RESEARCH: Implements Go/persistence parity using persistence competition and ladder helpers as reference semantics.
CONTEXT: Implements D-05, D-06, D-07, D-08, D-09, D-10, and D-11. Does not implement deferred owner-debug/auth alias cleanup, sandbox certification labels, package ecosystem policy, or final proof scans.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| saved account revision -> Go exhibition entry | Stored runtime/proof metadata crosses into MatchSet entrant creation. |
| saved account revision -> persistence competition/ladder | Stored runtime/proof metadata crosses into stricter TypeScript persistence gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-244-03-01 | Tampering | entry proof checks | mitigate | Verify source/artifact/provider identity at entry, not only at save time. |
| T-244-03-02 | Spoofing | unsupported provider/TinyGo | mitigate | Reject unsupported providers and hidden TinyGo via registry/proof checks. |
| T-244-03-03 | Elevation of privilege | non-counted exhibition | mitigate | Non-counted does not bypass proof, package, capability, or runtime metadata gates. |
| T-244-03-04 | Information disclosure | entry errors | mitigate | Use public-safe categories/messages and deny private marker exposure in tests. |
| T-244-03-05 | Spoofing | public/read labels | mitigate | Public Strategy/result/replay labels consume proof-aware semantics and tests reject TypeScript missing-proof counted-ready/local-fallback labels. |
</threat_model>

<verification>
Run:

```bash
cd apps/go-backend && go test ./... -run 'Test.*Runtime.*Play|Test.*Entry'
cd apps/go-backend && go test ./... -run 'Test.*Runtime.*Semantics|Test.*PublicStrategy'
pnpm --filter @cowards/spec test -- schemas.test.ts service.test.ts
pnpm --filter @cowards/web test -- public-go-read-client.test.ts result-view-model.test.ts
pnpm --filter @cowards/persistence test -- competition.test.ts ladder.test.ts
pnpm go:parity
```

If `pnpm go:parity` exposes unrelated existing failures, record them in the summary with the narrower targeted commands above as Phase 244 proof.
</verification>

<success_criteria>
- Counted Go exhibition, persistence competition, and ladder entry require current provider-grade proof for TypeScript, Python, Rust, and Zig.
- Non-counted exhibition cannot bypass proof, package, runtime metadata, TinyGo, unsupported-provider, or fallback gates.
- Go and persistence tests agree on the required readiness matrix.
- Public Strategy, result, and replay-facing labels are proof-aware, public-safe, and do not show TypeScript missing-proof rows as local/dev fallback or counted-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-03-SUMMARY.md`.
</output>
