---
phase: 249-competition-surface-inventory-and-policy-lock
plan: 3
type: execute
wave: 3
depends_on:
  - 249-01
  - 249-02
files_modified:
  - scripts/check-boundary-monitors.ts
  - scripts/check-boundary-monitors.test.ts
  - package.json
autonomous: true
requirements:
  - POST-01
  - POST-02
  - POST-03
  - POST-04
  - POST-05
must_haves:
  truths:
    - "The boundary monitor chain includes the v1.36 competition policy/inventory check."
    - "The root package exposes `v1.36:competition-policy` and `v1.36:competition-policy:check` scripts."
    - "Public copy monitors reject durable-rating, production-sandbox, package-ecosystem, TinyGo-production, raw-diagnostic, and private-runtime overclaims."
    - "The monitor scans `.planning`, `packages`, `apps`, `scripts`, and relevant fixture/snapshot paths by default."
    - "The monitor checks required posture-label presence and forbidden/private-marker absence."
  artifacts:
    - path: "scripts/check-boundary-monitors.ts"
      provides: "Registered v1.36 competition policy boundary monitor"
      exports:
        - "checkV136CompetitionPolicyMonitor"
    - path: "scripts/check-boundary-monitors.test.ts"
      provides: "Monitor registration, failure, package script, and full-chain tests"
    - path: "package.json"
      provides: "Root write/check scripts and boundary monitor chain wiring"
  key_links:
    - from: "scripts/check-boundary-monitors.ts"
      to: "scripts/evaluate-v1-36-competition-policy.ts"
      via: "checkV136CompetitionSurfaceInventoryArtifacts import"
      pattern: "checkV136CompetitionSurfaceInventoryArtifacts"
    - from: "package.json"
      to: "scripts/evaluate-v1-36-competition-policy.ts"
      via: "v1.36 scripts"
      pattern: "v1\\.36:competition-policy"
    - from: "package.json"
      to: "scripts/check-boundary-monitors.ts"
      via: "boundary:monitors"
      pattern: "v1\\.36:competition-policy:check.*check-boundary-monitors"
---

<objective>
Wire the v1.36 competition policy and surface inventory into the existing fail-loud boundary monitor chain.

Purpose: Phase 249 only succeeds if future copy, artifacts, fixtures, and public-trust surfaces cannot silently drift into durable rating, sandbox, package, TinyGo, raw diagnostic, private runtime, or posture-label violations.
Output: Package scripts, monitor registration, and tests that make `pnpm v1.36:competition-policy:check` and `pnpm boundary:monitors` enforce the Phase 249 contract.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-CONTEXT.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-RESEARCH.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-VALIDATION.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-PATTERNS.md
@packages/spec/src/competition-policy-v1-36.ts
@scripts/evaluate-v1-36-competition-policy.ts
@scripts/evaluate-v1-36-competition-policy.test.ts
@scripts/check-boundary-monitors.ts
@scripts/check-boundary-monitors.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add monitor and script wiring tests</name>
  <files>scripts/check-boundary-monitors.test.ts</files>
  <read_first>
    - scripts/check-boundary-monitors.test.ts
    - scripts/check-boundary-monitors.ts
    - scripts/evaluate-v1-36-competition-policy.ts
    - package.json
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-VALIDATION.md
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: package scripts include `v1.36:competition-policy` and `v1.36:competition-policy:check`, and `boundary:monitors` invokes `pnpm v1.36:competition-policy:check` before `pnpm exec tsx scripts/check-boundary-monitors.ts`.
    - Test 2: `checkV136CompetitionPolicyMonitor` returns a pass message when generated artifacts are current.
    - Test 3: `checkV136CompetitionPolicyMonitor` throws on a forbidden durable-rating, production-sandbox, package-ecosystem, TinyGo-production, raw-diagnostic, or private-runtime overclaim.
    - Test 4: `checkV136CompetitionPolicyMonitor` throws when a posture-required inventory row lacks exact `public beta trial competition` or reset/no-durable copy.
    - Test 5: `runBoundaryMonitorChecks()` includes a named `v1.36 competition policy` check without removing existing v1.35 checks.
  </behavior>
  <action>
    Extend `scripts/check-boundary-monitors.test.ts` using the v1.35 boundary inventory test pattern. Import planned `checkV136CompetitionPolicyMonitor` from `./check-boundary-monitors.ts` and inventory helpers from `./evaluate-v1-36-competition-policy.ts`.

    Add package script wiring assertions with exact values:
    - `v1.36:competition-policy` equals `pnpm exec tsx scripts/evaluate-v1-36-competition-policy.ts --write`.
    - `v1.36:competition-policy:check` equals `pnpm exec tsx scripts/evaluate-v1-36-competition-policy.ts --check`.
    - `boundary:monitors` contains `pnpm v1.36:competition-policy:check && pnpm exec tsx scripts/check-boundary-monitors.ts`.

    Add failure tests for the required forbidden claim categories and required posture-label presence per D-13 through D-16. Keep tests static and local; do not start services, browser, Go backend, Docker, runtime-service, database, or Strategy execution.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/check-boundary-monitors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'v1\\.36:competition-policy|v1\\.36:competition-policy:check|pnpm v1\\.36:competition-policy:check && pnpm exec tsx scripts/check-boundary-monitors.ts' scripts/check-boundary-monitors.test.ts` returns package script wiring tests.
    - `rg -n 'checkV136CompetitionPolicyMonitor|v1\\.36 competition policy' scripts/check-boundary-monitors.test.ts` returns monitor wrapper and full-chain inclusion tests.
    - `rg -n 'durable-rating|production-sandbox|package-ecosystem|TinyGo-production|raw-diagnostic|private-runtime|public beta trial competition|resettable Season-scoped standings|no durable permanent rating promise' scripts/check-boundary-monitors.test.ts` returns forbidden and required presence tests.
    - `rg -n 'v1\\.35 boundary surface inventory|v1\\.35 final proof|v1\\.36 competition policy' scripts/check-boundary-monitors.test.ts` returns evidence that existing v1.35 checks are preserved while v1.36 is added.
    - `pnpm exec vitest run scripts/check-boundary-monitors.test.ts` fails before Task 2 because the monitor/scripts are not wired, then passes after Task 2.
  </acceptance_criteria>
  <done>Monitor tests define required package scripts, v1.36 monitor behavior, full-chain inclusion, forbidden-claim failures, posture-label failures, and preservation of existing checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the v1.36 monitor and package scripts</name>
  <files>scripts/check-boundary-monitors.ts, scripts/check-boundary-monitors.test.ts, package.json</files>
  <read_first>
    - scripts/check-boundary-monitors.ts
    - scripts/check-boundary-monitors.test.ts
    - scripts/evaluate-v1-36-competition-policy.ts
    - package.json
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-CONTEXT.md
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `checkV136CompetitionPolicyMonitor` wraps `checkV136CompetitionSurfaceInventoryArtifacts` and throws joined failures when artifacts, forbidden claims, private markers, or required posture labels are invalid.
    - Test 2: `runBoundaryMonitorChecks()` includes the v1.36 monitor as a fail-loud static contract/privacy check.
    - Test 3: package scripts expose write/check commands and chain the check into `boundary:monitors`.
    - Test 4: focused Phase 249 validation commands pass with no watch-mode flags.
  </behavior>
  <action>
    Update `scripts/check-boundary-monitors.ts` to import `checkV136CompetitionSurfaceInventoryArtifacts` and its options type from `./evaluate-v1-36-competition-policy.ts`. Add:

    ```typescript
    export const checkV136CompetitionPolicyMonitor = (
      options: GenerateV136CompetitionSurfaceInventoryOptions = {},
    ): string => {
      const failures = checkV136CompetitionSurfaceInventoryArtifacts(options)
      if (failures.length > 0) {
        throw new Error(failures.join("; "))
      }
      return "v1.36 competition policy artifacts are current"
    }
    ```

    Register it in `runBoundaryMonitorChecks()` with name `v1.36 competition policy` and layer `contract_drift` or the nearest existing privacy/static layer used for artifact checks. Do not replace, weaken, or remove existing v1.35 monitor checks.

    Update `package.json` scripts:
    - Add `"v1.36:competition-policy": "pnpm exec tsx scripts/evaluate-v1-36-competition-policy.ts --write"`.
    - Add `"v1.36:competition-policy:check": "pnpm exec tsx scripts/evaluate-v1-36-competition-policy.ts --check"`.
    - Insert `pnpm v1.36:competition-policy:check` into `boundary:monitors` after `pnpm v1.35:final-proof:check` and before `pnpm exec tsx scripts/check-boundary-monitors.ts`.

    Preserve all prior script commands exactly except for adding the v1.36 command. Do not add service-backed proof, Playwright proof, entry enforcement, Season lifecycle behavior, standings recomputation, governance workflow, React game rules, Strategy execution, or Node `vm`.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-36-competition-policy.test.ts scripts/check-boundary-monitors.test.ts && pnpm v1.36:competition-policy:check && pnpm exec tsx scripts/check-boundary-monitors.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'checkV136CompetitionPolicyMonitor|checkV136CompetitionSurfaceInventoryArtifacts|v1\\.36 competition policy artifacts are current|v1\\.36 competition policy' scripts/check-boundary-monitors.ts` returns monitor wrapper and registration.
    - `node -e "const pkg=require('./package.json'); if(pkg.scripts['v1.36:competition-policy']!=='pnpm exec tsx scripts/evaluate-v1-36-competition-policy.ts --write') process.exit(1); if(pkg.scripts['v1.36:competition-policy:check']!=='pnpm exec tsx scripts/evaluate-v1-36-competition-policy.ts --check') process.exit(1); if(!pkg.scripts['boundary:monitors'].includes('pnpm v1.36:competition-policy:check && pnpm exec tsx scripts/check-boundary-monitors.ts')) process.exit(1)"` exits 0.
    - `rg -n 'v1\\.35:boundary-inventory:check|v1\\.35:account-provider-entry-proof:check|v1\\.35:ownership-alias-proof:check|v1\\.35:sandbox-readiness-proof:check|v1\\.35:package-policy-proof:check|v1\\.35:final-proof:check|v1\\.36:competition-policy:check' package.json` returns all existing v1.35 checks plus the v1.36 check.
    - `node -e "const s=require('fs').readFileSync('scripts/check-boundary-monitors.ts','utf8'); if(/from ['\\\"]node:vm['\\\"]/.test(s)||/\\bfetch\\s*\\(/.test(s)||/Date\\.now\\s*\\(/.test(s)||/Math\\.random\\s*\\(/.test(s)) process.exit(1)"` exits 0.
    - `pnpm exec vitest run scripts/evaluate-v1-36-competition-policy.test.ts scripts/check-boundary-monitors.test.ts` exits 0.
    - `pnpm v1.36:competition-policy:check` exits 0.
    - `pnpm exec tsx scripts/check-boundary-monitors.ts` exits 0.
  </acceptance_criteria>
  <done>The v1.36 competition policy check is exposed as a package script and registered in the existing boundary monitor chain without weakening existing monitors.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 249 validation gates</name>
  <files>package.json, scripts/check-boundary-monitors.ts, scripts/check-boundary-monitors.test.ts</files>
  <read_first>
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-VALIDATION.md
    - package.json
    - scripts/evaluate-v1-36-competition-policy.ts
    - scripts/check-boundary-monitors.ts
  </read_first>
  <action>
    Run the Phase 249 validation commands from `249-VALIDATION.md` with no watch-mode flags:
    - `pnpm exec vitest run packages/spec/src/spec.test.ts scripts/evaluate-v1-36-competition-policy.test.ts scripts/check-boundary-monitors.test.ts`
    - `pnpm v1.36:competition-policy:check`
    - `pnpm exec tsx scripts/check-boundary-monitors.ts`
    - `pnpm boundary:monitors` where the existing baseline permits; if it fails due to unrelated pre-existing baseline failures, capture the failing command and exact first unrelated failure in the plan summary.

    Also run grep gates to prove Phase 249 did not implement downstream behavior. Search modified files for database migrations, entry enforcement, Season lifecycle mutations, standings recompute behavior, governance workflow mutation, React game-rule logic, Strategy execution in web/API/Go, Node `vm`, durable rating claims, production sandbox claims, package ecosystem claims, TinyGo production claims, raw diagnostic public claims, and private runtime public claims.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/spec/src/spec.test.ts scripts/evaluate-v1-36-competition-policy.test.ts scripts/check-boundary-monitors.test.ts && pnpm v1.36:competition-policy:check && pnpm exec tsx scripts/check-boundary-monitors.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/spec/src/spec.test.ts scripts/evaluate-v1-36-competition-policy.test.ts scripts/check-boundary-monitors.test.ts` exits 0.
    - `pnpm v1.36:competition-policy:check` exits 0.
    - `pnpm exec tsx scripts/check-boundary-monitors.ts` exits 0.
    - `grep -R -n -E 'durable permanent rating|all-time ranking|rating refund|production sandbox certification|package ecosystem|TinyGo production support|raw diagnostics are public|private runtime internals are public' packages apps scripts .planning/artifacts/v1.36-competition-surface-inventory.* | grep -v 'forbidden' | grep -v 'no durable permanent rating promise' | grep -v 'does not promise' | grep -v 'future/defer' | grep -v 'reject' | grep -v 'exclusion' | grep -v 'Out of Scope'` returns no clear violation lines.
    - `grep -R -n -E 'node:vm|new vm\\.|runInNewContext|runInThisContext' packages apps scripts | grep -v '^#'` returns no new violation lines in Phase 249 modified files.
    - `grep -R -n -E 'execute.*Strategy|Strategy.*execution' apps/web packages scripts/evaluate-v1-36-competition-policy.ts scripts/check-boundary-monitors.ts | grep -v 'runtime-service' | grep -v 'forbidden' | grep -v 'must not' | grep -v 'no Strategy execution'` returns no new Strategy-execution-in-web/API/Go violation lines.
  </acceptance_criteria>
  <done>Focused Phase 249 checks pass, boundary monitor wiring works, and downstream-scope/game-rule/runtime overclaim grep gates show no Phase 249 scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory artifacts -> boundary monitor hub | Generated policy/inventory checks become fail-loud CI/local gates. |
| public copy files -> static scan | Text in `.planning`, `packages`, `apps`, `scripts`, fixtures, and snapshots is treated as potentially public-trust-shaping. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-249-09 | HIGH | Spoofing/Repudiation | Public copy monitor | mitigate | Monitor fails on durable-rating, all-time ranking, rating refund, mature moderation, production sandbox, package ecosystem, TinyGo production, raw diagnostic, and private runtime overclaims. |
| T-249-10 | HIGH | Information Disclosure | Broad scan scope | mitigate | Monitor checks forbidden/private-marker absence across `.planning`, `packages`, `apps`, `scripts`, fixtures, and snapshots through inventory-driven checks. |
| T-249-11 | HIGH | Tampering | Boundary chain | mitigate | Package scripts add `pnpm v1.36:competition-policy:check` before the monitor hub and tests assert existing v1.35 checks remain. |
| T-249-12 | MEDIUM | Denial of Service | False positives | mitigate | Evaluator supports explicit documented false-positive handling; tests cover clear violations and allowed calibrated wording. |
</threat_model>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Stable competition posture, vocabulary, authority, privacy boundaries, and forbidden claims before downstream work | 249-01, 249-02, 249-03 | COVERED | Contract, inventory, and monitor gates cover the phase goal. |
| REQ | POST-01 | Public competition surfaces use public beta trial competition with resettable Season-scoped standings | 249-01, 249-02, 249-03 | COVERED | Exact phrase in spec, inventory row requirements, monitor presence checks. |
| REQ | POST-02 | No durable ratings, all-time rankings, rating refunds, or mature staffed moderation promises | 249-01, 249-03 | COVERED | Forbidden categories/examples and monitor failures. |
| REQ | POST-03 | Versioned competition policy contract | 249-01 | COVERED | `@cowards/spec` owns `competition-policy-v1.36`. |
| REQ | POST-04 | Surface inventory with owner, data class, counted behavior, replay evidence, privacy risk | 249-02 | COVERED | Markdown and JSON evaluator/artifacts. |
| REQ | POST-05 | Public copy monitors reject specified overclaims | 249-03 | COVERED | Boundary monitor and package wiring. |
| RESEARCH | R-01 | No new dependencies; use existing pnpm/TypeScript/Vitest/Zod/spec stack | 249-01, 249-02, 249-03 | COVERED | Plans use existing files and commands only. |
| RESEARCH | R-02 | Reuse v1.35 inventory evaluator/artifact/monitor pattern | 249-02, 249-03 | COVERED | Tasks read v1.35 analogs and copy shape. |
| RESEARCH | R-03 | Keep Phase 249 contract/inventory/monitor only | 249-01, 249-02, 249-03 | COVERED | Scope creep grep gates and no behavior tasks. |
| RESEARCH | R-04 | Counted-state projection vocabulary first; persistence mappings in Phase 252 | 249-01 | COVERED | D-11 guardrail in code/tests. |
| CONTEXT | D-01 | Prominent, plain posture copy near competition trust surfaces | 249-01, 249-02, 249-03 | COVERED | Trust surfaces, inventory posture-required rows, monitor presence. |
| CONTEXT | D-02 | Default phrase exactly `public beta trial competition` | 249-01, 249-02, 249-03 | COVERED | Exact string tests and grep gates. |
| CONTEXT | D-03 | No-durable/reset language on every competition trust surface | 249-01, 249-02, 249-03 | COVERED | Contract labels and row requirements. |
| CONTEXT | D-04 | Completed/archived Seasons retain trial/resettable/no durable label | 249-01, 249-02 | COVERED | Contract completed/archived labels and inventory rows. |
| CONTEXT | D-05 | Route/code/artifact inventory modeled after v1.35 Phase 243 | 249-02 | COVERED | Evaluator and artifact tasks use v1.35 analog. |
| CONTEXT | D-06 | Inventory includes routes, DTOs, pages, persistence, Go, UI copy, docs, monitors, proof, tests, fixtures, snapshots | 249-02 | COVERED | Required groups and concrete references. |
| CONTEXT | D-07 | Exactly one downstream disposition per row from locked list | 249-02 | COVERED | Validator enforces allowed dispositions and one value. |
| CONTEXT | D-08 | Markdown plus JSON inventory | 249-02 | COVERED | Generated synchronized artifacts. |
| CONTEXT | D-09 | `competition-policy-v1.36` spec-owned in `@cowards/spec` | 249-01 | COVERED | New module and barrel export. |
| CONTEXT | D-10 | Lock vocabulary/public labels, privacy exclusions, forbidden claims | 249-01 | COVERED | Contract constants and tests. |
| CONTEXT | D-11 | Public projection counted-state vocabulary only | 249-01 | COVERED | Guardrail string and no persistence mapping. |
| CONTEXT | D-12 | Forbidden claims use categories plus examples | 249-01, 249-03 | COVERED | Contract taxonomy and monitor tests. |
| CONTEXT | D-13 | Broad copy/privacy scan | 249-03 | COVERED | Monitor wrapper and scan-scope validation via evaluator. |
| CONTEXT | D-14 | Fail loud with explicit false-positive handling | 249-02, 249-03 | COVERED | Validator failures and documented false-positive support. |
| CONTEXT | D-15 | Default scan scope includes `.planning`, `packages`, `apps`, `scripts`, fixtures/snapshots | 249-02, 249-03 | COVERED | Required groups and final grep gates. |
| CONTEXT | D-16 | Check required posture presence and forbidden/private-marker absence | 249-02, 249-03 | COVERED | Row-aware required copy and monitor absence checks. |
</source_audit>

<verification>
Focused commands:
- `pnpm exec vitest run packages/spec/src/spec.test.ts scripts/evaluate-v1-36-competition-policy.test.ts scripts/check-boundary-monitors.test.ts`
- `pnpm v1.36:competition-policy:check`
- `pnpm exec tsx scripts/check-boundary-monitors.ts`

Phase gate:
- `pnpm boundary:monitors` where existing baseline permits; disclose unrelated pre-existing failures in `249-03-SUMMARY.md`.
</verification>

<success_criteria>
- POST-01 through POST-05 appear in plan frontmatter and are enforced by contract, inventory, or monitor gates.
- D-01 through D-16 are covered by objectives, must-haves, tasks, and source audit rows.
- `pnpm v1.36:competition-policy:check` and `pnpm exec tsx scripts/check-boundary-monitors.ts` pass.
- No game-rule changes, Strategy execution in web/API/Go, unsupported runtime/sandbox/package/TinyGo/rating claims, or service-backed E2E proof are introduced in Phase 249.
</success_criteria>

<output>
After completion, create `.planning/phases/249-competition-surface-inventory-and-policy-lock/249-03-SUMMARY.md`
</output>
