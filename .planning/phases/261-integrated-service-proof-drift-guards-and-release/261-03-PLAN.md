---
phase: 261-integrated-service-proof-drift-guards-and-release
plan: "03"
type: execute
wave: 2
depends_on: ["261-01", "261-02"]
files_modified:
  - scripts/run-v1-37-integrated-service-proof.ts
  - scripts/run-v1-37-integrated-service-proof.test.ts
  - package.json
autonomous: true
requirements: [PROOF-02, PROOF-03]
must_haves:
  truths:
    - "Per D-01/D-02, the strict runner health-checks real PostgreSQL, Redis, selected Go, runtime-service, canonical kernel/Chronicle/replay, and exact TypeScript/Python/Rust/Zig providers/toolchains with current content identities."
    - "All four languages pass fresh functional ABI/full-trace execution; counted status remains independently fail-closed when deployable containment cannot be attested per D-09."
    - "Player violations remain attributable while timeout, crash, unavailable, transport, malformed, stale, and persistence failures remain system-owned and mutate no gameplay, memory, result, or standings state."
  artifacts:
    - path: scripts/run-v1-37-integrated-service-proof.ts
      provides: Production-shaped topology collector, signed restricted scenario receipts, and restricted browser proof-data handoff
    - path: scripts/run-v1-37-integrated-service-proof.test.ts
      provides: Strict preflight, cleanup, identity, typed-failure, and no-mutation tests
  key_links:
    - from: scripts/lib/v1-37-integrated-proof-manifest.ts
      to: scripts/run-v1-37-integrated-service-proof.ts
      via: exact manifest iteration with no skipped or ad hoc scenario
      pattern: "V137_INTEGRATED_PROOF_SCENARIOS"
    - from: scripts/run-v1-37-integrated-service-proof.ts
      to: scripts/lib/v1-37-restricted-evidence-store.ts
      via: raw process/service receipts are stored before safe projection
      pattern: "restricted"
---

<objective>Execute the hardest constraint first: one real, exact, four-language service topology with typed failure and no-mutation proof.</objective>
<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
</execution_context>
<context>
@.planning/phases/261-integrated-service-proof-drift-guards-and-release/261-01-SUMMARY.md
@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-VERIFICATION.md
@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-VERIFICATION.md
@apps/web/e2e/v1-36-competition-service-proof.spec.ts
@scripts/evaluate-v1-37-executable-conformance.ts
@scripts/evaluate-v1-37-truthful-inputs-set-fairness.ts
</context>
<interfaces>
The manifest and restricted store from 261-01 are the only scenario/evidence contracts. Existing proof evaluators expose pure `--check` inputs. The v1.36 service proof's `requiredEnvironment`, health checks, Go-owned scheduling, runtime-service execution, and cleanup patterns are reusable, but its v1.36 evidence is not current proof.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build strict topology preflight, lifecycle, and cleanup</name>
  <files>scripts/run-v1-37-integrated-service-proof.ts, scripts/run-v1-37-integrated-service-proof.test.ts</files>
  <read_first>261-01 contracts; apps/web/e2e/v1-36-competition-service-proof.spec.ts environment/health/cleanup; package.json services commands; current Phase-259/260 evaluator inputs; protected baseline checker.</read_first>
  <behavior>Source/fixture tests first require explicit DSNs and restricted root, current database head `active-v1.19-finalized`, exact content-addressed lane/toolchain/adapter/containment/certificate identities, healthy real services, owned-process tracking, strict no-mock participants, and teardown of only resources created by the runner. Missing infrastructure is failed/incomplete, never skipped or passed. No evidence artifact is required before this test seam passes.</behavior>
  <action>Implement start/preflight/migrate/health/execute/teardown orchestration with `spawn`/`execFile` argument arrays and redacted restricted receipts. Reuse `pnpm services:up`, selected Go/runtime-service startup contracts, current authority artifacts, and protected-baseline check. Never place secrets in arguments, logs, safe receipts, or Markdown. On any exception, capture restricted diagnostics, terminate only owned processes, preserve database evidence required by the scenario contract, and return a stable non-pass status per D-12.</action>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 --no-file-parallelism scripts/run-v1-37-integrated-service-proof.test.ts -t "preflight|topology|cleanup"</automated></verify>
  <acceptance_criteria>Mocked/missing/unhealthy/mismatched components and unsafe cleanup all fail before a proof receipt; exact real components reach ready state without exposing secrets or touching protected paths.</acceptance_criteria>
  <done>The runner can safely own a complete local production-shaped proof topology.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Execute four exact lanes, typed failures, Chronicle validation, reconstruction, and replay</name>
  <files>scripts/run-v1-37-integrated-service-proof.ts, scripts/run-v1-37-integrated-service-proof.test.ts</files>
  <read_first>Closed manifest positive/failure/Chronicle rows; Phase-259 executable proof and current certificates; runtime-service result contract; Go orchestrator/completion tests; replay validate/reconstruct tests.</read_first>
  <behavior>Each TypeScript/Python/Rust/Zig lane executes at least three fresh independent current runs through runtime-service. Full state, events, StrategyMemory, SoldierMemory, objectives, terminal data, and failure trace match current corpus roots. Success yields a semantically valid Chronicle and reconstruction-equivalent replay. One player violation is engine-attributed. Representative timeout, crash, unavailable, transport, malformed, stale, and persistence failures are system failures with exact before/after no-mutation hashes.</behavior>
  <action>Drive the production owner chain instead of importing provider or kernel helpers into the runner. Bind every restricted receipt to exact current tuple, certificate, artifact, toolchain, containment, request, response, Chronicle, reconstruction, and no-mutation roots. Create one restricted browser proof-data handoff containing the real MatchSet, Season, replay, lane-status, and historical identifiers plus a safe opaque `proofDataHandoffRef`; only the safe ref enters the service receipt. Per D-02/D-09, record functional conformance and counted containment as separate fields; an unattested containment lane remains explicitly non-counted without falsifying its functional result. Reject any response whose identity, failure owner, event vocabulary, final state, or replay root diverges.</action>
  <verify><automated>COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1 pnpm exec vitest run --maxWorkers=1 --no-file-parallelism scripts/run-v1-37-integrated-service-proof.test.ts -t "four lanes|typed failure|Chronicle|reconstruction|replay|no mutation"</automated></verify>
  <acceptance_criteria>All four exact lanes pass functional full-trace execution; player and system failures remain distinct; every system failure has zero gameplay/memory/result/standings mutation; Chronicle and replay roots agree.</acceptance_criteria>
  <done>PROOF-02 and the execution/Chronicle/replay half of PROOF-03 have real service evidence.</done>
</task>
<task type="auto">
  <name>Task 3: Expose deterministic live write and strict read-only check commands</name>
  <files>package.json, scripts/run-v1-37-integrated-service-proof.ts</files>
  <read_first>Task-1/2 runner API; existing v1.37 evaluator command names; D-05/D-06 write/check contract; package.json command ordering.</read_first>
  <action>Add `v1.37:integrated-service-proof:write` as the sole live execution/restricted capture command and `v1.37:integrated-service-proof:check` for signature/hash/schema/currentness validation only. Check mode must not start services, mutate PostgreSQL, refresh receipts, write files, or normalize artifacts. Tests must mutation-delete and stale each required receipt and the browser proof-data handoff before any live write. Require `COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1` for release checks and fail closed when restricted objects required at release are absent.</action>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 --no-file-parallelism scripts/run-v1-37-integrated-service-proof.test.ts &amp;&amp; COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1 pnpm v1.37:integrated-service-proof:write &amp;&amp; COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1 pnpm v1.37:integrated-service-proof:check &amp;&amp; COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1 pnpm v1.37:integrated-service-proof:check</automated></verify>
  <acceptance_criteria>Write is the sole live collection command; check is byte-read-only and detects stale/edited/missing current evidence without executing topology.</acceptance_criteria>
  <done>The real topology proof has a stable automation surface for later rollups and release gates.</done>
</task>
</tasks>

## Artifacts this phase produces

- A production-shaped local topology runner using real PostgreSQL, Redis, Go, runtime-service, and four exact provider lanes.
- Restricted signed receipts for functional success, typed failures, no-mutation, Chronicle, reconstruction, and replay.
- A restricted proof-data descriptor and safe opaque handoff reference for the later live browser collector.
- Separate live write and pure strict check commands.

<threat_model>
## Trust Boundaries
| Boundary | Untrusted input |
|---|---|
| host environment to runner | missing services, mismatched toolchains, secrets, and hostile process output |
| Go to runtime-service/providers | stale/mixed identity, malformed envelopes, timeout/crash/transport failures |
| runtime response to gameplay persistence | forged player blame, invalid Chronicle/final state, private diagnostics |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-261-03A | Spoofing | topology identity | critical | mitigate | Exact process/artifact/toolchain/containment/certificate joins and no-mock manifest rows. |
| T-261-03B | Tampering | runtime result | critical | mitigate | Signed exact identities, semantic Chronicle admission, reconstruction equality, and before/after hashes. |
| T-261-03C | Information Disclosure | subprocess capture | critical | mitigate | Restricted-first receipts, redaction, safe refs, and no secrets in arguments. |
| T-261-03D | Denial of Service | process lifecycle | high | mitigate | Bounded health/execution timeouts and owned-process cleanup on every exit. |
| T-261-03E | Elevation of Privilege | Strategy execution | critical | mitigate | Execution remains solely behind runtime-service/provider containment; no web/API/Go execution fallback. |
</threat_model>
<prohibitions>No mocked boundary, provider/kernel import shortcut, Go or web Strategy execution, gameplay mutation by the runner, floating toolchain identity, manual pass, public diagnostics, or protected-path cleanup is allowed.</prohibitions>
<verification>Run focused runner tests, live write once against exact services, strict check twice, lower conformance proof checks, and protected baseline.</verification>
<success_criteria>The hardest D-01/D-02/D-09 constraint is proven early and yields current restricted evidence without semantic or privacy ownership drift.</success_criteria>
<output>Create 261-03-SUMMARY.md.</output>
