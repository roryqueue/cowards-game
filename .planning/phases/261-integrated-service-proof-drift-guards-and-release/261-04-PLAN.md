---
phase: 261-integrated-service-proof-drift-guards-and-release
plan: "04"
type: execute
wave: 3
depends_on: ["261-03"]
files_modified:
  - scripts/run-v1-37-rollback-proof.ts
  - scripts/run-v1-37-rollback-proof.test.ts
  - apps/go-backend/v1_37_release_rollback_test.go
  - packages/persistence/src/v1-37-release-rollback.test.ts
  - scripts/check-v1-37-audit-reproduction.ts
  - scripts/check-v1-37-audit-reproduction.test.ts
  - package.json
autonomous: true
requirements: [PROOF-01, PROOF-03, PROOF-04]
must_haves:
  truths:
    - "Every persisted core-rules audit observation passes current HEAD or matches one exact prior compatibility ruling; generic waivers and expectation rewriting are impossible per D-10/D-12."
    - "Per D-11, real PostgreSQL/Go/service proof covers schedule/execution staleness, lane kill switch, completion transaction failure, idempotent retry, cohort invalidation, compensation, standings recomputation, service/runtime rollback, and mixed-tuple rejection."
    - "Historical v1.4/v1.17/v1.36 evidence remains byte-immutable and reachable only through its explicit original dispatch."
  artifacts:
    - path: scripts/run-v1-37-rollback-proof.ts
      provides: Closed D-11 rollback matrix runner with restricted receipts
    - path: scripts/check-v1-37-audit-reproduction.ts
      provides: Exact current reproduction and compatibility-ruling release gate
    - path: apps/go-backend/v1_37_release_rollback_test.go
      provides: Production-owner PostgreSQL rollback and recomputation scenarios
  key_links:
    - from: .planning/artifacts/v2.0-core-rules-audit/reproduce-core-rule-gaps.ts
      to: scripts/check-v1-37-audit-reproduction.ts
      via: fresh exact observations joined to approved Phase-257 result/rulings
      pattern: "reproduce-core-rule-gaps"
    - from: apps/go-backend/v1_37_release_rollback_test.go
      to: scripts/run-v1-37-rollback-proof.ts
      via: DSN-required focused Go test selection and restricted result receipt
      pattern: "TestV137"
---

<objective>After the service collector releases the shared database, prove the complete persistence, rollback, recomputation, and compatibility matrix against real owners.</objective>
<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
</execution_context>
<context>
@.planning/phases/261-integrated-service-proof-drift-guards-and-release/261-01-SUMMARY.md
@.planning/artifacts/v2.0-core-rules-audit/README.md
@.planning/artifacts/v2.0-core-rules-audit/reproduce-core-rule-gaps.ts
@apps/go-backend/completion_test.go
@apps/go-backend/job_lifecycle_test.go
@apps/go-backend/scoring_test.go
@packages/persistence/src/complete-match.test.ts
@packages/persistence/src/semantic-authority-selection-head.test.ts
@packages/replay/src/historical-v1-4.test.ts
@scripts/check-v1-36-historical-proof.ts
</context>
<interfaces>
Existing Go tests expose atomic completion, idempotence, retry, stale lease/evidence rejection, and scoring parity. Persistence tests expose system-failure no-mutation, late-write rollback, exact four-condition scoring, and semantic selection compensation. The new files must drive or compose those owners, not copy their rules.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make the current audit reproduction an exact non-overridable release gate</name>
  <files>scripts/check-v1-37-audit-reproduction.ts, scripts/check-v1-37-audit-reproduction.test.ts</files>
  <read_first>Permanent reproduction executable; Phase-257 current result JSON/Markdown and compatibility rulings; KERN-11; D-10/D-12; protected baseline.</read_first>
  <behavior>Tests first execute the current reproduction, require the exact seven-probe inventory and current repaired observations, and accept a semantic difference only when a pre-existing exact ruling names affected state, legality, event order, outcome, terminal semantics, observations, fixtures, requirements, and tuple versions. No boolean override, generic waiver, regenerated expected output, or missing infrastructure pass exists.</behavior>
  <action>Implement a pure release join that hashes the reproduction source, fresh result, retained Phase-257 result/rulings, and protected historical inputs. Emit only stable safe codes and hashes. If any proposed repair would change a valid Match semantic, stop execution for explicit compatibility approval rather than updating the baseline. Preserve the successful-push reversal ruling and all immutable v1.4 evidence.</action>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 --no-file-parallelism scripts/check-v1-37-audit-reproduction.test.ts &amp;&amp; pnpm exec tsx scripts/check-v1-37-audit-reproduction.ts</automated></verify>
  <acceptance_criteria>Fresh exact observations pass; every mismatched, missing, rewritten, generic-waiver, or insufficiently scoped ruling fixture fails closed without modifying audit artifacts.</acceptance_criteria>
  <done>PROOF-01 is an executable compatibility gate, not a prose assertion.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add real Go/PostgreSQL D-11 rollback and recomputation scenarios</name>
  <files>apps/go-backend/v1_37_release_rollback_test.go, packages/persistence/src/v1-37-release-rollback.test.ts</files>
  <read_first>apps/go-backend/completion_test.go atomic/idempotent cases; job_lifecycle_test.go stale/recovery cases; scoring_test.go current matrix cases; persistence complete-match and semantic-head transaction/compensation tests; D-11 exact list.</read_first>
  <behavior>Tests first cover lane kill switch and certificate staleness before scheduling and after claim, transaction failure before/after Chronicle and Match completion, exact retry/idempotence, cohort invalidation plus compensating reversal, standings/governance recomputation, runtime/service rollback to an explicit prior exact tuple, and mixed-state/tuple rejection at every boundary. Each failure asserts exact before/after database row and public result hashes.</behavior>
  <action>Add focused release tests that call existing Go lifecycle and persistence APIs under explicit DSNs. Use fault hooks/transaction triggers already established by completion and semantic-head tests; do not add proof-only mutation endpoints or new production classification logic. Require system-owned failures to leave gameplay, Chronicle, memory, outcome, counted rows, and player penalties unchanged, while approved cohort compensation remains append-only and recomputation-derived.</action>
  <verify><automated>DATABASE_URL="$DATABASE_URL" pnpm exec vitest run --maxWorkers=1 --no-file-parallelism packages/persistence/src/v1-37-release-rollback.test.ts &amp;&amp; cd apps/go-backend &amp;&amp; COWARDS_GO_BACKEND_TEST_DATABASE_URL="$COWARDS_GO_BACKEND_TEST_DATABASE_URL" PATH=/usr/local/go/bin:$PATH go test ./... -run TestV137ReleaseRollback -count=1</automated></verify>
  <acceptance_criteria>Every D-11 row executes against real PostgreSQL and normal Go/persistence owners; no configured database test skips; every system/identity failure is no-mutation and every correction is append/compensate/recompute.</acceptance_criteria>
  <done>The full service/persistence rollback matrix has direct database evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Capture the closed rollback and historical proof receipts</name>
  <files>scripts/run-v1-37-rollback-proof.ts, scripts/run-v1-37-rollback-proof.test.ts</files>
  <read_first>261-01 manifest/store; Task-1 audit gate; Task-2 focused suites; scripts/check-v1-36-historical-proof.ts archived-tag pattern; replay historical v1.4 tests.</read_first>
  <behavior>The runner requires explicit DSNs, executes every manifest rollback/history row without skips, stores raw receipts restricted-first, and exposes only safe scenario status/hash/limitation refs. It rechecks v1.4, explicit v1.17, and annotated v1.36 archive dispatch without modifying any historical bytes.</behavior>
  <action>Implement one focused orchestrator for audit, rollback, recomputation, and historical rows. It must invoke exact commands and parse structured receipts rather than infer a pass from exit labels. Add `v1.37:rollback-proof:write` as the sole database-mutating collector and `v1.37:rollback-proof:check` as a read-only missing/stale/hash/schema verifier. Tests run before artifacts and mutation-delete or stale every required receipt. Require two identical fresh runs where deterministic output applies; retain volatile timing/paths only in restricted evidence. On failure, return failed/incomplete and preserve evidence for diagnosis without any manual override field.</action>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 --no-file-parallelism scripts/run-v1-37-rollback-proof.test.ts &amp;&amp; COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1 pnpm v1.37:rollback-proof:write &amp;&amp; COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1 pnpm v1.37:rollback-proof:check &amp;&amp; COWARDS_V1_37_REQUIRE_INTEGRATED_PROOF=1 pnpm v1.37:rollback-proof:check</automated></verify>
  <acceptance_criteria>The safe receipt contains every D-11 and historical scenario exactly once, repeated deterministic roots agree, and any skip/failure/stale/mixed/history mutation blocks release.</acceptance_criteria>
  <done>PROOF-03/04 persistence, rollback, recomputation, retry, and immutable-history evidence is ready for the integrated rollup.</done>
</task>
</tasks>

## Artifacts this phase produces

- An exact current-audit reproduction/compatibility gate.
- DSN-required Go and persistence release rollback tests covering every D-11 row.
- Restricted rollback, recomputation, retry, and historical-dispatch receipts with safe references.

<threat_model>
## Trust Boundaries
| Boundary | Untrusted input |
|---|---|
| audit reproduction to release ruling | changed expectations, generic waivers, incomplete semantic descriptions |
| runtime/evidence state to Go scheduling/completion | stale/mixed identities and kill-switch races |
| transaction/correction to public standings | partial writes, duplicate retries, non-compensated history mutation |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-261-04A | Tampering | audit baseline | critical | mitigate | Fresh executable reproduction plus exact prior-ruling schema and source hashes. |
| T-261-04B | Repudiation | rollback/correction | high | mitigate | Transaction snapshots, append-only invalidation/compensation, deterministic recomputation receipts. |
| T-261-04C | Spoofing | retry/idempotence | high | mitigate | Exact condition/request/lease/receipt identity and duplicate equality checks. |
| T-261-04D | Information Disclosure | database/service failures | high | mitigate | Restricted raw receipts and stable safe public codes only. |
</threat_model>
<prohibitions>No expected-output rewrite, generic waiver, gameplay semantic change, proof-only production endpoint, partial correction, historical normalization, database skip, or manual pass is allowed.</prohibitions>
<verification>After 261-03 completes and releases shared services/database ownership, run audit reproduction directly, focused Vitest/Go suites with explicit DSNs, rollback write once, rollback check twice, historical proof, and protected baseline.</verification>
<success_criteria>Every persisted audit and D-11 rollback row passes executable exact evidence or stops for the only allowed explicit compatibility approval.</success_criteria>
<output>Create 261-04-SUMMARY.md.</output>
