---
phase: 51-service-contract-generation-and-route-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 51-02
files_modified:
  - package.json
  - scripts/check-service-boundary-imports.ts
autonomous: true
requirements:
  - GEN-03
  - GEN-07
must_haves:
  truths:
    - "Strict import guards fail when any named migrated route/page directly imports persistence roots, migration code, worker entrypoints, runtime adapters, or Strategy execution modules."
    - "A broader `apps/web/app` scan reports remaining direct persistence/runtime imports without blocking Phase 51."
    - "Contract drift, OpenAPI lint, service tests, web tests, and import guards compose into a runnable Phase 51 verification gate."
  artifacts:
    - path: "scripts/check-service-boundary-imports.ts"
      provides: "strict named-slice import guard plus report-only broader scan"
    - path: "package.json"
      provides: "root `boundary:imports` and phase contract/check command wiring"
  key_links:
    - from: "scripts/check-service-boundary-imports.ts"
      to: "apps/web/app/api/replays/[matchId]/metadata/route.ts"
      via: "strict named migrated file list"
      pattern: "api/replays"
    - from: "scripts/check-service-boundary-imports.ts"
      to: "apps/web/app"
      via: "report-only recursive scan"
      pattern: "reportOnly"
    - from: "package.json"
      to: "scripts/check-service-boundary-imports.ts"
      via: "`boundary:imports` script"
      pattern: "boundary:imports"
---

<objective>
Add Phase 51 boundary enforcement for the named migrated route slice.

Purpose: Satisfies D-10, D-11, and D-12 by making named migrated route/page direct imports strict while keeping the broader `apps/web/app` scan report-only. This proves the migration ratchet without blocking unrelated legacy server modules.
Output: Static import guard script, root command wiring, and a final phase verification command set.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-CONTEXT.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-RESEARCH.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-PATTERNS.md
@.planning/research/PITFALLS.md
@AGENTS.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-02-SUMMARY.md

<interfaces>
Named migrated files after Plan 51-02:
```txt
apps/web/app/api/service/health/route.ts
apps/web/app/api/matchsets/[matchSetId]/route.ts
apps/web/app/matchsets/[matchSetId]/page.tsx
apps/web/app/api/replays/[matchId]/metadata/route.ts
apps/web/app/strategies/[strategyId]/page.tsx
```

Forbidden direct import families for strict named slice:
```txt
@cowards/persistence
@cowards/worker
@cowards/runtime-js
@cowards/runtime-python
apps/worker
packages/runtime-js
packages/runtime-python
migrations
runWorkerOnce
StrategyExecutionAdapter
buildStrategyRevision
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create strict named-slice import guard with report-only app scan</name>
  <files>scripts/check-service-boundary-imports.ts</files>
  <read_first>
    - scripts/preflight.ts
    - packages/runtime-js/src/isolation-boundary.test.ts
    - apps/web/app/api/service/health/route.ts
    - apps/web/app/api/matchsets/[matchSetId]/route.ts
    - apps/web/app/matchsets/[matchSetId]/page.tsx
    - apps/web/app/api/replays/[matchId]/metadata/route.ts
    - apps/web/app/strategies/[strategyId]/page.tsx
    - .planning/phases/51-service-contract-generation-and-route-migration/51-CONTEXT.md
  </read_first>
  <behavior>
    - The script exits 1 when a strict named migrated file has a forbidden direct import.
    - The script exits 0 when only broader report-only `apps/web/app` findings exist.
    - The script avoids comment/string false positives by parsing import declarations or limiting regex checks to import/export-from lines.
  </behavior>
  <action>
    Create `scripts/check-service-boundary-imports.ts` using `tsx`-compatible ESM. Use deterministic sorted output. Define `strictMigratedFiles` exactly as:
    `apps/web/app/api/service/health/route.ts`,
    `apps/web/app/api/matchsets/[matchSetId]/route.ts`,
    `apps/web/app/matchsets/[matchSetId]/page.tsx`,
    `apps/web/app/api/replays/[matchId]/metadata/route.ts`,
    `apps/web/app/strategies/[strategyId]/page.tsx`.

    Detect direct import/export-from statements in those files and fail on forbidden specifiers or import text matching these exact families: `@cowards/persistence`, `@cowards/worker`, `@cowards/runtime-js`, `@cowards/runtime-python`, `apps/worker`, `packages/runtime-js`, `packages/runtime-python`, `migrations`, `runWorkerOnce`, `StrategyExecutionAdapter`, `buildStrategyRevision`. Print strict failures as `STRICT apps/web/...:line forbidden <pattern>`.

    Also recursively scan `apps/web/app` `.ts` and `.tsx` files, excluding `.next`, `.turbo`, `coverage`, `dist`, `node_modules`, `playwright-report`, and `test-results`. Print broad findings as `REPORT apps/web/...:line forbidden <pattern>` but do not set non-zero exit code for report-only findings. Include a summary line with exact counts: `strict_offenses=<n> report_only_offenses=<n>`. This implements D-10, D-11, and D-12 without broad strict enforcement.
  </action>
  <verify>
    <automated>pnpm exec tsx scripts/check-service-boundary-imports.ts</automated>
    <expected>Command exits 0 after Plan 51-02; output includes `strict_offenses=0` and may include report-only offenses for legacy modules.</expected>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/check-service-boundary-imports.ts` exits 0.
    - `rg -n 'strictMigratedFiles|strict_offenses|report_only_offenses' scripts/check-service-boundary-imports.ts` returns all three terms.
    - `rg -n 'apps/web/app/api/replays/\\[matchId\\]/metadata/route\\.ts' scripts/check-service-boundary-imports.ts` returns one match.
    - `pnpm exec tsx scripts/check-service-boundary-imports.ts | rg 'strict_offenses=0'` returns one match after the migrated slice is clean.
  </acceptance_criteria>
  <done>Strict named-slice import bypasses fail; broader app import debt is visible but report-only.</done>
</task>

<task type="auto">
  <name>Task 2: Wire root boundary command and final phase gate</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - packages/spec/package.json
    - scripts/check-service-boundary-imports.ts
    - .planning/phases/51-service-contract-generation-and-route-migration/51-RESEARCH.md
  </read_first>
  <action>
    Add root script `"boundary:imports": "pnpm exec tsx scripts/check-service-boundary-imports.ts"` to `package.json`. If Plan 51-01 did not add root `"contract:check"` and `"contract:lint"` scripts, add them now exactly as `"contract:check": "pnpm --filter @cowards/spec contract:check"` and `"contract:lint": "redocly lint --extends recommended packages/spec/artifacts/service-api-v1.8.openapi.json"`. Do not add broad strict `apps/web/app` enforcement to `test:fast`; the broad scan remains report-only in Phase 51 per D-11. Do not add generated TypeScript clients, API docs UX, GraphQL, gRPC, TypeSpec, auth/write/orchestration/runtime execution migration, or analytics migration.
  </action>
  <verify>
    <automated>pnpm boundary:imports</automated>
    <automated>pnpm --filter @cowards/spec contract:check</automated>
    <automated>pnpm contract:lint</automated>
    <automated>pnpm --filter @cowards/spec test</automated>
    <automated>pnpm --filter @cowards/service test</automated>
    <automated>pnpm --filter @cowards/web test</automated>
    <expected>All commands exit 0. `pnpm boundary:imports` prints `strict_offenses=0`; report-only counts are allowed for legacy app files.</expected>
  </verify>
  <acceptance_criteria>
    - `node -e 'const p=require("./package.json"); for (const k of ["boundary:imports","contract:check","contract:lint"]) if (!p.scripts[k]) process.exit(1)'` exits 0.
    - `pnpm boundary:imports | rg 'strict_offenses=0'` returns one match.
    - `rg -n 'test:fast.*boundary:imports' package.json` returns no matches unless all broader `apps/web/app` report-only findings are zero and the command remains stable.
  </acceptance_criteria>
  <done>GEN-03 and GEN-07 are runnable from root scripts without accidentally making report-only broad debt blocking.</done>
</task>

<task type="auto">
  <name>Task 3: Run integrated Phase 51 verification and inspect generated artifact</name>
  <files>packages/spec/artifacts/service-api-v1.8.openapi.json</files>
  <read_first>
    - packages/spec/artifacts/service-api-v1.8.openapi.json
    - package.json
    - .planning/phases/51-service-contract-generation-and-route-migration/51-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Run the final Phase 51 verification set and inspect the generated artifact for the required v1.8 public route surface. The artifact must include operations for `health`, `getPublicMatchSetSummary`, `getPublicReplayMetadata`, and `getPublicStrategyPage`. Confirm no public schema or example contains forbidden private keys. Record any remaining broad report-only `apps/web/app` findings in the plan summary; do not turn them into blocking failures in this phase unless they are in the strict named migrated files.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/spec contract:check</automated>
    <automated>pnpm contract:lint</automated>
    <automated>pnpm boundary:imports</automated>
    <automated>pnpm --filter @cowards/spec test</automated>
    <automated>pnpm --filter @cowards/service test</automated>
    <automated>pnpm --filter @cowards/web test</automated>
    <automated>node -e 'const a=require("./packages/spec/artifacts/service-api-v1.8.openapi.json"); const ids=JSON.stringify(a); for (const id of ["getPublicMatchSetSummary","getPublicReplayMetadata","getPublicStrategyPage"]) if (!ids.includes(id)) process.exit(1)'</automated>
    <expected>All checks exit 0 and the artifact contains the named public read operations.</expected>
  </verify>
  <acceptance_criteria>
    - `pnpm boundary:imports | rg 'strict_offenses=0'` returns one match.
    - `node -e 'const s=JSON.stringify(require("./packages/spec/artifacts/service-api-v1.8.openapi.json")); for (const k of ["strategyMemory","soldierMemory","objectivePayload","ownerDebug","awarenessGrid","stackTrace","stderr","tokens","hostPath","privateRuntimeInternals"]) if (s.includes(k)) process.exit(1)'` exits 0.
    - `git diff -- packages/spec/artifacts/service-api-v1.8.openapi.json` shows generated OpenAPI changes only, with no manual-only formatting churn after rerunning `contract:generate`.
  </acceptance_criteria>
  <done>Phase 51 has a single command set proving contract freshness, lint validity, strict named import safety, public privacy, and migrated read behavior.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 51 | Regenerate stable service artifacts and route selected low-risk reads through typed service boundary | 51-01, 51-02, 51-03 | COVERED | Final enforcement plan composes the gates.
REQ | GEN-01 | Deterministic artifact | 51-01 | COVERED | Generator and committed artifact.
REQ | GEN-02 | Route metadata completeness | 51-01 | COVERED | Metadata tests.
REQ | GEN-03 | Lint/stale-output checks | 51-01, 51-03 | COVERED | Redocly and contract check.
REQ | GEN-04 | Public DTO privacy separation | 51-01, 51-02 | COVERED | Schema/example/service tests.
REQ | GEN-05 | Named low-risk reads use service | 51-02 | COVERED | Migration slice.
REQ | GEN-06 | Behavior and privacy preserved | 51-02 | COVERED | Service/web tests.
REQ | GEN-07 | Strict import guard | 51-03 | COVERED | This plan.
CONTEXT | D-10 | Strict named-slice guard | 51-03 | COVERED | Task 1.
CONTEXT | D-11 | Broader report-only app scan | 51-03 | COVERED | Task 1.
CONTEXT | D-12 | Fail forbidden direct imports | 51-03 | COVERED | Task 1.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated web files -> forbidden implementation modules | Static imports can bypass the typed service boundary before runtime. |
| report-only app scan -> developer diagnostics | Broad app findings reveal legacy debt without blocking this phase. |
| root scripts -> local/CI enforcement | Developers need a stable command that fails on named migrated bypasses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03-01 | E | named migrated route/page files | mitigate | `boundary:imports` exits 1 on direct imports of persistence, worker, runtime, migration, or Strategy execution modules. |
| T-51-03-02 | T | broad legacy web modules | accept | Report-only scan is intentional per D-11; strict broader enforcement is excluded until a later phase. |
| T-51-03-03 | R | boundary check diagnostics | mitigate | Script prints deterministic file:line offense output plus strict/report counts. |
| T-51-03-04 | I | generated public artifact | mitigate | Final verification scans artifact JSON for forbidden private key strings. |
</threat_model>

<verification>
Final Phase 51 command set:

```bash
pnpm --filter @cowards/spec contract:check
pnpm contract:lint
pnpm boundary:imports
pnpm --filter @cowards/spec test
pnpm --filter @cowards/service test
pnpm --filter @cowards/web test
```

Expected output: all commands exit 0. `boundary:imports` prints `strict_offenses=0`; report-only findings are documented in the summary if present.
</verification>

<success_criteria>
- Strict named migrated route/page files cannot directly import persistence roots, migration code, worker entrypoints, runtime adapters, or Strategy execution modules.
- Broad `apps/web/app` scan reports remaining direct imports without blocking Phase 51.
- Contract drift, OpenAPI lint, service behavior, web behavior, and import boundaries are all covered by runnable commands.
</success_criteria>

<output>
After completion, create `.planning/phases/51-service-contract-generation-and-route-migration/51-03-SUMMARY.md`.
</output>
