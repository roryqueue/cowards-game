# Phase 216 Plan: Competition Hub and Competition Detail

## Tasks

1. Build `/competitions` from `getPublicCompetitionIndex`.
2. Build `/competitions/[competitionId]` from `getPublicCompetitionDetail`.
3. Represent exhibitions now and configured ladder public reads when available.

## Verification

- Playwright competition index -> detail journey.
- Public discovery service unit tests.
