---
phase: 252-counted-state-classifier-and-standings-recompute
plan: 02
type: execute
wave: 2
depends_on: [252-01]
autonomous: true
requirements: [RESULT-03, RESULT-04, RESULT-05, RESULT-06]
files_modified:
  - packages/persistence/src/standings-recompute.ts
  - packages/persistence/src/standings-recompute.test.ts
  - packages/persistence/src/ladder.ts
  - packages/persistence/src/ladder.test.ts
must_haves:
  truths:
    - Standings are a pure deterministic reduction of one Season's canonical counted inputs.
    - Excluded states never contribute points and remain visible as evidence/exclusion counts.
    - Public ladder GET no longer mutates MatchSet lifecycle.
  artifacts:
    - path: packages/persistence/src/standings-recompute.ts
      provides: Pure Season standings reducer and stable tie-break/evidence summaries
    - path: packages/persistence/src/standings-recompute.test.ts
      provides: Repetition, permutation, exclusion, tie-break, and cross-Season tests
    - path: packages/persistence/src/ladder.ts
      provides: Canonical Season reader using spec classifier and pure reducer
    - path: packages/persistence/src/ladder.test.ts
      provides: SQL Season scope and no-GET-mutation tests
  key_links:
    - from: packages/spec/src/competition-counted-state.ts
      to: packages/persistence/src/standings-recompute.ts
      via: classifier projection accepted as reducer input
    - from: packages/persistence/src/ladder.ts
      to: packages/persistence/src/standings-recompute.ts
      via: Season-scoped canonical rows
    - from: packages/persistence/src/standings-recompute.ts
      to: packages/spec/src/competition.ts
      via: PublicStandingDto evidence summaries
---

# Plan 252-02: Deterministic Season Standings Recompute

<objective>Replace read-time ad hoc aggregation with a pure Season-scoped recompute pipeline.</objective>

<tasks>
<task id="252-02-01" type="auto" tdd="true">
  <name>Build the pure standings reducer</name>
  <files>packages/persistence/src/standings-recompute.ts, packages/persistence/src/standings-recompute.test.ts</files>
  <action>Define canonical entrant and classified MatchSet inputs. Reduce only `counted` scoring, preserve the existing tie-break order, and accumulate per-entrant counted/excluded counts, evidence availability, and deduplicated stable result/replay links. Add repeated-run and input-permutation determinism tests.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/standings-recompute.test.ts</automated></verify>
  <done>The same Season input always returns byte-equivalent ordered public standings.</done>
</task>
<task id="252-02-02" type="auto" tdd="true">
  <name>Use the canonical classifier and reducer in public ladder reads</name>
  <files>packages/persistence/src/ladder.ts, packages/persistence/src/ladder.test.ts</files>
  <action>Remove local `classifyCountedStatus`, `addScore`, and GET-time `refreshMatchSetStatus`. Read stored status/scoring/Chronicle counts/governance state for one Season, call the spec classifier, project typed MatchSet states, and pass results to the pure reducer. Add source/SQL assertions that no cross-Season rows or lifecycle writes occur.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/ladder.test.ts packages/persistence/src/standings-recompute.test.ts</automated></verify>
  <done>Public Season reads are mutation-free and standings contain only canonical counted evidence.</done>
</task>
<task id="252-02-03" type="auto" tdd="true">
  <name>Prove exclusion and stable evidence behavior</name>
  <files>packages/persistence/src/standings-recompute.test.ts, packages/persistence/src/ladder.test.ts</files>
  <action>Add scenarios for degraded, non-counted, non-competitive, disputed, under-review, invalid, and invalidated MatchSets. Prove points never leak from exclusions, counts/links remain public, ties remain stable, and a second Season cannot contaminate the first.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/standings-recompute.test.ts packages/persistence/src/ladder.test.ts</automated></verify>
  <done>All exclusion states are visible but contribute zero standings score across repeated recompute.</done>
</task>
</tasks>

<verification>`pnpm --filter @cowards/persistence typecheck`</verification>
