---
phase: 252-counted-state-classifier-and-standings-recompute
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [RESULT-01, RESULT-02, RESULT-04]
files_modified:
  - packages/spec/src/competition-counted-state.ts
  - packages/spec/src/competition-counted-state.test.ts
  - packages/spec/src/competition.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/index.ts
  - packages/persistence/migrations/0010_competition_counted_states.sql
must_haves:
  truths:
    - One pure classifier covers the full v1.36 counted-state vocabulary and never trusts stored counted without complete evidence.
    - Every public state has calm explanation, standings effect, and evidence availability.
  artifacts:
    - path: packages/spec/src/competition-counted-state.ts
      provides: Canonical classifier input, precedence, and public projection copy
    - path: packages/spec/src/competition-counted-state.test.ts
      provides: Full matrix, precedence, determinism, and privacy tests
    - path: packages/spec/src/competition.ts
      provides: Typed MatchSet/result counted-state projections and standing evidence summary
    - path: packages/spec/src/schemas.ts
      provides: Runtime validation for counted-state and evidence fields
    - path: packages/persistence/migrations/0010_competition_counted_states.sql
      provides: Expanded repeatable counted/reason constraints
  key_links:
    - from: packages/spec/src/competition-policy-v1-36.ts
      to: packages/spec/src/competition-counted-state.ts
      via: exact public state vocabulary
    - from: packages/spec/src/competition-counted-state.ts
      to: packages/spec/src/competition.ts
      via: shared public projection types
    - from: packages/spec/src/competition-counted-state.ts
      to: packages/persistence/migrations/0010_competition_counted_states.sql
      via: storage enum parity
---

# Plan 252-01: Canonical Counted-State Contract

<objective>Define the single counted-state classifier, public copy, DTO fields, schemas, and storage vocabulary.</objective>

<tasks>
<task id="252-01-01" type="auto" tdd="true">
  <name>Implement the canonical classifier matrix</name>
  <files>packages/spec/src/competition-counted-state.ts, packages/spec/src/competition-counted-state.test.ts, packages/spec/src/index.ts</files>
  <action>Add RED table tests for all execution/evidence/governance combinations, then implement a pure classifier with governance precedence. Require complete status, scoring, positive Match count, and equal Chronicle count before `counted`; stored counted alone is insufficient. Add deterministic public label/explanation/standings-effect/evidence-availability copy and leak guard.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/competition-counted-state.test.ts</automated></verify>
  <done>All ten public states and precedence paths are exhaustive, deterministic, and leak-safe.</done>
</task>
<task id="252-01-02" type="auto" tdd="true">
  <name>Extend public MatchSet, result, and standings contracts</name>
  <files>packages/spec/src/competition.ts, packages/spec/src/schemas.ts, packages/spec/src/competition-counted-state.test.ts</files>
  <action>Add typed counted-state projection to `PublicLadderMatchSetSummaryDto` and public MatchSet result trust metadata. Add standing evidence summary fields for counted/excluded MatchSet counts, evidence availability, and stable result/replay links. Keep general result scoring fields unchanged.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/competition-counted-state.test.ts packages/spec/src/spec.test.ts</automated></verify>
  <done>Interfaces and Zod schemas require the same public-safe classifier/evidence shape.</done>
</task>
<task id="252-01-03" type="auto">
  <name>Expand counted-state storage constraints</name>
  <files>packages/persistence/migrations/0010_competition_counted_states.sql</files>
  <action>Add a repeatable migration that drops/recreates counted-status and public-reason checks with the full v1.36 storage vocabulary, including degraded system failure, disputed, and invalidated. Preserve existing rows and do not store private dispute diagnostics.</action>
  <verify><automated>rg -n "degraded_system_failure|disputed|invalidated|drop constraint if exists" packages/persistence/migrations/0010_competition_counted_states.sql</automated></verify>
  <done>Storage accepts every canonical state and remains safe to rerun.</done>
</task>
</tasks>

<verification>`pnpm --filter @cowards/spec typecheck` and `pnpm v1.36:competition-policy:check`</verification>
