---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/calibrate-v1-37-runtime-abi.ts
  - scripts/calibrate-v1-37-runtime-abi.test.ts
  - packages/spec/src/runtime-abi-v1-17.ts
  - packages/spec/src/runtime-abi-v1-17.test.ts
  - packages/spec/artifacts/runtime-abi-v1.17-contract.json
  - .planning/artifacts/v1.37-runtime-abi-calibration.json
  - .planning/artifacts/v1.37-runtime-abi-calibration.md
autonomous: true
requirements: [RABI-01, RABI-03, RABI-06, RABI-07, RABI-08]
must_haves:
  truths:
    - "One executable calibration freezes exact canonical-JSON ceilings, field caps, budget units and measurement boundaries before codec or adapter work begins (D-02 through D-08)."
    - "The successor is explicitly runtime ABI/service/receipt v1.17; v1.16 insertion-ordered receipt bytes and verification remain immutable historical evidence (D-13 through D-16)."
    - "Missing equivalent meters or exact executable/toolchain identity is represented as uncertified, never inferred from documentation or a gate name."
  artifacts:
    - path: "packages/spec/artifacts/runtime-abi-v1.17-contract.json"
      provides: "Frozen package-free canonical JSON, budget, ownership, and identity contract"
    - path: ".planning/artifacts/v1.37-runtime-abi-calibration.json"
      provides: "Executable ceiling and budget calibration receipt"
  key_links:
    - from: "scripts/calibrate-v1-37-runtime-abi.ts"
      to: "packages/spec/src/runtime-abi-v1-17.ts"
      via: "candidate observations checked against one frozen contract"
      pattern: "RUNTIME_ABI_V1_17"
---
<objective>Freeze the exact language-neutral successor ABI contract and its evidence-derived numerical ceilings before implementation fans out.</objective>
<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-CONTEXT.md
@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-RESEARCH.md
@.planning/artifacts/v1.37-kernel-integrity-proof.json
@.planning/artifacts/v1.37-v1.36-historical-proof-dispatch.json
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Calibrate exact parser ceilings and field caps</name>
  <files>scripts/calibrate-v1-37-runtime-abi.ts, scripts/calibrate-v1-37-runtime-abi.test.ts, .planning/artifacts/v1.37-runtime-abi-calibration.json, .planning/artifacts/v1.37-runtime-abi-calibration.md</files>
  <read_first>258-CONTEXT.md D-01 through D-04; 258-RESEARCH.md Canonical JSON Profile; packages/spec/src/schemas.ts; existing StrategyMemory, SoldierMemory, objective, HTTP, Go, WASM, output, and stderr caps; permanent deep-audit reproduction.</read_first>
  <behavior>
    - Scan every committed current contract/fixture without recursive traversal and record maximum raw bytes, depth, nodes, decoded string bytes, array entries, and object entries.
    - Run N-1/N/N+1 and hostile allocation probes for every candidate ceiling; produce deterministic typed observations rather than process exceptions.
    - Distinguish raw UTF-8, canonical payload, decoded string, and transport-frame byte units; lower domain caps remain explicit.
  </behavior>
  <action>Write RED tests first. Implement a package-free calibration evaluator that inventories committed inputs, tests the research candidates, records deterministic maxima and bounded performance evidence, and freezes exact reviewed values only when every existing valid artifact fits and every over-limit probe fails predictably. Do not treat current local tool versions as counted pins. Any candidate that admits uncontrolled allocation or excludes a valid v1.4 artifact stops this plan.</action>
  <verify><automated>pnpm exec vitest run scripts/calibrate-v1-37-runtime-abi.test.ts &amp;&amp; pnpm exec tsx scripts/calibrate-v1-37-runtime-abi.ts --write --check</automated></verify>
  <acceptance_criteria>Calibration JSON and Markdown agree byte-for-byte on exact global and lower field limits; all ±1 probes are represented; depth 3,000 is a typed rejection, not RangeError.</acceptance_criteria>
  <done>Exact ceilings are executable, deterministic, and frozen before downstream code consumes them.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Freeze successor ownership budget and identity registry</name>
  <files>packages/spec/src/runtime-abi-v1-17.ts, packages/spec/src/runtime-abi-v1-17.test.ts, packages/spec/artifacts/runtime-abi-v1.17-contract.json</files>
  <read_first>Task 1 calibration receipt; Phase-257 v1.16 wire golden and Go verifier; packages/spec/src/runtime.ts; packages/spec/src/runtime-execution-service.ts; packages/spec/src/runtime-evidence-attestation.ts.</read_first>
  <behavior>
    - Freeze canonical JSON grammar/numbers/Unicode/key order/error codes/ownership contexts per D-01 through D-04.
    - Freeze per-selectActivations, per-SoldierBrain, cumulative Match, and separate preflight vectors with wall/compute/memory/output/process/capability units and accounting boundaries per D-05 through D-12.
    - Freeze domain-tagged length-framed SHA-256 identities for original source, normalized source, artifact, manifest, executable, tuple, policy, corpus, and evidence per D-13 through D-16.
    - Define an atomic v1.17 migration map while retaining v1.16 dispatch and exact insertion-ordered bytes unchanged.
  </behavior>
  <action>Create an immutable typed registry and generated JSON contract from calibration output. Make all three outcomes discriminated and mixed fields unrepresentable. Name unsupported meters explicitly and map them to uncertified status. Include stable public-safe IDs separately from private evidence. Add a structural test that hashes the protected v1.16 golden/verifier inputs before and after generation and rejects any in-place serializer or migration-0017 rewrite. Add no dependency.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/runtime-abi-v1-17.test.ts &amp;&amp; pnpm exec tsx scripts/calibrate-v1-37-runtime-abi.ts --check</automated></verify>
  <acceptance_criteria>The v1.17 contract has exact values and a complete D-01..D-16 ownership/migration map; historical v1.16 hashes are unchanged; unsupported enforcement fails closed.</acceptance_criteria>
  <done>All downstream plans have one exact contract and no discretion to reinterpret units, ownership, identity, or historical bytes.</done>
</task>
</tasks>

## Artifacts this phase produces

- `packages/spec/src/runtime-abi-v1-17.ts` — sole typed successor contract.
- `packages/spec/artifacts/runtime-abi-v1.17-contract.json` — generated cross-language contract.
- `.planning/artifacts/v1.37-runtime-abi-calibration.{json,md}` — deterministic freeze evidence.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Convenient limits or units create cross-language drift | high | Executable inventory, ±1 probes, exact units, generated single registry. |
| Successor silently rewrites v1.16 history | critical | Hash-pinned v1.16 golden/verifier guard and separate v1.17 identifiers. |
| Documentation promotes an unmetered lane | high | Unsupported meters are machine-readable uncertified state. |
| Package supply-chain expansion | low | No install and standard-library implementation only. |
</threat_model>
<verification>Both tasks must pass in order; inspect generated diff for exact ceilings, version identifiers, and unchanged historical hashes.</verification>
<success_criteria>One frozen v1.17 ABI contract governs all later Phase-258 work while v1.16 remains immutable historical evidence.</success_criteria>
<output>Create `258-01-SUMMARY.md`.</output>
