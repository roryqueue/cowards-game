---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "06"
type: execute
wave: 4
depends_on: ["258-01", "258-03", "258-04"]
files_modified:
  - packages/spec/src/runtime-invocation-v1-17.ts
  - packages/spec/src/runtime-invocation-v1-17.test.ts
  - packages/spec/src/runtime.ts
  - packages/spec/src/runtime-execution-service.ts
  - packages/spec/artifacts/runtime-execution-service-request.v1.17.candidate.json
  - packages/spec/artifacts/runtime-execution-service-response.v1.17.candidate.wire.json
autonomous: true
requirements: [RABI-03, RABI-04, RABI-05, RABI-08]
must_haves:
  truths:
    - "Every successor boundary has exactly one discriminant: success, player_violation, or system_failure; mixed fields are type/schema impossible (D-09 through D-12)."
    - "The authenticated outer envelope is adapter-owned and binds request, method, tuple, ABI, source/artifact, budget, input, retry and payload digest."
  artifacts:
    - path: "packages/spec/src/runtime-invocation-v1-17.ts"
      provides: "Three-way successor request/response ABI"
---
<objective>Define the versioned three-way invocation ABI and authenticated adapter-owned envelope before runtime migration.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-01-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-03-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make three-way ownership structurally exclusive</name>
  <files>packages/spec/src/runtime-invocation-v1-17.ts, packages/spec/src/runtime-invocation-v1-17.test.ts, packages/spec/src/runtime.ts, packages/spec/src/runtime-execution-service.ts</files>
  <read_first>Context D-05 through D-12; existing RuntimeResult dual shapes; Python/WASM synthetic THROWN_EXCEPTION paths; Phase-257 kernel resume types.</read_first>
  <behavior>
    - Success carries only value/trace; player violation carries only canonical violation/trace; system failure carries only redacted failure/trace.
    - Missing/truncated/unauthenticated/wrong-binding outer frames and adapter/runtime/host crashes are system failures.
    - Canonically decoded Strategy payload invalidity and proven Strategy exception/exhaustion are player violations; ambiguity is system failure.
  </behavior>
  <action>Write compile-time/type and runtime schema negatives first. Introduce successor types without changing historical v1.16 definitions. Remove dual-shape construction only from successor branches and centralize exception conversion at the owning boundary. Trace contains safe codes and hashes, never source/output/memory/diagnostics.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/runtime-invocation-v1-17.test.ts &amp;&amp; pnpm --filter @cowards/spec typecheck</automated></verify>
  <acceptance_criteria>Every mixed or absent discriminant fixture fails; the complete ownership matrix has one exact expected kind.</acceptance_criteria>
  <done>Mixed player/system classifications are unrepresentable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Generate authenticated candidate wire envelopes</name>
  <files>packages/spec/artifacts/runtime-execution-service-request.v1.17.candidate.json, packages/spec/artifacts/runtime-execution-service-response.v1.17.candidate.wire.json, packages/spec/src/runtime-invocation-v1-17.test.ts</files>
  <read_first>Canonical encoder/domain registry; v1.16 TS-issued wire golden and Go exact-byte test; successor migration map.</read_first>
  <behavior>
    - Request/response canonical bytes bind invocation/kernel request IDs, method, semantic tuple, ABI, revision/artifact, budget profile/hash, input/payload hash and retry identity.
    - Adapter authentication covers the outer envelope; Strategy stdout is only payload material.
    - Candidate generation leaves v1.16 artifacts byte-identical.
  </behavior>
  <action>Generate candidate v1.17 canonical wire fixtures with deterministic test signing identity and verify tamper/wrong-request/wrong-budget/truncation negatives. Keep candidate explicitly non-current until Plan 14. Compare pinned v1.16 hashes before/after.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/runtime-invocation-v1-17.test.ts packages/spec/src/service-contract.test.ts</automated></verify>
  <acceptance_criteria>Canonical candidate bytes and signature inputs are exact; all binding tamper cases fail system-owned; v1.16 bytes are unchanged.</acceptance_criteria>
  <done>Runtime adapters can migrate to one authenticated successor envelope without self-authenticating guest output.</done>
</task>
</tasks>

## Artifacts this phase produces

- Three-way invocation ABI and v1.17 candidate request/response wire fixtures.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Guest forges transport authority | critical | Adapter-owned authenticated outer envelope. |
| Mixed failure shape causes penalty | critical | Discriminated union and schema negatives. |
| Candidate accidentally becomes current | high | Candidate IDs and activation deferred to Plan 14. |
</threat_model>
<verification>Type/schema/wire/tamper suites plus pinned historical hashes.</verification>
<success_criteria>All language adapters have one exclusive, authenticated successor ABI contract.</success_criteria>
<output>Create `258-06-SUMMARY.md`.</output>
