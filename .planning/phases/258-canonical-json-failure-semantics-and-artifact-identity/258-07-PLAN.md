---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "07"
type: execute
wave: 6
depends_on: ["258-05", "258-06"]
files_modified:
  - packages/engine/src/kernel/types.ts
  - packages/engine/src/kernel/driver.ts
  - packages/engine/src/kernel/runtime-ownership.test.ts
  - apps/runtime-service/src/execute-match.ts
  - apps/runtime-service/src/execute-match.test.ts
  - apps/runtime-service/src/semantic-integrity.test.ts
autonomous: true
requirements: [RABI-03, RABI-04, RABI-05]
must_haves:
  truths:
    - "Only the Phase-257 kernel driver converts a valid player violation into the existing v1.4 consequence; adapters/services never emit gameplay penalties (D-09 through D-11)."
    - "System failure returns byte-equivalent machine/memory/Chronicle/result state and retry reuses the identical request/prestate/budgets (D-12)."
  artifacts:
    - path: "packages/engine/src/kernel/runtime-ownership.test.ts"
      provides: "Three-way ownership and no-mutation proof"
---
<objective>Integrate the successor three-way result with the sole engine transition authority and runtime-service without gameplay drift.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-05-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-06-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove engine-only penalty and temporary response commit</name>
  <files>packages/engine/src/kernel/types.ts, packages/engine/src/kernel/driver.ts, packages/engine/src/kernel/runtime-ownership.test.ts</files>
  <read_first>Phase-257 kernel driver/types and compatibility fixtures; successor InvocationResult; KERN-10/KERN-11 proof receipt.</read_first>
  <behavior>
    - Valid response/memory commits only after full canonical/schema/legality validation.
    - Player violation discards valid prefixes, partial/oversized nested memory and illegal Action, preserves prior memories, then emits only existing canonical v1.4 consequence.
    - System failure returns unchanged state/events/outcome/observations and no player penalty.
  </behavior>
  <action>Write the full prefix/partial/oversize/illegal-action matrix first. Adapt the existing driver seam to the exclusive result union; do not add scheduling or penalty logic elsewhere. Hash pre/post machine, both memories, Chronicle, outcome, and Strategy observations. Any valid v1.4 behavior delta stops for approval instead of changing fixtures.</action>
  <verify><automated>pnpm exec vitest run packages/engine/src/kernel/runtime-ownership.test.ts packages/engine/src/kernel/kernel-contract.test.ts packages/engine/src/compatibility-fixtures.test.ts</automated></verify>
  <acceptance_criteria>Every player case has only the established penalty/event and prior memory; every system case has exact pre==post hashes.</acceptance_criteria>
  <done>The engine remains the sole gameplay classification authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bind service request response and bounded retry identity</name>
  <files>apps/runtime-service/src/execute-match.ts, apps/runtime-service/src/execute-match.test.ts, apps/runtime-service/src/semantic-integrity.test.ts</files>
  <read_first>Engine Task 1 API; Phase-257 execute-match/semantic receipt tests; v1.17 request/response candidate wire.</read_first>
  <behavior>
    - Service passes exact invocation/kernel/method/tuple/ABI/source/artifact/budget/input/retry bindings to driver.
    - Go-owned retry policy can retry only system failure from identical prestate/request/budgets; service never retries player violations.
    - Public/default output exposes safe classification and IDs only.
  </behavior>
  <action>Wire candidate successor semantics behind explicit version dispatch while v1.16 remains historical. Add wrong-binding, retry-mutation, partial-memory, adapter-crash and privacy tests. Do not claim live Go/database topology here.</action>
  <verify><automated>pnpm --filter @cowards/runtime-service test &amp;&amp; pnpm --filter @cowards/engine test</automated></verify>
  <acceptance_criteria>Exact bindings reach the kernel; retry identity cannot change; system failures never mutate or leak.</acceptance_criteria>
  <done>Service and engine agree on one three-way ownership boundary with exact no-mutation semantics.</done>
</task>
</tasks>

## Artifacts this phase produces

- Engine/runtime-service ownership integration and executable no-mutation matrix.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Partial memory commits before validation | critical | Temporary values and full-response commit gate. |
| Adapter/service invents penalty | critical | Structural ownership test and sole driver conversion. |
| Retry changes request/prestate | high | Signed identity and exact hash equality. |
</threat_model>
<verification>Run focused ownership tests then full engine/service suites.</verification>
<success_criteria>Three-way outcomes preserve v1.4 consequences and make every infrastructure failure no-mutation.</success_criteria>
<output>Create `258-07-SUMMARY.md`.</output>
