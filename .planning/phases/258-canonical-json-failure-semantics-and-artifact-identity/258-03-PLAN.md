---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "03"
type: execute
wave: 3
depends_on: ["258-02"]
files_modified:
  - packages/spec/src/canonical-json-scan.ts
  - packages/spec/src/canonical-json-scan.test.ts
  - packages/spec/src/canonical-json-parse.ts
  - packages/spec/src/canonical-json-parse.test.ts
autonomous: true
requirements: [RABI-01, RABI-02, RABI-03]
must_haves:
  truths:
    - "Raw bytes are scanned iteratively for grammar, UTF-8/scalars, duplicates, numbers, ordering and every frozen ceiling before host object conversion (D-01 through D-04)."
    - "Adversarial depth/allocation returns one typed bounded error and never throws or overflows."
  artifacts:
    - path: "packages/spec/src/canonical-json-scan.ts"
      provides: "Iterative raw-byte scanner"
    - path: "packages/spec/src/canonical-json-parse.ts"
      provides: "Bounded post-scan materializer"
---
<objective>Implement the spec-owned iterative canonical JSON scanner/parser against the frozen corpus.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-02-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build iterative raw-byte scanner</name>
  <files>packages/spec/src/canonical-json-scan.ts, packages/spec/src/canonical-json-scan.test.ts</files>
  <read_first>runtime-abi-v1-17.ts; canonical-json corpus manifest/raw fixtures; packages/spec/src/schemas.ts recursive JsonValue; Go strict JSON admission for error/path conventions.</read_first>
  <behavior>
    - Explicit stack tracks offset, depth, nodes, per-container entries, decoded string bytes, key bytes, and ownership context.
    - Duplicate detection uses decoded key equality before object construction; escaped-equivalent keys collide.
    - UTF-8/scalar, numeric grammar/range, and canonical-key-order failures carry stable code/path/offset without raw private data.
    - Scanner never recurses and refuses allocation beyond frozen limits.
  </behavior>
  <action>Drive implementation from failing corpus subsets in RED→GREEN order. Scan `Uint8Array` directly with bounded state; use no host JSON parser/tokenizer as the first operation and no recursive value walk. Retain only bounded key/location state needed for duplicate/path reporting. Catch no programming error as a player failure. Add allocation/depth tests and verify deterministic error precedence.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/canonical-json-scan.test.ts packages/spec/src/canonical-json-corpus.test.ts</automated></verify>
  <acceptance_criteria>Every scan vector has exact expected success/error; 3,000-depth and oversized inputs are typed; duplicates are caught before materialization.</acceptance_criteria>
  <done>The first operation on untrusted JSON bytes is iterative, bounded, and deterministic.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Materialize only scanner-approved bounded values</name>
  <files>packages/spec/src/canonical-json-parse.ts, packages/spec/src/canonical-json-parse.test.ts</files>
  <read_first>Task 1 scanner API/tests; canonical JSON success vectors; packages/spec/src/types.ts JsonValue definitions.</read_first>
  <behavior>
    - Materialization runs only after successful scan under the same profile/context and returns a discriminated typed result.
    - Negative zero becomes zero; exact strings retain normalization form; unsafe integers cannot appear.
    - Host parser exceptions are system-owned implementation failures, never player violations.
  </behavior>
  <action>Implement bounded materialization over scanner tokens/validated bytes, preserving number/string semantics. If a standard parser is used after scan, bind it to the scan receipt/hash and convert impossible disagreement to system failure. Add mutation/receipt mismatch tests so changed bytes cannot reuse approval.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/canonical-json-parse.test.ts packages/spec/src/canonical-json-corpus.test.ts</automated></verify>
  <acceptance_criteria>All positive vectors materialize exactly; no negative vector reaches host conversion; scan/materialization disagreement is system failure.</acceptance_criteria>
  <done>Canonical parsing is bounded and cannot lose raw-byte validation evidence.</done>
</task>
</tasks>

## Artifacts this phase produces

- Iterative raw-byte scanner and bounded materializer with typed ownership-aware errors.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Stack/heap exhaustion before a limit check | critical | Explicit stack and counters before allocation. |
| Duplicate/error precedence differs by runtime | high | Frozen corpus exact code/path/offset. |
| Parser disagreement becomes player penalty | high | Impossible post-scan disagreement is system-owned. |
</threat_model>
<verification>Run scanner, parser, and shared corpus tests together under constrained heap where supported.</verification>
<success_criteria>Untrusted JSON cannot recurse, allocate unboundedly, or reach object conversion before all canonical checks pass.</success_criteria>
<output>Create `258-03-SUMMARY.md`.</output>
