---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "11"
type: execute
wave: 7
depends_on: ["258-05", "258-06", "258-07", "258-08", "258-09", "258-10"]
files_modified:
  - apps/go-backend/runtime_service_client.go
  - apps/go-backend/runtime_service_client_test.go
  - apps/go-backend/runtime_execution_contract_gen.go
  - apps/go-backend/completion.go
  - apps/go-backend/completion_test.go
  - packages/persistence/src/complete-match.ts
  - packages/persistence/src/complete-match.test.ts
autonomous: true
requirements: [RABI-01, RABI-02, RABI-03, RABI-04, RABI-05, RABI-06, RABI-08]
must_haves:
  truths:
    - "Go decodes and encodes the v1.17 canonical profile with the same duplicate, numeric, Unicode, order, and bounded-limit outcomes as TypeScript."
    - "Every request signs the exact pre-state, method, tuple, identity, input and per-method/cumulative budget vector; any retry is byte-identical and system-owned."
    - "System failure and persistence failure commit no Chronicle, result, standings, StrategyMemory, SoldierMemory, or gameplay mutation."
  artifacts:
    - path: "apps/go-backend/runtime_service_client_test.go"
      provides: "Go canonical parity, signed-budget retry, and three-way response proof"
    - path: "apps/go-backend/completion_test.go"
      provides: "Go completion rollback and no-partial-persistence proof"
---
<objective>Bring the Go authority and persistence seam onto the v1.17 canonical request, signed budget, retry, and no-mutation contract.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-05-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-06-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-07-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-08-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-09-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-10-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Go canonical parity and exact signed retry binding</name>
  <files>apps/go-backend/runtime_service_client.go, apps/go-backend/runtime_service_client_test.go, apps/go-backend/runtime_execution_contract_gen.go</files>
  <read_first>Frozen v1.17 profile/corpus; v1.17 invocation schema; current v1.16 Go dispatch and wire golden; ownership and retry decisions D-01 through D-12.</read_first>
  <behavior>
    - Raw Go bytes reject duplicate keys and limits before map conversion and produce the frozen canonical bytes/hash.
    - Signed request binds exact pre-state, method, tuple, source/artifact/runtime/toolchain identity, input digest and the complete per-method/cumulative budget vector.
    - Only system failure may retry, and retry reuses identical request bytes, pre-state and budgets; player violation is never retried.
    - v1.16 fixtures continue through their historical decoder unchanged.
  </behavior>
  <action>Write Go consumers for every positive/negative raw corpus vector first. Add explicit v1.17 dispatch and canonical serialization without replacing the v1.16 insertion-ordered verifier. Make mixed result shapes, unsigned budgets, stale identity and changed retry bytes fail closed as system failures.</action>
  <verify><automated>cd apps/go-backend &amp;&amp; PATH=/usr/local/go/bin:$PATH go test ./... -count=1</automated></verify>
  <acceptance_criteria>Go agrees byte-for-byte on canonical positives, rejects all negatives with stable ownership/codes, and proves identical signed system retries.</acceptance_criteria>
  <done>The Go control plane cannot reinterpret JSON, budgets, identity, or retry state.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove transactional no-mutation across completion and persistence</name>
  <files>apps/go-backend/completion.go, apps/go-backend/completion_test.go, packages/persistence/src/complete-match.ts, packages/persistence/src/complete-match.test.ts</files>
  <read_first>Plan 07 engine no-mutation proof; current completion transaction; migration-0017 receipt persistence; all player/system failure cases.</read_first>
  <behavior>
    - System failure writes no result, Chronicle, receipt, standing, memory, score, gameplay state or idempotency success marker.
    - Player violation persists only the engine-produced canonical consequence, never adapter-proposed response/memory.
    - Any persistence error rolls back the successor receipt and every dependent write atomically.
  </behavior>
  <action>Add before/after database snapshots and injected-failure tests covering timeout, crash, transport, malformed envelope, stale artifact, unavailable accounting and persistence faults. Thread the three-way result through completion without catch-all conversion. Preserve prior memory on player violation and byte-equivalent state on system failure.</action>
  <verify><automated>pnpm --filter @cowards/persistence test &amp;&amp; cd apps/go-backend &amp;&amp; PATH=/usr/local/go/bin:$PATH go test ./... -count=1</automated></verify>
  <acceptance_criteria>Every non-success path has explicit ownership and transaction-level before/after equality; partial successor writes are impossible.</acceptance_criteria>
  <done>Go and persistence uphold the same no-mutation boundary as the engine and runtime service.</done>
</task>
</tasks>

## Artifacts this phase produces

- Go v1.17 parity and signed-retry implementation with rollback/no-mutation evidence.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Go map conversion hides duplicate keys | critical | Raw bounded decoder and shared corpus before conversion. |
| Retry changes budget or pre-state | critical | Signature covers canonical request; byte equality asserted. |
| System failure partially persists | critical | Transaction snapshots and injected rollback proofs. |
</threat_model>
<verification>Run Go, persistence, and parity suites; inspect v1.16 wire golden remains byte-identical.</verification>
<success_criteria>Go and persistence preserve canonical bytes, three-way ownership, exact retries, and zero mutation on system failure.</success_criteria>
<output>Create `258-11-SUMMARY.md`.</output>
