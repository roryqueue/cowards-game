---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "02"
type: execute
wave: 2
depends_on: ["258-01"]
files_modified:
  - scripts/generate-canonical-json-v1-1-corpus.ts
  - scripts/generate-canonical-json-v1-1-corpus.test.ts
  - packages/spec/src/fixtures/canonical-json-v1-1-vectors.json
  - packages/spec/src/fixtures/canonical-json-v1-1-raw
  - packages/spec/src/canonical-json-corpus.test.ts
  - apps/go-backend/canonical_json_corpus_test.go
autonomous: true
requirements: [RABI-01, RABI-02, RABI-03, RABI-06, RABI-08]
must_haves:
  truths:
    - "A single hash-addressed raw-byte corpus fixes success/error code/path/offset/ownership and canonical-byte expectations before codecs exist (D-01 through D-04)."
    - "Duplicate keys, malformed UTF-8, deep/nodal/string/collection limits, Unicode scalars, and numeric/order edge cases cannot be erased by host parsing."
  artifacts:
    - path: "packages/spec/src/fixtures/canonical-json-v1-1-vectors.json"
      provides: "Shared indexed manifest and exact TS/Go expectations"
    - path: "packages/spec/src/fixtures/canonical-json-v1-1-raw"
      provides: "Literal invalid and noncanonical byte fixtures"
---
<objective>Create the RED, raw-byte, language-neutral canonical JSON corpus that all Phase-258 codec work must satisfy.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Generate exhaustive raw-byte vectors</name>
  <files>scripts/generate-canonical-json-v1-1-corpus.ts, scripts/generate-canonical-json-v1-1-corpus.test.ts, packages/spec/src/fixtures/canonical-json-v1-1-vectors.json, packages/spec/src/fixtures/canonical-json-v1-1-raw</files>
  <read_first>258-RESEARCH.md Number and ordering vectors; runtime-abi.v1.17 contract; v2.0 audit deep-memory reproduction; existing semantic-integrity fixture generator pattern.</read_first>
  <behavior>
    - Cover valid/invalid UTF-8, overlong encodings, lone surrogates, escaped-equivalent duplicate keys, combining/non-BMP/mixed-case keys, U+2028/U+2029, and no Unicode normalization.
    - Cover -0, safe-integer edges, unsafe integers, subnormal/extreme binary64, exponent/trailing-zero forms, and host NaN/infinity boundaries.
    - Cover raw-byte/depth/node/string/array/object N-1/N/N+1 plus adversarial nesting/allocation.
    - Every vector fixes bytes, limits, context, success or stable error code/path/offset/owner, canonical output, and content hash.
  </behavior>
  <action>Write generator tests first. Store malformed input as literal binary fixtures, not JSON string approximations. Make the index deterministically ordered and domain-hashed from length-delimited raw bytes. Add duplicate-key cases at root and nested paths and in authenticated outer versus decoded Strategy payload ownership contexts. Reject any vector that a host JSON parse already normalized before assertion.</action>
  <verify><automated>pnpm exec vitest run scripts/generate-canonical-json-v1-1-corpus.test.ts &amp;&amp; pnpm exec tsx scripts/generate-canonical-json-v1-1-corpus.ts --write --check</automated></verify>
  <acceptance_criteria>All mandatory categories and every ±1 boundary are present; regeneration is byte-deterministic; each raw fixture hash matches the index.</acceptance_criteria>
  <done>The corpus completely fixes canonical JSON semantics without relying on any host object representation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Install RED TypeScript and Go consumers</name>
  <files>packages/spec/src/canonical-json-corpus.test.ts, apps/go-backend/canonical_json_corpus_test.go</files>
  <read_first>Generated vector manifest/raw files; existing TS fixture loaders; apps/go-backend semantic_integrity_test.go and runtime_service_client_test.go.</read_first>
  <behavior>
    - TS and Go load identical indexed raw bytes and compare exact classifications/offsets/paths/canonical bytes.
    - Tests prove the host parser is not the first untrusted-byte operation and document expected RED failures until Plans 03/04/11.
  </behavior>
  <action>Add corpus harnesses with explicit pending implementation adapters but no broad skip and no alternate expectations. The generator/manifest checks must pass now; codec assertions are named RED tests consumed by later plans. Keep Phase-259 full runtime conformance out of scope.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/canonical-json-corpus.test.ts &amp;&amp; (cd apps/go-backend &amp;&amp; go test ./... -run CanonicalJSONCorpus -count=1)</automated></verify>
  <acceptance_criteria>Both harnesses enumerate the same vector IDs and hashes; no vector is silently skipped or reparsed from escaped text.</acceptance_criteria>
  <done>Later TS and Go implementations have exact RED expectations with no semantic discretion.</done>
</task>
</tasks>

## Artifacts this phase produces

- Shared raw-byte corpus manifest and literal fixtures.
- TypeScript and Go RED corpus consumers.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Host parsing hides duplicates or invalid UTF-8 | critical | Binary fixtures and byte hashes before any conversion. |
| Corpus drifts between TS and Go | high | One manifest, identical IDs/hashes, deterministic generator. |
| Corpus overclaims Phase-259 certification | medium | Scope assertions to codec/ABI contract only. |
</threat_model>
<verification>Generator write/check and both consumers must enumerate identical content hashes.</verification>
<success_criteria>A complete RED corpus fixes every RABI-01/02 canonical-byte and typed-error edge before production codecs are written.</success_criteria>
<output>Create `258-02-SUMMARY.md`.</output>
