---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "13"
type: execute
wave: 11
depends_on: ["258-04", "258-09", "258-11", "258-12"]
files_modified:
  - packages/spec/src/runtime-evidence-attestation.ts
  - packages/spec/src/runtime-evidence-attestation.test.ts
  - packages/spec/src/runtime-execution-service.ts
  - apps/runtime-service/src/semantic-receipt.ts
  - apps/runtime-service/src/runtime-evidence-authority.ts
  - scripts/generate-go-parity-fixtures.ts
  - scripts/generate-go-parity-fixtures.test.ts
  - apps/go-backend/runtime_execution_contract_gen.go
  - apps/go-backend/runtime_semantic_receipt.go
  - apps/go-backend/runtime_service_client_test.go
  - packages/spec/artifacts/runtime-execution-service-response.v1.17.candidate.wire.json
autonomous: true
requirements: [RABI-05, RABI-06, RABI-07, RABI-08]
must_haves:
  truths:
    - "The full source-to-evidence graph is closed, acyclic, cardinality-correct and binds original/normalized source, artifact, manifest, runtime, toolchain, ABI, policy, corpus, budget and evidence domains."
    - "Counted nodes require exact executable/image/toolchain/settings pins and managed-identity attestation/signature; self-asserted or floating identity is uncertified."
    - "The successor receipt uses canonical v1.17 bytes while immutable v1.16 insertion-ordered verification and dispatch remain byte-for-byte available."
    - "All Go generated receipt/contract tables are emitted only by scripts/generate-go-parity-fixtures.ts, which checks both versions and cannot write historical v1.16."
  artifacts:
    - path: "packages/spec/artifacts/runtime-execution-service-response.v1.17.candidate.wire.json"
      provides: "Canonical successor TS/Go wire golden and receipt fixture"
---
<objective>Close the exact identity/evidence graph and mint a versioned canonical successor receipt without rewriting v1.16 history.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-04-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-09-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-11-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-12-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate the exact identity and evidence DAG</name>
  <files>packages/spec/src/runtime-evidence-attestation.ts, packages/spec/src/runtime-evidence-attestation.test.ts, apps/runtime-service/src/runtime-evidence-authority.ts</files>
  <read_first>Domain-framed hashes/manifest; Python original-normalized identity; capability artifact; existing runtime evidence authority and trusted-producer checks.</read_first>
  <behavior>
    - Every required node and edge exists once with the expected domain and digest; cycles, duplicate paths, cross-domain substitution and orphan nodes fail.
    - Executable/image digest, reported version, target/ABI, flags, adapter build, stdlib/sysroot and containment/runtime settings are exact.
    - Attestation/signature is accepted only from managed identity; production trusted producer set remains empty.
  </behavior>
  <action>Write adversarial graph fixtures first for cycle, cardinality, orphan, alias, digest swap, floating pin, self-attestation and legacy-byte fabrication. Extend the validator to enforce the schema-specific DAG and privacy-safe error surface, not reachability alone.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/runtime-evidence-attestation.test.ts apps/runtime-service/src/runtime-evidence-authority.test.ts</automated></verify>
  <acceptance_criteria>Only the exact closed DAG with managed identity validates; incomplete/floating graphs are machine-readably uncertified.</acceptance_criteria>
  <done>Artifact and evidence identity cannot be confused, substituted, cycled or self-certified.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Mint v1.17 receipt and preserve immutable v1.16 dispatch</name>
  <files>packages/spec/src/runtime-execution-service.ts, apps/runtime-service/src/semantic-receipt.ts, scripts/generate-go-parity-fixtures.ts, scripts/generate-go-parity-fixtures.test.ts, apps/go-backend/runtime_execution_contract_gen.go, apps/go-backend/runtime_semantic_receipt.go, apps/go-backend/runtime_service_client_test.go, packages/spec/artifacts/runtime-execution-service-response.v1.17.candidate.wire.json</files>
  <read_first>Phase-257 v1.16 wire golden and HMAC domain; v1.17 canonical manifest serializer; Go parity; D-15 and D-16.</read_first>
  <behavior>
    - v1.17 receipt signs domain-framed canonical bytes and the complete identity/evidence/budget graph.
    - TS and Go produce/verify the exact same successor wire bytes and digest.
    - v1.16 fixture bytes, hash, signature, parser and version dispatch remain unchanged and valid.
    - Unknown or cross-version receipts fail closed as system failure.
  </behavior>
  <action>Add explicit receipt version dispatch and a new v1.17 domain without swapping the v1.16 serializer. Extend only `scripts/generate-go-parity-fixtures.ts` to write the final v1.17 candidate wire/generated Go tables; it must recompute/check the v1.16 request, response, signature input and hashes in memory, reject historical writes, and fail if generated output lacks the sole-writer header. Assert TS/Go byte/hash/signature parity and unknown/cross-version rejection.</action>
  <verify><automated>pnpm exec vitest run scripts/generate-go-parity-fixtures.test.ts &amp;&amp; pnpm exec tsx scripts/generate-go-parity-fixtures.ts --write-v1.17 --check &amp;&amp; pnpm go:parity &amp;&amp; pnpm --filter @cowards/runtime-service test &amp;&amp; (cd apps/go-backend &amp;&amp; PATH=/usr/local/go/bin:$PATH go test ./... -count=1)</automated></verify>
  <acceptance_criteria>One generator check proves both versions, generated Go is source-guarded, successor parity is exact, v1.16 bytes are immutable, and version confusion is rejected.</acceptance_criteria>
  <done>A canonical successor receipt coexists safely with the historical insertion-ordered receipt.</done>
</task>
</tasks>

## Artifacts this phase produces

- Closed exact identity/evidence DAG and versioned v1.17 TS/Go receipt golden.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Reachable graph contains cycle/substitution | critical | DAG, cardinality, domain and edge-schema validation. |
| Local/floating tool identity is counted | critical | Exact pins plus managed-identity attestation. |
| Serializer swap invalidates history | critical | New receipt version/domain and immutable v1.16 dispatch tests. |
</threat_model>
<verification>Run spec, service and Go receipt suites; hash-check immutable v1.16 artifacts and assert trusted production producers remain empty.</verification>
<success_criteria>The complete identity graph and successor receipt are exact, privacy-safe, and backward compatible by explicit version.</success_criteria>
<output>Create `258-13-SUMMARY.md`.</output>
