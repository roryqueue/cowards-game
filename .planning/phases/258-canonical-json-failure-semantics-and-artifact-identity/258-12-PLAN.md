---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "12"
type: execute
wave: 8
depends_on: ["258-08", "258-09", "258-10", "258-11"]
files_modified:
  - packages/spec/src/runtime-abi-v1-17.ts
  - packages/spec/src/runtime-abi-v1-17.test.ts
  - apps/runtime-service/src/runtime-config.ts
  - apps/runtime-service/src/execute-match.ts
  - apps/runtime-service/src/counted-safety.test.ts
  - scripts/preflight.ts
  - packages/spec/artifacts/runtime-abi-v1.17-budget-capabilities.json
autonomous: true
requirements: [RABI-01, RABI-02, RABI-03, RABI-05, RABI-07, RABI-08]
must_haves:
  truths:
    - "Runtime execution and preflight use separate exact ledgers; per-method and cumulative limits are signed and never reset or silently shared."
    - "Every lane reports measured units, measurement boundary, enforcement mechanism and unsupported meters without claiming cross-lane equivalence."
    - "A missing or unbound equivalent meter/toolchain pin makes the lane explicitly uncertified and cannot create production counted authority."
  artifacts:
    - path: "packages/spec/artifacts/runtime-abi-v1.17-budget-capabilities.json"
      provides: "Cross-lane budget, meter, enforcement and certification posture"
---
<objective>Freeze executable runtime/preflight ledgers and truthful cross-lane resource capability posture.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-08-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-09-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-10-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-11-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce exact per-method, cumulative and preflight ledgers</name>
  <files>packages/spec/src/runtime-abi-v1-17.ts, packages/spec/src/runtime-abi-v1-17.test.ts, apps/runtime-service/src/runtime-config.ts, apps/runtime-service/src/execute-match.ts, apps/runtime-service/src/counted-safety.test.ts, scripts/preflight.ts</files>
  <read_first>Plan-01 calibrated budget vector; signed request from Plan 11; language adapter metering evidence; D-05 through D-08.</read_first>
  <behavior>
    - Each method invocation debits a signed method ledger and one signed match cumulative ledger with exact units.
    - Preflight has a separate signed ledger and cannot consume, refill or prove execution budget.
    - Proven Strategy exhaustion is player violation; host/accounting/unavailable meter failure is system failure.
    - Retry reuses the original ledger values and does not refund or double-debit outside the locked rule.
  </behavior>
  <action>Start with table-driven vectors for every budget dimension, exact-boundary and one-over cases, cumulative exhaustion, preflight separation and retry. Implement one spec-owned ledger model consumed by service and preflight; do not translate wall timeout into compute or heap into process memory.</action>
  <verify><automated>pnpm --filter @cowards/spec test &amp;&amp; pnpm --filter @cowards/runtime-service test</automated></verify>
  <acceptance_criteria>All exact budget vectors are deterministic, signed, separately accounted and ownership-correct at every boundary.</acceptance_criteria>
  <done>No lane or retry can silently change, reset, merge, or mislabel a resource budget.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Publish fail-closed cross-lane capability evidence</name>
  <files>packages/spec/artifacts/runtime-abi-v1.17-budget-capabilities.json, apps/runtime-service/src/counted-safety.test.ts, scripts/preflight.ts</files>
  <read_first>TypeScript, Python and WASM meter reports; exact runtime/toolchain identity rules; current trusted-producer configuration.</read_first>
  <behavior>
    - Capability rows name exact unit, scope, measurement point, enforcement mechanism, artifact/toolchain binding and unavailable dimensions.
    - Counted eligibility requires every frozen equivalent dimension and exact pin; unsupported/floating/local-only stays uncertified.
    - Production trusted producers remain empty pending Phase 259 conformance.
  </behavior>
  <action>Generate the capability artifact from contract-owned inputs. Add schema and negative tests for missing dimensions, incomparable aliases, floating pins and producer promotion. Keep local probes diagnostic and public projection privacy-safe.</action>
  <verify><automated>pnpm exec tsx scripts/preflight.ts &amp;&amp; pnpm --filter @cowards/runtime-service test</automated></verify>
  <acceptance_criteria>The artifact reports support honestly, rejects false equivalence, and cannot certify any incomplete lane or producer.</acceptance_criteria>
  <done>Cross-language resource posture is executable and fail closed without premature conformance claims.</done>
</task>
</tasks>

## Artifacts this phase produces

- Exact signed budget ledgers and machine-readable cross-lane capability posture.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Retry resets cumulative budget | critical | Signed original ledger and retry vectors. |
| Incomparable meters are called equivalent | high | Exact unit/scope/mechanism schema; unsupported is explicit. |
| Diagnostic lane becomes counted | critical | Empty trusted producers and fail-closed certification gate. |
</threat_model>
<verification>Run spec, runtime-service and preflight gates; inspect each lane has no inferred or floating capability.</verification>
<success_criteria>Budget enforcement is exact and cross-lane claims are truthful, signed and uncertified when incomplete.</success_criteria>
<output>Create `258-12-SUMMARY.md`.</output>
