---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "10"
type: execute
wave: 6
depends_on: ["258-06", "258-07"]
files_modified:
  - packages/runtime-wasm-wasi/src/validation.ts
  - packages/runtime-wasm-wasi/src/wasm-wasi-subprocess-adapter.ts
  - packages/runtime-wasm-wasi/src/wasm-wasi-subprocess-adapter.test.ts
  - packages/runtime-wasm-wasi/src/metadata.ts
  - scripts/evaluate-wasm-wasi-runtime.ts
  - packages/spec/artifacts/runtime-abi-v1.17-wasm-language-envelopes.json
autonomous: true
requirements: [RABI-01, RABI-02, RABI-03, RABI-05, RABI-06, RABI-07, RABI-08]
must_haves:
  truths:
    - "Rust and Zig guests share one WASM/WASI payload ABI while the host adapter owns/authenticates the outer envelope."
    - "Trap attribution, stale artifact, exact Wasmtime settings, artifact/toolchain identity and unavailable meters fail with the common ownership/uncertified rules."
  artifacts:
    - path: "packages/spec/artifacts/runtime-abi-v1.17-wasm-language-envelopes.json"
      provides: "Rust/Zig/WASM common envelope fixtures"
---
<objective>Migrate Rust/Zig through the WASM/WASI host to the shared raw payload ABI and exact identity/failure semantics.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-07-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move WASM outer envelope authority to the adapter</name>
  <files>packages/runtime-wasm-wasi/src/validation.ts, packages/runtime-wasm-wasi/src/wasm-wasi-subprocess-adapter.ts, packages/runtime-wasm-wasi/src/wasm-wasi-subprocess-adapter.test.ts</files>
  <read_first>Current guest-written envelope flow; v1.17 ABI/ownership corpus; Plan 07 no-mutation contract; Wasmtime fuel/memory/stack handling.</read_first>
  <behavior>
    - Guest writes only Strategy-owned payload bytes; host scans, classifies, binds and authenticates the outer response.
    - Proven guest trap/fuel/memory/output exhaustion is player-owned; ambiguous trap, Wasmtime/host/transport crash or unavailable accounting is system-owned.
    - Stale artifact/runtime/toolchain/settings binding is system failure and never reaches gameplay.
  </behavior>
  <action>Write hostile raw payload/trap/stale identity tests first. Replace guest envelope trust with host-owned construction and canonical scanning. Record exact fuel, linear-memory, stack and process/capability evidence without equating incomparable meters. Remove mixed system/player result shapes. No new runtime/package.</action>
  <verify><automated>pnpm --filter @cowards/runtime-wasm-wasi test</automated></verify>
  <acceptance_criteria>All malformed guest output is classified at the correct stage; adapter owns every outer binding/signature; ambiguous failures are system-owned.</acceptance_criteria>
  <done>WASM guests cannot authenticate or classify their own transport envelope.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bind Rust and Zig envelopes to exact artifact toolchain settings</name>
  <files>packages/runtime-wasm-wasi/src/metadata.ts, scripts/evaluate-wasm-wasi-runtime.ts, packages/spec/artifacts/runtime-abi-v1.17-wasm-language-envelopes.json, packages/runtime-wasm-wasi/src/wasm-wasi-subprocess-adapter.test.ts</files>
  <read_first>Existing Rust/Zig readiness artifacts; runtime/toolchain identity registry; observed versions versus counted pin rules.</read_first>
  <behavior>
    - Rust and Zig fixtures expose identical request/payload/result/budget fields and stable failure codes.
    - Identity binds executable/image digest, reported version, target/ABI, flags, adapter build, stdlib/sysroot, Wasmtime settings and containment profile.
    - Floating/latest/stable/local-only identity is discovery-only and explicitly uncertified.
  </behavior>
  <action>Generate candidate common-language envelope fixtures from the contract and evaluate real available Rust/Zig artifacts as contract probes, not Phase-259 conformance certification. Require digest-addressed pins for counted evidence; retain production trusted producers empty. Keep unsupported enforcement visible.</action>
  <verify><automated>pnpm exec vitest run packages/runtime-wasm-wasi/src/wasm-wasi-subprocess-adapter.test.ts &amp;&amp; pnpm exec tsx scripts/evaluate-wasm-wasi-runtime.ts</automated></verify>
  <acceptance_criteria>Rust/Zig envelope schemas are identical, exact settings are bound, and any floating or incomplete lane is machine-readably uncertified.</acceptance_criteria>
  <done>Rust and Zig share the documented ABI without false counted-equivalence claims.</done>
</task>
</tasks>

## Artifacts this phase produces

- Host-owned WASM/WASI v1.17 adapter and common Rust/Zig language envelope fixtures.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Guest forges success/transport identity | critical | Host-owned canonical authenticated envelope. |
| Ambiguous trap penalizes player | critical | Proven-attribution rule; ambiguity is system failure. |
| Floating tool label promotes counted use | high | Exact digest/settings schema and uncertified fallback. |
</threat_model>
<verification>Run full WASM suite and candidate evaluator; inspect no trusted production producer is added.</verification>
<success_criteria>Rust/Zig/WASM expose one package-free ABI with truthful identity and failure semantics.</success_criteria>
<output>Create `258-10-SUMMARY.md`.</output>
