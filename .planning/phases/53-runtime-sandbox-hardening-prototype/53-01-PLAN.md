---
phase: 53-runtime-sandbox-hardening-prototype
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - SBX-01
  - SBX-02
  - SBX-03
  - SBX-04
  - SBX-05
  - SBX-06
---

<objective>
Add a repeatable runtime sandbox evaluation harness that compares practical hostile Strategy boundary candidates without changing counted Match defaults or promoting any candidate.
</objective>

<tasks>

<task>
  <name>Task 1: Add sandbox candidate and probe model</name>
  <files>packages/runtime-js/src/sandbox-evaluation.ts, packages/runtime-js/src/sandbox-evaluation.test.ts</files>
  <behavior>Define evaluation-only candidate records, explicit spec/executable adapter id mapping, hostile probe fixtures, public-safe result projection, no-promotion decision text, and tradeoff-only rows for unavailable candidates.</behavior>
  <verify>pnpm --filter @cowards/runtime-js test</verify>
</task>

<task>
  <name>Task 2: Add repeatable evaluation command and artifact</name>
  <files>scripts/evaluate-runtime-sandbox.ts, package.json, .planning/artifacts/runtime-sandbox-evaluation.json</files>
  <behavior>Run executable candidates through the existing StrategyExecutionAdapter ABI, write deterministic public-safe JSON evidence, skip optional Docker candidate when unavailable, and fail stale artifact checks.</behavior>
  <verify>pnpm sandbox:evaluate && pnpm sandbox:evaluate:check</verify>
</task>

<task>
  <name>Task 3: Add drift guards and documentation</name>
  <files>packages/runtime-js/src/sandbox-evaluation.test.ts, .planning/phases/53-runtime-sandbox-hardening-prototype/53-VALIDATION.md</files>
  <behavior>Prove the harness is not imported by counted worker/web/persistence paths, output is public-safe, defaults remain worker-thread, and no candidate is promoted in v1.8.</behavior>
  <verify>pnpm --filter @cowards/runtime-js test && pnpm typecheck</verify>
</task>

</tasks>

<source_audit>
SBX-01 through SBX-06 are covered by Tasks 1 through 3.
</source_audit>
