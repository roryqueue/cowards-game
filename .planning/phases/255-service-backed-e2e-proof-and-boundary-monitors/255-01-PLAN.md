---
phase: 255-service-backed-e2e-proof-and-boundary-monitors
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [PROOF-01, PROOF-02]
files_modified:
  - playwright.config.ts
  - apps/web/e2e/v1-36-competition-service-proof.spec.ts
  - scripts/evaluate-v1-36-service-proof.ts
  - scripts/evaluate-v1-36-service-proof.test.ts
  - package.json
  - .planning/artifacts/v1.36-competition-service-proof.json
  - .planning/artifacts/v1.36-competition-service-proof.md
must_haves:
  truths:
    - A live signed-in flow crosses the current account-save, counted-entry, Season scheduling, Go orchestration, runtime-service, public result, standings, and replay boundaries and ends in canonical counted evidence.
    - Service-backed entry rejection covers stale, missing, mismatched, unsupported provider/language, hidden TinyGo, invalid provenance, unavailable runtime lane, package-policy violation, same-user duplicate entry, and mid-Season replacement without creating standings evidence.
    - Proof artifacts contain only public ids, coarse categories, timings, link paths, hashes, and outcomes; they never contain source, artifact bytes, memories, objectives, diagnostics, paths, environment, tokens, database details, or private runtime/provider payloads.
    - Ordinary local runs can record a fresh environment limitation, but strict mode rejects unavailable, skipped, stale, or non-passed service evidence.
    - The proof uses direct SQL only for isolated administration/lifecycle setup that has no public mutation route; acceptance, rejection, scheduling, execution, and public reads use production boundaries.
  artifacts:
    - path: apps/web/e2e/v1-36-competition-service-proof.spec.ts
      provides: Live positive counted competition path and authenticated negative eligibility matrix
    - path: scripts/evaluate-v1-36-service-proof.ts
      provides: Deterministic artifact writer/checker with strict versus unavailable semantics
    - path: .planning/artifacts/v1.36-competition-service-proof.json
      provides: Machine-readable public-safe service proof status and scenario outcomes
    - path: .planning/artifacts/v1.36-competition-service-proof.md
      provides: Human-readable service proof, command, timing, and limitation summary
  key_links:
    - from: apps/web/e2e/v1-36-competition-service-proof.spec.ts
      to: apps/web/app/api/ladder/seasons/[seasonId]/entries/route.ts
      via: Signed-in entry requests and canonical public category responses
    - from: apps/web/e2e/v1-36-competition-service-proof.spec.ts
      to: apps/web/app/api/ladder/seasons/[seasonId]/schedule/route.ts
      via: Admin scheduling followed by normal Go/worker/runtime execution
    - from: apps/web/e2e/v1-36-competition-service-proof.spec.ts
      to: apps/go-backend/live_backend.go
      via: Selected public result, ladder, standings, and replay evidence reads
    - from: scripts/evaluate-v1-36-service-proof.ts
      to: .planning/artifacts/v1.36-competition-service-proof.json
      via: Leak-safe canonical rendering and strict freshness validation
---

# Plan 255-01: Counted Competition Service Matrix

<objective>Prove the full counted Season path and every required entry rejection through the live application topology, with explicit strict/unavailable environment semantics.</objective>

<tasks>
<task id="255-01-01" type="auto" tdd="true">
  <name>Define strict public-safe service proof artifacts</name>
  <files>scripts/evaluate-v1-36-service-proof.ts, scripts/evaluate-v1-36-service-proof.test.ts, package.json</files>
  <action>Add a deterministic evaluator for `v1.36-competition-service-proof.{json,md}`. Model `passed-local-services` and `not-run-environment-unavailable` distinctly; include required positive/negative scenario ids, public result/link/timing facts, artifact hashes, commands, and bounded limitations. Reuse public-output leak guards and reject secret-like values, absolute paths, DSNs, headers, source, artifacts, memories, objectives, diagnostics, report/operator material, and private runtime/provider payloads. Add `v1.36:service-proof`, `v1.36:service-proof:check`, `v1.36:service-proof:strict`, and `e2e:v1.36-service-proof` scripts. `COWARDS_V1_36_REQUIRE_SERVICE_PROOF=1` must reject unavailable, skipped, missing, stale, incomplete, or non-passed evidence. A non-strict unavailable write must require a short `COWARDS_V1_36_SERVICE_PROOF_LIMITATION` and remain visibly non-passed.</action>
  <verify><automated>pnpm exec vitest run scripts/evaluate-v1-36-service-proof.test.ts</automated></verify>
  <done>The evaluator writes/checks synchronized leak-safe artifacts, distinguishes unavailable from passed, and fails strict mode unless live service proof passed every required scenario.</done>
</task>
<task id="255-01-02" type="auto" tdd="true">
  <name>Prove the live counted Season path</name>
  <files>playwright.config.ts, apps/web/e2e/v1-36-competition-service-proof.spec.ts, .planning/artifacts/v1.36-competition-service-proof.json, .planning/artifacts/v1.36-competition-service-proof.md</files>
  <action>Build a prefixed, cleanup-safe signed-in proof using the v1.28/v1.32 cookie, DB setup, polling, privacy, and artifact patterns. Forward `COWARDS_GO_BACKEND_URL`, `COWARDS_GO_ACCOUNT_REVISIONS`, and `COWARDS_GO_PUBLIC_READS` through Playwright's web-server environment so selected Go ownership is explicit. When `RUN_V1_36_SERVICE_PROOF` is absent, skip with the documented topology requirement. When it equals `1`, validate `DATABASE_URL`, `REDIS_URL`, `COWARDS_GO_BACKEND_URL`, `COWARDS_GO_BACKEND_INTERNAL_TOKEN`, and `COWARDS_RUNTIME_SERVICE_URL`, then verify provider/private-artifact readiness and service health before the test; missing configuration or unhealthy services are failures, not skips. Create the smallest valid set of distinct proof accounts and current immutable revisions through the selected account save route, create a Season through `POST /api/ladder/seasons`, use DB setup only to grant the proof admin/open lifecycle where no public route exists, enter through the signed-in entry route, schedule through the signed-in admin route, and let normal Go orchestration/worker/runtime-service execution complete. Poll selected public result/ladder/replay reads and assert canonical `counted`, complete Match/Chronicle coverage, standings contribution, stable links, idempotent scheduling, byte-stable repeated standings, and public result/replay page reachability. Never invoke engine or Strategy runtime code in the proof process.</action>
  <verify><automated>RUN_V1_36_SERVICE_PROOF=1 PLAYWRIGHT_TEST=1 COWARDS_GO_ACCOUNT_REVISIONS=1 COWARDS_GO_PUBLIC_READS=1 DATABASE_URL="$DATABASE_URL" REDIS_URL="$REDIS_URL" COWARDS_GO_BACKEND_URL="$COWARDS_GO_BACKEND_URL" COWARDS_GO_BACKEND_INTERNAL_TOKEN="$COWARDS_GO_BACKEND_INTERNAL_TOKEN" COWARDS_RUNTIME_SERVICE_URL="$COWARDS_RUNTIME_SERVICE_URL" pnpm exec playwright test --project=desktop --workers=1 v1-36-competition-service-proof.spec.ts --grep 'counted Season path'</automated></verify>
  <done>A real counted entry becomes a scheduled, executed, replay-backed counted MatchSet whose score appears in deterministic public Season standings.</done>
</task>
<task id="255-01-03" type="auto" tdd="true">
  <name>Exercise the authenticated eligibility rejection matrix</name>
  <files>apps/web/e2e/v1-36-competition-service-proof.spec.ts, .planning/artifacts/v1.36-competition-service-proof.json, .planning/artifacts/v1.36-competition-service-proof.md</files>
  <action>Using isolated prefixed rows and the same live topology, construct stale proof, missing proof, source/artifact mismatch, unsupported provider/language, hidden TinyGo, invalid provenance/compatibility, unavailable runtime lane, package mode/declaration violation, same-user second revision, and mid-Season replacement scenarios. Use direct DB mutation only to construct impossible/invalid stored evidence states, then submit every decision through `POST /api/ladder/seasons/:seasonId/entries`. Assert the stable public category/status/remediation, absence of private evidence or constraint names, no active entry for rejected attempts, original snapshot preservation for same-user/replacement cases, and no standings/schedule side effects. Write only coarse scenario results to the proof artifacts, run their leak scan, and require the strict evaluator to pass after the full live spec.</action>
  <verify><automated>RUN_V1_36_SERVICE_PROOF=1 PLAYWRIGHT_TEST=1 COWARDS_GO_ACCOUNT_REVISIONS=1 COWARDS_GO_PUBLIC_READS=1 DATABASE_URL="$DATABASE_URL" REDIS_URL="$REDIS_URL" COWARDS_GO_BACKEND_URL="$COWARDS_GO_BACKEND_URL" COWARDS_GO_BACKEND_INTERNAL_TOKEN="$COWARDS_GO_BACKEND_INTERNAL_TOKEN" COWARDS_RUNTIME_SERVICE_URL="$COWARDS_RUNTIME_SERVICE_URL" pnpm exec playwright test --project=desktop --workers=1 v1-36-competition-service-proof.spec.ts && COWARDS_V1_36_REQUIRE_SERVICE_PROOF=1 pnpm v1.36:service-proof:check</automated></verify>
  <done>All required negative cases fail through the production entry boundary with correct public categories and no rejected evidence reaches counted competition.</done>
</task>
</tasks>

<verification>
- `pnpm exec vitest run scripts/evaluate-v1-36-service-proof.test.ts`
- `pnpm --filter @cowards/spec typecheck && pnpm --filter @cowards/persistence typecheck && pnpm --filter @cowards/service typecheck && pnpm --filter @cowards/web typecheck`
- Without live topology: `COWARDS_V1_36_SERVICE_PROOF_LIMITATION="local database and services not configured" pnpm v1.36:service-proof -- --record-unavailable && pnpm v1.36:service-proof:check`; record that strict proof remains outstanding.
- With live topology: `RUN_V1_36_SERVICE_PROOF=1 PLAYWRIGHT_TEST=1 COWARDS_GO_ACCOUNT_REVISIONS=1 COWARDS_GO_PUBLIC_READS=1 DATABASE_URL="$DATABASE_URL" REDIS_URL="$REDIS_URL" COWARDS_GO_BACKEND_URL="$COWARDS_GO_BACKEND_URL" COWARDS_GO_BACKEND_INTERNAL_TOKEN="$COWARDS_GO_BACKEND_INTERNAL_TOKEN" COWARDS_RUNTIME_SERVICE_URL="$COWARDS_RUNTIME_SERVICE_URL" pnpm exec playwright test --project=desktop --workers=1 v1-36-competition-service-proof.spec.ts`
- Strict closeout: `COWARDS_V1_36_REQUIRE_SERVICE_PROOF=1 pnpm v1.36:service-proof:check`
</verification>

<boundaries>
- Do not execute Strategy code from Playwright, web/API, Go public handlers, or evaluator scripts; execution must remain worker -> runtime-service -> Runtime Broker -> provider.
- Do not change game rules, scoring values, tie-break order, Season policy, runtime/package/TinyGo claims, or sandbox posture to make proof pass.
- Do not serialize source, artifacts, memories, objectives, diagnostics, paths, env values, tokens, DSNs, private runtime/provider details, reporter/operator data, or recovery payloads.
- Do not treat an unavailable/recorded limitation as passed proof. Leave Phase 255 and milestone closure pending until strict mode passes.
</boundaries>
