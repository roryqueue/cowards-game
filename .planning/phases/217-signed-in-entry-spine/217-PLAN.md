# Phase 217 Plan: Signed-In Entry Spine

## Tasks

1. Build `/competitions/[competitionId]/enter`.
2. Use `getSignedInCompetitionEntryDashboard` for account-safe revision lists.
3. Reuse the exhibition creation client where the competition is an exhibition preset.

## Verification

- Unit test for signed-in source-free dashboard.
- Playwright anonymous sign-in gate.
