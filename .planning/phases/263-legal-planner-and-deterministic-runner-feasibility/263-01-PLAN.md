---
phase: 263-legal-planner-and-deterministic-runner-feasibility
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["packages/strategy-lab/package.json","packages/strategy-lab/tsconfig.json","packages/strategy-lab/src/index.ts","packages/strategy-lab/src/contracts.ts","packages/strategy-lab/src/contracts.test.ts","packages/strategy-lab/src/feasibility-protocol.ts","packages/strategy-lab/src/feasibility-protocol.test.ts","scripts/check-v1-38-lab-boundaries.ts","scripts/check-v1-38-lab-boundaries.test.ts"]
autonomous: true
requirements: ["PLAN-06","FACT-01","FACT-04"]
must_haves:
  truths:
    - "The operator can validate a bounded Phase263 manifest against unchanged admitted roots without launching a runtime."
    - "The allocation records24 attempts but only8 unique scientific cells; alias checks never inflate payoffs."
    - "Production cannot transitively reach the lab, and the lab core cannot import apps or database clients."
  artifacts:
    - path: "packages/strategy-lab/src/contracts.ts"
      provides: "Strict private envelope and manifest admission"
      exports: ["LabManifestSchema","LabAttemptSchema","LabSemanticRecordSchema","LabOperationalRecordSchema","admitLabManifest"]
    - path: "packages/strategy-lab/src/feasibility-protocol.ts"
      provides: "Fixed source-independent measurement/allocation protocol"
      exports: ["PLANNER_FEASIBILITY_PROTOCOL","buildFeasibilityCorpus","validateFeasibilityAllocation"]
    - path: "scripts/check-v1-38-lab-boundaries.ts"
      provides: "Negative production reachability monitor"
      exports: ["checkLabBoundaries"]
  key_links:
    - from: "packages/strategy-lab/src/contracts.ts"
      to: ".planning/artifacts/v1.38-pre-search-measurement-policy.json"
      via: "admission binds policy root and exact non-replaced gates"
    - from: "scripts/check-v1-38-lab-boundaries.ts"
      to: "packages/strategy-lab/package.json"
      via: "AST and deployment graph deny reverse imports"
---

<objective>
Create the private contract spine and freeze the source-independent feasibility protocol per D-01, D-04, D-05, D-14, D-16 and D-20. Resolve the benchmark and allocation before any measured candidate output.
Purpose: Prove legal private feasibility before factory scale, without changing canonical rules or historic evidence.
Output: The named implementation and tests below; no empirical pass is presumed.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-CONTEXT.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-RESEARCH.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-PATTERNS.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-VALIDATION.md
@.planning/artifacts/v1.38-pre-search-study-policy.json
@.planning/artifacts/v1.38-pre-search-measurement-policy.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-203-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md
</context>

## Artifacts this phase produces

- `packages/strategy-lab/src/contracts.ts`: `LabManifestSchema`, `LabAttemptSchema`, `LabSemanticRecordSchema`, `LabOperationalRecordSchema`, `admitLabManifest` — Strict private envelope and manifest admission.
- `packages/strategy-lab/src/feasibility-protocol.ts`: `PLANNER_FEASIBILITY_PROTOCOL`, `buildFeasibilityCorpus`, `validateFeasibilityAllocation` — Fixed source-independent measurement/allocation protocol.
- `scripts/check-v1-38-lab-boundaries.ts`: `checkLabBoundaries` — Negative production reachability monitor.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create private bounded contracts</name>
  <files>packages/strategy-lab/package.json, packages/strategy-lab/tsconfig.json, packages/strategy-lab/src/index.ts, packages/strategy-lab/src/contracts.ts, packages/strategy-lab/src/contracts.test.ts</files>
  <read_first>packages/engine/package.json; tsconfig.base.json; packages/spec/src/canonical-json-encode.ts; packages/spec/src/canonical-json-parse.ts; scripts/lib/v1-38-study-contract.ts; scripts/lib/v1-38-measurement.ts</read_first>
  <behavior>Reject unknown keys, oversized/raw noncanonical data, wrong tuple/root/source, invalid enums and absolute/traversal artifact IDs; valid bounded envelopes round-trip canonically.</behavior>
  <action>Create private ESM @cowards/strategy-lab with existing workspace packages and inherited strict TypeScript settings. No external install or lockfile upgrade. Export only current contract symbols in index.ts; later code imports module paths without racing edits to the barrel. Define discriminated success/player_violation/system_failure/unused attempt records, semantic records, separately rooted operational records and a manifest including admitted study/measurement/current tuple/runtime/image/profile/source/corpus/budget roots, deterministic versions, pre-enumerated allocation and trace policy. Validate raw byte caps before parsing, exact keys and safe generated artifact identifiers. Per D-01/D-05/D-16/D-20, bind current Phase262 verified roots but never use its consumed markers as authority or fresh capacity. Retain existing custody references without accessing secrets. Artifact root is private/offline, not a production database or canonical evidence path.</action>
  <acceptance_criteria>Contract tests pass; private package typecheck works; malformed root/identity/bounds fail closed.</acceptance_criteria>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 packages/strategy-lab/src/contracts.test.ts</automated></verify>
  <done>Contract tests pass; private package typecheck works; malformed root/identity/bounds fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind fixed benchmark and charged allocation</name>
  <files>packages/strategy-lab/src/feasibility-protocol.ts, packages/strategy-lab/src/feasibility-protocol.test.ts</files>
  <read_first>263-RESEARCH.md Bounded Planning Guidance; studyPolicy.arenas and completeCells in pre-search-study-policy.json; measurementPolicy.gates in pre-search-measurement-policy.json; packages/engine/src/runtime-inputs.ts; packages/spec/src/runtime-budget-profile-v1-18.ts</read_first>
  <behavior>Protocol enumerates12 label tasks/8 unique geometry cells, two passes24 charges; cap overflow, alias inflation, retry, unknown benchmark and threshold drift are rejected; corpus ordering is stable.</behavior>
  <action>Implement immutable PLANNER_FEASIBILITY_PROTOCOL before source output or timing is inspected. Retain benchmark identity v1.38-direct-execution-benchmark-v1, hardware-class label profile-neutral-fixed-hardware-class-v1, and inherited implementation root as the measurement-policy generator binding; bind new corpus/harness digests as subordinate protocol identities, not replacements. The inspected policy contains no warmup/sample/timing implementation or Phase263 aggregate Match ceiling; fill those details here. Per D-14 choose100 fixed warmups then1000 measured calls for each of selectActivations and soldierBrain,2200 benchmark invocations total, fixed inter-method order, fresh method input/memory reset for each corpus case, nearest-rank sample990 of1000 and strictly below5ms for EACH method. Method timing must come from the trusted observer inside existing supervision, never caller/transport time. Build100 canonical legal-input cases per method with fixed traversal order: ten missions × positive/stale/failure/fallback/tactic/defense/boundary/Advance/memory/hostile-schema case families; invalid-input gate cases are separate from legal timing corpus. Freeze exact case hashes before measurement. Use beam4,256 assignment expansions,64 optional brain evaluations and separately charged nine-Action fallback reservation per call. Source <=65536 bytes, preferred <49152, StrategyMemory32768/SoldierMemory2048/objective1024 and exact selected output/per-method limits remain unchanged. One hierarchical candidate versus the exact Advanced fixture revision from admitted Phase262 source, current canonical start only. Three labels × two sides × two initiative states produce12 label execution tasks but only8 scientific geometry cells; retain aliases as charged compatibility checks excluded from unique scientific/payoff denominators. Two runs:1worker/shard1/forward and2workers/shard3/reversed. No retries. Allocate24 Match attempts,120000ms each and3600000ms overall including emitted build, benchmark, startup and cleanup; the command's walltime is operational only. Source development is before this timebox and not empirical evidence. Add256 fixed supervised validation invocations (separate from2200 timing calls), with exact case inventory frozen by Plan06, no additional ad hoc live probes. Full private trace retention for all24 attempts satisfies any inherited sample subset without changing scientific thresholds; set reviewed sample to first8 geometry-distinct baseline cells and all failures. Record unused allocations and stop on exhaustion. Bind exact machine identity before execution, no friendlier hardware substitution after results.</action>
  <acceptance_criteria>Fixed protocol tests prove exact counts, identities, timing calculation and unchanged gates; no runtime or Match launched.</acceptance_criteria>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 packages/strategy-lab/src/feasibility-protocol.test.ts</automated></verify>
  <done>Fixed protocol tests prove exact counts, identities, timing calculation and unchanged gates; no runtime or Match launched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce one-way lab boundaries</name>
  <files>scripts/check-v1-38-lab-boundaries.ts, scripts/check-v1-38-lab-boundaries.test.ts</files>
  <read_first>scripts/check-v1-37-integrity-boundaries.ts AST import analysis; 263-PATTERNS.md boundary guidance</read_first>
  <behavior>Negative fixtures catch imports, reexports, require/dynamic imports, aliases/transitive paths, app imports from core, deployment/image/Go/generated exposures and unsafe receipts; intended lab-to-canonical imports pass.</behavior>
  <action>Implement checkLabBoundaries per D-04/D-16 with repository root from import.meta.url. Scan production package/app/Go/generated-contract/image/deployment entry points for direct and transitive lab references and source/artifact exposure. Core may import only approved canonical packages and Node trusted-offline capabilities; scripts host orchestration but core never imports apps. Use TypeScript AST/module resolution for imports, reexports and supported dynamic forms; fail closed for unresolved potentially lab-directed edges rather than string-only tests. Add fixture-based negative tests so forbidden constructs need not exist in production. Keep private receipt allowlist and no canonical Chronicle/public replay route. Do not add lab to production builds.</action>
  <acceptance_criteria>Negative dependency and privacy fixtures pass with no product runtime/schema changes.</acceptance_criteria>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 scripts/check-v1-38-lab-boundaries.test.ts</automated></verify>
  <done>Negative dependency and privacy fixtures pass with no product runtime/schema changes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

Private manifest/files → trusted coordinator; coordinator → hostile Strategy supervisor; lab → production exclusion.
ASVS L1 scope, not certification. Every unresolved high/critical finding blocks empirical execution and downstream handoff.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-263-01-A | Tampering | Manifest/protocol | high | mitigate | contracts.test and feasibility-protocol.test reject changed roots, bounds, alias inflation and hidden allocations. |
| T-263-01-B | Information disclosure | Production graph | high | mitigate | AST/transitive/deployment negative fixtures and safe receipt allowlist. |
| T-263-01-SC | Tampering | Dependency setup | high | mitigate | Use existing locked dependencies only; no install or upgrade is planned. New external dependencies require research legitimacy audit before installation. |
</threat_model>

<verification>
Run each task's focused command after its tests are created, then the implemented phase synthetic suites and package typecheck. Test first, implement, rerun. Keep normal corrections in this plan; do not invent numbered review/custody successors. Never rerun consumed Phase262 selectors.
</verification>

<success_criteria>
The operator can validate a bounded Phase263 manifest against unchanged admitted roots without launching a runtime.
The allocation records24 attempts but only8 unique scientific cells; alias checks never inflate payoffs.
Production cannot transitively reach the lab, and the lab core cannot import apps or database clients.
No source, objectives, memory, teacher state, private trace, runtime internals or host diagnostics enter public/default outputs. No formation namespace or holdout access; no factory-scale execution.
</success_criteria>

## Contract reconciliation
The frozen study policy binds geometry identity and excludes duplicate labels from scientific cells. The24-attempt implementation therefore preserves label compatibility coverage without asserting12 independent cells or3 geometries. Frozen benchmark labels bind policy generation, not an existing executable sampling implementation; subordinate harness/corpus binding fills the documented gap without replacing the5ms threshold. No new candidate-factory or formation allocation is created.

The canonical driver and admitted fixture select maxPhases100; preserve it. With4 Rounds,2 Strategy calls/Round,2 entrants × (1+2+3+4) activations ×12 Cycles, the conservative bound is24800 method calls/Match,595200 across24 attempts,597656 total including2456 validation/benchmark calls. This is a safety/accounting upper bound, not a changed gameplay budget; exact selected cumulative runtime limits still dominate. Legal timing corpus cases use valid schema-boundary inputs; invalid hostile-schema cases belong only to the separate256-call validation inventory.


<output>
Create `.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-01-SUMMARY.md` with actual commands/results and honest limitations. Commit only owned changes and preserve other agents' edits.
</output>
