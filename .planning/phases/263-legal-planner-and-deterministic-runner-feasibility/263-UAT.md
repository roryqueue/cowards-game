---
status: passed
phase: 263-legal-planner-and-deterministic-runner-feasibility
source: [263-01-SUMMARY.md,263-02-SUMMARY.md,263-03-SUMMARY.md,263-04-SUMMARY.md,263-05-SUMMARY.md,263-06-SUMMARY.md]
started: 2026-09-10
updated: 2026-09-13
mode: autonomous-private-operator-observation
---

## Current Calibrated Test — terminal pass

The approved single fresh calibrated attempt passed. Retained evidence independently confirms 256 validation cases / 232 guest calls / 24 rejects, 2,200 benchmark calls, selection p99 `17.019799 ms < 20 ms`, SoldierBrain p99 `1.58373 ms < 5 ms`, 24 charged Match attempts / 0 unused, zero uncertain units, cleanup complete, 24 successful attempts, 16 shards, 24 traces, 12 identical paired semantic-root pairs, and 8 scientific cells / 3 labels / 2 geometries. The parent read-only `--verify` returned `status: passed` with `productionAuthorized:false`. No new run or human UI confirmation was performed; private evidence is machine-verifiable.

Summary of the current UAT: total 6, passed 6, issues 0, pending 0, blocked 0. The historical original/retry-1/2/3 non-pass observations below remain immutable history and are not current dispatch instructions.

### Current retained Match observations (Tests 5–6)

- **Test 5 — reproduction/resume:** Direct metadata audit found 24 successful traces, 16 shard records, and 12 paired semantic-root comparisons with 12 identical / 0 differing pairs. Reduction reports 8 scientific cells, 3 labels, and 2 geometries; all 24 attempt records report complete cleanup and no failure classification.
- **Test 6 — baseline realism:** I parsed the first `beforeState` of each retained private trace (without exposing payloads). All 24/24 starts exactly match the canonical eight bottom positions `(x=2..9,y=11)` and eight top positions `(x=2..9,y=0)`. All 24/24 use initial bounds `0..11` on both axes; active starting Soldiers have 0 out-of-bounds positions. The 16 `arena:smoke:v1` traces have 0 terrain stones and the 8 `arena:standard-cross:v1` traces have the expected four cross stones, with 0/24 terrain mismatches. There were 0 failed Match traces, so the “inspect every failure” subset is vacuous; this is an observed zero-failure result, not an inferred review.

## Historical Current Test (preserved)

The original run and all three subsequently approved attempts are terminal non-pass. The approved envelope is exhausted. No user response is fabricated: these are direct automated/private-operator observations under the user's autonomous testing instruction. Real Match/replay acceptance remains blocked by the timing gate. Latest evidence and immutable roots are in263-RETRY-ENVELOPE.md and263-feasibility-retry-3-manifest.json.

## Tests

### 1. Prepare exact private run without launching source
expected: One private immutable manifest and case inventory; no runtime invocation from preparation.
result: pass
evidence: Actual --prepare returned prepared. Manifest binds source, implementation, review, machine and fixed budgets. Synthetic tests prove no-clobber preparation and deny-by-default live host construction.

### 2. Validate legal information and hostile boundaries
expected: Same legal input gives equal decisions/memory across hidden-state and fresh/reused contexts; visible/tactical controls and hostile rejection behave as specified.
result: pass
evidence: Actual256-case validation passed with232 supervised calls,16 source rejections,8 input rejections and complete cleanup.

### 3. Measure planner speed before allowing Matches
expected: Both methods have direct p99 strictly below5ms with100warmups and1000measured calls each.
result: issue
severity: blocker
observed: Latest selection14.171262ms failed; SoldierBrain1.732708ms passed. All2200calls completed; no sample was replaced. Prior selection p99 values64.135270/33.819629/20.915845ms remain historical non-passes, not overwritten.

### 4. Fail closed and retain a verifiable result
expected: A failed hard gate stops Matches, retains charged/unused accounting and permits read-only reconstruction.
result: pass
evidence: Latest receipt and dedicated records agree on complete cleanup, zero Matches charged,24unused and no uncertain calls. Actual --verify returned non_pass without execution. No owned container remained. The original run's conservative cleanup-summary defect was corrected only in later source; its original bytes remain preserved. All3approved attempts have truthful complete-cleanup receipts.

### 5. Reproduce complete Matches across workers, shards and resume
expected: All24attempts complete with identical scientific reductions and zero redispatch on completed resume.
result: blocked
blocked_by: prior-phase
reason: The Phase263 timing prerequisite failed. Synthetic worker-layout checks pass but cannot substitute for actual-source Match evidence.

### 6. Inspect realistic full-board baseline Match traces
expected: Inspect8geometry-distinct baseline starts and every failed Match without clipped/out-of-bounds Soldiers or terrain.
result: blocked
blocked_by: prior-phase
reason: Zero Matches ran and no Match traces exist. Do not invent browser/private replay acceptance.

## Summary

total: 6
passed: 3
issues: 1
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: Both planner methods satisfy strict direct p99 below5ms.
  status: failed
  reason: Latest actual selection p99 is14.171262ms across the frozen1000measured calls; all3approved attempts are consumed.
  severity: blocker
  test: 3
  root_cause: Reviewed construction/scoring/representation optimizations reduced observed latency but did not meet the strict5ms threshold; exact residual time shares are not profiled.
  artifacts: [263-RETRY-ENVELOPE.md,263-feasibility-retry-3-manifest.json,263-VERIFICATION.md]
  missing: [Operator decision on next bounded admission contract,Passing real timing gate,Conditional24-Match reproduction and trace acceptance]
  debug_session: none

No public UI, formation, holdout, counted-play, rule, archive or tag behavior is admitted by these tests. Source repair remains within the existing plan; no automatic retry is authorized by this UAT record.
