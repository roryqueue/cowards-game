---
phase: 263-legal-planner-and-deterministic-runner-feasibility
plan: "05"
type: execute
wave: 2
depends_on: ["263-01"]
files_modified: ["packages/strategy-lab/src/tasks.ts","packages/strategy-lab/src/identity.ts","packages/strategy-lab/src/tasks.test.ts","packages/strategy-lab/src/shards.ts","packages/strategy-lab/src/shards.test.ts","packages/strategy-lab/src/reduce.ts","packages/strategy-lab/src/runner.ts","packages/strategy-lab/src/worker.ts","packages/strategy-lab/src/runner-invariance.test.ts"]
autonomous: true
requirements: ["FACT-03","FACT-04"]
must_haves:
  truths:
    - "Worker count, shard packing, ordering and restart/resume cannot alter scientific task identities or semantic reduced bytes."
    - "Every allocation has one charged attempt or explicit unused disposition; failures and uncertain work cannot become payoffs."
    - "Publication and reduction reject gaps, duplicate coverage, conflict, truncation, stale roots and tampering without overwriting history."
  artifacts:
    - path: "packages/strategy-lab/src/tasks.ts"
      provides: "Fixed task graph before worker launch"
      exports: ["enumerateLabTasks","assignLabTasks"]
    - path: "packages/strategy-lab/src/identity.ts"
      provides: "Canonical domain-separated identities"
      exports: ["deriveLabTaskId","deriveLabStream"]
    - path: "packages/strategy-lab/src/shards.ts"
      provides: "Single-publisher atomic validated shards"
      exports: ["publishLabShard","readLabShard","resumeLabInventory"]
    - path: "packages/strategy-lab/src/reduce.ts"
      provides: "Canonical semantic and separate operational reduction"
      exports: ["reduceLabRecords"]
    - path: "packages/strategy-lab/src/runner.ts"
      provides: "Bounded trusted task runner"
      exports: ["runLabTasks"]
  key_links:
    - from: "packages/strategy-lab/src/runner.ts"
      to: "packages/strategy-lab/src/tasks.ts"
      via: "dispatch only preassigned task IDs"
    - from: "packages/strategy-lab/src/runner.ts"
      to: "packages/strategy-lab/src/shards.ts"
      via: "publish verified complete records through single coordinator"
    - from: "packages/strategy-lab/src/reduce.ts"
      to: "packages/strategy-lab/src/shards.ts"
      via: "validate full inventory then canonical task-id sort"
---

<objective>
Implement stable tasks, complete charged ledgers, atomic shards and deterministic worker/resume reduction per D-05/D-17/D-18/D-19/D-22. Use synthetic effects only; worker-scale empirical execution waits for Plan06 gate.
Purpose: Prove legal private feasibility before factory scale, without changing canonical rules or historic evidence.
Output: The named implementation and tests below; no empirical pass is presumed.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-CONTEXT.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-RESEARCH.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-PATTERNS.md
@.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-VALIDATION.md
@.planning/artifacts/v1.38-pre-search-study-policy.json
@.planning/artifacts/v1.38-pre-search-measurement-policy.json
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-203-SUMMARY.md
@.planning/phases/262-foundation-admission-measurement-custody-and-containment-con/262-VERIFICATION.md
</context>

## Artifacts this phase produces

- `packages/strategy-lab/src/tasks.ts`: `enumerateLabTasks`, `assignLabTasks` — Fixed task graph before worker launch.
- `packages/strategy-lab/src/identity.ts`: `deriveLabTaskId`, `deriveLabStream` — Canonical domain-separated identities.
- `packages/strategy-lab/src/shards.ts`: `publishLabShard`, `readLabShard`, `resumeLabInventory` — Single-publisher atomic validated shards.
- `packages/strategy-lab/src/reduce.ts`: `reduceLabRecords` — Canonical semantic and separate operational reduction.
- `packages/strategy-lab/src/runner.ts`: `runLabTasks` — Bounded trusted task runner.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define fixed task and stream identity</name>
  <files>packages/strategy-lab/src/tasks.ts, packages/strategy-lab/src/identity.ts, packages/strategy-lab/src/tasks.test.ts</files>
  <read_first>packages/strategy-lab/src/contracts.ts; feasibility-protocol.ts; scripts/lib/v1-38-study-contract.ts allocation domain framing</read_first>
  <behavior>Root/algorithm/input/split/budget/ordinal/purpose changes identity; workers/shards/time do not; domains differ; seed vectors are stable; alias checks are separate purposes not duplicate scientific coverage.</behavior>
  <action>Create enumerateLabTasks/assignLabTasks, deriveLabTaskId and deriveLabStream per D-05/D-17/D-18. Pre-enumerate complete graph and attempt allocation before launch. Scientific task identity binds admitted root, algorithm/version, candidate/opponent/input roots, development split, geometry-side-entrant initiative cell, budget, ordinal and purpose. Alias compatibility tasks explicitly reference the scientific representative and stay outside payoff denominator. Reproduction runs share semantic task IDs but have distinct charged attempt IDs and operational run identity. Derive independent streams with domain-separated SHA256 counter blocks; deterministic PRNG/version/counter schema are explicit. Never seed from worker, shard, timestamp, run order or host identity. Include all attempts/rejections/failures/unused allocations in append-only accounting and reject invented retry capacity. Tests fix canonical vectors and malformed/colliding identity inputs.</action>
  <acceptance_criteria>Complete task/attempt inventory and independent stream vectors are deterministic and budget-bounded.</acceptance_criteria>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 packages/strategy-lab/src/tasks.test.ts</automated></verify>
  <done>Complete task/attempt inventory and independent stream vectors are deterministic and budget-bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish strict shards and safe resume inventory</name>
  <files>packages/strategy-lab/src/shards.ts, packages/strategy-lab/src/shards.test.ts</files>
  <read_first>packages/strategy-lab/src/contracts.ts; tasks.ts; 263-PATTERNS.md shards guidance</read_first>
  <behavior>Faults before/during write, before/after publication and before ledger observation preserve evidence; gaps/duplicates/conflicts/truncation/tamper/stale roots/unsafe paths fail; resume never reruns completed tasks.</behavior>
  <action>Implement bounded raw-byte shard schemas and private generated paths. Single coordinator writes complete temp files in destination directory, flushes/closes, rereads/validates exact schema/task inventory/digests, then atomically publishes with destination no-clobber semantics; rename alone is not no-clobber. Reject symlink/traversal/unexpected file targets and preserve conflicting files. On resume read and validate published records before exclusion from dispatch; reconcile append-only attempt ledger and publication uncertainty without refund/retry. An interrupted launched live attempt is charged and terminal non-pass under this no-retry allocation; synthetic fault tests may exercise safe resume at boundaries before dispatch or after complete publication. Full-trace references are private/bounded and verified. Keep temporary/incomplete output out of reduction; content-address physical shard data and ledger without putting packing layout in scientific roots per D-19.</action>
  <acceptance_criteria>Atomic publication and all crash-boundary/duplicate/tamper/path tests pass with no silent overwrite or lost charge.</acceptance_criteria>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 packages/strategy-lab/src/shards.test.ts</automated></verify>
  <done>Atomic publication and all crash-boundary/duplicate/tamper/path tests pass with no silent overwrite or lost charge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement trusted runner and canonical reduction</name>
  <files>packages/strategy-lab/src/reduce.ts, packages/strategy-lab/src/runner.ts, packages/strategy-lab/src/worker.ts, packages/strategy-lab/src/runner-invariance.test.ts</files>
  <read_first>packages/strategy-lab/src/tasks.ts; shards.ts; contracts.ts; scripts/lib/v1-38-lean-runner-feasibility.ts reducer analogy</read_first>
  <behavior>Semantic bytes match1/2workers, shard1/3, forward/reverse/fixed permutation, restart-after-completed-publication and resume; operational roots differ and retain all charges; incomplete/system-failed work is unscored.</behavior>
  <action>Implement trusted worker pool and runLabTasks with fixed assignments and bounded worker result envelopes; workers execute injected trusted jobs, never hostile source outside supervision. reduceLabRecords validates full expected coverage and attempt identity, rejects even byte-identical duplicate coverage within a pass, sorts by canonical task ID and rederives semantic roots/payoffs/telemetry. Explicit typed semantic projection excludes physical packing, attempts, worker count, timing and host provenance; separately rooted operational evidence retains those fields. No recursive arbitrary field deletion to manufacture equality. Preserve outcome/final-state/ordered-transition-event/runtime-semantic accounting roots and unscored player/system classifications exactly. Synthetic matrix is Cartesian workers{1,2}×shards{1,3}×order{forward,reverse,fixed permutation}×lifecycle{continuous,restart-after-publish,resume-complete}; inject lost worker and ambiguous publication as non-pass cases with charged attempts. Real matrix uses only Plan01's two runs and no retries; this plan performs no live source/Match work per D-18/D-22.</action>
  <acceptance_criteria>All declared synthetic invariance variants have identical semantic bytes and accurate separate operational/attempt evidence.</acceptance_criteria>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 packages/strategy-lab/src/runner-invariance.test.ts</automated></verify>
  <done>All declared synthetic invariance variants have identical semantic bytes and accurate separate operational/attempt evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

Private manifest/files → trusted coordinator; coordinator → hostile Strategy supervisor; lab → production exclusion.
ASVS L1 scope, not certification. Every unresolved high/critical finding blocks empirical execution and downstream handoff.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-263-05-A | Tampering | Files/shards | high | mitigate | Bounded raw admission, no-clobber publisher and full fault-injection matrix. |
| T-263-05-B | Repudiation | Attempts/resume | high | mitigate | Preassigned attempt IDs, append-only charges and explicit uncertainty non-pass. |
| T-263-05-C | Tampering | Reduction | high | mitigate | Exact coverage, duplicate rejection and typed semantic projection before canonical sort. |
| T-263-05-SC | Tampering | Dependency setup | high | mitigate | Use existing locked dependencies only; no install or upgrade is planned. New external dependencies require research legitimacy audit before installation. |
</threat_model>

<verification>
Run each task's focused command after its tests are created, then the implemented phase synthetic suites and package typecheck. Test first, implement, rerun. Keep normal corrections in this plan; do not invent numbered review/custody successors. Never rerun consumed Phase262 selectors.
</verification>

<success_criteria>
Worker count, shard packing, ordering and restart/resume cannot alter scientific task identities or semantic reduced bytes.
Every allocation has one charged attempt or explicit unused disposition; failures and uncertain work cannot become payoffs.
Publication and reduction reject gaps, duplicate coverage, conflict, truncation, stale roots and tampering without overwriting history.
No source, objectives, memory, teacher state, private trace, runtime internals or host diagnostics enter public/default outputs. No formation namespace or holdout access; no factory-scale execution.
</success_criteria>


<output>
Create `.planning/phases/263-legal-planner-and-deterministic-runner-feasibility/263-05-SUMMARY.md` with actual commands/results and honest limitations. Commit only owned changes and preserve other agents' edits.
</output>

