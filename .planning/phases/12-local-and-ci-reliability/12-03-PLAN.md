---
phase: 12-local-and-ci-reliability
plan: 03
type: execute
wave: 2
status: complete
depends_on: [12-01]
files_modified:
  - apps/web/e2e/workshop-to-replay.spec.ts
  - apps/web/app/api/test-support/run-worker-once/route.ts
  - apps/web/app/api/test-support/run-worker-once/route.test.ts
  - package.json
autonomous: true
requirements: [REL-04, REL-05, REL-06]
---

# Plan 12-03: Service-Backed E2E Diagnostics

## Objective

Make the existing Workshop edit -> submit revision -> execute Match -> replay flow a first-class service-backed E2E command with failure-layer diagnostics.

## Tasks

1. Add `e2e:service` that runs `RUN_SERVICE_E2E=1 PLAYWRIGHT_TEST=1 pnpm e2e -- workshop-to-replay.spec.ts`.
2. Improve `workshop-to-replay.spec.ts` assertions with labelled failure messages for worker execution, Chronicle/replay availability, projection, and UI rendering.
3. Improve `/api/test-support/run-worker-once` error responses to include `layer: "worker_execution"` and bounded stdout/stderr snippets.
4. Add route tests for success and failure diagnostics.

## Verification

- `pnpm --filter @cowards/web test -- run-worker-once/route.test.ts`
- `RUN_SERVICE_E2E=1 PLAYWRIGHT_TEST=1 pnpm e2e -- workshop-to-replay.spec.ts`

## Done

The service E2E command is locally runnable and failed runs tell the developer which layer broke.
