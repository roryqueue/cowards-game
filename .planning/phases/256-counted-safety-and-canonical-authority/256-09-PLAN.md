---
phase: 256-counted-safety-and-canonical-authority
plan: "09"
type: execute
wave: 7
depends_on: ["256-02", "256-03", "256-17", "256-18"]
files_modified:
  - apps/runtime-service/src/runtime-evidence-authority.ts
  - apps/runtime-service/src/runtime-evidence-authority.test.ts
  - apps/runtime-service/src/execute-match.ts
  - apps/runtime-service/src/server.ts
  - apps/runtime-service/src/index.ts
  - apps/runtime-service/src/counted-safety.test.ts
  - apps/runtime-service/src/execute-match.test.ts
  - apps/runtime-service/src/four-language-parity.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04, AUTH-03]
must_haves:
  truths:
    - "Runtime-service starts only with a valid current locally mounted signed authority bundle verified against a separately configured public key; request payloads only reference identities and cannot echo proof into trust."
    - "Runtime-service startup and refresh enforce the deployment-pinned minimum plus a protected durable generation/hash high-water anchor before listening or accepting a newer bundle, including after restart."
    - "Runtime-service rejects either entrant's missing containment before adapter creation and reopens/reverifies the bundle immediately before invocation and after execution."
    - "Evidence drift is a redacted system failure with no gameplay, memory, Chronicle, success, or player penalty."
    - "Every runtime-service request builder, including direct execution and four-language parity fixtures, carries only authority references resolved from an explicit fixture-domain bundle that production verification rejects."
  artifacts:
    - path: "apps/runtime-service/src/runtime-evidence-authority.ts"
      provides: "DB-free mounted authority-bundle loader with independent Node Ed25519/hash/schema/freshness verification"
    - path: "apps/runtime-service/src/counted-safety.test.ts"
      provides: "Request-time and in-flight exact-evidence matrix"
    - path: "apps/runtime-service/src/execute-match.test.ts"
      provides: "Reference-only execution-request fixtures bound to non-production authority"
    - path: "apps/runtime-service/src/four-language-parity.test.ts"
      provides: "Reference-only four-language fixtures that prove parity without implying production eligibility"
  key_links:
    - from: "apps/runtime-service/src/execute-match.ts"
      to: "packages/spec/src/runtime-evidence.ts"
      via: "canonical per-side evaluation from a freshly verified mounted authority bundle at acceptance, pre-invocation, and post-execution"
      pattern: "verifyRuntimeEvidenceAuthorityBundle|evaluateExecutableLaneEligibility"
    - from: "apps/runtime-service/src/index.ts"
      to: "apps/runtime-service/src/runtime-evidence-authority.ts"
      via: "verifies the installed publication, bootstrap pin, and durable high-water anchor before creating or listening on the HTTP server"
      pattern: "load.*RuntimeEvidenceAuthority"
---

<objective>
Enforce exact current evidence from an independent signed authority channel around every runtime-service Strategy invocation.

Purpose: D-01 through D-04 require quarantine, execution containment, restart-safe anti-rollback, and no-mutation system failure. Output: focused boundary tests, durable startup/refresh anchoring, and service rechecks. Focused legacy-worker retirement is owned by Plan 19.
</objective>

## Artifacts this phase produces

- A DB-free runtime-service loader that independently verifies the mounted signed authority envelope, deployment pin, and durable high-water anchor.
- Reference-only request builders across `execute-match.test.ts`, `counted-safety.test.ts`, and `four-language-parity.test.ts`, backed only by explicit fixture-domain authority bundles rejected in production mode.
- Pre-invocation and post-execution exact evidence rechecks with redacted system-failure semantics.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-03-SUMMARY.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-18-SUMMARY.md
@apps/runtime-service/src/execute-match.ts
@apps/runtime-service/src/server.ts
@packages/spec/src/runtime-evidence.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load and independently verify the mounted runtime evidence authority</name>
  <read_first>
    - apps/runtime-service/src/execute-match.ts
    - apps/runtime-service/src/execute-match.test.ts
    - apps/runtime-service/src/server.ts
    - packages/spec/src/runtime-evidence-authority-bundle.ts
    - packages/spec/src/runtime-evidence.ts
    - packages/spec/src/runtime-execution-service.ts
  </read_first>
  <files>apps/runtime-service/src/runtime-evidence-authority.ts, apps/runtime-service/src/runtime-evidence-authority.test.ts, apps/runtime-service/src/server.ts, apps/runtime-service/src/index.ts</files>
  <behavior>
    - Startup fails closed before listening when the mounted bundle is missing, oversized, malformed, incorrectly signed, keyed to the wrong separately configured public key/key ID, stale/not-yet-valid, generation-regressed, hash-mismatched, or has a dangling/unverified attestation graph.
    - Verification uses Node platform `crypto.verify` with Ed25519 and SHA-256 over the exact payload bytes from the single-file signed envelope; no request field, DB access, injected resolver, gate name, documentation, or fixture can become authority.
    - The bundle contains registry generation, validity window, tuple-manifest hash, verified producer/import graph, exact certificates, revocations/supersessions, and operator lane disables; production conformance certificates are absent until Phase 259 supplies a trusted producer/import.
    - Operators publish a complete new envelope to a temporary file, fsync/close it, then atomically rename it over the mounted path; each verification opens and reads one file descriptor, and generation may only increase.
    - Startup requires configured bundle/public-key/high-water paths plus deployment-pinned minimum generation/hash. Initial creation of a missing anchor is allowed only in explicit bootstrap mode when the bundle exactly matches the pin; normal cold start with a missing, corrupt, or unverifiable anchor fails closed.
    - A valid newer bundle is not accepted for startup or execution until the new generation/hash anchor is atomically temp-written, file-fsynced, renamed, and parent-directory-fsynced. Lower generation and same-generation/different-hash reject across process restart; unwritable anchors leave the last-good bundle authoritative.
  </behavior>
  <action>Write loader/verifier tests first, then add a DB-free authority component configured by `COWARDS_RUNTIME_EVIDENCE_AUTHORITY_BUNDLE_PATH`, separate `COWARDS_RUNTIME_EVIDENCE_AUTHORITY_PUBLIC_KEY_PATH`, `COWARDS_RUNTIME_EVIDENCE_AUTHORITY_MIN_GENERATION`, `COWARDS_RUNTIME_EVIDENCE_AUTHORITY_MIN_BUNDLE_HASH`, and a protected `COWARDS_RUNTIME_EVIDENCE_AUTHORITY_HIGH_WATER_PATH`. Strictly cap/read the single signed envelope, decode the exact payload bytes, verify Ed25519 using Node platform crypto, recompute the payload hash, validate the closed schema/evidence graph/freshness/generation, compare both deployment pin and durable anchor, deep-freeze the result, and fail `index.ts` before server creation/listen on any error. Initial anchor creation requires explicit `COWARDS_RUNTIME_EVIDENCE_AUTHORITY_BOOTSTRAP=1` plus an exact pin; normal mode never treats a missing anchor as first boot. For a valid newer generation, atomically persist and fsync the strict non-secret anchor before returning it as accepted authority. Reopen and fully verify rather than trusting a cached request echo. Use injectable platform-fs seams for missing/corrupt/unwritable anchor, fsync/rename failure, concurrent refresh, restart after refresh, rollback, revocation rollback, and kill-switch rollback tests. Test-only keys/bundles must carry a fixture trust-domain ID that production mode rejects. Public/default output and logs may expose neither configured paths nor anchor internals.</action>
  <verify><automated>pnpm --filter @cowards/runtime-service exec vitest run src/runtime-evidence-authority.test.ts src/server.test.ts</automated></verify>
  <acceptance_criteria>
    - Invalid/unavailable/stale/regressed/forked authority or invalid/unwritable anti-rollback state prevents server startup before listening.
    - The mounted bundle, separate public key, and production trust-domain checks are the only authority inputs.
    - Atomic replacement yields either the complete old or complete new valid envelope, never a partial authority state.
  </acceptance_criteria>
  <done>Runtime-service has a production-shaped, DB-free, independently verified current evidence authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce per-side authority checks around every Strategy invocation</name>
  <read_first>
    - apps/runtime-service/src/runtime-evidence-authority.ts
    - apps/runtime-service/src/counted-safety.test.ts
    - apps/runtime-service/src/execute-match.test.ts
    - apps/runtime-service/src/four-language-parity.test.ts
    - packages/spec/src/runtime-execution-service.ts
  </read_first>
  <files>apps/runtime-service/src/counted-safety.test.ts, apps/runtime-service/src/execute-match.ts, apps/runtime-service/src/execute-match.test.ts, apps/runtime-service/src/four-language-parity.test.ts</files>
  <behavior>
    - Request acceptance resolves only the tuple and ordered bottom/top certificate references from the freshly verified bundle; request-embedded certificate-shaped objects are forbidden.
    - Every `RuntimeExecutionServiceRequest` builder in runtime-service tests uses a reference-only fixture identity (bundle hash/generation, exact tuple, per-side lane/certificate IDs/hashes) resolved from an explicit fixture trust-domain bundle; fixture records are impossible to validate in production mode.
    - Four-language parity remains an execution-shape test only and cannot be cited as counted containment/conformance certification or production eligibility.
    - Missing/failed/stale containment on either side rejects before adapter factory or Strategy callback; counted purpose requires current conformance on both sides, while explicit exhibition requires containment on both (D-02, D-03).
    - The service reopens/reverifies and durably advances high-water immediately before adapter invocation and after execution. Revocation, kill switch, bundle replacement/hash change, generation drift, anchor-write failure, or either side's identity drift aborts as redacted system failure with no adapter result, gameplay, memory, Chronicle, success, or player penalty (D-04).
  </behavior>
  <action>Write the service spy/immutability matrix, then validate request identity references against a fresh authority load at acceptance, immediately before adapter invocation, and after the adapter returns. Migrate every runtime-service `RuntimeExecutionServiceRequest` constructor/builder—especially `requestFor` in `execute-match.test.ts`, focused counted-safety fixtures, inline malformed/drift cases, and `requestForPair` in `four-language-parity.test.ts`—to populate only the reference-only authority envelope from Plan 17. Build the referenced tuple, lane, certificate IDs/hashes, bundle hash, and generation from one explicit fixture-domain authority bundle/load helper; never put certificate bodies, graph nodes, signatures, gate results, or a production trust-domain identity in a request. Assert production-mode authority loading rejects those fixture bundles and that four-language parity success does not yield a counted certificate or production eligibility. The loader may return a valid newer bundle only after its high-water anchor is durable; anchor failure retains the last-good authority and rejects the request. Keep the exact authority snapshot/hash used at invocation and require post-check equality; any valid newer bundle still aborts the in-flight result so a fresh job can run. Return only schema-valid redacted system failure.</action>
  <verify><automated>pnpm --filter @cowards/runtime-service exec vitest run src/runtime-evidence-authority.test.ts src/counted-safety.test.ts src/execute-match.test.ts src/server.test.ts</automated></verify>
  <acceptance_criteria>
    - Disabled or drifting requests never construct/return an adapter result and emit only privacy-safe system failure.
    - All direct, inline, and four-language request builders are reference-only and fixture-domain proof can never satisfy production authority.
    - Package-wide runtime-service tests typecheck and run without a legacy request builder bypass.
  </acceptance_criteria>
  <done>Every runtime-service execution is bound to one exact current independently verified authority snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Request to hostile Strategy adapter | Forged/stale evidence must not permit execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-10 | Spoofing/TOCTOU | runtime-service checks | high | mitigate | Independently verified mounted signed authority at startup/pre/post, deployment pin, durable high-water fsync/rename, exact snapshot/hash comparison, redacted system failure, and invocation spies. |
| T-256-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>

<verification>Run focused runtime-service suites plus package typecheck.</verification>
<success_criteria>No containment means no execution, and in-flight drift never becomes gameplay or player fault.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-09-SUMMARY.md` when done.</output>
