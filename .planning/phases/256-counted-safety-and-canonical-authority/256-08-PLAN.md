---
phase: 256-counted-safety-and-canonical-authority
plan: "08"
type: execute
wave: 7
depends_on: ["256-02", "256-03", "256-04", "256-05", "256-07", "256-15", "256-17", "256-18"]
files_modified:
  - packages/persistence/src/ladder.ts
  - packages/persistence/src/ladder.test.ts
  - packages/persistence/src/jobs.ts
  - packages/persistence/src/jobs.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "TypeScript entry and scheduling evaluate current exact evidence for every entrant and pass one semantic tuple plus the complete normalized entrant map to the real MatchSet writer."
    - "Scheduling and claim bind to a persisted installed-publication generation/hash/source set rather than deriving authority from loose certificate-shaped rows."
    - "Claim skips stale, revoked, mixed, or generation-drifted queued work before any job, Match, lease, or attempt mutation."
  artifacts:
    - path: "packages/persistence/src/ladder.ts"
      provides: "Evidence-derived per-entrant entry/scheduling snapshot owner"
    - path: "packages/persistence/src/jobs.ts"
      provides: "Exact-identity pre-claim SQL gate"
  key_links:
    - from: "packages/persistence/src/ladder.ts"
      to: "packages/persistence/src/matchset-service.ts"
      via: "passes the validated semantic tuple and complete MatchSetIntegrityIdentity to insertMatchSetWithMatrixOnClient"
      pattern: "insertMatchSetWithMatrixOnClient"
---

<objective>
Enforce exact evidence at TypeScript entry, scheduling, and claim boundaries.

Purpose: D-04 requires rejection both before scheduling and before claim. Output: canonical evaluator wiring, exact snapshot persistence through the writer from Plan 05, and no-mutation claim proof.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-05-SUMMARY.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-18-SUMMARY.md
@packages/persistence/src/ladder.ts
@packages/persistence/src/jobs.ts
@packages/spec/src/runtime-evidence.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate entry and scheduling across every entrant's current exact evidence</name>
  <read_first>
    - packages/persistence/src/ladder.ts
    - packages/persistence/src/ladder.test.ts
    - packages/persistence/src/matchset-service.ts
    - packages/spec/src/runtime-evidence.ts
  </read_first>
  <files>packages/persistence/src/ladder.ts, packages/persistence/src/ladder.test.ts</files>
  <behavior>
    - Missing containment for any entrant prevents scheduling; all entrants having containment but at least one lacking conformance permits only explicitly non-counted exhibition; counted scheduling requires complete exact current proof for every entrant (D-02, D-03).
    - Evaluation occurs inside the locked transaction and passes one semantic tuple plus a complete normalized two-to-eight entrant evidence map to `insertMatchSetWithMatrixOnClient` (D-04, AUTH-02).
    - Valid mixed-language entrants keep independent lane/certificate identities; neither the first entrant nor a MatchSet-level value is reused for the cohort.
    - Wildcards, aliases, mixed/partial tuples, provider proof alone, and unresolved history reject (D-07, AUTH-03).
  </behavior>
  <action>Replace provider-proof and independent spec/engine acceptance with the canonical evaluator. Load the current persisted installed-publication record produced by Plan 18, verify its exact bundle generation/hash/source set against the append-only publication ledger, evaluate every entrant's exact active lane inside the existing scheduling transaction, derive aggregate purpose fail-closed, build one semantic tuple plus normalized entrant map, and pass it to the actual MatchSet writer. Loose certificate rows or a merely prepared/failed publication are never scheduling authority. Test two-to-eight entrants, mixed TypeScript/Python/Rust/Zig lanes, one-bad-entrant aggregate failure, exhibition-only mixtures, counted impossibility while the production conformance producer set is empty, publication/source drift, mutation, and stale-after-entry cases.</action>
  <verify><automated>pnpm --filter @cowards/persistence exec vitest run src/ladder.test.ts src/matchset-service.test.ts</automated></verify>
  <acceptance_criteria>
    - No unproved lane creates counted work.
    - Exhibition work is explicit and non-counted.
    - Scheduled record families share one tuple and the same complete per-entrant snapshot with correctly ordered Match pairs.
  </acceptance_criteria>
  <done>TypeScript scheduling derives and persists exact eligibility once per transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject stale queued work before claim mutation</name>
  <read_first>
    - packages/persistence/src/jobs.ts
    - packages/persistence/src/jobs.test.ts
    - packages/persistence/migrations/0012_integrity_authority.sql
    - packages/spec/src/runtime-evidence.ts
  </read_first>
  <files>packages/persistence/src/jobs.ts, packages/persistence/src/jobs.test.ts</files>
  <behavior>
    - Candidate SQL locks and joins the Match/job semantic tuple, ordered bottom/top evidence pair, active installed-publication record/hash/generation/source set, and both sides' containment/conformance records before changing status (D-04).
    - Stale, revoked, mismatched, missing, or generation-drifted candidates remain queued with unchanged lease/attempt/Match fields.
    - A valid claim returns the exact snapshot for transport/completion.
  </behavior>
  <action>Extend the actual `FOR UPDATE SKIP LOCKED` claim query and transaction, not a display classifier. Re-evaluate the exact ordered bottom/top pair against the independently verified current authority bundle before any `running`, lease, attempt, or Match update. Skip a candidate if either side fails or the bundle hash/generation drifts, and return the immutable tuple/pair on success. Add query-recording and real PostgreSQL assertions for every per-side drift class and heterogeneous pairs.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; pnpm --filter @cowards/persistence exec vitest run src/jobs.test.ts &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence exec vitest run src/jobs.test.ts -t "integrity identity|before claim"</automated></verify>
  <acceptance_criteria>
    - Failed candidates are byte-for-byte lifecycle-equivalent after claim attempt.
    - Valid claims carry the complete locked identity.
    - Rejection happens before all lifecycle mutation.
  </acceptance_criteria>
  <done>The TypeScript claim owner fails closed on current-evidence drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry/certificates to scheduler and claim owner | Stale proof could create or run counted jobs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-09 | Elevation of Privilege/TOCTOU | scheduling and claim | high | mitigate | In-transaction evaluation, exact snapshot copy, joined claim recheck, and no-mutation DB tests. |
| T-256-SC | Tampering | package supply chain | low | accept | Existing stack only. |
</threat_model>

<verification>Run ladder and job tests including configured PostgreSQL claim cases.</verification>
<success_criteria>No missing/stale/mixed evidence can create counted or claimable TypeScript work.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-08-SUMMARY.md` when done.</output>
