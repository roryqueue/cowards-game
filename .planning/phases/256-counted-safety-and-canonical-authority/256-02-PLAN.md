---
phase: 256-counted-safety-and-canonical-authority
plan: "02"
type: execute
wave: 2
depends_on: ["256-01"]
files_modified:
  - packages/spec/src/runtime-evidence.ts
  - packages/spec/src/runtime-evidence.test.ts
  - packages/spec/src/runtime.ts
  - packages/spec/src/competition-entry-eligibility.ts
  - packages/spec/src/competition-entry-eligibility.test.ts
  - packages/spec/src/public-output-privacy.ts
  - packages/spec/src/index.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04, AUTH-01, AUTH-03]
must_haves:
  truths:
    - "Every current runtime lane is quarantined until exact current containment and conformance certificates prove its full executable identity."
    - "Missing containment disables execution; containment without conformance permits exhibition only; complete exact evidence alone permits counted use."
    - "Operators can disable a lane but cannot promote it around missing evidence."
    - "Public status is calm and allowlisted while restricted operator detail remains credential, source, artifact-byte, memory, objective, diagnostic, host-path, and exploit safe."
  artifacts:
    - path: "packages/spec/src/runtime-evidence.ts"
      provides: "Executable lane identity, certificates, reason codes, evaluator, and public/operator projections"
      exports: ["EXECUTABLE_LANE_EVIDENCE_REASON_CODES", "evaluateExecutableLaneEligibility", "projectPublicLaneEvidence", "projectOperatorLaneEvidence"]
    - path: "packages/spec/src/runtime-evidence.test.ts"
      provides: "Wave 0 exact-evidence, quarantine, freshness, kill-switch, and privacy matrix"
  key_links:
    - from: "packages/spec/src/runtime.ts"
      to: "packages/spec/src/runtime-evidence.ts"
      via: "legacy product semantics are derived from the canonical evaluator instead of static counted booleans"
      pattern: "evaluateExecutableLaneEligibility"
    - from: "packages/spec/src/competition-entry-eligibility.ts"
      to: "packages/spec/src/runtime-evidence.ts"
      via: "entry categories and safe copy project canonical reason codes"
      pattern: "reasonCode|eligibility"
---

<objective>
Replace counted-by-declaration with one pure, exact, evidence-derived lane evaluator and privacy-safe projections.

Purpose: Per D-01 through D-03, all lanes begin quarantined and promotion is evidence-derived. SAFE-01, SAFE-02, and SAFE-04 require exact current proof and a strict public/operator split.
Output: Canonical executable evidence contracts, exhaustive evaluation tests, converged runtime compatibility views, and safe public/operator DTOs.
</objective>

## Artifacts this phase produces

- `packages/spec/src/runtime-evidence.ts`: `ExecutableLaneIdentity`, `ContainmentCertificate`, `ConformanceCertificate`, `ExecutableLaneEvidenceBundle`, `ExecutableLaneEligibility`, `ExecutableLaneEvidenceReasonCode`, `PublicLaneEvidenceProjection`, `OperatorLaneEvidenceProjection`, `evaluateExecutableLaneEligibility`, `projectPublicLaneEvidence`, and `projectOperatorLaneEvidence`.
- `packages/spec/src/runtime-evidence.test.ts`: Wave 0 certificate mutation/freshness/quarantine/privacy suite.
- Updated runtime and competition-entry facades that derive status from the canonical evaluator.
- Expanded public-output denylist for evidence/security internals.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-RESEARCH.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-PATTERNS.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-VALIDATION.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-01-SUMMARY.md
@packages/spec/src/integrity-authority.ts
@packages/spec/src/runtime.ts
@packages/spec/src/competition-entry-eligibility.ts
@packages/spec/src/competition-governance.ts
@packages/spec/src/public-output-privacy.ts

<interfaces>
`SUPPORTED_STRATEGY_LANGUAGES`, `STRATEGY_RUNTIME_ADAPTER_REGISTRY`, `evaluateStrategyRuntimeCountedEligibility`, and `describeStrategyRuntimeProductSemantics` are existing compatibility surfaces. Preserve their callable shape where practical, but their counted readiness must be a projection of the new evaluator rather than an independent authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact containment and conformance evidence evaluation</name>
  <read_first>
    - packages/spec/src/integrity-authority.ts
    - packages/spec/src/runtime.ts
    - packages/spec/src/competition-governance.ts
    - .planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
  </read_first>
  <files>packages/spec/src/runtime-evidence.ts, packages/spec/src/runtime-evidence.test.ts</files>
  <behavior>
    - With no current containment certificate, every language/provider lane returns `disabled`; there is no TypeScript exception (D-01, D-02).
    - Current exact containment with missing, stale, revoked, failed, or mismatched conformance returns `exhibition_only`; two current exact passing certificates return `counted` (D-02, D-03).
    - The exact identity binds provider, language, runtime, toolchain, adapter, policy, corpus, artifact, implementation/build, and semantic tuple ID; mutating or omitting any member invalidates the bundle (SAFE-01).
    - Evaluation consumes verified certificate references resolved from the current authority, not arbitrary certificate-shaped objects; unit fixtures use an explicit non-production verifier output.
    - Operator disable always wins; removing it re-evaluates evidence and cannot itself promote a lane (D-03).
    - Freshness uses a caller-supplied evaluation instant and certificate bounds, with no hidden wall-clock read.
  </behavior>
  <action>Write the Wave 0 matrix first, then implement immutable certificate-reference and identity contracts plus a pure evaluator. The evaluator receives certificates only through an authority-resolution interface branded by the verified import/bundle path in Plans 15 and 17; plain object shape cannot assert verification. Persist stable reason codes for operator disable, missing/stale/revoked/failed/unverifiable containment or conformance, identity mismatch, unknown/uncertified tuple, and registry-generation drift. Keep containment and conformance independent, make exact identity comparison structural and exhaustive, and never add an approval boolean that can bypass evidence. Test all current registry lanes as quarantined when no new certificates are supplied, per D-01.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec exec vitest run src/runtime-evidence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every lane has deterministic disabled/exhibition/countable behavior from exact evidence only.
    - No current static provider, adapter, or signed-source label grants counted eligibility.
    - Per-field mutation, stale/revoked evidence, and kill-switch cases are exhaustive and typed.
  </acceptance_criteria>
  <done>One pure evaluator derives all lane status from exact current containment and conformance evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Converge runtime and counted-entry compatibility facades</name>
  <read_first>
    - packages/spec/src/runtime-evidence.ts
    - packages/spec/src/runtime.ts
    - packages/spec/src/competition-entry-eligibility.ts
    - packages/spec/src/competition-entry-eligibility.test.ts
  </read_first>
  <files>packages/spec/src/runtime.ts, packages/spec/src/competition-entry-eligibility.ts, packages/spec/src/competition-entry-eligibility.test.ts, packages/spec/src/index.ts</files>
  <behavior>
    - Existing runtime product semantics never report counted without a canonical exact evidence bundle.
    - Entry eligibility distinguishes disabled and exhibition-only lanes using stable canonical reason codes.
    - Aliases, ranges, partial tuples, independently-known members, and old provider proof cannot satisfy the evaluator (D-07).
  </behavior>
  <action>Export the new module and route existing runtime broker/product and competition entry decisions through it. Retain compatibility-facing functions needed by callers, but remove static `countedResultsAllowed` and provider-proof-as-readiness authority from their decision path. Make unsupported or absent evidence fail closed. Keep current language/provider registries descriptive; they may not manufacture containment, conformance, or tuple certification. Extend focused entry tests to prove old labels and provider proof alone cannot promote any lane.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec exec vitest run src/runtime-evidence.test.ts src/competition-entry-eligibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All exported counted-entry decisions consume canonical evidence evaluation.
    - All currently configured lanes remain disabled unless a test supplies exact current containment evidence.
    - No runtime/entry compatibility helper retains an independent promotion path.
  </acceptance_criteria>
  <done>Legacy callers receive fail-closed semantics from the single canonical evidence evaluator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive separate public and restricted operator evidence projections</name>
  <read_first>
    - packages/spec/src/runtime-evidence.ts
    - packages/spec/src/public-output-privacy.ts
    - packages/spec/src/competition-governance.ts
    - packages/spec/src/competition-entry-eligibility.ts
  </read_first>
  <files>packages/spec/src/runtime-evidence.ts, packages/spec/src/runtime-evidence.test.ts, packages/spec/src/competition-entry-eligibility.ts, packages/spec/src/public-output-privacy.ts</files>
  <behavior>
    - Public projections include only status, calm stable reason category/copy, semantic tuple ID, non-sensitive evidence version/hash, and freshness date (D-13, D-15).
    - Operator projections add exact identities, gate results, remediation, cohort impact, and restricted proof IDs/links while excluding source, artifact bytes, memories, objectives, credentials, host paths, raw diagnostics, and exploit details (D-14).
    - Nested forbidden fields and marker values fail privacy assertions in both projections (SAFE-04).
  </behavior>
  <action>Define separate allowlisted constructors rather than redacting a shared oversized DTO. Map stable reason codes to calm public copy and precise restricted remediation. Expand the shared public-output privacy vocabulary for evidence bytes, proof storage paths, host/toolchain diagnostics, credentials, source, memory, objectives, and security details. Treat hashes as public only when they identify the safe certificate record/version rather than restricted source or artifact bytes. Cover D-13 through D-15 with table-driven and nested-leak tests.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec exec vitest run src/runtime-evidence.test.ts src/competition-entry-eligibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Public DTOs are constructed from an explicit allowlist and pass the shared leak scanner.
    - Operator DTOs remain structured and useful without carrying forbidden private bytes or diagnostics.
    - Public copy never serializes internal runtime error text.
  </acceptance_criteria>
  <done>Evidence status has privacy-safe public and restricted operator representations derived from one reason-code authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Certificate records to eligibility evaluator | Potentially stale or forged evidence controls whether hostile Strategy execution may run or count. |
| Canonical evaluator to public/operator DTOs | Sensitive runtime and proof detail must not cross public output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-02 | Spoofing/Tampering | executable evidence bundle | high | mitigate | Exact full-identity equality, certificate freshness/revocation checks, exhaustive reason codes, and no override promotion. |
| T-256-03 | Information Disclosure | lane evidence projections | high | mitigate | Separate allowlisted constructors plus nested field and marker privacy scans. |
| T-256-SC | Tampering | package supply chain | low | accept | No package install is planned; only existing workspace contracts are used. |
</threat_model>

<verification>
Run focused spec tests, then `pnpm --filter @cowards/spec typecheck`. Inspect every current lane fixture and confirm it is non-counted without fresh Phase-256 certificates.
</verification>

<success_criteria>
- Counted eligibility is evidence-derived and exact.
- Missing containment disables execution and missing conformance prevents counted use.
- Operators can only reduce eligibility.
- Public and restricted operator views are separately typed and privacy safe.
</success_criteria>

<output>
Create `.planning/phases/256-counted-safety-and-canonical-authority/256-02-SUMMARY.md` when done.
</output>
