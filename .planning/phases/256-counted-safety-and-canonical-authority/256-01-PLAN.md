---
phase: 256-counted-safety-and-canonical-authority
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/spec/src/integrity-authority.ts
  - packages/spec/src/integrity-authority.test.ts
  - scripts/generate-v1-37-integrity-authority.ts
  - packages/spec/artifacts/v1.37-integrity-authority.json
  - packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-05]
must_haves:
  truths:
    - "Maintainers can resolve exactly one owner package and symbol for every canonical authority domain."
    - "A semantic compatibility tuple is accepted only when its six expanded components hash to its exact registered identifier."
    - "Executable implementation identity remains separate from semantic tuple identity."
  artifacts:
    - path: "packages/spec/src/integrity-authority.ts"
      provides: "Immutable owner registry, semantic tuple contract, fixed-order hashing, and exact lookup"
      exports: ["CANONICAL_AUTHORITY_REGISTRY", "CANONICAL_COMPATIBILITY_TUPLES", "hashCanonicalCompatibilityTuple", "resolveCanonicalCompatibilityTuple"]
    - path: "packages/spec/artifacts/v1.37-integrity-authority.json"
      provides: "Committed generated authority and tuple manifest"
    - path: "packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json"
      provides: "Language-neutral tuple byte and SHA-256 vectors"
  key_links:
    - from: "scripts/generate-v1-37-integrity-authority.ts"
      to: "packages/spec/src/integrity-authority.ts"
      via: "imports the canonical registry and deterministic renderer inputs"
      pattern: "integrity-authority"
    - from: "packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json"
      to: "packages/spec/src/integrity-authority.test.ts"
      via: "committed byte/hash vectors are verified against the TypeScript implementation"
      pattern: "tupleId|encodedBytes|sha256"
---

<objective>
Establish the spec-owned authority registry and atomic semantic compatibility tuple before any consumer can make eligibility decisions.

Purpose: AUTH-01 through AUTH-03 require one immutable ownership and identity source. Per D-05 through D-08, semantic behavior identity must be exact, domain-separated, and distinct from rebuild/runtime/toolchain evidence identity.
Output: A tested authority/tuple module plus deterministic committed manifests and cross-language hash vectors.
</objective>

## Artifacts this phase produces

- `packages/spec/src/integrity-authority.ts`: `CanonicalAuthorityDomain`, `CanonicalAuthorityRecord`, `CanonicalCompatibilityTuple`, `CanonicalCompatibilityTupleRecord`, `CANONICAL_AUTHORITY_REGISTRY`, `CANONICAL_COMPATIBILITY_TUPLES`, `encodeCanonicalCompatibilityTuple`, `hashCanonicalCompatibilityTuple`, and `resolveCanonicalCompatibilityTuple`.
- `packages/spec/src/integrity-authority.test.ts`: Wave 0 ownership, immutability, exact lookup, and hash-vector regression suite.
- `scripts/generate-v1-37-integrity-authority.ts`: deterministic `--write`/`--check` generator.
- `packages/spec/artifacts/v1.37-integrity-authority.json`: canonical owner and semantic tuple manifest.
- `packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json`: fixed UTF-8 bytes and SHA-256 vectors for TypeScript and Go parity.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-RESEARCH.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-PATTERNS.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-VALIDATION.md
@packages/spec/src/competition-governance.ts
@packages/spec/src/runtime.ts
@packages/replay/src/hash.ts
@scripts/evaluate-v1-35-boundary-surface-inventory.ts

<interfaces>
Use the repository's literal registry pattern (`as const` arrays plus derived union types), pure exact-lookup functions, platform `node:crypto` SHA-256, and deterministic generated-artifact `--write`/`--check` convention. Do not use JSON serialization as the tuple hash input because Phase 258 owns canonical JSON.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define one immutable authority registry and exact six-component tuple</name>
  <read_first>
    - packages/spec/src/competition-governance.ts
    - packages/spec/src/runtime.ts
    - packages/replay/src/hash.ts
    - .planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
  </read_first>
  <files>packages/spec/src/integrity-authority.ts, packages/spec/src/integrity-authority.test.ts</files>
  <behavior>
    - Every authority domain has one package/symbol owner and duplicate domains or owners are rejected (D-05, AUTH-01, AUTH-05).
    - The tuple contains exactly rules, engine, runtimeAbi, chronicle, arenaCatalog, and setPolicy in fixed order; changing any field changes the encoded bytes and hash (D-05, D-08).
    - Lookup accepts only an exact registered tuple ID whose expansion re-hashes to that ID; partial members, ranges, wildcards, aliases, unknown IDs, and mixed expansions fail atomically (D-07).
    - Exported constants and returned records cannot be mutated to alter later lookups.
  </behavior>
  <action>Create the Wave 0 test first, then implement the pure spec module. Encode tuple bytes with an explicit domain tag, NUL separators, fixed field order, UTF-8 byte lengths, and platform SHA-256. Register rules, transition semantics, runtime classification, Chronicle validation, arena authority, and Set policy owners. Keep the semantic tuple free of provider, implementation, toolchain, adapter, policy-certificate, corpus, artifact, and build identity so executable evidence can stale without minting a behavior tuple, per D-08. Return immutable copies or deeply frozen records rather than writable registry references. Cite D-05, D-07, and D-08 in module documentation.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec exec vitest run src/integrity-authority.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The authority domain set is exhaustive and contains exactly one owner per domain.
    - Exact registered ID plus matching expansion resolves; every component mutation and non-exact selector rejects.
    - Mutation attempts do not change registry state or subsequent hash/lookup results.
  </acceptance_criteria>
  <done>The spec owns a deterministic, immutable, exact authority and semantic tuple contract with passing Wave 0 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate committed authority manifests and language-neutral hash vectors</name>
  <read_first>
    - packages/spec/src/integrity-authority.ts
    - scripts/evaluate-v1-35-boundary-surface-inventory.ts
    - packages/spec/artifacts/service-api-v1.8.openapi.json
  </read_first>
  <files>scripts/generate-v1-37-integrity-authority.ts, packages/spec/artifacts/v1.37-integrity-authority.json, packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json, packages/spec/src/integrity-authority.test.ts</files>
  <behavior>
    - Repeated generation is byte-identical and `--check` fails on stale or hand-edited output.
    - Hash vectors publish the domain-separated encoded bytes and expected lowercase SHA-256 without host paths, timestamps that vary per run, secrets, or executable evidence bytes.
    - The manifest includes the tuple ID and complete expansion and separately identifies package/symbol ownership (D-05).
  </behavior>
  <action>Build a deterministic generator following the established artifact evaluator pattern. The authority artifact must contain stable schema/generator versions, the single owner registry, and registered semantic tuples. The vector artifact must include multiple mutation cases sufficient for Go parity in Plan 10. Extend the test to load both committed artifacts, verify every tuple expansion hashes to its identifier, and prove byte-stable regeneration. Keep all executable lane/certificate identity out of this semantic artifact, per D-08.</action>
  <verify>
    <automated>pnpm exec tsx scripts/generate-v1-37-integrity-authority.ts --write &amp;&amp; pnpm exec tsx scripts/generate-v1-37-integrity-authority.ts --check &amp;&amp; pnpm --filter @cowards/spec exec vitest run src/integrity-authority.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both artifacts are committed, deterministic, parseable, and covered by stale-artifact tests.
    - No environment-dependent or restricted value enters either artifact.
    - The vectors are directly consumable by Go without reimplementing TypeScript serialization.
  </acceptance_criteria>
  <done>Canonical authority and tuple identity have deterministic committed artifacts and cross-language proof inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated manifest to runtime consumers | Committed ownership and tuple identity controls every downstream acceptance decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-01 | Tampering | authority registry and tuple artifacts | high | mitigate | Exact hash verification, immutable records, deterministic stale-artifact checks, and mutation tests. |
| T-256-SC | Tampering | package supply chain | low | accept | Research found no package installation; this plan uses existing dependencies and platform cryptography only. |
</threat_model>

<verification>
Run the focused authority suite and generator check. Confirm `git diff -- packages/spec/artifacts` is empty after a second `--write`, proving deterministic output.
</verification>

<success_criteria>
- One spec-owned owner registry covers every canonical authority domain.
- Exact tuple ID/expansion equality is executable and language-neutral.
- Executable evidence identity is not collapsed into semantic compatibility identity.
- Generated artifacts are deterministic and stale output fails closed.
</success_criteria>

<output>
Create `.planning/phases/256-counted-safety-and-canonical-authority/256-01-SUMMARY.md` when done.
</output>
