---
phase: 256-counted-safety-and-canonical-authority
plan: "07"
type: execute
wave: 6
depends_on: ["256-02", "256-04", "256-15"]
files_modified:
  - packages/persistence/src/integrity-evidence.ts
  - packages/persistence/src/integrity-evidence.test.ts
  - packages/persistence/src/governance.ts
  - packages/persistence/src/standings-recompute.ts
  - packages/persistence/src/standings-recompute.test.ts
autonomous: true
requirements: [SAFE-03, SAFE-04, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Historical identity resolves read-only as resolved-historical, legacy-incomplete, or unresolved without changing original Match evidence or counted meaning."
    - "Current MatchSet evidence adds standings points only after exact tuple-registry resolution and certified evidence for every entrant; missing, unknown, mixed, or uncertified current identity is excluded."
    - "Only explicitly resolved historical v1.4 records remain eligible under their original semantics; legacy-incomplete and unresolved history never enters new recomputation."
    - "Operators preview deterministic cohorts before append-only classification/invalidation and rollback only through compensation plus recomputation."
  artifacts:
    - path: "packages/persistence/src/integrity-evidence.ts"
      provides: "Legacy resolver and deterministic cohort preview/apply/compensate fold"
    - path: "packages/persistence/src/governance.ts"
      provides: "Authorized append-only governance service"
  key_links:
    - from: "packages/persistence/src/integrity-evidence.ts"
      to: "packages/persistence/src/standings-recompute.ts"
      via: "effective classifications feed a pure recomputation gate that distinguishes exact certified current evidence from explicitly resolved historical v1.4 evidence"
      pattern: "recomputeSeasonStandings|resolved_historical|certified"
---

<objective>
Implement historical resolution and correction governance separately from active storage and scheduling writers.

Purpose: D-06 and D-09 through D-12 preserve original v1.4 evidence while enabling evidence-backed correction. AUTH-03 additionally requires current standings input to resolve one exact registered tuple and a certified evidence set for every entrant. Output: read-only resolution, exact current/historical recomputation gates, deterministic cohort governance, compensation, and standings proof.
</objective>

## Artifacts this phase produces

- A read-only legacy resolver that returns `resolved_historical`, `legacy_incomplete`, or `unresolved` without changing source rows.
- A standings input/gate that admits current evidence only after exact tuple-registry resolution and certified per-entrant evidence, while admitting explicitly resolved v1.4 history only under its original semantics.
- Deterministic cohort preview/apply/compensate services whose folded effective classification feeds recomputation.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-RESEARCH.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-04-SUMMARY.md
@packages/persistence/src/governance.ts
@packages/persistence/src/standings-recompute.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve historical compatibility without writes</name>
  <read_first>
    - packages/persistence/src/integrity-evidence.ts
    - packages/persistence/src/integrity-evidence.test.ts
    - packages/spec/artifacts/v1.37-integrity-authority.json
    - CowardsGameSpec_CycleInterleaved_v1.4.md
  </read_first>
  <files>packages/persistence/src/integrity-evidence.ts, packages/persistence/src/integrity-evidence.test.ts</files>
  <behavior>
    - Resolution uses only authoritative persisted fields plus immutable release manifests and returns resolved-historical, legacy-incomplete, or unresolved (D-06).
    - Original outcome and counted meaning remain distinct from containment/conformance evidence status (D-09).
    - Unresolved history cannot support counted recomputation or be upgraded to a current tuple (AUTH-03, AUTH-04).
  </behavior>
  <action>Implement a pure resolver and historical projection. Hash source rows before and after all reads to prove non-mutation. Preserve original rules/Chronicle profile and counted label, add a calm legacy note only when useful, and emit a prominent warning only for a concrete governance finding, per D-16.</action>
  <verify><automated>pnpm --filter @cowards/persistence exec vitest run src/integrity-evidence.test.ts -t "historical|legacy|unresolved"</automated></verify>
  <acceptance_criteria>
    - Resolution has no write query path.
    - Historical source hashes remain identical.
    - No partial historical identity becomes current certification.
  </acceptance_criteria>
  <done>Historical evidence is truthful, explicit, and immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append, compensate, and recompute deterministic cohort findings</name>
  <read_first>
    - packages/persistence/src/governance.ts
    - packages/persistence/src/standings-recompute.ts
    - packages/persistence/src/integrity-evidence.ts
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>packages/persistence/src/integrity-evidence.ts, packages/persistence/src/integrity-evidence.test.ts, packages/persistence/src/governance.ts, packages/persistence/src/standings-recompute.ts, packages/persistence/src/standings-recompute.test.ts</files>
  <behavior>
    - Invalidation requires exact reproducible Match/cohort evidence; newer documentation standards alone reject (D-10).
    - Apply locks and re-evaluates the deterministic predicate/preview hash, appends a classification event, and recomputes standings (D-11).
    - Rollback appends compensation with reason/evidence and recomputes; original events/evidence are never deleted or rewritten (D-12).
    - A current MatchSet contributes points only when its tuple ID and six-field expansion resolve atomically in the canonical registry and every entrant has exact certified evidence for that tuple; missing, unknown, mixed, or uncertified current tuples remain visible but excluded (AUTH-03).
    - An explicitly `resolved_historical` v1.4 record may contribute only under its persisted original counted semantics; `legacy_incomplete` and `unresolved` records are visible but cannot enter the new recomputation (D-06, D-09, AUTH-04).
    - Recompute and historical/current gating never update the original Match, MatchSet, Chronicle, entrant, score, or evidence rows.
  </behavior>
  <action>Define a versioned predicate AST, stable ordering/encoding, preview count/hash/sample IDs, evidence refs, actor/reason/action, and monotonic sequence. Re-evaluate under one transaction, reject preview drift, and fold effective classifications. Extend `ClassifiedSeasonMatchSet` (or a dedicated validated input created before it reaches `recomputeSeasonStandings`) with a discriminated integrity-resolution result: current evidence must carry an exact canonical tuple resolution plus a certified evidence entry for every entrant, while historical evidence must carry an explicit `resolved_historical` v1.4 resolution and its persisted original counted semantics. Make the pure recomputer add score only after this gate; missing, unknown, mixed, uncertified, legacy-incomplete, and unresolved inputs increment excluded evidence rather than points. Preserve the existing deterministic sort/tie-break behavior. Add focused tests for exact certified current input, each current failure class, resolved historical v1.4 counted/non-counted input, legacy-incomplete/unresolved history, governance classification/compensation, and before/after source-row hashes proving no original-row mutation. Replace mutable status-as-authority paths for integrity correction.</action>
  <verify><automated>pnpm --filter @cowards/persistence exec vitest run src/integrity-evidence.test.ts src/governance.test.ts src/standings-recompute.test.ts</automated></verify>
  <acceptance_criteria>
    - Apply/compensate are deterministic and append-only.
    - Preview drift and documentation-only invalidation fail closed.
    - Current points require exact registered tuple resolution plus certified evidence for every entrant.
    - Only resolved historical v1.4 evidence can retain original-semantic eligibility; incomplete/unresolved history adds no points.
    - Standings derive from effective events, never direct points repair.
    - Focused unit and PostgreSQL-backed immutability tests prove recomputation never changes original source rows.
  </acceptance_criteria>
  <done>Historical corrections are evidence-backed, reversible, and non-mutating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Governance predicate to historical evidence/standings | Overbroad or drifting cohorts could rewrite competitive meaning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-08 | Tampering/Repudiation | cohort governance | high | mitigate | Deterministic preview hashes, transaction recheck, append-only sequence, compensation, and recomputation. |
| T-256-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>

<verification>Run focused governance/standings tests for exact certified current evidence, every current tuple/certificate rejection, resolved/incomplete/unresolved history, compensation, and source-row immutability hashes.</verification>
<success_criteria>Current points require exact registered and certified identity; only explicitly resolved v1.4 history retains original-semantic eligibility; original rows stay immutable while corrections remain deterministic, append-only, and compensatable.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-07-SUMMARY.md` when done.</output>
