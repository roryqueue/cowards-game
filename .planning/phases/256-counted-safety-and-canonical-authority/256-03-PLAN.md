---
phase: 256-counted-safety-and-canonical-authority
plan: "03"
type: execute
wave: 3
depends_on: ["256-01", "256-02"]
files_modified:
  - packages/spec/src/runtime-execution-service.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/spec.test.ts
  - packages/spec/src/match-execution-contract.ts
  - packages/spec/src/match-execution-contract.test.ts
  - packages/replay/src/validate.ts
  - packages/replay/src/validate.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Every new execution request and evidence snapshot carries one semantic tuple plus an ordered bottom/top pair of exact per-entrant lane and certificate references."
    - "Missing, unknown, mixed, partial, or uncertified identity data is rejected as one atomic contract failure."
    - "An execution-time evidence drift is representable only as a system failure and cannot be encoded as gameplay or player fault."
  artifacts:
    - path: "packages/spec/src/runtime-execution-service.ts"
      provides: "Execution request/result contract carrying one semantic tuple and ordered bottom/top entrant evidence references"
    - path: "packages/spec/src/schemas.ts"
      provides: "Strict Zod schemas for the exact execution identity envelope"
    - path: "packages/spec/src/match-execution-contract.ts"
      provides: "Persistence/public evidence snapshot contract with tuple expansion and safe eligibility projection"
    - path: "packages/replay/src/validate.ts"
      provides: "Atomic tuple validation for current replay while preserving explicit historical dispatch"
  key_links:
    - from: "packages/spec/src/schemas.ts"
      to: "packages/spec/src/integrity-authority.ts"
      via: "request schema validates tuple ID and full expansion as one object"
      pattern: "compatibilityTuple|tupleId"
    - from: "packages/spec/src/runtime-execution-service.ts"
      to: "packages/spec/src/runtime-evidence.ts"
      via: "request identity and system-failure reason types reuse canonical evidence contracts"
      pattern: "ExecutableLaneIdentity|Evidence"
---

<objective>
Carry the exact compatibility tuple and evidence decision snapshot through shared execution and Match evidence contracts.

Purpose: D-04 and D-05 require checks at scheduling and execution boundaries, with in-flight drift ending as a system failure and no gameplay mutation. AUTH-02 and AUTH-03 require atomic tuple persistence and rejection.
Output: Strict language-neutral request/response and Match evidence schemas with focused contract regressions.
</objective>

## Artifacts this phase produces

- `RuntimeExecutionCompatibilityIdentity`, `RuntimeEntrantExecutionEvidence`, `RuntimeExecutionEvidencePair`, and typed evidence-drift system-failure codes in `runtime-execution-service.ts`.
- Strict `RuntimeExecutionCompatibilityIdentitySchema` and `RuntimeExecutionEvidenceSnapshotSchema` composed into `RuntimeExecutionServiceRequestSchema`.
- Match/evidence DTO fields for tuple ID plus expansion, exact lane identity, certificate IDs/hashes, registry generation, decision status/reason, and evaluation/freshness instants.
- Contract fixtures proving missing/mixed/unknown/uncertified identity rejects atomically.
- Replay validation that rejects current missing/mixed tuples while retaining explicit original-semantics dispatch for immutable historical Chronicles.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-RESEARCH.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-02-SUMMARY.md
@packages/spec/src/integrity-authority.ts
@packages/spec/src/runtime-evidence.ts
@packages/spec/src/runtime-execution-service.ts
@packages/spec/src/schemas.ts
@packages/spec/src/match-execution-contract.ts

<interfaces>
`RuntimeExecutionServiceRequest` is the shared transport shape and `RuntimeExecutionServiceRequestSchema` is its strict runtime parser. `MatchExecutionRuntimeEvidenceV1Schema` and its constructor are the existing Match/result evidence seam. Extend these owners rather than adding consumer-local identity objects.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add atomic compatibility and evidence identity to runtime execution requests</name>
  <read_first>
    - packages/spec/src/runtime-execution-service.ts
    - packages/spec/src/schemas.ts
    - packages/spec/src/spec.test.ts
    - packages/spec/src/integrity-authority.ts
    - packages/spec/src/runtime-evidence.ts
  </read_first>
  <files>packages/spec/src/runtime-execution-service.ts, packages/spec/src/schemas.ts, packages/spec/src/spec.test.ts</files>
  <behavior>
    - A complete request contains one tuple ID and exact six-field expansion plus an ordered `bottom`/`top` pair; each side has its own entrant key, Strategy Revision ID, registry generation, full executable lane identity, containment/conformance references and hashes, and scheduling decision snapshot (D-04, D-05).
    - The bottom/top pair may contain different supported languages, providers, runtimes, toolchains, adapters, artifacts, and certificate identities while both sides retain the same semantic tuple (D-05, D-08).
    - Omitting any identity member, mixing a tuple expansion, using an alias/range, or sending an unknown/uncertified tuple fails the full request parse (D-07, AUTH-03).
    - Identity/evidence drift has a typed redacted system-failure code and cannot use a player-violation or success envelope (D-04).
  </behavior>
  <action>Write failing schema cases first, then extend the shared TypeScript interface and strict Zod request parser. Reuse canonical tuple/evidence types and literal reason codes rather than duplicating them. Represent executable proof as a required ordered `bottom`/`top` pair keyed to the corresponding Strategy Revision, never as one Match-wide lane. Make unknown keys reject at the identity boundary. Add explicit system-failure codes for stale, revoked, mismatched, unverifiable, or registry-drift evidence while preserving the existing redacted outer failure contract. Keep gameplay and Strategy payload fields unchanged.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec exec vitest run src/spec.test.ts -t "RuntimeExecutionServiceRequestSchema|execution evidence identity"</automated>
  </verify>
  <acceptance_criteria>
    - Complete exact identity parses and round-trips without dropping expanded tuple fields.
    - Every partial/mixed/unknown/alias input rejects before execution.
    - Evidence drift is represented only as system failure.
  </acceptance_criteria>
  <done>The shared runtime request contract carries and atomically validates exact tuple and executable evidence identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist the semantic tuple and ordered entrant pair in Match evidence</name>
  <read_first>
    - packages/spec/src/match-execution-contract.ts
    - packages/spec/src/match-execution-contract.test.ts
    - packages/spec/src/runtime-execution-service.ts
    - packages/spec/src/runtime-evidence.ts
  </read_first>
  <files>packages/spec/src/match-execution-contract.ts, packages/spec/src/match-execution-contract.test.ts</files>
  <behavior>
    - New Match/evidence fixtures carry both tuple ID and exact expansion, and the constructor verifies they agree (D-05, AUTH-02).
    - Runtime evidence captures the ordered bottom/top entrant keys, Strategy Revision IDs, exact lane/certificate hashes, authority-bundle hash/generation, and per-side decision snapshots without including restricted bytes or raw diagnostics.
    - Fixtures with partial identity, independently valid but uncertified tuple members, or inconsistent tuple hash reject as a whole (AUTH-03).
  </behavior>
  <action>Extend the canonical Match execution evidence schema and constructors with the same semantic tuple plus ordered bottom/top executable-evidence pair used by the runtime request. Prove side order and Strategy Revision binding, and reject a swapped pair even when both individual lanes are valid. Keep safe public evidence fields separate from restricted operator fields. Update current fixture constructors rather than hardcoding identity fields repeatedly. Ensure old historical fixture parsing remains explicit and version-routed; do not rewrite historical fixture bytes or infer missing values. Add exact positive, heterogeneous-language, swap, and per-side mutation tests.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec exec vitest run src/match-execution-contract.test.ts src/spec.test.ts -t "execution|compatibility|evidence"</automated>
  </verify>
  <acceptance_criteria>
    - Every newly constructed Match evidence record has an exact tuple and evidence snapshot.
    - Historical fixture compatibility is explicit and non-mutating.
    - No restricted source/artifact bytes, memory, objective, credential, host path, or raw diagnostic enters the public evidence schema.
  </acceptance_criteria>
  <done>Execution and persisted evidence contracts share one semantic tuple and the same exact, privacy-safe ordered entrant pair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make replay reject mixed current tuples and preserve explicit historical dispatch</name>
  <read_first>
    - packages/replay/src/validate.ts
    - packages/replay/src/validate.test.ts
    - packages/spec/src/integrity-authority.ts
    - packages/spec/src/match-execution-contract.ts
    - CowardsGameSpec_CycleInterleaved_v1.4.md
  </read_first>
  <files>packages/replay/src/validate.ts, packages/replay/src/validate.test.ts</files>
  <behavior>
    - Current replay input requires a registered tuple ID whose complete expansion re-hashes exactly; independently-known or mixed fields reject atomically (AUTH-03).
    - Historical v1.4 Chronicles remain byte-immutable and route through their original explicit Chronicle/rules compatibility branch without tuple backfill (D-06, AUTH-04).
    - Validation never upgrades unresolved historical evidence into a current certified tuple.
  </behavior>
  <action>Add focused failing tests, then call the spec-owned exact tuple resolver from current Chronicle/replay compatibility validation. Replace independent current version-member acceptance with one atomic registered tuple check. Preserve the existing historical schema/version path as read-only original-semantics dispatch and return an explicit unresolved/legacy result where identity cannot be proved; never mutate or synthesize fields on the source Chronicle. Do not implement Phase-259 per-slot Chronicle semantics here.</action>
  <verify>
    <automated>pnpm --filter @cowards/replay exec vitest run src/validate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Missing/unknown/mixed current tuple fixtures return typed compatibility errors.
    - Immutable historical fixtures still validate only through their original version route and remain byte-identical.
    - No current tuple is inferred from partial historical fields.
  </acceptance_criteria>
  <done>Replay participates in atomic tuple rejection while preserving immutable historical semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Scheduler/Go to runtime-service | Untrusted or stale request identity must be rejected before hostile code executes. |
| Runtime evidence to persisted/public Match contract | Identity proof must remain exact without leaking restricted material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-04 | Tampering | execution identity envelope | high | mitigate | Strict atomic schemas, tuple re-hash, exact identity comparison, and typed system-failure-only drift. |
| T-256-03 | Information Disclosure | Match execution evidence | high | mitigate | Separate safe fields, shared privacy assertions, and no restricted byte/path/diagnostic fields. |
| T-256-SC | Tampering | package supply chain | low | accept | No package changes are required. |
</threat_model>

<verification>
Run focused spec contract tests and `pnpm --filter @cowards/spec typecheck`. Confirm every new request fixture contains the exact tuple expansion and evidence snapshot.
</verification>

<success_criteria>
- Runtime requests and Match evidence use the same atomic compatibility identity.
- Partial or mixed identity never reaches execution.
- Evidence drift is typed as system failure only.
- Historical fixture compatibility remains explicit and immutable.
</success_criteria>

<output>
Create `.planning/phases/256-counted-safety-and-canonical-authority/256-03-SUMMARY.md` when done.
</output>
