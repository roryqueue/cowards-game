---
phase: 10-runtime-isolation-hardening
plan: 04
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - apps/worker/src/runtime-config.ts
  - apps/worker/src/runner.ts
  - apps/worker/src/runner.test.ts
  - apps/worker/src/index.ts
autonomous: true
requirements: [ISO-01, ISO-02, ISO-07]
must_haves:
  truths:
    - "Worker startup identifies the active Strategy execution adapter."
    - "Worker can opt into subprocess adapter by configuration while worker-thread remains default."
    - "Worker records infrastructure failures as system attempt failures, not gameplay Chronicles."
  artifacts:
    - path: "apps/worker/src/runtime-config.ts"
      provides: "Worker adapter selection and visibility"
    - path: "apps/worker/src/runner.ts"
      provides: "Adapter-aware runtime construction and system failure propagation"
    - path: "apps/worker/src/runner.test.ts"
      provides: "Worker propagation tests"
  key_links:
    - from: "apps/worker/src/runtime-config.ts"
      to: "@cowards/runtime-js/worker"
      via: "adapter factory import"
      pattern: "create.*StrategyExecutionAdapter"
    - from: "apps/worker/src/runner.ts"
      to: "recordAttemptFailure"
      via: "system failure catch path"
      pattern: "recordAttemptFailure"
---

<objective>
Wire worker-side adapter selection, active-adapter visibility, and strategy/system propagation tests.

Purpose: ISO-01, ISO-02, ISO-07, D-03, D-04, D-09, and D-10 require configurability and visibility without changing default behavior or allowing infrastructure failures into gameplay semantics.
Output: Worker runtime config, startup logs, adapter-aware Match input loading, and worker tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-runtime-isolation-hardening/10-CONTEXT.md
@.planning/phases/10-runtime-isolation-hardening/10-RESEARCH.md
@.planning/phases/10-runtime-isolation-hardening/10-PATTERNS.md
@apps/worker/src/runner.ts
@apps/worker/src/runner.test.ts
@apps/worker/src/index.ts
@packages/runtime-js/src/worker.ts

<interfaces>
Current worker runtime creation:
```typescript
runtime: createSideDispatchRuntime(
  createRuntimeFromRevision(bottomRevision),
  createRuntimeFromRevision(topRevision),
  {
    bottomPlayerId: String(match.bottom_player_id),
    topPlayerId: String(match.top_player_id),
  },
)
```

Current system failure handling:
```typescript
await dependencies.recordAttemptFailure(pool, {
  jobId: claimed.jobId,
  leaseToken: claimed.leaseToken,
  errorClass: error instanceof Error ? error.name : "UnknownError",
  errorMessage: error instanceof Error ? error.message : String(error),
  retryable: true,
  details: { workerId: options.workerId, matchId: claimed.matchId },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add worker runtime adapter config</name>
  <files>apps/worker/src/runtime-config.ts, apps/worker/src/runner.test.ts</files>
  <behavior>
    - Test 1: no environment/config input resolves to worker-thread adapter.
    - Test 2: explicit subprocess config resolves to subprocess adapter.
    - Test 3: unknown adapter id throws a system configuration error before any Strategy source executes.
    - Test 4: resolved config exposes id, label, and isolation boundary description.
  </behavior>
  <action>Create `runtime-config.ts` with `createWorkerRuntimeConfig(input?: { strategyExecutionAdapter?: string })`. Read an explicit id such as `worker-thread` or `subprocess`; default to `worker-thread` per D-03. Return `{ adapter, metadata }` for runner use. Do not read Strategy source in this module and do not make subprocess default per D-04.</action>
  <verify>
    <automated>pnpm --filter @cowards/worker test -- runner.test.ts</automated>
  </verify>
  <done>Worker config can select adapters explicitly and exposes active adapter metadata while defaulting to worker-thread.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use selected adapter in runner and logs</name>
  <files>apps/worker/src/runner.ts, apps/worker/src/index.ts, apps/worker/src/runner.test.ts</files>
  <behavior>
    - Test 1: `loadRunMatchInput` passes selected adapter into both bottom/top `createRuntimeFromRevision` calls.
    - Test 2: worker startup logs active adapter id and isolation boundary.
    - Test 3: default runner behavior remains compatible with existing tests.
  </behavior>
  <action>Extend runner options/dependencies so worker config supplies the adapter to `createRuntimeFromRevision`. Keep current call sites working by default. Update `index.ts` to read an env var such as `STRATEGY_EXECUTION_ADAPTER` and log active adapter id plus boundary text; do not log Strategy source or secrets. Preserve `@cowards/runtime-js/worker` as the only executable runtime import in worker code.</action>
  <verify>
    <automated>pnpm --filter @cowards/worker test -- runner.test.ts</automated>
    <automated>pnpm --filter @cowards/worker typecheck</automated>
  </verify>
  <done>Active adapter is configurable and visible to developers, with worker-thread default preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve gameplay violations versus system failures</name>
  <files>apps/worker/src/runner.ts, apps/worker/src/runner.test.ts</files>
  <behavior>
    - Test 1: Chronicle `RUNTIME_VIOLATION` events still complete the Match and do not call `recordAttemptFailure`.
    - Test 2: adapter system errors from malformed IPC record attempt failure with retryable system details.
    - Test 3: adapter system errors from subprocess exit/signal record attempt failure and can become `failed_system` after retry exhaustion.
    - Test 4: validation failure inside Strategy output remains gameplay violation when produced as `RuntimeResult`, not a system failure.
  </behavior>
  <action>Use the runtime package system-failure class/code from Plans 10-02/10-03 in worker catch-path details. Do not convert system failures to Chronicle events. Include adapter id and system failure code in `recordAttemptFailure.details` for diagnostics. Keep strategy/player-caused violations inside normal engine/Chronicle flow per D-10.</action>
  <verify>
    <automated>pnpm --filter @cowards/worker test -- runner.test.ts</automated>
    <automated>pnpm --filter @cowards/runtime-js test -- subprocess-adapter.test.ts hostile-matrix.test.ts</automated>
  </verify>
  <done>Worker tests prove strategy violations and system failures propagate through different persistence paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| worker config -> runtime adapter | Environment/config chooses isolation boundary. |
| runtime adapter -> persistence job failure path | Infrastructure failures become retryable system records. |
| engine Chronicle -> persistence completion path | Player-caused violations become completed Matches with Chronicle events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-11 | S/T | adapter config | mitigate | Whitelist known adapter ids and fail closed on unknown values. |
| T-10-12 | R/I | worker diagnostics | mitigate | Log adapter metadata, never Strategy source/secrets; include adapter id in failure details. |
| T-10-13 | T/R | failure propagation | mitigate | Tests enforce distinct complete-vs-recordAttemptFailure paths. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/worker test -- runner.test.ts`, `pnpm --filter @cowards/worker typecheck`, and `pnpm --filter @cowards/runtime-js test -- subprocess-adapter.test.ts hostile-matrix.test.ts`.
</verification>

<success_criteria>
ISO-01, ISO-02, and ISO-07 are satisfied at worker level when active adapter metadata is visible, worker-thread remains default, subprocess is opt-in, and worker tests prove system failures are retried/failed outside gameplay semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/10-runtime-isolation-hardening/10-04-SUMMARY.md`
</output>
