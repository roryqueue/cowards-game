---
phase: 10-runtime-isolation-hardening
plan: 01
type: execute
status: complete
wave: 1
depends_on: []
files_modified:
  - packages/runtime-js/src/adapter.ts
  - packages/runtime-js/src/worker-thread-adapter.ts
  - packages/runtime-js/src/worker-bridge.ts
  - packages/runtime-js/src/executor.ts
  - packages/runtime-js/src/worker.ts
  - packages/runtime-js/src/adapter-contract.test.ts
  - packages/runtime-js/src/executor.test.ts
autonomous: true
requirements: [ISO-01, ISO-02, ISO-03, ISO-05]
must_haves:
  truths:
    - "Developer can inspect the active Strategy execution adapter name and isolation boundary."
    - "Worker-thread execution remains the default Strategy runtime behavior."
    - "Engine rules continue to depend only on StrategyRuntime, not adapter implementation details."
  artifacts:
    - path: "packages/runtime-js/src/adapter.ts"
      provides: "StrategyExecutionAdapter contract, metadata, config, and system error types"
    - path: "packages/runtime-js/src/worker-thread-adapter.ts"
      provides: "Default worker-thread adapter implementation"
    - path: "packages/runtime-js/src/executor.ts"
      provides: "StrategyRuntime wrapper built from a configurable adapter"
  key_links:
    - from: "packages/runtime-js/src/executor.ts"
      to: "packages/runtime-js/src/adapter.ts"
      via: "createRuntimeFromRevision options"
      pattern: "StrategyExecutionAdapter"
    - from: "packages/runtime-js/src/worker.ts"
      to: "packages/runtime-js/src/adapter.ts"
      via: "metadata exports"
      pattern: "activeStrategyExecutionAdapter"
---

<objective>
Define the runtime adapter boundary and keep the current worker-thread path as the default implementation.

Purpose: ISO-01, ISO-02, ISO-03, D-01, D-03, and D-04 require an explicit, visible, replaceable Strategy execution boundary without changing engine rules or moving Strategy execution into web/API processes.
Output: Adapter contract, worker-thread adapter extraction, executor wiring, and contract tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-runtime-isolation-hardening/10-CONTEXT.md
@.planning/phases/10-runtime-isolation-hardening/10-RESEARCH.md
@.planning/phases/10-runtime-isolation-hardening/10-PATTERNS.md
@packages/runtime-js/src/worker-bridge.ts
@packages/runtime-js/src/worker-harness.ts
@packages/runtime-js/src/executor.ts
@packages/runtime-js/src/guards.ts
@packages/runtime-js/src/worker.ts
@packages/runtime-js/src/executor.test.ts
@packages/engine/src/types.ts

<interfaces>
From `packages/engine/src/types.ts`:
```typescript
export type RuntimeResult<T> =
  | { ok: true; value: T }
  | { ok: false; violation: RuntimeViolation }

export interface StrategyRuntime {
  selectActivations(input: StrategyInput): RuntimeResult<StrategyResult>
  runSoldierBrain(input: SoldierBrainInput): RuntimeResult<SoldierBrainResult>
}
```

From `packages/runtime-js/src/worker-bridge.ts`:
```typescript
export const runStrategyMethodInWorker = (args: {
  source: string
  methodName: "selectActivations" | "soldierBrain"
  input: unknown
  timeoutMs?: number | undefined
}): RuntimeResult<unknown>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define adapter contract and metadata</name>
  <files>packages/runtime-js/src/adapter.ts, packages/runtime-js/src/adapter-contract.test.ts</files>
  <behavior>
    - Test 1: default adapter metadata has id `worker-thread`, label, isolation boundary text, and `default: true`.
    - Test 2: `StrategyExecutionAdapter` accepts `source`, `methodName`, `input`, and optional timeout/output cap options and returns `RuntimeResult&lt;unknown&gt;`.
    - Test 3: adapter metadata must not describe worker threads as a final sandbox.
  </behavior>
  <action>Create `packages/runtime-js/src/adapter.ts` with `StrategyMethodName`, `StrategyExecutionAdapterMetadata`, `StrategyExecutionAdapterOptions`, `StrategyExecutionRequest`, `StrategyExecutionAdapter`, and `activeStrategyExecutionAdapter`/`getStrategyExecutionAdapterMetadata` exports. Make metadata explicit that worker-thread is default compatibility containment per D-03/D-04, not final sandbox. Do not import from `packages/engine/src/*`; use package imports only.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- adapter-contract.test.ts</automated>
  </verify>
  <done>Adapter contract and metadata compile, tests prove visibility, and no engine files are modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract worker-thread adapter without behavior change</name>
  <files>packages/runtime-js/src/worker-thread-adapter.ts, packages/runtime-js/src/worker-bridge.ts, packages/runtime-js/src/adapter-contract.test.ts</files>
  <behavior>
    - Test 1: worker-thread adapter delegates valid calls to `runStrategyMethodInWorker` and returns the same valid result shape.
    - Test 2: timeout still maps to `TIMEOUT`.
    - Test 3: adapter metadata reports resource limits and empty env/execArgv behavior.
  </behavior>
  <action>Create `createWorkerThreadStrategyExecutionAdapter()` in `worker-thread-adapter.ts` that implements `StrategyExecutionAdapter` by delegating to `runStrategyMethodInWorker`. Keep `runStrategyMethodInWorker` behavior unchanged, including `env: {}`, `execArgv: []`, `resourceLimits`, and timeout handling per ISO-05. Use this extraction to satisfy D-01 while preserving D-03.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- adapter-contract.test.ts executor.test.ts</automated>
  </verify>
  <done>Default worker-thread adapter returns existing runtime outcomes and existing executor tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire executor to configurable adapter</name>
  <files>packages/runtime-js/src/executor.ts, packages/runtime-js/src/worker.ts, packages/runtime-js/src/executor.test.ts</files>
  <behavior>
    - Test 1: `createRuntimeFromRevision(revision)` uses worker-thread adapter by default.
    - Test 2: `createRuntimeFromRevision(revision, { adapter })` uses the supplied adapter.
    - Test 3: `@cowards/runtime-js/worker` exports active adapter metadata but the safe root entrypoint remains non-executing.
  </behavior>
  <action>Change `createRuntimeFromRevision` to accept optional `{ adapter?: StrategyExecutionAdapter; timeoutMs?: number }` while preserving current call sites. Route both `selectActivations` and `runSoldierBrain` through the adapter, then keep existing normalization in `executor.ts`. Export adapter metadata from `worker.ts` for ISO-01. Do not change `packages/engine` or any React/web files.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- executor.test.ts adapter-contract.test.ts</automated>
    <automated>pnpm --filter @cowards/runtime-js typecheck</automated>
  </verify>
  <done>Runtime behavior is unchanged by default, custom adapters can be injected, and active adapter metadata is visible from the worker subpath.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| worker process -> Strategy adapter | Host process sends untrusted Strategy source and inputs into executable runtime boundary. |
| adapter -> engine `StrategyRuntime` | Adapter results cross back into deterministic engine semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-01 | S/T/E | `StrategyExecutionAdapter` | mitigate | Contract keeps executable source behind worker runtime subpath and exposes metadata for active boundary. |
| T-10-02 | I | `worker-thread` metadata | mitigate | Metadata must state worker threads are compatibility containment, not final sandbox. |
| T-10-03 | T | `executor.ts` normalization | mitigate | Raw adapter output remains schema-normalized before engine receives `StrategyResult` or `SoldierBrainResult`. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/runtime-js test -- adapter-contract.test.ts executor.test.ts` and `pnpm --filter @cowards/runtime-js typecheck`.
</verification>

<success_criteria>
ISO-01, ISO-02, ISO-03, and the worker-thread portion of ISO-05 are satisfied when the adapter contract exists, the worker-thread adapter is default and visible, existing runtime behavior stays green, and no engine/web execution boundary is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/10-runtime-isolation-hardening/10-01-SUMMARY.md`
</output>
