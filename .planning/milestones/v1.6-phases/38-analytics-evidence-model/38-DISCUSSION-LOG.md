# Phase 38: Analytics Evidence Model - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-22
**Phase:** 38-Analytics Evidence Model
**Areas discussed:** Contract Ownership, Evidence Band Rules, Compatibility Keys, Replay Reference Shape

---

## Contract Ownership

| Option | Description | Selected |
| --- | --- | --- |
| Shared contracts in `packages/spec` | Analytics DTOs, schemas, evidence-band enums, replay-reference shapes, and compatibility metadata become first-class shared contracts. | ✓ |
| Persistence-owned DTOs in `packages/persistence` | Analytics summaries are service-layer DTOs generated by persistence and consumed by web. |  |
| Split model | Stable outward-facing types live in `spec`; query/aggregation helpers and intermediate records stay in persistence. |  |
| You decide | The agent locks the approach that best fits the current architecture. |  |

**User's choice:** Shared contracts in `packages/spec`.
**Notes:** User selected the strongest shared-contract posture.

| Option | Description | Selected |
| --- | --- | --- |
| DTO + Zod schemas for all outward analytics shapes | Define TypeScript types and runtime schemas for profiles, runs, matchup records, evidence bands, replay references, and export summaries. | ✓ |
| Types first, schemas only for public/export boundaries | Define all contracts, but runtime-validate only the riskiest boundaries. |  |
| Minimal enum/schema spine | Define only primitives and let services compose bigger DTOs. |  |
| You decide | The agent chooses contract depth. |  |

**User's choice:** DTO plus Zod schemas for all outward analytics shapes.
**Notes:** This supports privacy and cross-package validation.

| Option | Description | Selected |
| --- | --- | --- |
| Spec schemas validate every boundary crossing | Persistence, web/API, exports, and tests validate against shared schemas. | ✓ |
| Schemas define the contract; tests validate representative boundaries | Runtime code trusts typed construction most of the time. |  |
| Schemas mostly for tests and docs | Production code uses TypeScript types; schemas support tests/fixtures. |  |
| You decide | The agent chooses validation posture. |  |

**User's choice:** Spec schemas validate every boundary crossing.
**Notes:** This matches the project’s trust-contract posture.

| Option | Description | Selected |
| --- | --- | --- |
| Dedicated analytics module | Add `packages/spec/src/analytics.ts` plus schema exports and `index.ts` re-exports. | ✓ |
| Extend competition contracts | Put evidence types near existing competition contracts. |  |
| Split by domain | Keep analytics and replay-reference primitives in separate domain files. |  |
| You decide | The agent chooses organization. |  |

**User's choice:** Dedicated analytics module.
**Notes:** Avoids muddying competition/replay modules.

---

## Evidence Band Rules

| Option | Description | Selected |
| --- | --- | --- |
| Strict counted replay-backed threshold | Strong evidence requires counted, completed, replay-backed Matches and mirrored side coverage when applicable. | ✓ |
| Moderate counted threshold | Lower count threshold and side coverage is advisory. |  |
| Profile-relative threshold | Strong evidence depends on the profile’s intended size. |  |
| You decide | The agent chooses the safest rule. |  |

**User's choice:** Strict counted replay-backed threshold.
**Notes:** Protects players from overreading small samples.

| Option | Description | Selected |
| --- | --- | --- |
| Degraded/non-counted overrides strength | A degraded/non-counted band prevents calling evidence strong. | ✓ |
| Mixed band with counted subset | Strong counted evidence can show degraded warnings. |  |
| Exclude degraded/non-counted from banding | Degraded/non-counted appears only in details. |  |
| You decide | The agent chooses the framing. |  |

**User's choice:** Degraded/non-counted is its own overriding band.
**Notes:** Reliability issues should be visible at the top level.

| Option | Description | Selected |
| --- | --- | --- |
| System-failed overrides everything | Platform failure becomes the top band. | ✓ |
| Overrides only when conclusion is affected | System failures are top-level only when numerous or decisive. |  |
| Always non-counted detail | System failures appear in details unless all evidence failed. |  |
| You decide | The agent chooses platform-failure framing. |  |

**User's choice:** System-failed overrides everything.
**Notes:** Separates platform failure from Strategy quality.

| Option | Description | Selected |
| --- | --- | --- |
| Any counted replay-backed evidence below threshold | Thin evidence is any counted replay-backed evidence below strong. | ✓ |
| Tiered thin evidence | Split thin internally or visually. |  |
| Thin only after minimum small sample | One-offs remain insufficient. |  |
| You decide | The agent chooses the simplest honest rule. |  |

**User's choice:** Any counted, completed, replay-backed evidence below the strong threshold.
**Notes:** Details should show actual count.

| Option | Description | Selected |
| --- | --- | --- |
| Lock fixed threshold now | Pick exact N in discussion. |  |
| Define threshold constants with named defaults | Create named constants and tests around defaults. | ✓ |
| Define only classification mechanism | Later phases choose values. |  |
| You decide | The agent chooses specificity level. |  |

**User's choice:** Define threshold constants with named defaults.
**Notes:** Concrete enough for tests while still easy to tune during planning.

---

## Compatibility Keys

| Option | Description | Selected |
| --- | --- | --- |
| Strict deterministic identity | Candidates, opponents, preset, seeds/order, mirror policy, scoring, versions, runtime adapter/version, and matrix order block comparison. | ✓ |
| Gameplay identity only | Runtime and matrix ordering differences are allowed or warned. |  |
| Result-shape identity | Only broad result shape fields block comparison. |  |
| You decide | The agent chooses strictness. |  |

**User's choice:** Strict deterministic identity.
**Notes:** Deterministic truth wins over convenience.

| Option | Description | Selected |
| --- | --- | --- |
| Display metadata never affects compatibility | Names, descriptions, labels, notes, and annotations can change freely. | ✓ |
| Opponent display metadata affects compatibility | Opponent labels/archetype tags block comparison. |  |
| All captured metadata affects compatibility | Any display text change changes the key. |  |
| You decide | The agent chooses metadata rule. |  |

**User's choice:** Display metadata never affects compatibility.
**Notes:** Compatibility relies on stable ids, hashes, versions, policies, seeds, and expansion order.

| Option | Description | Selected |
| --- | --- | --- |
| Tags do not affect compatibility, but snapshots are stored | Tags support interpretation and audit without blocking comparison. | ✓ |
| Primary archetype affects compatibility | Primary tag changes block comparison. |  |
| All archetype tags affect compatibility | Any tag change blocks comparison. |  |
| You decide | The agent chooses tag behavior. |  |

**User's choice:** Tags do not affect compatibility, but snapshots are stored.
**Notes:** Evidence comparison is about deterministic play inputs.

| Option | Description | Selected |
| --- | --- | --- |
| Structured mismatch reasons | Define typed reason codes for exact incompatibility causes. | ✓ |
| Human-readable messages only | Return display text explanations. |  |
| Boolean plus opaque details | Return compatible false and optional debug details. |  |
| You decide | The agent chooses mismatch format. |  |

**User's choice:** Structured mismatch reasons.
**Notes:** Supports UI, tests, and explainability.

| Option | Description | Selected |
| --- | --- | --- |
| Component fields plus canonical hash | Store component fields and deterministic compatibility hash. | ✓ |
| Component fields only | Compare fields directly every time. |  |
| Hash only | Compare hashes; generate reasons only when profiles are available. |  |
| You decide | The agent chooses storage shape. |  |

**User's choice:** Component fields plus canonical compatibility hash.
**Notes:** The user also approved the agent locking follow-ons that strongly align with prior decisions.

---

## Replay Reference Shape

| Option | Description | Selected |
| --- | --- | --- |
| Compact public moment reference | Include `matchId`, `momentType`, `sequence`, `label`, `side`, and fallback state. | ✓ |
| Context-rich public moment reference | Include opponent/archetype/profile/evidence context inside every replay reference. |  |
| Reference id only | Include a replay reference id and resolve details server-side. |  |
| You decide | The agent chooses the shape. |  |

**User's choice:** Compact public moment reference.
**Notes:** Surrounding records carry opponent/archetype/profile context.

| Option | Description | Selected |
| --- | --- | --- |
| Fixed enum for v1.6 moment types | Define `BACKSTAB`, `CONTRACTION`, `NO_ADVANCE_CLEANUP`, `FALL`, `DECISIVE_PUSH`, `LATE_CYCLE_STABILIZATION`. | ✓ |
| Open string moment type | Allow arbitrary strings for future moments. |  |
| Event-type driven only | Use Chronicle event types directly. |  |
| You decide | The agent chooses vocabulary. |  |

**User's choice:** Fixed enum for v1.6 moment types.
**Notes:** Derived moments need more than raw Chronicle event types.

| Option | Description | Selected |
| --- | --- | --- |
| Explicit fallback state in the reference | Include typed state such as `TARGET_AVAILABLE` or `MATCH_REPLAY_UNAVAILABLE`. | ✓ |
| Optional sequence only | Omit sequence and fall back to Match start when unavailable. |  |
| Generate references only when valid | Do not include references unless the target can open. |  |
| You decide | The agent chooses explainability shape. |  |

**User's choice:** Explicit fallback state.
**Notes:** The agent also locked implied follow-ons: public projection source, no duplicated context, sequence as v1.6 target, and explained Match-start degradation.

## the agent's Discretion

- The user approved the agent locking follow-on recommendations when they are strongly implied by prior locked decisions.

## Deferred Ideas

None.
