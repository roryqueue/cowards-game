---
plan_id: 73-01
phase: 73-boundary-enforcement-and-source-free-type-cleanup
status: complete
type: implementation
files_modified:
  - apps/web/app/workshop/types.ts
  - scripts/check-service-boundary-imports.ts
  - scripts/check-boundary-monitors.ts
---

# Phase 73 Plan 01: Enforce Migrated Workshop Reads

## Objective

Promote the migrated Workshop read routes and safe dependency closure to strict enforcement, remove real report-only fingerprints, and prove `report_only_offenses` drops below 30.

## Tasks

1. Add the migrated Workshop test-summary and analytics-compare routes plus safe helpers to strict import enforcement.
2. Keep adapter allowlists limited to the Workshop read service adapter.
3. Remove direct persistence type imports from `apps/web/app/workshop/types.ts` using local web-owned types and spec-owned DTOs.
4. Update the known report-only baseline after fresh boundary output proves exact fingerprint removal.
5. Record final boundary evidence and rollback notes.

## Acceptance Criteria

- `pnpm boundary:imports` reports `strict_offenses=0 report_only_offenses<30`.
- Migrated route/helper closure has no direct persistence/runtime/worker/migration imports except the narrow allowed adapter.
- No source-bearing Workshop type is promoted into public/service output.
- No game rules, Chronicle reconstruction, scoring, or runtime behavior moves into React, routes, DTO mappers, or Go.

