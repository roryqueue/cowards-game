# 85-01 Plan: Cut Over Auth, Session, and Account Reads

## Goal

Move auth/session read and mutation plus account revision metadata list to Go-selected ownership with token-safe evidence.

## Tasks

1. Add Go session token generation/hash/verify, password verification, signup/signin/signout/session handlers, and account list provider.
2. Add web proxy/adapters for selected Go auth/session/account list routes.
3. Preserve `cowards_session` cookie semantics and clear-cookie behavior.
4. Add tests for valid, missing, expired, revoked, malformed, wrong password, duplicate signup, storage unavailable, and privacy failure cases.
5. Update ownership manifest/evidence.

## Acceptance

- No token/cookie/hash/session details appear in JSON, monitor, topology, or evidence output.
- Selected-Go auth failures fail closed without TypeScript fallback.

