# 87-01 Plan: Cut Over Exhibition Creation

## Goal

Move exhibition MatchSet creation to Go-selected ownership while TypeScript worker/runtime remains explicit execution owner.

## Tasks

1. Add Go transaction for exhibition creation with preset/revision/ownership/eligibility/rate-limit/duplicate checks.
2. Insert MatchSet, competition entrants, Matches, queued jobs, match links, revision locks, provenance, and submission audit event atomically.
3. Add web selected-owner proxy for `/api/exhibitions`.
4. Add tests for success, invalid preset/revisions, unauthorized, invalid runtime, duplicate active exhibition, rate limit, transaction rollback, and no source execution.
5. Update manifest/evidence and worker-owned route statuses.

## Acceptance

- Go-created jobs are claimable by existing TypeScript worker schema.
- Go does not execute Strategies or own worker lifecycle.
- Selected-Go failures fail closed without TypeScript fallback.

