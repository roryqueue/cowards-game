# 83-01 Plan: Add Live Go DTO Foundation

## Goal

Allow Go to run against live PostgreSQL and assemble selected service DTOs with schema-adjacent shape checks, privacy scans, and sanitized errors.

## Tasks

1. Add Go PostgreSQL dependency and live server construction.
2. Add explicit data mode resolution; fixture mode remains default for existing tests, live mode requires `DATABASE_URL`.
3. Add route-specific live provider scaffolding and safe JSON response helpers.
4. Add canonical public-safe service error helpers.
5. Add Go tests for mode selection, sanitized startup failures, privacy scanner coverage, and no fixture-backed promotion claim.
6. Update docs/evidence and route manifest status.

## Acceptance

- `go test ./...` passes in fixture mode.
- Live mode fails closed without `DATABASE_URL` and does not print DSNs.
- Live responses are passed through privacy validation before being written.

