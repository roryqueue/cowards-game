# 84-01 Plan: Cut Over Public Reads

## Goal

Serve selected public reads from Go live data in selected v1.13 topology while retaining TypeScript as explicit rollback/reference.

## Tasks

1. Add Go handlers/providers for public Strategy, player, ladder, MatchSet summary, and replay metadata.
2. Extend web Go read client to parse all public read DTO schemas and service errors.
3. Replace single Strategy flag with route-family selected owner resolution.
4. Add tests for success, not found, invalid JSON, privacy failure, schema failure, and stopped-Go no-fallback behavior.
5. Update ownership manifest and topology/monitor evidence.

## Acceptance

- Selected public reads call Go when configured.
- Go unavailable or invalid output throws public-safe selected-Go errors without TypeScript fallback.
- Public DTOs exclude source, memory, objective payloads, owner debug, raw replay internals, and host/DB/runtime internals.

