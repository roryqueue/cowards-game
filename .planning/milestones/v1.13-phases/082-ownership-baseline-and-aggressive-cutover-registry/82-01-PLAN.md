# 82-01 Plan: Capture v1.13 Ownership Baseline

## Goal

Produce the canonical v1.13 ownership registry, readable matrix, baseline evidence, and monitor validation that downstream phases update.

## Tasks

1. Add v1.13 route ownership manifest covering public reads, session/account reads, account source/write/fork, exhibition creation, worker-owned runtime surfaces, and deferred surfaces.
2. Add human-readable ownership matrix generated from the same facts.
3. Add baseline evidence for v1.12 `promote-none-yet`, Go fixture inventory, strict/report-only import counts, and selected v1.13 scope.
4. Extend boundary monitors to validate manifest schema, states, fallback policies, rollback owners, disallowed scopes, and selected route coverage.
5. Run `pnpm boundary:imports`, `pnpm exec tsx scripts/check-boundary-monitors.ts`, and `git diff --check`.

## Acceptance

- Manifest is canonical and monitor-readable.
- Matrix matches manifest route families and states.
- Evidence records the starting fixture-backed Go limitation.
- Monitor fails on invalid v1.13 route ownership state or missing selected route.

