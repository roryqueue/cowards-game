# 86-01 Plan: Cut Over Account Source and Writes

## Goal

Move owner-private source retrieval, Strategy Revision save/create, Starter fork, and Advanced fork to Go-selected ownership without Strategy execution.

## Tasks

1. Add Go owner-private source route and web proxy preserving text/no-store semantics.
2. Add Go source save/create route with hash/bytes/runtime/engine/validation metadata.
3. Add Go Starter and Advanced fork routes with lineage metadata.
4. Add selected-owner web routes and no-fallback failure handling.
5. Add tests for owner-only source, invalid source, invalid fork id, duplicate/storage/schema/privacy failures, and no source in public evidence.

## Acceptance

- Go never executes Strategy source or invokes Node `vm`.
- Source appears only in authenticated owner source response.
- Selected-Go account writes fail closed without TypeScript fallback.

