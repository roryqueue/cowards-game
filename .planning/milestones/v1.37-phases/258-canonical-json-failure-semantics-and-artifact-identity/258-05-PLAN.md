---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "05"
type: execute
wave: 6
depends_on: ["258-03", "258-04", "258-06"]
files_modified:
  - packages/spec/src/canonical-json.ts
  - packages/spec/src/canonical-json-boundaries.test.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/runtime-execution-service.ts
  - packages/spec/src/index.ts
  - apps/runtime-service/src/server.ts
  - apps/runtime-service/src/server.test.ts
  - .planning/artifacts/v2.0-core-rules-audit/reproduce-core-rule-gaps.ts
autonomous: true
requirements: [RABI-01, RABI-02, RABI-03]
must_haves:
  truths:
    - "JsonValue, StrategyMemory, SoldierMemory, objectives and service admission consume raw-byte preflight before recursive schema or host parse."
    - "The permanent deep reproduction returns a stable typed limit result and every valid v1.4 fixture remains unchanged (D-01 through D-04)."
  artifacts:
    - path: "packages/spec/src/canonical-json.ts"
      provides: "Public profile-aware scan/parse/encode/hash facade"
---
<objective>Install bounded canonical JSON at the spec, memory/objective, and runtime-service admission boundaries.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-03-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-04-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-06-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace recursive untrusted JSON admission</name>
  <files>packages/spec/src/canonical-json.ts, packages/spec/src/canonical-json-boundaries.test.ts, packages/spec/src/schemas.ts, packages/spec/src/runtime-execution-service.ts, packages/spec/src/index.ts</files>
  <read_first>Plans 03/04/06 summaries; schemas.ts recursive JsonValue; Plan-06-owned runtime-execution-service successor schemas; all memory/objective lower caps in v1.17 contract.</read_first>
  <behavior>
    - Raw bytes are scanned under exact ownership/profile before shape validation; duplicate/UTF-8/limit errors retain code/path/offset.
    - Lower StrategyMemory, SoldierMemory, objective, request, response and manifest caps override but never loosen global caps.
    - Already-materialized internal values use iterative semantic validation and canonical re-encoding; no recursive Zod first pass remains on attacker data.
  </behavior>
  <action>Write boundary tests first. Compose the scanner/parser/encoder behind one spec facade and integrate it into the already-landed Plan-06 v1.17 discriminated schema; Plan 05 may not redefine Plan-06 outcome/envelope types. Migrate only successor admission call sites, retain frozen historical v1.4/v1.16 decoders unchanged, and keep schema validation after bounded materialization for domain legality.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/canonical-json-boundaries.test.ts packages/spec/src/canonical-json-corpus.test.ts &amp;&amp; pnpm --filter @cowards/spec test</automated></verify>
  <acceptance_criteria>No public successor admission performs host JSON conversion or recursive value walking first; all lower caps and stable errors are tested.</acceptance_criteria>
  <done>Spec-owned data boundaries are iterative and bounded end to end.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Gate runtime-service raw admission and permanent audit</name>
  <files>apps/runtime-service/src/server.ts, apps/runtime-service/src/server.test.ts, .planning/artifacts/v2.0-core-rules-audit/reproduce-core-rule-gaps.ts</files>
  <read_first>server raw-body handling/tests; canonical facade; permanent seven-gap audit and immutable Phase-257 result artifact.</read_first>
  <behavior>
    - Service rejects malformed outer bytes as system-owned and decoded Strategy payload canonical/schema failures as player-owned without exposing raw bytes.
    - Deep-memory reproduction is typed/non-throwing while all Phase-257 lifecycle observations remain exact.
  </behavior>
  <action>Preserve raw request bytes through canonical scan before object conversion and route ownership explicitly. Update only the live reproduction code/current result path; never rewrite immutable pre-refactor or Phase-257 proof artifacts. Add privacy assertions and a valid-v1.4 compatibility stop if any state/action/event/outcome/observation changes.</action>
  <verify><automated>pnpm --filter @cowards/runtime-service test &amp;&amp; pnpm exec tsx .planning/artifacts/v2.0-core-rules-audit/reproduce-core-rule-gaps.ts</automated></verify>
  <acceptance_criteria>Deep audit reports the exact typed limit code, not RangeError; service returns the correct owner class and redacted public message.</acceptance_criteria>
  <done>Service admission cannot erase byte evidence or turn infrastructure corruption into a player penalty.</done>
</task>
</tasks>

## Artifacts this phase produces

- Public canonical JSON facade and bounded schema/service integrations.
- Updated live deep-audit observation without rebaselining history.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Recursive schema remains reachable first | critical | Adversarial boundary tests and structural call-order checks. |
| Outer corruption blamed on Strategy | high | Explicit ownership context and redacted system failure. |
| Audit update rewrites history/gameplay | critical | Immutable artifact denylist and KERN-11 compatibility stop. |
</threat_model>
<verification>Run spec, service, corpus and permanent audit gates together.</verification>
<success_criteria>All untrusted JSON/memory/objective/service boundaries enforce the same bounded profile without gameplay drift.</success_criteria>
<output>Create `258-05-SUMMARY.md`.</output>
