---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "04"
type: execute
wave: 4
depends_on: ["258-03"]
files_modified:
  - packages/spec/src/canonical-json-encode.ts
  - packages/spec/src/canonical-json-encode.test.ts
  - packages/spec/src/canonical-json-corpus.test.ts
  - scripts/generate-canonical-json-v1-1-corpus.ts
  - scripts/generate-canonical-json-v1-1-corpus.test.ts
  - packages/spec/src/fixtures/canonical-json-v1-1-vectors.json
  - packages/spec/src/fixtures/canonical-json-v1-1-raw
  - scripts/check-canonical-json-v1-1-red.ts
  - .planning/artifacts/v1.37-canonical-json-red.json
  - packages/spec/src/canonical-identity-domains.ts
  - packages/spec/src/canonical-identity-domains.test.ts
  - packages/spec/src/runtime-identity-manifest.ts
  - packages/spec/src/runtime-identity-manifest.test.ts
autonomous: true
requirements: [RABI-01, RABI-02, RABI-06]
must_haves:
  truths:
    - "D-03: Valid UTF-8/Unicode scalars are preserved without normalization and canonical object keys sort lexicographically by UTF-8 bytes."
    - "Canonical serialization is iterative, bounded, UTF-8-byte-key ordered, negative-zero normalized, and shortest-round-trip for binary64 (D-02 through D-04)."
    - "Every identity uses one fixed domain tag and canonical length-delimited SHA-256 framing rather than ambient host serialization (D-13 through D-16)."
  artifacts:
    - path: "packages/spec/src/canonical-json-encode.ts"
      provides: "Canonical iterative encoder"
    - path: "packages/spec/src/canonical-identity-domains.ts"
      provides: "Closed identity-domain registry and framing"
---
<objective>Complete the TypeScript codec after scanner/parser, retire its RED sentinel, and build the domain-framed identity foundation.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement iterative canonical encoder</name>
  <files>packages/spec/src/canonical-json-encode.ts, packages/spec/src/canonical-json-encode.test.ts, packages/spec/src/canonical-json-corpus.test.ts, scripts/generate-canonical-json-v1-1-corpus.ts, scripts/generate-canonical-json-v1-1-corpus.test.ts, packages/spec/src/fixtures/canonical-json-v1-1-vectors.json, packages/spec/src/fixtures/canonical-json-v1-1-raw, scripts/check-canonical-json-v1-1-red.ts, .planning/artifacts/v1.37-canonical-json-red.json</files>
  <read_first>Canonical corpus numeric/string/order vectors; runtime-abi-v1-17.ts; existing canonical-instant serializer and runtime receipt serializers.</read_first>
  <behavior>
    - Object keys sort lexicographically by UTF-8 bytes, not locale or UTF-16 code units.
    - Finite binary64 emits one specified shortest-round-trip decimal; safe integers only; -0 emits 0.
    - Strings preserve scalars/normalization while using one escape form; lone surrogate input is typed invalid.
    - Encoder uses explicit stack/counters and enforces the same ceilings without recursion.
  </behavior>
  <action>Write number/order/string tests first from corpus. Implement standard-library-only deterministic encoding, with a reviewed shortest-round-trip algorithm whose vectors cover subnormals/extremes/exponent thresholds. Correct the two derived object-entry boundary canonical expectations that Plan 02 accidentally left insertion-ordered; preserve every raw input byte, vector ID, and the raw-byte corpus root, and record old/new derived hashes explicitly. After scanner/parser/encoder satisfy every TS vector, remove exactly `[EXPECTED_RED:MISSING_CANONICAL_JSON_TS_CODEC]` and make the TS corpus test green; do not alter the Go sentinel owned by Plan 11. Do not use localeCompare, default Go/Python JSON spellings, or ambient `JSON.stringify` as canonical authority.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/canonical-json-encode.test.ts packages/spec/src/canonical-json-corpus.test.ts &amp;&amp; pnpm exec tsx scripts/check-canonical-json-v1-1-red.ts --stage go-missing --write --check</automated></verify>
  <acceptance_criteria>Full TS corpus passes, TS sentinel is absent, and the stage receipt proves only the exact Go sentinel remains; every positive vector emits exact bytes and round-trips.</acceptance_criteria>
  <done>Canonical bytes are independent of insertion order, locale, and host formatter quirks.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Unify domain hashes and identity-manifest serialization</name>
  <files>packages/spec/src/canonical-identity-domains.ts, packages/spec/src/canonical-identity-domains.test.ts, packages/spec/src/runtime-identity-manifest.ts, packages/spec/src/runtime-identity-manifest.test.ts</files>
  <read_first>runtime-abi-v1-17 identity registry; packages/spec/src/runtime-evidence-attestation.ts; existing hash-vector generators; Phase-257 semantic receipt.</read_first>
  <behavior>
    - Closed registry covers original/normalized source, normalization policy, artifact, manifest, runtime/compiler/sysroot/adapter, budget/profile, tuple, corpus, containment, and evidence.
    - Hash framing is fixed domain tag plus unambiguous lengths and canonical bytes; unknown domains fail.
    - Manifest serialization is canonical and rejects missing/extra/duplicate semantic bindings.
  </behavior>
  <action>Consolidate new successor hashing behind one registry without rewriting historical hash helpers used to verify old receipts. Make manifests consume canonical encoder output and preserve safe public IDs separately. Add collision-framing, ordering, unknown-domain, tamper, and historical-stability tests.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/canonical-identity-domains.test.ts packages/spec/src/runtime-identity-manifest.test.ts packages/spec/src/canonical-json-encode.test.ts</automated></verify>
  <acceptance_criteria>All successor domains have unique tags and deterministic vectors; reordered manifest objects hash identically; altered binding bytes do not.</acceptance_criteria>
  <done>Every successor identity is explicit and language-neutral without mutating historical v1.16 semantics.</done>
</task>
</tasks>

## Artifacts this phase produces

- Iterative canonical encoder.
- Closed domain registry, framed hash vectors, and canonical runtime identity manifest serializer.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Locale/host numeric drift changes signed bytes | critical | Exact corpus and custom reviewed canonical encoder. |
| Cross-domain or ambiguous concatenation collision | high | Unique tags and length-delimited framing. |
| New canonical helper corrupts historical receipt verification | critical | Separate successor registry and pinned v1.16 stability tests. |
</threat_model>
<verification>Run encoder, identity-domain, manifest, and corpus tests together; inspect that no v1.16 artifact is modified.</verification>
<success_criteria>Canonical bytes and identity hashes are deterministic across insertion order and ready for all language/service integrations.</success_criteria>
<output>Create `258-04-SUMMARY.md`.</output>
