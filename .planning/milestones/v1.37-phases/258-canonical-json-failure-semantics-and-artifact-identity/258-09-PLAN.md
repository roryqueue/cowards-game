---
phase: 258-canonical-json-failure-semantics-and-artifact-identity
plan: "09"
type: execute
wave: 8
depends_on: ["258-06", "258-07"]
files_modified:
  - packages/runtime-python/src/validation.ts
  - packages/runtime-python/src/python-subprocess-adapter.ts
  - packages/runtime-python/src/python_runtime_host.py
  - packages/runtime-python/src/python_validation_host.py
  - packages/runtime-python/src/python-subprocess-adapter.test.ts
  - apps/web/lib/account-revision-write-boundary.ts
  - apps/web/lib/account-revision-write-boundary.test.ts
  - apps/web/app/api/account/revisions/save/route.ts
  - apps/web/app/api/account/revisions/save/route.test.ts
  - apps/web/app/competitive/server.ts
  - apps/web/app/competitive/server.test.ts
  - apps/web/app/workshop/workshop-client.tsx
  - apps/web/e2e/v1-37-runtime-source-identity-proof.spec.ts
  - apps/go-backend/live_backend.go
  - apps/go-backend/source_identity_test.go
  - packages/persistence/migrations/0018_strategy_revision_source_identity.sql
  - packages/persistence/src/schema.ts
  - packages/persistence/src/migrations.test.ts
  - packages/persistence/src/account-revisions.ts
  - packages/persistence/src/account-revisions.test.ts
autonomous: true
requirements: [RABI-01, RABI-02, RABI-03, RABI-05, RABI-06, RABI-08]
must_haves:
  truths:
    - "Python binds immutable original bytes and separately hashed normalized bytes/policy/line-ending facts; CRLF executes against normalized artifact identity (D-13, D-14)."
    - "Python host boundaries preserve raw bytes and exact preflight/execution ownership; adapter failure never becomes synthetic player THROWN_EXCEPTION."
    - "The real web POST save route, selected Go backend, and PostgreSQL row preserve LF, CRLF, CR, mixed endings and no-final-newline source bytes exactly and persist the complete v2 identity tuple."
    - "Both current save callers—the Workshop UI/API route and competitiveServer.saveAccountRevision server action—use one exact-byte boundary; neither normalizeText nor trim may touch source."
  artifacts:
    - path: "packages/runtime-python/src/python-subprocess-adapter.ts"
      provides: "Python v1.17 adapter and source identity binding"
    - path: "packages/persistence/migrations/0018_strategy_revision_source_identity.sql"
      provides: "Additive nullable legacy-safe original/normalized source identity storage"
---
<objective>Repair Python raw-boundary classification and original/normalized source identity without manufacturing legacy history.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-07-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Separate original and normalized Python source domains</name>
  <files>packages/runtime-python/src/validation.ts, packages/runtime-python/src/python_validation_host.py, apps/web/lib/account-revision-write-boundary.ts, apps/web/lib/account-revision-write-boundary.test.ts, apps/web/app/api/account/revisions/save/route.ts, apps/web/app/api/account/revisions/save/route.test.ts, apps/web/app/competitive/server.ts, apps/web/app/competitive/server.test.ts, apps/web/app/workshop/workshop-client.tsx, apps/web/e2e/v1-37-runtime-source-identity-proof.spec.ts, apps/go-backend/live_backend.go, apps/go-backend/source_identity_test.go, packages/persistence/migrations/0018_strategy_revision_source_identity.sql, packages/persistence/src/schema.ts, packages/persistence/src/migrations.test.ts, packages/persistence/src/account-revisions.ts, packages/persistence/src/account-revisions.test.ts</files>
  <read_first>Research Source domains and CRLF probe; current Python builder; account-revision-write-boundary.ts; API save route; competitive/server.ts normalizeText and saveAccountRevision; Workshop save UI; Go createStrategyRevision/insertAccountRevision; strategy_revisions schema/immutability triggers; v1.17 normalization registry.</read_first>
  <behavior>
    - New revisions preserve exact submitted UTF-8 bytes, line-ending counts/type and terminal-newline fact; emptiness validation does not trim stored bytes.
    - Normalization produces separately hashed derivative with policy/version; artifact manifest binds both.
    - Existing persisted bytes become immutable legacy-original; absent pre-trim bytes are never reconstructed.
    - Migration 0018 stores `source_identity_version`, `original_source_hash`, `original_source_bytes`, `normalized_source_hash`, `normalized_source_bytes`, `source_normalization_policy`, `source_line_endings`, and `source_has_final_newline` as one all-or-none immutable v2 group.
    - Web `POST /api/account/revisions/save` forwards the exact JavaScript string without trim/line-ending conversion; Go `POST /account/strategy-revisions` computes and writes identity from exact UTF-8 bytes in the same insert transaction.
    - `competitiveServer.saveAccountRevision` validates emptiness on a derived `source.trim()` predicate but passes the original string to persistence; Workshop UI textarea and fetch body preserve the original string.
  </behavior>
  <action>Write exact LF, CRLF, lone-CR, mixed `CRLF+LF+CR`, no-final-newline, BOM and whitespace-only vectors first. Add migration 0018 as nullable all-or-none v2 columns with locked-content coverage and no legacy backfill. Export one exact-source validator/identity input used by the API boundary and `competitiveServer.saveAccountRevision`; remove source from `normalizeText` while retaining trimmed label/notes. Make Workshop fetch, selected Go backend and direct persistence server action preserve the original UTF-8 string. DB tests exercise both save paths, fetch private source, query byte hex/identity columns, and compare hashes, ending counts and final-newline. The Playwright proof types/pastes mixed-ending and no-final-newline sources in the real Workshop UI, clicks Save revision, and verifies the owner-source response plus server-action integration receipt.</action>
  <verify><automated>PGUSER=cowards PGPASSWORD=cowards DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game scripts/dev-local-postgres.sh --setup-only &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run apps/web/lib/account-revision-write-boundary.test.ts apps/web/app/api/account/revisions/save/route.test.ts apps/web/app/competitive/server.test.ts packages/persistence/src/migrations.test.ts packages/persistence/src/account-revisions.test.ts packages/runtime-python/src/python-subprocess-adapter.test.ts &amp;&amp; (cd apps/go-backend &amp;&amp; COWARDS_GO_BACKEND_TEST_DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game PATH=/usr/local/go/bin:$PATH go test ./... -run 'TestPhase258SourceIdentityPostgres' -v -count=1) &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec playwright test apps/web/e2e/v1-37-runtime-source-identity-proof.spec.ts --project=desktop</automated></verify>
  <acceptance_criteria>Both real save paths and the UI E2E preserve exact LF/CRLF/CR/mixed/no-final-newline bytes; named Go test prints PASS not SKIP; v2 fields are all-or-none/immutable and legacy rows remain byte-identical/null-v2.</acceptance_criteria>
  <done>Python artifacts have truthful, immutable two-domain source provenance.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Migrate Python raw envelope and classification</name>
  <files>packages/runtime-python/src/python-subprocess-adapter.ts, packages/runtime-python/src/python_runtime_host.py, packages/runtime-python/src/python-subprocess-adapter.test.ts</files>
  <read_first>Successor envelope/ownership matrix; current host json.loads/dumps call order; Plan 07 engine/service contract.</read_first>
  <behavior>
    - Adapter authenticates outer envelope and scans raw Strategy payload before decoded schema checks.
    - Invalid source/artifact is submission violation; preflight unavailable is system failure; healthy Strategy exception is player violation; host/transport ambiguity is system failure.
    - Exact canonical wire and safe trace match the common ABI envelope.
  </behavior>
  <action>Add raw duplicate/UTF-8/truncation/deep/output/throw/crash/preflight matrices. Remove synthetic mixed `THROWN_EXCEPTION` from system failures. The Python helper may materialize only scanner-approved bytes and must not become gameplay authority. Keep package-free standard library only.</action>
  <verify><automated>pnpm --filter @cowards/runtime-python test</automated></verify>
  <acceptance_criteria>Every matrix row has one correct result kind, CRLF regression succeeds, and no adapter/system failure carries a player violation.</acceptance_criteria>
  <done>Python implements the same ABI semantics and truthful source identity as every other lane.</done>
</task>
</tasks>

## Artifacts this phase produces

- Python v1.17 adapter, exact web/Go save path, migration 0018, and immutable original/normalized source identity repair.

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Normalization overwrites revision identity | critical | Separate domains/hashes and immutable legacy handling. |
| Adapter crash becomes player penalty | critical | Exclusive classification tests and no synthetic violation. |
| Save path trims meaningful bytes | high | Exact-byte persistence tests. |
| Legacy rows are fabricated into v2 identity | critical | Nullable additive migration, no backfill, explicit legacy dispatch. |
</threat_model>
<verification>Run source identity/CRLF tests and full runtime-python suite.</verification>
<success_criteria>Python preserves exact provenance and the common three-way, raw-byte ABI without gameplay mutation.</success_criteria>
<output>Create `258-09-SUMMARY.md`.</output>
