---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "25"
type: execute
wave: 2
depends_on: ["259-24"]
files_modified:
  - packages/runtime-supervisor/package.json
  - packages/runtime-supervisor/tsconfig.json
  - packages/runtime-supervisor/src/supervisor-contract.ts
  - packages/runtime-supervisor/src/supervisor-contract.test.ts
  - packages/runtime-supervisor/src/index.ts
  - tsconfig.json
autonomous: true
requirements: [CONF-01, CONF-03, CONF-04]
must_haves:
  truths:
    - "One package-free internal `@cowards/runtime-supervisor` package depends on public `@cowards/spec` workspace exports and owns strict request/raw-receipt parsing and canonical host-evidence verification."
    - "A raw supervisor receipt is accepted only when its nonce, request hash, units, platform conversion, limits, exit/cancellation state, process-group containment, and supervisor identities match the exact invocation."
    - "Guest stdout/payload can never construct, replace, or sign host accounting evidence."
    - "The root TypeScript project registers the package; Plan 31 owns the one lockfile update after all consumer manifests exist, and no package imports another workspace's `src` path."
  artifacts:
    - path: packages/runtime-supervisor/src/supervisor-contract.ts
      provides: Strict native request/receipt protocol, verifier, and canonical adapter evidence input
    - path: packages/runtime-supervisor/package.json
      provides: "Build/test/lint/typecheck package boundary with @cowards/spec workspace dependency"
  key_links:
    - from: packages/spec/src/runtime-invocation-v1-18.ts
      to: packages/runtime-supervisor/src/supervisor-contract.ts
      via: public `@cowards/spec` export defines exact schema/profile/root and counter conversions; source-relative imports are forbidden
    - from: packages/runtime-supervisor/src/supervisor-contract.ts
      to: packages/runtime-js/src/supervised-subprocess-adapter.ts
      via: only a branded verified raw receipt may enter adapter-owned canonical evidence signing
    - from: packages/runtime-supervisor/src/supervisor-contract.ts
      to: packages/runtime-wasm-wasi/src/supervised-wasm-wasi-adapter.ts
      via: identical verifier wraps Wasmtime while local fuel remains separate defense-in-depth evidence
---
<objective>Create the package-free shared supervisor protocol and verifier used by all counted language adapters.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-24-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement strict supervisor request and raw-receipt verification</name>
  <files>packages/runtime-supervisor/src/supervisor-contract.ts, packages/runtime-supervisor/src/supervisor-contract.test.ts, packages/runtime-supervisor/src/index.ts</files>
  <read_first>259-24 v1.18 invocation/profile; canonical JSON/byte framing; existing runtime adapter authenticated outer-envelope patterns.</read_first>
  <behavior>
    - Request contains one random host nonce, canonical request hash, executable/argv/environment allowlist, input bytes, exact limits, expected platform/supervisor identities, and cancellation channel.
    - Raw receipt has a closed shape and exact nonce/request binding; any extra/missing/noncanonical/overflow/negative/wrong-unit field rejects before adapter evidence exists.
    - Verifier independently recomputes wall/cgroup CPU/memory/pids/events/output/payload/cancellation classifications and rejects contradictory exit/signal/timeout/empty-cgroup claims.
    - Branded verified output contains no Strategy source, artifacts, memories, objectives, diagnostics, host paths, raw stderr, or key material.
  </behavior>
  <action>Write forged guest frame, nonce replay, request substitution, counter overflow, platform conversion, exit/signal contradiction, late-result, incomplete group-reap, private-poison, and strict-shape RED tests. Implement pure request encoding and raw-receipt verification only; signing remains injected and adapter-owned.</action>
  <verify><automated>pnpm exec vitest run packages/runtime-supervisor/src/supervisor-contract.test.ts</automated></verify>
  <acceptance_criteria>Only a raw receipt from the exact invocation can become bounded canonical host evidence input, and guest material cannot forge it.</acceptance_criteria>
  <done>The common supervisor protocol is strict and reusable before native execution is added.</done>
</task>
<task type="auto">
  <name>Task 2: Register the package and root project reference before consumer wiring</name>
  <files>packages/runtime-supervisor/package.json, packages/runtime-supervisor/tsconfig.json, tsconfig.json</files>
  <read_first>Spec/runtime package manifests and project references; root tsconfig; packages-star pnpm workspace; root ESLint/Turbo scripts; current lockfile importers.</read_first>
  <action>Add `@cowards/runtime-supervisor` with `@cowards/spec: workspace:*`, main/types/exports pointing only to its public index, and build/test/lint/typecheck scripts matching repository package conventions. Its composite tsconfig references `../spec`; root `tsconfig.json` references `packages/runtime-supervisor`. The existing `packages/*` workspace glob already registers it, so do not edit pnpm-workspace.yaml. Do not edit the workspace lockfile here; Plan 31 is its sole owner after consumer manifests are updated. Add no third-party dependency, postinstall, build allowlist, source-relative import, or download behavior.</action>
  <verify><automated>pnpm --filter @cowards/runtime-supervisor build &amp;&amp; pnpm --filter @cowards/runtime-supervisor lint &amp;&amp; pnpm --filter @cowards/runtime-supervisor typecheck &amp;&amp; pnpm --filter @cowards/runtime-supervisor test &amp;&amp; pnpm exec tsc -b --dry</automated></verify>
  <acceptance_criteria>The package is root/project resolvable, publicly imports spec, runs all four package scripts, and defers the only lockfile update to the complete consumer-wiring plan.</acceptance_criteria>
  <done>The supervisor is a registered workspace project ready for explicit consumer dependencies and final lock resolution.</done>
</task>
</tasks>

## Artifacts this phase produces

- Package-free `@cowards/runtime-supervisor` protocol/verifier package.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Guest output impersonates a host receipt | high | Nonce/request binding plus raw-channel separation and strict verifier. |
| Receipt contradiction hides cancellation race | high | Exact exit/signal/group-reap state machine and mutation tests. |
| Dependency install broadens attack surface | high | No third-party dependencies, offline lock, no install hook. |
</threat_model>
<verification>Run supervisor contract, strict-shape, poison, package build/test/lint/typecheck, root project reference, and source-import checks; Plan 31 owns offline lock closure.</verification>
<success_criteria>One reusable package-free verifier defines the counted subprocess evidence boundary.</success_criteria>
<output>Create `259-25-SUMMARY.md`.</output>
