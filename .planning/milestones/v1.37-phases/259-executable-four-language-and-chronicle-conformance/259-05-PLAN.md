---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/replay/src/grammar.ts
  - packages/replay/src/grammar.test.ts
autonomous: true
requirements: [CHRN-01]
must_haves:
  truths:
    - "D-13: current Chronicle grammar tracks selected/started/open/closed state, actor/player, next Cycle, Advance, terminal reason, and boundaries independently per activationId alongside global Round/initiative state."
    - "The first rejected event does not mutate slot or global grammar state."
  artifacts:
    - path: packages/replay/src/grammar.ts
      provides: Deterministic per-activation-slot current grammar state machine and stable codes
  key_links:
    - from: packages/replay/src/grammar.ts
      to: packages/replay/src/validate.ts
      via: validateCurrentChronicleSemantics advances the per-activationId state machine before semantic acceptance
    - from: packages/replay/src/grammar.ts
      to: packages/replay/src/replay-transition.ts
      via: exact slot and open-boundary coordinates are compared at the same transition ordinal
---
<objective>Replace the singleton current Chronicle lifecycle cursor with the D-13 per-slot state machine without changing valid v1.4 behavior.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-CONTEXT.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-RESEARCH.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify interleaved per-slot grammar transitions</name>
  <files>packages/replay/src/grammar.ts, packages/replay/src/grammar.test.ts</files>
  <read_first>packages/replay/src/grammar.ts; packages/replay/src/grammar.test.ts; packages/engine/src/kernel/types.ts; CowardsGameSpec_CycleInterleaved_v1.4.md scheduling boundaries; D-13.</read_first>
  <behavior>
    - Interleaved activation slots retain independent actor, Soldier, index, next-Cycle, Advance, and close history while exactly one open boundary owns action events.
    - Duplicate select/start/end, skipped/repeated Cycle, wrong slot/actor/Soldier, after-close events, Advance mismatch, and terminal-reason mismatch return stable first-failure codes.
    - Slot iteration and serialized diagnostics are deterministic and never expose a host Map.
  </behavior>
  <action>Build the valid interleaving fixture and one RED mutation per stable error family before implementation. Replace singleton fields with internal immutable slot records plus an exact open-boundary coordinate. Preserve accepted event order and terminal timing; any valid-v1.4 delta is an explicit stop.</action>
  <verify><automated>pnpm exec vitest run packages/replay/src/grammar.test.ts</automated></verify>
  <acceptance_criteria>Valid interleaved slots pass, every lifecycle mutation fails at the first exact event, and rejection leaves prior grammar state unchanged.</acceptance_criteria>
  <done>Current grammar can validate every activation slot independently and deterministically.</done>
</task>
</tasks>

## Artifacts this phase produces

- `CurrentActivationSlotGrammarState`, `CurrentChronicleGrammarState`, and per-slot stable grammar codes in `packages/replay/src/grammar.ts`.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Event subject spoofs another activation slot | high | Exact activation/actor/Soldier/open-boundary ownership. |
| Rejected event partially advances state | high | Copy-on-accept transition with no mutation after first failure. |
| Host collection ordering changes validation | medium | Explicit deterministic slot/event order; Map is never serialized. |
</threat_model>
<verification>Run replay grammar tests including valid interleaving and every adversarial mutation family.</verification>
<success_criteria>Per-slot lifecycle and Cycle state are exact without changing any approved valid Match behavior.</success_criteria>
<output>Create `259-05-SUMMARY.md`.</output>
