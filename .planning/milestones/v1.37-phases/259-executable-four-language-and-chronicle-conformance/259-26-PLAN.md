---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "26"
type: execute
wave: 3
depends_on: ["259-25"]
files_modified:
  - packages/runtime-supervisor/native/Cargo.toml
  - packages/runtime-supervisor/native/Cargo.lock
  - packages/runtime-supervisor/native/src/main.rs
  - packages/runtime-supervisor/src/native-supervisor.ts
  - packages/runtime-supervisor/src/native-supervisor.test.ts
  - packages/runtime-supervisor/src/linux-certification-container.ts
  - packages/runtime-supervisor/src/linux-certification-container.test.ts
  - scripts/build-runtime-supervisor.ts
  - scripts/build-runtime-supervisor.test.ts
autonomous: true
requirements: [CONF-01, CONF-03, CONF-04]
must_haves:
  truths:
    - "A pinned zero-dependency Rust supervisor on Linux creates one delegated cgroup-v2 subtree per invocation and launches exactly one session/process group whose threads, descendants, and reparented children inherit cgroup membership."
    - "The supervisor owns cgroup cpu/memory/pids limits and aggregate reads, process-group/cgroup.kill cancellation, exact output capture, and empty-cgroup proof before raw receipt."
    - "The required four-language certification on macOS uses an exact two-stage Docker/cgroupfs-v2 delegation protocol: a pinned trusted bootstrap container provisions one writable subtree without Strategy input, then an unprivileged hardened supervisor container receives only that subtree."
    - "Missing controller/delegation/read/write, cgroup escape/linger/race, Docker/image/kernel drift, or native non-Linux counted attempt is system failure and creates no receipt/certificate."
  artifacts:
    - path: packages/runtime-supervisor/native/src/main.rs
      provides: Package-free native process/session supervisor and raw receipt producer
    - path: packages/runtime-supervisor/src/native-supervisor.ts
      provides: Pinned build/binary identity verification and invocation seam
    - path: packages/runtime-supervisor/src/linux-certification-container.ts
      provides: Pinned Docker/Linux/cgroup-v2 certification controller for macOS development hosts
    - path: scripts/build-runtime-supervisor.ts
      provides: Reproducible locked build/check command with exact identity manifest
  key_links:
    - from: packages/runtime-supervisor/native/src/main.rs
      to: packages/runtime-supervisor/src/supervisor-contract.ts
      via: native stdout emits only the nonce/request-bound raw receipt accepted by the strict verifier
    - from: scripts/build-runtime-supervisor.ts
      to: packages/runtime-supervisor/src/native-supervisor.ts
      via: exact source/lock/toolchain/target/OS/settings/binary hashes are checked before invocation
    - from: packages/runtime-supervisor/src/native-supervisor.ts
      to: packages/runtime-python/src/python-supervised-subprocess-adapter.ts
      via: one verified binary launches the real Python child under the shared quantitative meter
---
<objective>Build the package-free pinned native supervisor that provides the common real host meter and process-group cancellation evidence.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-25-SUMMARY.md @.planning/phases/258-canonical-json-failure-semantics-and-artifact-identity/258-12-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Linux cgroup-v2 aggregate supervision</name>
  <files>packages/runtime-supervisor/native/Cargo.toml, packages/runtime-supervisor/native/Cargo.lock, packages/runtime-supervisor/native/src/main.rs</files>
  <read_first>259-24 units/boundaries; 259-25 raw protocol; Rust std Unix process APIs; repository package-free/runtime containment rules.</read_first>
  <behavior>
    - Cargo manifest has zero dependencies and Linux-only counted compilation; native macOS/other targets return stable non-counted/unsupported before child launch.
    - Supervisor validates delegated cgroup-v2 cpu/memory/pids controllers, creates a nonce-bound 0700 invocation subtree, writes `cpu.max`, `memory.max`, `pids.max`, and places the session leader in `cgroup.procs` before guest execution.
    - Guest drops to a distinct unprivileged UID with no writable cgroup path/capability; all threads/descendants/reparented children remain in the invocation cgroup and escape attempts fail.
    - Receipt uses baseline/final `cpu.stat usage_usec`, `memory.peak`, `pids.current/events`, `memory.events`, exact stdout/stderr/payload bytes, and monotonic wall time.
    - Deadline/cancel kills the process group and uses cgroup.kill where available, rejects late output, then waits until `cgroup.procs` is empty before receipt/removal.
  </behavior>
  <action>Write Linux native fixtures for normal exit, signal, timeout, cancel-before/after-exit race, short-lived descendants, orphan/reparented children, multi-threaded Node/Python/Wasmtime-shaped commands, aggregate CPU, memory.peak, pids exhaustion/events, exact/N+1 output, cgroup escape attempts, lingering process, missing controller/delegation/read/write, cgroup.kill fallback, unsupported platform, and overflow. Implement with Rust std plus required Linux FFI/files only; no crate dependency, per-process rusage accounting, shell wrapper, wall clock, guest network/cgroup capability, or receipt before empty-cgroup proof.</action>
  <verify><automated>cargo test --locked --manifest-path packages/runtime-supervisor/native/Cargo.toml &amp;&amp; cargo tree --locked --manifest-path packages/runtime-supervisor/native/Cargo.toml --depth 1</automated></verify>
  <acceptance_criteria>The native binary produces exact bounded raw receipts for one process group and fails closed on every unsupported or ambiguous measurement/cancellation state.</acceptance_criteria>
  <done>The common quantitative meter exists as real package-free native execution.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Pin Linux supervisor and cgroup policy identity</name>
  <files>packages/runtime-supervisor/src/native-supervisor.ts, packages/runtime-supervisor/src/native-supervisor.test.ts, scripts/build-runtime-supervisor.ts, scripts/build-runtime-supervisor.test.ts</files>
  <read_first>Task-1 binary/protocol; exact runtime/toolchain/artifact identity patterns; no-fallback candidate execution rules.</read_first>
  <behavior>
    - Build command records exact source/Cargo.lock, rustc, Cargo, Linux target/kernel, cgroup-v2 controller/delegation/settings, guest UID, and binary hashes and refuses stale/substituted output.
    - Invocation verifies the pinned manifest and executable before launch, passes the exact nonce/request, verifies the raw receipt, and returns only branded safe evidence input.
    - Missing Rust toolchain/Linux cgroup support/delegation, changed binary/settings, malformed receipt, or supervisor crash is a no-mutation system failure with no adapter fallback.
  </behavior>
  <action>Write build/source/toolchain/target/binary, Linux kernel/controller/delegation/settings/guest-UID substitution, permissions, malformed receipt, missing toolchain, native macOS counted rejection, and no-fallback RED tests. Implement locked build/check and invocation seams; build artifacts/cgroup paths remain private and only exact identities enter safe evidence.</action>
  <verify><automated>pnpm exec vitest run packages/runtime-supervisor/src/native-supervisor.test.ts scripts/build-runtime-supervisor.test.ts &amp;&amp; pnpm exec tsx scripts/build-runtime-supervisor.ts --build --check &amp;&amp; pnpm --filter @cowards/runtime-supervisor typecheck</automated></verify>
  <acceptance_criteria>Only the exact pinned package-free supervisor binary can launch a counted subprocess, and every unavailable/substituted state fails without mutation or fallback.</acceptance_criteria>
  <done>The runtime adapters can invoke one authenticated real supervisor.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Run required certification inside a pinned hardened Linux Docker controller</name>
  <files>packages/runtime-supervisor/src/linux-certification-container.ts, packages/runtime-supervisor/src/linux-certification-container.test.ts</files>
  <read_first>Existing container-subprocess Docker arguments/evaluation; Task-1/2 supervisor binary and cgroup policy; Phase-258 exact image/toolchain identity rules.</read_first>
  <behavior>
    - On macOS the counted path requires the existing Docker engine, exact engine/server version, pinned digest-addressed Linux image, Linux kernel/cgroup-v2/controller identity, and exact supervisor/toolchains.
    - Preflight requires Docker server `OSType=linux`, cgroup v2, and the exact pinned cgroup driver/settings. The initial implementation supports `cgroupfs`; a systemd or unknown driver is typed unsupported and cannot fall back.
    - A short-lived pinned bootstrap image, containing only the supervisor bootstrap/finalizer command and no Strategy source/artifact mount, runs with `--cgroupns=host`, `--cap-drop=ALL`, exact `SYS_ADMIN,CHOWN,DAC_OVERRIDE` additions, `--security-opt=no-new-privileges`, `--network=none`, read-only root, and a writable bind of daemon-host `/sys/fs/cgroup` at `/host-cgroup`.
    - Bootstrap creates `/host-cgroup/cowards/<run-id>` as mode 0700, enables `cpu memory pids`, chowns only the directory plus delegation control files to the fixed supervisor UID/GID, and emits a nonce-bound descriptor hash; any extra capability, mount, controller, owner, mode, or descriptor drift fails before guest launch.
    - The real supervisor container runs with `--cgroupns=host`, `--cgroup-parent=/cowards/<run-id>`, the fixed unprivileged supervisor UID/GID, `--cap-drop=ALL`, `--security-opt=no-new-privileges`, `--network=none`, read-only root, bounded tmpfs, exact outer PID/memory/CPU bounds, and a writable bind of only `/sys/fs/cgroup/cowards/<run-id>` at `/run/cowards-cgroup`.
    - Guest children use a distinct unprivileged UID and cannot traverse the 0700 delegation mount. The supervisor may create only nonce-bound invocation children beneath it and must prove controller membership, aggregate reads, and empty cleanup.
    - A pinned trusted finalizer reopens and verifies the descriptor, uses `cgroup.kill` only on failure/cancellation, refuses non-empty or foreign children, removes the subtree, and treats cleanup failure as system failure; no stale delegation survives as reusable authority.
    - Missing Docker/image/cgroup delegation or any engine/image/kernel/settings drift fails loudly with no native-macOS or unsupervised fallback.
    - Linux CI/native hosts use the same identity/settings contract; macOS-native execution may run proof-only but is never counted equivalent.
  </behavior>
  <action>Write exact bootstrap, supervisor, and finalizer argument-vector tests plus a real macOS-engine preflight. The preflight must create a unique daemon-host cgroup root, enable controllers, delegate only the required files to the supervisor UID, launch an unprivileged container with `--cgroup-parent` and the subtree-only bind, create/write/remove one invocation child, verify the guest UID cannot traverse or write the mount, and clean the root. Test digest-only image, engine/kernel/cgroup-driver/controller mutation, unavailable image/daemon/delegation, bootstrap capability/mount expansion, wrong ownership/mode, descriptor replay/substitution, read-only/no-network/cap-drop/no-new-privileges, supervisor-vs-guest UID, short-lived descendant/reparent accounting, cleanup/foreign-child failure, cgroup escape, and no-macOS-fallback. Reuse established Docker argument/inspection patterns, but never grant the long-lived supervisor `SYS_ADMIN`, a host-wide cgroup mount, Strategy-controlled bootstrap input, or public host paths; return only exact safe identity/receipt/descriptor roots.</action>
  <verify><automated>pnpm exec vitest run packages/runtime-supervisor/src/linux-certification-container.test.ts packages/runtime-supervisor/src/native-supervisor.test.ts &amp;&amp; COWARDS_RUN_CONTAINER_SANDBOX=1 pnpm exec tsx scripts/build-runtime-supervisor.ts --build-linux-container --check</automated></verify>
  <acceptance_criteria>The macOS development host passes a real cgroupfs-v2 bootstrap/delegate/unprivileged-write/finalize probe and can produce required four-language evidence only through that exact pinned hardened controller; any capability, mount, driver, ownership, cleanup, or identity drift blocks certification.</acceptance_criteria>
  <done>The sole counted quantitative platform is executable locally without claiming macOS meter equivalence.</done>
</task>
</tasks>

## Artifacts this phase produces

- Package-free Rust native supervisor.
- Locked build/binary/toolchain/OS/settings identity and real invocation verifier.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Descendant escapes cancellation/accounting | high | Inherited per-invocation cgroup, process-group/cgroup.kill, and empty-cgroup proof. |
| Supervisor binary is substituted | high | Source/lock/toolchain/target/settings/binary identity manifest and prelaunch check. |
| Docker/macOS path masquerades as native equivalence | high | Pinned Linux controller identity and explicit native-macOS non-counted status. |
| Late result mutates after timeout | high | Group termination, payload discard, reap-before-receipt, no-mutation adapter contract. |
</threat_model>
<verification>Run Linux cgroup-v2 aggregate CPU/memory/pids, descendants/reparenting/threads, cancellation/escape, build identity, pinned Docker controller, non-Linux rejection, and wrapper suites.</verification>
<success_criteria>A single exact Linux cgroup-v2 supervisor/controller produces trustworthy aggregate receipts for every counted language process, including macOS-hosted certification.</success_criteria>
<output>Create `259-26-SUMMARY.md`.</output>
