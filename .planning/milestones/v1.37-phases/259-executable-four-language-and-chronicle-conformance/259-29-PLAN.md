---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "29"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture-v1-37-protected-baseline.ts
  - scripts/capture-v1-37-protected-baseline.test.ts
  - .planning/artifacts/v1.37-protected-working-tree-baseline.json
autonomous: true
requirements: [CONF-04]
must_haves:
  truths:
    - "Existing user-owned dirt in `.planning/config.json` and `CowardsGameSpec_Full_Consolidated_v1.md` is preserved exactly rather than requiring a clean or unstaged worktree."
    - "The phase-start baseline binds raw file hashes, HEAD blob identities, staged and unstaged binary-diff hashes/bytes, modes, and exact protected path inventory."
    - "Final verification compares against the captured baseline and fails on any Phase-259 change while accepting identical pre-existing dirt."
  artifacts:
    - path: .planning/artifacts/v1.37-protected-working-tree-baseline.json
      provides: Exact phase-start protected bytes/diff/status baseline
    - path: scripts/capture-v1-37-protected-baseline.ts
      provides: Strict write-once baseline capture and pure comparison command
  key_links:
    - from: .planning/config.json
      to: .planning/artifacts/v1.37-protected-working-tree-baseline.json
      via: raw bytes plus HEAD/staged/unstaged binary diff identities are captured before implementation
    - from: .planning/artifacts/v1.37-protected-working-tree-baseline.json
      to: scripts/evaluate-v1-37-executable-conformance.ts
      via: final evaluator requires exact equality to phase-start protected bytes and dirt rather than clean status
---
<objective>Capture the exact pre-existing protected working-tree state so final checks preserve user dirt byte-for-byte.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/STATE.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Capture and mutation-prove the protected phase-start baseline</name>
  <files>scripts/capture-v1-37-protected-baseline.ts, scripts/capture-v1-37-protected-baseline.test.ts, .planning/artifacts/v1.37-protected-working-tree-baseline.json</files>
  <read_first>Current protected file bytes/status; repository exact hash/proof patterns; git binary diff and mode semantics.</read_first>
  <behavior>
    - Write mode accepts exactly the two protected paths and refuses overwrite unless existing baseline bytes are identical.
    - Baseline stores raw SHA-256/size/mode, HEAD blob identity or absence, `git diff --binary --no-ext-diff` bytes/hash, cached binary-diff bytes/hash, and porcelain status per path.
    - Check mode is read-only and rejects raw-byte, mode, staged, unstaged, path-inventory, baseline, or diff mutation.
    - Existing modified/unstaged status is valid when exactly equal to the captured phase-start baseline.
  </behavior>
  <action>Write clean, modified, staged, mixed, missing, mode-change, raw-byte, diff-only, extra-path, and overwrite RED tests. Implement strict write-once capture plus pure check using non-interactive git commands and canonical safe JSON; never modify, stage, restore, or normalize either protected file.</action>
  <verify><automated>pnpm exec vitest run scripts/capture-v1-37-protected-baseline.test.ts &amp;&amp; pnpm exec tsx scripts/capture-v1-37-protected-baseline.ts --write &amp;&amp; pnpm exec tsx scripts/capture-v1-37-protected-baseline.ts --check</automated></verify>
  <acceptance_criteria>The exact pre-existing protected bytes and dirt are captured once and every later deviation is detectable without requiring cleanliness.</acceptance_criteria>
  <done>Phase 259 can prove it did not alter user-owned protected work.</done>
</task>
</tasks>

## Artifacts this phase produces

- Exact write-once protected working-tree baseline and pure drift checker.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Existing user dirt is erased to satisfy a clean check | high | Baseline exact bytes/diffs; no restore/stage/write behavior. |
| Baseline is rewritten after mutation | high | Write-once identical-only semantics and baseline self-hash. |
| Diff normalization hides byte changes | high | Raw file hash plus staged/unstaged binary diff bytes and modes. |
</threat_model>
<verification>Run baseline shape, modified/staged/mode/path mutation, write-once, and exact current check suites.</verification>
<success_criteria>Final protected-file proof preserves the exact phase-start working-tree state, including valid pre-existing modifications.</success_criteria>
<output>Create `259-29-SUMMARY.md`.</output>
