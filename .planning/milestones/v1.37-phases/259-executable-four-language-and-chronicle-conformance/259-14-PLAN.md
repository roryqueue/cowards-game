---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "14"
type: execute
wave: 3
depends_on: ["259-02", "259-06", "259-08"]
files_modified:
  - packages/replay/src/replay-transition.ts
  - packages/replay/src/replay-transition.test.ts
  - packages/replay/src/reconstruct.ts
  - packages/replay/src/reconstruct.test.ts
autonomous: true
requirements: [CHRN-05, CHRN-06]
must_haves:
  truths:
    - "D-14 reconstruction compares kind, before/after hashes, events, exact coordinates, and terminal data at every step, then final state/outcome/trace root, rejecting the first stable mismatch."
    - "D-15/D-16 historical reconstruction dispatches only to frozen original semantics and never to mutable current transition logic."
  artifacts:
    - path: packages/replay/src/replay-transition.ts
      provides: Exact current per-transition equality and frozen historical dispatch
    - path: packages/replay/src/reconstruct.ts
      provides: Reconstruction-equivalent final state/outcome/trace-root validator
  key_links:
    - from: packages/replay/src/validate.ts
      to: packages/replay/src/reconstruct.ts
      via: validateCurrentReplayReconstruction calls validateCurrentChronicleSemantics exactly once; validation never calls reconstruction
    - from: packages/replay/src/record.ts
      to: packages/replay/src/replay-transition.ts
      via: RecordedCanonicalTransitionV137 fields are compared at each ordinal before applying later transitions
    - from: packages/replay/src/reconstruct.ts
      to: apps/runtime-service/src/execute-match.ts
      via: verified transition, final state, outcome, terminal, and trace-root receipt gates service success
---
<objective>Prove D-14 transition-by-transition reconstruction equality while preserving D-15/D-16 historical dispatch.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-02-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-06-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-08-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Compare every current replay transition to its recorded kernel transition</name>
  <files>packages/replay/src/replay-transition.ts, packages/replay/src/replay-transition.test.ts</files>
  <read_first>259-02 recorded transition API; packages/replay/src/replay-transition.ts; packages/engine/src/kernel/apply-transition.ts; D-14.</read_first>
  <behavior>
    - Each ordinal compares exact kind, coordinates, ordered events, before/after state hashes, terminal/failure material, and accumulated root.
    - Mutating any field or ordering rejects at the first stable transition index without applying later transitions.
    - Current replay calls only the canonical transition implementation; historical replay calls only the frozen v1.4 module.
  </behavior>
  <action>Write exhaustive per-field differential RED tests first. Add exact comparison around the existing canonical transition application and explicit version dispatch; do not add a replay-local Match loop or infer coordinates from payloads.</action>
  <verify><automated>pnpm exec vitest run packages/replay/src/replay-transition.test.ts</automated></verify>
  <acceptance_criteria>Every transition mutation fails at the first exact coordinate and historical/current implementations remain disjoint.</acceptance_criteria>
  <done>Stepwise replay is exact against the recorded kernel stream.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Require final state, outcome, terminal, and trace-root equivalence</name>
  <files>packages/replay/src/reconstruct.ts, packages/replay/src/reconstruct.test.ts</files>
  <read_first>Task-1 transition result; 259-08 validateCurrentChronicleSemantics contract; packages/replay/src/reconstruct.ts; existing deterministic reconstruction fixtures.</read_first>
  <behavior>
    - Reconstruction calls validateCurrentChronicleSemantics exactly once before replay, compares every step, then exact final state/outcome/terminal anchors and full trace root.
    - Validation, first-step mismatch, later-step mismatch, final mismatch, and historical unresolved results remain distinct stable safe codes.
    - Invalid input produces no replay projection and mutates no source/execution data.
    - `validateCurrentReplayReconstruction` never calls `validateCurrentChronicle`, and validation never imports or calls reconstruction.
  </behavior>
  <action>Build the final/root mutation matrix plus call-count/no-recursion RED tests first. Make reconstruction call the package-internal semantic core directly exactly once, then compose the transition comparator. Explicitly prohibit calls to the public `validateCurrentChronicle` wrapper and preserve original input bytes with bounded safe mismatch data.</action>
  <verify><automated>pnpm exec vitest run packages/replay/src/reconstruct.test.ts packages/replay/src/replay-transition.test.ts &amp;&amp; pnpm --filter @cowards/replay typecheck</automated></verify>
  <acceptance_criteria>Reconstruction equivalence is exact at every step/final root, semantic validation executes once, and the call graph is acyclic for current and historical evidence.</acceptance_criteria>
  <done>Chronicle replay is provably equivalent to canonical engine execution without recursive validation.</done>
</task>
</tasks>

## Artifacts this phase produces

- `compareCurrentReplayTransitionV137` and stable first-mismatch codes.
- `validateCurrentReplayReconstruction` returning exact transition/final/trace-root equivalence.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| Final-root equality hides an earlier divergent step | high | Per-transition comparison and first mismatch stop. |
| Replay reimplements rules and drifts | high | Canonical transition application only. |
| Current refactor changes historical result | high | Frozen explicit historical dispatch and digest tests. |
</threat_model>
<verification>Run replay transition/reconstruction suites and historical immutable proof.</verification>
<success_criteria>Every accepted Chronicle reconstructs the same transitions, final state, outcome, and root as the engine.</success_criteria>
<output>Create `259-14-SUMMARY.md`.</output>
