---
phase: 259-executable-four-language-and-chronicle-conformance
plan: "28"
type: execute
wave: 8
depends_on: ["259-19"]
files_modified:
  - packages/persistence/migrations/0024_runtime_authority_import_trust_roots.sql
  - packages/persistence/src/schema.ts
  - packages/persistence/src/migrations.test.ts
  - packages/persistence/src/runtime-evidence-authority-publisher.ts
  - packages/persistence/src/runtime-evidence-authority-publisher.test.ts
  - scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.ts
  - scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.test.ts
  - .planning/artifacts/v1.37-runtime-authority-import-trust-roots-bootstrap.json
autonomous: true
requirements: [CONF-04, CONF-05]
must_haves:
  truths:
    - "First certificate import extends the existing plural `COWARDS_RUNTIME_AUTHORITY_IMPORT_TRUST_ROOTS_PATH` and `RuntimeEvidenceAuthorityImportTrustRoot` mechanism rather than inventing a singular or circular trust root."
    - "A protected public-root descriptor is strict, bounded, permission-checked, canonical-hashed, and matched to non-secret expected descriptor hash, producer ID, key ID, and trust domain before bootstrap."
    - "The authenticated descriptor identity is pinned append-only in the existing authority deployment/high-water path before any conformance certificate is signed or imported."
    - "Operator import trust and runtime producer trust remain distinct; neither root can satisfy the other's verification boundary."
  artifacts:
    - path: packages/persistence/migrations/0024_runtime_authority_import_trust_roots.sql
      provides: Append-only deployment/high-water pins for authenticated plural import trust-root descriptors
    - path: scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.ts
      provides: Protected descriptor validation and pre-sign transactional bootstrap command
    - path: .planning/artifacts/v1.37-runtime-authority-import-trust-roots-bootstrap.json
      provides: Public-safe exact descriptor/high-water bootstrap receipt
  key_links:
    - from: COWARDS_RUNTIME_AUTHORITY_IMPORT_TRUST_ROOTS_PATH
      to: scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.ts
      via: existing plural descriptor is size/permission/schema/hash/pin verified before database high-water installation
    - from: scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.ts
      to: packages/persistence/src/runtime-evidence-authority-publisher.ts
      via: exact RuntimeEvidenceAuthorityImportTrustRoot descriptor identity is transactionally pinned before first import
    - from: .planning/artifacts/v1.37-runtime-authority-import-trust-roots-bootstrap.json
      to: scripts/sign-v1-37-language-conformance-certificate.ts
      via: signing/import refuses to start without the matching authenticated plural-root high-water receipt
---
<objective>Bootstrap the existing plural operator import trust roots safely before external signing or certificate import.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/259-executable-four-language-and-chronicle-conformance/259-19-SUMMARY.md @.planning/phases/256-counted-safety-and-canonical-authority/256-18-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add append-only plural-root deployment high-water pins</name>
  <files>packages/persistence/migrations/0024_runtime_authority_import_trust_roots.sql, packages/persistence/src/schema.ts, packages/persistence/src/migrations.test.ts</files>
  <read_first>Migration 0023 and existing authority publication/high-water tables; RuntimeEvidenceAuthorityImportTrustRoot exact identity; append-only trigger conventions.</read_first>
  <behavior>
    - Migration 0024 follows 0023 and adds only the existing authority deployment/high-water extension keyed by canonical descriptor hash plus producer/key/domain/public-key fingerprint.
    - Rows are append-only, generation-monotonic, conflict-detecting, and cannot store private key/path or runtime producer-trust assertions.
    - Re-bootstrap of identical bytes is idempotent; same identity with changed descriptor/key or generation rollback rejects.
  </behavior>
  <action>Write ordering, append-only, idempotency, hash/key/domain/producer conflict, rollback, and private-field RED tests. Add the deployment/high-water extension without creating a second runtime evidence registry or changing prior migrations.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/migrations.test.ts --testTimeout=15000</automated></verify>
  <acceptance_criteria>The existing authority path can pin one exact plural public-root descriptor before import with no secret or parallel runtime trust.</acceptance_criteria>
  <done>First-import trust has a durable non-circular high-water anchor.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Authenticate and bootstrap the protected plural descriptor before signing</name>
  <files>packages/persistence/src/runtime-evidence-authority-publisher.ts, packages/persistence/src/runtime-evidence-authority-publisher.test.ts, scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.ts, scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.test.ts, .planning/artifacts/v1.37-runtime-authority-import-trust-roots-bootstrap.json</files>
  <read_first>Task-1 high-water API; existing plural loader in publish-v1-37-runtime-evidence-authority.ts; RuntimeEvidenceAuthorityImportTrustRoot verification; protected-file permission patterns.</read_first>
  <behavior>
    - Bootstrap requires `COWARDS_RUNTIME_AUTHORITY_IMPORT_TRUST_ROOTS_PATH`, `COWARDS_RUNTIME_AUTHORITY_IMPORT_TRUST_ROOTS_EXPECTED_SHA256`, `COWARDS_RUNTIME_AUTHORITY_IMPORT_PRODUCER_ID`, `COWARDS_RUNTIME_AUTHORITY_IMPORT_KEY_ID`, and `COWARDS_RUNTIME_AUTHORITY_IMPORT_TRUST_DOMAIN`.
    - Descriptor is at most 64 KiB, regular-file, owner-controlled/not group-or-world-writable, strict array of closed roots, canonical encoded, and hash/pin exact.
    - Transaction rechecks the descriptor and high-water under lock; changed bytes, substitution, TOCTOU, unknown/duplicate root, or existing conflict writes nothing.
    - Receipt contains only descriptor hash, selected producer/key/domain/public-key fingerprint, generation, and installed/idempotent status.
  </behavior>
  <action>Write missing pin, singular-env substitution, size/type/permission/schema, duplicate, hash/key/domain/producer mismatch, TOCTOU, runtime-producer-trust substitution, conflict, rollback, and poison RED tests. Extend the existing plural loader/API and bootstrap it transactionally; never accept the external signer private key/path as import trust.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.test.ts packages/persistence/src/runtime-evidence-authority-publisher.test.ts --testTimeout=15000 &amp;&amp; pnpm exec tsx scripts/bootstrap-v1-37-runtime-authority-import-trust-roots.ts --write-safe-receipt=.planning/artifacts/v1.37-runtime-authority-import-trust-roots-bootstrap.json --check</automated></verify>
  <acceptance_criteria>The exact existing plural public-root descriptor is authenticated and high-water pinned before signing, with operator import trust isolated from runtime producer trust.</acceptance_criteria>
  <done>The external signing/import plan has a non-circular trusted public verification root.</done>
</task>
</tasks>

## Artifacts this phase produces

- Existing plural import-root bootstrap/high-water extension.
- Public-safe exact bootstrap receipt.

<threat_model>
| STRIDE threat | Severity | Mitigation |
|---|---|---|
| First import trusts an attacker-supplied key | high | Protected descriptor plus independent expected hash/producer/key/domain pins and locked high-water. |
| Import root becomes runtime producer trust | high | Disjoint schemas, APIs, negative substitutions, and no shared pass flag. |
| Descriptor changes between check/import | high | Re-read/re-hash inside serializable locked transaction. |
| Secret signer material leaks into bootstrap | high | Public-only descriptor and poison/closed-field tests. |
</threat_model>
<verification>Run migration, descriptor, permissions, expected-pin, TOCTOU, high-water, rollback, privacy, and safe-receipt suites.</verification>
<success_criteria>Certificate import starts from an authenticated existing plural operator trust mechanism without circular or singular trust.</success_criteria>
<output>Create `259-28-SUMMARY.md`.</output>
