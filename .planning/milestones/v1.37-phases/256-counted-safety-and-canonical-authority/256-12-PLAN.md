---
phase: 256-counted-safety-and-canonical-authority
plan: "12"
type: execute
wave: 8
depends_on: ["256-04", "256-10", "256-15", "256-17", "256-18"]
files_modified:
  - apps/go-backend/live_backend.go
  - apps/go-backend/integrity_creation_test.go
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "The normal Go createExhibitionMatchSet path independently verifies current exact evidence for every entrant before its transaction and rejects unsupported counted/exhibition purposes before any insert."
    - "Inside the creation transaction and before any insert, Go locks and requires one immutable successful installed-publication receipt matching the mounted authority generation, payload hash, envelope hash, publication ID, and exact verified source set."
    - "Missing, failed, uncertain, unreconciled, or mismatched installation provenance creates no MatchSet, entrant, Match, or job rows."
    - "Accepted creation persists one semantic tuple, the installed-publication receipt identity, a normalized complete per-entrant evidence set/hash, each entrant's exact lane/certificates, and each Match/job's ordered bottom/top pair."
    - "Rejected or drifted input creates zero rows in every involved table."
  artifacts:
    - path: "apps/go-backend/live_backend.go"
      provides: "Exact-evidence normal exhibition/MatchSet creation writer"
    - path: "apps/go-backend/integrity_creation_test.go"
      provides: "Real PostgreSQL receipt provenance, reconciliation, identity propagation, and zero-row creation proof"
  key_links:
    - from: "apps/go-backend/live_backend.go"
      to: "apps/go-backend/integrity_evidence.go"
      via: "evaluates current exact identity, then locks the matching immutable installed-publication receipt before the first insert and carries both through every record"
      pattern: "createExhibitionMatchSet|installed.*publication.*receipt"
---

<objective>
Fix the normal Go MatchSet creation path rather than only lifecycle/read helpers.

Purpose: `createExhibitionMatchSet` directly inserts MatchSets, entrants, Matches, and jobs, so AUTH-02 requires it to be an exact-identity writer and must not treat a merely mounted or post-rename-unreceipted envelope as installed authority. Output: pre-transaction evaluation, in-transaction immutable receipt lock, full record propagation, and zero-row PostgreSQL proof.
</objective>

## Artifacts this phase produces

- A single current exact scheduling decision resolved before Go exhibition creation begins writing.
- An immutable installed-publication receipt locked inside the creation transaction and matched to the exact mounted generation, payload/envelope hashes, publication ID, and verified source set before the first insert.
- Identity columns on all directly created Go records.
- Dedicated database tests for counted/exhibition eligibility, receipt failure/uncertainty/reconciliation, per-field mismatch, and transaction rollback.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-10-SUMMARY.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-18-SUMMARY.md
@apps/go-backend/live_backend.go
@apps/go-backend/integrity_evidence.go
@packages/persistence/migrations/0012_integrity_authority.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evaluate exact lane evidence before Go MatchSet creation</name>
  <read_first>
    - apps/go-backend/live_backend.go
    - apps/go-backend/integrity_evidence.go
    - apps/go-backend/main_test.go
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>apps/go-backend/live_backend.go, apps/go-backend/integrity_creation_test.go</files>
  <behavior>
    - `createExhibitionMatchSet` independently verifies the signed authority bundle and evaluates each entrant's exact lane/certificates before `server.pool.Begin`, then inside that transaction locks and requires the immutable successful installed-publication receipt for the exact mounted generation, payload hash, envelope hash, publication ID, and verified source set before the first insert (D-04).
    - Counted creation requires current exact containment and conformance for every entrant; explicit non-counted creation requires current exact containment for every entrant (D-02, D-03).
    - Two-to-eight mixed-language entrants remain independently identified; one failing entrant fails the aggregate closed and production counted creation remains impossible while the Phase-259 conformance producer set is empty.
    - Missing, failed, uncertain, unreconciled, or mismatched receipts—including the post-rename/pre-receipt reconciliation window—return a safe system-owned validation result and write zero MatchSet, entrant, Match, or job rows.
    - Missing, stale, revoked, mixed, unknown, partial, or generation-drifted evidence returns a safe validation error and opens no transaction/writes zero rows.
  </behavior>
  <action>Add a dedicated creation test with authority-loader/Begin spies and real PostgreSQL fixtures. Replace `runtimeAllowsCountedPlay`/provider-proof creation authority with independent verification of the signed mounted publication, deployment pin, durable high-water anchor, and one per-entrant evidence decision map under the selected semantic tuple/policy. Evaluate after owned entrant loading but before `Begin`; do not require executable lanes to be identical. Inside the creation transaction, before duplicate/rate-limit/arena helpers or any insert can write, lock the publication ledger/head and require exactly one immutable successful installed-publication receipt from Plan 18 whose publication ID, registry generation, payload hash, envelope hash, tuple-manifest identity, and sorted verified source-ID/hash set equal the mounted bundle and evaluation snapshot. A prepared, failed, uncertain, post-rename/pre-receipt, stale, superseded, missing, or mismatched record must roll back with zero creation rows. Support only Plan-18's deterministic reconciliation path: after it verifies the durable target and appends the exact idempotent installed receipt, the same creation succeeds. Preserve public error safety and do not expose internal evidence reasons, paths, source IDs, or anchor contents.</action>
  <verify><automated>cd apps/go-backend &amp;&amp; go test ./... -run 'TestCreateExhibitionMatchSet.*Integrity' -count=1</automated></verify>
  <acceptance_criteria>
    - Initially rejected input does not call Begin; evidence drift detected after Begin still performs zero inserts.
    - Receipt rejection occurs under transaction lock before every creation insert and covers missing, failed, uncertain, source-set mismatch, and post-rename/pre-receipt states.
    - A reconciled exact receipt succeeds without regenerating or re-signing authority.
    - Counted/exhibition purpose floors match D-02 exactly.
    - Provider proof alone cannot create work.
  </acceptance_criteria>
  <done>Normal Go creation fails closed before transaction on unproved identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist one tuple, every entrant's evidence, and ordered Match pairs</name>
  <read_first>
    - apps/go-backend/live_backend.go
    - apps/go-backend/integrity_creation_test.go
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>apps/go-backend/live_backend.go, apps/go-backend/integrity_creation_test.go</files>
  <behavior>
    - Accepted creation stores tuple ID/expansion, installed-publication ID/receipt ID/generation/payload/envelope/source-set hashes, and the normalized complete entrant set/hash on MatchSet; each entrant stores its authority bundle/lane/certificate/decision data; every Match/job stores the correct ordered bottom/top pair (D-05, D-08, AUTH-02).
    - No child row can substitute or omit identity.
    - A forced failure on the final child insert rolls back every record family.
    - Claim, execution-response acceptance, and completion can re-resolve the persisted receipt identity; if that receipt later becomes failed, uncertain, superseded incompatibly, or otherwise unverifiable, the work follows the system-failure path and no gameplay, Chronicle, memory, standings, or player-penalty mutation is accepted (D-04).
  </behavior>
  <action>Extend the inserts in `createExhibitionMatchSet` with the tuple, immutable installed-publication/receipt identity and exact source-set hash, normalized entrant set/hash, generic execution-entrant rows, competition-entrant links, and ordered Match/job pairs; keep `match_set_matches` as linkage only. Make the persisted receipt reference part of the immutable claim/runtime/completion snapshot consumed by Plan 11 so later receipt uncertainty cannot be mistaken for player failure or accepted gameplay. Add configured-PostgreSQL queries for two-to-eight heterogeneous entrants asserting receipt/source-set provenance, complete map coverage and correct per-Match side order, zero rows for every rejected identity/receipt class, successful reconciled receipt creation, and zero rows after a forced late insert failure.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; cd apps/go-backend &amp;&amp; COWARDS_GO_BACKEND_TEST_DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game go test ./... -run 'TestCreateExhibitionMatchSet.*Integrity' -count=1</automated></verify>
  <acceptance_criteria>
    - MatchSet has one semantic tuple/full set, each entrant has its own exact evidence, and Matches/jobs have the correct ordered pair.
    - Every record family is traceable to the same exact immutable installed-publication receipt and verified source set.
    - Rejected input creates zero rows across all involved tables.
    - Late failure rolls back the complete creation transaction.
  </acceptance_criteria>
  <done>The normal Go creation path is exact, transactional, and database-proved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Web/API creation input to direct Go SQL writer | A bypass here can create claimable records without exact proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-14 | Elevation of Privilege/Tampering | createExhibitionMatchSet | high | mitigate | Pre-Begin exact evaluation, in-transaction installed-receipt lock/source-set equality before insert, immutable propagation, reconciliation proof, and zero-row PostgreSQL tests. |
| T-256-SC | Tampering | package supply chain | low | accept | Existing Go/PostgreSQL stack only. |
</threat_model>

<verification>Run dedicated unit spies and configured-PostgreSQL creation tests for missing/failed/uncertain/mismatched and post-rename/pre-receipt provenance, successful reconciliation, full identity propagation, and late-failure rollback.</verification>
<success_criteria>The normal Go creation path cannot write new work without exact entrant evidence and a matching immutable successful installed-publication receipt; later receipt uncertainty fails as system failure without gameplay mutation.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-12-SUMMARY.md` when done.</output>
