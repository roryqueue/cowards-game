---
phase: 256-counted-safety-and-canonical-authority
plan: "17"
type: execute
wave: 4
depends_on: ["256-01", "256-02", "256-03"]
files_modified:
  - packages/spec/src/runtime-evidence-authority-bundle.ts
  - packages/spec/src/runtime-evidence-authority-bundle.test.ts
  - packages/spec/src/runtime-execution-service.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/index.ts
  - packages/spec/artifacts/v1.37-runtime-evidence-authority-vectors.json
  - scripts/generate-v1-37-runtime-evidence-authority-vectors.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-01, AUTH-03]
must_haves:
  truths:
    - "Go scheduling and runtime-service consume one exact signed authority-bundle format/hash but verify it independently with separately configured public keys."
    - "Both consumers enforce a deployment-pinned bootstrap floor and a protected durable generation/hash high-water anchor before accepting a bundle, so process restart cannot roll authority backward."
    - "Requests reference a bundle hash/generation, tuple, and ordered entrant certificate IDs only; they cannot carry trusted certificate contents."
    - "The production bundle has a closed evidence graph, revocations, operator kill switches, and no conformance certificates until Phase 259."
  artifacts:
    - path: "packages/spec/src/runtime-evidence-authority-bundle.ts"
      provides: "Strict single-file signed-envelope/payload contract, bootstrap/high-water anti-rollback contract, and deterministic fixture-vector renderer"
    - path: "packages/spec/artifacts/v1.37-runtime-evidence-authority-vectors.json"
      provides: "Cross-language exact payload/hash/signature and invalid-mutation vectors"
  key_links:
    - from: "packages/spec/src/runtime-execution-service.ts"
      to: "packages/spec/src/runtime-evidence-authority-bundle.ts"
      via: "transport carries authority identity references rather than certificate-shaped trust"
      pattern: "authorityBundleHash|registryGeneration"
---

<objective>
Define the production authority channel shared by Go and runtime-service without introducing a database dependency or request-echo trust.

Purpose: D-04 requires current independent execution-time evidence. A signed, atomically replaceable local bundle plus deployment pin and durable high-water anchor lets both owners verify the same authority bytes/hash independently without becoming vulnerable to restart rollback.
Output: Strict signed-envelope and anti-rollback contracts, reference-only request schema, deterministic cross-language vectors, and refresh semantics.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-RESEARCH.md
@packages/spec/src/runtime-evidence.ts
@packages/spec/src/runtime-execution-service.ts
@packages/spec/src/schemas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define one bounded signed authority-bundle envelope and payload</name>
  <read_first>
    - packages/spec/src/runtime-evidence.ts
    - packages/spec/src/integrity-authority.ts
    - packages/replay/src/hash.ts
  </read_first>
  <files>packages/spec/src/runtime-evidence-authority-bundle.ts, packages/spec/src/runtime-evidence-authority-bundle.test.ts, packages/spec/src/index.ts</files>
  <behavior>
    - A single bounded envelope contains schema version, production/test trust-domain ID, key ID, Ed25519 algorithm, exact payload bytes as base64, and signature; payload SHA-256 is over those exact bytes.
    - The strict payload contains bundle version/hash inputs, monotonic registry generation, issued/valid-from/valid-until, semantic tuple-manifest hash, trusted attestation/import graph, exact derived certificates, revocations/supersessions, and operator lane disables.
    - Graph references are closed and exact; duplicate IDs, dangling nodes, unverified imports, stale validity, generation regression, unknown keys, and any fixture-domain bundle in production reject.
    - Consumer bootstrap configuration pins a minimum generation and bundle hash; a strict durable high-water record pins the greatest accepted generation/hash and rejects lower generations or the same generation with another hash after restart.
  </behavior>
  <action>Define a bounded Phase-256 artifact format, not the general canonical JSON ABI owned by Phase 258. The signed unit is the decoded payload byte string; the outer envelope is strict and size-capped. Provide pure strict parse/graph/freshness/reference resolution helpers, leaving signature verification to each service's platform crypto. Define a strict non-secret high-water record containing schema version, generation, and payload hash, plus the comparison rules shared by Node and Go: reject below the deployment pin, reject below durable high-water, reject same-generation/different-hash, and require a valid newer anchor to be durably installed before the bundle becomes executable authority. Initial bootstrap is allowed only under an explicit bootstrap mode with an exact deployment-pinned generation/hash, after which normal startup requires the durable anchor. Specify refresh as write complete envelope to a same-filesystem temporary file, fsync/close, atomic rename, and parent-directory fsync; consumers open/read one file descriptor per check. Keep production containment/conformance inventories empty by default and test trust-domain values impossible to accept in production.</action>
  <verify><automated>pnpm --filter @cowards/spec exec vitest run src/runtime-evidence-authority-bundle.test.ts</automated></verify>
  <acceptance_criteria>
    - One exact payload byte string yields one hash across languages.
    - Closed graph, freshness, generation, and production trust-domain rules fail closed.
    - Atomic replacement semantics cannot expose a half-old/half-new bundle.
  </acceptance_criteria>
  <done>The authority bundle is bounded, signed, closed, refreshable, and language neutral.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make runtime requests reference authority identities and publish vectors</name>
  <read_first>
    - packages/spec/src/runtime-evidence-authority-bundle.ts
    - packages/spec/src/runtime-execution-service.ts
    - packages/spec/src/schemas.ts
    - scripts/generate-v1-37-integrity-authority.ts
  </read_first>
  <files>packages/spec/src/runtime-execution-service.ts, packages/spec/src/schemas.ts, packages/spec/src/runtime-evidence-authority-bundle.test.ts, packages/spec/artifacts/v1.37-runtime-evidence-authority-vectors.json, scripts/generate-v1-37-runtime-evidence-authority-vectors.ts</files>
  <behavior>
    - A request carries authority bundle payload hash/generation, semantic tuple ID/expansion, and ordered bottom/top entrant/revision/lane/certificate IDs/hashes; certificate bodies, graph nodes, signatures, and gate results are forbidden.
    - Deterministic vectors include valid empty-production and fixture-domain envelopes plus bad signature/hash/key, stale/future, generation, revocation, dangling graph, reference, replacement, restart rollback, same-generation fork, corrupt anchor, and bootstrap cases.
    - Repeated vector generation is byte-identical and `--check` detects drift; vectors and fixture keys are labeled non-production and cannot promote a normal lane.
  </behavior>
  <action>Refine the shared request schema from Plan 03 to reference-only authority identity. Build deterministic cross-language payload/hash/signature/invalid vectors using an obvious fixture-only Ed25519 key and trust domain. Never commit a production private key or a production-valid certificate. Include expected Node and Go verification outcomes for bootstrap pins, durable high-water comparisons, restart rollback, revocation/kill-switch rollback, corrupt anchors, and atomic replacement sequences. Add `--write`/`--check` generation and stale-artifact tests.</action>
  <verify><automated>pnpm exec tsx scripts/generate-v1-37-runtime-evidence-authority-vectors.ts --write &amp;&amp; pnpm exec tsx scripts/generate-v1-37-runtime-evidence-authority-vectors.ts --check &amp;&amp; pnpm --filter @cowards/spec exec vitest run src/runtime-evidence-authority-bundle.test.ts src/spec.test.ts -t "authority bundle|execution evidence identity"</automated></verify>
  <acceptance_criteria>
    - Transport cannot make certificate contents authoritative.
    - Go and Node have exact shared bytes/hash and negative vectors.
    - No committed key/fixture can validate as production evidence.
  </acceptance_criteria>
  <done>The services share one authority identity contract while retaining independent verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mounted authority file and configured public key to Go/runtime-service | A forged, stale, partially replaced, or request-echoed bundle could authorize execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-19 | Spoofing/Tampering/TOCTOU | signed authority bundle | high | mitigate | Exact bytes/hash, Ed25519 with separate key, bounded strict envelope, closed graph, deployment pin, durable high-water anchor, atomic rename/fsync, and independent Node/Go vectors. |
| T-256-SC | Tampering | package supply chain | low | accept | Node/Go platform cryptography only. |
</threat_model>

<verification>Run deterministic generation/check and the full strict/negative vector suite.</verification>
<success_criteria>One independently verifiable authority channel replaces injected/request-echo trust and remains empty of production conformance evidence.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-17-SUMMARY.md` when done.</output>
