---
phase: 256-counted-safety-and-canonical-authority
plan: "15"
type: execute
wave: 5
depends_on: ["256-01", "256-02", "256-04", "256-17"]
files_modified:
  - packages/spec/src/runtime-evidence-attestation.ts
  - packages/spec/src/runtime-evidence-attestation.test.ts
  - packages/spec/src/index.ts
  - packages/persistence/src/runtime-evidence-import.ts
  - packages/persistence/src/runtime-evidence-import.test.ts
  - packages/persistence/src/index.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-01, AUTH-03]
must_haves:
  truths:
    - "Certificate-shaped records, documentation, gate names, and fixtures cannot promote a lane; only successful verification of a closed trusted attestation graph may append a certificate."
    - "Production has no authorized conformance producer in Phase 256, so no runtime lane can become counted before Phase 259 executable conformance evidence exists."
    - "Every imported certificate binds the exact producer, schema, command, corpus, policy, toolchain, artifact, tuple, lane, result digests, validity window, and complete verifiable graph."
  artifacts:
    - path: "packages/spec/src/runtime-evidence-attestation.ts"
      provides: "Closed attestation/trust-root/graph contracts and pure verification"
      exports: ["verifyRuntimeEvidenceAttestation", "RUNTIME_EVIDENCE_TRUSTED_PRODUCERS"]
    - path: "packages/persistence/src/runtime-evidence-import.ts"
      provides: "Sole transaction that appends verified imports and derived certificates"
  key_links:
    - from: "packages/persistence/src/runtime-evidence-import.ts"
      to: "packages/spec/src/runtime-evidence-attestation.ts"
      via: "successful closed-graph verification is required before the certificate insert"
      pattern: "verifyRuntimeEvidenceAttestation"
---

<objective>
Replace certificate-shaped promotion with a closed verified-evidence attestation and import path.

Purpose: D-01 through D-03 and SAFE-01 forbid promotion by declarations. Phase 256 must provide the verification substrate while keeping production counted eligibility impossible until Phase 259 supplies trusted executable conformance evidence.
Output: Strict attestation contracts, exhaustive forgery tests, and one append-only transactional import service.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-RESEARCH.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-04-SUMMARY.md
@packages/spec/src/runtime-evidence.ts
@packages/persistence/migrations/0012_integrity_authority.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify a closed exact evidence-attestation graph</name>
  <read_first>
    - packages/spec/src/runtime-evidence.ts
    - packages/spec/src/integrity-authority.ts
    - packages/spec/src/public-output-privacy.ts
  </read_first>
  <files>packages/spec/src/runtime-evidence-attestation.ts, packages/spec/src/runtime-evidence-attestation.test.ts, packages/spec/src/index.ts</files>
  <behavior>
    - An attestation binds kind, schema/version, producer/key/trust domain, exact command ID/digest, corpus ID/hash, policy ID/hash, runtime/toolchain/adapter/artifact/build identities, semantic tuple, lane identity, result manifest/digests, gate results, issued/validity times, and a closed node/edge graph.
    - Verification rejects unknown/untrusted/wrong-kind producers, bad signatures, missing bytes, digest mismatch, dangling/unreachable/duplicate graph nodes, failed or omitted gates, aliases, renamed gates, docs-only claims, stale results, and any identity mismatch.
    - Production trusted containment and conformance producer registries begin empty. Test producers are explicitly fixture-domain and production verification rejects them; Phase 259 must add a separately reviewed conformance producer before counted certificates are possible.
  </behavior>
  <action>Define immutable strict contracts and a pure verifier using platform cryptography and caller-supplied bytes/time/trust roots. Require every referenced producer, schema, command, corpus, policy, toolchain, artifact, result, and graph node to be present, hash-verifiable, reachable from the signed root, and exact for the certificate kind. Return a verified-attestation value that cannot be constructed by plain object shape. Keep production trusted-producer sets empty in this phase. Add forgery matrices for certificate-shaped objects, documentation, renamed gates, missing artifacts/bytes, dangling graphs, fixture-domain trust, wrong signatures, and digest/identity drift.</action>
  <verify><automated>pnpm --filter @cowards/spec exec vitest run src/runtime-evidence-attestation.test.ts</automated></verify>
  <acceptance_criteria>
    - Only a complete signed and closed exact graph yields a verified value in test trust domain.
    - Production mode rejects every fixture and has zero conformance producers/certificates.
    - No boolean approval, name match, or record shape can bypass graph verification.
  </acceptance_criteria>
  <done>Attestation verification is executable, closed, exact, and cannot be satisfied by declaration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make verified import the sole certificate creation path</name>
  <read_first>
    - packages/spec/src/runtime-evidence-attestation.ts
    - packages/persistence/migrations/0012_integrity_authority.sql
    - packages/persistence/src/integrity-evidence.ts
    - packages/persistence/src/db.ts
  </read_first>
  <files>packages/persistence/src/runtime-evidence-import.ts, packages/persistence/src/runtime-evidence-import.test.ts, packages/persistence/src/index.ts</files>
  <behavior>
    - Import verifies supplied original evidence bytes/graph before SQL, then appends the verified import, graph/result digests, and derived certificate in one transaction.
    - Database constraints reject a certificate without the exact successful verified-import foreign key or with mismatched kind/identity/digests/validity.
    - Re-import is deterministic/idempotent by attestation digest; failed verification, forged direct insert, or late SQL failure creates zero certificate/import rows.
  </behavior>
  <action>Expose `importVerifiedRuntimeEvidenceAttestation` as the only application certificate writer; remove/export-block any raw certificate insert helper from Plan 04. Reverify inside the transaction, store the immutable verified graph/result metadata, and derive—not accept—the certificate fields. Add fake-pool and real PostgreSQL tests for forged, docs-only, renamed-gate, missing-artifact, unverifiable, fixture-production, duplicate, and rollback cases. Assert the production certificate inventory is empty after migration and fixtures cannot enter normal scheduling.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; pnpm --filter @cowards/persistence exec vitest run src/runtime-evidence-import.test.ts &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence exec vitest run src/runtime-evidence-import.test.ts -t "PostgreSQL|forged|production"</automated></verify>
  <acceptance_criteria>
    - Every persisted certificate has one successful exact verified import.
    - All failed or forged imports write zero rows.
    - Phase-256 production counted eligibility is structurally impossible.
  </acceptance_criteria>
  <done>Certificates are derived only from executable verified evidence, never declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Evidence producer/artifacts to certificate store | Forged or incomplete claims could promote hostile execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-18 | Spoofing/Tampering | attestation verification/import | high | mitigate | Trusted producer keys, closed hash graph, exact identity/result verification, derived certificates, and DB foreign-key/rollback proof. |
| T-256-SC | Tampering | package supply chain | low | accept | Platform cryptography and existing dependencies only. |
</threat_model>

<verification>Run spec forgery matrices and configured-PostgreSQL import/zero-row tests.</verification>
<success_criteria>No lane can be promoted by certificate shape, prose, gate name, or fixture; production conformance remains empty until Phase 259.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-15-SUMMARY.md` when done.</output>
