---
phase: 256-counted-safety-and-canonical-authority
plan: "01"
subsystem: spec-integrity
tags: [authority-registry, semantic-tuple, sha256, deterministic-artifacts, tdd]
requires:
  - phase: v1.36
    provides: preserved v1.4 compatibility strings and competition/runtime ownership baseline
provides:
  - immutable canonical owner registry for all six authority domains
  - exact six-component semantic compatibility tuple with domain-separated hashing
  - deterministic authority manifest and language-neutral mutation vectors
affects: [phase-256-runtime-evidence, persistence, runtime-service, go-backend, phase-257-kernel, phase-260-fairness]
tech-stack:
  added: []
  patterns: [spec-owned literal registry, fixed-order NUL-delimited UTF-8 hashing, generated-artifact write-check]
key-files:
  created:
    - packages/spec/src/integrity-authority.ts
    - packages/spec/src/integrity-authority.test.ts
    - scripts/generate-v1-37-integrity-authority.ts
    - packages/spec/artifacts/v1.37-integrity-authority.json
    - packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json
  modified:
    - packages/spec/src/index.ts
key-decisions:
  - "The semantic tuple uses a domain-tagged, fixed-order, NUL-delimited UTF-8 encoding with explicit byte lengths; canonical JSON is not part of tuple identity."
  - "The registered v1.4 tuple formalizes arena-catalog and Set-policy identities without including provider, toolchain, adapter, artifact, build, or certificate evidence."
  - "Exact resolution requires tupleId and a complete expansion whose re-hash and every component match the registered record."
patterns-established:
  - "Atomic identity: carry tupleId and all six expanded components together and reject every partial or alias selector."
  - "Immutable authority: freeze exported registries and return fresh deeply frozen resolution records."
requirements-completed: [AUTH-01, AUTH-02, AUTH-03, AUTH-05]
coverage:
  - id: D1
    description: "One immutable package/symbol owner registry and exact six-component semantic tuple contract"
    requirement: AUTH-01
    verification:
      - kind: unit
        ref: "packages/spec/src/integrity-authority.test.ts#v1.37 canonical integrity authority"
        status: pass
      - kind: other
        ref: "pnpm --filter @cowards/spec typecheck"
        status: pass
    human_judgment: false
  - id: D2
    description: "Deterministic committed authority manifest and language-neutral base plus mutation hash vectors"
    requirement: AUTH-02
    verification:
      - kind: unit
        ref: "packages/spec/src/integrity-authority.test.ts#committed artifact and hash vectors"
        status: pass
      - kind: other
        ref: "pnpm exec tsx scripts/generate-v1-37-integrity-authority.ts --check"
        status: pass
    human_judgment: false
duration: 6min
completed: 2026-07-13
status: complete
---

# Phase 256 Plan 01: Canonical Authority and Semantic Tuple Summary

**An immutable six-domain authority registry now binds the preserved v1.4 semantic tuple to deterministic, language-neutral SHA-256 bytes without conflating executable evidence identity.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-07-13T01:14:00Z
- **Completed:** 2026-07-13T01:19:48Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Registered exactly one distinct package/symbol owner for rules, transition semantics, runtime classification, Chronicle validation, arena authority, and Set scheduling policy.
- Added exact tuple encoding, hashing, immutable lookup, and rejection of partial, wildcard, alias, unknown, mixed, or mutation attempts.
- Generated byte-stable authority and cross-language vector artifacts covering the registered v1.4 tuple and every single-component mutation.

## Task Commits

Each TDD task was committed atomically through RED and GREEN:

1. **Task 1 RED: authority and tuple behavior** - `9d51f66` (test)
2. **Task 1 GREEN: canonical authority module** - `4a92ecf` (feat)
3. **Task 2 RED: generated artifact behavior** - `5e039ac` (test)
4. **Task 2 GREEN: generator and committed artifacts** - `731a886` (feat)

## Files Created/Modified

- `packages/spec/src/integrity-authority.ts` - Canonical domains, owner registry, exact tuple encoding/hash, immutable registry, and resolver.
- `packages/spec/src/integrity-authority.test.ts` - Ownership, identity, mutation, immutability, stale-artifact, and vector regression coverage.
- `packages/spec/src/index.ts` - Public package export for downstream canonical consumers.
- `scripts/generate-v1-37-integrity-authority.ts` - Deterministic `--write` and fail-closed `--check` generator.
- `packages/spec/artifacts/v1.37-integrity-authority.json` - Stable ownership and registered tuple manifest.
- `packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json` - Exact UTF-8 byte, base64, hex, tuple ID, and SHA-256 vectors.

## Decisions Made

- Used platform SHA-256 over an explicitly documented binary encoding rather than JSON, reserving canonical JSON semantics for Phase 258.
- Kept semantic tuple identity free of executable lane/provider/toolchain/corpus/build evidence so recertification can stale independently, per D-08.
- Returned cloned frozen resolution records in addition to freezing the exported registry, preventing callers from mutating future lookup behavior.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Exported the canonical module from `@cowards/spec`**
- **Found during:** Task 1
- **Issue:** A source-only module would not be consumable through the package's sole public export, leaving downstream owners to import an internal path.
- **Fix:** Added the canonical module to `packages/spec/src/index.ts`.
- **Files modified:** `packages/spec/src/index.ts`
- **Verification:** `pnpm --filter @cowards/spec typecheck` and the full spec suite passed.
- **Committed in:** `4a92ecf`

**2. [Rule 3 - Blocking] Kept generator verification outside the spec TypeScript project boundary**
- **Found during:** Task 2
- **Issue:** Directly importing the repository-level generator from a spec test violated the package `rootDir`/composite project boundary.
- **Fix:** The test reads committed artifacts and invokes the generator's `--check` CLI, preserving stale-output proof without cross-project compilation.
- **Files modified:** `packages/spec/src/integrity-authority.test.ts`
- **Verification:** Focused Vitest, spec typecheck, generator check, and full spec tests passed.
- **Committed in:** `731a886`

---

**Total deviations:** 2 auto-fixed (1 missing critical functionality, 1 blocking integration issue).
**Impact on plan:** Both changes preserve the planned architecture and make the authority contract safely consumable; no gameplay semantics changed.

## Issues Encountered

- The RED commits intentionally failed because their new modules/artifacts did not yet exist. Both corresponding GREEN commits now pass the focused suite.

## User Setup Required

None - no external service configuration required.

## Verification

- `pnpm exec tsx scripts/generate-v1-37-integrity-authority.ts --check` - passed.
- `pnpm --filter @cowards/spec exec vitest run src/integrity-authority.test.ts` - 6/6 passed.
- `pnpm --filter @cowards/spec typecheck` - passed.
- `pnpm --filter @cowards/spec test` - 70/70 passed.
- Repeated `--write` output produced no artifact diff.

## Next Phase Readiness

- The exact semantic tuple and owner manifest are ready for Plan 256-02 runtime evidence modeling and later Go hash-vector parity.
- No blockers. Existing v1.4 gameplay and the user-owned consolidated-spec edit were untouched.

## Self-Check: PASSED

- All five planned created artifacts exist.
- All four RED/GREEN task commits exist.
- Focused, typecheck, full spec, generator stale-check, and deterministic rewrite checks pass.

---
*Phase: 256-counted-safety-and-canonical-authority*
*Completed: 2026-07-13*
