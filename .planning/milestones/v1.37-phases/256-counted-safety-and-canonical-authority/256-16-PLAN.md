---
phase: 256-counted-safety-and-canonical-authority
plan: "16"
type: execute
wave: 7
depends_on: ["256-05", "256-15", "256-17", "256-19"]
files_modified:
  - packages/persistence/src/competition.ts
  - packages/persistence/src/competition.test.ts
  - packages/persistence/src/workshop.ts
  - packages/persistence/src/workshop.test.ts
  - packages/persistence/src/dev-smoke.ts
  - packages/persistence/src/dev-smoke.test.ts
  - scripts/run-v1-5-advanced-demo.ts
  - scripts/check-v1-37-integrity-boundaries.ts
  - scripts/check-v1-37-integrity-boundaries.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-02, AUTH-03, AUTH-05]
must_haves:
  truths:
    - "Every active TypeScript Match/MatchSet creation caller obtains exact verified per-entrant evidence or creates zero rows."
    - "createFromPreset, createFromMatrix, competition, Workshop, dev smoke, demos, and every direct Match writer preserve heterogeneous entrants instead of inventing a singular lane."
    - "A structural inventory fails when any new service call or direct Match/MatchSet/job insert bypasses the required tuple, complete entrant set, or ordered pair."
    - "Dev/demo callers cannot reinterpret the retired direct-worker boundary as successful execution or add an alternate claim path."
  artifacts:
    - path: "scripts/check-v1-37-integrity-boundaries.ts"
      provides: "Exhaustive creation-call/direct-writer inventory with fail-closed evidence-wiring checks"
  key_links:
    - from: "packages/persistence/src/competition.ts"
      to: "packages/persistence/src/matchset-service.ts"
      via: "passes verified evidence per locked entrant into createFromMatrix"
      pattern: "executionEntrants"
---

<objective>
Migrate every active TypeScript creation caller, not just the core writer, to exact per-entrant evidence.

Purpose: AUTH-02/AUTH-03 apply to all reachable creation surfaces. Default production evidence is empty, so callers must fail before rows until exact verified containment exists; counted remains impossible before Phase 259.
Output: Fully migrated competition/Workshop/dev/demo callers plus a structural inventory that catches future bypasses.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-05-SUMMARY.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-15-SUMMARY.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-19-SUMMARY.md
@packages/persistence/src/matchset-service.ts
@packages/persistence/src/competition.ts
@packages/persistence/src/workshop.ts
@packages/persistence/src/dev-smoke.ts
@scripts/run-v1-5-advanced-demo.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate production competition and Workshop creation</name>
  <read_first>
    - packages/persistence/src/competition.ts
    - packages/persistence/src/workshop.ts
    - packages/persistence/src/matchset-service.ts
    - packages/persistence/src/runtime-evidence-import.ts
  </read_first>
  <files>packages/persistence/src/competition.ts, packages/persistence/src/competition.test.ts, packages/persistence/src/workshop.ts, packages/persistence/src/workshop.test.ts</files>
  <behavior>
    - `createManualExhibitionMatchSet`, `createWorkshopTestMatchSet`, `createFromPreset`, `createFromMatrix`, and `insertMatchSetWithMatrixOnClient` resolve every distinct entrant from verified authority, pass the semantic tuple plus complete entrant map, and derive each Match's ordered pair.
    - Missing containment on one entrant creates zero rows; all containment with missing conformance permits only explicit exhibition/Workshop; counted creation requires all entrant conformance and therefore remains impossible with the Phase-256 production producer registry.
    - Two-to-eight valid mixed-language v1.4 entrants remain representable and historical rows/results are not rewritten.
  </behavior>
  <action>Add caller-level tests before wiring. Require callers to load locked entrant revisions, resolve exact verified evidence by stable entrant key, and pass the complete map into the core writer. Change `createFromPreset` to accept or resolve explicit evidence for both entrants rather than silently constructing identity. Ensure competition side/matrix generation maps each Match to the correct keys, Workshop uses its revision and opponent independently, and every failure happens before the first insert. Test mixed-language two/three/eight entrants, one-bad-entrant aggregate failure, pair order, default empty production authority, and PostgreSQL zero rows.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; pnpm --filter @cowards/persistence exec vitest run src/competition.test.ts src/workshop.test.ts src/matchset-service.test.ts &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence exec vitest run src/competition.test.ts src/workshop.test.ts src/matchset-service.test.ts -t "integrity evidence|mixed language|zero rows"</automated></verify>
  <acceptance_criteria>
    - Every reachable production service caller passes complete per-entrant evidence.
    - One invalid entrant prevents all writes and counted is unavailable in Phase 256.
    - Valid heterogeneous entrants are not collapsed or rejected solely for language difference.
  </acceptance_criteria>
  <done>Production TypeScript MatchSet callers are exact, heterogeneous-safe, and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate dev/demo callers and enforce a complete writer inventory</name>
  <read_first>
    - packages/persistence/src/dev-smoke.ts
    - scripts/run-v1-5-advanced-demo.ts
    - scripts/check-v1-37-integrity-boundaries.ts
    - scripts/check-boundary-monitors.ts
  </read_first>
  <files>packages/persistence/src/dev-smoke.ts, packages/persistence/src/dev-smoke.test.ts, scripts/run-v1-5-advanced-demo.ts, scripts/check-v1-37-integrity-boundaries.ts, scripts/check-v1-37-integrity-boundaries.test.ts</files>
  <behavior>
    - Dev smoke and advanced demo either receive an explicit fixture-domain authority through test-only configuration or fail before creating rows; fixture evidence cannot enter production or counted results.
    - Advanced demo does not invoke or bypass the retired `apps/worker` execution path and reports execution as unavailable unless it is routed through an explicitly supported authority-checked service boundary.
    - The inventory discovers every `createMatch`, `createFromPreset`, `createFromMatrix`, `insertMatchSetWithMatrixOnClient`, and direct SQL writer for MatchSets/entrants/Matches/jobs/Chronicles and rejects unrecognized/unwired callers.
    - Synthetic bypasses in competition, Workshop, dev scripts, arbitrary new TS files, and direct SQL all fail independently.
  </behavior>
  <action>Thread explicit fixture-domain evidence through development-only functions and demo commands; make production mode reject it and never use documentation/gate labels as proof. Remove the advanced demo's direct `runWorkerOnce` execution attempt or route it only through an existing authority-checked service boundary; it may not catch the stable Plan-19 retirement error and report a Match as executed. Extend the structural monitor from a named allowlist into a repository inventory: enumerate service call sites, direct SQL record writers, and legacy-worker consumers, require each creation path to be an approved owner/caller with tuple, complete entrant-set, and ordered-pair wiring, and fail on newly discovered execution/claim paths. Include `packages/persistence/src/competition.ts`, `workshop.ts`, `dev-smoke.ts`, `scripts/run-v1-5-advanced-demo.ts`, `createFromPreset`, every `createFromMatrix`, direct `createMatch`, and TS SQL inserts in the production assertion and synthetic fixtures.</action>
  <verify><automated>pnpm --filter @cowards/persistence exec vitest run src/dev-smoke.test.ts &amp;&amp; pnpm exec vitest run scripts/check-v1-37-integrity-boundaries.test.ts -t "creation inventory|caller bypass"</automated></verify>
  <acceptance_criteria>
    - Dev/demo use only explicit non-production evidence or write nothing.
    - Inventory output accounts for every current call/direct writer and no unknown path passes.
    - Adding any unwired caller makes the structural test fail.
    - Dev/demo code cannot bypass or misreport the retired direct worker.
  </acceptance_criteria>
  <done>All active TS callers are migrated and future creation bypasses are structurally blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Service/dev/demo callers to canonical writers | A missed caller or fixture shortcut could create unproved claimable rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-20 | Elevation of Privilege/Tampering | TS creation callers and inventory | high | mitigate | Complete per-entrant resolution, zero-row tests, production/fixture trust split, and structural call/SQL inventory. |
| T-256-SC | Tampering | package supply chain | low | accept | Existing workspace dependencies only. |
</threat_model>

<verification>Run all production/dev caller tests, configured-PostgreSQL zero-row proof, and synthetic inventory failures.</verification>
<success_criteria>Every active TypeScript creation path either has verified per-entrant evidence or creates zero rows, and new bypasses fail structurally.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-16-SUMMARY.md` when done.</output>
