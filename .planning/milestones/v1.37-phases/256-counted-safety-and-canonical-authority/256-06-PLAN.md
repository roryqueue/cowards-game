---
phase: 256-counted-safety-and-canonical-authority
plan: "06"
type: execute
wave: 6
depends_on: ["256-03", "256-04", "256-15"]
files_modified:
  - packages/persistence/src/chronicle-store.ts
  - packages/persistence/src/chronicle-store.test.ts
  - packages/persistence/src/complete-match.ts
  - packages/persistence/src/complete-match.test.ts
autonomous: true
requirements: [SAFE-02, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Completion locks the Match tuple and ordered bottom/top entrant-evidence pair, rejects either side's drift before gameplay mutation, and inserts a Chronicle with that same ordered pair."
    - "Historical Chronicles remain unchanged and never receive inferred current identity."
  artifacts:
    - path: "packages/persistence/src/complete-match.ts"
      provides: "Transactional completion identity recheck"
    - path: "packages/persistence/src/chronicle-store.ts"
      provides: "Semantic-tuple and ordered bottom/top entrant-evidence Chronicle writer"
  key_links:
    - from: "packages/persistence/src/complete-match.ts"
      to: "packages/persistence/src/chronicle-store.ts"
      via: "passes the locked Match identity unchanged into Chronicle insertion"
      pattern: "integrityIdentity"
---

<objective>
Make TypeScript completion preserve the locked scheduling identity and fail safely on drift.

Purpose: D-04 and D-05 require execution/completion rechecks and exact Chronicle identity. Output: focused completion/Chronicle changes and no-mutation PostgreSQL tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-04-SUMMARY.md
@packages/persistence/src/complete-match.ts
@packages/persistence/src/chronicle-store.ts
@packages/persistence/src/integrity-evidence.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require exact identity in every new Chronicle insert</name>
  <read_first>
    - packages/persistence/src/chronicle-store.ts
    - packages/persistence/src/integrity-evidence.ts
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>packages/persistence/src/chronicle-store.ts, packages/persistence/src/chronicle-store.test.ts</files>
  <behavior>
    - New Chronicle inserts require tuple ID/expansion and the complete ordered bottom/top entrant-evidence pair from the Match (D-05, AUTH-02).
    - Partial, swapped, singular-lane, wrong-revision, or independently supplied evidence rejects atomically.
    - Historical reads remain version-routed and byte-immutable (D-06, AUTH-04).
  </behavior>
  <action>Add failing writer tests, extend the canonical insert input with the shared semantic tuple and ordered evidence pair, validate its side/revision bindings before SQL, and persist every field including authority-bundle hash/generation. Audit Chronicle insert call sites in the test suite so no active writer can omit or collapse the pair. Do not backfill historical rows or infer a current tuple from Chronicle metadata.</action>
  <verify><automated>pnpm --filter @cowards/persistence exec vitest run src/chronicle-store.test.ts</automated></verify>
  <acceptance_criteria>
    - New Chronicle rows are identity-complete.
    - Partial/mixed identity produces no row.
    - Historical fixture hashes remain unchanged.
  </acceptance_criteria>
  <done>The Chronicle store records exact transitions/evidence identity without rewriting history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recheck the locked Match identity before completion mutation</name>
  <read_first>
    - packages/persistence/src/complete-match.ts
    - packages/persistence/src/complete-match.test.ts
    - packages/persistence/src/chronicle-store.ts
    - packages/persistence/src/integrity-evidence.ts
  </read_first>
  <files>packages/persistence/src/complete-match.ts, packages/persistence/src/complete-match.test.ts</files>
  <behavior>
    - `completeMatch` locks the Match and compares the response tuple and ordered bottom/top pair with its stored scheduling snapshot (D-04).
    - Missing, swapped, stale, revoked, mixed, or generation-drifted evidence on either side fails as system failure before Chronicle, Match, memory, outcome, standings, or player-penalty mutation.
    - Success copies the locked tuple and pair unchanged into Chronicle insertion.
  </behavior>
  <action>Within the existing completion transaction, lock and load the Match tuple and ordered pair, validate full per-side equality against the runtime response, and only then insert Chronicle and update completion state. Route either side's drift to the established system-failure path; never make it a player violation. Add real PostgreSQL cases for heterogeneous pairs, swaps, every drift class on each side, rollback after a late write error, and exact pair propagation.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; pnpm --filter @cowards/persistence exec vitest run src/complete-match.test.ts src/chronicle-store.test.ts &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence exec vitest run src/complete-match.test.ts -t "integrity identity|system failure"</automated></verify>
  <acceptance_criteria>
    - Drift yields only system failure and zero gameplay/player mutation.
    - Success Chronicle identity equals the locked Match snapshot.
    - PostgreSQL rollback proves no partial completion.
  </acceptance_criteria>
  <done>TypeScript completion is exact-identity, transaction-safe, and historically compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime response to completion transaction | In-flight evidence drift must not become a completed Match. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-07 | Tampering/TOCTOU | completion and Chronicle writer | high | mitigate | Row locks, exact snapshot comparison, system-failure routing, and rollback proof. |
| T-256-SC | Tampering | package supply chain | low | accept | Existing stack only. |
</threat_model>

<verification>Run focused unit and configured-PostgreSQL completion tests.</verification>
<success_criteria>Completion cannot mutate gameplay under stale identity and every new Chronicle preserves the exact scheduling snapshot.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-06-SUMMARY.md` when done.</output>
