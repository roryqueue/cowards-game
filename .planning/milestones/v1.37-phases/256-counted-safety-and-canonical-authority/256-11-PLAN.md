---
phase: 256-counted-safety-and-canonical-authority
plan: "11"
type: execute
wave: 8
depends_on: ["256-03", "256-04", "256-08", "256-09", "256-10", "256-15", "256-17", "256-18"]
files_modified:
  - apps/go-backend/orchestrator.go
  - apps/go-backend/orchestrator_test.go
  - apps/go-backend/job_lifecycle.go
  - apps/go-backend/job_lifecycle_test.go
  - apps/go-backend/runtime_service_client.go
  - apps/go-backend/completion.go
  - apps/go-backend/completion_test.go
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Go claim independently verifies the current authority bundle and its persisted immutable installed-publication receipt, rejects stale or uncertain provenance on either side before lifecycle mutation, and returns the locked tuple plus ordered pair on success."
    - "Go claim and post-response recheck accept only an installed publication at or above the restart-safe durable high-water anchor; a newer generation is anchored before use."
    - "The runtime-service request carries only that tuple, ordered bottom/top identity references, publication/receipt references, bundle hash, and generation; post-response bundle, receipt-certainty, source-set, or per-side drift becomes system failure before completion."
    - "Go completion locks the Match tuple/pair and inserts the same ordered pair into Chronicle or performs zero gameplay/player mutation."
  artifacts:
    - path: "apps/go-backend/job_lifecycle.go"
      provides: "Database-enforced pre-claim exact-evidence gate"
    - path: "apps/go-backend/runtime_service_client.go"
      provides: "Strict exact-identity service request validation"
    - path: "apps/go-backend/completion.go"
      provides: "Transactional completion/Chronicle identity preservation"
  key_links:
    - from: "apps/go-backend/orchestrator.go"
      to: "apps/go-backend/runtime_service_client.go"
      via: "carries the claimed immutable authority/receipt identity and rechecks active evidence plus installed-publication certainty before completion"
      pattern: "registryGeneration|certificate|publicationReceipt"
---

<objective>
Enforce exact evidence through the normal Go claim, transport, and completion lifecycle.

Purpose: D-04 requires both pre-claim and in-flight rechecks with system-failure/no-mutation handling. Output: lifecycle SQL, strict transport identity, completion lock/copy, and database-backed proof.
</objective>

## Artifacts this phase produces

- Go claim SQL that joins and re-evaluates exact tuple/evidence state before status mutation.
- Runtime-service request DTO/validator carrying the complete scheduling snapshot.
- Completion transaction that locks, verifies, and copies exact identity into Chronicle.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-10-SUMMARY.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-18-SUMMARY.md
@apps/go-backend/orchestrator.go
@apps/go-backend/job_lifecycle.go
@apps/go-backend/runtime_service_client.go
@apps/go-backend/completion.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject stale evidence in the actual Go claim transaction</name>
  <read_first>
    - apps/go-backend/job_lifecycle.go
    - apps/go-backend/job_lifecycle_test.go
    - apps/go-backend/integrity_evidence.go
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>apps/go-backend/job_lifecycle.go, apps/go-backend/job_lifecycle_test.go</files>
  <behavior>
    - Candidate SQL locks and joins the Match/job tuple, ordered bottom/top entrant pair, authority bundle hash/generation, persisted publication/receipt identity and exact source-set hash, and both sides' certificates before any running/lease/attempt update (D-04).
    - Stale/revoked/missing/mismatched/generation-drifted evidence or a missing/failed/uncertain/unreconciled/mismatched installed-publication receipt leaves job and Match lifecycle fields byte-equivalent.
    - Valid claim returns the complete immutable identity.
  </behavior>
  <action>Modify `claimNextMatchJob` itself, not only the orchestrator. Independently load/verify the mounted publication in the same canonical signed bundle format as runtime-service, enforce the deployment pin and durable high-water anchor before use, and under the claim transaction lock the immutable installed-publication receipt persisted by creation. Require exact equality of publication ID, receipt status, registry generation, payload/envelope hashes, tuple manifest, and sorted verified source-set hash before resolving both entrant references and aggregate classification. Skip if either side, receipt certainty, publication provenance, bundle identity, source set, or anchor persistence fails, and return the tuple/pair plus publication/receipt identity and bundle hash/generation for transport. Add heterogeneous-pair SQL/unit and configured-PostgreSQL no-mutation tests including receipt becoming uncertain after creation.</action>
  <verify><automated>cd apps/go-backend &amp;&amp; go test ./... -run 'TestMatchJobLifecycle.*Integrity|TestIntegrityEvidenceDatabaseBoundary' -count=1</automated></verify>
  <acceptance_criteria>
    - Rejection precedes all lifecycle mutation.
    - Valid claim returns exact tuple/lane/certificate/decision identity.
    - Database tests cover every drift class.
  </acceptance_criteria>
  <done>Go cannot claim unproved queued work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry and recheck exact identity through runtime transport</name>
  <read_first>
    - apps/go-backend/orchestrator.go
    - apps/go-backend/orchestrator_test.go
    - apps/go-backend/runtime_service_client.go
    - packages/spec/src/runtime-execution-service.ts
  </read_first>
  <files>apps/go-backend/orchestrator.go, apps/go-backend/orchestrator_test.go, apps/go-backend/runtime_service_client.go</files>
  <behavior>
    - Request validation requires tuple ID/expansion, ordered bottom/top entrant keys/revisions/lane and certificate references, reference-only publication/installed-receipt IDs, authority bundle hash/generation, per-side decision status/reason, and evaluation/freshness instants; it carries no certificate-shaped authority payload.
    - Any missing/mixed member rejects atomically before transport (AUTH-03).
    - Active evidence and installed-receipt certainty reloaded after the response must equal the claim snapshot; mismatch calls system-failure recording and never completion (D-04).
  </behavior>
  <action>Extend the DTO/builder/validator from the claimed record and preserve the existing `claim -> build request -> execute -> complete` sequence. Send only exact references—including publication/receipt IDs—and the locked bundle hash/generation; the runtime-service still trusts only its independently mounted verified authority. After response, independently reopen/reverify the canonical mounted bundle and transactionally re-resolve the immutable installed receipt/source set before comparing the exact ordered pair. Route receipt failure/uncertainty, post-creation provenance mismatch, replacement, revocation, kill switch, generation, or per-side drift through `recordAttemptFailure` as system failure. Prove completion, gameplay, memories, standings, and player penalties are untouched.</action>
  <verify><automated>cd apps/go-backend &amp;&amp; go test ./... -run 'TestGoMatchOrchestrator.*Integrity|TestRuntimeServiceRequest.*Integrity' -count=1</automated></verify>
  <acceptance_criteria>
    - Transport rejects partial/mixed identity.
    - In-flight drift records only system failure.
    - Completion spy stays at zero on drift.
  </acceptance_criteria>
  <done>Go transport preserves and rechecks the exact claim snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify exact identity before Go completion and Chronicle insert</name>
  <read_first>
    - apps/go-backend/completion.go
    - apps/go-backend/completion_test.go
    - apps/go-backend/job_lifecycle.go
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>apps/go-backend/completion.go, apps/go-backend/completion_test.go</files>
  <behavior>
    - Completion locks the running lease, Match tuple/ordered pair, and immutable publication/receipt identity and rejects missing/swapped/mixed/drifted evidence or uncertain/mismatched receipt provenance before mutation.
    - Success writes tuple ID/expansion, publication/receipt reference, authority bundle hash/generation, source-set hash, and ordered bottom/top entrant lane/certificate/decision snapshots into Chronicle.
    - Failure leaves Chronicle, Match, memories, outcome, standings, and player penalties unchanged.
  </behavior>
  <action>Put the tuple, immutable installed-receipt/source-set resolution, and full ordered-pair comparison inside the completion transaction and pass the locked snapshot into the Chronicle insert. Audit every Go Chronicle insert path. Add heterogeneous-language, swapped-pair, per-side drift, receipt-later-uncertain, source-set mismatch, bundle replacement, and forced-late-error PostgreSQL rollback cases.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; cd apps/go-backend &amp;&amp; COWARDS_GO_BACKEND_TEST_DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game go test ./... -run 'TestMatchCompletion.*Integrity|TestIntegrityEvidenceDatabaseBoundary' -count=1</automated></verify>
  <acceptance_criteria>
    - Chronicle identity equals the locked Match identity.
    - Drift/late errors create no partial completion.
    - No system failure becomes player fault.
  </acceptance_criteria>
  <done>The normal Go lifecycle is exact-identity and failure-safe end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PostgreSQL to Go claim and completion | Stale identity could mutate lifecycle or Chronicle. |
| Go to runtime-service and back | Evidence can drift in flight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-13 | Tampering/TOCTOU | Go lifecycle | high | mitigate | Claim/completion row locks, strict transport DTO, post-response recheck, and PostgreSQL no-mutation tests. |
| T-256-SC | Tampering | package supply chain | low | accept | Existing Go modules only. |
</threat_model>

<verification>Run focused Go unit tests and configured-PostgreSQL claim/completion proof.</verification>
<success_criteria>Go claim, transport, and completion all enforce one locked exact identity and fail only as system failure on drift.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-11-SUMMARY.md` when done.</output>
