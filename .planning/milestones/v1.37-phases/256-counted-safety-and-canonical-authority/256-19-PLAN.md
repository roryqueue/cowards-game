---
phase: 256-counted-safety-and-canonical-authority
plan: "19"
type: execute
wave: 2
depends_on: ["256-01"]
files_modified:
  - apps/worker/src/runner.ts
  - apps/worker/src/runner.test.ts
  - apps/worker/src/index.ts
  - scripts/check-v1-37-worker-retirement.ts
  - scripts/check-v1-37-worker-retirement.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-03, AUTH-05]
must_haves:
  truths:
    - "The legacy apps/worker entrypoint and runWorkerOnce reject before database-pool creation or claim for normal, rollback, test, parity, and unknown purposes."
    - "No environment variable, lifecycle-owner label, adapter selection, test helper, demo caller, or rollback label can restore direct Match execution."
    - "Claim, input loading, runtime creation, Chronicle construction, completion, failure mutation, and player penalty remain at zero calls for every retired-worker invocation."
    - "A focused structural sentinel fails if an executable purpose exception, pre-retirement side effect, alternate worker loop, or direct claim-to-engine/Chronicle/completion path reappears."
  artifacts:
    - path: "apps/worker/src/runner.ts"
      provides: "Stable fail-closed retirement boundary before every direct-worker side effect"
    - path: "apps/worker/src/runner.test.ts"
      provides: "All-purpose zero-call retirement regression matrix"
    - path: "scripts/check-v1-37-worker-retirement.ts"
      provides: "Focused structural sentinel for the retired direct TypeScript worker"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "apps/worker/src/runner.ts"
      via: "unconditional retirement assertion before runtime configuration, pool construction, or loop startup"
      pattern: "assertTypeScriptWorker.*Retired"
    - from: "scripts/check-v1-37-worker-retirement.ts"
      to: "apps/worker/src/runner.ts"
      via: "parsed source-region proof that retirement dominates claim and execution effects"
      pattern: "runWorkerOnce|claimNextMatchJob|buildChronicleFromMatch|completeMatch"
---

<objective>
Retire the legacy direct TypeScript Match worker as a focused, structurally guarded boundary.

Purpose: D-01 and D-02 require all current lanes to begin quarantined and prohibit containment bypasses even for rollback, test, or parity labels. Output: an unconditional before-claim failure, zero-side-effect regression proof, and a focused sentinel consumed by the final Phase-256 gate.
</objective>

## Artifacts this phase produces

- One stable retired-worker error and unconditional startup/run boundary.
- An all-purpose zero-call test matrix.
- A focused structural sentinel with synthetic bypass fixtures.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-01-SUMMARY.md
@apps/worker/src/runner.ts
@apps/worker/src/runner.test.ts
@apps/worker/src/index.ts
@scripts/check-service-boundary-imports.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject every direct-worker purpose before claim or startup effects</name>
  <read_first>
    - apps/worker/src/runner.ts
    - apps/worker/src/runner.test.ts
    - apps/worker/src/index.ts
    - scripts/run-v1-5-advanced-demo.ts
    - scripts/preflight.ts
    - scripts/run-v1-4-demo-tournament.ts
    - apps/web/app/api/test-support/run-worker-once/route.ts
  </read_first>
  <files>apps/worker/src/runner.ts, apps/worker/src/runner.test.ts, apps/worker/src/index.ts</files>
  <behavior>
    - `assertTypeScriptWorkerEntrypointAllowed` and `runWorkerOnce` throw one stable retirement error for normal, rollback, test, parity, malformed, and omitted purpose inputs (D-01, D-02).
    - `runWorkerOnce` rejects before reading runtime configuration and before claim; claim, load, runtime, Chronicle, completion, retry/failure, and penalty spies remain zero.
    - `index.ts` asserts retirement before adapter configuration, database-pool creation, signal handlers, logging readiness, or `runWorkerLoop`.
    - Existing demo, preflight, and test-support callers receive the same fail-closed result and cannot pass an alternate dependency set or environment label to execute.
  </behavior>
  <action>Replace the rollback/test/parity allowlist with an unconditional stable `TypeScriptWorkerRetiredError` carrying a non-sensitive code. Keep only pure helper exports that later code still imports, but make every executable entrypoint call the retirement assertion as its first operation. Restructure `index.ts` so the assertion happens before configuration, pool, loop, or readiness output. Rewrite the runner tests around a table containing every declared and malformed purpose, explicit TypeScript/Go/unspecified lifecycle ownership, injected dependencies, and environment combinations. Assert the stable error plus zero calls for claim, load, runtime/adapter creation, Chronicle build, completion, attempt failure, persistence mutation, and player penalty. Do not add an emergency, test, parity, demo, or rollback escape hatch.</action>
  <verify><automated>pnpm --filter @cowards/worker exec vitest run src/runner.test.ts &amp;&amp; pnpm --filter @cowards/worker typecheck</automated></verify>
  <acceptance_criteria>
    - Every direct-worker purpose fails before claim and before process startup effects.
    - Every execution, Chronicle, and persistence spy remains at zero.
    - The error and logs expose no host, source, artifact, memory, objective, credential, or diagnostic detail.
  </acceptance_criteria>
  <done>The apps/worker lane has no executable purpose and cannot mutate Match lifecycle state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make worker retirement an executable structural invariant</name>
  <read_first>
    - apps/worker/src/runner.ts
    - apps/worker/src/index.ts
    - scripts/check-service-boundary-imports.ts
    - scripts/check-service-boundary-imports.test.ts
    - scripts/check-boundary-monitors.ts
  </read_first>
  <files>scripts/check-v1-37-worker-retirement.ts, scripts/check-v1-37-worker-retirement.test.ts</files>
  <behavior>
    - The sentinel passes only when the startup assertion dominates pool/loop setup and the run-once assertion dominates claim, runtime, Chronicle, completion, and failure mutation.
    - Synthetic fixtures independently fail for each former purpose allowlist, environment bypass, assertion-after-claim ordering, alternate worker loop, executable dependency injection, and direct claim-to-completion route.
    - The sentinel inventories `apps/worker/src/index.ts` and `runner.ts` by parsed source regions rather than accepting comments or names as proof.
  </behavior>
  <action>Implement a deterministic focused evaluator with injectable repository-root/source inputs. Parse imports, exports, and function bodies sufficiently to prove that both public executable boundaries unconditionally throw before their first side effect, former purpose values are never accepted, and no second worker loop or claim/runtime/Chronicle/completion route exists in `apps/worker/src`. Add synthetic temporary-repository cases for each bypass class and a real-repository pass. Emit only stable public-safe category/path diagnostics. Plan 14 will call this focused sentinel from the active boundary chain; do not duplicate the broad authority/writer monitor here.</action>
  <verify><automated>pnpm exec vitest run scripts/check-v1-37-worker-retirement.test.ts &amp;&amp; pnpm exec tsx scripts/check-v1-37-worker-retirement.ts</automated></verify>
  <acceptance_criteria>
    - Every former direct-worker escape class fails independently.
    - Comment text, purpose labels, and test names cannot satisfy the sentinel.
    - Real repository evaluation passes only with an unreachable direct worker.
  </acceptance_criteria>
  <done>Worker retirement is guarded against structural regression before the final phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Process or caller to legacy worker | Environment labels or injected dependencies must not regain lifecycle authority. |
| Worker source to CI sentinel | Cosmetic guards must not conceal a reachable claim/execution path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-23 | Elevation of Privilege/Tampering | apps/worker executable paths | high | mitigate | Unconditional before-side-effect retirement assertion and exhaustive zero-call tests. |
| T-256-24 | Spoofing/Repudiation | retirement sentinel | high | mitigate | Parsed source-region checks and independent synthetic bypass fixtures. |
| T-256-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>

<verification>Run the worker package tests/typecheck and focused structural sentinel, then let Plan 14 invoke the same sentinel from the serialized boundary-monitor chain.</verification>
<success_criteria>The legacy TypeScript worker cannot claim, execute, build a Chronicle, complete, or mutate failure state for any purpose, and structural drift fails CI.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-19-SUMMARY.md` when done.</output>
