---
phase: 256-counted-safety-and-canonical-authority
plan: "04"
type: execute
wave: 4
depends_on: ["256-01", "256-02", "256-03"]
files_modified:
  - packages/persistence/migrations/0012_integrity_authority.sql
  - packages/persistence/src/migrations.test.ts
  - packages/persistence/src/integrity-evidence.ts
  - packages/persistence/src/integrity-evidence.test.ts
  - packages/persistence/src/index.ts
autonomous: true
requirements: [SAFE-03, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "PostgreSQL stores one semantic tuple on a MatchSet, a complete normalized per-entrant evidence set, each entrant's exact lane/certificates, and the ordered bottom/top pair on every Match, queued job, request snapshot, and Chronicle."
    - "Verified attestations, certificates, release manifests, lane controls, classifications, and compensations are append-only at the database boundary; a certificate cannot exist without a successful verified-attestation import."
  artifacts:
    - path: "packages/persistence/migrations/0012_integrity_authority.sql"
      provides: "Semantic tuple columns, per-entrant evidence rows, ordered side-pair snapshots, verified-attestation constraints, immutable evidence/governance tables, and mutation-rejection triggers"
    - path: "packages/persistence/src/integrity-evidence.ts"
      provides: "Typed row mappers and shared exact scheduling/evidence snapshot persistence primitives"
  key_links:
    - from: "packages/persistence/src/integrity-evidence.ts"
      to: "packages/spec/src/integrity-authority.ts"
      via: "validates tuple ID plus expansion atomically before any caller writes"
      pattern: "resolveCanonicalCompatibilityTuple"
---

<objective>
Create the database and repository foundation for exact compatibility/evidence identity without mixing schema work with active writer migration or historical governance.

Purpose: D-05, D-06, D-09, D-11, and D-12 require exact new-write identity plus immutable historical/evidence storage. Output: migration 0012, typed persistence primitives, and real PostgreSQL trigger/constraint proof.
</objective>

## Artifacts this phase produces

- Migration 0012 with one tuple ID/expansion on MatchSets, a normalized complete evidence-set hash, generic MatchSet execution-entrant rows, links from competition entrants, and ordered bottom/top evidence snapshots on Matches, jobs, and Chronicles.
- Immutable verified-attestation, certificate, release-manifest, lane-control, cohort-classification, and compensation tables; certificates carry a required foreign key to a successful verified import.
- Typed exact-identity row/input helpers reused by Plans 05-08.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-RESEARCH.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-VALIDATION.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-03-SUMMARY.md
@packages/persistence/migrations/0011_competition_governance_surfaces.sql
@packages/persistence/src/migrations.ts
@packages/spec/src/integrity-authority.ts
@packages/spec/src/runtime-evidence.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact identity schema and append-only evidence tables</name>
  <read_first>
    - packages/persistence/migrations/0011_competition_governance_surfaces.sql
    - packages/persistence/src/migrations.ts
    - packages/spec/src/integrity-authority.ts
    - packages/spec/src/runtime-evidence.ts
  </read_first>
  <files>packages/persistence/migrations/0012_integrity_authority.sql, packages/persistence/src/migrations.test.ts</files>
  <behavior>
    - New application writes persist tuple ID plus all six expanded components once on the MatchSet, a deterministic complete per-entrant evidence set/hash, each entrant's own registry/lane/certificate/decision snapshot, and an ordered bottom/top pair on Match, queued-job, and Chronicle rows (D-05, D-08, AUTH-02).
    - Two-to-eight entrant MatchSets may mix supported language/runtime lanes without collapsing them to a singular executable identity; aggregate execution/counting status is derived from every entrant.
    - Partial identity is rejected; historical rows remain nullable and are never guessed or backfilled (D-06).
    - Certificate inserts require a successful immutable verified-attestation row whose exact producer/schema/command/corpus/policy/toolchain/artifact/result graph matches the certificate; direct certificate-shaped inserts reject.
    - Attestation, certificate, manifest, lane-control, classification, and compensation rows reject update/delete (D-09, D-11, D-12).
  </behavior>
  <action>Write migration tests first, then add the normal numbered migration. Model executable proof per entrant: add immutable `match_set_execution_entrants` (stable entrant key, Strategy Revision, exact lane identity, certificate refs/hashes, decision, authority bundle hash/generation), store the normalized complete set/hash on `match_sets`, link any `competition_entrants` row to its execution entrant, and store ordered bottom/top entrant keys plus complete snapshots on `matches`, `match_jobs`, and `chronicles`. Add verified-attestation/import tables and require every certificate to reference one passing import; no raw certificate row can promote. Use all-or-none checks, foreign keys, deterministic unique keys and indexes, and mutation-rejection triggers. Include the columns required by actual TS and Go writers. Do not add defaults that manufacture proof or guess legacy identity.</action>
  <verify><automated>pnpm --filter @cowards/persistence exec vitest run src/migrations.test.ts</automated></verify>
  <acceptance_criteria>
    - Migration 0012 executes through the existing runner.
    - Every evidence/governance authority is append-only in SQL.
    - Existing rows remain byte-equivalent and explicitly unresolved where identity is absent.
  </acceptance_criteria>
  <done>The database can represent exact new identity and enforce immutable evidence/history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared exact-identity persistence primitives and PostgreSQL proof</name>
  <read_first>
    - packages/persistence/src/integrity-evidence.ts
    - packages/persistence/src/db.ts
    - packages/persistence/src/governance.test.ts
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>packages/persistence/src/integrity-evidence.ts, packages/persistence/src/integrity-evidence.test.ts, packages/persistence/src/index.ts</files>
  <behavior>
    - One validated `MatchSetIntegrityIdentity` carries tuple ID/expansion plus a stable, normalized map of every entrant to its exact `EntrantExecutionEvidence`; a `MatchExecutionEvidencePair` selects ordered bottom/top entries for each Match/job/Chronicle.
    - Tuple/hash mismatch, incomplete entrant coverage, duplicate entrant keys, wrong revision binding, swapped side pairs, singular-lane collapse, and any partial or uncertified identity reject before SQL.
    - Real PostgreSQL rejects forbidden mutation and rolls back partial record sets.
  </behavior>
  <action>Create reusable normalized-set/pair validators, SQL column/value helpers, and row mappers. The set encoder sorts by stable entrant key, binds each key to its Strategy Revision, and computes a deterministic set hash; the pair helper must resolve the exact bottom/top keys used by a Match. Certificate insertion is not exported here: Plan 15 owns the sole verified import path. Add fake-pool and configured-PostgreSQL tests with two through eight heterogeneous entrants covering complete identity, missing/extra/duplicate entries, pair swaps, per-field mutation, append-only triggers, all-or-nothing rollback, and unchanged historical source-row hashes.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; pnpm --filter @cowards/persistence exec vitest run src/integrity-evidence.test.ts &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence exec vitest run src/integrity-evidence.test.ts -t "PostgreSQL|integrity schema"</automated></verify>
  <acceptance_criteria>
    - Shared helpers accept one complete exact identity and reject all partial/mixed forms.
    - PostgreSQL proves all constraints/triggers and transaction rollback.
    - No historical record is rewritten during resolution or test setup.
  </acceptance_criteria>
  <done>All later writers have one tested exact-identity persistence primitive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application to PostgreSQL | Forged, partial, or mutable proof could enter counted evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-05 | Tampering/Repudiation | identity and append-only tables | high | mitigate | All-or-none constraints, exact validators, immutable triggers, and rollback tests. |
| T-256-SC | Tampering | package supply chain | low | accept | No package install; existing PostgreSQL and workspace libraries only. |
</threat_model>

<verification>Run migration and focused persistence tests against real PostgreSQL.</verification>
<success_criteria>One semantic tuple plus exact per-entrant executable identity is representable, and unverified or mutable proof/history cannot be silently introduced.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-04-SUMMARY.md` when done.</output>
