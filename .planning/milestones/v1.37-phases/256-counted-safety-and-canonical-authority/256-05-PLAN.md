---
phase: 256-counted-safety-and-canonical-authority
plan: "05"
type: execute
wave: 6
depends_on: ["256-03", "256-04", "256-15"]
files_modified:
  - packages/persistence/src/match-service.ts
  - packages/persistence/src/match-service.test.ts
  - packages/persistence/src/matchset-service.ts
  - packages/persistence/src/matchset-service.test.ts
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Every direct TypeScript Match creation requires one semantic tuple and an ordered bottom/top pair of verified per-entrant evidence, then writes the pair on the Match and queued job in one transaction."
    - "Every matrix MatchSet creation writes one tuple, a complete normalized per-entrant evidence set, each entrant's own evidence row, and the correct ordered pair on every Match and queued job, or creates zero rows."
  artifacts:
    - path: "packages/persistence/src/match-service.ts"
      provides: "Semantic-tuple plus ordered per-side evidence direct Match writer"
    - path: "packages/persistence/src/matchset-service.ts"
      provides: "Complete per-entrant evidence-set and ordered-pair MatchSet/matrix writer"
  key_links:
    - from: "packages/persistence/src/matchset-service.ts"
      to: "packages/persistence/src/integrity-evidence.ts"
      via: "requires one validated IntegritySchedulingIdentity before the first insert"
      pattern: "IntegritySchedulingIdentity"
---

<objective>
Migrate the two active TypeScript creation writers to exact transactional identity.

Purpose: AUTH-02 applies to the real `createMatch` and `insertMatchSetWithMatrixOnClient` paths, not only a new repository helper. Output: focused writer changes and zero-row rollback proof.
</objective>

## Artifacts this phase produces

- `CreateMatchRecordInput` for matrix members plus `CreateMatchInput.integrityIdentity` required by direct Match creation.
- `CreateMatchSetFromMatrixInput.integrityIdentity` required once and copied to MatchSet, entrants, Matches, and jobs.
- Writer-level unit and PostgreSQL tests proving rejected identity produces zero rows.

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-04-SUMMARY.md
@packages/persistence/src/match-service.ts
@packages/persistence/src/matchset-service.ts
@packages/persistence/src/integrity-evidence.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require exact ordered per-side evidence in createMatch</name>
  <read_first>
    - packages/persistence/src/match-service.ts
    - packages/persistence/src/integrity-evidence.ts
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>packages/persistence/src/match-service.ts, packages/persistence/src/match-service.test.ts</files>
  <behavior>
    - `createMatch` rejects a missing/unknown semantic tuple or any missing, mixed, stale, unverified, wrongly bound, or swapped bottom/top entrant evidence before the transaction writes (D-04, D-05, D-07).
    - A valid ordered pair may use different supported executable lanes while both sides bind to the Match's exact Strategy Revisions and semantic tuple (D-08).
    - The tuple and ordered pair are copied unchanged onto both `matches` and `match_jobs` transactionally.
    - Rejection or SQL failure leaves both tables with zero new rows.
  </behavior>
  <action>Add focused failing tests. Split the current shared shape into `CreateMatchRecordInput` (the per-Match gameplay fields plus bottom/top entrant keys) and `CreateMatchInput` (record fields plus required tuple and `MatchExecutionEvidencePair`) so direct creation requires both sides without inventing a Match-wide lane. Validate the tuple, verified-import-backed certificate references, revision binding, side order, authority-bundle hash/generation, and per-side freshness before opening the write transaction, then include the complete ordered pair in both inserts. Keep revision and arena locks. Never synthesize identity from old spec/engine strings or certificate-shaped fixtures.</action>
  <verify><automated>pnpm --filter @cowards/persistence exec vitest run src/match-service.test.ts</automated></verify>
  <acceptance_criteria>
    - Complete exact identity appears identically on Match and queued job.
    - Every rejected identity class creates zero rows.
    - Existing locking and retry behavior remains intact.
  </acceptance_criteria>
  <done>Direct TypeScript Match creation is atomically identity-complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist a complete per-entrant set and ordered pairs in insertMatchSetWithMatrixOnClient</name>
  <read_first>
    - packages/persistence/src/matchset-service.ts
    - packages/persistence/src/match-service.ts
    - packages/persistence/src/integrity-evidence.ts
    - packages/persistence/migrations/0012_integrity_authority.sql
  </read_first>
  <files>packages/persistence/src/matchset-service.ts, packages/persistence/src/matchset-service.test.ts</files>
  <behavior>
    - `insertMatchSetWithMatrixOnClient` requires one exact tuple plus a normalized evidence map covering every distinct entrant key and only those keys before the MatchSet insert (D-04, AUTH-02).
    - Each generic execution-entrant row and linked competition entrant persists its own exact lane/certificates; the MatchSet persists the complete normalized set/hash; each Match/job persists the ordered bottom/top entries selected by its entrant keys.
    - Two-to-eight entrant matrices may be heterogeneous by language/lane; aggregate containment must pass for execution and every entrant's conformance must pass for counted purpose. One failing entrant fails the aggregate closed.
    - Rejected input, missing/extra/duplicate entrant evidence, a wrong Strategy Revision binding, a swapped side pair, or any child failure rolls back the complete matrix.
  </behavior>
  <action>Change `CreateMatchSetFromMatrixInput.matches` to `CreateMatchRecordInput[]`, add one top-level semantic tuple and a deterministic `executionEntrants` map, and validate complete coverage before SQL. Persist generic execution-entrant rows for every caller, link competition entrants where present, store the normalized set/hash on the MatchSet, and resolve each Match's ordered bottom/top pair into Match/job snapshots. Remove the possibility that a MatchSet-wide lane or entrant snapshot implies compatibility. Test direct service creation and the exported on-client helper with two, three, and eight entrants including mixed TypeScript/Python/Rust/Zig fixtures, per-field mutations, missing/extra entries, pair swaps, and forced late child failure. Use real PostgreSQL to assert zero rows across all record families on rejection.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; pnpm --filter @cowards/persistence exec vitest run src/matchset-service.test.ts &amp;&amp; DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence exec vitest run src/matchset-service.test.ts -t "integrity identity|zero rows"</automated></verify>
  <acceptance_criteria>
    - One semantic tuple is shared, while exact executable evidence remains per entrant and ordered per Match side.
    - Rejected identity is detected before the first insert.
    - Forced late failure rolls the entire matrix transaction back.
  </acceptance_criteria>
  <done>The normal TypeScript MatchSet writer cannot create partial or independently versioned work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Creation API to PostgreSQL | Partial or mixed scheduling identity could become claimable work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-06 | Tampering/Elevation of Privilege | Match and MatchSet writers | high | mitigate | Pre-write exact validation, one transaction, identical copied snapshot, and zero-row PostgreSQL tests. |
| T-256-SC | Tampering | package supply chain | low | accept | No new package. |
</threat_model>

<verification>Run focused writer tests plus configured-PostgreSQL zero-row assertions.</verification>
<success_criteria>Every active TypeScript creation record shares one validated exact scheduling identity transactionally.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-05-SUMMARY.md` when done.</output>
