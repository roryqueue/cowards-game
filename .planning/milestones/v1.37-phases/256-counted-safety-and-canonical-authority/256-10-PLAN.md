---
phase: 256-counted-safety-and-canonical-authority
plan: "10"
type: execute
wave: 7
depends_on: ["256-01", "256-02", "256-03", "256-17", "256-18"]
files_modified:
  - apps/go-backend/integrity_evidence.go
  - apps/go-backend/integrity_evidence_test.go
  - apps/go-backend/runtime_evidence_authority.go
  - apps/go-backend/runtime_evidence_authority_test.go
  - apps/go-backend/provider_readiness.go
  - apps/go-backend/provider_readiness_test.go
  - apps/go-backend/live_backend.go
  - apps/go-backend/main.go
  - apps/go-backend/main_test.go
autonomous: true
requirements: [SAFE-01, SAFE-02, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Go reproduces committed tuple bytes/hashes and independently verifies the same signed authority-bundle bytes/hash/schema/graph as runtime-service without an independent promotion rule."
    - "Go enforces the deployment-pinned minimum and a protected durable generation/hash high-water anchor before live server construction, orchestration startup, or HTTP listen, including after process restart."
    - "Every current Go lane is disabled without fresh containment evidence; provider proof alone never promotes it."
  artifacts:
    - path: "apps/go-backend/integrity_evidence.go"
      provides: "Generated-data-backed tuple verification and exact evidence classifier"
    - path: "apps/go-backend/runtime_evidence_authority.go"
      provides: "Independent Go Ed25519/SHA-256/schema/freshness verifier plus restart-safe bootstrap/high-water enforcement for the canonical mounted authority bundle"
    - path: "apps/go-backend/live_backend.go"
      provides: "Fail-closed live startup wiring that verifies and anchors authority before orchestrator construction/start"
    - path: "apps/go-backend/integrity_evidence_test.go"
      provides: "Cross-language vector and per-field mutation matrix"
  key_links:
    - from: "apps/go-backend/integrity_evidence.go"
      to: "packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json"
      via: "verifies identical domain-separated bytes and SHA-256"
      pattern: "sha256"
    - from: "apps/go-backend/main.go"
      to: "apps/go-backend/runtime_evidence_authority.go"
      via: "handlerFromEnv and NewLiveServer require verified, durably anchored authority before returning a handler or starting orchestration"
      pattern: "handlerFromEnv|NewLiveServer"
---

<objective>
Prove Go tuple/evidence parity and fail live startup closed before wiring claim, creation, completion, or read consumers.

Purpose: D-01 through D-03 and D-05/D-08 forbid Go-specific counted declarations or restart rollback. Output: generated-registry consumer, signed-publication verification, durable anti-rollback, fail-closed live startup, hash parity, exact classifier, and mutation tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-counted-safety-and-canonical-authority/256-CONTEXT.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-02-SUMMARY.md
@.planning/phases/256-counted-safety-and-canonical-authority/256-18-SUMMARY.md
@packages/spec/artifacts/v1.37-integrity-authority.json
@packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json
@apps/go-backend/provider_readiness.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify canonical tuple vectors and the signed authority bundle in Go</name>
  <read_first>
    - packages/spec/artifacts/v1.37-integrity-authority.json
    - packages/spec/artifacts/v1.37-integrity-authority-hash-vectors.json
    - apps/go-backend/runtime_service_client.go
  </read_first>
  <files>apps/go-backend/integrity_evidence.go, apps/go-backend/integrity_evidence_test.go, apps/go-backend/runtime_evidence_authority.go, apps/go-backend/runtime_evidence_authority_test.go</files>
  <behavior>
    - Go reproduces every committed fixed-order byte/hash vector exactly (D-05).
    - Tuple expansion mismatch, partial member, alias, range, wildcard, or unknown ID rejects atomically (D-07, AUTH-03).
    - Semantic tuple identity remains separate from executable build/toolchain/corpus identity (D-08).
    - Go reads the same single-file envelope and exact payload hash as runtime-service, verifies Ed25519 with a separately configured public key using the standard library, validates the closed graph/freshness/generation independently, and rejects fixture trust domains in production.
    - Go applies the deployment-pinned minimum generation/hash and durable high-water generation/hash before accepting a bundle; lower generation or same-generation/different-hash rejects after restart, and valid newer authority is accepted only after atomically fsyncing the new anchor.
  </behavior>
  <action>Write table tests first, then implement the explicit fixed-order UTF-8 tuple encoder and `crypto/sha256` verification against committed vectors. Also implement a Go authority loader using only standard-library `crypto/ed25519`, `crypto/sha256`, `os`/`io`/`encoding/json`, strict decoding, size limits, exact payload bytes, the separately configured public key, closed evidence-graph validation, freshness, and the same fixture/production trust-domain split as Plan 17. Enforce the configured deployment minimum generation/hash and strict service-local high-water file. Initial missing-anchor creation requires explicit bootstrap mode and an exact pin; normal mode rejects missing/corrupt/unwritable state. Before returning a valid newer authority, temp-write, file-sync, rename, and parent-directory-sync the new generation/hash; on failure retain the last-good anchor/authority. Test cold rollback, restart after refresh, revocation and kill-switch rollback, corrupt/unwritable anchor, concurrent refresh, fsync/rename failure, and same-generation hash forks. Consume generated manifest/vector values; do not hand-maintain semantic tuple or bundle policy constants. Assert TypeScript/runtime-service and Go report the identical payload hash and generation for the same bytes, and never expose paths/anchor details in public/default output.</action>
  <verify><automated>cd apps/go-backend &amp;&amp; go test ./... -run 'TestIntegrityEvidence.*Tuple|TestRuntimeEvidenceAuthority' -count=1</automated></verify>
  <acceptance_criteria>
    - TypeScript/Go tuple bytes and hashes are identical.
    - Every selector other than exact ID plus matching expansion rejects.
    - No executable identity field enters tuple minting.
    - Restart rollback, same-generation forks, and invalid/unwritable anchors reject without advancing authority.
  </acceptance_criteria>
  <done>Go has executable, generated-data-backed semantic tuple parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace provider-proof readiness with exact evidence classification</name>
  <read_first>
    - apps/go-backend/provider_readiness.go
    - apps/go-backend/provider_readiness_test.go
    - apps/go-backend/integrity_evidence.go
    - packages/spec/src/runtime-evidence.ts
  </read_first>
  <files>apps/go-backend/integrity_evidence.go, apps/go-backend/integrity_evidence_test.go, apps/go-backend/provider_readiness.go, apps/go-backend/provider_readiness_test.go</files>
  <behavior>
    - No containment means disabled; containment without conformance means exhibition-only; complete exact proof means counted (D-02).
    - Per-field per-entrant identity/certificate/freshness/generation/operator-switch mutations from the independently verified bundle match committed TypeScript expected statuses/reasons.
    - Provider proof and hand-written language/adapter switches cannot promote (D-01, D-03).
  </behavior>
  <action>Preserve the pure-classifier shape while replacing duplicated registry/promotion logic with generated data and exact evidence selected from the verified authority bundle. Remove unconditional counted readiness. Use caller-supplied evaluation time. Production conformance eligibility must remain empty until Phase 259 adds an authorized conformance producer/import; test fixtures use a non-production trust domain and cannot promote normal lanes. Compare against committed TypeScript fixtures rather than a second prose implementation.</action>
  <verify><automated>cd apps/go-backend &amp;&amp; go test ./... -run 'TestIntegrityEvidence|TestRevisionReadiness' -count=1</automated></verify>
  <acceptance_criteria>
    - All current lanes are disabled without certificates.
    - Every mutation matches canonical status/reason.
    - Unknown values fail closed.
  </acceptance_criteria>
  <done>Go classification is parity evidence, not independent authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail live Go startup before orchestration or HTTP listen on invalid authority</name>
  <read_first>
    - apps/go-backend/main.go
    - apps/go-backend/main_test.go
    - apps/go-backend/live_backend.go
    - apps/go-backend/runtime_evidence_authority.go
  </read_first>
  <files>apps/go-backend/main.go, apps/go-backend/main_test.go, apps/go-backend/live_backend.go, apps/go-backend/runtime_evidence_authority_test.go</files>
  <behavior>
    - `handlerFromEnv` and `NewLiveServer` verify the installed bundle/public key/deployment pin/high-water anchor before constructing or starting the orchestrator and before `http.ListenAndServe` can receive a handler.
    - Missing, malformed, stale, revoked, wrong-key, regressed, same-generation/different-hash, domain-invalid, corrupt-anchor, and unwritable-anchor states return startup error with zero orchestrator-start and zero listen calls.
    - Fixture data mode remains explicitly non-live and cannot produce production execution/counting authority.
  </behavior>
  <action>Add dependency-injected startup tests around `handlerFromEnv`/`NewLiveServer`, then load and durably anchor production authority before pool-backed orchestrator construction/start and before returning live routes. Keep the live failure message stable and redacted; do not serialize configured paths, signatures, graph nodes, or anchor content. Ensure cleanup closes any pool opened before a later startup failure. Preserve fixture-mode behavior as a non-production UI/test surface that cannot claim or execute counted work.</action>
  <verify><automated>cd apps/go-backend &amp;&amp; go test ./... -run 'TestRuntimeEvidenceAuthority|TestHandlerFromEnv.*Authority|TestNewLiveServer.*Authority' -count=1</automated></verify>
  <acceptance_criteria>
    - All invalid authority/anchor cases fail before orchestration and listen.
    - Valid pinned/anchored authority starts once and survives restart without accepting rollback.
    - Startup output remains privacy-safe.
  </acceptance_criteria>
  <done>Go cannot serve or start execution ownership without restart-safe verified authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated TypeScript authority data to Go | Divergent encoding or copied policy could create a second authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-256-12 | Tampering | Go parity classifier and live startup | high | mitigate | Committed vectors, per-field mutation matrix, deployment pin, durable high-water fsync/rename, generated data, and pre-orchestrator/listen fail-closed defaults. |
| T-256-SC | Tampering | package supply chain | low | accept | Go standard library only. |
</threat_model>

<verification>Regenerate authority artifacts, then rerun focused Go vector/classifier, anti-rollback restart, and live-startup tests.</verification>
<success_criteria>Go agrees exactly with the canonical tuple/evidence authority, cannot promote from declaration, and cannot start live ownership from stale or unanchored authority.</success_criteria>
<output>Create `.planning/phases/256-counted-safety-and-canonical-authority/256-10-SUMMARY.md` when done.</output>
