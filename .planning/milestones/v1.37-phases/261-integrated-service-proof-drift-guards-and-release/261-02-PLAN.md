---
phase: 261-integrated-service-proof-drift-guards-and-release
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-v1-37-release-boundaries.ts
  - scripts/check-v1-37-release-boundaries.test.ts
  - scripts/check-boundary-monitors.ts
  - scripts/check-boundary-monitors.test.ts
  - package.json
autonomous: true
requirements: [PROOF-05, PROOF-06]
must_haves:
  truths:
    - "Per D-07, release output passes both closed-schema validation and recursive concrete-preimage scanning across APIs, documents, logs, fixtures, contracts, proofs, audits, and handoffs."
    - "Every PROOF-06 regression class has a mutation that fails the release checker: transition duplication, tuple mixing, adapter gameplay, stale evidence, event drift, arena duplication, unfair scheduling, unproved counting, and private leakage."
    - "Construction uses a source/fixture-only boundary mode that does not require not-yet-collected artifacts; strict release mode rejects every missing or stale required artifact and is wired into the default boundary hub only after collectors and rollups exist."
  artifacts:
    - path: scripts/check-v1-37-release-boundaries.ts
      provides: Release-specific privacy and authority mutation checker
    - path: scripts/check-v1-37-release-boundaries.test.ts
      provides: One adversarial fixture per PROOF-06 drift class
  key_links:
    - from: packages/spec/src/public-output-privacy.ts
      to: scripts/check-v1-37-release-boundaries.ts
      via: closed recursive safe-output assertion and concrete preimage augmentation
      pattern: "assertPublicOutputLeakSafe"
    - from: scripts/check-v1-37-release-boundaries.ts
      to: scripts/check-boundary-monitors.ts
      via: one serialized pure release assertion in the root hub
      pattern: "release"
---

<objective>Create source/fixture-first privacy and drift guards, including a strict release mode that later becomes the final default boundary gate.</objective>
<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
</execution_context>
<context>
@.planning/phases/261-integrated-service-proof-drift-guards-and-release/261-CONTEXT.md
@.planning/phases/261-integrated-service-proof-drift-guards-and-release/261-RESEARCH.md
@scripts/check-v1-37-integrity-boundaries.ts
@scripts/check-boundary-monitors.ts
@packages/spec/src/public-output-privacy.ts
</context>
<interfaces>
`assertV137IntegrityPublicPayload` and `analyzeV137IntegritySources` already guard current tuple/kernel/runtime/Chronicle/arena/Set ownership. `runBoundaryMonitorChecks` is the serialized root hub. Extend these seams; do not create another broad source scanner.
</interfaces>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement release privacy and named-drift mutation checks</name>
  <files>scripts/check-v1-37-release-boundaries.ts, scripts/check-v1-37-release-boundaries.test.ts</files>
  <read_first>PROOF-05/06; D-07/D-09/D-10; scripts/check-v1-37-integrity-boundaries.ts findings and public assertion; packages/spec/src/public-output-privacy.ts; existing mutation fixtures in scripts/check-boundary-monitors.test.ts.</read_first>
  <behavior>Source/fixture tests first inject one real regression per named PROOF-06 class and one concrete secret/source/artifact/memory/objective/path/env/diagnostic/restricted-ID preimage into every public artifact class. Strict-mode fixtures additionally reject a missing, stale, edited, duplicated, or identity-mixed service, rollback/history, browser, integrated-proof, prearchive-proof, audit, handoff, or readiness artifact. Policy prose that merely names a forbidden category remains allowed; actual fields, markers, values, and bytes reject recursively.</behavior>
  <action>Build a thin release checker with explicit `source-fixture` and `strict-release` modes. Source/fixture mode composes existing integrity analysis and in-memory adversarial fixtures without reading not-yet-created release artifacts. Strict release mode requires exact current proof input hashes, event/arena/Set authorities, every collected receipt and canonical release artifact, and public privacy utilities; missing evidence is a failure, never an empty pass. Add only release-specific artifact inventory and concrete-preimage comparisons. Require safe-schema exact keys before scanning and stable finding codes; reject unproved `counted` claims independently of functional four-language support per D-09. Do not copy the transition-owner AST, tuple resolver, arena geometry, Set fairness, or public privacy engines.</action>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 --no-file-parallelism scripts/check-v1-37-release-boundaries.test.ts scripts/check-v1-37-integrity-boundaries.test.ts</automated></verify>
  <acceptance_criteria>Every PROOF-06 class and every concrete PROOF-05 leak class fails under mutation, honest limitations pass, and the checker emits no private diagnostic value.</acceptance_criteria>
  <done>Release truth and privacy are executable structural properties rather than labels.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Register source/fixture checks without prematurely enabling strict release mode</name>
  <files>scripts/check-boundary-monitors.ts, scripts/check-boundary-monitors.test.ts, package.json</files>
  <read_first>scripts/check-boundary-monitors.ts `runBoundaryMonitorChecks`; package.json current `boundary:monitors`; Phase-260 one-check wiring tests; Task-1 checker API.</read_first>
  <behavior>The root chain invokes `v1.37:release-boundaries:source-check` exactly once after lower source authorities, the command performs no live service launch, artifact read requirement, or write, and strict-mode mutation fixtures already prove missing/stale release evidence fails. Plan 261-11 owns the later atomic switch to `strict-release` after all required artifacts exist.</behavior>
  <action>Add the source/fixture package command and one non-recursive monitor registration. Extend wiring tests to reject missing, duplicated, write-mode, strict-mode-before-artifacts, or recursive invocations. Preserve all current 44 assertions and report-only import policy; do not weaken earlier boundaries to make the new gate green. Leave the strict release command implemented and fixture-tested but not yet selected by the default hub.</action>
  <verify><automated>pnpm exec vitest run --maxWorkers=1 --no-file-parallelism scripts/check-boundary-monitors.test.ts scripts/check-v1-37-release-boundaries.test.ts &amp;&amp; pnpm v1.37:release-boundaries:source-check</automated></verify>
  <acceptance_criteria>The source/fixture checker is callable directly and exactly once from `boundary:monitors`; strict mode has missing/stale mutation coverage but is not executed before its evidence exists.</acceptance_criteria>
  <done>Release drift guards exist before collection without creating an impossible missing-artifact dependency.</done>
</task>
</tasks>

## Artifacts this phase produces

- A release-specific pure boundary/privacy checker.
- A complete mutation corpus for all nine PROOF-06 regressions and all PROOF-05 leak classes.
- One serialized construction-time source/fixture command, plus a fixture-tested strict mode reserved for the final boundary gate.

<threat_model>
## Trust Boundaries
| Boundary | Untrusted input |
|---|---|
| repository source/artifacts to checker | aliases, computed imports, stale pins, forged labels, private nested data |
| release checker to root monitor hub | duplicate, skipped, reordered, or write-mode command wiring |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-261-02A | Spoofing | counted-lane labels | critical | mitigate | Exact installed evidence/containment join and limitation mutation tests. |
| T-261-02B | Tampering | authority ownership | critical | mitigate | Compose existing AST/inventory checks and one mutation per named class. |
| T-261-02C | Information Disclosure | public release corpus | critical | mitigate | Exact safe schemas, recursive keys/markers, and concrete private preimages. |
| T-261-02D | Denial of Service | default monitor hub | medium | mitigate | Pure non-recursive check registered exactly once. |
</threat_model>
<prohibitions>No second broad scanner, live topology launch, write mode, gameplay implementation, public raw evidence, or weakening of earlier monitors is allowed.</prohibitions>
<verification>Run focused source/fixture and strict-mode mutation tests, the direct source check, and the root wiring suite.</verification>
<success_criteria>PROOF-05 and PROOF-06 have independent privacy-safe drift guards before live proof artifacts exist, without falsely requiring those future artifacts.</success_criteria>
<output>Create 261-02-SUMMARY.md.</output>
