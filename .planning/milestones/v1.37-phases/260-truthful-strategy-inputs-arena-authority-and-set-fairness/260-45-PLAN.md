---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "45"
type: execute
wave: 28
depends_on: ["260-44"]
files_modified:
  - packages/persistence/src/jobs.test.ts
autonomous: true
requirements: [STRAT-04]
must_haves:
  truths:
    - "The containment-only PostgreSQL claim test derives eligibility from the job's persisted `run_after`, independent of setup duration or host load."
    - "The exact claim SQL, production scheduling, authority admission, evidence assertions, selectors, and database head remain unchanged."
  artifacts:
    - path: packages/persistence/src/jobs.test.ts
      provides: Persisted-time-relative queued-job claim assertion
---
<objective>Remove a load-sensitive wall-clock race from the real activation persistence gate after setup occasionally outlived the test's pre-setup `now + 1 second` claim timestamp.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-44-SUMMARY.md @.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-14-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Claim after the persisted queue eligibility time</name>
  <files>packages/persistence/src/jobs.test.ts</files>
  <action>After `createFromMatrix`, read the exact queued job's persisted `run_after`, assert the row exists, and call `claimNextMatchJob` at one second after that database-owned time. Preserve the original evidence-validity clock, match allowlist, claim expectation, null-conformance assertions, and all production code. Run the focused PostgreSQL test repeatedly, the exact full persistence package gate, typecheck, lint, and protected baseline.</action>
  <verify><automated>export DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game; pnpm exec vitest run packages/persistence/src/jobs.test.ts -t "creates and claims containment-only exhibition entrants with null conformance identity" --maxWorkers=1 --no-file-parallelism --no-cache &amp;&amp; pnpm --filter @cowards/persistence test</automated></verify>
  <done>The claim assertion is deterministic under load because its eligibility time comes from the persisted job rather than a pre-setup wall clock.</done>
</task>
</tasks>
<verification>Require repeated focused success, all 328 persistence tests, workspace static gates, protected baseline, and exact unchanged v1.17 database head.</verification>
<success_criteria>The real coordinator persistence gate cannot miss the test job solely because setup took longer than one second.</success_criteria>
<output>Create 260-45-SUMMARY.md.</output>
