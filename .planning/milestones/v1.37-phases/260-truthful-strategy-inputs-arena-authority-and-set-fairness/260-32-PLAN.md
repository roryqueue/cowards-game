---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "32"
type: execute
wave: 14
depends_on: ["260-27", "260-28"]
files_modified:
  - packages/persistence/src/presets.ts
  - packages/persistence/src/match-service.ts
  - packages/persistence/src/match-service.test.ts
  - packages/persistence/src/matchset-service.ts
  - packages/persistence/src/matchset-service.test.ts
  - packages/persistence/src/competition.ts
  - packages/persistence/src/competition.test.ts
  - packages/persistence/src/ladder.ts
  - packages/persistence/src/ladder.test.ts
  - packages/persistence/src/jobs.ts
  - packages/persistence/src/jobs.test.ts
autonomous: true
requirements: [STRAT-04, SET-01, SET-02, SET-03, SET-04, SET-05]
must_haves:
  truths:
    - "Persistence scheduling/creation is the sole TypeScript reader of the active database head."
    - "Absent/default selection requires exact equality between active head and compact file authority, with no pending intent."
    - "The complete selection is frozen into Matches, MatchSets, jobs, and competition/ladder evidence; downstream runtime receives a signed selection and performs no database lookup."
    - "Explicit historical v1.17 and candidate v1.19 dispatch remain unchanged while live current stays v1.17."
---
<objective>Enforce the database head at every TypeScript scheduling boundary and freeze exact selection into work.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-27-SUMMARY.md @.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-28-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resolve defaults only at DB-bound Match and MatchSet creation</name>
  <files>packages/persistence/src/presets.ts, packages/persistence/src/match-service.ts, packages/persistence/src/match-service.test.ts, packages/persistence/src/matchset-service.ts, packages/persistence/src/matchset-service.test.ts</files>
  <behavior>Creation reads active head, rejects pending/absent/stale/file-mismatch, resolves exact v1.17 legacy or v1.19 four-condition path, and persists the complete selection JSON/root on MatchSet plus matching roots on Matches/jobs. Retry preserves identical selection.</behavior>
  <action>Remove omitted-selector paths that throw or silently stay legacy independent of current authority. Keep explicit candidate and historical APIs. Populate the migration-0028 frozen-work columns and signed request envelope. Add active/pending/mixed/stale/absent head, row/root mismatch, retry, D-04, privacy, and frozen-envelope tests.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/match-service.test.ts packages/persistence/src/matchset-service.test.ts packages/persistence/src/jobs.test.ts packages/persistence/src/semantic-authority-selection-head.test.ts --maxWorkers=1</automated></verify>
  <done>All Match/MatchSet defaults are file/head-consistent and downstream work is self-describing.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce the same head in competition and ladder entry points</name>
  <files>packages/persistence/src/competition.ts, packages/persistence/src/competition.test.ts, packages/persistence/src/ladder.ts, packages/persistence/src/ladder.test.ts</files>
  <behavior>Competition, exhibition, Workshop-adjacent analytics, and ladder scheduling delegate to the corrected Match/MatchSet creation path and cannot bypass active-head, D-04, certificate, or four-condition checks.</behavior>
  <action>Add direct and transitive entry-point tests for live v1.17, injected exact v1.19, pending intent, file/head mismatch, uncovered revision, system failure, and explicit historical dispatch.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/competition.test.ts packages/persistence/src/ladder.test.ts packages/persistence/src/matchset-service.test.ts --maxWorkers=1</automated></verify>
  <done>No TypeScript scheduling caller can treat the database head as advisory.</done>
</task>
<task type="auto">
  <name>Task 3: Bind job claim/execution to the frozen selection</name>
  <files>packages/persistence/src/jobs.ts, packages/persistence/src/jobs.test.ts, packages/persistence/src/match-service.test.ts, packages/persistence/src/matchset-service.test.ts, packages/persistence/src/competition.test.ts, packages/persistence/src/ladder.test.ts</files>
  <action>Require claim SQL to join equal MatchSet/Match/job selection roots, return the frozen complete selection in the job request, and reject current-head reread/substitution, pending activation, or mismatched rows. Run full serialized persistence/PostgreSQL, runtime request-envelope integration, ownership/privacy monitors, and protected baseline. Assert no runtime-service database import.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence test &amp;&amp; pnpm boundary:imports &amp;&amp; pnpm exec tsx scripts/capture-v1-37-protected-baseline.ts --check</automated></verify>
  <done>Persistence alone reads current DB authority and current live behavior is unchanged.</done>
</task>
</tasks>
<verification>Every scheduling entry point proves active/file equality, pending rejection, frozen selection transport, history, privacy, and live v1.17 equivalence.</verification>
<success_criteria>No counted or default scheduling can occur from a missing, pending, stale, or mixed authority head.</success_criteria>
<output>Create 260-32-SUMMARY.md.</output>
