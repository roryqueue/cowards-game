---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "37"
type: execute
wave: 19
depends_on: ["260-36"]
files_modified:
  - scripts/generate-v1-37-conformance-corpus.ts
  - scripts/generate-v1-37-conformance-corpus.test.ts
  - packages/engine/src/test/current-kernel-runtime.ts
  - packages/engine/src/test/current-kernel-runtime.test.ts
autonomous: true
requirements: [STRAT-01, STRAT-02, STRAT-03, STRAT-04]
must_haves:
  truths:
    - "The immutable v3 observation corpus is regenerated, reviewed, and checked only against the exact immutable v2 baseline, regardless of which reviewed corpus is current."
    - "General future-candidate generation continues to use the selected current corpus and cannot reuse the active version."
    - "Fixture-only bound v1.17 runtime requests use the exact v1.17 tuple rather than a current-selector alias; v1.19 raw observation execution remains unchanged."
    - "No production adapter, runtime ABI, corpus evidence byte, Match behavior, Chronicle, selector, or historical result changes."
---
<objective>Remove the last two current-selector assumptions exposed in the declared generator and replay gates after exact v1.19 promotion.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-36-SUMMARY.md @packages/golden/src/fixtures/v1-37-conformance-corpus/v2/corpus.json @packages/golden/src/fixtures/v1-37-conformance-corpus/v3/corpus.json @packages/engine/src/test/current-kernel-runtime.ts @packages/replay/src/record.test.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Recheck immutable v3 from its exact v2 baseline</name>
  <files>scripts/generate-v1-37-conformance-corpus.ts, scripts/generate-v1-37-conformance-corpus.test.ts</files>
  <behavior>Observation-specific v3 creation and semantic-diff checks use the exact validated v2 corpus/path/root even after v3 becomes current. They reproduce the committed v3 bytes, case order, review, semantic diff, and pin without duplicate cases. The general next-version writer remains selected-current based.</behavior>
  <action>Load and validate the fixed immutable v2 baseline explicitly for observation-v3-only functions. Parameterize shared diff construction so the observation path supplies v2 while the general candidate writer retains the selected current default. Bind the baseline path/root and add regression assertions that v3 regeneration is idempotent and selector-independent. Do not edit v2/v3 evidence bytes or relabel lifecycle review facts.</action>
  <verify><automated>pnpm exec vitest run scripts/generate-v1-37-conformance-corpus.test.ts packages/golden/src/v1-37-conformance-corpus.test.ts --maxWorkers=1 --no-file-parallelism</automated></verify>
  <done>Committed v3 checks succeed both before and after promotion from the same immutable v2 evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bind historical test runtime requests to exact v1.17</name>
  <files>packages/engine/src/test/current-kernel-runtime.ts, packages/engine/src/test/current-kernel-runtime.test.ts</files>
  <behavior>A second-argument v1.17 kernel request produces a bound request with the exact v1.17 tuple and runtime ABI, independent of current selection. Successor v1.19 execution continues to receive the raw observation contract.</behavior>
  <action>Replace the current semantic tuple alias used only inside the fixture adapter's authenticated v1.17 request constructor with the explicit v1.17 tuple. Make the adapter regression request and assertions explicitly v1.17. Do not change the kernel, recorder, production adapters, result classification, or runtime ownership.</action>
  <verify><automated>pnpm exec vitest run packages/engine/src/test/current-kernel-runtime.test.ts packages/replay/src/record.test.ts --maxWorkers=1 --no-file-parallelism</automated></verify>
  <done>Historical replay fixtures remain completed under exact v1.17 after v1.19 promotion, without weakening successor dispatch.</done>
</task>
</tasks>
<verification>Focused generator, Golden, engine-helper, and replay tests; exact isolated v1.19 selector simulation; typecheck; lint; protected baseline; and main-tree allowlist pass.</verification>
<success_criteria>Plan 33 resumes with both immutable corpus derivation and historical runtime binding independent of current selection.</success_criteria>
<output>Create 260-37-SUMMARY.md.</output>
