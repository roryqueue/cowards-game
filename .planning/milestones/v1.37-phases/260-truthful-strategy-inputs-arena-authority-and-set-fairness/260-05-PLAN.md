---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "05"
type: execute
wave: 5
depends_on: ["260-03", "260-04", "260-17"]
files_modified:
  - packages/persistence/src/presets.ts
  - packages/persistence/src/match-service.ts
  - packages/persistence/src/match-service.test.ts
  - packages/persistence/src/matchset-service.ts
  - packages/persistence/src/matchset-service.test.ts
  - packages/persistence/src/competition.ts
  - packages/persistence/src/competition.test.ts
autonomous: true
requirements: [STRAT-03, SET-01, SET-02, SET-03, SET-04, SET-05]
must_haves:
  truths:
    - "The explicitly addressed runtime-v1.19 TypeScript candidate creates exactly four canonical conditions with one base seed, while every current Phase-259 preset, map-config, creation path, and selector behaves exactly as before activation."
    - "Candidate scheduling freezes exact D-04 evidence for each Strategy Revision plus catalog snapshot, tuple, scenario, and all four Match requests before any candidate job is visible."
    - "No successor preset, mirror flag, seed suffix, alias, or pre-v1.19 revision without its own exact real v1.19 evidence can manufacture counted eligibility, fairness, or arena diversity."
  artifacts:
    - path: packages/persistence/src/matchset-service.ts
      provides: Atomic validated scenario and four-condition creation
    - path: packages/persistence/src/competition.test.ts
      provides: Entrant-level TypeScript Cartesian and rollback proof
  key_links:
    - from: packages/spec/src/set-condition-policy-v1-37.ts
      to: packages/persistence/src/matchset-service.ts
      via: pure canonical scenario generator and validator
    - from: packages/persistence/src/repositories.ts
      to: packages/persistence/src/matchset-service.ts
      via: exact catalog freeze occurs before scenario/job insertion
---
<objective>Stage an explicitly dispatched TypeScript runtime-v1.19 four-condition candidate while preserving every Phase-259 current scheduling behavior.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-03-SUMMARY.md @.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add a versioned catalog-backed candidate beside unchanged current presets</name>
  <files>packages/persistence/src/presets.ts, packages/persistence/src/match-service.ts, packages/persistence/src/match-service.test.ts</files>
  <read_first>presets.ts; match-service CreateMatchRecordInput; Plan-01 catalog/policy; engine successor explicit initial initiative input.</read_first>
  <behavior>Exact Phase-259 presets, current map-config selection, mirror rows, Match input defaults, and Open Field treatment remain unchanged behind their versioned legacy branch. The explicitly addressed runtime-v1.19 candidate contains schedulable semantic arena IDs and base seeds only; its CreateMatchRecordInput requires scenarioId, conditionId/ordinal, catalog version, semantic hash, explicit bottom/top entrants and players, and initial initiative owner.</behavior>
  <action>Stage a selector-backed versioned dispatch that resolves the current/default branch from the generated Phase-259 selector and resolves runtime-v1.19 only when explicitly requested. In the successor branch, mirrorSides is not fairness authority and Open Field is excluded from schedulable diversity per D-09 through D-14; validate explicit condition identity and initiative rather than parsing seed, ID, or index. Do not remove, rewrite, or redirect any current Phase-259 preset, public constructor, replayable Match input, or default export in this plan.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/match-service.test.ts packages/persistence/src/competition.test.ts</automated></verify>
  <acceptance_criteria>Explicit successor Match records are self-describing and exclude historical aliases, while snapshot and behavioral tests prove all Phase-259 current preset and creation outputs remain exact.</acceptance_criteria>
  <done>The TypeScript candidate contract carries explicit fairness facts without activation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Validate and insert complete scenarios atomically</name>
  <files>packages/persistence/src/matchset-service.ts, packages/persistence/src/matchset-service.test.ts</files>
  <read_first>validateMatchSetCreation; insertMatchSetWithMatrixOnClient; 0026 schema; catalog freeze API; Set policy validator.</read_first>
  <behavior>For explicit runtime-v1.19 only, validate all four rows in memory, require exact persisted real-v1.19 revalidation evidence for each immutable Strategy Revision, freeze revisions/evidence/catalog/tuple, insert scenario and conditions, then publish jobs in one transaction. Missing/duplicate/substituted row, absent/revoked/cross-revision/old-tuple evidence, changed seed/side/initiative/hash/revision/tuple, or fault at any write rolls back all rows and locks. Phase-259 default creation remains unchanged.</behavior>
  <action>Implement D-13 through D-16 in the successor createFromMatrix/createFromPreset branch with deterministic condition order and exact expected membership. Enforce D-04 at scheduling admission by looking up and freezing one complete revision-bound runtime-v1.19 real-execution evidence row for every entrant revision; additive JSON tolerance, language-level certificates, or another revision's evidence never admit it. Bind the signed Match request identity to every frozen field and evidence root. Preserve existing three-way failure ownership, leave the current selector on Phase 259, and make candidate creation impossible through a partial matrix or uncovered revision.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/matchset-service.test.ts --maxWorkers=1</automated></verify>
  <acceptance_criteria>Exactly four explicitly versioned candidate jobs with exact per-revision evidence become visible together or the transaction leaves no MatchSet/scenario/Match/job/evidence-lock residue; current Phase-259 creation snapshots remain exact.</acceptance_criteria>
  <done>Candidate-only atomic four-condition creation and revision admission are database-proved.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Migrate the TypeScript competition parity path</name>
  <files>packages/persistence/src/competition.ts, packages/persistence/src/competition.test.ts</files>
  <read_first>generateCompetitionPairwiseMatrix; createManualExhibitionMatchSet; TypeScript competition quarantine role; canonical policy/projection APIs.</read_first>
  <behavior>When runtime-v1.19 is explicitly selected, each entrant pair, schedulable semantic arena, and base seed produces one scenario and four rows. Entrant A/B each appear twice on each side and twice with initiative. Reordering entrants, insertion, or completion cannot change canonical identities or scoring inputs. Default/current calls still produce the exact Phase-259 matrix.</behavior>
  <action>Use the public spec generator for D-13 through D-15 and exact repository creation for D-16 only in the candidate branch. Keep this TypeScript path rollback/parity/test only; do not promote it over Go or flip its selector. Add tests that the former two-row mirror and any seed-suffix fairness claim fail only when presented as runtime-v1.19, while exact Phase-259 calls remain accepted and byte-identical before Plan 14.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/competition.test.ts packages/persistence/src/matchset-service.test.ts --maxWorkers=1</automated></verify>
  <acceptance_criteria>Explicit TypeScript candidate parity produces the exact canonical matrix, current/default behavior remains Phase-259 exact, and the parity path cannot become a second normal scheduler.</acceptance_criteria>
  <done>The rollback/parity creation path agrees with the spec policy.</done>
</task>
</tasks>

## Artifacts this phase produces

- Versioned candidate TypeScript presets and explicit successor Match input behind the unchanged Phase-259 selector.
- Atomic four-condition MatchSet creation, D-04 revision-admission, and rollback proof for explicit runtime-v1.19.

<threat_model>
## Trust Boundaries
| Boundary | Untrusted input |
|---|---|
| caller to MatchSet service | entrants, preset, arena, seed, condition rows |
| transaction to queued jobs | frozen revisions/catalog/tuple and signed request identity |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260-05A | Tampering | matrix membership | critical | mitigate | Exact policy validation before transaction writes. |
| T-260-05B | Race/TOCTOU | revision/catalog freeze | high | mitigate | Locks and all writes in one transaction before jobs. |
| T-260-05C | Spoofing | seed suffix | high | mitigate | Explicit fields and seed-independent fairness tests. |
| T-260-05D | Elevation of Privilege | TypeScript parity path | medium | mitigate | Quarantine-role structural tests retain Go as selected normal owner. |
| T-260-05E | Spoofing | revision eligibility | critical | mitigate | Exact persisted revision/source/artifact/ABI/tuple/real-receipt admission before candidate job creation. |
</threat_model>
<verification>Run focused unit and PostgreSQL atomicity, D-04 absence/substitution/revocation, permutation, mutation, candidate parity, and Phase-259 current-behavior snapshot suites.</verification>
<success_criteria>The explicit TypeScript successor proves revision-granular admission and the full Cartesian matrix without seed inference, partial publication, or any preactivation current behavior change.</success_criteria>
<output>Create 260-05-SUMMARY.md.</output>
