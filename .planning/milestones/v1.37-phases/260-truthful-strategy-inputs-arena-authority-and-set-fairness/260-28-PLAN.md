---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "28"
type: execute
wave: 13
depends_on: ["260-27"]
files_modified:
  - packages/persistence/migrations/0028_semantic_authority_selection_head.sql
  - packages/persistence/src/migrations.ts
  - packages/persistence/src/migrations.test.ts
  - packages/persistence/src/semantic-authority-selection-head.ts
  - packages/persistence/src/semantic-authority-selection-head.test.ts
autonomous: true
requirements: [STRAT-03, STRAT-04, SET-01, SET-02, SET-03, SET-04, SET-05]
must_haves:
  truths:
    - "One singleton database head owns the complete active semantic selection plus at most one durable pending activation intent."
    - "The head starts at exact Phase 259 and can move only by short locked prepare/finalize/abort transactions over whole canonical values."
    - "No partial tuple, certificate, corpus, trace, Workshop, arena, Set, or D-04 selector can be written."
    - "A crash at any activation boundary leaves a durable, exact, recoverable state and counted scheduling fails closed."
  artifacts:
    - path: packages/persistence/src/semantic-authority-selection-head.ts
      provides: Exact read/lock/CAS/restore API for one complete current selection
---
<objective>Create the real transactional database selection head required by atomic activation.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-27-SUMMARY.md @.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-13-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add one exact Phase-259 head with durable activation intent</name>
  <files>packages/persistence/migrations/0028_semantic_authority_selection_head.sql, packages/persistence/src/migrations.ts, packages/persistence/src/migrations.test.ts</files>
  <behavior>A singleton row stores the active complete selection. Exact states are discriminated: `active-v1.17-bootstrap` has no pending and no activation metadata; forward `pending-precommit` retains bootstrap active and requires direction, activation ID, expected old root, exact v1.19 target/root, parent HEAD and selector manifest; `active-v1.19-finalized` has no pending and requires activation ID, proof digest and actual commit/tree; reverse `pending-compensation` retains finalized v1.19, binds its source activation/finalization, and targets the recorded exact v1.17 preimage; `active-v1.17-compensated` has no pending, restores the exact bootstrap selection value, and requires compensation commit/tree/recovery receipt metadata. Every transition also writes append-only history. Shape/state constraints reject partial/extra/mixed values. The migration adds nullable historical-safe complete selection/root to MatchSets and matching roots to Matches/jobs, with v1.19 exact-shape/equality constraints.</behavior>
  <action>Create head, append-only finalization history, exact Phase-259 bootstrap, and frozen-work columns/constraints without publishing v1.19. Pending is initially absent. Keep certificate references immutable; countability still requires live append-only evidence. Prohibit ordinary partial updates and require v1.19 Match/MatchSet/job rows to carry one exact selection root while historical rows remain null/original.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/migrations.test.ts --maxWorkers=1</automated></verify>
  <done>A fresh/current database has one exact old-current selection head.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement short prepare/finalize/abort and restart recovery</name>
  <files>packages/persistence/src/semantic-authority-selection-head.ts, packages/persistence/src/semantic-authority-selection-head.test.ts</files>
  <behavior>Readers obtain one frozen validated active value and optional exact forward/reverse pending intent. `prepare` writes only values known before mutation in a short transaction. Allowed directions are bootstrap-v1.17 to reviewed-v1.19, or finalized-v1.19 back to its recorded exact v1.17 preimage. No intermediate pending update exists. `finalize` accepts proof/recovery digest and actual commit/tree, validates pending manifest/parent, writes append-only history, promotes the target, records exact forward or compensation metadata, and clears pending atomically. `abort` clears only the exact direction/intent. `recover` finalizes an exact matching committed tree or restores/aborts a precommit attempt; it never infers success.</behavior>
  <action>Expose explicit Phase-259/reviewed-v1.19 validators and the bounded reverse transition without version inference. Add forbidden directions/shapes, forward and reverse finalization, crashes before mutation, after mutation, after proof, after Git commit, and during finalization; stale CAS, concurrent token, missing/extra member, certificate substitution, mismatched commit/tree/proof, partial rollback, idempotent recover/abort, privacy, and transaction-abort tests. Counted scheduling must reject any pending or file/head mismatch.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/semantic-authority-selection-head.test.ts packages/persistence/src/migrations.test.ts --maxWorkers=1</automated></verify>
  <done>Files and database can participate in one exact activation/rollback protocol.</done>
</task>
</tasks>
<verification>Migration, exact-shape, short prepare/finalize/abort, crash recovery, lock, rollback, privacy, and Phase-259 bootstrap tests pass on fresh and current databases.</verification>
<success_criteria>One durable two-phase database head replaces the nonexistent eleven independent selector rows without a long-running transaction or unrecoverable crash window.</success_criteria>
<output>Create 260-28-SUMMARY.md.</output>
