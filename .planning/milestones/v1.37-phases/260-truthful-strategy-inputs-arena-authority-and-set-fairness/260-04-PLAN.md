---
phase: 260-truthful-strategy-inputs-arena-authority-and-set-fairness
plan: "04"
type: execute
wave: 3
depends_on: ["260-02"]
files_modified:
  - packages/persistence/migrations/0026_arena_catalog_and_set_conditions.sql
  - packages/persistence/src/migrations.ts
  - packages/persistence/src/migrations.test.ts
  - packages/persistence/src/schema.ts
  - packages/persistence/src/repositories.ts
  - packages/persistence/src/repositories.test.ts
autonomous: true
requirements: [STRAT-03, SET-01, SET-02, SET-03, SET-04, SET-05]
must_haves:
  truths:
    - "Released arena catalog rows are insert-or-exact-match immutable, and aliases cannot become schedulable diversity."
    - "Successor Matches persist scenario, condition, catalog, semantic geometry, sides, and explicit initiative; historical rows remain readable without fabricated values."
    - "Database constraints make incomplete or mixed successor identity fail before jobs become visible."
    - "Per D-04, every pre-v1.19 Strategy Revision is ineligible and non-counted for runtime-v1.19 until immutable exact real-execution evidence is persisted for that revision, ABI, tuple, source/artifact identity, and reviewed receipt."
  artifacts:
    - path: packages/persistence/migrations/0026_arena_catalog_and_set_conditions.sql
      provides: Additive historical-safe catalog/scenario/condition storage plus revision-granular runtime-v1.19 revalidation evidence and constraints
  key_links:
    - from: packages/spec/src/arena-catalog-v1-37.ts
      to: packages/persistence/src/repositories.ts
      via: exact immutable catalog install and lookup
    - from: packages/spec/src/set-condition-policy-v1-37.ts
      to: packages/persistence/migrations/0026_arena_catalog_and_set_conditions.sql
      via: successor all-present columns and unique condition membership
    - from: packages/persistence/src/repositories.ts
      to: packages/persistence/src/matchset-service.ts
      via: exact revision-bound v1.19 revalidation lookup is required before successor scheduling
---
<objective>Create the additive immutable database substrate for arena authority, explicit Set conditions, and revision-granular v1.19 execution evidence.</objective>
<execution_context>@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/260-truthful-strategy-inputs-arena-authority-and-set-fairness/260-02-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-19-SUMMARY.md @.planning/phases/259-executable-four-language-and-chronicle-conformance/259-28-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add historical-safe catalog, scenario, and condition storage</name>
  <files>packages/persistence/migrations/0026_arena_catalog_and_set_conditions.sql, packages/persistence/src/migrations.ts, packages/persistence/src/migrations.test.ts, packages/persistence/src/schema.ts</files>
  <read_first>migrations 0012 through 0025; migrations.ts ordered registry; schema.ts; Plan-01 catalog/policy fields; Phase-256 tuple columns.</read_first>
  <behavior>Add immutable catalog rows keyed by catalog version and arena ID; scenario rows keyed by MatchSet/scenario; nullable historical-safe successor columns on Matches; exact uniqueness and all-present successor-policy constraints; append-only guards for released authority; and an append-only Strategy Revision revalidation table keyed by revision ID plus ABI/tuple/evidence identity.</behavior>
  <action>Write PostgreSQL RED tests for forward-only migration, historical null preservation, successor all-or-none identity, alias/hash/status constraints, duplicate condition, bad ordinal, missing scenario, catalog mutation, and rollback. Implement D-09 through D-16 additively. For D-04, add revision-granular evidence rows binding revision ID, source/artifact digest, language/provider lane, strategy-runtime-abi-v1.19, runtime-v1.19, real execution request/result/receipt roots, certification identity, created-at evidence, and revocation/status; reject inferred/additive compatibility, old tuple receipts, mismatched revision bytes, synthetic evidence, duplicates, or mutable replacement. Do not backfill eligibility and do not rewrite old revision, arena, MatchSet, Match, Chronicle, receipt, certificate, or evidence rows.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/migrations.test.ts --maxWorkers=1</automated></verify>
  <acceptance_criteria>Migration applies once, historical rows remain exact, every pre-v1.19 revision starts without current-tuple eligibility, and every malformed successor or revision-evidence identity fails atomically.</acceptance_criteria>
  <done>The database can store but cannot silently reinterpret successor authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Install and freeze exact catalog records through repositories</name>
  <files>packages/persistence/src/repositories.ts, packages/persistence/src/repositories.test.ts</files>
  <read_first>createRepositories arena methods; current upsertArenaVariant; spec arena catalog projection; 0026 constraints.</read_first>
  <behavior>Install a released catalog in one transaction; identical reinstallation is idempotent; changed config/status/alias/hash/version fails. Lookup resolves active and historical alias records explicitly without making aliases schedulable.</behavior>
  <action>Replace mutable canonical arena upsert use with D-09 through D-12 insert-or-exact-match APIs. Lock the catalog record used by successor MatchSet creation and return a branded immutable snapshot containing catalog version, arena ID/status, alias target, semantic hash, and exact config. Keep generic historical arena reads available only for historical dispatch.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/repositories.test.ts packages/persistence/src/migrations.test.ts --maxWorkers=1</automated></verify>
  <acceptance_criteria>Released geometry cannot drift under an existing identity and Open Field cannot be selected as distinct current diversity.</acceptance_criteria>
  <done>Persistence consumes the spec authority without becoming another registry.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Persist immutable revision-scoped v1.19 revalidation evidence</name>
  <files>packages/persistence/src/repositories.ts, packages/persistence/src/repositories.test.ts</files>
  <read_first>Strategy Revision repository and immutable submission records; runtime evidence ledger/import APIs; migration 0026 revalidation table; D-04; Phase-259 certificate exact-tuple admission.</read_first>
  <behavior>Exact real v1.19 evidence can be appended once for one immutable Strategy Revision and queried by revision plus runtime-v1.19. A revision with no row, old ABI/tuple evidence, another revision's receipt, changed source/artifact hash, synthetic/local-only result, revoked evidence, or incomplete identity is fail-closed and non-counted. Old evidence remains readable and immutable but never satisfies the successor predicate.</behavior>
  <action>Add repository APIs to append exact reviewed service/runtime revalidation evidence and to return a branded admission result only for a complete non-revoked D-04 binding. Make the eligibility predicate revision-granular rather than language- or certificate-granular, require the real execution receipt and exact source/artifact bytes, and add negative/adversarial tests for every rejected substitution. Keep historical execution and certificates append-only and separately addressable.</action>
  <verify><automated>DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm exec vitest run packages/persistence/src/repositories.test.ts packages/persistence/src/migrations.test.ts --maxWorkers=1</automated></verify>
  <acceptance_criteria>No existing pre-v1.19 revision is eligible for runtime-v1.19 until its own exact real-execution row exists, and no old or sibling evidence can be relabeled to satisfy it.</acceptance_criteria>
  <done>D-04 has an immutable revision-level persistence predicate before scheduling is wired.</done>
</task>
</tasks>

## Artifacts this phase produces

- Migration 0026 with immutable catalog and explicit scenario/condition storage.
- Exact catalog install/freeze repository APIs.
- Append-only per-Strategy-Revision v1.19 revalidation evidence and fail-closed admission API.

<threat_model>
## Trust Boundaries
| Boundary | Untrusted input |
|---|---|
| repository to PostgreSQL | catalog and scenario documents, concurrent writers |
| historical rows to current readers | nullable legacy identity and old geometry records |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260-04A | Tampering | released arena row | critical | mitigate | Append-only triggers and insert-or-exact-match transaction. |
| T-260-04B | Tampering | condition rows | high | mitigate | Unique scenario/condition membership and all-present constraints. |
| T-260-04C | Repudiation | historical migration | high | mitigate | No backfill plus exact pre/post historical byte checks. |
| T-260-04D | Denial of Service | concurrent installs | low | accept | Serializable/advisory locking and idempotent exact replay bound contention. |
| T-260-04E | Spoofing | Strategy Revision compatibility | critical | mitigate | Exact revision/source/artifact/ABI/tuple/real-receipt binding; no inferred or cross-revision compatibility. |
</threat_model>
<verification>Run isolated PostgreSQL migration, repository idempotency, revision-evidence substitution/absence/revocation, mutation, rollback, and historical preservation tests.</verification>
<success_criteria>The database enforces immutable arena, Set, and revision-granular runtime-v1.19 evidence identity without rewriting history.</success_criteria>
<output>Create 260-04-SUMMARY.md.</output>
