---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "06"
type: execute
wave: 3
depends_on: ["257-01", "257-03", "257-04", "257-05"]
files_modified:
  - packages/spec/src/constants.ts
  - packages/engine/src/state.test.ts
  - packages/engine/src/purity.test.ts
  - packages/engine/src/kernel/create-initial-state.ts
  - packages/engine/src/kernel/create-initial-state.test.ts
autonomous: true
requirements: [KERN-03, KERN-08, KERN-10, KERN-11]
must_haves:
  truths:
    - "Canonical nested constants cannot be mutated through imports or outward state construction."
    - "The candidate initial factory owns cloned bounds, terrain, starts, versions, players, and Soldiers; active current factory remains unchanged until Plan 19 (KERN-08)."
    - "Candidate initial state is semantically validated and valid v1.4 starts remain byte-equivalent (D-05, D-07, D-12)."
  artifacts:
    - path: "packages/engine/src/kernel/create-initial-state.ts"
      provides: "Inactive clone-owning, semantic-validating initial-state factory"
    - path: "packages/spec/src/constants.ts"
      provides: "Deeply immutable canonical constant source"
  key_links:
    - from: "packages/engine/src/kernel/create-initial-state.ts"
      to: "packages/spec/src/semantic-integrity.ts"
      via: "initial arena/state admission before returning GameState"
      pattern: "validateCanonical"
---

<objective>
Eliminate mutable canonical aliases and make initial-state admission semantic and failure-safe.

Purpose: KERN-08 and the initial KERN-03 boundary must ensure external mutation cannot change later Matches, fixtures, hashes, or evidence.
Output: Frozen internal constants, deep-cloned states, and mutation-isolation proof.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-05-SUMMARY.md
@packages/spec/src/constants.ts
@packages/engine/src/state.ts
@packages/spec/src/integrity-authority.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deep-freeze canonical constants and clone all outward values</name>
  <files>packages/spec/src/constants.ts, packages/engine/src/kernel/create-initial-state.ts, packages/engine/src/kernel/create-initial-state.test.ts, packages/engine/src/state.test.ts</files>
  <behavior>
    - Imported bounds/start arrays/positions/version values reject mutation or expose no shared mutable reference.
    - Mutating caller arena input after state creation cannot affect the state.
    - Mutating a returned first state or completed first Match cannot affect a second state, fixture, authority hash, or Match.
    - Nested terrain/start/player/Soldier/versions references differ across Match constructions while values remain equal.
  </behavior>
  <action>Write mutation attempts first, deep-freeze internal canonical arrays/objects, and implement explicit finite deep cloning in the inactive candidate initial-state factory. Keep the active `createInitialGameState` facade/behavior unchanged until Plan 19. Preserve values and never rely on `as const` alone or a generic hostile-value clone.</action>
  <verify><automated>pnpm --filter @cowards/engine exec vitest run src/state.test.ts -t "mutation|clone|constant"</automated></verify>
  <done>All constant/caller/state/Match mutation paths are isolated and repeated Matches remain pristine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate initial arena and state before exposure</name>
  <files>packages/engine/src/kernel/create-initial-state.ts, packages/engine/src/kernel/create-initial-state.test.ts, packages/engine/src/state.test.ts, packages/engine/src/purity.test.ts</files>
  <behavior>
    - Shape-valid overlapping/out-of-bounds/duplicate arena data fails with the stable system/integrity issue set before a GameState is returned.
    - Canonical valid initial states pass, remain deterministic, and preserve v1.4 initiative/start semantics.
    - Initial validation performs no time, randomness, filesystem, network, database, or runtime effect.
  </behavior>
  <action>Call shared validators on cloned candidate states. Return the typed integrity construction failure without partial state and extend purity guards. Preserve exact starts/initiative, assert active-old behavior byte equality, and leave facade switching to Plan 19.</action>
  <verify><automated>pnpm --filter @cowards/engine exec vitest run src/kernel/create-initial-state.test.ts src/state.test.ts src/purity.test.ts</automated></verify>
  <done>Initial construction is deterministic, clone-owned, semantically admitted, and side-effect free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Imported/caller-owned objects to canonical Match state | Shared references could mutate later Matches and invalidate evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-06 | Tampering | constants and initial-state factory | high | mitigate | Explicit deep freeze/clone, mutation tests across Matches, semantic admission, purity sentinel. |
| T-257-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>

<verification>Run state/purity suites, then rerun the v1.4 compatibility corpus to prove values did not drift.</verification>
<success_criteria>No caller can mutate canonical constants or later Match state, and invalid initial semantics fail before gameplay.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-06-SUMMARY.md` when done.</output>
