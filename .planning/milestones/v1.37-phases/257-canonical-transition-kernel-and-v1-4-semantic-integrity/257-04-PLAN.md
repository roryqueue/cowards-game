---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/spec/src/fixtures/semantic-integrity-vectors.json
  - packages/spec/src/semantic-integrity.test.ts
  - packages/engine/src/kernel/semantic-boundaries.test.ts
  - packages/replay/src/semantic-integrity.test.ts
  - apps/runtime-service/src/semantic-integrity.test.ts
  - packages/persistence/src/semantic-integrity.test.ts
  - apps/go-backend/semantic_integrity_test.go
autonomous: true
requirements: [KERN-03, KERN-11]
must_haves:
  truths:
    - "One shared RED vector corpus covers arena, initial, transition, runtime-final, persisted, and reconstructed semantic invalidity."
    - "Stable invariant codes are bounded, deterministic, canonically ordered, and ownership-classified at every trust boundary (D-05 through D-08)."
    - "Invalid canonical evidence is system/integrity failure with unchanged gameplay and zero canonical Chronicle/database mutation; only illegal Strategy output may be player violation."
  artifacts:
    - path: "packages/spec/src/fixtures/semantic-integrity-vectors.json"
      provides: "Language-neutral semantic-validity and stable-code vectors"
    - path: "packages/persistence/src/semantic-integrity.test.ts"
      provides: "Pre-write/no-mutation PostgreSQL RED contract"
  key_links:
    - from: "apps/go-backend/semantic_integrity_test.go"
      to: "packages/spec/src/fixtures/semantic-integrity-vectors.json"
      via: "shared code/order vectors without Go scheduling"
      pattern: "semantic-integrity-vectors"
---

<objective>
Create the cross-boundary semantic-integrity RED matrix before validators exist.

Purpose: KERN-03 and D-05 through D-08 require identical meaning and failure ownership across engine, replay, runtime-service, TypeScript persistence, and Go.
Output: Shared vectors and boundary-specific tests that fail only on missing semantic enforcement.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-RESEARCH.md
@packages/spec/src/types.ts
@packages/spec/src/schemas.ts
@packages/engine/src/invariants.test.ts
@packages/replay/src/validate.ts
@packages/persistence/src/complete-match.ts
@apps/go-backend/runtime_service_client.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shape-valid semantic vectors and stable expected codes</name>
  <files>packages/spec/src/fixtures/semantic-integrity-vectors.json, packages/spec/src/semantic-integrity.test.ts, packages/engine/src/kernel/semantic-boundaries.test.ts</files>
  <behavior>
    - Reject inverted/degenerate bounds, out-of-bounds or duplicate terrain, terrain/start overlap, and non-canonical starts.
    - Reject duplicate Soldier/player IDs, unknown owners, duplicate occupancy, out-of-bounds positions, terrain occupancy, and invalid ACTIVE/STONE/FALLEN position/facing combinations.
    - Reject bad initiative, lifecycle cursor/quota/pending-effect identity, mixed/unknown tuple, outcome/active-count/winner incoherence, post-terminal continuation, and event/hash mismatch.
    - Return all applicable issues up to fixed issue/path/metadata bounds in declared family/code order, independent of insertion or locale order (D-08).
  </behavior>
  <action>Create valid baseline vectors plus single- and multi-fault mutations that are structurally schema-valid. Name expected public category, restricted stable codes, paths, and safe metadata. Add permutation tests for ordering and explicit caps. Add exact named spec and engine tests that emit `[EXPECTED_RED:MISSING_SEMANTIC_CONTRACT:SPEC]` and `[EXPECTED_RED:MISSING_SEMANTIC_CONTRACT:ENGINE]` only after each test reaches the absent semantic contract. The tests must fail for missing semantics, not malformed JSON, Zod shape rejection, missing discovery, imports, compilation, or configuration.</action>
  <verify><automated>zsh -c 'set -eu; run_red() { LABEL=$1; shift; set +e; OUT=$("$@" 2&gt;&amp;1); RC=$?; set -e; print -r -- "$OUT"; test $RC -ne 0; print -r -- "$OUT" | rg -F "[EXPECTED_RED:MISSING_SEMANTIC_CONTRACT:$LABEL]"; if print -r -- "$OUT" | rg -qi "no test files|no tests found|cannot find module|failed to load|compile error|type error|configuration|timed out|timeout"; then return 1; fi; }; run_red SPEC pnpm --filter @cowards/spec exec vitest run src/semantic-integrity.test.ts -t "missing-semantic-contract: shared vectors require validator"; run_red ENGINE pnpm --filter @cowards/engine exec vitest run src/kernel/semantic-boundaries.test.ts -t "missing-semantic-contract: engine boundaries require validator"'</automated></verify>
  <done>Every KERN-03 invariant class has an exact deterministic RED vector.</done>
</task>

<task type="auto">
  <name>Task 2: Add replay, runtime-service, and TypeScript persistence RED boundaries</name>
  <files>packages/replay/src/semantic-integrity.test.ts, apps/runtime-service/src/semantic-integrity.test.ts, packages/persistence/src/semantic-integrity.test.ts</files>
  <behavior>
    - Exact test `missing-semantic-enforcement: replay rejects invalid intermediate state` rejects the first invalid intermediate/final state and never schedules gameplay.
    - Exact test `missing-semantic-enforcement: runtime final becomes system failure without Chronicle` returns a redacted retryable system failure and no canonical Chronicle/player penalty.
    - Exact test `missing-semantic-enforcement: persistence rejects before mutation` validates before deriving/writing and leaves Match, job, Chronicle, result, and standings rows unchanged.
  </behavior>
  <action>Consume the shared vectors at each trust boundary. Add spies/row snapshots proving validation happens before recorder, derived completion fields, transaction mutation, public success, or player-violation cleanup. Each exact named test must deliberately emit its unique `[EXPECTED_RED:MISSING_SEMANTIC_ENFORCEMENT:*]` assertion marker only when the missing semantic guard is reached. Initialize real local PostgreSQL first, execute the three tests separately, capture each output and exit status, require nonzero plus the matching marker, and reject compile, configuration, missing-test, module-resolution, database-connection, migration, timeout, and other infrastructure failures. An arbitrary nonzero is never RED evidence.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; zsh -c 'set -eu; run_red() { LABEL=$1; shift; set +e; OUT=$("$@" 2&gt;&amp;1); RC=$?; set -e; print -r -- "$OUT"; test $RC -ne 0; print -r -- "$OUT" | rg -F "[EXPECTED_RED:MISSING_SEMANTIC_ENFORCEMENT:$LABEL]"; if print -r -- "$OUT" | rg -qi "no test files|no tests found|cannot find module|failed to load|compile error|type error|ECONNREFUSED|connection refused|database .* does not exist|migration.*fail|timed out|timeout"; then return 1; fi; }; run_red REPLAY pnpm --filter @cowards/replay exec vitest run src/semantic-integrity.test.ts -t "missing-semantic-enforcement: replay rejects invalid intermediate state"; run_red RUNTIME pnpm --filter @cowards/runtime-service exec vitest run src/semantic-integrity.test.ts -t "missing-semantic-enforcement: runtime final becomes system failure without Chronicle"; run_red PERSISTENCE env DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game pnpm --filter @cowards/persistence exec vitest run src/semantic-integrity.test.ts -t "missing-semantic-enforcement: persistence rejects before mutation"'</automated></verify>
  <done>Every TypeScript consumer boundary has a precise missing-validation RED contract and no-mutation assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Add Go shared-vector and rollback RED boundary</name>
  <files>apps/go-backend/semantic_integrity_test.go</files>
  <behavior>
    - Go returns the same stable codes/order/public category as TypeScript vectors.
    - Runtime response and completion reject semantic invalidity before orchestration or SQL mutation.
    - Go contains no Phase/Round/Cycle/Activation/Contraction scheduler.
  </behavior>
  <action>Load the committed JSON vectors relative to repository root using existing checksum/path conventions. Add response, orchestration, and explicit PostgreSQL rollback subtests. The exact Go test must reach the absent validator and emit `[EXPECTED_RED:MISSING_SEMANTIC_ENFORCEMENT:GO]`; no arbitrary nonzero result counts. Fail only on the missing Go validator/guard; do not implement gameplay resolution or copy the TypeScript loop.</action>
  <verify><automated>scripts/dev-local-postgres.sh --setup-only &amp;&amp; zsh -c 'set -eu; cd apps/go-backend; set +e; OUT=$(COWARDS_GO_BACKEND_TEST_DATABASE_URL=postgresql://cowards:cowards@localhost:5432/cowards_game go test ./... -run "^TestSemanticIntegrityMissingEnforcement$" -count=1 2&gt;&amp;1); RC=$?; set -e; print -r -- "$OUT"; test $RC -ne 0; print -r -- "$OUT" | rg -F "[EXPECTED_RED:MISSING_SEMANTIC_ENFORCEMENT:GO]"; if print -r -- "$OUT" | rg -qi "no tests to run|undefined:|build failed|cannot find|compile|connection refused|ECONNREFUSED|database .* does not exist|migration.*fail|timed out|timeout"; then exit 1; fi'</automated></verify>
  <done>Go has a shared-vector RED contract for meaning, ordering, pre-write rejection, and no scheduling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shape-valid data to canonical semantic evidence | Cross-field invalidity can pass type/schema checks and become gameplay or persisted truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-04 | Tampering/Denial of Service/Elevation of Privilege | semantic boundary matrix | high | mitigate | Shape-valid adversarial vectors, bounded deterministic codes, ownership classification, pre-effect/pre-write spies, and real PostgreSQL rollback. |
| T-257-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>

<verification>Confirm each boundary RED command fails for missing semantic enforcement and the shared vector file is deterministic.</verification>
<success_criteria>All KERN-03 boundaries are represented before implementation and system failure cannot be confused with Strategy fault.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-04-SUMMARY.md` when done.</output>
