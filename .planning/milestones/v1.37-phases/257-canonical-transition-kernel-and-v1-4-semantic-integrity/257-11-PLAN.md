---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "11"
type: execute
wave: 2
depends_on: ["257-01", "257-02"]
files_modified:
  - scripts/check-v1-37-executable-reference-inventory.ts
  - scripts/check-v1-37-executable-reference-inventory.test.ts
autonomous: true
requirements: [KERN-01, KERN-02, KERN-07, KERN-10, KERN-11]
must_haves:
  truths:
    - "One token/AST-aware manifest is the authoritative ownership table for every exact buildChronicleFromMatch and resolveActivation definition, import, property, type, and call reference (D-13, D-16)."
    - "Near-name identifiers, comments, README text, and negative monitor/guard literals are classified separately and cannot satisfy or fail executable cleanup gates."
    - "Every staged mode identifies its owning plan and exact allowed remainder; no group can claim another group's migration."
  artifacts:
    - path: "scripts/check-v1-37-executable-reference-inventory.ts"
      provides: "Exact executable-reference ownership manifest and staged deletion gates"
  key_links:
    - from: "scripts/check-v1-37-executable-reference-inventory.ts"
      to: "packages/replay/src/build.ts"
      via: "TypeScript token/AST positions rather than substring search"
      pattern: "buildChronicleFromMatch|resolveActivation"
---
<objective>Freeze the complete live executable-reference inventory and migration ownership before any caller group moves.</objective>
<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-RESEARCH.md
@packages/replay/src/build.ts
@packages/engine/src/activation.ts
@scripts/check-v1-37-integrity-boundaries.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Install the authoritative exact-reference ownership table and staged modes</name>
  <files>scripts/check-v1-37-executable-reference-inventory.ts, scripts/check-v1-37-executable-reference-inventory.test.ts</files>
  <behavior>
    - Baseline classifies replay build/source/tests, replay determinism/grammar/integration/reconstruct/snapshot/validate tests, persistence complete-match tests, runtime-js, worker, runtime-service source/tests, golden/test-utils/generator, Activation tests/definition, audit probe, README/docs/comments, and negative monitor literals.
    - Ownership is exact: Plan 10 owns recorder plus fixture/golden/runtime-js/worker; Plan 13 replay semantic core; Plan 14 replay fixture tests; Plan 15 runtime-service/build tests; Plan 16 persistence; Plan 18 Activation tests/probe; Plan 19 final service caller and both definitions/deletions.
    - Modes are mutation-tested and monotonic: `--baseline`, `--recorder-migrated`, `--replay-core-ready`, `--replay-fixtures-ready`, `--runtime-service-ready`, `--persistence-ready`, `--activation-callers-ready`, `--activation-ready`, and `--current`.
    - `--activation-callers-ready` is resolveActivation-specific: it requires zero exact imports/calls outside the retained active definition/export while independently allowing the builder inventory appropriate to unfinished Plans 13-16; `--activation-ready` permits only the two old definitions immediately before deletion; `--current` permits no executable reference or definition.
  </behavior>
  <action>Parse TypeScript tokens/AST nodes and record exact file, node kind, symbol role, and owner. Seed from a fresh current-HEAD inventory and fail unowned or moved references. Mutation-test exact identifiers, aliases, property keys, type positions, real calls, comments, strings, near names such as `resolveActivationCycle`, and allowed negative guard literals. The ownership table above is authoritative; later plans may only satisfy their named mode and may not edit this manifest to hide a reference.</action>
  <verify><automated>pnpm exec vitest run scripts/check-v1-37-executable-reference-inventory.test.ts &amp;&amp; pnpm exec tsx scripts/check-v1-37-executable-reference-inventory.ts --baseline</automated></verify>
  <done>Every live exact reference has one migration owner and every stage has an executable no-false-positive gate.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Source text to deletion proof | Substrings or incomplete inventories could conceal a live duplicate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-11 | Tampering/Repudiation | executable reference inventory | high | mitigate | AST roles, complete owner table, monotonic stage modes, mutation tests. |
| T-257-SC | Tampering | package supply chain | low | accept | No install. |
</threat_model>
<verification>Inventory tests and exact baseline mode pass before any assigned caller migration.</verification>
<success_criteria>No caller or definition can disappear from planning or survive activation unowned.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-11-SUMMARY.md` when done.</output>
