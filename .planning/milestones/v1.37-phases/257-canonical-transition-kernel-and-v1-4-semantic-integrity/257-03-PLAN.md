---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/engine/src/fixtures/v1-4-compatibility.ts
  - packages/engine/src/compatibility-fixtures.test.ts
  - CowardsGameSpec_RulesIntegrityAddendum_v1.37.md
autonomous: true
requirements: [KERN-10, KERN-11]
must_haves:
  truths:
    - "A before-refactor corpus compares initial/full intermediate/final state, ordered events, runtime calls, Strategy observations, memories, objectives, outcome, and failure trace for every audited v1.4 ambiguity."
    - "The only allowed semantic deltas are D-09 through D-15; every other valid-input delta halts execution for explicit approval."
    - "Cycle-start Backstab remains present and HOLD/END_ACTIVATION remains absent (D-12 and deferred boundaries)."
    - "Approved current and preserved historical rulings are documented without changing the user-dirty consolidated spec."
  artifacts:
    - path: "packages/engine/src/fixtures/v1-4-compatibility.ts"
      provides: "Named full-observation v1.4 compatibility corpus"
    - path: "CowardsGameSpec_RulesIntegrityAddendum_v1.37.md"
      provides: "Current companion ruling record with protected-spec provenance"
  key_links:
    - from: "packages/engine/src/compatibility-fixtures.test.ts"
      to: "packages/engine/src/fixtures/v1-4-compatibility.ts"
      via: "byte-stable full-observation fixture comparisons"
      pattern: "allowedDelta|strategyObservations|events"
---

<objective>
Capture the complete valid-v1.4 semantic envelope before the kernel refactor.

Purpose: D-12 and KERN-10/KERN-11 require evidence stronger than outcome-only goldens, while the protected consolidated spec cannot be edited.
Output: Named executable compatibility fixtures and a v1.37 companion addendum.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-CONTEXT.md
@CowardsGameSpec_CycleInterleaved_v1.4.md
@CowardsGame_Technical_Architecture_Spec_v1.4.md
@packages/engine/src/movement.test.ts
@packages/engine/src/backstab.test.ts
@packages/golden/src/parity.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the full v1.4 compatibility corpus</name>
  <files>packages/engine/src/fixtures/v1-4-compatibility.ts, packages/engine/src/compatibility-fixtures.test.ts</files>
  <action>Build independently named deterministic scenarios for same-direction rear blocking; head-to-head and occupied-target distinctions; successful push pusher history and unchanged pushed-Soldier history; terrain/STONE/active/failed-push blocking; illegal reversal; Cycle-interleaved snake order and initiative; Cycle-start/end/simultaneous/mutual/multi-victim Backstab; push/TURN boundary eligibility; contraction/final 2x2; player violation versus system failure; observation/memory/objective ordering; and exactly one terminal event. Capture initial state, every lifecycle coordinate and transition-equivalent observation available before refactor, ordered event payload/context/privacy, runtime call/input/output sequence, StrategyMemory/SoldierMemory/objective handoff, final state, outcome, and deterministic hashes. Assert repeated runs are identical. Do not encode Phase-257 defects as preserved semantics.</action>
  <verify><automated>pnpm --filter @cowards/engine exec vitest run src/compatibility-fixtures.test.ts</automated></verify>
  <done>Every audited ambiguity has a passing, full-observation pre-refactor fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce an explicit compatibility-delta allowlist</name>
  <files>packages/engine/src/compatibility-fixtures.test.ts</files>
  <behavior>
    - Only D-09, D-10, D-11, D-13, D-14, and D-15 may explain a post-refactor delta.
    - D-12 preserved state, legality, event order, outcome, terminal behavior, runtime calls, or Strategy observations cannot be regenerated into passing expectations.
    - Fixture regeneration requires an explicit named approved delta and rejects unknown/missing/duplicate approval IDs.
  </behavior>
  <action>Add a mutation test for every compared dimension and prove each unapproved mutation fails with a bounded human-readable compatibility finding. Make fixture write/update mode fail closed unless every delta maps to the locked allowlist. If implementation later reveals any other valid-input delta, executors must stop and ask the user rather than edit this corpus.</action>
  <verify><automated>pnpm --filter @cowards/engine exec vitest run src/compatibility-fixtures.test.ts -t "compatibility delta"</automated></verify>
  <done>Golden drift cannot silently authorize a gameplay or Strategy-observation change.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the non-overlapping v1.37 rules-integrity addendum</name>
  <files>CowardsGameSpec_RulesIntegrityAddendum_v1.37.md</files>
  <action>Before any task edit, capture both the SHA-256 of the exact dirty working-copy bytes and the SHA-256 of `git diff --binary -- CowardsGameSpec_Full_Consolidated_v1.md` in task-local variables/evidence. Read canonical committed bytes only with `git show HEAD:CowardsGameSpec_Full_Consolidated_v1.md`; never use the dirty copy as canonical and never modify it. Document D-09 through D-15 exact order/reasons, the full D-12 preservation bundle, current event removal and tuple consequence, historical v1.4 routing, KERN-11 approval gate, Cycle-start scan retention, and HOLD/END_ACTIVATION deferral. Immediately before commit, recompute both working-copy and binary-diff hashes and require exact equality with the captured pre-task values; a merely still-dirty file is not success. Record both before/after pairs in the SUMMARY.</action>
  <verify><automated>test -s CowardsGameSpec_RulesIntegrityAddendum_v1.37.md &amp;&amp; rg -n "D-09|BACKSTABBED|PUSH_ATTEMPTED|Cycle-start|HOLD|KERN-11" CowardsGameSpec_RulesIntegrityAddendum_v1.37.md</automated></verify>
  <done>The approved semantics are documented in a new companion, and exact pre/post working bytes plus binary diff prove protected user work did not change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Refactor output to compatibility fixtures | Re-recorded goldens could conceal a valid-v1.4 behavior change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-03 | Tampering/Repudiation | compatibility corpus and addendum | high | mitigate | Full-observation comparisons, mutation-tested allowlist, committed-spec provenance, and blocking KERN-11 gate. |
| T-257-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>

<verification>Run the corpus twice, run mutation cases, and confirm only the new addendum is planned for documentation changes.</verification>
<success_criteria>Valid v1.4 behavior is executable, comprehensive, and cannot be silently rewritten during refactor.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-03-SUMMARY.md` when done.</output>
