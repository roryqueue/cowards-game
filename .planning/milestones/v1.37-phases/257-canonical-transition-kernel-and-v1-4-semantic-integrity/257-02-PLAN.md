---
phase: 257-canonical-transition-kernel-and-v1-4-semantic-integrity
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/engine/src/lifecycle-repairs.test.ts
  - packages/engine/src/activation.test.ts
autonomous: true
requirements: [KERN-04, KERN-05, KERN-06, KERN-10, KERN-11]
must_haves:
  truths:
    - "RED fixtures freeze exact status/closure/outcome ordering for the final-Soldier no-Advance defect (D-11)."
    - "RED fixtures freeze simultaneous Cycle-end Backstab events followed by one BACKSTABBED slot closure and immediate outcome (D-10)."
    - "Activation orders are capped before validation, with a complete retained-prefix/suffix precedence matrix (D-09)."
    - "Preserved movement, Backstab, scheduling, terminal, and Strategy-observation semantics cannot drift while repairs are implemented (D-12, KERN-11)."
  artifacts:
    - path: "packages/engine/src/lifecycle-repairs.test.ts"
      provides: "Exact lifecycle and retained-prefix RED regressions"
  key_links:
    - from: "packages/engine/src/lifecycle-repairs.test.ts"
      to: "packages/engine/src/activation.ts"
      via: "public engine behavior and exact event order"
      pattern: "BACKSTABBED|NO_ADVANCE|MATCH_ENDED"
---

<objective>
Write the exact approved lifecycle and excess-order repairs as executable RED cases before changing resolution code.

Purpose: KERN-04 through KERN-06 are narrow semantic changes; D-09 through D-12 and KERN-11 must constrain every observable detail.
Output: A permanent focused regression matrix that fails on current defects for the correct reasons.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/gsd-core/workflows/execute-plan.md
@/Users/roryquinlan/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-CONTEXT.md
@.planning/artifacts/v2.0-core-rules-audit/reproduce-core-rule-gaps.ts
@packages/engine/src/activation.ts
@packages/engine/src/backstab.ts
@packages/engine/src/outcome.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze no-Advance and Cycle-end Backstab event order</name>
  <files>packages/engine/src/lifecycle-repairs.test.ts</files>
  <behavior>
    - Final active Soldier cleanup emits SOLDIER_STONED, ACTIVATION_ENDED with the existing no-Advance reason, then exactly one MATCH_ENDED before any skipped/scheduled slot (D-11).
    - A Cycle-end scan completes all simultaneous Backstab events, emits CYCLE_ENDED under the existing boundary contract, closes the removed actor once as BACKSTABBED, then evaluates outcome immediately (D-10).
    - A removed actor cannot resume or be scheduled again, and no event occurs after terminal Match outcome.
  </behavior>
  <action>Create independent state/event fixtures that assert full state, lifecycle coordinates, exact event types/payload/context order, terminal reason, outcome, runtime-call count, and absence of later scheduling. Ensure current production fails only on the documented missing outcome/closure behavior. Do not change event expectations outside D-10/D-11.</action>
  <verify><automated>zsh -c 'set +e; OUT=$(pnpm --filter @cowards/engine exec vitest run src/lifecycle-repairs.test.ts -t "no-Advance|Cycle-end Backstab" 2&gt;&amp;1); STATUS=$?; print -r -- "$OUT"; test $STATUS -ne 0; print -r -- "$OUT" | rg "MATCH_ENDED|BACKSTABBED|outcome|slot"'</automated></verify>
  <done>Both confirmed lifecycle defects have precise RED state/event/terminal contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze literal cap-then-validate precedence</name>
  <files>packages/engine/src/lifecycle-repairs.test.ts, packages/engine/src/activation.test.ts</files>
  <behavior>
    - Slice raw activation-order entries to the Round quota before shape or semantic validation (D-09).
    - Validate every retained entry for shape, identity, owner, ACTIVE status, and duplication.
    - Malformed, duplicate, unknown, wrong-owner, or inactive entries outside the retained prefix are ignored; the same cases inside are player violations.
    - An invalid retained entry is never replaced by scanning a later valid entry.
  </behavior>
  <action>Add a table crossing quota, position inside/outside retained prefix, and valid/malformed/duplicate/unknown/wrong-owner/inactive classifications. Assert retained order, violation events, runtime classification, and observation count. Keep the existing player-violation cleanup for invalid retained Strategy output; do not reclassify canonical-state failure as player fault.</action>
  <verify><automated>zsh -c 'set +e; OUT=$(pnpm --filter @cowards/engine exec vitest run src/lifecycle-repairs.test.ts -t "retained prefix" 2&gt;&amp;1); STATUS=$?; print -r -- "$OUT"; test $STATUS -ne 0; print -r -- "$OUT" | rg "retained|prefix|violation"'</automated></verify>
  <done>The full D-09 precedence matrix is executable and current malformed-excess behavior is demonstrably RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hostile Strategy order/action output to engine legality | Ignored suffix data or malformed retained data could alter scheduling or blame ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-257-02 | Tampering/Denial of Service | activation selection and closure | high | mitigate | Literal cap-first matrix, exact event ordering, no replacement scan, and KERN-11 differential gate. |
| T-257-SC | Tampering | package supply chain | low | accept | No package install. |
</threat_model>

<verification>Run the focused RED subsets and verify their failures name only the three confirmed defects.</verification>
<success_criteria>Every approved repair has an exact failing behavior contract, and no other v1.4 observable is authorized to change.</success_criteria>
<output>Create `.planning/phases/257-canonical-transition-kernel-and-v1-4-semantic-integrity/257-02-SUMMARY.md` when done.</output>
