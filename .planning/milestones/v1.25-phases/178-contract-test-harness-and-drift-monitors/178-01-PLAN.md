# Phase 178 Plan

1. Add schema tests for the app-facing contract and fixtures.
2. Validate committed Go public fixtures by projecting them into the app contract.
3. Add app adapter tests for fixture mode.
4. Extend the aggregate boundary monitor with v1.25 drift checks.
