# Phase 180 Plan

1. Add a fixture-backed public read client for Match execution fixtures.
2. Gate fixture mode to test/dev explicit flags.
3. Wire result and replay server paths to use fixture DTOs when explicitly enabled.
4. Test enabled, disabled, known fixture, and unknown fixture behavior.
