# Phase 175 Summary

Completed in `packages/spec/src/match-execution-contract.ts`.

The canonical states are `queued`, `accepted`, `running`, `complete`, `failed`, `degraded`, and `unavailable`. Retryability is represented by `retryDisposition`.
