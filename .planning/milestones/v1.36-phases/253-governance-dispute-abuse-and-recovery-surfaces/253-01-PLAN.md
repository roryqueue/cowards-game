---
phase: 253-governance-dispute-abuse-and-recovery-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [GOV-01, GOV-02, GOV-03, GOV-04, GOV-05, GOV-06]
files_modified:
  - packages/spec/src/competition-governance.ts
  - packages/spec/src/competition-governance.test.ts
  - packages/spec/src/competition.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/index.ts
  - packages/persistence/migrations/0011_competition_governance_surfaces.sql
  - packages/persistence/src/migrations.test.ts
  - packages/persistence/src/competition.ts
  - packages/persistence/src/competition.test.ts
  - packages/persistence/src/ladder.ts
  - packages/persistence/src/ladder.test.ts
  - packages/persistence/src/profiles.ts
must_haves:
  truths:
    - Report, dispute, and admin-governance inputs use one bounded spec-owned vocabulary; no HTTP input can supply arbitrary public explanation copy.
    - Public governance output is limited to coarse status, safe reason, fixed explanation, applicable timestamp, standings effect, and Chronicle-derived replay availability.
    - Fair-play and recovery copy describes current limits without promising automatic punishment, staffed response or appeal SLAs, public sanction history, permanent rating repair, ownership transfer, or full recovery.
    - Storage provides immutable report history, race-safe open-submission deduplication, disputed review state, a public-safe governance timestamp, and append-only audit enforcement.
  artifacts:
    - path: packages/spec/src/competition-governance.ts
      provides: Canonical submission/action vocabulary, safe-copy matrix, public projection, policy copy, and governance-specific leak guard
    - path: packages/spec/src/competition-governance.test.ts
      provides: Input bounds, action matrix, fixed-copy, forbidden-claim, and public leak tests
    - path: packages/persistence/src/competition.ts
      provides: TypeScript public result governance projection from canonical MatchSet and Chronicle evidence
    - path: packages/persistence/src/ladder.ts
      provides: Mutation-free Season/standings governance projection with replay-independent evidence links
    - path: packages/persistence/migrations/0011_competition_governance_surfaces.sql
      provides: Report storage, review-state expansion, public timestamp, indexes, and immutable-audit triggers
  key_links:
    - from: packages/spec/src/competition-counted-state.ts
      to: packages/spec/src/competition-governance.ts
      via: Canonical counted states, public reasons, standings effects, and evidence-derived replay semantics
    - from: packages/spec/src/competition-governance.ts
      to: packages/spec/src/competition.ts
      via: Shared PublicCompetitionGovernanceProjection on ladder and result DTOs
    - from: packages/spec/src/competition-governance.ts
      to: packages/persistence/src/competition.ts
      via: Fixed projection copy and governance-specific leak assertion
    - from: packages/spec/src/competition-governance.ts
      to: packages/persistence/migrations/0011_competition_governance_surfaces.sql
      via: Submission, review, report-status, and governance-category storage vocabulary parity
---

# Plan 253-01: Governance Contract, Storage, and Policy Copy

<objective>Lock the public/private governance contract and database invariants before any report, dispute, or admin mutation is implemented.</objective>

<tasks>
<task id="253-01-01" type="auto" tdd="true">
  <name>Define the canonical governance and policy contract</name>
  <files>packages/spec/src/competition-governance.ts, packages/spec/src/competition-governance.test.ts, packages/spec/src/index.ts</files>
  <action>Add RED table tests, then define `report | dispute` submissions, the five product-facing categories, bounded optional private detail (trimmed, maximum 500 characters, no NUL/control payloads), allowed admin state/reason combinations, fixed public copy, and the coarse public governance projection. Derive standings effect from the Phase 252 counted-state contract and keep replay availability as a caller-supplied Chronicle fact. Add spec-owned fair-play/reporting and policy-only account-recovery copy plus a governance-specific leak assertion rejecting reporter/operator identity, private detail, report counts, workflow/rate-limit metadata, audit payloads, recovery evidence, credentials, source/memory/objective data, diagnostics, tokens, paths, and database internals.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/competition-governance.test.ts</automated></verify>
  <done>The action matrix is exhaustive, public copy is deterministic, private input is bounded, and policy/leak tests reject every forbidden claim and field class.</done>
</task>
<task id="253-01-02" type="auto" tdd="true">
  <name>Wire DTO, schema, and TypeScript public projections</name>
  <files>packages/spec/src/competition.ts, packages/spec/src/schemas.ts, packages/spec/src/competition-governance.test.ts, packages/persistence/src/competition.ts, packages/persistence/src/competition.test.ts, packages/persistence/src/ladder.ts, packages/persistence/src/ladder.test.ts, packages/persistence/src/profiles.ts</files>
  <action>Add the typed governance projection beneath existing competition metadata on public ladder MatchSet summaries and public MatchSet results. Define strict request/receipt schemas for signed-in submissions and single/group admin actions with no body actor ids, no arbitrary public explanation, and a bounded unique MatchSet-id list. Project fixed copy through TypeScript reads using only canonical MatchSet fields, `governance_changed_at`, and Chronicle presence; never read private report/audit rows or prefer legacy arbitrary public copy. The temporary quarantined Next route consumes these schemas in Plan 02 without claiming selected service ownership.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/competition-governance.test.ts packages/persistence/src/competition.test.ts packages/persistence/src/ladder.test.ts packages/persistence/src/standings-recompute.test.ts</automated></verify>
  <done>Interfaces, schemas, and TypeScript public readers agree on one constrained coarse projection without reading governance-private storage.</done>
</task>
<task id="253-01-03" type="auto" tdd="true">
  <name>Add report and immutable-audit storage invariants</name>
  <files>packages/persistence/migrations/0011_competition_governance_surfaces.sql, packages/persistence/src/migrations.test.ts</files>
  <action>Create a repeatable additive migration that adds `governance_changed_at`, expands `review_status` to `none | under_review | disputed | resolved`, and creates append-oriented `competition_reports` storage with trial-MatchSet and reporter foreign keys, submission/category/status checks, bounded private detail, timestamps, a partial unique index for identical open submissions, and a reporter/time index. Leave `result_flags` legacy/read-only. Add database trigger functions rejecting update/delete of `competition_audit_events`; corrections must be compensating events. Pin migration order and required DDL in tests without adding report or recovery data to generic MatchSet metadata.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/migrations.test.ts</automated></verify>
  <done>The migration is safe to rerun, races cannot create duplicate open reports, disputed review state is valid, and audit history cannot be edited or deleted.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @cowards/spec typecheck && pnpm --filter @cowards/persistence typecheck`
- `pnpm exec vitest run packages/spec/src/competition-governance.test.ts packages/persistence/src/migrations.test.ts packages/persistence/src/competition.test.ts packages/persistence/src/ladder.test.ts packages/persistence/src/standings-recompute.test.ts`
- `pnpm v1.36:competition-policy:check`
</verification>

<boundaries>
- Do not implement report/admin mutations or UI in this plan; this wave only locks contracts and storage invariants.
- Do not accept free-form public explanations, expose private report/audit fields, or place sensitive intake in generic metadata.
- Do not add sanctions, appeals, moderation queues, response SLAs, account-recovery intake, ownership transfer, or permanent-rating repair.
- Do not change game rules, scoring, Strategy execution ownership, runtime/provider boundaries, or replay evidence.
</boundaries>
