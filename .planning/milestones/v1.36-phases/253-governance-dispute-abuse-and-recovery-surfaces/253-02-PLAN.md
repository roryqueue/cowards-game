---
phase: 253-governance-dispute-abuse-and-recovery-surfaces
plan: 02
type: execute
wave: 2
depends_on: [253-01]
autonomous: true
requirements: [GOV-01, GOV-02, GOV-04]
files_modified:
  - packages/persistence/src/governance.ts
  - packages/persistence/src/governance.test.ts
  - apps/web/app/competitive/server.ts
  - apps/web/app/api/matchsets/[matchSetId]/reports/route.ts
  - apps/web/app/api/matchsets/[matchSetId]/reports/route.test.ts
  - apps/web/app/api/matchsets/[matchSetId]/flags/route.ts
  - apps/web/app/api/admin/matchsets/[matchSetId]/governance/route.ts
  - apps/web/app/api/admin/matchsets/governance/route.ts
  - apps/web/app/api/admin/matchsets/governance/route.test.ts
must_haves:
  truths:
    - Any signed-in Player can file private general intake, but only an entrant owner dispute places counted evidence on hold.
    - Duplicate open submissions are idempotent, intake is transactionally bounded, and receipts never echo private workflow data.
    - Single and bounded group governance actions lock targets in stable order, validate before mutation, audit every target, and roll back as a unit.
    - Governance cannot force counted without complete scoring and Chronicle coverage and never edits standings, Matches, Chronicles, or replay evidence.
  artifacts:
    - path: packages/persistence/src/governance.ts
      provides: Transactional report/dispute intake and evidence-gated governance actions
    - path: packages/persistence/src/governance.test.ts
      provides: Authorization, dedupe, rate-limit, atomicity, audit, and evidence-gate tests
    - path: apps/web/app/api/matchsets/[matchSetId]/reports/route.ts
      provides: Signed-in constrained report/dispute endpoint
    - path: apps/web/app/api/admin/matchsets/governance/route.ts
      provides: Strict all-or-none group governance endpoint
  key_links:
    - from: packages/spec/src/competition-governance.ts
      to: packages/persistence/src/governance.ts
      via: Validated categories, action matrix, and fixed public copy
    - from: apps/web/app/api/matchsets/[matchSetId]/reports/route.ts
      to: apps/web/app/competitive/server.ts
      via: Authenticated temporary quarantined TypeScript delegation
---

# Plan 253-02: Atomic Governance Mutations and Routes

<objective>Implement private-safe report/dispute intake and auditable governance mutations behind strict authenticated routes.</objective>

<tasks>
<task id="253-02-01" type="auto" tdd="true">
  <name>Implement transactional report and governance persistence</name>
  <files>packages/persistence/src/governance.ts, packages/persistence/src/governance.test.ts</files>
  <action>Implement transaction-owned `submitCompetitionReport`: lock the reporter, require an eligible trial result, leave general reports intake-only, require entrant ownership for disputes, and atomically set disputed state/timestamp plus one audit event only for a new entrant dispute. Return existing open duplicates without changing private detail and bound accepted intake to five per ten minutes. Implement admin-only single/group actions for exactly the canonical six actions: deduplicate/sort at most 100 ids, lock/validate all targets before writes, gate counted through the Phase 252 classifier, stamp the public timestamp, and write one append-only audit event per target.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/governance.test.ts packages/persistence/src/standings-recompute.test.ts packages/persistence/src/ladder.test.ts</automated></verify>
  <done>General reports cannot suppress standings, disputes/actions are atomic and audited, and incomplete evidence cannot be forced counted.</done>
</task>
<task id="253-02-02" type="auto" tdd="true">
  <name>Expose strict authenticated report and admin routes</name>
  <files>apps/web/app/competitive/server.ts, apps/web/app/api/matchsets/[matchSetId]/reports/route.ts, apps/web/app/api/matchsets/[matchSetId]/reports/route.test.ts, apps/web/app/api/matchsets/[matchSetId]/flags/route.ts, apps/web/app/api/admin/matchsets/[matchSetId]/governance/route.ts, apps/web/app/api/admin/matchsets/governance/route.ts, apps/web/app/api/admin/matchsets/governance/route.test.ts</files>
  <action>Consume strict spec schemas through the explicitly quarantined competitive server boundary. Add the canonical report route, retain flags only as a constrained compatibility wrapper, and replace route-local defaults/arbitrary public text in admin routes. Derive identity only from session, reject unknown fields and invalid action/category combinations, and return stable coarse 401/403/404/409/422/429 responses without raw persistence text. Record this direct Next/persistence path as temporary non-normal ownership.</action>
  <verify><automated>pnpm exec vitest run 'apps/web/app/api/matchsets/[matchSetId]/reports/route.test.ts' 'apps/web/app/api/admin/matchsets/governance/route.test.ts'</automated></verify>
  <done>Routes expose only constrained receipts/errors and preserve all submission/operator data behind the server boundary.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @cowards/persistence typecheck && pnpm --filter @cowards/web typecheck`
- `pnpm exec vitest run packages/persistence/src/governance.test.ts packages/persistence/src/standings-recompute.test.ts packages/persistence/src/ladder.test.ts 'apps/web/app/api/matchsets/[matchSetId]/reports/route.test.ts' 'apps/web/app/api/admin/matchsets/governance/route.test.ts'`
</verification>

<boundaries>
- General reports are private intake only; only entrant-owner disputes automatically create a standings hold.
- The direct TypeScript/Next mutation path is a documented temporary quarantine exception, not selected-normal backend ownership.
- Do not expose request bodies, report/audit rows, reporter/operator identity, private detail, or persistence errors.
- Do not add moderation queues, sanctions, appeals, SLAs, recovery intake, ownership transfer, rating repair, game-rule changes, or Strategy execution.
</boundaries>
