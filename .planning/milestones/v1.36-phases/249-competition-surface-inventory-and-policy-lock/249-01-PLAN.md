---
phase: 249-competition-surface-inventory-and-policy-lock
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/spec/src/competition-policy-v1-36.ts
  - packages/spec/src/index.ts
  - packages/spec/src/spec.test.ts
autonomous: true
requirements:
  - POST-01
  - POST-02
  - POST-03
must_haves:
  truths:
    - "The spec package exports a versioned `competition-policy-v1.36` contract."
    - "The default posture phrase is exactly `public beta trial competition`."
    - "The contract defines resettable Season-scoped standings and no durable rating promise labels for current, completed, and archived competition trust surfaces."
    - "The contract defines public projection counted-state vocabulary only, without final persistence enum mappings."
    - "The contract defines forbidden claim categories plus examples for durable ratings, production sandbox, package ecosystem, TinyGo production, raw diagnostics, and private runtime overclaims."
  artifacts:
    - path: "packages/spec/src/competition-policy-v1-36.ts"
      provides: "Spec-owned v1.36 competition policy constants, schemas, owners, privacy exclusions, and forbidden claim taxonomy"
      exports:
        - "COMPETITION_POLICY_V1_36_ID"
        - "COMPETITION_POLICY_V1_36_POSTURE"
        - "COMPETITION_POLICY_V1_36_COUNTED_STATE_PUBLIC_PROJECTIONS"
        - "COMPETITION_POLICY_V1_36_FORBIDDEN_CLAIMS"
        - "assertCompetitionPolicyV136PublicLeakSafe"
    - path: "packages/spec/src/index.ts"
      provides: "Barrel export for the policy module"
    - path: "packages/spec/src/spec.test.ts"
      provides: "Focused contract and privacy tests"
  key_links:
    - from: "packages/spec/src/index.ts"
      to: "packages/spec/src/competition-policy-v1-36.ts"
      via: "barrel export"
      pattern: "competition-policy-v1-36\\.js"
    - from: "packages/spec/src/competition-policy-v1-36.ts"
      to: "packages/spec/src/public-output-privacy.ts"
      via: "public leak guard"
      pattern: "assertPublicOutputLeakSafe"
---

<objective>
Create the spec-owned `competition-policy-v1.36` contract that downstream inventory, monitors, UI, entry, Season, standings, governance, and proof work can consume.

Purpose: Phase 249 must lock posture, vocabulary, owners, privacy exclusions, and forbidden claim categories before any later behavior phase builds on competition trust surfaces.
Output: A tested `@cowards/spec` policy module with exact public posture copy, reset/no-durable labels, public counted-state projection vocabulary, privacy exclusions, authoritative owners, and forbidden claim categories/examples.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-CONTEXT.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-RESEARCH.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-VALIDATION.md
@.planning/phases/249-competition-surface-inventory-and-policy-lock/249-PATTERNS.md
@packages/spec/src/competition.ts
@packages/spec/src/public-discovery.ts
@packages/spec/src/public-output-privacy.ts
@packages/spec/src/index.ts
@packages/spec/src/spec.test.ts

<interfaces>
Create these contracts in `packages/spec/src/competition-policy-v1-36.ts`:

```typescript
export const COMPETITION_POLICY_V1_36_ID = "competition-policy-v1.36" as const

export const COMPETITION_POLICY_V1_36_POSTURE = {
  publicLabel: "public beta trial competition",
  standingsScope: "resettable Season-scoped standings",
  durableRatingPromise: "no durable permanent rating promise",
  completedSeasonLabel:
    "completed Seasons remain resettable public beta trial competition evidence, not durable rating truth",
  archivedSeasonLabel:
    "archived Seasons remain resettable public beta trial competition evidence, not durable rating truth",
} as const

export type CompetitionPolicyV136CountedStatePublicProjection =
  | "pending"
  | "counted"
  | "retrying"
  | "degraded_system_failure"
  | "non_counted"
  | "non_competitive"
  | "under_review"
  | "disputed"
  | "invalid"
  | "invalidated"

export type CompetitionPolicyV136ForbiddenClaimCategory =
  | "durable-rating"
  | "production-sandbox"
  | "package-ecosystem"
  | "tinygo-production"
  | "raw-diagnostic"
  | "private-runtime"
  | "mature-staffed-moderation"
  | "rating-refund"
  | "all-time-ranking"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing contract tests for competition-policy-v1.36</name>
  <files>packages/spec/src/spec.test.ts</files>
  <read_first>
    - packages/spec/src/spec.test.ts
    - packages/spec/src/competition.ts
    - packages/spec/src/public-output-privacy.ts
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-CONTEXT.md
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-VALIDATION.md
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `COMPETITION_POLICY_V1_36_ID` equals `competition-policy-v1.36` and `COMPETITION_POLICY_V1_36_POSTURE.publicLabel` equals exactly `public beta trial competition` per D-02 and POST-01.
    - Test 2: posture labels include `resettable Season-scoped standings`, `no durable permanent rating promise`, and completed/archived Season labels that preserve resettable/no durable rating copy per D-03 and D-04.
    - Test 3: public counted-state projection vocabulary includes pending, counted, retrying, degraded/system failure, non-counted, non-competitive, under review/disputed, invalid, and invalidated meanings, and the test asserts this is projection vocabulary only per D-10 and D-11.
    - Test 4: forbidden claims are represented as categories plus examples for durable-rating, production-sandbox, package-ecosystem, TinyGo-production, raw-diagnostic, private-runtime, mature-staffed-moderation, rating-refund, and all-time-ranking per D-12 and POST-02.
    - Test 5: authoritative owners include spec contract, Go/backend orchestration, runtime-service/provider boundary, persistence, web projection, and static monitor/proof owner labels without moving game rules into React or Strategy execution into web/API/Go.
    - Test 6: `assertCompetitionPolicyV136PublicLeakSafe` accepts the policy payload and rejects private markers including Strategy source, StrategyMemory, SoldierMemory, objective payload, raw diagnostics, artifact bytes, host paths, env values, tokens, DB details, package paths, private runtime internals, dispute internals, account-recovery payloads, and operator-only governance details.
  </behavior>
  <action>
    Extend `packages/spec/src/spec.test.ts` with a focused `describe("competition-policy-v1.36", ...)` block before broad integration tests. Import the policy symbols from `./competition-policy-v1-36.js`; the imports should fail until Task 2 implements the module.

    The tests must assert exact strings for `competition-policy-v1.36` and `public beta trial competition`. They must verify reset/no-durable copy for current, completed, and archived Seasons per D-01 through D-04. They must cover POST-01, POST-02, and POST-03 explicitly in test names or assertion messages. They must not add tests for entry enforcement, Season lifecycle transitions, standings recomputation, governance workflow mutation, public page redesign, or service-backed E2E proof because those are Phase 250-255 scopes.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/spec/src/spec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'competition-policy-v1\\.36|public beta trial competition|resettable Season-scoped standings|no durable permanent rating promise' packages/spec/src/spec.test.ts` returns all four strings.
    - `rg -n 'durable-rating|production-sandbox|package-ecosystem|tinygo-production|raw-diagnostic|private-runtime|mature-staffed-moderation|rating-refund|all-time-ranking' packages/spec/src/spec.test.ts` returns every forbidden claim category.
    - `rg -n 'StrategyMemory|SoldierMemory|objective payload|raw diagnostics|artifact bytes|host paths|env values|tokens|DB details|private runtime internals|dispute internals|account-recovery payloads|operator-only governance' packages/spec/src/spec.test.ts` returns privacy exclusion coverage.
    - `rg -n 'POST-01|POST-02|POST-03|D-02|D-11|D-12' packages/spec/src/spec.test.ts` returns requirement and decision traceability in test names, comments, or assertion labels.
    - `pnpm exec vitest run packages/spec/src/spec.test.ts` fails before Task 2 because the module does not exist, then passes after Task 2.
  </acceptance_criteria>
  <done>Spec tests describe the locked v1.36 competition policy contract before implementation and fail only because the new module/export is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and export the spec-owned policy contract</name>
  <files>packages/spec/src/competition-policy-v1-36.ts, packages/spec/src/index.ts, packages/spec/src/spec.test.ts</files>
  <read_first>
    - packages/spec/src/competition.ts
    - packages/spec/src/public-discovery.ts
    - packages/spec/src/public-output-privacy.ts
    - packages/spec/src/index.ts
    - packages/spec/src/spec.test.ts
    - .planning/phases/249-competition-surface-inventory-and-policy-lock/249-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: the module exports constants and types named with `COMPETITION_POLICY_V1_36` and the exact ID `competition-policy-v1.36`.
    - Test 2: the posture contract exposes prominent, plain posture labels for every competition trust surface category: standings, entry, result, replay, player, Strategy, Season, and competition index/detail per D-01 through D-04.
    - Test 3: counted-state values are public projection labels only and include no persistence enum mapping table, storage migration name, database enum, or final internal mapping per D-11.
    - Test 4: forbidden claim entries carry both a category and examples per D-12.
    - Test 5: policy public payload passes `assertPublicOutputLeakSafe`.
  </behavior>
  <action>
    Create `packages/spec/src/competition-policy-v1-36.ts` as a focused spec module. Import `assertPublicOutputLeakSafe` from `./public-output-privacy.js`; do not add new dependencies.

    Define:
    - `COMPETITION_POLICY_V1_36_ID = "competition-policy-v1.36" as const`.
    - `COMPETITION_POLICY_V1_36_POSTURE` with exact `publicLabel: "public beta trial competition"`, `standingsScope: "resettable Season-scoped standings"`, `durableRatingPromise: "no durable permanent rating promise"`, and completed/archived Season labels that state those Seasons remain resettable public beta trial competition evidence and not durable rating truth.
    - `COMPETITION_POLICY_V1_36_TRUST_SURFACES` including `competition-index`, `competition-detail`, `entry`, `season`, `standings`, `result`, `replay`, `player`, `strategy`, `governance`, `docs`, `fixtures`, `snapshots`, `proof-artifacts`, and `monitors`.
    - `COMPETITION_POLICY_V1_36_COUNTED_STATE_PUBLIC_PROJECTIONS` with public labels and standing effects for `pending`, `counted`, `retrying`, `degraded_system_failure`, `non_counted`, `non_competitive`, `under_review`, `disputed`, `invalid`, and `invalidated`. Include a module comment or exported metadata string stating Phase 249 locks public projection vocabulary only and Phase 252 owns final persistence enum mappings.
    - `COMPETITION_POLICY_V1_36_PRIVACY_EXCLUSIONS` containing Strategy source, artifact bytes, raw diagnostics, host paths, env values, package paths, tokens, DB details, private runtime internals, StrategyMemory, SoldierMemory, objective payloads, dispute internals, account-recovery payloads, and operator-only governance details.
    - `COMPETITION_POLICY_V1_36_FORBIDDEN_CLAIMS` entries for categories `durable-rating`, `production-sandbox`, `package-ecosystem`, `tinygo-production`, `raw-diagnostic`, `private-runtime`, `mature-staffed-moderation`, `rating-refund`, and `all-time-ranking`, each with at least two concrete example phrases to reject.
    - `COMPETITION_POLICY_V1_36_AUTHORITY_OWNERS` where spec owns vocabulary/schema, Go/backend owns normal orchestration, runtime-service/providers own hostile Strategy validation/build/execution, persistence owns canonical stored evidence, web owns rendering public projections, and scripts/monitors own static drift checks.
    - `assertCompetitionPolicyV136PublicLeakSafe(value = COMPETITION_POLICY_V1_36_PUBLIC_PAYLOAD): void`.

    Update `packages/spec/src/index.ts` with `export * from "./competition-policy-v1-36.js"`. Keep this phase contract/inventory/monitor only: no migrations, no entry gate behavior, no Season lifecycle behavior, no standings recompute, no governance workflow, no React rule logic, no Strategy execution, and no Node `vm`.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/spec/src/spec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'COMPETITION_POLICY_V1_36_ID = "competition-policy-v1\\.36"|publicLabel: "public beta trial competition"|resettable Season-scoped standings|no durable permanent rating promise' packages/spec/src/competition-policy-v1-36.ts` returns exact contract strings.
    - `rg -n 'competition-index|competition-detail|entry|season|standings|result|replay|player|strategy|governance|proof-artifacts|monitors' packages/spec/src/competition-policy-v1-36.ts` returns trust surface coverage.
    - `rg -n 'pending|counted|retrying|degraded_system_failure|non_counted|non_competitive|under_review|disputed|invalid|invalidated' packages/spec/src/competition-policy-v1-36.ts` returns counted-state public projection vocabulary.
    - `rg -n 'Phase 252 owns final persistence enum mappings|public projection vocabulary only' packages/spec/src/competition-policy-v1-36.ts` returns the D-11 guardrail.
    - `rg -n 'durable-rating|production-sandbox|package-ecosystem|tinygo-production|raw-diagnostic|private-runtime|mature-staffed-moderation|rating-refund|all-time-ranking' packages/spec/src/competition-policy-v1-36.ts` returns every forbidden claim category.
    - `rg -n 'Strategy source|StrategyMemory|SoldierMemory|objective payloads|raw diagnostics|artifact bytes|host paths|env values|package paths|tokens|DB details|private runtime internals|dispute internals|account-recovery payloads|operator-only governance' packages/spec/src/competition-policy-v1-36.ts` returns privacy exclusion labels.
    - `rg -n 'competition-policy-v1-36\\.js' packages/spec/src/index.ts` returns the barrel export.
    - `node -e "const s=require('fs').readFileSync('packages/spec/src/competition-policy-v1-36.ts','utf8'); if(/\\bfetch\\s*\\(/.test(s)||/Date\\.now\\s*\\(/.test(s)||/Math\\.random\\s*\\(/.test(s)||/from ['\\\"]node:(vm|fs|net|http|https|child_process)['\\\"]/.test(s)) process.exit(1)"` exits 0.
    - `pnpm exec vitest run packages/spec/src/spec.test.ts` exits 0.
  </acceptance_criteria>
  <done>`@cowards/spec` exports a tested v1.36 competition policy contract that downstream plans can import without adding behavior outside Phase 249 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec contract -> public UI/proof scripts | Public labels and privacy exclusions cross into user-facing copy and static monitors. |
| policy vocabulary -> downstream behavior phases | Later entry, Season, standings, governance, UX, and proof work consume this contract as authority. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-249-01 | HIGH | Spoofing/Repudiation | `competition-policy-v1.36` posture labels | mitigate | Tests require exact `public beta trial competition`, resettable Season-scoped standings, and no durable permanent rating promise strings. |
| T-249-02 | HIGH | Information Disclosure | Policy public payload | mitigate | `assertCompetitionPolicyV136PublicLeakSafe` uses `assertPublicOutputLeakSafe` and tests private marker rejection. |
| T-249-03 | HIGH | Elevation of Privilege | Policy vocabulary consumed by UI | mitigate | Contract states web renders public projections only; no game rules, entry enforcement, Strategy execution, or persistence mappings are added. |
| T-249-04 | MEDIUM | Tampering | Counted-state vocabulary | mitigate | Tests assert D-11 guardrail: projection vocabulary only, with Phase 252 owning final persistence mappings. |
</threat_model>

<verification>
Focused command: `pnpm exec vitest run packages/spec/src/spec.test.ts`

Wave gate after this plan: `pnpm exec vitest run packages/spec/src/spec.test.ts`
</verification>

<success_criteria>
- POST-01, POST-02, and POST-03 have a tested spec-owned contract.
- D-01 through D-04 and D-09 through D-12 are implemented in code/tests by exact strings and exported policy objects.
- No Phase 250-255 behavior is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/249-competition-surface-inventory-and-policy-lock/249-01-SUMMARY.md`
</output>
