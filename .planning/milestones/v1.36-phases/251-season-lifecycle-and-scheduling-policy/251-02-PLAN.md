---
phase: 251-season-lifecycle-and-scheduling-policy
plan: 02
type: execute
wave: 2
depends_on: [251-01]
autonomous: true
requirements: [SEAS-02, SEAS-04, SEAS-05]
files_modified:
  - packages/persistence/src/matchset-service.ts
  - packages/persistence/src/ladder.ts
  - packages/persistence/src/ladder.test.ts
must_haves:
  truths:
    - Scheduling freezes one Season and active entrant snapshots before creating any MatchSet.
    - The run is atomic, idempotent, and cannot mix or duplicate Season data.
    - Below-minimum Seasons complete with public insufficient-evidence output.
  artifacts:
    - path: packages/persistence/src/matchset-service.ts
      provides: Caller-owned transaction insertion path plus existing standalone transaction wrapper
    - path: packages/persistence/src/ladder.ts
      provides: Validated lifecycle mutation and one-transaction scheduling
    - path: packages/persistence/src/ladder.test.ts
      provides: Transition, freeze, rollback, idempotency, insufficient-evidence, and Season-isolation tests
  key_links:
    - from: packages/persistence/src/ladder.ts
      to: packages/spec/src/competition-season-policy.ts
      via: transition and lifecycle-window helpers
    - from: packages/persistence/src/ladder.ts
      to: packages/persistence/src/matchset-service.ts
      via: caller-owned scheduling transaction
    - from: packages/persistence/src/ladder.ts
      to: trial_ladder_schedule_runs
      via: one locked Season run and Season-scoped MatchSet foreign keys
---

# Plan 251-02: Atomic Season Scheduling

<objective>
Enforce monotonic lifecycle transitions and make each Season scheduling run atomic, idempotent, snapshot-locked, and Season-isolated.
</objective>

<tasks>

<task id="251-02-01" type="auto" tdd="true">
  <name>Enforce lifecycle transitions and entry freeze</name>
  <files>packages/persistence/src/ladder.ts, packages/persistence/src/ladder.test.ts</files>
  <action>Add RED tests for every legal/illegal transition and timestamp. Update `setTrialLadderSeasonStatus` to consume the spec transition contract, set `closed_at` on the first move from open into scheduling or a terminal state, and prevent backward/post-archive changes. Preserve audit events with public explanations only.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/ladder.test.ts -t "lifecycle|transition|closed"</automated></verify>
  <done>Illegal transitions fail before mutation and the entry window closes exactly once.</done>
</task>

<task id="251-02-02" type="auto" tdd="true">
  <name>Expose caller-owned MatchSet insertion transactions</name>
  <files>packages/persistence/src/matchset-service.ts, packages/persistence/src/ladder.test.ts</files>
  <action>Extract transaction-client insertion from `insertMatchSetWithMatrix`. Keep current `createMatchSetService(pool)` behavior transactional for existing callers, and add a narrowly named client-owned insertion path used only by scheduling. Validate matrices and lock revisions on the supplied client.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/ladder.test.ts packages/persistence/src/competition.test.ts</automated></verify>
  <done>Existing callers retain atomic creation and scheduling can compose MatchSet inserts into its outer transaction.</done>
</task>

<task id="251-02-03" type="auto" tdd="true">
  <name>Schedule one frozen Season atomically and idempotently</name>
  <files>packages/persistence/src/ladder.ts, packages/persistence/src/ladder.test.ts</files>
  <action>Run `scheduleTrialLadderSeason` inside one transaction. Lock the Season row `FOR UPDATE`, transition/freeze before reading active snapshots, detect an existing completed/no-op run, create all pods on the same client, persist one run, and move to active with `scheduled` outcome. If entries are below minimum, create a no-op run and complete with `insufficient_evidence`. Add rollback, repeat-run, entry-freeze-order, cross-Season, and deterministic pod tests.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/ladder.test.ts -t "scheduling|insufficient|idempotent|Season"</automated></verify>
  <done>No partial or duplicate run is observable; all created MatchSets and entrants belong to the locked Season.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cowards/persistence typecheck`
- `pnpm exec vitest run packages/persistence/src/ladder.test.ts packages/persistence/src/competition.test.ts`
</verification>

<boundaries>Scheduling consumes stored immutable snapshots only; result classification and governance remain downstream.</boundaries>
