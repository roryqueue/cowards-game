---
phase: 251-season-lifecycle-and-scheduling-policy
plan: 03
type: execute
wave: 3
depends_on: [251-02]
autonomous: true
requirements: [SEAS-01, SEAS-03, SEAS-04, SEAS-05]
files_modified:
  - packages/persistence/src/ladder.ts
  - packages/persistence/src/ladder.test.ts
  - apps/go-backend/live_backend.go
  - apps/go-backend/matchset_status_test.go
must_haves:
  truths:
    - TypeScript and Go public reads agree on lifecycle windows, timestamps, outcome, stable Season/standings links, and MatchSet result/replay links.
    - Completed and archived Seasons retain public evidence links and reset/no-durable posture.
    - Public reads stay Season-scoped and source/memory/objective/private-runtime safe.
  artifacts:
    - path: packages/persistence/src/ladder.ts
      provides: Canonical TypeScript public Season projection
    - path: packages/persistence/src/ladder.test.ts
      provides: Archived link, outcome, and Season-isolation projection tests
    - path: apps/go-backend/live_backend.go
      provides: Selected Go public ladder projection parity
    - path: apps/go-backend/matchset_status_test.go
      provides: Go DTO parity and stable evidence-link tests
  key_links:
    - from: packages/spec/src/competition.ts
      to: packages/persistence/src/ladder.ts
      via: PublicTrialLadderSeasonDto construction
    - from: packages/persistence/src/ladder.ts
      to: apps/go-backend/live_backend.go
      via: field-for-field public ladder parity tests
    - from: trial_ladder_seasons
      to: public Season links and outcome
      via: Season-scoped database reads only
---

# Plan 251-03: Public Season Read Parity

<objective>
Project the completed lifecycle policy through TypeScript and selected Go public reads with stable Season, standings, result, and replay links.
</objective>

<tasks>

<task id="251-03-01" type="auto" tdd="true">
  <name>Project lifecycle windows, outcome, and stable links in TypeScript</name>
  <files>packages/persistence/src/ladder.ts, packages/persistence/src/ladder.test.ts</files>
  <action>Read outcome columns and use spec helpers in `buildTrialLadderSeasonDto`. Populate `links.seasonHref`, `links.standingsHref`, MatchSet `resultHref`, and `replayHref` only when replay-backed evidence exists. Add completed/archived and insufficient-evidence tests plus cross-Season contamination assertions.</action>
  <verify><automated>pnpm exec vitest run packages/persistence/src/ladder.test.ts -t "public Season|archived|links|outcome"</automated></verify>
  <done>TypeScript DTOs expose honest windows/outcomes and stable evidence links after completion/archive.</done>
</task>

<task id="251-03-02" type="auto" tdd="true">
  <name>Align selected Go public ladder reads</name>
  <files>apps/go-backend/live_backend.go, apps/go-backend/matchset_status_test.go</files>
  <action>Extend `publicLadder` to read all lifecycle timestamps and outcome columns, derive the same windows, and emit the same stable links and reset/no-durable policy fields as TypeScript. Populate MatchSet replay links from public Chronicle-backed Match rows only. Add field-for-field fixture assertions for active, completed, archived, and insufficient-evidence Seasons.</action>
  <verify><automated>cd apps/go-backend && PATH=/usr/local/go/bin:$PATH go test ./... -run 'Test.*Ladder|Test.*Season' -count=1</automated></verify>
  <done>Go public ladder output validates against the shared DTO and matches TypeScript lifecycle/link semantics.</done>
</task>

<task id="251-03-03" type="auto">
  <name>Run privacy and boundary verification</name>
  <files>packages/persistence/src/ladder.test.ts, apps/go-backend/matchset_status_test.go</files>
  <action>Add serialized-output scans for source, memory, objective, raw diagnostic, path, environment, token, database, proof, and operator-only markers. Confirm no Strategy execution or scoring rule moved into web/API/Go.</action>
  <verify><automated>pnpm boundary:monitors</automated></verify>
  <done>Season public output and ownership monitors pass without suppressing a real public leak.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run packages/spec/src/competition-season-policy.test.ts packages/persistence/src/ladder.test.ts`
- `cd apps/go-backend && PATH=/usr/local/go/bin:$PATH go test ./... -run 'Test.*Ladder|Test.*Season' -count=1`
- `pnpm v1.36:competition-policy:check`
</verification>

<boundaries>No UI redesign, result classifier, governance workflow, game rule, or Strategy execution change.</boundaries>
