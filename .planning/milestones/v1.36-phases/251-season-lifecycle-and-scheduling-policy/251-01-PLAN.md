---
phase: 251-season-lifecycle-and-scheduling-policy
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SEAS-01, SEAS-03, SEAS-05]
files_modified:
  - packages/spec/src/competition-season-policy.ts
  - packages/spec/src/competition-season-policy.test.ts
  - packages/spec/src/competition.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/index.ts
  - packages/persistence/migrations/0009_competition_season_lifecycle.sql
must_haves:
  truths:
    - Season transitions, windows, reset policy, stable public links, and insufficient-evidence outcome are spec-owned and leak-safe.
    - Public Season DTOs remain explicitly resettable and make no durable rating promise.
  artifacts:
    - path: packages/spec/src/competition-season-policy.ts
      provides: Pure lifecycle transition, window, outcome, and public-copy contract
    - path: packages/spec/src/competition-season-policy.test.ts
      provides: Transition matrix, window, link, posture, and privacy tests
    - path: packages/spec/src/competition.ts
      provides: Season lifecycle/window/outcome/link DTO fields
    - path: packages/spec/src/schemas.ts
      provides: Runtime validation for the extended public Season DTO
    - path: packages/spec/src/index.ts
      provides: Public export of the Season policy contract
    - path: packages/persistence/migrations/0009_competition_season_lifecycle.sql
      provides: Additive constrained Season outcome storage
  key_links:
    - from: packages/spec/src/competition-season-policy.ts
      to: packages/spec/src/competition.ts
      via: shared lifecycle, outcome, and link types
    - from: packages/spec/src/competition.ts
      to: packages/spec/src/schemas.ts
      via: PublicTrialLadderSeasonDto schema parity
    - from: packages/persistence/migrations/0009_competition_season_lifecycle.sql
      to: packages/spec/src/competition-season-policy.ts
      via: constrained outcome category vocabulary
---

# Plan 251-01: Season Lifecycle Contract

<objective>
Create the canonical Season lifecycle, window, transition, outcome, stable-link, and storage contract.
</objective>

<tasks>

<task id="251-01-01" type="auto" tdd="true">
  <name>Define the pure Season lifecycle policy</name>
  <files>packages/spec/src/competition-season-policy.ts, packages/spec/src/competition-season-policy.test.ts, packages/spec/src/index.ts</files>
  <action>Add RED tests, then implement `TRIAL_SEASON_ALLOWED_TRANSITIONS`, `assertTrialSeasonTransition`, lifecycle-window projection, `scheduled` and `insufficient_evidence` outcomes, reset/no-durable public copy, and a leak-safety assertion. Export the module from the spec barrel.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/competition-season-policy.test.ts</automated></verify>
  <done>Every status pair is accepted or rejected deterministically; windows and outcomes use public-safe canonical copy.</done>
</task>

<task id="251-01-02" type="auto" tdd="true">
  <name>Extend the public Season DTO and schema</name>
  <files>packages/spec/src/competition.ts, packages/spec/src/schemas.ts, packages/spec/src/competition-season-policy.test.ts</files>
  <action>Extend `PublicTrialLadderSeasonDto` and `PublicTrialLadderSeasonDtoSchema` with `entryWindow`, `schedulingWindow`, `outcome`, and stable `links: { seasonHref, standingsHref }`. Keep MatchSet `resultHref` required and `replayHref` optional only when evidence exists. Add completed/archived DTO tests proving links and reset/no-durable posture remain present.</action>
  <verify><automated>pnpm exec vitest run packages/spec/src/competition-season-policy.test.ts packages/spec/src/spec.test.ts</automated></verify>
  <done>The TypeScript interface and Zod schema agree and archived Season fixtures retain stable links and honest posture.</done>
</task>

<task id="251-01-03" type="auto">
  <name>Add constrained Season outcome storage</name>
  <files>packages/persistence/migrations/0009_competition_season_lifecycle.sql</files>
  <action>Add nullable `outcome_status` and `public_outcome_explanation` columns to `trial_ladder_seasons`; constrain outcome status to `scheduled` or `insufficient_evidence`. Do not add private diagnostics or modify earlier migrations.</action>
  <verify><automated>rg -n "outcome_status|scheduled|insufficient_evidence|public_outcome_explanation" packages/persistence/migrations/0009_competition_season_lifecycle.sql</automated></verify>
  <done>The additive migration stores only the public outcome vocabulary required by the spec contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cowards/spec typecheck`
- `pnpm v1.36:competition-policy:check`
</verification>

<boundaries>No game rules, scoring, standings recompute, Strategy execution, package claims, or private outcome diagnostics.</boundaries>
