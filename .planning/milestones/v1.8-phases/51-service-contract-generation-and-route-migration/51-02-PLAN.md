---
phase: 51-service-contract-generation-and-route-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - packages/service/src/index.ts
  - packages/service/src/service.test.ts
  - apps/web/app/competitive/server.ts
  - apps/web/app/matches/server.ts
  - apps/web/app/matches/server.test.ts
  - apps/web/app/api/replays/[matchId]/metadata/route.ts
  - apps/web/app/api/matchsets/[matchSetId]/route.ts
  - apps/web/app/matchsets/[matchSetId]/page.tsx
  - apps/web/app/strategies/[strategyId]/page.tsx
autonomous: true
requirements:
  - GEN-04
  - GEN-05
  - GEN-06
must_haves:
  truths:
    - "Service health, public MatchSet summary, public replay metadata, and public Strategy page reads flow through `@cowards/service` without changing public behavior."
    - "Public Strategy page uses the Phase 51 selected low-risk read from D-06 and does not introduce analytics, auth, writes, orchestration, or runtime execution migration."
    - "Migrated public DTO outputs preserve compatibility/version fields, deterministic ordering, public error shapes, and privacy redaction."
  artifacts:
    - path: "packages/service/src/index.ts"
      provides: "typed service methods for public MatchSet summary, replay metadata, and public Strategy page"
      exports: ["createCowardsLocalService", "CowardsService"]
    - path: "apps/web/app/api/replays/[matchId]/metadata/route.ts"
      provides: "public replay metadata route backed by service boundary"
      exports: ["GET"]
    - path: "apps/web/app/competitive/server.ts"
      provides: "web facade calls service for public Strategy card/page and MatchSet summary"
    - path: "apps/web/app/matches/server.ts"
      provides: "replay metadata service-backed helper while full replay remains TypeScript-owned"
  key_links:
    - from: "apps/web/app/strategies/[strategyId]/page.tsx"
      to: "packages/service/src/index.ts"
      via: "competitiveServer.getPublicStrategyCard -> cowardsService.getPublicStrategyPage"
      pattern: "getPublicStrategyPage"
    - from: "apps/web/app/api/replays/[matchId]/metadata/route.ts"
      to: "packages/service/src/index.ts"
      via: "matches server helper returning service DTO"
      pattern: "getPublicReplayMetadata"
    - from: "packages/service/src/index.ts"
      to: "packages/spec/src/schemas.ts"
      via: "parse service DTOs with canonical schemas"
      pattern: "PublicStrategyPageServiceDtoSchema"
---

<objective>
Migrate the named Phase 51 public read slice behind `@cowards/service`.

Purpose: Satisfies D-04, D-05, and D-06 by keeping the slice tight: existing service health, public MatchSet summary, public replay metadata, and the selected public Strategy page read. No auth mutation, Strategy source retrieval, MatchSet creation, ladder mutation, Workshop execution, worker test-support, analytics write, export write, orchestration, or runtime execution migration is included.
Output: Service-backed public Strategy page and replay metadata route, with service tests proving DTO equivalence, public-safe outputs, and null/error behavior.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-CONTEXT.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-RESEARCH.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-PATTERNS.md
@.planning/research/PITFALLS.md
@AGENTS.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-01-SUMMARY.md

<interfaces>
Plan 51-01 creates these canonical schemas and types:
```typescript
export const PublicMatchSetSummaryServiceDtoSchema = z.object(...)
export const PublicReplayMetadataServiceDtoSchema = z.object(...)
export const PublicStrategyPageServiceDtoSchema = z.object(...)
export interface PublicStrategyPageServiceDto {
  apiVersion: typeof SERVICE_API_VERSION
  kind: "publicPage"
  page: "strategy"
  canonicalHref: string
  payload: PublicStrategyCardDto
}
```

Existing service method shape from `packages/service/src/index.ts`:
```typescript
export interface CowardsService {
  health(): ServiceHealthDto
  getPublicMatchSetSummary(matchSetId: MatchSetId): Promise<PublicMatchSetSummaryServiceDto | null>
  getPublicReplayMetadata(matchId: MatchId): Promise<PublicReplayMetadataServiceDto | null>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add service methods and DTO validation for public Strategy page and replay metadata</name>
  <files>packages/service/src/index.ts, packages/service/src/service.test.ts</files>
  <read_first>
    - packages/service/src/index.ts
    - packages/service/src/service.test.ts
    - packages/spec/src/service.ts
    - packages/spec/src/schemas.ts
    - packages/spec/src/competition.ts
    - packages/persistence/src/profiles.ts
    - .planning/phases/51-service-contract-generation-and-route-migration/51-PATTERNS.md
  </read_first>
  <behavior>
    - `CowardsService` exposes `getPublicStrategyPage(strategyId)` returning `PublicStrategyPageServiceDto | null`.
    - Existing `getPublicReplayMetadata(matchId)` parses the DTO through the canonical schema from Plan 51-01 before returning.
    - Service outputs run `assertPublicServiceDtoLeakSafe` before return.
    - Missing Strategy or missing Chronicle returns `null`, not an internal error.
  </behavior>
  <action>
    Update `packages/service/src/index.ts`. Import `buildPublicStrategyCardDto` from `@cowards/persistence/profiles`, and import `PublicStrategyPageServiceDto`, `PublicStrategyPageServiceDtoSchema`, and `PublicReplayMetadataServiceDtoSchema` from `@cowards/spec`. Add `getPublicStrategyPage(strategyId: StrategyId): Promise<PublicStrategyPageServiceDto | null>` to `CowardsService`. Add optional injection `buildPublicStrategyCard?: typeof buildPublicStrategyCardDto` to `CreateCowardsLocalServiceOptions`. Implement `getPublicStrategyPage` with the same `options.withPool` pattern as `getPublicMatchSetSummary`: call the injected/default persistence helper, return `null` if no card, build `{ apiVersion: SERVICE_API_VERSION, kind: "publicPage", page: "strategy", canonicalHref: \`/strategies/${encodeURIComponent(strategyId)}\`, payload: card }`, run `assertPublicServiceDtoLeakSafe(dto)`, and return `PublicStrategyPageServiceDtoSchema.parse(dto)`.

    Update `getPublicReplayMetadata` to parse `PublicReplayMetadataServiceDtoSchema.parse(dto)` after the leak check. Keep full replay Chronicle loading in `apps/web/app/matches/server.ts`; do not move full Chronicle/replay reconstruction into this service method.

    Expand `packages/service/src/service.test.ts` with tests for `getPublicStrategyPage`: success envelope, `null` when persistence returns `null`, and failure when the returned public card includes a forbidden private field. Add a replay metadata test using injected `createChronicleStore` that verifies version, event count, snapshot count, player ids, and null behavior. Use `SERVICE_API_VERSION` imported from `@cowards/spec` in expectations rather than hard-coding `"service-api-v1.7"`.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/service test</automated>
    <automated>pnpm --filter @cowards/service typecheck</automated>
    <expected>Service tests pass and typecheck proves the new method/schema imports are valid.</expected>
  </verify>
  <acceptance_criteria>
    - `rg -n 'getPublicStrategyPage' packages/service/src/index.ts packages/service/src/service.test.ts` returns matches in both files.
    - `rg -n 'PublicStrategyPageServiceDtoSchema\\.parse' packages/service/src/index.ts` returns one match.
    - `rg -n 'PublicReplayMetadataServiceDtoSchema\\.parse' packages/service/src/index.ts` returns one match.
    - `rg -n 'service-api-v1.7' packages/service/src/service.test.ts` returns no matches.
  </acceptance_criteria>
  <done>`@cowards/service` owns the new public Strategy page read and validates replay metadata DTOs against canonical spec schemas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire migrated web reads to the service boundary</name>
  <files>apps/web/app/competitive/server.ts, apps/web/app/matches/server.ts, apps/web/app/matches/server.test.ts, apps/web/app/api/replays/[matchId]/metadata/route.ts, apps/web/app/api/matchsets/[matchSetId]/route.ts, apps/web/app/matchsets/[matchSetId]/page.tsx, apps/web/app/strategies/[strategyId]/page.tsx</files>
  <read_first>
    - apps/web/app/competitive/server.ts
    - apps/web/app/matches/server.ts
    - apps/web/app/matches/server.test.ts
    - apps/web/app/api/service/health/route.ts
    - apps/web/app/api/matchsets/[matchSetId]/route.ts
    - apps/web/app/matchsets/[matchSetId]/page.tsx
    - apps/web/app/strategies/[strategyId]/page.tsx
    - apps/web/app/competitive/http.ts
    - .planning/phases/51-service-contract-generation-and-route-migration/51-CONTEXT.md
  </read_first>
  <behavior>
    - `competitiveServer.getPublicStrategyCard` delegates to `cowardsService.getPublicStrategyPage` and adapts `payload` back to the existing page shape.
    - A public replay metadata route returns service DTO JSON or `{ error: "Replay metadata not found." }` with 404.
    - Existing MatchSet route/page and Strategy page rendering behavior stays unchanged.
    - No task in this plan migrates auth mutation, Strategy source retrieval, MatchSet creation, ladder mutations, Workshop execution, worker test-support routes, analytics writes, exports, orchestration, or runtime execution.
  </behavior>
  <action>
    Update `apps/web/app/competitive/server.ts` so `getPublicStrategyCard(strategyId)` calls `cowardsService.getPublicStrategyPage(strategyId)` and returns `page ? (page.payload as StrategyCardDto) : null`. Remove `buildPublicStrategyCardDto` from the direct imports in this file only if it is no longer used; keep `buildPublicPlayerProfileDto` and other persistence imports for non-migrated legacy methods because broad app migration is not part of Phase 51 per D-05 and D-11.

    Update `apps/web/app/matches/server.ts` to import `createCowardsLocalService` from `@cowards/service` and add a method `getPublicReplayMetadata(matchId: MatchId): Promise<PublicReplayMetadataServiceDto | null>` on the object returned by `createMatchReplayServer`. The method must decode the match id with existing `decodeMatchId`, call a `cowardsService.getPublicReplayMetadata(resolvedMatchId)` instance built from the existing `withPool`, catch storage-table-not-installed errors with the same public unavailable behavior used by current replay code if applicable, and return only the service DTO. Do not expose full Chronicle events, owner debug, Awareness Grid, stderr, stack traces, Strategy source, StrategyMemory, SoldierMemory, objective payloads, sessions, tokens, host paths, or runtime internals.

    Create `apps/web/app/api/replays/[matchId]/metadata/route.ts`. Follow the App Router pattern from `apps/web/app/api/matchsets/[matchSetId]/route.ts`: decode params, call `matchReplayServer.getPublicReplayMetadata`, return `Response.json(dto)` on success, return `Response.json({ error: "Replay metadata not found." }, { status: 404 })` for null, and return `competitiveErrorResponse(error)` for caught service/storage errors.

    Keep `apps/web/app/api/matchsets/[matchSetId]/route.ts`, `apps/web/app/matchsets/[matchSetId]/page.tsx`, and `apps/web/app/strategies/[strategyId]/page.tsx` behavior-compatible. Edit them only if needed for import typing or to consume the new service-backed facade; do not alter visible text, sorting, compatibility fields, status labels, or public error shapes.

    Extend `apps/web/app/matches/server.test.ts` with tests for `getPublicReplayMetadata`: success DTO, decoded route param, missing Chronicle/null, and public privacy. Add or adjust web tests if existing assertions depend on hard-coded service-api-v1.7.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/web test</automated>
    <automated>pnpm --filter @cowards/web typecheck</automated>
    <automated>pnpm --filter @cowards/service test</automated>
    <expected>Web and service tests pass; route/page behavior remains compatible except service version literal changes to v1.8.</expected>
  </verify>
  <acceptance_criteria>
    - `test -f 'apps/web/app/api/replays/[matchId]/metadata/route.ts'` exits 0.
    - `rg -n 'getPublicStrategyPage' apps/web/app/competitive/server.ts` returns one match.
    - `rg -n 'buildPublicStrategyCardDto' apps/web/app/competitive/server.ts` returns no matches after the method is migrated.
    - `rg -n 'getPublicReplayMetadata' apps/web/app/matches/server.ts apps/web/app/matches/server.test.ts 'apps/web/app/api/replays/[matchId]/metadata/route.ts'` returns matches in all three files.
    - `rg -n '@cowards/persistence|@cowards/runtime-js|@cowards/worker|runWorkerOnce' 'apps/web/app/api/replays/[matchId]/metadata/route.ts' 'apps/web/app/api/matchsets/[matchSetId]/route.ts' 'apps/web/app/strategies/[strategyId]/page.tsx'` returns no matches.
  </acceptance_criteria>
  <done>The named public read slice routes through `@cowards/service` and preserves existing public DTO shapes, ordering, compatibility fields, errors, and privacy redaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web route/page -> service boundary | Migrated public reads leave Next route/page code and enter `@cowards/service`. |
| service boundary -> persistence | `@cowards/service` may import persistence implementation details; migrated route files must not. |
| public DTO -> browser/API caller | Public JSON/page data must stay redacted and behavior-compatible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02-01 | E | `apps/web/app/strategies/[strategyId]/page.tsx` path | mitigate | Page calls service-backed `competitiveServer.getPublicStrategyCard`; Plan 03 strict import guard blocks direct persistence/runtime imports. |
| T-51-02-02 | I | replay metadata route | mitigate | Route returns only `PublicReplayMetadataServiceDto`; no Chronicle events, owner debug, raw Awareness Grid, stderr, stacks, or Strategy data. |
| T-51-02-03 | T | DTO behavior during migration | mitigate | Service/web tests compare existing public shapes and use canonical schemas from Plan 51-01. |
| T-51-02-04 | D | missing storage during replay metadata reads | mitigate | Preserve existing public unavailable/error behavior; missing metadata returns stable 404 public error. |
</threat_model>

<verification>
Run after all tasks in this plan:

```bash
pnpm --filter @cowards/service test
pnpm --filter @cowards/service typecheck
pnpm --filter @cowards/web test
pnpm --filter @cowards/web typecheck
pnpm --filter @cowards/spec contract:check
```

Expected output: all commands exit 0; no public DTO tests expose forbidden private fields.
</verification>

<success_criteria>
- The Phase 51 migrated read slice is exactly service health, public MatchSet summary, public replay metadata, and public Strategy page.
- The public Strategy page read is the chosen extra public read per D-06.
- No auth/write/orchestration/runtime execution migration appears in this plan.
- Public DTOs and visible pages keep existing behavior while calling through `@cowards/service`.
</success_criteria>

<output>
After completion, create `.planning/phases/51-service-contract-generation-and-route-migration/51-02-SUMMARY.md`.
</output>
