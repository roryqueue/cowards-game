---
phase: 51-service-contract-generation-and-route-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - packages/spec/package.json
  - packages/spec/src/service.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/service-fixtures.ts
  - packages/spec/src/service-contract.test.ts
  - packages/spec/scripts/generate-service-openapi.ts
  - packages/spec/artifacts/service-api-v1.8.openapi.json
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
must_haves:
  truths:
    - "Developer can regenerate packages/spec/artifacts/service-api-v1.8.openapi.json from @cowards/spec without hand-editing generated output."
    - "Generated service contract metadata includes stable route ids, operation ids, methods, paths, auth scopes, privacy classes, schemas, shared errors, examples, and fixture references."
    - "Public contract examples and schemas reject Strategy source, StrategyMemory, SoldierMemory, objective payloads, owner debug, raw Awareness Grid, stack traces, stderr, sessions, tokens, host paths, and private runtime internals by default."
  artifacts:
    - path: "packages/spec/src/service.ts"
      provides: "service-api-v1.8 route metadata source of truth"
      contains: "SERVICE_API_VERSION = \"service-api-v1.8\""
    - path: "packages/spec/src/schemas.ts"
      provides: "Zod 4 request/response DTO schemas for service contract generation"
      exports: ["PublicMatchSetSummaryServiceDtoSchema", "PublicReplayMetadataServiceDtoSchema", "PublicStrategyPageServiceDtoSchema"]
    - path: "packages/spec/scripts/generate-service-openapi.ts"
      provides: "deterministic OpenAPI 3.1 generator and --check stale-output mode"
    - path: "packages/spec/artifacts/service-api-v1.8.openapi.json"
      provides: "committed generated v1.8 service artifact for review and Phase 52 Go parity"
  key_links:
    - from: "packages/spec/src/service.ts"
      to: "packages/spec/scripts/generate-service-openapi.ts"
      via: "SERVICE_API_ROUTES metadata consumed by generator"
      pattern: "SERVICE_API_ROUTES"
    - from: "packages/spec/src/schemas.ts"
      to: "packages/spec/artifacts/service-api-v1.8.openapi.json"
      via: "z.toJSONSchema in generator"
      pattern: "toJSONSchema"
    - from: "packages/spec/src/service-fixtures.ts"
      to: "packages/spec/src/service-contract.test.ts"
      via: "public examples checked by assertPublicServiceDtoLeakSafe"
      pattern: "assertPublicServiceDtoLeakSafe"
---

<objective>
Create the authoritative v1.8 service contract source and committed generated artifact.

Purpose: Satisfies D-01, D-02, D-03, D-07, D-08, and D-09 by keeping `@cowards/spec` authoritative, generating deterministic OpenAPI 3.1 output from Zod 4 schemas, and making stale generated output fail locally.
Output: Enriched route metadata, generation-ready DTO schemas, public-safe examples, generator/check scripts, Redocly lint wiring, and `packages/spec/artifacts/service-api-v1.8.openapi.json`.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-CONTEXT.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-RESEARCH.md
@.planning/phases/51-service-contract-generation-and-route-migration/51-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@AGENTS.md

<interfaces>
From `packages/spec/src/service.ts` before this plan:
```typescript
export const SERVICE_API_VERSION = "service-api-v1.7"
export const SERVICE_API_ROUTES = {
  health: "GET /health",
  authSession: "GET /auth/session",
  createSession: "POST /auth/session",
  revokeSession: "DELETE /auth/session",
  listStrategyRevisions: "GET /account/strategy-revisions",
  createStrategyRevision: "POST /account/strategy-revisions",
  getStrategyRevisionSource: "GET /account/strategy-revisions/{strategyRevisionId}/source",
  createMatchSet: "POST /matchsets",
  getPublicMatchSetSummary: "GET /public/matchsets/{matchSetId}/summary",
  getPublicReplayMetadata: "GET /public/replays/{matchId}/metadata",
  listAnalyticsProfiles: "GET /analytics/profiles",
  createAnalyticsRun: "POST /analytics/profiles/{profileId}/runs",
  exportAnalyticsRun: "GET /analytics/runs/{runId}/export",
  listLadderSeasons: "GET /ladders/seasons",
  enterLadderSeason: "POST /ladders/seasons/{seasonId}/entries",
  getPublicPlayerPage: "GET /public/players/{handle}",
  getPublicStrategyPage: "GET /public/strategies/{strategyId}",
} as const
export type ServiceApiRouteId = keyof typeof SERVICE_API_ROUTES
```

From `packages/spec/src/schemas.ts` before this plan:
```typescript
export const ServiceErrorDtoSchema = z.object({ ... })
export const ServiceHealthDtoSchema = z.object({
  ok: z.literal(true),
  service: z.literal("cowards-service"),
  version: z.literal(SERVICE_API_VERSION),
})
export const ServiceApiRouteIdSchema = z.enum(Object.keys(SERVICE_API_ROUTES) as [...])
export const AuthSessionServiceDtoSchema = z.object({ ... })
export const StrategyRevisionSummaryServiceDtoSchema = z.object({ ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich canonical service route metadata and DTO schemas</name>
  <files>packages/spec/src/service.ts, packages/spec/src/schemas.ts</files>
  <read_first>
    - packages/spec/src/service.ts
    - packages/spec/src/schemas.ts
    - packages/spec/src/competition.ts
    - packages/spec/src/runtime.ts
    - .planning/phases/51-service-contract-generation-and-route-migration/51-CONTEXT.md
    - .planning/phases/51-service-contract-generation-and-route-migration/51-RESEARCH.md
  </read_first>
  <behavior>
    - `SERVICE_API_VERSION` is exactly `"service-api-v1.8"`.
    - `SERVICE_API_ROUTES` remains the single exported route registry and is enriched in place; no new parallel route registry is introduced.
    - Each route metadata object includes `id`, `operationId`, `method`, `path`, `authScope`, `privacyClass`, `request`, `response`, `error`, `examples`, and `fixtureRefs`.
    - Public route schemas/examples reject the forbidden private fields listed in AGENTS.md and Phase constraints.
  </behavior>
  <action>
    Update `packages/spec/src/service.ts` per D-03, D-07, D-08, and D-09. Replace string route values with metadata objects while preserving `export type ServiceApiRouteId = keyof typeof SERVICE_API_ROUTES`. Add exact literal unions/types for `ServiceRouteMethod`, `ServiceRouteAuthScope`, `ServiceRoutePrivacyClass`, and `ServiceRouteContract` if needed by TypeScript. Keep these route ids exactly: `health`, `authSession`, `createSession`, `revokeSession`, `listStrategyRevisions`, `createStrategyRevision`, `getStrategyRevisionSource`, `createMatchSet`, `getPublicMatchSetSummary`, `getPublicReplayMetadata`, `listAnalyticsProfiles`, `createAnalyticsRun`, `exportAnalyticsRun`, `listLadderSeasons`, `enterLadderSeason`, `getPublicPlayerPage`, `getPublicStrategyPage`. Mark the Phase 51 migrated public reads with `privacyClass: "public"` and `authScope: "public"`: `health`, `getPublicMatchSetSummary`, `getPublicReplayMetadata`, and `getPublicStrategyPage` per D-04 and D-06. Preserve auth/write route metadata but do not migrate auth mutation, Strategy source retrieval, MatchSet creation, ladder mutations, Workshop execution, worker test-support routes, analytics writes, or export writes in this phase per D-05.

    Update `packages/spec/src/schemas.ts` with Zod 4 schemas needed by the metadata: empty params/query/body schemas, `{ matchSetId }`, `{ matchId }`, `{ strategyId }`, `{ strategyRevisionId }`, `{ profileId }`, `{ runId }`, `{ seasonId }`, and `{ handle }` params schemas; `PublicMatchSetSummaryServiceDtoSchema`; `PublicReplayMetadataServiceDtoSchema`; `PublicStrategyCardDtoSchema`; `PublicStrategyPageServiceDtoSchema`; and keep existing `ServiceErrorDtoSchema`, `ServiceHealthDtoSchema`, `AuthSessionServiceDtoSchema`, and `StrategyRevisionSummaryServiceDtoSchema` exported. Extend `assertPublicServiceDtoLeakSafe` forbidden keys in `service.ts` to include plural/session/path variants exactly: `sessions`, `tokens`, `hostPath`, `hostPaths`, `runtimeInternal`, `runtimeInternals`, `privateRuntimeInternal`, and `privateRuntimeInternals`.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/spec typecheck</automated>
    <expected>Typecheck reports no missing exports or broken route id schema after the canonical metadata and schemas are added.</expected>
  </verify>
  <acceptance_criteria>
    - `rg -n 'SERVICE_API_VERSION = "service-api-v1.8"' packages/spec/src/service.ts` returns one match.
    - `rg -n 'getPublicStrategyPage: \\{' packages/spec/src/service.ts` returns one match and `rg -n 'GET /public/strategies/\\{strategyId\\}' packages/spec/src/service.ts` returns one match.
    - `rg -n 'export const PublicStrategyPageServiceDtoSchema|export const PublicReplayMetadataServiceDtoSchema|export const PublicMatchSetSummaryServiceDtoSchema' packages/spec/src/schemas.ts` returns all three exports.
    - `rg -n 'export const SERVICE_API_ROUTES = \\{' packages/spec/src/service.ts` returns one match and `rg -n 'SERVICE_API_ROUTES_2|OPENAPI_ROUTES|CONTRACT_ROUTES' packages/spec/src packages/spec/scripts` returns no matches.
  </acceptance_criteria>
  <done>Canonical v1.8 route metadata and Zod DTO schemas exist in `@cowards/spec`; route metadata covers D-08 fields; public schemas/examples have no private field names by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public-safe service fixtures and contract metadata tests</name>
  <files>packages/spec/src/service-fixtures.ts, packages/spec/src/service-contract.test.ts, packages/spec/package.json</files>
  <read_first>
    - packages/spec/src/spec.test.ts
    - packages/spec/src/service.ts
    - packages/spec/src/schemas.ts
    - packages/spec/src/competition.ts
    - packages/spec/package.json
    - .planning/phases/51-service-contract-generation-and-route-migration/51-PATTERNS.md
  </read_first>
  <behavior>
    - Every `SERVICE_API_ROUTES` entry has all D-08 metadata fields.
    - Every retained `SERVICE_API_ROUTES` route has at least one non-empty `examples` entry and at least one non-empty `fixtureRefs` entry.
    - Every `fixtureRefs` entry resolves to an exported fixture object from `packages/spec/src/service-fixtures.ts`.
    - Public examples pass `assertPublicServiceDtoLeakSafe`.
    - Contract metadata tests fail if public examples or public schema property names include private Strategy/runtime fields.
  </behavior>
  <action>
    Create `packages/spec/src/service-fixtures.ts` exporting typed route-specific example objects and fixture refs for every retained `SERVICE_API_ROUTES` route, not just the Phase 51 migrated routes. Include these exported fixture constants exactly: `serviceHealthExample`, `authSessionExample`, `createSessionExample`, `revokeSessionExample`, `listStrategyRevisionsExample`, `createStrategyRevisionExample`, `getStrategyRevisionSourceExample`, `createMatchSetExample`, `publicMatchSetSummaryExample`, `publicReplayMetadataExample`, `listAnalyticsProfilesExample`, `createAnalyticsRunExample`, `exportAnalyticsRunExample`, `listLadderSeasonsExample`, `enterLadderSeasonExample`, `publicPlayerPageExample`, and `publicStrategyPageExample`. Each fixture must be backed by a concrete response schema or DTO type exported from `packages/spec/src/schemas.ts` or `packages/spec/src/service.ts`; do not use generic placeholder DTO shapes to satisfy non-empty checks. Keep examples public-safe: no Strategy source, StrategyMemory, SoldierMemory, objective payloads, owner debug, raw Awareness Grid, stack traces, stderr, sessions, tokens, host paths, or private runtime internals.

    Create `packages/spec/src/service-contract.test.ts`. Test all route metadata fields from D-08 for every route id. Test `Object.entries(SERVICE_API_ROUTES)` has `route.id === routeId`, non-empty `operationId`, `method`, `path`, `authScope`, `privacyClass`, `request`, `response`, `error`, `examples`, and `fixtureRefs`. Add assertions that `route.examples.length > 0`, `route.fixtureRefs.length > 0`, and every fixture ref resolves to an exported key in `SERVICE_API_FIXTURES` from `packages/spec/src/service-fixtures.ts`. Add assertions that every `route.examples` entry parses with `route.response` and every request example parses with `route.request` where request examples exist; this must be a schema parse, not just a presence check. Test every exported public route example runs through `assertPublicServiceDtoLeakSafe`; for non-public routes, test generated examples are not included in public artifact examples unless the route metadata `privacyClass` is `"public"`. Test public schema JSON generated through `z.toJSONSchema()` does not contain forbidden public property names. Update `packages/spec/package.json` test script from `vitest run spec.test.ts` to `vitest run spec.test.ts service-contract.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/spec test</automated>
    <expected>Both `spec.test.ts` and `service-contract.test.ts` pass.</expected>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spec/src/service-fixtures.ts` exits 0.
    - `test -f packages/spec/src/service-contract.test.ts` exits 0.
    - `rg -n 'assertPublicServiceDtoLeakSafe' packages/spec/src/service-contract.test.ts` returns at least one match.
    - `rg -n 'examples\\.length > 0|fixtureRefs\\.length > 0|SERVICE_API_FIXTURES' packages/spec/src/service-contract.test.ts` returns matches for non-empty examples, non-empty fixture refs, and fixture-ref resolution.
    - `rg -n 'authSessionExample|createSessionExample|revokeSessionExample|publicStrategyPageExample' packages/spec/src/service-fixtures.ts` returns all four fixture exports.
    - `rg -n 'service-contract.test.ts' packages/spec/package.json` returns one match.
  </acceptance_criteria>
  <done>Public-safe examples and route metadata tests prove GEN-02 and GEN-04 without adding a second contract source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate, lint, and check the committed OpenAPI artifact</name>
  <files>package.json, pnpm-lock.yaml, packages/spec/package.json, packages/spec/scripts/generate-service-openapi.ts, packages/spec/artifacts/service-api-v1.8.openapi.json</files>
  <read_first>
    - package.json
    - packages/spec/package.json
    - packages/spec/src/service.ts
    - packages/spec/src/schemas.ts
    - packages/spec/src/service-fixtures.ts
    - scripts/preflight.ts
    - .planning/research/STACK.md
    - .planning/phases/51-service-contract-generation-and-route-migration/51-RESEARCH.md
  </read_first>
  <behavior>
    - `pnpm --filter @cowards/spec contract:generate` writes a deterministic OpenAPI 3.1 JSON artifact.
    - `pnpm --filter @cowards/spec contract:check` exits non-zero if the committed artifact is stale.
    - `pnpm contract:lint` runs Redocly lint against `packages/spec/artifacts/service-api-v1.8.openapi.json`.
  </behavior>
  <action>
    Install `@redocly/cli@2.31.4` as a root dev dependency with `pnpm add -D -w @redocly/cli@2.31.4` per research. Create `packages/spec/scripts/generate-service-openapi.ts` with a `--check` mode. The script must import `SERVICE_API_VERSION`, `SERVICE_API_ROUTES`, and service schemas from `@cowards/spec` source files, convert Zod schemas through `z.toJSONSchema()`, compose OpenAPI `openapi: "3.1.0"`, `info.title: "Coward's Game Service API"`, `info.version: SERVICE_API_VERSION`, `paths`, and `components.schemas`, sort object keys deterministically, and write `packages/spec/artifacts/service-api-v1.8.openapi.json` with one trailing newline. In `--check`, compare the generated string to the committed file and throw the exact stale message `service-api-v1.8.openapi.json is stale; run pnpm --filter @cowards/spec contract:generate`.

    Add `contract:generate` and `contract:check` to `packages/spec/package.json`: `pnpm exec tsx scripts/generate-service-openapi.ts` and `pnpm exec tsx scripts/generate-service-openapi.ts --check`. Add root scripts in `package.json`: `contract:check` as `pnpm --filter @cowards/spec contract:check` and `contract:lint` as `redocly lint --extends recommended packages/spec/artifacts/service-api-v1.8.openapi.json`. Generate and commit `packages/spec/artifacts/service-api-v1.8.openapi.json` per D-01 and D-02.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/spec contract:generate</automated>
    <automated>pnpm --filter @cowards/spec contract:check</automated>
    <automated>pnpm contract:lint</automated>
    <automated>pnpm --filter @cowards/spec test</automated>
    <expected>Generator writes the artifact once, check mode reports no drift, Redocly lint exits 0, and spec tests pass.</expected>
  </verify>
  <acceptance_criteria>
    - `test -f packages/spec/artifacts/service-api-v1.8.openapi.json` exits 0.
    - `node -e 'const a=require("./packages/spec/artifacts/service-api-v1.8.openapi.json"); if(a.openapi!=="3.1.0"||a.info.version!=="service-api-v1.8") process.exit(1)'` exits 0.
    - `rg -n '"@redocly/cli"' package.json pnpm-lock.yaml` returns matches in both files.
    - `rg -n '"contract:generate"|"contract:check"' packages/spec/package.json` returns both scripts.
  </acceptance_criteria>
  <done>GEN-01 and GEN-03 are satisfied: the artifact is committed, reproducible, linted, and stale-output checked from canonical `@cowards/spec` source.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 51 | Regenerate stable service artifacts and route selected low-risk reads through typed service boundary | 51-01, 51-02, 51-03 | COVERED | Contract generation, migration, and guards are split by dependency.
REQ | GEN-01 | Deterministic service contract artifact from canonical `@cowards/spec` schemas | 51-01 | COVERED | Tasks 1 and 3.
REQ | GEN-02 | Route ids, operation ids, methods, paths, auth scopes, privacy classes, schemas, errors, examples, fixture refs | 51-01 | COVERED | Tasks 1 and 2.
REQ | GEN-03 | Linting and stale-output checks fail on drift | 51-01, 51-03 | COVERED | Redocly and generator `--check`.
REQ | GEN-04 | Public/owner/internal DTO separation and public privacy exclusion | 51-01, 51-02 | COVERED | Schema/example leak checks and service DTO validation.
REQ | GEN-05 | Named low-risk reads move to `@cowards/service` | 51-02 | COVERED | Health, MatchSet summary, replay metadata, public Strategy page.
REQ | GEN-06 | Migrated reads preserve DTO behavior, ordering, compatibility fields, errors, privacy | 51-02 | COVERED | Service/web equivalence tests.
REQ | GEN-07 | Import boundary checks fail for migrated direct forbidden imports | 51-03 | COVERED | Strict named-slice guard.
RESEARCH | R-01 | Use Zod 4 JSON Schema and deterministic OpenAPI 3.1 composer | 51-01 | COVERED | Task 3.
RESEARCH | R-02 | Add `@redocly/cli@2.31.4` lint/check | 51-01 | COVERED | Task 3.
RESEARCH | R-03 | Choose public Strategy page as fourth public read | 51-02 | COVERED | Implements D-06.
RESEARCH | R-04 | Strict direct named-slice guard and broad report-only app scan | 51-03 | COVERED | Tasks 1 and 2 in Plan 03.
CONTEXT | D-01 | Commit generated v1.8 service artifact | 51-01 | COVERED | Task 3.
CONTEXT | D-02 | Deterministic generation plus lint/stale checks | 51-01 | COVERED | Task 3.
CONTEXT | D-03 | Generate from canonical Zod schemas in `@cowards/spec` | 51-01 | COVERED | Tasks 1 and 3.
CONTEXT | D-04 | Tight migration slice: health, MatchSet summary, replay metadata, one extra public read | 51-02 | COVERED | Public Strategy page selected.
CONTEXT | D-05 | No auth/write/orchestration/runtime execution migration | 51-01, 51-02, 51-03 | COVERED | Explicit exclusions in tasks and guards.
CONTEXT | D-06 | Extra public read low-risk and high boundary value | 51-02 | COVERED | Public Strategy page.
CONTEXT | D-07 | Enrich `SERVICE_API_ROUTES` in place | 51-01 | COVERED | Task 1.
CONTEXT | D-08 | Route metadata fields | 51-01 | COVERED | Tasks 1 and 2.
CONTEXT | D-09 | `@cowards/spec` authoritative | 51-01, 51-03 | COVERED | Generator and guards consume spec.
CONTEXT | D-10 | Strict guard named migrated slice | 51-03 | COVERED | Plan 03.
CONTEXT | D-11 | Broader report-only scan over `apps/web/app` | 51-03 | COVERED | Plan 03.
CONTEXT | D-12 | Strict guard forbidden direct imports | 51-03 | COVERED | Plan 03.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical spec -> generated artifact | Generated OpenAPI is consumed by humans and Phase 52 tooling but must never become the hand-authored source. |
| public examples/schemas -> downstream clients | Public contract output is externally visible and must omit private Strategy/runtime data. |
| package scripts -> CI/local gates | Stale checks must fail when artifact output drifts from canonical schemas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01-01 | T | `packages/spec/artifacts/service-api-v1.8.openapi.json` | mitigate | `contract:check` compares deterministic generation output to committed artifact and fails on drift. |
| T-51-01-02 | I | public route examples and schemas | mitigate | `service-contract.test.ts` runs `assertPublicServiceDtoLeakSafe` and forbidden property-name scans over public routes. |
| T-51-01-03 | E | route metadata auth/privacy classes | mitigate | Route metadata requires explicit `authScope` and `privacyClass` for every route id and tests fail if missing. |
| T-51-01-04 | R | generated artifact provenance | mitigate | Artifact info.version is `service-api-v1.8`; scripts document stale remediation command and generated file path. |
</threat_model>

<verification>
Run after all tasks in this plan:

```bash
pnpm --filter @cowards/spec test
pnpm --filter @cowards/spec typecheck
pnpm --filter @cowards/spec contract:check
pnpm contract:lint
```

Expected output: all commands exit 0; `contract:check` reports no stale generated output.
</verification>

<success_criteria>
- `packages/spec/artifacts/service-api-v1.8.openapi.json` is committed and regenerates byte-for-byte from `@cowards/spec`.
- `SERVICE_API_ROUTES` is metadata-rich in place and remains the only route registry.
- Public service contract output is tested against the required private-field denylist.
- Redocly lint and stale-output checks are runnable through pnpm scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/51-service-contract-generation-and-route-migration/51-01-SUMMARY.md`.
</output>
