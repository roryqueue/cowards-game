---
phase: 52-go-read-only-backend-parity-against-real-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - GO-01
  - GO-02
  - GO-03
  - GO-04
  - GO-05
  - GO-06
---

<objective>
Promote the Go backend spike to v1.8 read-only parity against generated real fixtures while keeping backend ownership TypeScript-owned.
</objective>

<tasks>

<task>
  <name>Task 1: Add canonical parity fixture generation</name>
  <files>scripts/generate-go-parity-fixtures.ts, package.json, apps/go-backend/testdata/service-fixtures/*.json</files>
  <behavior>Generate deterministic fixture JSON by invoking the TypeScript service boundary over canonical golden/demo data; fail stale output.</behavior>
  <verify>pnpm go:parity</verify>
</task>

<task>
  <name>Task 2: Add TypeScript analytics summary service DTO</name>
  <files>packages/spec/src/schemas.ts, packages/spec/src/service.ts, packages/spec/src/service-fixtures.ts, packages/service/src/index.ts, packages/service/src/service.test.ts</files>
  <behavior>Add a read-only analytics run summary envelope backed by `AnalyticsGauntletRunSummarySchema`; parse and leak-check service output.</behavior>
  <verify>pnpm --filter @cowards/service test && pnpm --filter @cowards/spec test</verify>
</task>

<task>
  <name>Task 3: Update Go backend to load fixtures and enforce read-only route inventory</name>
  <files>apps/go-backend/main.go, apps/go-backend/main_test.go, apps/go-backend/README.md</files>
  <behavior>Serve v1.8 fixture-backed health, public MatchSet summary, replay metadata, and owner-gated analytics summary; reject mutation verbs and unsupported routes; validate fixtures on startup; document TypeScript ownership.</behavior>
  <verify>(cd apps/go-backend && go test ./...)</verify>
</task>

</tasks>

<source_audit>
GO-01 through GO-06 are covered by Tasks 1 through 3.
</source_audit>
