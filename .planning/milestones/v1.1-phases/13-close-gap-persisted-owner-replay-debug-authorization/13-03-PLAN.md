---
phase: 13-close-gap-persisted-owner-replay-debug-authorization
plan: 03
type: execute
wave: 2
status: ready
depends_on: [13-01, 13-02]
files_modified:
  - apps/web/e2e/workshop-to-replay.spec.ts
autonomous: true
requirements: [DEBUG-04, DEBUG-05, REL-04]
---

# Plan 13-03: Service-Backed Failure Replay Proof

## Objective

Prove the end-to-end path: failure-mode Strategy sample -> submit/run through worker -> persisted Chronicle -> owner replay link -> Soldier inactivity explanation, with public privacy asserted for the same Match.

## Read First

- `.planning/phases/13-close-gap-persisted-owner-replay-debug-authorization/13-RESEARCH.md`
- `apps/web/e2e/workshop-to-replay.spec.ts`
- `packages/persistence/src/workshop.ts`
- `apps/web/app/api/test-support/run-worker-once/route.ts`

## Tasks

1. Add a service-backed Playwright case using a fast failure-mode sample such as thrown exception or invalid output.
2. Submit the sample, launch a Workshop test, run `/api/test-support/run-worker-once`, and wait for persisted replay availability.
3. Open the owner-debug replay link from the Workshop Match row.
4. Assert the persisted replay status chip is `Owner debug`, enable owner debug, and verify the Soldier inactivity explanation reflects a runtime violation or invalid action path from DTO data.
5. Open the public replay URL for the same Match and assert owner debug/private markers are absent.

## Threat Model

- Strategy code must execute through the worker/runtime boundary only.
- Public replay privacy must be proved with the same persisted Match used for owner-debug success.

## Verification

- `pnpm e2e:service`

## Done

The service-backed E2E demonstrates a real failing Strategy Chronicle can be debugged by its owner while the public replay stays private-data-free.
