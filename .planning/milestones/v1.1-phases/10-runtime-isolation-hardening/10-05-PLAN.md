---
phase: 10-runtime-isolation-hardening
plan: 05
type: execute
wave: 5
status: complete
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - packages/runtime-js/src/isolation-boundary.test.ts
  - .planning/phases/10-runtime-isolation-hardening/10-VALIDATION.md
  - .planning/phases/10-runtime-isolation-hardening/10-05-SUMMARY.md
  - .planning/phases/10-runtime-isolation-hardening/10-05-PLAN.md
autonomous: true
requirements: [ISO-01, ISO-02, ISO-03, ISO-04, ISO-05, ISO-06, ISO-07]
must_haves:
  truths:
    - "Automated tests prove executable runtime entrypoints are not imported by web/API code."
    - "Phase validation evidence maps every ISO requirement to commands and passing results."
    - "Docker/container direction is documented as later work and not required for Phase 10 completion."
  artifacts:
    - path: "packages/runtime-js/src/isolation-boundary.test.ts"
      provides: "Import and forbidden-boundary audit tests"
    - path: ".planning/phases/10-runtime-isolation-hardening/10-VALIDATION.md"
      provides: "ISO requirement validation evidence"
  key_links:
    - from: "packages/runtime-js/src/isolation-boundary.test.ts"
      to: "apps/web"
      via: "source import scan"
      pattern: "@cowards/runtime-js/worker"
    - from: ".planning/phases/10-runtime-isolation-hardening/10-VALIDATION.md"
      to: "Phase 10 summaries"
      via: "verification result table"
      pattern: "ISO-0[1-7]"
---

<objective>
Add final boundary audit tests and fill the Phase 10 validation evidence scaffold.

Purpose: ISO-01 through ISO-07 need explicit validation commands and acceptance evidence. This plan prevents source execution from creeping into web/API and records the completed proof for the phase.
Output: Boundary scan test and updated `10-VALIDATION.md`.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-runtime-isolation-hardening/10-CONTEXT.md
@.planning/phases/10-runtime-isolation-hardening/10-RESEARCH.md
@.planning/phases/10-runtime-isolation-hardening/10-MANIFEST.md
@.planning/phases/10-runtime-isolation-hardening/10-VALIDATION.md
@packages/runtime-js/package.json
@packages/runtime-js/src/worker.ts
@apps/worker/src/runner.ts
@apps/web/app/workshop/server.ts

<interfaces>
Package boundary rule:
```json
"exports": {
  ".": "./src/index.ts",
  "./worker": "./src/worker.ts"
}
```

Safe pattern: web/API may import `@cowards/runtime-js` for validation/build helpers, but must not import `@cowards/runtime-js/worker` or call `createRuntimeFromRevision`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable runtime import boundary test</name>
  <files>packages/runtime-js/src/isolation-boundary.test.ts</files>
  <behavior>
    - Test 1: `apps/web` and API route files do not import `@cowards/runtime-js/worker`.
    - Test 2: `apps/web` and API route files do not call `createRuntimeFromRevision`.
    - Test 3: runtime harness files do not import `node:vm` or use `vm.` as a security boundary.
    - Test 4: only `apps/worker` imports executable runtime subpath.
  </behavior>
  <action>Create a Vitest source-scan test using `node:fs` and `node:path`. Exclude `node_modules`, `.next`, `dist`, generated snapshots, and `.planning`. Keep grep-like assertions filtered to source files so comments in this plan cannot self-trigger. The test should fail with a message naming the offending path and pattern.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- isolation-boundary.test.ts</automated>
  </verify>
  <done>ISO-02 has automated source-boundary coverage and the no-Node-vm rule covers new harness files.</done>
</task>

<task type="auto">
  <name>Task 2: Run complete Phase 10 validation command set</name>
  <files>.planning/phases/10-runtime-isolation-hardening/10-VALIDATION.md</files>
  <action>Run the commands listed in `10-VALIDATION.md`: runtime-js test/typecheck, worker test/typecheck, spec test/typecheck, engine test/typecheck, and Prettier check for changed runtime/worker/planning files. Update result cells with pass/fail and short notes. If a command fails due to unrelated pre-existing formatting or service setup, record the exact failing file/layer and do not mark the relevant ISO row green until the blocking Phase 10 evidence has a passing targeted command.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test && pnpm --filter @cowards/runtime-js typecheck && pnpm --filter @cowards/worker test && pnpm --filter @cowards/worker typecheck && pnpm --filter @cowards/spec test && pnpm --filter @cowards/spec typecheck && pnpm --filter @cowards/engine test && pnpm --filter @cowards/engine typecheck</automated>
  </verify>
  <done>`10-VALIDATION.md` contains concrete command results and every ISO-01 through ISO-07 evidence row is pass or explicitly blocked with reason.</done>
</task>

<task type="auto">
  <name>Task 3: Final acceptance grep gates</name>
  <files>.planning/phases/10-runtime-isolation-hardening/10-VALIDATION.md</files>
  <action>Record final grep gates in `10-VALIDATION.md`: scan non-planning source files for forbidden executable web imports, `node:vm`, `vm.`, and accidental `process.env` passing into subprocess adapter. Use filtered commands that exclude comments/planning/docs where appropriate and list exact commands/results. Include a Docker note that Docker/container end-to-end validation is Phase 12 unless this phase intentionally adds a non-required local diagnostic.</action>
  <verify>
    <automated>rg -n '@cowards/runtime-js/worker|createRuntimeFromRevision' apps/web packages --glob '*.{ts,tsx}' --glob '!**/*.test.ts' --glob '!packages/runtime-js/**' --glob '!apps/worker/**' | wc -l | tr -d ' ' | grep '^0$'</automated>
    <automated>rg -n 'node:vm|[^A-Za-z0-9_]vm\\.' packages/runtime-js apps/worker --glob '*.{ts,tsx}' | wc -l | tr -d ' ' | grep '^0$'</automated>
  </verify>
  <done>Final acceptance gates are recorded and pass without relying on manual inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| source imports -> executable runtime | Build-time import boundary prevents web/API from executing Strategy source. |
| validation evidence -> phase closure | Phase closure depends on automated proof, not manual claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-14 | E | web/API import boundary | mitigate | Source-scan test rejects executable runtime imports outside worker/runtime packages. |
| T-10-15 | R | validation scaffold | mitigate | Every ISO row requires command-backed evidence. |
| T-10-16 | I | Docker note | accept | Docker end-to-end is Phase 12; Phase 10 documents non-requirement to avoid false validation claims. |
</threat_model>

<verification>
Run the full command set in `10-VALIDATION.md` plus final grep gates. Update `10-VALIDATION.md` with results before writing the summary.
</verification>

<success_criteria>
Phase 10 is complete when boundary tests pass, all runtime/worker/spec/engine gates pass or have explicitly unrelated blockers, and `10-VALIDATION.md` maps ISO-01 through ISO-07 to command-backed evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/10-runtime-isolation-hardening/10-05-SUMMARY.md`
</output>
