---
phase: 10-runtime-isolation-hardening
plan: 03
type: execute
status: complete
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - packages/runtime-js/src/hostile-matrix.test.ts
  - packages/runtime-js/src/adapter-contract.test.ts
  - packages/runtime-js/src/executor.test.ts
  - packages/runtime-js/src/guards.ts
  - packages/spec/src/types.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/spec.test.ts
autonomous: true
requirements: [ISO-05, ISO-06, ISO-07]
completed: 2026-05-18
must_haves:
  truths:
    - "Hostile Strategy behavior is covered by a matrix, not isolated one-off examples."
    - "Player-caused violations have stable runtime violation types."
    - "Infrastructure failures are not added to public gameplay violation semantics."
  artifacts:
    - path: "packages/runtime-js/src/hostile-matrix.test.ts"
      provides: "Hostile Strategy matrix across runtime adapter boundary"
    - path: "packages/spec/src/types.ts"
      provides: "RuntimeViolation type contract if any player-caused taxonomy additions are needed"
    - path: "packages/spec/src/schemas.ts"
      provides: "RuntimeViolation schema contract if any player-caused taxonomy additions are needed"
  key_links:
    - from: "packages/runtime-js/src/hostile-matrix.test.ts"
      to: "packages/runtime-js/src/executor.ts"
      via: "createRuntimeFromRevision"
      pattern: "createRuntimeFromRevision"
    - from: "packages/spec/src/schemas.ts"
      to: "packages/engine/src/activation.ts"
      via: "RuntimeViolationTypeSchema accepted by Chronicle payload"
      pattern: "RuntimeViolationTypeSchema"
---

<objective>
Expand hostile Strategy tests and lock the player-caused versus system-failure taxonomy.

Purpose: ISO-06, ISO-07, D-08, D-09, D-10, D-11, and D-12 require broad hostile coverage and precise classification. This plan keeps system/infrastructure failures out of `RuntimeViolationType` unless a failure is player-caused and schema-valid.
Output: Table-driven hostile matrix tests and any necessary spec/runtime taxonomy updates.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-runtime-isolation-hardening/10-CONTEXT.md
@.planning/phases/10-runtime-isolation-hardening/10-RESEARCH.md
@.planning/phases/10-runtime-isolation-hardening/10-PATTERNS.md
@packages/runtime-js/src/executor.test.ts
@packages/runtime-js/src/validation.test.ts
@packages/runtime-js/src/guards.ts
@packages/spec/src/types.ts
@packages/spec/src/schemas.ts
@packages/engine/src/activation.ts

<interfaces>
Current public gameplay violation types:
```typescript
export type RuntimeViolationType =
  | "INVALID_OUTPUT"
  | "TIMEOUT"
  | "THROWN_EXCEPTION"
  | "FORBIDDEN_CAPABILITY"
  | "OVERSIZED_OUTPUT"
```

Current public runtime violation payload projection allows only public identifiers and violation `type`; detailed messages remain owner/private.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hostile Strategy matrix</name>
  <files>packages/runtime-js/src/hostile-matrix.test.ts, packages/runtime-js/src/executor.test.ts</files>
  <behavior>
    - Test 1: forbidden globals map to `FORBIDDEN_CAPABILITY`.
    - Test 2: dynamic import attempts are rejected before or during execution as import/forbidden capability failures.
    - Test 3: process, worker, filesystem, and network attempts fail closed.
    - Test 4: infinite loop maps to `TIMEOUT`.
    - Test 5: memory pressure fails as `TIMEOUT`, `OVERSIZED_OUTPUT`, or a system failure only if the process boundary genuinely dies.
    - Test 6: oversized output maps to `OVERSIZED_OUTPUT`.
    - Test 7: invalid output maps to `INVALID_OUTPUT`.
    - Test 8: thrown Strategy exceptions map to `THROWN_EXCEPTION`.
  </behavior>
  <action>Create `hostile-matrix.test.ts` using table-driven Vitest cases. Reuse existing fixture inputs from `executor.test.ts` by extracting local helpers inside the new file rather than importing test internals. Run cases through `createRuntimeFromRevision` with the default worker-thread adapter; add opt-in subprocess coverage for cases that are stable under the subprocess adapter after Plan 10-02. Keep assertions category-based where platform resource failures can differ, but never allow hostile cases to produce `ok: true`.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- hostile-matrix.test.ts executor.test.ts</automated>
  </verify>
  <done>ISO-06 hostile categories are all represented in a single matrix and no hostile case succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock runtime violation taxonomy</name>
  <files>packages/spec/src/types.ts, packages/spec/src/schemas.ts, packages/spec/src/spec.test.ts, packages/runtime-js/src/guards.ts</files>
  <behavior>
    - Test 1: existing player-caused types parse through `RuntimeViolationTypeSchema`.
    - Test 2: infrastructure labels such as `MALFORMED_IPC`, `SUBPROCESS_EXIT`, `SUBPROCESS_SIGNAL`, `SPAWN_FAILED`, and `SYSTEM_FAILURE` are not accepted as public `RuntimeViolationType` unless the executor deliberately adds a player-caused schema-backed type with tests.
    - Test 3: helper functions preserve `INVALID_OUTPUT`, `TIMEOUT`, `THROWN_EXCEPTION`, `FORBIDDEN_CAPABILITY`, and `OVERSIZED_OUTPUT`.
  </behavior>
  <action>Prefer keeping `packages/spec` runtime violation types unchanged unless implementing a clearly player-caused new type is necessary. If adding a player-caused type, update `types.ts`, `schemas.ts`, and `spec.test.ts` together and confirm replay public projection remains privacy-safe. Do not add malformed IPC, subprocess exit, signal termination, spawn failure, or generic system failure to gameplay `RuntimeViolationType` per D-10.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec test -- spec.test.ts</automated>
    <automated>pnpm --filter @cowards/runtime-js test -- executor.test.ts hostile-matrix.test.ts</automated>
  </verify>
  <done>Player-caused runtime violation taxonomy is explicit and infrastructure failure labels cannot leak into public gameplay semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend adapter contract failure cases</name>
  <files>packages/runtime-js/src/adapter-contract.test.ts, packages/runtime-js/src/hostile-matrix.test.ts</files>
  <behavior>
    - Test 1: default worker-thread adapter and opt-in subprocess adapter both return schema-normalized results for valid output.
    - Test 2: player-caused invalid output is returned as `RuntimeResult` failure.
    - Test 3: infrastructure failures throw or reject through the runtime package system-failure channel and are not converted to `RuntimeViolation`.
  </behavior>
  <action>Update adapter contract tests to run shared valid/invalid/timeout cases against worker-thread and subprocess adapters. Keep adapter-specific infrastructure failure cases in subprocess tests but assert the same classification semantics. This plan depends on Plan 10-02 system-failure class/code names; reuse those names instead of inventing parallel labels.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- adapter-contract.test.ts subprocess-adapter.test.ts hostile-matrix.test.ts</automated>
    <automated>pnpm --filter @cowards/runtime-js typecheck</automated>
    <automated>pnpm --filter @cowards/spec typecheck</automated>
  </verify>
  <done>Adapter behavior is contract-tested across both adapters and taxonomy distinctions are stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| hostile Strategy source -> runtime adapter | Attacker-controlled code attempts to escape or exhaust runtime. |
| runtime failure -> Chronicle semantics | Failure category determines whether engine records gameplay violation or worker records system failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-08 | E | hostile source matrix | mitigate | Matrix covers forbidden globals, imports, process/worker/fs/network, memory, loops, invalid output, oversized output, and exceptions. |
| T-10-09 | I/R | runtime taxonomy | mitigate | Infrastructure failures are excluded from public `RuntimeViolationType`. |
| T-10-10 | D | memory/loop hostile cases | mitigate | Timeout/resource limits are tested and failures never return success. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/runtime-js test -- hostile-matrix.test.ts adapter-contract.test.ts subprocess-adapter.test.ts executor.test.ts`, `pnpm --filter @cowards/spec test -- spec.test.ts`, `pnpm --filter @cowards/runtime-js typecheck`, and `pnpm --filter @cowards/spec typecheck`.
</verification>

<success_criteria>
ISO-05, ISO-06, and ISO-07 are satisfied at runtime-package level when hostile matrix tests cover every required category and system/infrastructure failures cannot become gameplay `RuntimeViolation` payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/10-runtime-isolation-hardening/10-03-SUMMARY.md`
</output>
