---
phase: 11-doctrine-debugging-ux
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - apps/web/app/workshop/types.ts
  - apps/web/app/workshop/workshop-client-state.ts
  - apps/web/app/workshop/workshop-client.tsx
  - apps/web/app/workshop/workshop-client.test.tsx
autonomous: true
requirements: [DEBUG-01, DEBUG-02, DEBUG-03]
must_haves:
  truths:
    - "Workshop validation rows render Strategy API constraint and remediation copy."
    - "Workshop sample entries distinguish starter samples from failure-mode samples."
    - "Workshop Match result rows show Open replay only when a completed Match has replay data."
    - "Pending, running, failed, and completed-without-Chronicle Match rows explain why replay is unavailable."
  artifacts:
    - path: "apps/web/app/workshop/workshop-client-state.ts"
      provides: "Pure copy helpers for validation guidance and replay availability reasons"
    - path: "apps/web/app/workshop/workshop-client.tsx"
      provides: "Workshop rendering for guidance, sample groups, and replay-unavailable reasons"
    - path: "apps/web/app/workshop/workshop-client.test.tsx"
      provides: "Helper and component regression coverage"
  key_links:
    - from: "apps/web/app/workshop/workshop-client.tsx"
      to: "apps/web/app/workshop/workshop-client-state.ts"
      via: "validation/replay display helpers"
      pattern: "format.*Guidance|getReplay.*Reason"
    - from: "apps/web/app/workshop/workshop-client.tsx"
      to: "apps/web/app/workshop/types.ts"
      via: "Workshop sample DTO fields"
      pattern: "sampleKind"
---

<objective>
Render the actionable Workshop debugging surface for validation failures, sample selection, and replay handoff states.

Purpose: DEBUG-01 through DEBUG-03 and D-08 through D-12 require user-visible copy while preserving the existing helper-driven Workshop pattern.
Output: Workshop helper functions, UI rendering, and focused tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-doctrine-debugging-ux/11-CONTEXT.md
@.planning/phases/11-doctrine-debugging-ux/11-RESEARCH.md
@.planning/phases/11-doctrine-debugging-ux/11-PATTERNS.md
@.planning/phases/11-doctrine-debugging-ux/11-01-SUMMARY.md
@.planning/phases/11-doctrine-debugging-ux/11-02-SUMMARY.md
@apps/web/app/workshop/types.ts
@apps/web/app/workshop/workshop-client-state.ts
@apps/web/app/workshop/workshop-client.tsx
@apps/web/app/workshop/workshop-client.test.tsx

<interfaces>
From `apps/web/app/workshop/workshop-client-state.ts`:
```typescript
export const formatValidationIssueHeading = (
  issue: StrategyRevisionValidationReport["errors"][number],
): string => `${issue.severity.toUpperCase()} · ${issue.code}`

export const getReplayHref = (matchId: string): string =>
  `/matches/${encodeURIComponent(matchId)}/replay`

export const canOpenReplay = (match: WorkshopMatchSummary): boolean =>
  match.status === "complete" && match.hasReplay === true
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Workshop copy helpers</name>
  <files>apps/web/app/workshop/workshop-client-state.ts, apps/web/app/workshop/workshop-client.test.tsx</files>
  <behavior>
    - Test 1: validation issue guidance renders as concise `constraint -> remediation` copy when fields exist.
    - Test 2: validation issue guidance falls back to the existing `message` when guidance fields are absent.
    - Test 3: replay availability helper returns open state only for `status: "complete"` with `hasReplay: true`.
    - Test 4: pending, running, failed system, blocked, and complete/no-replay states each return a distinct unavailable reason per D-09.
  </behavior>
  <action>Add pure helpers such as `formatValidationIssueGuidance(issue)` and `getReplayAvailability(match)` in `workshop-client-state.ts`. Keep `canOpenReplay` as the dead-link gate per D-08, and make unavailable copy concise per D-10. Do not embed status branching directly in JSX.</action>
  <verify>
    <automated>pnpm --filter @cowards/web test -- workshop-client.test.tsx</automated>
  </verify>
  <done>Workshop display decisions are covered by helper tests and include all replay-unavailable states required by D-09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render validation guidance and sample groups</name>
  <files>apps/web/app/workshop/types.ts, apps/web/app/workshop/workshop-client.tsx, apps/web/app/workshop/workshop-client.test.tsx</files>
  <behavior>
    - Test 1: validation rows include the issue heading plus one guidance/remediation line.
    - Test 2: starter samples and failure-mode samples render as separate groups or clearly labeled controls.
    - Test 3: applying a failure-mode sample still uses the existing dirty-draft confirmation flow.
  </behavior>
  <action>Update Workshop DTO imports/types to reflect the sample metadata from Plan 11-02. Render validation guidance from the Plan 11-01 fields using helper output. Render failure-mode samples separately or with an explicit label per D-05 and D-06, without hiding valid starter samples.</action>
  <verify>
    <automated>pnpm --filter @cowards/web test -- workshop-client.test.tsx</automated>
    <automated>pnpm --filter @cowards/web typecheck</automated>
  </verify>
  <done>Workshop users can see concise validation remediation and choose from both mechanics and failure-mode samples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render replay links and unavailable reasons</name>
  <files>apps/web/app/workshop/workshop-client.tsx, apps/web/app/workshop/workshop-client.test.tsx</files>
  <behavior>
    - Test 1: completed Match with replay data renders an `Open replay` link using `getReplayHref`.
    - Test 2: pending and running rows render unavailable copy and no anchor.
    - Test 3: failed, blocked, and complete/no-replay rows render unavailable copy and no dead link.
  </behavior>
  <action>Replace the generic `Replay unavailable` span with helper-provided reason copy while preserving `canOpenReplay(match)` as the only condition that renders the anchor per D-08 and D-09. Keep row labels short and focused on Match/replay state.</action>
  <verify>
    <automated>pnpm --filter @cowards/web test -- workshop-client.test.tsx</automated>
    <automated>pnpm --filter @cowards/web typecheck</automated>
  </verify>
  <done>Workshop Match result rows either link to an existing replay or explain exactly why no replay link is available.</done>
</task>

</tasks>

<risks>
- Risk: UI copy duplicates logic already represented in status fields. Mitigation: helpers translate existing DTO state; they do not infer Match rules or replay availability beyond `status` and `hasReplay`.
- Risk: failure-mode samples look like broken starter templates. Mitigation: Plan 11-02 metadata is rendered visibly in this plan.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| validation report -> Workshop UI | Structured validation data becomes user-facing copy. |
| Match summary -> replay link | Persisted Match status controls whether a replay URL is exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-07 | R | replay handoff rows | mitigate | `canOpenReplay` remains the only link gate and tests cover every unavailable state. |
| T-11-08 | I | validation rows | mitigate | Render only structured guidance and existing safe messages, not raw runtime private details. |
| T-11-09 | T | sample rendering | mitigate | Use explicit sample metadata and dirty-draft confirmation to prevent accidental draft replacement. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/web test -- workshop-client.test.tsx` and `pnpm --filter @cowards/web typecheck`.
</verification>

<success_criteria>
DEBUG-01, DEBUG-02, and DEBUG-03 are user-visible in the Workshop when validation rows show constraint/remediation, samples are grouped by intent, and replay rows never show dead links.
</success_criteria>

<output>
After completion, create `.planning/phases/11-doctrine-debugging-ux/11-03-SUMMARY.md`
</output>
