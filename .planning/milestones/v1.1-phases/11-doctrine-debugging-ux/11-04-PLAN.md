---
phase: 11-doctrine-debugging-ux
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - packages/spec/src/types.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/spec.test.ts
  - packages/replay/src/debug-explanations.ts
  - packages/replay/src/debug-explanations.test.ts
  - packages/replay/src/index.ts
autonomous: true
requirements: [DEBUG-04, DEBUG-05]
must_haves:
  truths:
    - "Owner debug explanation DTOs expose stable cause codes and concise labels."
    - "Cause coverage includes not selected, invalid action, blocked movement, timeout, thrown exception, STONE, FALLEN, and Match ended."
    - "Explanation derivation uses replay/engine-derived facts rather than React rule inference."
  artifacts:
    - path: "packages/spec/src/types.ts"
      provides: "Soldier inactivity explanation cause-code DTO types"
    - path: "packages/spec/src/schemas.ts"
      provides: "Zod schemas for owner debug explanation DTOs"
    - path: "packages/replay/src/debug-explanations.ts"
      provides: "Pure replay-derived Soldier inactivity explanation generator"
    - path: "packages/replay/src/debug-explanations.test.ts"
      provides: "Cause-code coverage tests"
  key_links:
    - from: "packages/replay/src/debug-explanations.ts"
      to: "packages/spec/src/types.ts"
      via: "SoldierInactivityExplanationCause"
      pattern: "not_selected|invalid_action|blocked_movement|timeout|thrown_exception|stone|fallen|match_ended"
    - from: "packages/replay/src/debug-explanations.ts"
      to: "Chronicle events and replay states"
      via: "event/snapshot derived facts"
      pattern: "RUNTIME_VIOLATION|MOVE_BLOCKED|SOLDIER_STONED|SOLDIER_FELL|MATCH_ENDED"
---

<objective>
Create owner-only Soldier inactivity explanation DTOs and pure derivation logic.

Purpose: DEBUG-04, DEBUG-05, D-01, and D-02 require stable cause codes and concise labels generated from replay/engine facts before React rendering.
Output: Shared DTO/schema types, replay package explanation generator, exports, and cause coverage tests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-doctrine-debugging-ux/11-CONTEXT.md
@.planning/phases/11-doctrine-debugging-ux/11-RESEARCH.md
@.planning/phases/11-doctrine-debugging-ux/11-PATTERNS.md
@.planning/phases/11-doctrine-debugging-ux/11-01-SUMMARY.md
@packages/spec/src/types.ts
@packages/spec/src/schemas.ts
@packages/replay/src/reconstruct.ts
@packages/replay/src/project.ts
@packages/replay/src/index.ts
@packages/test-utils/src/replay-scenarios.ts

<interfaces>
Required cause codes from D-02:
```typescript
type SoldierInactivityCause =
  | "not_selected"
  | "invalid_action"
  | "blocked_movement"
  | "timeout"
  | "thrown_exception"
  | "stone"
  | "fallen"
  | "match_ended"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define explanation DTO contract</name>
  <files>packages/spec/src/types.ts, packages/spec/src/schemas.ts, packages/spec/src/spec.test.ts</files>
  <behavior>
    - Test 1: schema accepts all eight required cause codes from D-02.
    - Test 2: schema requires `soldierId`, `cause`, `label`, `remediation`, and `sequence`.
    - Test 3: labels/remediations must be non-empty strings and payloads must be serializable JSON.
  </behavior>
  <action>Add shared owner-debug DTO types such as `SoldierInactivityExplanationCause` and `SoldierInactivityExplanationDto`. Include `soldierId`, optional `playerId`, `sequence`, `cause`, `label`, `remediation`, and optional public-safe `details`. Mirror the DTO in Zod schemas. Use D-01 structured cause codes and D-02 coverage exactly.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec test -- spec.test.ts</automated>
    <automated>pnpm --filter @cowards/spec typecheck</automated>
  </verify>
  <done>Shared DTO and schema contracts exist and enumerate all required inactivity causes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate replay-derived inactivity explanations</name>
  <files>packages/replay/src/debug-explanations.ts, packages/replay/src/debug-explanations.test.ts, packages/replay/src/index.ts</files>
  <behavior>
    - Test 1: Soldier omitted from selected activation orders yields `not_selected`.
    - Test 2: runtime `INVALID_OUTPUT` or immediate reversal facts yield `invalid_action`.
    - Test 3: `MOVE_BLOCKED` or `PUSH_BLOCKED` facts yield `blocked_movement`.
    - Test 4: runtime `TIMEOUT` yields `timeout`; runtime `THROWN_EXCEPTION` yields `thrown_exception`.
    - Test 5: STONE, FALLEN, and ended Match facts yield `stone`, `fallen`, and `match_ended`.
  </behavior>
  <action>Create `packages/replay/src/debug-explanations.ts` with a pure function such as `buildSoldierInactivityExplanations(input)` that consumes validated Chronicle events and replay states/snapshots. Derive causes only from existing event payloads, Soldier status snapshots, runtime violation type, and Match end facts. Export the helper from `packages/replay/src/index.ts`. Do not import React, web code, persistence, filesystem, network, clock, or random APIs.</action>
  <verify>
    <automated>pnpm --filter @cowards/replay test -- debug-explanations.test.ts</automated>
    <automated>pnpm --filter @cowards/replay typecheck</automated>
  </verify>
  <done>Replay package produces deterministic owner debug explanations covering every D-02 cause without UI rule inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock explanation source boundaries</name>
  <files>packages/replay/src/debug-explanations.test.ts</files>
  <behavior>
    - Test 1: explanation output contains no Strategy source, StrategyMemory, SoldierMemory, objective payload, Awareness Grid, raw runtime details, or stack/message strings.
    - Test 2: explanations are deterministic for the same Chronicle/replay-state input.
    - Test 3: each explanation carries the event/replay sequence that caused it.
  </behavior>
  <action>Add privacy and determinism tests for `buildSoldierInactivityExplanations`. The output may include public-safe reason details such as blocked movement reason codes, but must not include private payload keys or raw runtime messages per D-03 and D-04.</action>
  <verify>
    <automated>pnpm --filter @cowards/replay test -- debug-explanations.test.ts</automated>
  </verify>
  <done>Explanation DTOs are deterministic, sequence-linked, and privacy-safe before any web rendering work begins.</done>
</task>

</tasks>

<risks>
- Risk: explanation derivation drifts into a second rules engine. Mitigation: derive only from Chronicle events and replay snapshots already produced by engine/replay layers.
- Risk: not-selected detection lacks enough facts in some Chronicles. Mitigation: tests should cover available activation order facts; unknown cases should omit an explanation rather than guess.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Chronicle/private facts -> owner DTO | Owner-only/private replay facts become structured debug explanations. |
| replay package -> web UI | Data-derived DTOs cross into React rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-10 | I | `debug-explanations.ts` | mitigate | Tests assert no private keys, Strategy source, memory, objectives, raw Awareness Grid, or raw runtime details in DTOs. |
| T-11-11 | T/R | cause-code mapper | mitigate | Required causes are enum/schema-backed and tested against replay facts. |
| T-11-12 | I | unknown facts | accept | If replay facts are insufficient, omit explanation rather than infer rules in React. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/spec test -- spec.test.ts`, `pnpm --filter @cowards/replay test -- debug-explanations.test.ts`, and both package typechecks.
</verification>

<success_criteria>
DEBUG-04 and DEBUG-05 are data-contract ready when all D-02 causes have stable DTOs, replay-derived generation tests pass, and no React/UI code is needed to compute explanations.
</success_criteria>

<output>
After completion, create `.planning/phases/11-doctrine-debugging-ux/11-04-SUMMARY.md`
</output>
