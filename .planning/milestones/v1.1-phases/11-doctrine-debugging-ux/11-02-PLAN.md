---
phase: 11-doctrine-debugging-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/persistence/src/workshop.ts
  - packages/persistence/src/workshop.test.ts
  - packages/runtime-js/src/validation.test.ts
autonomous: true
requirements: [DEBUG-02]
must_haves:
  truths:
    - "User can start from samples that teach common doctrine mechanics."
    - "User can inspect failure-mode samples that intentionally violate Strategy API constraints."
    - "Sample Strategy sources are reusable as validation/runtime test inputs where practical."
  artifacts:
    - path: "packages/persistence/src/workshop.ts"
      provides: "Expanded Workshop sample Strategy catalog"
    - path: "packages/persistence/src/workshop.test.ts"
      provides: "Catalog coverage and validation expectation tests"
    - path: "packages/runtime-js/src/validation.test.ts"
      provides: "Failure-mode sample validation regression coverage"
  key_links:
    - from: "packages/persistence/src/workshop.ts"
      to: "packages/runtime-js/src/validation.ts"
      via: "validateStrategySource for catalog entries"
      pattern: "validateStrategySource"
---

<objective>
Expand the built-in Strategy sample catalog to teach doctrine patterns and failure modes.

Purpose: DEBUG-02 and D-05 through D-07 require useful starter samples plus intentionally failing examples that double as regression inputs.
Output: Catalog metadata, new sample sources, and tests that prove each sample validates or fails as intended.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-doctrine-debugging-ux/11-CONTEXT.md
@.planning/phases/11-doctrine-debugging-ux/11-RESEARCH.md
@.planning/phases/11-doctrine-debugging-ux/11-PATTERNS.md
@packages/persistence/src/workshop.ts
@packages/persistence/src/workshop.test.ts
@packages/runtime-js/src/validation.test.ts

<interfaces>
From `packages/persistence/src/workshop.ts`:
```typescript
export interface WorkshopTemplateSummary {
  id: "template:cautious" | "template:reckless" | "template:sentinel"
  label: string
  source: string
  validation: StrategyRevisionValidationReport
}

export const listWorkshopTemplates = (): WorkshopTemplateSummary[] => [
  /* built-in validated sources */
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sample metadata contract</name>
  <files>packages/persistence/src/workshop.ts, packages/persistence/src/workshop.test.ts</files>
  <behavior>
    - Test 1: every sample has an id, label, `sampleKind`, concise description, and source.
    - Test 2: valid mechanics samples have `sampleKind: "starter"` and pass validation.
    - Test 3: failure-mode samples have `sampleKind: "failure-mode"` and fail validation with the expected code.
  </behavior>
  <action>Extend `WorkshopTemplateSummary` or add a sibling `WorkshopSampleSummary` so the catalog can distinguish valid starter samples from deliberate failure-mode samples per D-05 and D-06. Preserve existing template consumers by keeping the currently expected starter entries valid. Include optional `expectedValidationCode` only for failure-mode samples so tests can assert the intended failure.</action>
  <verify>
    <automated>pnpm --filter @cowards/persistence test -- workshop.test.ts</automated>
    <automated>pnpm --filter @cowards/persistence typecheck</automated>
  </verify>
  <done>The catalog can represent both starter and failure-mode samples without weakening existing valid-template assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mechanics starter samples</name>
  <files>packages/persistence/src/workshop.ts, packages/persistence/src/workshop.test.ts</files>
  <behavior>
    - Test 1: starter samples include basic advance/turn, push setup, Backstab setup, and stoning/blocking.
    - Test 2: every starter sample validates with zero errors.
    - Test 3: starter sample ids remain stable and start with `sample:`.
  </behavior>
  <action>Add valid starter samples required by D-06: basic advance/turn, push setup, Backstab setup, and stoning/blocking. Keep Strategy code deterministic and within existing source limits. Do not add new runtime execution in persistence; use validation only.</action>
  <verify>
    <automated>pnpm --filter @cowards/persistence test -- workshop.test.ts</automated>
  </verify>
  <done>Valid mechanics samples are available from the Workshop catalog and each validates successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add failure-mode samples as regression inputs</name>
  <files>packages/persistence/src/workshop.ts, packages/persistence/src/workshop.test.ts, packages/runtime-js/src/validation.test.ts</files>
  <behavior>
    - Test 1: failure-mode samples include a runtime violation example and minimal do-nothing or invalid-output example per D-06.
    - Test 2: invalid-output/do-nothing sample validation behavior is explicit and asserted by expected code.
    - Test 3: failure-mode sample sources are imported or duplicated into runtime validation tests so D-07 is satisfied where practical.
  </behavior>
  <action>Add clearly labeled failure-mode samples for runtime violation and minimal invalid output/do-nothing behavior per D-05 through D-07. If a runtime violation sample is syntactically valid but fails only during execution, mark it as `expectedRuntimeViolationType` instead of forcing validation failure. Keep the UI-facing labels explicit that these samples are meant to demonstrate failures.</action>
  <verify>
    <automated>pnpm --filter @cowards/persistence test -- workshop.test.ts</automated>
    <automated>pnpm --filter @cowards/runtime-js test -- validation.test.ts</automated>
  </verify>
  <done>Failure-mode samples are discoverable, explicitly labeled, and tested as intentional inputs rather than accidental broken templates.</done>
</task>

</tasks>

<risks>
- Risk: intentionally invalid samples confuse users if displayed with valid starters. Mitigation: catalog metadata separates `starter` and `failure-mode` entries for the Workshop renderer.
- Risk: sample sources drift from validation constraints. Mitigation: catalog tests validate every starter and assert intended failure codes for failure-mode entries.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| sample Strategy source -> Workshop catalog | Built-in Strategy source is shown to users and may be submitted or executed later through normal boundaries. |
| sample validation -> tests | Failure-mode samples become regression inputs without bypassing validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-04 | R | sample catalog | mitigate | Metadata labels whether a sample is starter or failure-mode and tests assert expected validation outcomes. |
| T-11-05 | E | sample source | mitigate | Persistence only validates and stores source; it does not execute Strategy code in web/API or persistence contexts. |
| T-11-06 | T | sample ids | mitigate | Tests require stable `sample:` ids to prevent UI/test references from silently drifting. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/persistence test -- workshop.test.ts`, `pnpm --filter @cowards/runtime-js test -- validation.test.ts`, and `pnpm --filter @cowards/persistence typecheck`.
</verification>

<success_criteria>
DEBUG-02 is satisfied when the Workshop sample catalog includes D-06 mechanics and failure-mode samples, every sample has explicit metadata, valid samples pass validation, and failure samples are tested with expected outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-doctrine-debugging-ux/11-02-SUMMARY.md`
</output>
