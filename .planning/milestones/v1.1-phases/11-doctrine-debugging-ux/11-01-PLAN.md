---
phase: 11-doctrine-debugging-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/spec/src/types.ts
  - packages/spec/src/schemas.ts
  - packages/spec/src/spec.test.ts
  - packages/runtime-js/src/validation.ts
  - packages/runtime-js/src/guards.ts
  - packages/runtime-js/src/validation.test.ts
autonomous: true
requirements: [DEBUG-01]
must_haves:
  truths:
    - "Validation errors identify the violated Strategy API constraint."
    - "Validation and runtime violation descriptors include one concise remediation step."
    - "Raw runtime details remain separate from user-safe guidance copy."
  artifacts:
    - path: "packages/spec/src/types.ts"
      provides: "Shared validation guidance and runtime guidance DTO types"
    - path: "packages/spec/src/schemas.ts"
      provides: "Zod schemas for expanded validation report DTOs"
    - path: "packages/runtime-js/src/validation.ts"
      provides: "Strategy validation issues with actionable constraint/remediation metadata"
    - path: "packages/runtime-js/src/guards.ts"
      provides: "Safe runtime violation guidance mapper"
  key_links:
    - from: "packages/runtime-js/src/validation.ts"
      to: "packages/spec/src/types.ts"
      via: "StrategyRevisionValidationIssue guidance fields"
      pattern: "constraint.*remediation"
    - from: "packages/runtime-js/src/guards.ts"
      to: "packages/spec/src/types.ts"
      via: "RuntimeViolationType guidance mapping"
      pattern: "RuntimeViolationType"
---

<objective>
Define the shared actionable-message contract for Strategy validation and runtime violations.

Purpose: DEBUG-01 and D-10 through D-12 require concise messages that name the Strategy API constraint and a remediation path without turning the UI into a docs dump.
Output: Typed guidance fields, schemas, runtime mapping helpers, and unit coverage.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-doctrine-debugging-ux/11-CONTEXT.md
@.planning/phases/11-doctrine-debugging-ux/11-RESEARCH.md
@.planning/phases/11-doctrine-debugging-ux/11-PATTERNS.md
@packages/spec/src/types.ts
@packages/spec/src/schemas.ts
@packages/spec/src/spec.test.ts
@packages/runtime-js/src/validation.ts
@packages/runtime-js/src/guards.ts
@packages/runtime-js/src/validation.test.ts

<interfaces>
From `packages/spec/src/types.ts`:
```typescript
export type StrategyRevisionValidationCode =
  | "SOURCE_TOO_LARGE"
  | "FORBIDDEN_PATTERN"
  | "MISSING_DEFAULT_EXPORT"
  | "MISSING_SELECT_ACTIVATIONS"
  | "MISSING_SOLDIER_BRAIN"
  | "ASYNC_METHOD_NOT_ALLOWED"
  | "IMPORT_NOT_ALLOWED"
  | "TRANSPILE_FAILED"
  | "ENGINE_INCOMPATIBLE"

export interface StrategyRevisionValidationIssue {
  code: StrategyRevisionValidationCode
  severity: StrategyRevisionValidationSeverity
  message: string
  pattern?: string | undefined
}

export const RUNTIME_VIOLATION_TYPES = [
  "INVALID_OUTPUT",
  "TIMEOUT",
  "THROWN_EXCEPTION",
  "FORBIDDEN_CAPABILITY",
  "OVERSIZED_OUTPUT",
] as const
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared guidance DTO fields</name>
  <files>packages/spec/src/types.ts, packages/spec/src/schemas.ts, packages/spec/src/spec.test.ts</files>
  <behavior>
    - Test 1: `StrategyRevisionValidationIssueSchema` accepts issues with `constraint`, `remediation`, and optional `reference` fields.
    - Test 2: the schema rejects empty `constraint` or empty `remediation`.
    - Test 3: existing validation reports without optional guidance fields remain valid for compatibility.
  </behavior>
  <action>Add optional structured guidance fields to `StrategyRevisionValidationIssue`: `constraint`, `remediation`, and `reference`. `constraint` names the violated Strategy API rule, `remediation` is one short next step, and `reference` is a short docs/sample pointer when useful per D-12. Mirror the fields in `packages/spec/src/schemas.ts` and add schema tests. Do not remove existing `message` or `pattern` fields.</action>
  <verify>
    <automated>pnpm --filter @cowards/spec test -- spec.test.ts</automated>
    <automated>pnpm --filter @cowards/spec typecheck</automated>
  </verify>
  <done>Expanded validation issue DTOs are typed, schema-validated, and backward-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit actionable validation guidance</name>
  <files>packages/runtime-js/src/validation.ts, packages/runtime-js/src/validation.test.ts</files>
  <behavior>
    - Test 1: missing default export names the `export default` Strategy API constraint and recommends adding the default Strategy object.
    - Test 2: missing `selectActivations` and missing `soldierBrain` name the exact required method and recommend implementing it.
    - Test 3: forbidden import, async method, source size, transpile, and engine compatibility failures each include a non-empty constraint and remediation.
  </behavior>
  <action>Update the local `issue(...)` helper to accept guidance metadata and populate every validation error path per D-10 and D-11. Keep messages concise and continue returning the same validation codes. Avoid long reference text in report fields; use short sample/doc identifiers only when useful per D-12.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- validation.test.ts</automated>
    <automated>pnpm --filter @cowards/runtime-js typecheck</automated>
  </verify>
  <done>Every Strategy validation error produced by `validateStrategySource` includes code, message, constraint, and remediation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Map runtime violations to user-safe guidance</name>
  <files>packages/runtime-js/src/guards.ts, packages/runtime-js/src/validation.test.ts</files>
  <behavior>
    - Test 1: `TIMEOUT` maps to a Strategy API constraint about bounded synchronous execution and recommends simplifying logic.
    - Test 2: `INVALID_OUTPUT` maps to schema-valid `selectActivations` or `soldierBrain` return values.
    - Test 3: `THROWN_EXCEPTION`, `FORBIDDEN_CAPABILITY`, and `OVERSIZED_OUTPUT` map to safe labels without raw stack/source details.
  </behavior>
  <action>Add an exported pure helper such as `describeRuntimeViolationForUser(violation)` that returns `{ label, constraint, remediation }` keyed by `RuntimeViolation.type`. Use it for downstream Workshop/replay copy. Do not expose `violation.message`, stack traces, Strategy source, StrategyMemory, SoldierMemory, or objective payloads through this helper.</action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-js test -- validation.test.ts</automated>
  </verify>
  <done>Runtime violations have deterministic, user-safe guidance descriptors covering all violation types.</done>
</task>

</tasks>

<risks>
- Risk: guidance copy becomes verbose and distracts from Workshop iteration. Mitigation: tests assert concise non-empty fields and UI rendering in later plans uses only one remediation line.
- Risk: raw runtime details leak through guidance. Mitigation: the runtime helper maps by violation type and never forwards raw `message` values.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Strategy source -> validation report | Hostile user-authored source is parsed into user-facing diagnostics. |
| runtime violation -> UI copy | Potentially private/runtime-specific failure details become user-visible guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-01 | I | `describeRuntimeViolationForUser` | mitigate | Map only violation type/category to safe copy; never include raw exception messages or source. |
| T-11-02 | T/R | validation issue metadata | mitigate | Keep structured code/constraint/remediation fields tested by schema and runtime unit tests. |
| T-11-03 | D | validation report size | accept | Guidance fields are short strings and do not change source-size validation limits. |
</threat_model>

<verification>
Run `pnpm --filter @cowards/spec test -- spec.test.ts`, `pnpm --filter @cowards/runtime-js test -- validation.test.ts`, and both package typechecks.
</verification>

<success_criteria>
DEBUG-01 is contract-ready when validation reports and runtime violation descriptors consistently name the Strategy API constraint and one remediation step, with tests proving no raw runtime details are exposed by the guidance helper.
</success_criteria>

<output>
After completion, create `.planning/phases/11-doctrine-debugging-ux/11-01-SUMMARY.md`
</output>
