# v1.22 Verify Work

## Result

Status: PASS with one honest caveat: full signed-in browser MatchSet proof was not rerun in this pass; the runtime, Workshop/account save wiring, and evidence gates are verified locally, and pages are listed for manual/live proof.

## Checks

- Rust compile and Wasmtime runtime regression: pass.
- Zig compile, immutable artifact, Wasmtime ABI proof: pass.
- Runtime-service Zig execution: pass.
- Workshop Zig template/sample validation: pass.
- Go account revision save path accepts Zig only through runtime-service validation: pass.
- WASM/WASI hardening artifacts: pass, 19/19 probes.
- ABI decision: pass, no alternate ABI execution path added.
- Promotion decision: pass, no counted/ranked/sandbox promotion.
- `/workshop` browser smoke: pass; Zig alpha selector, Zig template, editor, validation action, and non-counted/no-std copy are visible.

## Relevant Pages

- `/workshop`
- `/exhibitions/new`
- `/matchsets/{matchSetId}`
- `/matches/{matchId}/replay`
- Screenshot: `.planning/artifacts/v1.22-workshop-zig-alpha-smoke.png`

## Caveat

The realistic signed-in proof should be rerun against live local services before treating v1.22 as a user-facing release candidate in another environment. Zig remains fail-loud and environment-specific: if `zig`, `wasmtime`, or runtime-service validation is unavailable, Zig must remain unavailable there.
