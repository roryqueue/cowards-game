---
phase: 59
plan: 01
slug: owner-account-read-service-slice
status: completed
created: 2026-05-22
---

# Phase 59-01 Plan — Owner Account Read Service Slice

## Tasks

1. [x] Extend service owner-read DTOs.
   - Add owner-safe username to auth session DTOs.
   - Expand Strategy Revision summary DTOs with metadata, hashes, status, runtime semantics, counted eligibility, and compatibility data.

2. [x] Add service-owned owner reads.
   - Add `getAuthSession(sessionId)` to `CowardsService`.
   - Add `listStrategyRevisions(sessionId)` to `CowardsService`.
   - Authorize by resolving the session token before listing revisions.

3. [x] Add web account read boundary.
   - Add `apps/web/lib/account-service-adapter.ts`.
   - Add `apps/web/lib/account-service-boundary.ts`.
   - Keep session ids inside the adapter and out of DTOs.

4. [x] Rewire owner read surfaces.
   - Account page uses service-owned session and revision-list reads.
   - Auth session route returns the service-owned session DTO.
   - Account revisions `GET` uses service-owned revision list.
   - Exhibition entry page uses service-owned revision list while keeping exhibition creation out of scope.

5. [x] Verify and update monitors.
   - Add service tests for auth sessions, revision list authorization, source omission, and missing sessions.
   - Add strict import targets and update monitor baselines.
   - Run service/spec/web tests, boundary monitors, and typecheck.
