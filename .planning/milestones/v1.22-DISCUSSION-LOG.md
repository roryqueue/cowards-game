# v1.22 Discussion Log

## Locked Decisions

- Zig is exposed only after binary detection, target support, immutable WASM artifact metadata, Wasmtime execution, and ABI-valid Match behavior all pass.
- v1.22 runtime hardening is candidate-readiness evidence only, not production sandbox certification.
- ABI evolution produces a decision record comparing Preview 1 JSON, direct exports, and component model/WIT. Preview 1 stdin/stdout JSON remains the only v1.22 execution path.

## Top Uncertainties Discussed

- Zig ergonomics versus proof honesty: v1.22 chooses a tiny no-std Zig starter because Zig std currently imports capabilities outside the allowed WASI candidate lane.
- Sandbox language: stronger probes improve confidence, but v1.22 must avoid production certification claims.
- ABI direction: direct exports and component model are interesting but need a dedicated memory/interface proof before execution changes.

## Autonomous Defaults

- Preserve Go ownership and runtime-service execution boundary.
- Preserve JS/TS counted path and Python exhibition beta.
- Keep Rust/Zig/WASM non-counted unless a future milestone explicitly proves and approves a narrower promotion.
- Fail loud on toolchain/runtime gaps rather than silently substituting another language/runtime.

