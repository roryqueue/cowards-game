# Phase 160 Research

The v1.21 proof script is the best base: it signs up, saves account revisions, creates non-counted exhibitions, runs bounded Go worker iterations, opens MatchSet result and replay pages, and scans public pages for private markers.

Product reality requires distinct revision ids for same-language exhibitions. v1.23 therefore saves one primary JS/TS, Rust, and Zig revision plus companion Rust/Zig revisions to make Rust-vs-Rust and Zig-vs-Zig legal without duplicate ids.
