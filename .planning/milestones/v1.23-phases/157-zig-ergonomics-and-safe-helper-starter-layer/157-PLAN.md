# Phase 157 Plan

1. Replace the Workshop Zig starter/sample with a small no-std helper layer around input search and envelope output.
2. Add v1.23 Zig helper capability evidence proving compiled imports stay limited to `fd_read` and `fd_write`.
3. Keep Workshop/sample copy honest: Zig is non-counted exhibition beta only after proof and never counted/ranked/ladder/gauntlet eligible.
4. Validate with `pnpm --filter @cowards/persistence test` and `pnpm wasm-wasi:beta-evaluate:check`.
