# Phase 158 Plan

1. Add a v1.23 WASM/WASI beta readiness evaluator and package scripts.
2. Cover Rust and Zig compile, artifact metadata, execution, invalid output, malformed IPC, timeout/fuel, memory, stdout cap, forbidden imports, stale hash, missing bytes, target mismatch, and ABI mismatch.
3. Emit JSON and Markdown evidence with beta-readiness-only language.
4. Validate with runtime-wasm-wasi tests and evaluator check mode.
