# Phase 161 UI Spec

Rust and Zig labels should be compact and consistent:

- `Rust · non-counted exhibition beta`
- `Zig · non-counted exhibition beta`

Controls must still keep counted play restricted to JS/TS. Public evidence may show runtime labels, non-counted status, artifact/evidence summaries, MatchSet status, and replay links. Public pages must not show Strategy source, private memory, objectives, raw diagnostics, host paths, env values, tokens, DB DSNs, package paths, or private runtime internals.
