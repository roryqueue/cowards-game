# Phase 119 Plan: Python Validation and Public-Safe Diagnostics

## Goal

Replace heuristic syntax validation with a real AST/compile host and safe diagnostic normalization.

## Tasks

- Add a Python validation host that uses `ast.parse` and `compile`.
- Keep validation non-executing: never call Strategy API functions.
- Map validation host issues to existing Strategy Revision validation codes.
- Update validation warnings to say Python is non-counted exhibition beta.
- Test syntax, import, missing API, and public-safe diagnostic behavior.

## Verification

- `pnpm --filter @cowards/runtime-python test`
- `pnpm --filter @cowards/runtime-python typecheck`
- `pnpm exec tsx scripts/check-boundary-monitors.ts`

