# Phase 121 Plan: Signed-In Multi-Language Exhibition Proof

## Goal

Create a repeatable signed-in proof for JS/TS plus Python non-counted exhibition beta through Go -> Runtime Broker -> runtime implementation -> replay evidence.

## Tasks

- Add a Playwright proof spec gated by `RUN_V1_18_PROOF=1`.
- Sign up a local account.
- Save a JS/TS account revision and a Python account revision.
- Create a non-counted exhibition using both revisions.
- Execute queued Matches via Go internal run-once endpoint.
- Open MatchSet result and replay evidence.
- Scan public replay body for private-output markers.

## Verification

- `pnpm --filter @cowards/web typecheck`
- `RUN_V1_18_PROOF=1 ... pnpm e2e --project=desktop --workers=1 v1-18-exhibition-proof.spec.ts`

