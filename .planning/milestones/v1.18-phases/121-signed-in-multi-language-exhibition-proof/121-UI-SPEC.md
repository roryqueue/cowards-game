# Phase 121 UI Spec

## Contract

- Account page must show the saved Python revision with non-counted exhibition beta labeling.
- Exhibition creation must allow Python only when unranked/non-counted is selected.
- MatchSet result must expose replay evidence when Go orchestration completes.
- Replay page must render a plausible board and omit private Strategy/runtime markers.

