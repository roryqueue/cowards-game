# Phase 121 Research

## Key Findings

- Existing service E2E proof is skipped unless local services are enabled.
- Go live backend exposes `/internal/match-jobs/run-once` for controlled local orchestration when an internal token is configured.
- A realistic proof should use account sign-up, account revision saves, non-counted exhibition creation, Go job execution, public MatchSet result, and replay evidence.

