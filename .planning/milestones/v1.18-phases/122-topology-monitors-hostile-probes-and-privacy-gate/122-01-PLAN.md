# Phase 122 Plan: Topology, Monitors, Hostile Probes, and Privacy Gate

## Goal

Extend monitors and privacy gates so runtime isolation, ownership, fallback, proof, and leak regressions fail loudly.

## Tasks

- Add monitor checks for the v1.18 isolation baseline artifact.
- Add monitor checks for Python hardening, validation host, account revision metadata, UI labels, and proof spec source markers.
- Add monitor checks for the signed-in exhibition proof artifact.
- Keep existing runtime registry, no-backend, topology, and privacy monitors intact.

## Verification

- `pnpm exec tsx scripts/check-boundary-monitors.ts`
- `pnpm boundary:monitors`
- `RUN_V1_18_PROOF=1 ... pnpm e2e --project=desktop --workers=1 v1-18-exhibition-proof.spec.ts`

