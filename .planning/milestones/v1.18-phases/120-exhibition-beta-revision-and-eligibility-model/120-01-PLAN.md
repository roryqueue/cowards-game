# Phase 120 Plan: Exhibition Beta Revision and Eligibility Model

## Goal

Make Python saveable as account-owned immutable non-counted exhibition beta revisions without weakening JS/TS counted eligibility.

## Tasks

- Pass `sourceFormat` through account revision save.
- Add Go-side Python runtime metadata and conservative validation metadata for account-owned revisions.
- Return safe Python runtime semantics in account summaries.
- Keep Python rejected from counted play while allowed in non-counted exhibitions.
- Update user-facing labels to say "non-counted exhibition beta."

## Verification

- `cd apps/go-backend && PATH=/usr/local/go/bin:$PATH go test ./...`
- `pnpm --filter @cowards/web typecheck`
- `pnpm exec tsx scripts/check-boundary-monitors.ts`

