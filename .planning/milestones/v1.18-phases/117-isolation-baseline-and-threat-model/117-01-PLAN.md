# Phase 117 Plan: Isolation Baseline and Threat Model

## Goal

Create a monitor-readable v1.18 baseline and human threat model that preserves v1.17 topology while separating non-counted exhibition beta readiness from production sandbox certification.

## Tasks

- Create `.planning/artifacts/v1.18-isolation-baseline.json`.
- Create `.planning/artifacts/v1.18-isolation-baseline.md`.
- Extend boundary monitors to parse the v1.18 baseline artifact.
- Verify JS/TS remains the counted baseline and Python remains runtime-only/non-counted.

## Verification

- `pnpm exec tsx scripts/check-boundary-monitors.ts`
- `pnpm --filter @cowards/spec test -- --run`

