---
phase: 243-boundary-surface-inventory-and-contract-lock
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/evaluate-v1-35-boundary-surface-inventory.ts
  - scripts/evaluate-v1-35-boundary-surface-inventory.test.ts
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
must_haves:
  truths:
    - "Developer has a deterministic evaluator for the v1.35 boundary inventory before rows are populated."
    - "Evaluator rejects missing INV-01 surface groups, invalid dispositions, missing D-05 row fields, duplicate row IDs, invalid downstream phase assignments, invalid v1.35 requirement IDs, and rows without downstream requirement traceability."
    - "Evaluator rejects forbidden sandbox/package/TinyGo overclaim patterns and public/default private-marker leakage in inventory rows and rendered artifacts."
    - "Evaluator supports --check for committed markdown and JSON artifacts without requiring runtime-service, Go backend, Docker, browser, or external services."
  artifacts:
    - path: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      provides: "Inventory schema, required groups, markdown/JSON rendering, write/check CLI"
      exports:
        - "generateV135BoundarySurfaceInventory"
        - "validateV135BoundarySurfaceInventory"
        - "writeV135BoundarySurfaceInventoryArtifacts"
        - "checkV135BoundarySurfaceInventoryArtifacts"
    - path: "scripts/evaluate-v1-35-boundary-surface-inventory.test.ts"
      provides: "Focused Vitest coverage for evaluator invariants"
  key_links:
    - from: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      to: ".planning/artifacts/v1.35-boundary-surface-inventory.json"
      via: "artifactPaths.json"
      pattern: "v1\\.35-boundary-surface-inventory\\.json"
    - from: "scripts/evaluate-v1-35-boundary-surface-inventory.test.ts"
      to: "scripts/evaluate-v1-35-boundary-surface-inventory.ts"
      via: "direct imports"
      pattern: "validateV135BoundarySurfaceInventory"
---

<objective>
Create the Wave 0 evaluator and tests that make the Phase 243 inventory machine-checkable before downstream phases rely on it.

Purpose: Phase 243 is inventory/contract/characterization only, so the first deliverable must enforce the row taxonomy and required surface coverage without changing account, provider-proof, owner-debug, alias, sandbox, package, or runtime behavior.
Output: A deterministic TypeScript evaluator and Vitest test suite for the authoritative v1.35 boundary surface inventory.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-CONTEXT.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-RESEARCH.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-PATTERNS.md
@.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-VALIDATION.md
@scripts/generate-typescript-backend-inventory.ts
@scripts/generate-typescript-backend-inventory.test.ts

<interfaces>
Create these public evaluator contracts for downstream plans:

```typescript
export type V135BoundarySurfaceGroup =
  | "account-save"
  | "account-source-read"
  | "owner-debug-replay"
  | "workshop-alias"
  | "competition-entry"
  | "go-read-write"
  | "provider-proof"
  | "sandbox-claim"
  | "package-policy"
  | "tinygo-visibility"
  | "privacy-monitor"
  | "evidence-artifact"

export type V135BoundaryDisposition =
  | "fix-now"
  | "quarantine"
  | "deprecate-remove"
  | "document-only"
  | "future"

export type V135RequirementId =
  | "INV-01"
  | "INV-02"
  | "INV-03"
  | "ACCT-01"
  | "ACCT-02"
  | "ACCT-03"
  | "ACCT-04"
  | "ACCT-05"
  | "ENTRY-01"
  | "ENTRY-02"
  | "ENTRY-03"
  | "ENTRY-04"
  | "AUTH-01"
  | "AUTH-02"
  | "PRIV-01"
  | "PRIV-02"
  | "API-01"
  | "API-02"
  | "API-03"
  | "SBOX-01"
  | "SBOX-02"
  | "LABEL-01"
  | "LABEL-02"
  | "PKG-01"
  | "PKG-02"
  | "PKG-03"
  | "PKG-04"
  | "PROOF-01"
  | "PROOF-02"
  | "PROOF-03"
  | "PROOF-04"
  | "PROOF-05"

export interface V135BoundarySurfaceRow {
  id: string
  surfaceGroup: V135BoundarySurfaceGroup
  codeReferences: readonly string[]
  currentOwner: string
  intendedOwner: string
  trustBoundary: string
  dataClass: "public" | "session" | "owner-private" | "internal-private"
  affectedRequirements: readonly V135RequirementId[]
  currentBehavior: string
  disposition: V135BoundaryDisposition
  requiredTestsOrProof: readonly string[]
  privacyRisks: readonly string[]
  downstreamPhase: 244 | 245 | 246 | 247 | 248 | "none"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the v1.35 inventory evaluator contract</name>
  <files>scripts/evaluate-v1-35-boundary-surface-inventory.ts</files>
  <read_first>
    - scripts/generate-typescript-backend-inventory.ts
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-CONTEXT.md
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-PATTERNS.md
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-VALIDATION.md
  </read_first>
  <behavior>
    - Testable behavior 1: validation fails when any D-01 required group is absent: account-save, account-source-read, owner-debug-replay, workshop-alias, competition-entry, go-read-write, provider-proof, sandbox-claim, package-policy, tinygo-visibility, privacy-monitor.
    - Testable behavior 2: validation fails when a row uses any disposition outside D-04: fix-now, quarantine, deprecate-remove, document-only, future.
    - Testable behavior 3: validation fails when a row omits any D-05 field: currentOwner, intendedOwner, trustBoundary, dataClass, affectedRequirements, currentBehavior, disposition, requiredTestsOrProof, privacyRisks, downstreamPhase.
    - Testable behavior 4: validation allows every v1.35 requirement ID exactly as defined in REQUIREMENTS.md: INV-*, ACCT-*, ENTRY-*, AUTH-*, PRIV-*, API-*, SBOX-*, LABEL-*, PKG-*, and PROOF-*; validation rejects unknown IDs and rejects downstream-phase rows that only cite INV IDs.
    - Testable behavior 5: validation fails on contradictory overclaim patterns including production sandbox certification, TinyGo production support, rich package ecosystem/package support, host import support, TypeScript/Python WASM isolation, and default/public leakage of raw diagnostics, source, artifact bytes, host paths, env values, tokens, DB details, private runtime internals, StrategyMemory, SoldierMemory, or objective payloads.
    - Testable behavior 6: `--check` compares `.planning/artifacts/v1.35-boundary-surface-inventory.md` and `.planning/artifacts/v1.35-boundary-surface-inventory.json` to deterministic renders, verifies row IDs, dispositions, downstream phases, and affected requirements are synchronized between both artifacts, and exits nonzero on missing, stale, or desynchronized artifacts.
  </behavior>
  <action>
    Create `scripts/evaluate-v1-35-boundary-surface-inventory.ts` using the existing `generate-typescript-backend-inventory.ts` CLI style: shebang `#!/usr/bin/env -S pnpm exec tsx`, `repoRoot` derived from `import.meta.url`, deterministic `artifactPaths`, exported type unions, exported validation/render/write/check functions, and a CLI supporting no flag, `--write`, and `--check`.

    Use `schemaVersion = "v1.35-boundary-surface-inventory"` and `milestone = "v1.35"`. The evaluator must be static file/artifact logic only. It must not call runtime-service, Go backend, Docker, browser APIs, database, network, Strategy execution, or Node `vm`. Implement `requiredSurfaceGroups` exactly as the D-01 set plus optional `"evidence-artifact"` for proof/monitor rows. Implement `allowedDataClasses` exactly as `public`, `session`, `owner-private`, `internal-private`. Implement `allowedDownstreamPhases` exactly as `244`, `245`, `246`, `247`, `248`, and `"none"`. Implement `allowedRequirementIds` from REQUIREMENTS.md with every v1.35 ID: `INV-01` through `INV-03`, `ACCT-01` through `ACCT-05`, `ENTRY-01` through `ENTRY-04`, `AUTH-01` through `AUTH-02`, `PRIV-01` through `PRIV-02`, `API-01` through `API-03`, `SBOX-01` through `SBOX-02`, `LABEL-01` through `LABEL-02`, `PKG-01` through `PKG-04`, and `PROOF-01` through `PROOF-05`.

    Require every row to include affected downstream requirement IDs, not only the Phase 243 INV IDs. Rows with downstreamPhase `244` must include one or more `ACCT-*` or `ENTRY-*` IDs; downstreamPhase `245` must include one or more `AUTH-*`, `PRIV-*`, or `API-*` IDs; downstreamPhase `246` must include one or more `SBOX-*` or `LABEL-*` IDs; downstreamPhase `247` must include one or more `PKG-*` IDs; downstreamPhase `248` must include one or more `PROOF-*` IDs. Rows with downstreamPhase `"none"` must still include the INV IDs they satisfy and any downstream IDs they document.

    Add explicit forbidden-pattern validators for public/default rows and claim-calibration rows. These validators must fail on wording that claims or implies current production sandbox certification, TypeScript/Python WASM isolation, TinyGo production support, rich package ecosystem support, host import support, package mode other than `none` as production-supported, or exposure of raw diagnostics/source/artifact bytes/host paths/env values/tokens/DB details/private runtime internals/StrategyMemory/SoldierMemory/objective payloads in public/default outputs. Positive allowed wording must remain the calibrated claims: TypeScript/Python provenance-only, Rust/Zig immutable WASM/WASI Preview 1 artifact-backed, TinyGo spike-only/hidden, no current lane production sandbox certified, package mode `none`.

    Export `generateV135BoundarySurfaceInventory`, `validateV135BoundarySurfaceInventory`, `renderV135BoundarySurfaceInventoryMarkdown`, `renderV135BoundarySurfaceInventoryJson`, `writeV135BoundarySurfaceInventoryArtifacts`, and `checkV135BoundarySurfaceInventoryArtifacts`. The generated inventory can start from an in-file static row array that Plan 02 will populate; this plan must establish the contract and validation rules, not behavioral fixes.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-35-boundary-surface-inventory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'schemaVersion = "v1.35-boundary-surface-inventory"' scripts/evaluate-v1-35-boundary-surface-inventory.ts` returns a matching line.
    - `rg -n 'account-save|account-source-read|owner-debug-replay|workshop-alias|competition-entry|go-read-write|provider-proof|sandbox-claim|package-policy|tinygo-visibility|privacy-monitor' scripts/evaluate-v1-35-boundary-surface-inventory.ts` returns every listed group.
    - `rg -n 'fix-now|quarantine|deprecate-remove|document-only|future' scripts/evaluate-v1-35-boundary-surface-inventory.ts` returns every allowed disposition.
    - `rg -n 'ACCT-01|ENTRY-01|AUTH-01|PRIV-01|API-01|SBOX-01|LABEL-01|PKG-01|PROOF-01|allowedRequirementIds|downstreamPhase' scripts/evaluate-v1-35-boundary-surface-inventory.ts` returns downstream requirement traceability logic.
    - `rg -n 'production sandbox certification|TypeScript/Python WASM isolation|TinyGo production support|package ecosystem|host import|raw diagnostics|StrategyMemory|SoldierMemory|objective payload' scripts/evaluate-v1-35-boundary-surface-inventory.ts scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` returns forbidden overclaim and privacy leakage validators/tests.
    - `node -e "const s=require('fs').readFileSync('scripts/evaluate-v1-35-boundary-surface-inventory.ts','utf8'); if(/\\bfetch\\s*\\(/.test(s)||/\\bexecSync\\s*\\(/.test(s)||/Date\\.now\\s*\\(/.test(s)||/Math\\.random\\s*\\(/.test(s)||/from ['\\\"]node:(vm|child_process)['\\\"]/.test(s)||/new WebSocket\\s*\\(/.test(s)) process.exit(1)"` exits 0.
  </acceptance_criteria>
  <done>The evaluator defines the Phase 243 row contract, renders markdown and JSON deterministically, and check mode can fail on missing/stale inventory artifacts without requiring live services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused evaluator tests before inventory population</name>
  <files>scripts/evaluate-v1-35-boundary-surface-inventory.test.ts</files>
  <read_first>
    - scripts/generate-typescript-backend-inventory.test.ts
    - scripts/evaluate-v1-35-boundary-surface-inventory.ts
    - .planning/phases/243-boundary-surface-inventory-and-contract-lock/243-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: a fixture missing one required surface group returns a validation error naming that group.
    - Test 2: a fixture with invalid disposition `defer-later` returns a validation error naming the row ID and invalid disposition.
    - Test 3: a fixture row with empty owner, trust boundary, required tests/proof, or privacy risks returns D-05 field errors.
    - Test 4: unknown requirement IDs are rejected, all v1.35 requirement IDs from REQUIREMENTS.md are accepted, and downstream-phase rows that cite only INV IDs are rejected.
    - Test 5: contradictory overclaim fixtures are rejected for production sandbox certification, TypeScript/Python WASM isolation, TinyGo production support, package ecosystem/rich-package support, host import support, and package mode other than `none` as production-supported.
    - Test 6: public/default leakage fixtures are rejected for raw diagnostics, source, artifact bytes, host paths, env values, package paths, tokens, DB details, private runtime internals, StrategyMemory, SoldierMemory, and objective payloads.
    - Test 7: duplicate row IDs and duplicate code references for the same row family are rejected where they make the inventory ambiguous.
    - Test 8: `checkV135BoundarySurfaceInventoryArtifacts` reports missing artifacts, then returns no failures after `writeV135BoundarySurfaceInventoryArtifacts`, then reports stale markdown or JSON/markdown row-sync drift after modifying the artifact files.
  </behavior>
  <action>
    Create `scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` using the temp-repo pattern from `generate-typescript-backend-inventory.test.ts`. Import the evaluator exports from `./evaluate-v1-35-boundary-surface-inventory.ts`.

    Tests must be deterministic and must not depend on the real repo artifact contents except for the final happy-path check against evaluator-generated output. Use fixture rows that explicitly cover D-01, D-04, D-05, D-09, D-10, D-11, every v1.35 requirement family, forbidden overclaim patterns, and public/default private-marker leakage. Include test names that mention `INV-01`, `INV-02`, and `INV-03` so requirement coverage is grep-verifiable.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/evaluate-v1-35-boundary-surface-inventory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'INV-01|INV-02|INV-03' scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` returns all three IDs.
    - `rg -n 'missing.*account-save|invalid disposition|requiredTestsOrProof|privacyRisks|stale' scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` returns coverage for missing group, invalid disposition, required tests/proof, privacy risk, and stale artifact checks.
    - `rg -n 'ACCT-01|ENTRY-01|AUTH-01|PRIV-01|API-01|SBOX-01|LABEL-01|PKG-01|PROOF-01|only INV|unknown requirement' scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` returns requirement-family and downstream traceability tests.
    - `rg -n 'production sandbox certification|TypeScript/Python WASM isolation|TinyGo production support|package ecosystem|rich-package|host import|raw diagnostics|artifact bytes|host paths|env values|tokens|DB details|private runtime internals|StrategyMemory|SoldierMemory|objective payload' scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` returns forbidden overclaim and public leakage tests.
    - `pnpm exec vitest run scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` exits 0.
  </acceptance_criteria>
  <done>Focused tests prove the evaluator enforces required groups, row fields, dispositions, privacy-risk metadata, duplicate guards, and stale artifact checks before Plan 02 populates the inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact -> downstream implementation | Inventory rows become instructions for Phases 244-248; overclaiming or omissions can cause later behavior fixes to target the wrong surfaces. |
| repo source -> generated artifacts | Evaluator reads static source/artifact metadata and writes planning evidence; it must not execute Strategy code or rely on live services. |
| public/private vocabulary -> inventory rows | Data-class and privacy-risk fields determine which surfaces need redaction proof later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-01 | Tampering | `validateV135BoundarySurfaceInventory` | mitigate | Reject missing required D-01 surface groups, invalid dispositions, duplicate row IDs, and missing D-05 fields. |
| T-243-02 | Information Disclosure | markdown/JSON renderers | mitigate | Require each public/default row to carry privacy risks and required tests/proof; do not embed Strategy source, artifact bytes, tokens, DB details, host paths, env values, or private runtime internals in fixture rows. |
| T-243-03 | Repudiation | inventory check mode | mitigate | `--check` must fail on missing or stale markdown/JSON so downstream phases can prove they used the locked contract. |
| T-243-04 | Elevation of Privilege | evaluator implementation | mitigate | Keep evaluator static; forbid runtime-service, Go backend, network, database, Node `vm`, or Strategy execution calls in this plan. |
</threat_model>

<verification>
Run:

```bash
pnpm exec vitest run scripts/evaluate-v1-35-boundary-surface-inventory.test.ts
```
</verification>

<success_criteria>
- `scripts/evaluate-v1-35-boundary-surface-inventory.ts` exists and exports the schema, generator, validator, renderer, write, and check functions.
- `scripts/evaluate-v1-35-boundary-surface-inventory.test.ts` exists and passes.
- No Phase 244-248 behavior files are modified by this plan.
- INV-01, INV-02, and INV-03 are represented in evaluator validation.
</success_criteria>

<output>
After completion, create `.planning/phases/243-boundary-surface-inventory-and-contract-lock/243-01-SUMMARY.md`
</output>
