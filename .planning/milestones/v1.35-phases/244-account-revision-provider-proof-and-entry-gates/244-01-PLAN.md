---
phase: 244-account-revision-provider-proof-and-entry-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/go-backend/runtime_service_client.go
  - apps/go-backend/runtime_service_client_test.go
  - apps/runtime-service/src/server.test.ts
autonomous: true
requirements:
  - ACCT-01
  - ACCT-04
  - ACCT-05
requirements_addressed:
  - ACCT-01
  - ACCT-04
  - ACCT-05
must_haves:
  truths:
    - "Go can request TypeScript validation from runtime-service through the same provider boundary as Python, Rust, and Zig."
    - "Malformed, oversized, mismatched, unavailable, and incomplete TypeScript validation responses fail closed before account-save readiness can be claimed."
    - "Runtime-service validation failure details exposed through Go client tests remain public-safe and omit private runtime/source/proof details."
  artifacts:
    - path: "apps/go-backend/runtime_service_client.go"
      provides: "TypeScript-capable runtime-service validation client with fail-closed response validation"
    - path: "apps/go-backend/runtime_service_client_test.go"
      provides: "Go client regression coverage for TypeScript validation success and failure states"
    - path: "apps/runtime-service/src/server.test.ts"
      provides: "Runtime-service TypeScript provider-validation contract coverage"
  key_links:
    - from: "apps/go-backend/runtime_service_client.go"
      to: "apps/runtime-service/src/server.ts /validate-strategy"
      via: "POST sourceFormat=typescript"
      pattern: "sourceFormat.*typescript"
    - from: "apps/go-backend/runtime_service_client_test.go"
      to: "apps/go-backend/runtime_service_client.go"
      via: "httptest /validate-strategy assertions"
      pattern: "TestRuntimeServiceClient.*TypeScript"
---

<objective>
Allow Go to consume TypeScript provider validation from runtime-service without adding any Go-side Strategy execution or local TypeScript readiness shortcut.

Purpose: Phase 244 cannot make account saves honest until the Go runtime-service client stops rejecting TypeScript before transport and proves TypeScript responses are schema-, source-identity-, and privacy-safe.
Output: Failing-then-passing Go/runtime-service tests and a hardened Go validation client for TypeScript provider proof requests.
</objective>

<execution_context>
@/Users/roryquinlan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/roryquinlan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-CONTEXT.md
@.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-RESEARCH.md
@.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-PATTERNS.md
@.planning/artifacts/v1.35-boundary-surface-inventory.md
@apps/go-backend/runtime_service_client.go
@apps/go-backend/runtime_service_client_test.go
@apps/runtime-service/src/server.ts
@apps/runtime-service/src/server.test.ts

<interfaces>
Existing contracts to preserve:

```go
type runtimeServiceValidationResponse struct {
	OK                  bool           `json:"ok"`
	Kind                string         `json:"kind"`
	SourceFormat        string         `json:"sourceFormat"`
	Runtime             map[string]any `json:"runtime,omitempty"`
	Validation          map[string]any `json:"validation,omitempty"`
	EngineCompatibility map[string]any `json:"engineCompatibility,omitempty"`
	Metadata            map[string]any `json:"metadata,omitempty"`
	SourceHash          string         `json:"sourceHash,omitempty"`
	SourceBytes         int            `json:"sourceBytes,omitempty"`
	Error               string         `json:"error,omitempty"`
}

func (client *runtimeServiceClient) validateStrategy(ctx context.Context, sourceFormat string, source string, strategyID string) (*runtimeServiceValidationResponse, *runtimeServiceFailure)
func hashStrategySourceForGo(source string) string
```

Runtime-service already accepts `sourceFormat` values `typescript`, `python`, `rust`, and `zig` at `/validate-strategy`; Go must consume that boundary instead of locally validating TypeScript.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize TypeScript provider validation across runtime-service and Go client</name>
  <files>apps/go-backend/runtime_service_client_test.go, apps/runtime-service/src/server.test.ts</files>
  <behavior>
    - Runtime-service `/validate-strategy` returns a `strategyValidation` response for valid TypeScript with provider metadata, source hash/bytes, engine compatibility, `sourceArtifact`, and `providerValidation`.
    - Go client posts `sourceFormat: "typescript"` to `/validate-strategy` and accepts a complete matching success response.
    - Go client rejects TypeScript success responses with wrong source format, missing runtime/validation/metadata/source identity, mismatched source hash, mismatched source bytes, malformed JSON, oversized payload, transport error, or stopped endpoint.
    - Runtime-service and Go client tests assert responses do not expose raw diagnostics, Strategy source, artifact bytes/base64, host paths, env values, package paths, tokens, DB details, provider signing material, private runtime internals, StrategyMemory, SoldierMemory, or objective payloads by default.
  </behavior>
  <action>
    Add failing characterization tests before implementation. In `apps/runtime-service/src/server.test.ts`, add the missing TypeScript `/validate-strategy` provider-proof case using the existing Python/Rust/Zig style and assert the public response shape, not private artifact payloads. In `apps/go-backend/runtime_service_client_test.go`, add TypeScript success, source mismatch, incomplete success, and unsupported-format tests around `validateStrategy`. Reference D-01, D-02, D-04, D-09, and D-10 in test names or comments so the locked decisions are traceable.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-service test -- server.test.ts && cd apps/go-backend && go test ./... -run 'TestRuntimeServiceClient'</automated>
  </verify>
  <done>Tests fail before the Go client accepts TypeScript and pass only when TypeScript provider validation uses runtime-service with fail-closed, public-safe response handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enable TypeScript in the Go runtime-service validation client</name>
  <files>apps/go-backend/runtime_service_client.go</files>
  <behavior>
    - `validateStrategy` accepts exactly `typescript`, `python`, `rust`, and `zig`; any other source format still returns a client-side contract mismatch.
    - A TypeScript validation request uses the same HTTP path, bounded response read, JSON decoding, `kind == "strategyValidation"` check, `SourceFormat` match check, required success fields check, and source hash/bytes identity check as other provider languages.
    - Runtime-service unavailable, stopped, malformed, oversized, mismatched, incomplete, or unverifiable responses never produce a success object that account save can treat as execution-ready.
  </behavior>
  <action>
    Update `apps/go-backend/runtime_service_client.go` narrowly: change the source-format allowlist and stale error text so TypeScript is accepted alongside Python, Rust, and Zig per D-01. Keep the existing request/response path and response byte cap. Do not add TypeScript parsing, transpilation, local execution, Node `vm`, filesystem, network, database, package installation, or any Strategy execution inside Go. Preserve sanitized `runtimeServiceFailure` behavior and do not include raw provider proof, source, artifact bytes, host paths, env values, package paths, tokens, DB details, provider signing material, or private runtime internals in public failure messages per D-09 and D-10.
  </action>
  <verify>
    <automated>pnpm --filter @cowards/runtime-service test -- server.test.ts && cd apps/go-backend && go test ./... -run 'TestRuntimeServiceClient'</automated>
  </verify>
  <done>Go runtime-service validation accepts TypeScript provider validation and all existing Python/Rust/Zig client behavior remains intact.</done>
</task>

</tasks>

<source_coverage_audit>
GOAL: Phase 244 goal is partially covered here by making TypeScript provider validation reachable from Go.
REQ: Covers ACCT-01, ACCT-04, ACCT-05. ENTRY requirements are intentionally covered in later Phase 244 plans.
RESEARCH: Implements the "Runtime-Service TypeScript Validation Parity" pattern and avoids the "Go-local Strategy validation as readiness" anti-pattern.
CONTEXT: Implements D-01, D-02, D-04, D-09, D-10. Does not implement deferred Phase 245/246/247/248 ideas.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Go backend -> runtime-service | Go sends hostile Strategy source to the dedicated provider-validation service and receives a JSON proof envelope. |
| runtime-service -> Go public failure mapping | Service failures are normalized before account-save or entry layers can expose them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-244-01-01 | Elevation of privilege | `runtime_service_client.go` | mitigate | Keep TypeScript validation behind runtime-service; do not add Go/web/API Strategy execution, local transpilation, or Node `vm`. |
| T-244-01-02 | Tampering | `validateStrategy` response handling | mitigate | Require `kind`, `sourceFormat`, source hash/bytes, runtime, validation, engine compatibility, and metadata before success is returned. |
| T-244-01-03 | Information disclosure | runtime-service failure messages | mitigate | Tests assert public-safe failure categories and deny forbidden private markers from D-10. |
| T-244-01-04 | Denial of service | runtime-service response body | mitigate | Preserve bounded response reads and oversized-response failure behavior. |
</threat_model>

<verification>
Run:

```bash
pnpm --filter @cowards/runtime-service test -- server.test.ts
cd apps/go-backend && go test ./... -run 'TestRuntimeServiceClient'
```

No PostgreSQL service is required for this plan.
</verification>

<success_criteria>
- TypeScript is no longer rejected client-side by Go runtime-service validation.
- TypeScript success requires complete provider-validation response identity.
- Failure messages remain public-safe.
- No Strategy execution, package expansion, sandbox certification claim, or TinyGo production surface is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/244-account-revision-provider-proof-and-entry-gates/244-01-SUMMARY.md`.
</output>
