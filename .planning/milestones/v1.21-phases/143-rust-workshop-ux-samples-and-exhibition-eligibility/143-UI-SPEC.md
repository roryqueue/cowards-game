# Phase 143 UI Spec

Rust UI must appear inside the existing Workshop workflow, not as a new landing page. Controls remain compact: the language selector adds a Rust alpha segment, templates list Rust with a Rust alpha chip, and validation diagnostics remain source-safe. No ranked/counting language is used for Rust labels.

