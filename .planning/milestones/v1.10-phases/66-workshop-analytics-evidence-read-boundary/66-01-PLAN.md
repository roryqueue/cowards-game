# Phase 66 Plan: Workshop Analytics Evidence Read Boundary

**Status:** Complete  
**Requirements:** ANALYTICS-01, ANALYTICS-02, ANALYTICS-03, ANALYTICS-04, ANALYTICS-05

## Plan

1. Move `WorkshopAnalyticsSnapshot` ownership to `@cowards/spec` and add a schema that composes existing analytics profile/run schemas and privacy checks.
2. Add `getWorkshopAnalyticsSnapshot()` to `@cowards/service`, validating the raw persistence snapshot before and after schema parsing.
3. Add web service adapter/boundary modules for Workshop analytics reads.
4. Point `apps/web/app/workshop/evidence/page.tsx` at the service boundary and narrow `EvidenceExplorerClient` props to analytics-only data.
5. Replace persistence-backed web analytics state test fixtures with a spec-only local fixture.
6. Promote the Evidence page/read boundary to strict import enforcement and remove migrated report-only fingerprints.
7. Validate service tests, web tests, typecheck, boundary imports, boundary monitors, and topology checks.

## Acceptance Evidence

- Evidence Explorer read data comes from `@cowards/service`.
- Analytics DTOs are parsed through spec schemas and privacy guards.
- Workshop source/save/test/runtime/rerun/export paths are unchanged.
- Strict import enforcement covers the migrated read slice.
- Report-only broad web persistence debt drops below the Phase 65 count.

