---
plan_id: 65-01
phase: 65-account-revision-list-read-boundary
status: complete
type: implementation
files_modified:
  - apps/web/app/api/account/revisions/route.ts
  - apps/web/app/api/account/revisions/save/route.ts
  - apps/web/lib/account-revision-write-boundary.ts
  - apps/web/app/workshop/workshop-client.tsx
  - scripts/check-service-boundary-imports.ts
  - scripts/check-boundary-monitors.ts
---

# Phase 65 Plan 01: Disentangle Account Revision List GET

## Objective

Make the account Strategy Revision list route a strict service-backed read boundary without migrating account save/source/fork/write behavior.

## Tasks

1. Remove `competitive/server` from `apps/web/app/api/account/revisions/route.ts`.
2. Use `getCurrentAccountReadUser()` and `listAccountReadRevisions()` for the GET route.
3. Move account save POST behavior to `/api/account/revisions/save`.
4. Update Workshop account-save fetch to call `/api/account/revisions/save`.
5. Add `apps/web/app/api/account/revisions/route.ts` to strict migrated import enforcement.
6. Remove the old mixed-route report-only baseline fingerprint.
7. Run focused boundary and typecheck validation.

## Acceptance Criteria

- `/api/account/revisions` is strict-clean and read-only.
- Account revision list still returns owner-safe metadata without source.
- Workshop account save still has a route and remains a deferred write path.
- `pnpm boundary:imports` reports `strict_offenses=0 report_only_offenses=33`.
- Boundary monitors pass with the new baseline.

