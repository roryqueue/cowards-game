# Phase 67 Plan: Public Strategy Go Read-Model Route

**Status:** Complete  
**Requirements:** GO-01, GO-02, GO-03, GO-04, GO-05, GO-06, GO-07

## Plan

1. Generate a public Strategy page fixture by invoking `@cowards/service.getPublicStrategyPage()` with a controlled parity Strategy card.
2. Add `public-strategy-page.json` to the generated fixture set, fixture manifest, and embedded Go checksum reference.
3. Add `GET /public/strategies/{strategyId}` to the Go route inventory and manifest.
4. Serve the fixture from Go by decoded Strategy id and return the public not-found fixture for misses.
5. Add strict Go fixture shape validation for public Strategy page DTOs.
6. Extend Go tests for route parity, missing Strategy behavior, mutation verb rejection, and private-field fixture rejection.
7. Update topology and boundary monitors to expect exactly five read-only Go routes.

## Acceptance Evidence

- `pnpm go:parity` passes.
- Topology reports five read-only Go routes.
- Boundary monitors report five Go route manifest entries.
- Public Strategy page fixture contains public metadata, source hash/bytes, runtime labels, compatibility, record, and links, but no Strategy source or private runtime details.

