# Phase 23 UI Spec

## Contract

- Result pages show counted status, public reason, and public explanation.
- Result flag endpoint accepts signed-in entrant/admin dispute notes.
- Admin governance endpoint is not exposed as public player UI.
- Public moderation language must not reveal private review data.

