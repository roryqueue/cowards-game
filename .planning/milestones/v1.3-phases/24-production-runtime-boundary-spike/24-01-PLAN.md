# Phase 24 Plan: Production Runtime Boundary Spike

## Scope

1. Add adapter metadata for production-readiness and runtime controls.
2. Add `container-subprocess` adapter using Docker with no network, read-only root, tmpfs, memory, CPU, pids, dropped caps, and no shell.
3. Keep worker-thread as local/dev fallback.
4. Harden subprocess harness against Function constructor import-time escapes.
5. Add regression tests for timeout, ENOBUFS, malformed IPC, unsafe Docker image references, and system-failure classification.

