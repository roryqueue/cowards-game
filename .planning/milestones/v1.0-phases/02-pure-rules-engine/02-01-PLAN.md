---
phase: 2
plan: 02-01
title: Spec Amendment and Engine Foundation
type: implementation
wave: 1
depends_on: []
autonomous: true
requirements:
  - ENG-01
  - ENG-15
  - ENG-21
  - TEST-01
  - TEST-02
files_modified:
  - /Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md
  - .planning/spec-amendments/02-backstab-rule.md
  - packages/engine/package.json
  - packages/engine/src/index.ts
  - packages/engine/src/types.ts
  - packages/engine/src/state.ts
  - packages/engine/src/selectors.ts
  - packages/engine/src/state.test.ts
  - packages/test-utils/src/index.ts
  - packages/test-utils/src/engine-scenarios.ts
---

# Plan 02-01: Spec Amendment and Engine Foundation

<objective>
Update the canonical Backstab rule source, then establish the pure engine state model, selector foundation, initial state creation, and spec-only scenario helpers that later rule plans build on.
</objective>

<requirements_addressed>
ENG-01, ENG-15, ENG-21, TEST-01, TEST-02
</requirements_addressed>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-02-01: Implementing tests against a rule not present in the canonical spec | high | Update `/Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md` and add `.planning/spec-amendments/02-backstab-rule.md` before engine code asserts the clarified Backstab behavior. |
| T-02-02: Engine foundation accidentally introduces side effects | high | Keep foundation modules synchronous and pure; add purity grep to verification and tests for deterministic initial state. |
| T-02-03: Derived state becomes stale | medium | Store only canonical `GameState`; expose selectors for occupancy, active counts, snapshots, and behind-square calculations. |
</threat_model>

<tasks>
<task id="02-01-01" type="execute">
  <title>Record the Backstab rule clarification in canonical specs</title>
  <read_first>
    - .planning/phases/02-pure-rules-engine/02-CONTEXT.md
    - .planning/phases/02-pure-rules-engine/02-RESEARCH.md
    - /Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md
  </read_first>
  <action>
    Update `/Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md` section `## 23. Backstab` so it states:
    - Backstab is position-triggered, not only voluntary-advance-triggered.
    - At the beginning and end of every Soldier Activation, check all ACTIVE Soldiers on the board.
    - If any ACTIVE Soldier is directly behind an enemy ACTIVE Soldier, that enemy becomes STONE.
    - Resolve all Backstabs at the same activation boundary from a simultaneous snapshot, then turn all victims to STONE together.
    - Mutual Backstab cases stone both Soldiers.
    - Pushed Soldiers can trigger Backstab by ending behind an enemy.
    - Pushes do not update `lastSuccessfulMoveDirection`.
    - After Backstab boundary resolution, immediately check match-end conditions.

    Also add `.planning/spec-amendments/02-backstab-rule.md` with the same rule text and note that it supersedes the original "after each successful Advance" wording for Phase 2 planning and execution.
  </action>
  <verify>
    rg "position-triggered" /Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md
    rg "simultaneous snapshot" /Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md
    rg "Pushed Soldiers can trigger Backstab" .planning/spec-amendments/02-backstab-rule.md
  </verify>
  <acceptance_criteria>
    - `/Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md` contains `Backstab is position-triggered`
    - `/Users/roryquinlan/Downloads/CowardsGameSpec_Full_Consolidated_v1.md` contains `simultaneous snapshot`
    - `.planning/spec-amendments/02-backstab-rule.md` contains `Pushed Soldiers can trigger Backstab`
  </acceptance_criteria>
</task>

<task id="02-01-02" type="execute">
  <title>Create engine state and transition types</title>
  <read_first>
    - packages/engine/src/index.ts
    - packages/engine/package.json
    - packages/spec/src/types.ts
    - packages/spec/src/constants.ts
    - packages/spec/src/versions.ts
    - .planning/phases/02-pure-rules-engine/02-CONTEXT.md
  </read_first>
  <action>
    Replace the skeletal engine export with `packages/engine/src/types.ts` and curated exports from `packages/engine/src/index.ts`.

    Define:
    - `export type PlayerSide = "bottom" | "top"`
    - `export interface EnginePlayer { id: PlayerId; side: PlayerSide; strategyRevisionId: StrategyRevisionId; strategyMemory: StrategyMemory }`
    - `export type MatchPhase = "ROUND" | "CONTRACTION" | "COMPLETE"`
    - `export interface GameState` containing `matchId`, `seed`, `versions`, `arenaVariant`, `players`, `phaseNumber`, `roundNumber`, `activationCount`, `initiativePlayerId`, `bounds`, `soldiers`, `terrainStones`, `outcome`
    - `export type TransitionEventSummary` with event `type`, `sequence`, and `payload`
    - `export interface TransitionResult<TState = GameState> { state: TState; events: TransitionEventSummary[] }`
    - `export type RuntimeResult<T> = { ok: true; value: T } | { ok: false; violation: RuntimeViolation }`
    - `export interface StrategyRuntime { selectActivations(input: StrategyInput): RuntimeResult<StrategyResult>; runSoldierBrain(input: SoldierBrainInput): RuntimeResult<SoldierBrainResult> }`
    - terminal/interruption reason unions for blocked, invalid, fallen, stoned, runtime violation, and cycle exhaustion.

    Keep all imports type-only where possible and import contracts from `@cowards/spec`.
  </action>
  <verify>
    pnpm --filter @cowards/engine typecheck
    rg "interface GameState" packages/engine/src/types.ts
    rg "interface StrategyRuntime" packages/engine/src/types.ts
    rg "RuntimeResult" packages/engine/src/types.ts
  </verify>
  <acceptance_criteria>
    - `packages/engine/src/types.ts` contains `export interface GameState`
    - `packages/engine/src/types.ts` contains `export interface StrategyRuntime`
    - `packages/engine/src/types.ts` contains `selectActivations(input: StrategyInput)`
    - `packages/engine/src/types.ts` contains `runSoldierBrain(input: SoldierBrainInput)`
    - `pnpm --filter @cowards/engine typecheck` exits 0
  </acceptance_criteria>
</task>

<task id="02-01-03" type="execute">
  <title>Implement initial state creation and selectors</title>
  <read_first>
    - packages/engine/src/types.ts
    - packages/spec/src/constants.ts
    - packages/spec/src/fixtures/valid.ts
    - packages/spec/src/schemas.ts
    - packages/spec/src/versions.ts
  </read_first>
  <action>
    Add `packages/engine/src/state.ts` with `createInitialGameState(input)` that accepts `matchId`, `seed`, `arenaVariant`, bottom/top player IDs, and bottom/top StrategyRevision IDs. It must:
    - parse `arenaVariant` with `ArenaVariantSchema`
    - create 16 ACTIVE Soldiers from `BOTTOM_STARTING_POSITIONS` and `TOP_STARTING_POSITIONS`
    - assign bottom Soldiers to the provided bottom player ID facing `UP`
    - assign top Soldiers to the provided top player ID facing `DOWN`
    - set `phaseNumber: 1`, `roundNumber: 1`, `activationCount: 1`, `phase: "ROUND"`
    - set versions from `COMPATIBILITY_VERSIONS`
    - set both players' `strategyMemory` to `{}`
    - determine initial initiative deterministically from the seed without `Math.random`

    Add `packages/engine/src/selectors.ts` with pure helpers:
    - `positionKey(position): string`
    - `isWithinBounds(position, bounds): boolean`
    - `getOccupyingSoldier(state, position): Soldier | undefined`
    - `getTerrainStoneAt(state, position): Position | undefined`
    - `getActiveSoldiers(state, playerId?): Soldier[]`
    - `getSoldierSnapshot(soldier): SoldierSnapshot`
    - `getFullBoardSnapshot(state): FullBoardSnapshot`
    - `getBehindSquare(soldier): Position | null`
    - `countActiveByPlayer(state): Map<PlayerId, number>`
  </action>
  <verify>
    pnpm --filter @cowards/engine typecheck
    pnpm --filter @cowards/engine test -- state.test.ts
  </verify>
  <acceptance_criteria>
    - `packages/engine/src/state.ts` contains `export const createInitialGameState`
    - `packages/engine/src/selectors.ts` contains `export const getBehindSquare`
    - `packages/engine/src/selectors.ts` contains `export const getFullBoardSnapshot`
    - `packages/engine/src/state.ts` does not contain `Math.random`
    - `pnpm --filter @cowards/engine test -- state.test.ts` exits 0
  </acceptance_criteria>
</task>

<task id="02-01-04" type="test">
  <title>Add foundation tests and spec-only engine scenario builders</title>
  <read_first>
    - packages/engine/src/state.ts
    - packages/engine/src/selectors.ts
    - packages/spec/src/fixtures/valid.ts
    - packages/test-utils/src/index.ts
  </read_first>
  <action>
    Add `packages/engine/src/state.test.ts` covering:
    - initial state has bounds `{ minX: 0, maxX: 11, minY: 0, maxY: 11 }`
    - exactly 16 Soldiers are created
    - bottom side Soldiers face `UP`; top side Soldiers face `DOWN`
    - bottom/top player IDs are not hardcoded to `"bottom"` or `"top"`
    - FALLEN Soldiers remain in the roster and do not occupy squares when passed through selectors
    - repeated `createInitialGameState` calls with identical input produce deeply equal output

    Add `packages/test-utils/src/engine-scenarios.ts` with spec-only helpers:
    - `createScenarioSoldier(overrides)`
    - `createScenarioStateParts(overrides)`
    - `createEmptyBoardScenario()`

    Export these helpers from `packages/test-utils/src/index.ts`. Do not import `@cowards/engine` from `packages/test-utils` in this plan.
  </action>
  <verify>
    pnpm --filter @cowards/engine test -- state.test.ts
    pnpm --filter @cowards/test-utils typecheck
    pnpm verify
  </verify>
  <acceptance_criteria>
    - `packages/engine/src/state.test.ts` contains `toHaveLength(16)`
    - `packages/engine/src/state.test.ts` contains `facing === "UP"`
    - `packages/engine/src/state.test.ts` contains `facing === "DOWN"`
    - `packages/test-utils/src/engine-scenarios.ts` contains `export const createScenarioSoldier`
    - `packages/test-utils/src/index.ts` exports from `./engine-scenarios.js`
    - `pnpm verify` exits 0
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:

```bash
pnpm --filter @cowards/engine test -- state.test.ts
pnpm --filter @cowards/test-utils typecheck
pnpm verify
! rg "Math\\.random|Date\\.now|new Date\\(|fetch\\(|from ['\\\"]fs|from ['\\\"]node:fs|process\\.env|postgres|redis" packages/engine/src --glob '!*.test.ts' --glob '!test/**'
```
</verification>

<must_haves>
- D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08 are implemented or explicitly represented in the state/types/selectors foundation.
- D-25, D-26, D-27, D-28, D-29, D-30, D-31, D-32, D-33, and D-34 are recorded in source-of-truth spec material before Backstab implementation begins.
- `@cowards/engine` imports from `@cowards/spec`, never from `@cowards/test-utils`.
- No engine file uses `Math.random`, clock APIs, filesystem, network, database, or Redis.
</must_haves>

<success_criteria>
- Initial state creation satisfies ENG-01.
- Engine foundation has explicit pure state, runtime, and transition types.
- Backstab clarification is written to the canonical source spec and project-local amendment.
- Foundation tests run through `pnpm verify`.
</success_criteria>
