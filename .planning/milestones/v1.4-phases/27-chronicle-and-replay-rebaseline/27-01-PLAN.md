# Phase 27 Plan 01: Rebaseline Chronicle and Replay for v1.4

## Objective

Make Chronicle generation, validation, replay projection, and active replay
fixtures consume the v1.4 Cycle-interleaved engine contract.

## Tasks

1. Add v1.4 slot/Cycle lifecycle event schema and type coverage.
2. Emit `chronicle-v1.4` artifacts from the replay builder.
3. Replace the replay builder's duplicated scheduler with engine `resolveRound`
   summaries.
4. Update grammar and validation for explicit selected-slot/Cycle events and
   default rejection of old Chronicle versions.
5. Treat new lifecycle events as replay no-ops while preserving board-changing
   transition validation.
6. Rebaseline active replay tests, persistence fixtures, test-utils fixtures,
   and web replay labels.

## Verification

- `pnpm --filter @cowards/spec test -- --run`
- `pnpm --filter @cowards/replay test -- --run`
- `pnpm --filter @cowards/test-utils test -- --run`
- `pnpm --filter @cowards/persistence test -- chronicle-store.test.ts --run`
- `pnpm --filter @cowards/web test -- --run`
- `pnpm test:fast`

