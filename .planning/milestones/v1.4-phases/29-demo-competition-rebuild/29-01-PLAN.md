# Phase 29 Plan 01: Rebuild v1.4 Demo Tournament

## Objective

Replace the v1.3 demo tournament path with regenerated v1.4 public evidence
using eight high-probability starter entrants and local browser verification.

## Tasks

1. Rename the demo script to `run-v1-4-demo-tournament.ts`.
2. Clean both old `v13-demo` and current `v1-4-demo` demo rows before seeding.
3. Seed eight strong v1.4 starter entrants with current rule/source metadata.
4. Schedule, execute, complete, and validate the trial ladder.
5. Assert v1.4 Chronicle schema/rule versions, interleaved Cycle signals,
   movement, blocked movement, Backstab, contraction, and replay samples.
6. Inspect the ladder, MatchSet, replay, Strategy card, and profile pages
   locally.

## Verification

- `pnpm services:up`
- `STRATEGY_EXECUTION_ADAPTER=worker-thread pnpm exec tsx scripts/run-v1-4-demo-tournament.ts`
- `pnpm test:fast`
- Browser verification against `http://localhost:3000`

