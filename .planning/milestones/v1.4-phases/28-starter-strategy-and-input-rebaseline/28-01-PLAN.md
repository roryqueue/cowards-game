# Phase 28 Plan 01: Rebaseline Starter Strategies for v1.4

## Objective

Refresh the Starter Strategy Library so stable starter IDs now represent v1.4
Cycle-interleaved doctrine sources and provenance.

## Tasks

1. Bump `StarterStrategyDefinition.version` and all starter definitions to
   `v1.4`.
2. Replace the old Cycle 4 stabilization heuristic with a rule tied to
   `input.maxCycles`.
3. Extend Workshop tests to assert all starters are v1.4, valid, unique, and
   still separated from generic samples.
4. Add an interleaved starter gauntlet that executes every starter in at least
   one real Match, observes Cycle events, movement, and contraction, and rejects
   system-failed outcomes.

## Verification

- `pnpm --filter @cowards/persistence lint`
- `pnpm --filter @cowards/persistence typecheck`
- `pnpm --filter @cowards/persistence test -- workshop.test.ts --run`
- `pnpm test:fast`

