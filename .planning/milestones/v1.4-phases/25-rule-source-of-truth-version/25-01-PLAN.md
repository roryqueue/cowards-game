# Phase 25 Plan 01: Publish v1.4 Rule Source of Truth

## Objective

Create a canonical v1.4 rules and architecture contract that downstream engine,
replay, starter, and demo work can implement without relying on historical
full-Activation language.

## Tasks

1. Add a root v1.4 rules specification that explicitly supersedes v1 timing.
2. Define Cycle-layer selected-slot scheduling and the Round 3 example.
3. Define Cycle-start/Cycle-end Backstab and remove `post-advance` as an active
   v1.4 boundary.
4. Add a root v1.4 architecture map covering all implementation surfaces.
5. Update README source-of-truth pointers.

## Verification

- Read the new docs for RULE-01 through RULE-07 coverage.
- Confirm old v1 documents remain historical and unmodified.

