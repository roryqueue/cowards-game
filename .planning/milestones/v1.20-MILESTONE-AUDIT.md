---
milestone: v1.20
status: passed
updated: 2026-05-25
---

# v1.20 Milestone Audit

## Audit Findings

| ID | Severity | Finding | Status |
| --- | --- | --- | --- |
| A-01 | BLOCKER | Signed-in proof initially recorded creation-time `queued` status instead of observed terminal MatchSet status. | Fixed. Proof now fetches public MatchSet DTOs after result-page evidence renders. |
| A-02 | BLOCKER | Proof initially allowed replay-visible pages while sibling Matches could still be running. | Fixed. Proof now settles within the documented browser/MatchSet budget and requires complete observed Match statuses. |
| A-03 | BLOCKER | Go Match job lease was 30s while runtime-service HTTP budget was 90s, causing long Python-vs-Python Matches to fail completion after lease expiry. | Fixed. Go orchestration now claims runtime-service jobs with timeout plus grace while preserving the default minimum lease. |
| A-04 | WARNING | Repeated proof using the same revision tuple hit active duplicate exhibition guardrails. | Fixed. Each bounded cycle creates a fresh account and fresh immutable revisions. |
| A-05 | EXPECTED | `pnpm sandbox:evaluate:runsc` fails because `runsc` is unavailable locally. | Accepted fail-loud non-promotion evidence. |

## Promotion Decision

Python remains **non-counted exhibition beta**. Runtime isolation remains **readiness evidence only**. Docker/container subprocess is a stronger executable candidate with real local evidence, but v1.20 does **not** claim production sandbox certification. No Python counted play, arbitrary packages, gVisor/runsc promotion, cloud deployment proof, or broad multi-language support was promoted.

## Verification Snapshot

- `pnpm sandbox:evaluate`
- `pnpm sandbox:evaluate:container`
- `pnpm sandbox:evaluate:check`
- `pnpm sandbox:evaluate:runsc` expected strict failure: required `gvisor-runsc` unavailable locally.
- `pnpm --filter @cowards/runtime-js test`
- `pnpm --filter @cowards/runtime-python test`
- `pnpm --filter @cowards/spec test`
- `pnpm --filter @cowards/web typecheck`
- `PATH=/usr/local/go/bin:$PATH go test ./...` from `apps/go-backend`
- `pnpm exec vitest run apps/runtime-service/src/execute-match.test.ts apps/web/app/matchsets/evidence-copy.test.ts scripts/check-boundary-monitors.test.ts`
- `pnpm exec tsx scripts/check-boundary-monitors.ts`
- `RUN_V1_20_PROOF=1 PLAYWRIGHT_TEST=1 COWARDS_GO_BACKEND_URL=http://127.0.0.1:8087 COWARDS_GO_BACKEND_INTERNAL_TOKEN=v120-proof-token COWARDS_RUNTIME_SERVICE_PROOF_ADAPTER=container-subprocess pnpm e2e --project=desktop --workers=1 v1-20-reliability-proof.spec.ts`

## Signed-In Proof Result

- Three bounded cycles passed.
- Six signed-in non-counted exhibition MatchSets completed.
- Mixed JS/TS-vs-Python and Python-vs-Python MatchSets both completed in every cycle.
- Result and replay pages opened with evidence panels, labels, timeout/candidate evidence, replay controls, plausible board images, and private-marker scans.
- Final artifact: `.planning/artifacts/v1.20-signed-in-reliability-proof.json`.

## Result

No open audit findings remain. Python stays non-counted exhibition beta and runtime isolation stays readiness evidence only.
