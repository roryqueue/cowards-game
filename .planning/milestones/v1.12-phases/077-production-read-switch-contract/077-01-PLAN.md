---
status: complete
phase: 077
requirements: [SWCH-01, SWCH-02, SWCH-03, SWCH-04, SWCH-05, SWCH-06]
---

# Plan

1. Add a route-scoped ownership resolver for `getPublicStrategyPage`.
2. Add a typed Go read client with schema parsing, raw privacy scanning, and
   sanitized failure classes.
3. Default production web reads to TypeScript unless
   `COWARDS_GO_PUBLIC_STRATEGY_READS=1`.
4. Ensure Go-selected failures do not call TypeScript.
5. Cover default, selected, missing URL, network, malformed, schema, privacy,
   and divergence cases with focused tests.

UI phase: not required; visible behavior is unchanged by default.
