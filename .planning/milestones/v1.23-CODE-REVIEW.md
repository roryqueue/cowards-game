# v1.23 Code Review

## Result

Passed after fixes.

## Findings Fixed

1. Signed-in proof saved compiled Rust/Zig revisions in parallel, which exceeded the selected Go backend write timeout under live compile validation.
   - Fix: proof saves revisions sequentially; selected Go backend ownership routes now use a 30s default timeout with `COWARDS_GO_BACKEND_SERVICE_TIMEOUT_MS` override.

2. Early Rust/Zig proof strategies could return empty activation orders, creating a completed Match whose public Chronicle failed replay validation for missing activation events.
   - Fix: proof strategies now activate the first available Soldier before returning Soldier actions.

3. Centralized runtime labels changed the TypeScript surface inventory.
   - Fix: regenerated v1.16 TypeScript backend inventory and final surface-label artifacts; boundary monitors now pass with 189 surfaces.

## Residual Risk

WASM/WASI remains readiness evidence and non-counted exhibition beta support only. Production sandbox certification and counted play require future dedicated milestones.
