# v1.31 Milestone Audit

**Milestone:** Public Site Spine and Discovery Reads
**Date:** 2026-05-31
**Status:** Passed

## Summary

v1.31 makes Coward's Game navigable as a public competitive site. `/` is now a public discovery hub, `/workshop` remains the canonical Workshop, and public users can move through Watch, Competitions, competition detail, entry sign-in, Learn, MatchSet results, replay, player, and Strategy pages.

## Requirements

- Requirements complete: 57/57
- Phases complete: 211-221
- Public discovery reads delivered:
  - `getPublicHomeDiscovery`
  - `getPublicWatchIndex`
  - `getPublicCompetitionIndex`
  - `getPublicCompetitionDetail`
  - `getSignedInCompetitionEntryDashboard`

## Boundary Decision

The new discovery DTOs and service reads are `public-discovery-v1` under the `public-discovery` namespace. They are explicitly separate from `match-execution-app-v1`.

No `match-execution-app-v1` version bump, DTO field addition, field rename, or field repurpose was made. No Go match execution, runtime-service behavior, retry/recovery policy, quarantine semantics, job lifecycle, MatchSet scoring, Chronicle persistence, operator controls, ABI status, runtime promotion, counted-language eligibility, or Strategy execution boundary changed.

## Validation

Passed:

- `pnpm --filter @cowards/spec build`
- `pnpm --filter @cowards/spec typecheck`
- `pnpm --filter @cowards/spec test`
- `pnpm --filter @cowards/web typecheck`
- `pnpm --filter @cowards/web lint`
- `pnpm --filter @cowards/web test`
- `pnpm format:check`
- `pnpm public-discovery:check`
- `pnpm boundary:monitors`
- `PLAYWRIGHT_TEST=1 pnpm e2e --project=desktop --project=mobile -- v1-31-public-site-spine.spec.ts`

Visual proof:

- `/tmp/cowards-v131-visual/home-desktop.png`
- `/tmp/cowards-v131-visual/watch-mobile.png`
- `/tmp/cowards-v131-visual/competition-desktop.png`

## Audit Findings

All blocking findings were fixed:

- Discovery hrefs now validate as safe relative public paths.
- Go public ladder result/replay links are guarded through the public read client.
- Exhibition detail pages filter MatchSets to the requested preset.
- Configured ladder discovery tolerates one failed season read.
- Public discovery monitors scan DTOs, service, components, routes, unsafe hrefs, and rendered public pages.
- v1.16 TypeScript surface artifacts were regenerated after merged-baseline surface count drift.

## Privacy

Public discovery output does not expose Strategy source, StrategyMemory, SoldierMemory, objective payloads, raw diagnostics, host paths, env values, tokens, DB details, package paths, private runtime internals, quarantine details, operator action details, or recovery payloads.

## Non-Claims

- JS/TS remains the only counted Strategy path.
- Python, Rust, and Zig remain non-counted exhibition beta only.
- WASI Preview 1 stdin/stdout JSON remains the active WASM/WASI ABI.
- Strategy code does not execute in web/API/Go.
- Live signed-in entry-to-result-to-replay proof remains environment-dependent; v1.31 proves the signed-in entry dashboard at service-test level and the anonymous entry gate in browser proof.

## Closure

Milestone is ready for commit and tag.
