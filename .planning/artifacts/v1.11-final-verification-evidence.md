# v1.11 Final Verification Evidence

**Captured:** 2026-05-23

## Boundary and Contract Evidence

- `pnpm contract:check` - passed.
- `pnpm contract:lint` - passed.
- `pnpm boundary:imports` - passed with `strict_offenses=0 report_only_offenses=29`.
- `pnpm boundary:monitors` - passed.

## Test and Type Evidence

- Focused DTO/service/route tests:
  - `packages/spec/src/spec.test.ts`
  - `packages/service/src/service.test.ts`
  - `apps/web/app/api/workshop/tests/[matchSetId]/route.test.ts`
  - `apps/web/app/api/workshop/analytics/profiles/[profileId]/compare/route.test.ts`
  - Result: 4 files, 51 tests passed.
- `pnpm --filter @cowards/spec typecheck && pnpm --filter @cowards/spec lint` - passed.
- `pnpm lint` - passed, 12 packages.
- `pnpm typecheck` - passed, 12 packages.
- `pnpm test` - passed, 12 packages.
- `pnpm format:check` - passed.
- `git diff --check` - passed.

## Replay Smoke and Realism Correction

Initial `pnpm e2e:smoke` failed because the local database was missing the `workshop_gauntlet_profiles` migration table. Running:

```sh
pnpm preflight -- --skip-redis --skip-web
```

applied the missing migrations (`applied 3, skipped 2`). Preflight then hit a development MatchSet worker-idle caveat, but the migration correction made the web smoke realistic. The rerun:

```sh
pnpm e2e:smoke
```

passed 6 Playwright replay privacy tests.

## Live Go Evidence

With Go running:

```sh
cd apps/go-backend && COWARDS_GO_BACKEND_OWNER_TOKENS=local-owner-token=user:local go run .
pnpm topology:check -- --require-go --json
```

passed required health, public MatchSet summary, public replay metadata, public Strategy page, unauthenticated owner analytics rejection, and privacy-safe diagnostics.

After stopping Go, the same required command failed with `fetch failed` for all required Go checks while diagnostic output remained privacy-safe. This proves required live evidence does not silently fall back to TypeScript.

## Final Boundary State

- Starting report-only baseline: 30.
- Final report-only count: 29.
- Strict offenses: 0.
- Strict migrated files:
  - `apps/web/app/api/workshop/tests/[matchSetId]/route.ts`
  - `apps/web/app/api/workshop/analytics/profiles/[profileId]/compare/route.ts`
  - `apps/web/lib/workshop-read-service-boundary.ts`
- Direct-persistence allowlist remains limited to the local service adapter.

## Non-Promotion State

Go remains read-only and evidence-only. Production web traffic remains on the TypeScript service path. Runtime isolation remains evidence-only. Python and other non-JS runtimes remain experimental and non-counted.
