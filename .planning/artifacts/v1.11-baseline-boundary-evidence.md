# v1.11 Baseline Boundary Evidence

**Captured:** 2026-05-23  
**Scope:** Phase 70 starting baseline before v1.11 implementation changes.

## Boundary Imports

Command:

```sh
pnpm boundary:imports
```

Observed starting result:

```text
strict_offenses=0 report_only_offenses=30
```

The starting report-only baseline included broad Competitive, Match/replay, and Workshop server/type fingerprints. The selected Workshop route files were not themselves counted report-only fingerprints because their legacy imports referenced `workshop/server`, while the counted Workshop fingerprints were direct persistence imports in the broad Workshop server/types files.

## Baseline Monitors

Command:

```sh
pnpm boundary:monitors
```

Starting-state expectation:

- Go route manifest remains five read-only fixture-backed routes.
- Runtime isolation remains evidence-only.
- Python/non-JS runtimes remain experimental and non-counted.
- Known broad web report-only offenses are baseline-gated.

## Topology

Command:

```sh
pnpm topology:check -- --json
```

Starting-state expectation:

- Static topology checks pass.
- Live Go smoke is skipped unless `--go-url` or `--require-go` is passed.
- Required live Go validation is deferred to Phase 74.

## Notes

- v1.11 must reduce the count from 30 by removing exact report-only fingerprints, not by hiding broad persistence behind another facade.
- The final implementation reduced the count to 29 by removing the `apps/web/app/workshop/types.ts` direct persistence type import.

