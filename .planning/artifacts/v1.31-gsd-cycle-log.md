# v1.31 GSD Cycle Log

**Milestone:** Public Site Spine and Discovery Reads
**Run mode:** synchronous autonomous implementation after user confirmation
**Boundary:** discovery reads are `public-discovery-v1`; they are explicitly not `match-execution-app-v1`.

## Research

- Re-read AGENTS/project planning context and prior v1.27/v1.29 route/result/replay artifacts.
- Inventoried existing routes and public object reads: Workshop, Account, MatchSet result workbench, Replay, Player, Strategy, Ladder, Exhibition.
- Confirmed missing public discovery/read spine: `/`, `/watch`, `/competitions`, `/competitions/[competitionId]`, `/competitions/[competitionId]/enter`, `/learn`.
- Confirmed existing individual public reads should be composed, not expanded.

## Implementation

- Added `packages/spec/src/public-discovery.ts` with separate DTO schemas for:
  - `PublicHomeDiscoveryDto`
  - `PublicWatchIndexDto`
  - `PublicCompetitionIndexDto`
  - `PublicCompetitionDetailDto`
  - `SignedInCompetitionEntryDashboardDto`
- Added `apps/web/lib/public-discovery-service.ts` as the server-first discovery read boundary.
- Added global site shell navigation and made `/` the public discovery hub while `/workshop` remains canonical Workshop.
- Added `/watch`, `/competitions`, `/competitions/[competitionId]`, `/competitions/[competitionId]/enter`, and `/learn`.
- Added cross-links across Account, Exhibition, MatchSet, Replay, Player, and Strategy pages.
- Added `scripts/check-public-discovery-boundary.ts` and `public-discovery:check`.

## Verification

- `pnpm --filter @cowards/spec build`
- `pnpm --filter @cowards/spec typecheck`
- `pnpm --filter @cowards/spec test`
- `pnpm --filter @cowards/web typecheck`
- `pnpm --filter @cowards/web lint`
- `pnpm --filter @cowards/web test`
- `pnpm public-discovery:check`
- `pnpm boundary:monitors`
- `PLAYWRIGHT_TEST=1 pnpm e2e --project=desktop --project=mobile -- v1-31-public-site-spine.spec.ts`
- Playwright screenshot QA for `/`, `/watch`, and `/competitions/exhibition%3Astandard-exhibition-v1` after the in-app browser bridge was unavailable.

## Review Fixes

- Added safe relative href validation to discovery DTOs and Go public ladder link reads.
- Filtered exhibition detail MatchSets to the requested exhibition preset.
- Made configured ladder discovery resilient to one season read failing.
- Expanded privacy/boundary monitor coverage to public discovery components, routes, unsafe hrefs, and rendered public pages.
- Regenerated v1.16 TypeScript backend inventory and final surface labels after the merged baseline plus new discovery files changed scanned surface counts.

## Non-Claims

- No `match-execution-app-v1` change, version bump, or DTO field expansion.
- No Go execution/runtime/retry/recovery/quarantine/job/scoring/Chronicle/operator behavior change.
- No Strategy source, private memory, diagnostics, host/env/token/DB/package paths, quarantine/operator/recovery payload exposure.
- No Strategy execution in web/API/Go.
- No counted non-JS promotion; Python/Rust/Zig remain non-counted exhibition beta only.
