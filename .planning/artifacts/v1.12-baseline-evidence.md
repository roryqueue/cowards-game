# v1.12 Baseline Evidence

**Captured:** 2026-05-23

## Commands

| Command | Result |
| --- | --- |
| `pnpm boundary:imports` | Passed, `strict_offenses=0 report_only_offenses=29` |
| `pnpm go:parity` | Passed during `pnpm boundary:monitors` |
| `pnpm topology:check` | Passed static topology diagnostics |
| `pnpm boundary:monitors` | Passed with route ownership manifest gate |

## Carry-Forward Caveat

v1.11 proved direct live Go checks pass when Go is running and fail loudly when
Go is stopped. v1.11 also recorded a local preflight caveat where migrations
applied but the development MatchSet worker smoke returned idle; replay smoke
passed after migrations.

## Baseline State

Production web reads remain TypeScript-owned by default. Go remains read-only
and evidence-only. Runtime isolation and non-JS counted play remain out of scope.
