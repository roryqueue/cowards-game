# v1.21 GSD Verify Work

Verification ran as an automated local UAT pass across all phases.

## Passed Gates

- `pnpm test:fast`
- `pnpm boundary:monitors`
- `pnpm wasm-wasi:evaluate`
- `cd apps/go-backend && PATH=/usr/local/go/bin:$PATH go test ./...`
- `RUN_V1_21_PROOF=1 PLAYWRIGHT_TEST=1 pnpm exec playwright test --project=desktop --workers=1 v1-21-rust-wasm-exhibition-proof.spec.ts`

## Phase Verdicts

- Phase 140: Pass. ABI/artifact contract and conservative gates are represented in spec and broker artifacts.
- Phase 141: Pass. Rust artifact generation and metadata tests pass.
- Phase 142: Pass. Runtime-service executes Rust WASM artifacts through Wasmtime and rejects missing artifacts.
- Phase 143: Pass. Rust Workshop/account/exhibition/result labels are covered by compile/tests and the live signed-in proof.
- Phase 144: Pass. WASM/WASI hostile probe evidence passes 15/15.
- Phase 145: Pass. Zig fails loudly as unavailable and is not exposed as working.
- Phase 146: Pass. Signed-in proof completed two bounded cycles covering JS/TS-vs-Rust and Rust-vs-Rust with result/replay evidence and private-marker scans.
- Phase 147: Pass. Promotion decision remains conservative: Rust is non-counted exhibition alpha only, Zig is fail-loud unavailable, and WASM/WASI is candidate evidence only.
