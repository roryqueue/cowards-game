# v1.10 Baseline Boundary Evidence

**Captured:** 2026-05-23  
**Phase:** 64 - Boundary Debt Triage and Scope Lock

## Summary

v1.10 starts from the v1.9 shipped baseline:

| Check | Result | Evidence |
| --- | --- | --- |
| `pnpm boundary:imports` | Passed | `strict_offenses=0 report_only_offenses=34` |
| `pnpm exec tsx scripts/check-boundary-monitors.ts` | Passed | 9 monitor groups passed; web import drift baseline has 34 known broad web offenses |
| `pnpm topology:check -- --json` | Passed | 9 topology diagnostics passed |
| Go route manifest | Passed | 4 read-only routes |
| Runtime isolation status | Passed | `evidence_only_not_promoted`; selected container candidate has `noFallback=true` |

## Boundary Import Baseline

`pnpm boundary:imports` reported no strict offenses and 34 report-only offenses:

```text
strict_offenses=0 report_only_offenses=34
```

Report-only offenses are classified in `.planning/artifacts/v1.10-boundary-offense-classification.md`.

## Boundary Monitor Baseline

`pnpm exec tsx scripts/check-boundary-monitors.ts` passed:

- contract drift: public OpenAPI route artifact checked
- privacy: public service route examples checked
- privacy: Go service fixtures checked
- web boundary: 34 known broad web offenses baseline-gated
- runtime adapter: JS/TS and Python experimental gates checked
- runtime isolation: container evidence remains required before promotion
- non-JS runtime: Python remains experimental
- Go parity: 4 route manifest entries checked
- topology: 9 static topology diagnostics checked

## Topology Baseline

`pnpm topology:check -- --json` passed with:

- 4 read-only Go routes from `apps/go-backend/testdata/service-fixtures/route-manifest.json`
- committed Go parity fixtures parse and public fixtures are privacy-safe
- TypeScript service health reports `cowards-service service-api-v1.8`
- runtime adapter metadata is worker-thread and explicitly not a final hostile-code sandbox
- runtime isolation readiness is evidence-only and not promoted
- live web and live Go smoke remain optional unless explicitly required
- diagnostic output contains no private markers

## v1.10 Release Expectations

By Phase 68/69, v1.10 must preserve:

- `strict_offenses=0`
- public/service/Go/topology/monitor outputs free of private Strategy/runtime fields by default
- runtime isolation and non-JS candidates evidence-only/non-counted

And must improve:

- `report_only_offenses` below 34 by removing selected migrated fingerprints, not hiding them.
- Go route manifest by exactly one public read-only route if Phase 67 lands.

