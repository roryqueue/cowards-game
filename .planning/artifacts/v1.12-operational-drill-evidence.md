# v1.12 Operational Drill Evidence

**Captured:** 2026-05-23

## Drill Results

| Drill | Evidence | Result |
| --- | --- | --- |
| Default owner | `createPublicReadService({ env: {} })` test calls TypeScript service | Passed |
| Forward switch | `COWARDS_GO_PUBLIC_STRATEGY_READS=1` with a Go client calls only Go for `getPublicStrategyPage` | Passed |
| Missing Go URL | Switch enabled without `COWARDS_GO_BACKEND_URL` throws before TypeScript can run | Passed |
| Go unavailable | Go client classifies network failure as `go_unavailable` | Passed |
| Non-JSON | Go client classifies malformed JSON as `go_non_json` | Passed |
| Schema drift | Go client classifies invalid DTOs as `go_schema_invalid` | Passed |
| Privacy failure | Go client scans raw JSON before schema parsing and classifies leaks as `go_privacy_violation` | Passed |
| Divergence | Go client rejects mismatched canonical href with `go_body_divergent` | Passed |
| Rollback | Removing the route switch returns the route to TypeScript service ownership | Passed |
| Live web-through-Go | Go on `127.0.0.1:8087` and web on `localhost:3100` rendered `Go Parity Sentinel` through the selected route | Passed |
| Live stopped-Go | With Go stopped, the switched web page rendered `Strategy temporarily unavailable` instead of TypeScript data | Passed |

## Live Topology Hook

`pnpm topology:check -- --require-web-go-public-strategy-read --web-url http://localhost:3000`
now provides required web-through-selected-route evidence when the web process is
started with the route switch and Go URL. It fails if the Go parity sentinel is
not rendered through the selected route.

Live local evidence was captured in
`.planning/artifacts/v1.12-live-web-go-topology.json` using:

```sh
pnpm topology:check -- --require-web-go-public-strategy-read --web-url http://localhost:3100 --go-url http://127.0.0.1:8087 --json
```

## Decision Impact

Operational drills prove fail-closed switch behavior, but promotion is still
blocked because the Go handler is fixture-backed rather than production-equivalent.
