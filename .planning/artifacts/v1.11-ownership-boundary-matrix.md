# v1.11 Ownership Boundary Matrix

**Captured:** 2026-05-23  
**Starting baseline:** `strict_offenses=0 report_only_offenses=30`

## Selected Slices

| Surface | Starting Fingerprint | v1.11 Decision | Trigger |
| --- | --- | --- | --- |
| Workshop test summary GET | Route was not directly counted; broad `workshop/server.ts` and `workshop/types.ts` were counted | Selected read migration | Service DTO can be source-free and route response can stay compatible |
| Workshop analytics compare GET | Route was not directly counted; broad `workshop/server.ts` analytics import was counted | Selected read migration | Service DTO can preserve comparison shape and keep local gate in route |
| Workshop source-free type cleanup | `apps/web/app/workshop/types.ts:7:@cowards/persistence` | Selected cleanup tied to read DTO ownership | Needed for real below-30 fingerprint removal |

## Deferred Report-Only Offenses

| Class | Fingerprints | v1.11 Status | Future Trigger |
| --- | --- | --- | --- |
| Competitive account/auth/fork/governance/exhibition/ladder/matchset writes | 12 route imports of `competitive/server` plus 8 persistence imports in `competitive/server.ts` | Deferred | Split read/write ownership with auth/session/write semantics and rollback |
| Match/replay private/runtime surfaces | 6 direct persistence imports in `matches/*` | Deferred | Public/private replay split and Chronicle projection ownership |
| Workshop broad server | 3 direct persistence imports in `workshop/server.ts` | Deferred except selected reads | Source/save/validation/test-launch/rerun/export/runtime surfaces must remain TypeScript-owned |

## Non-Goals

- No Go writes, auth/session mutation, ladder writes, Match orchestration, jobs, migrations, persistence ownership, Strategy source retrieval, or Strategy execution.
- No production web routing to Go.
- No production runtime sandbox promotion.
- No counted non-JS play.
- No Strategy source, StrategyMemory, SoldierMemory, objective payload, owner debug, raw Awareness Grid, stack trace, stderr, session, token, host path, or private runtime internals in public/service/Go/topology/monitor outputs.

## Final v1.11 Boundary Result

After implementation, `pnpm boundary:imports` reports:

```text
strict_offenses=0 report_only_offenses=29
```

The removed exact fingerprint is:

```text
apps/web/app/workshop/types.ts:7:@cowards/persistence:import type { WorkshopOpponentSummary, WorkshopPresetSummary, WorkshopRevisionSummary, WorkshopSnapshot, WorkshopSampleSummary, WorkshopTemplateSummary, WorkshopTestSummary, } from "@cowards/persistence/workshop"
```

