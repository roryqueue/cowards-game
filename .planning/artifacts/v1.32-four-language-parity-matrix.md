# v1.32 Four-Language Parity Matrix

## Corpus

- Version: `four-language-golden-corpus-v1.32`
- Languages: TypeScript, Python, Rust, Zig
- Behavior: first active Soldier turns to STONE
- Private markers: StrategyMemory, SoldierMemory, and objective payload markers are deliberately emitted by the corpus and must be absent from public projections.

## Matrix

The matrix declares all 16 pairwise combinations:

- TypeScript vs TypeScript, Python, Rust, Zig
- Python vs TypeScript, Python, Rust, Zig
- Rust vs TypeScript, Python, Rust, Zig
- Zig vs TypeScript, Python, Rust, Zig

Runtime-service verification executed all locally available pairs through provider/broker boundaries and passed with no runtime violations.

## Conformance Requirements

The corpus declares required gates for valid behavior, invalid output, timeout, oversized output, forbidden capability, memory-heavy output, deterministic repeat, runtime unavailable, malformed runtime result, missing/stale artifact, no silent fallback, public result/replay shape, and privacy parity. Later phases must keep converting these requirements into monitors and product-surface checks.

## Privacy Proof

Public Chronicle projections were scanned for the corpus private markers and passed.
