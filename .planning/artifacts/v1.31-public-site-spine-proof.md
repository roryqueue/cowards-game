# v1.31 Public Site Spine Proof

**Milestone:** v1.31 Public Site Spine and Discovery Reads
**Date:** 2026-05-31
**Status:** Passed

## Route Coverage

- `/` public discovery hub backed by `getPublicHomeDiscovery`
- `/watch` Watch hub backed by `getPublicWatchIndex`
- `/competitions` competition index backed by `getPublicCompetitionIndex`
- `/competitions/[competitionId]` competition detail backed by `getPublicCompetitionDetail`
- `/competitions/[competitionId]/enter` signed-in entry spine backed by `getSignedInCompetitionEntryDashboard`
- `/workshop`, `/account`, `/matchsets/[matchSetId]`, `/matches/[matchId]/replay`, `/players/[handle]`, `/strategies/[strategyId]`, and `/learn` cross-linked into the shell

## Discovery Boundary

- Discovery DTO namespace: `public-discovery`
- Discovery API version: `public-discovery-v1`
- Execution boundary marker: `not-match-execution-app-v1`
- No `match-execution-app-v1` version bump, field addition, rename, or repurpose.
- No Go execution, runtime-service, retry/recovery, quarantine, job lifecycle, MatchSet scoring, Chronicle persistence, operator-control, ABI, or counted-language behavior change.

## Verification

- `pnpm --filter @cowards/spec build`
- `pnpm --filter @cowards/spec typecheck`
- `pnpm --filter @cowards/spec test`
- `pnpm --filter @cowards/web typecheck`
- `pnpm --filter @cowards/web lint`
- `pnpm --filter @cowards/web test`
- `pnpm format:check`
- `pnpm public-discovery:check`
- `pnpm boundary:monitors`
- `PLAYWRIGHT_TEST=1 pnpm e2e --project=desktop --project=mobile -- v1-31-public-site-spine.spec.ts`

## Visual Proof

- Desktop home: `/tmp/cowards-v131-visual/home-desktop.png`
- Mobile Watch: `/tmp/cowards-v131-visual/watch-mobile.png`
- Desktop competition detail: `/tmp/cowards-v131-visual/competition-desktop.png`

The in-app browser bridge was unavailable, so visual QA used Playwright screenshots against `http://localhost:3000`.

## Privacy Proof

- Public discovery DTO leak-safe validation covers the new DTO family.
- Rendered public pages are scanned for private Strategy/memory/objective/diagnostic/runtime/operator/recovery markers.
- Discovery href schemas and Playwright checks reject unsafe external, protocol, and script hrefs.
- Public route/component monitor coverage includes the discovery service, components, routes, DTOs, and negative unsafe href examples.

## Non-Claims

- Strategy code does not execute in web/API/Go.
- JS/TS remains the only counted Strategy path.
- Python, Rust, and Zig remain non-counted exhibition beta only.
- WASI Preview 1 stdin/stdout JSON remains the active WASM/WASI ABI.
- Public discovery does not expose Strategy source, StrategyMemory, SoldierMemory, objective payloads, raw diagnostics, host paths, env values, tokens, DB details, package paths, private runtime internals, quarantine details, operator action details, or recovery payloads.
