# v1.12 Ownership Boundary Matrix

**Captured:** 2026-05-23

## Baseline

| Surface | Current production owner | Go state | v1.12 status |
| --- | --- | --- | --- |
| `GET /health` | Go evidence endpoint | Fixture-backed Go route | Evidence-only, not a product read |
| `GET /public/matchsets/{matchSetId}/summary` | TypeScript service | Fixture-backed Go parity route | Blocked, not selected |
| `GET /public/replays/{matchId}/metadata` | TypeScript service | Fixture-backed Go parity route | Blocked, not selected |
| `GET /public/strategies/{strategyId}` | TypeScript service | Fixture-backed Go parity route plus web switch code | Candidate only; not promoted |
| `GET /analytics/runs/{runId}/summary` | TypeScript service | Owner-token fixture evidence | Evidence-only, not public web traffic |
| Public player page | TypeScript service | No Go route | Out of slice |
| Public ladder page | TypeScript service | No Go route | Out of slice |

## Web Boundary

`pnpm boundary:imports` reports `strict_offenses=0 report_only_offenses=29`.
The 29 report-only offenses remain broad web/persistence paths outside this
single-route cutover slice and are baseline-gated by `pnpm boundary:monitors`.

## Decision

Only `getPublicStrategyPage` is selected as a candidate. The milestone result is
`promote-none-yet` because the Go handler remains fixture-backed and does not
own a production-equivalent data provider.
