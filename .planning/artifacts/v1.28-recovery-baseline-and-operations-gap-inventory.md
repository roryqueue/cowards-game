# v1.28 Recovery Baseline and Operations Gap Inventory

**Generated:** 2026-05-30T23:45:00.000Z
**Phase:** 201
**Contract:** `match-execution-app-v1`
**Decision:** No app-facing contract change is planned.

## Baseline

v1.28 starts from v1.26. The parallel v1.27 milestone is not available in this checkout and is not a dependency.

v1.26 already proves:

- Go owns job claiming, runtime-service invocation, retry/failure classification, Match completion, Chronicle persistence handoff, MatchSet refresh, public evidence, and promotion decisions.
- Runtime-service owns hostile Strategy execution only through schema-validated envelopes and registered runtime implementations.
- `match-execution-app-v1` remains frozen.
- JS/TS remains counted.
- Python, Rust, and Zig remain non-counted exhibition beta.
- Preview 1 stdin/stdout JSON remains the active WASM/WASI execution ABI.

## Surface Inventory

| Surface | File | Classification | Notes |
| --- | --- | --- | --- |
| Go orchestrator runOnce | `apps/go-backend/orchestrator.go` | Go internal | Claims jobs, builds runtime-service requests, records failures, completes Matches. |
| Go job lifecycle | `apps/go-backend/job_lifecycle.go` | Go internal | Owns claim, lease, heartbeat, retry queue, terminal failure, and failure detail sanitization. |
| Go completion service | `apps/go-backend/completion.go` | Go internal | Owns Chronicle persistence handoff, Match completion, and MatchSet refresh under lease. |
| Go retry policy | `apps/go-backend/retry_policy.go` | Go internal | Classifies runtime unavailable, timeout, malformed runtime result, stale artifact, and system failure categories. |
| Runtime-service client | `apps/go-backend/runtime_service_client.go` | Go boundary | Translates runtime-service HTTP, timeout, malformed, oversized, and contract failure shapes. |
| Public projection | `apps/go-backend/live_backend.go` | Go public projection | Projects public-safe result/replay evidence without raw diagnostics. |
| Runtime-service execute-match | `apps/runtime-service/src/execute-match.ts` | Runtime-service internal | Owns hostile Strategy execution and schema-valid runtime envelopes only. |
| Runtime-service redaction | `apps/runtime-service/src/redaction.ts` | Runtime-service internal | Redacts runtime diagnostics before envelopes leave the runtime boundary. |
| Public match execution contract | `packages/spec/src/match-execution-contract.ts` | Frozen public contract | Public compatibility target remains `match-execution-app-v1`. |
| v1.26 reliability proof | `scripts/evaluate-v1-26-match-execution-reliability.ts` | Proof harness | Pattern for contract fixtures, privacy scans, markers, and non-claims. |

## Operations Gaps

| Gap | Target Phase | Summary |
| --- | --- | --- |
| Private dead-letter/quarantine | 202 | No dedicated private operator quarantine model exists for exhausted and non-retryable execution jobs. |
| Deterministic operator requeue/rerun | 203 | Existing run-once supports execution, but explicit idempotent recovery controls are not modeled. |
| Live operations drill harness | 204 | v1.26 proof covers reliability fixtures, but v1.28 needs stateful recovery drills across local topology. |
| Stale lease, duplicate worker, interrupted MatchSet recovery | 205 | Existing lifecycle tests cover basics; operator recovery needs convergence proof under manual and duplicate paths. |
| Operator evidence redaction | 206 | Public redaction exists, but private operator artifacts need their own allowlist and leak scans. |
| Operations contract monitoring | 207 | Boundary monitors need v1.28 proof checks for no contract drift, no ownership creep, and no leaks. |

## Compatibility Decision

The default v1.28 path is no `match-execution-app-v1` change. Quarantine, requeue, rerun, operator action IDs, and private drill evidence are internal/operator concepts. Public surfaces should continue to expose only public-safe categories and existing result/replay compatibility shapes.

## Non-Claims

- No result/replay UX expansion.
- No public operations UI.
- No runtime promotion.
- No production sandbox certification.
- No direct-export ABI migration.
- No Component Model/WIT ABI migration.
- No counted non-JS play.
- No v1.27 dependency.

## Requirements Covered

- BASE-01
- BASE-02
- BASE-03
- BASE-04
- BASE-05
- BASE-06
