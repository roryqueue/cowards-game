# v1.11 Live Go Readiness Evidence

**Captured:** 2026-05-23  
**Scope:** Evidence-only validation. Production web traffic remains on the TypeScript service path.

## Commands

```sh
pnpm boundary:monitors
cd apps/go-backend && COWARDS_GO_BACKEND_OWNER_TOKENS=local-owner-token=user:local go run .
pnpm topology:check -- --require-go --json
pnpm topology:check -- --require-go --json # after stopping Go, expected failure
```

## Passing Evidence

`pnpm boundary:monitors` passed, including:

- OpenAPI public route artifact checked.
- Public service route examples checked.
- Go public and owner-summary fixtures checked.
- Web import drift baseline gated at 29 known broad web offenses.
- Runtime isolation remains evidence-only.
- Python/non-JS guardrails remain experimental/non-counted.
- Go route manifest has five GET-only entries.
- Static topology diagnostics are privacy-safe.

Required live Go topology passed with:

```text
ok=true
Go health: pass at http://127.0.0.1:8087/
getPublicMatchSetSummary: pass, public-safe bytes
getPublicReplayMetadata: pass, public-safe bytes
getPublicStrategyPage: pass, public-safe bytes
owner analytics auth gate: rejected unauthenticated request with HTTP 403
diagnostic output: diagnostics contain no private markers
```

## No-Fallback Evidence

After stopping the Go backend, the same required command exited nonzero:

```text
ok=false
Go health: required failure, fetch failed
getPublicMatchSetSummary: required failure, fetch failed
getPublicReplayMetadata: required failure, fetch failed
getPublicStrategyPage: required failure, fetch failed
owner analytics auth gate: required failure, fetch failed
diagnostic output: pass
```

This proves required live Go validation fails loudly when Go is unavailable and does not silently fall back to TypeScript.

## Rollback and Non-Promotion Notes

- Remove this evidence lane by dropping the required live topology command from validation; user-facing TypeScript service behavior is unaffected.
- Go remains read-only and fixture-backed.
- No Go writes, auth/session mutation, ladder writes, Match orchestration, jobs, migrations, persistence ownership, Strategy source retrieval, Strategy execution, production web routing, production runtime sandbox promotion, or counted non-JS play were added.

