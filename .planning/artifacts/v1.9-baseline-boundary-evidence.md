# v1.9 Baseline Boundary Evidence

**Milestone:** v1.9 Backend and Runtime Ownership Split  
**Captured:** 2026-05-22  
**Purpose:** Establish the pre-migration boundary baseline for Phase 57.

## Commands

| Command | Result | Evidence |
| --- | --- | --- |
| `pnpm boundary:imports` | Passed | `strict_offenses=0 report_only_offenses=41` |
| `pnpm boundary:monitors` | Passed | Contract, privacy, web boundary, runtime adapter, Go parity, and topology monitors passed. |

## Import Boundary Baseline

`pnpm boundary:imports` reported no strict offenses and 41 report-only broad web persistence/runtime offenses.

### Report-Only Clusters

| Cluster | Current evidence | v1.9 handling |
| --- | --- | --- |
| Account reads and account APIs | `apps/web/app/account/page.tsx`, account revision/fork routes, auth session/sign-in/sign-out/sign-up routes import `competitive/server`. | Phase 59 migrates only session snapshot and account revision list reads. Mutations/source retrieval remain out of scope. |
| Competitive server persistence facade | `apps/web/app/competitive/server.ts` imports persistence auth, account revisions, competition, ladder, governance, profiles, starter, and advanced strategy modules. | Service slices should draw persistence lookup behind `@cowards/service` and remove direct page dependencies slice by slice. |
| Public player profile | `apps/web/app/players/[handle]/page.tsx` imports `competitive/server`. | Phase 58 migrates to `@cowards/service` and strict enforcement. |
| Public ladder page and ladder APIs | `apps/web/app/ladder/[seasonId]/page.tsx` and ladder mutation routes import `competitive/server`. | Phase 60 migrates selected public ladder season read only. Ladder entries, schedule, status, and other mutations remain out of scope. |
| Match/replay internals | Match server and replay fixture helpers import persistence. | Out of scope for v1.9 service read migration. |
| Workshop analytics and source flows | Workshop server/tests/types import persistence. | Out of scope for v1.9; source/test/save/rerun/export flows do not move. |

## Boundary Monitor Baseline

`pnpm boundary:monitors` completed successfully with these reported checks:

- Contract drift: 6 public OpenAPI paths checked.
- Privacy: 6 public service route examples checked.
- Privacy: Go public and owner-summary fixtures checked.
- Web boundary: 41 known broad web offenses baseline-gated.
- Runtime adapter: 3 JS/TS adapters and Python experimental gate checked.
- Go parity: 4 Go route manifest entries checked.
- Topology: 8 static topology diagnostics checked.

## Phase 57 Requirement Evidence

| Requirement | Evidence |
| --- | --- |
| OWN-01 | `.planning/artifacts/v1.9-ownership-boundary-matrix.md` names ownership for selected routes/files/DTOs/processes. |
| OWN-02 | This artifact records `pnpm boundary:imports` and `pnpm boundary:monitors` baseline results. |
| OWN-03 | The ownership matrix records explicit v1.9 non-goals for Go writes, sandbox promotion, counted non-JS play, replay projection, Workshop source/test/save flows, and backend rewrites. |
| OWN-04 | The ownership matrix states that game rules remain in the engine and do not move into React components, web route handlers, service DTO mappers, or Go handlers. |

## Expected Baseline Change in Later Phases

- After Phase 58, `report_only_offenses` should drop below 41 by removing the public player page direct `competitive/server` import and adding strict enforcement for the migrated slice.
- After Phase 59, account read offenses selected for migration should leave the report-only baseline, while mutation/source routes remain report-only or explicitly out of scope.
- After Phase 60, the selected public ladder season read should leave the report-only baseline, while ladder mutation routes remain out of scope.
