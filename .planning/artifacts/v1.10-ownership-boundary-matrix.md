# v1.10 Ownership Boundary Matrix

**Captured:** 2026-05-23  
**Phase:** 64 - Boundary Debt Triage and Scope Lock

| Surface | Web Owns | `@cowards/service` Owns | `@cowards/spec` Owns | Persistence Owns | Go Owns | Runtime Owns | v1.10 Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Account Strategy Revision list read | Cookie/session extraction, route response, UI shape adapters | Session-authorized revision list DTO construction and schema parse | Owner-safe revision summary schemas, runtime labels, compatibility fields, service route metadata | Internal lookup behind service implementation only | None | None | Selected for Phase 65 |
| Account Strategy Revision save/fork/source | Route handlers and current web ownership | Existing read DTOs only | Existing contracts | Mutation/source retrieval internals | None | Validation/runtime stays TypeScript-owned | Deferred |
| Workshop Evidence Explorer analytics reads | Page/client rendering, query params, UI sorting/filtering state | Owner-safe analytics profile/list/summary read DTOs and schema parse | Analytics DTO schemas, privacy denylist, service route metadata | Internal analytics snapshot lookup behind service implementation only | None | None | Selected for Phase 66 |
| Workshop source/save/validate/test execution | Current Workshop routes and UI | Not moved in v1.10 | Runtime/source validation schemas only | Current persistence/workshop internals | None | JS/TS runtime adapters and worker execution | Deferred |
| Public Strategy page read | Existing web page remains TypeScript-service-backed | Canonical `getPublicStrategyPage` DTO | Public Strategy page route/schema metadata | Internal profile lookup behind service implementation only | Fixture-backed GET read for parity proof | None | Selected for Phase 67 Go route |
| Public player/profile and public ladder reads | Existing pages | Already service-backed | Existing public page schemas | Internal lookup behind service implementation | None in v1.10 | None | Already migrated in v1.9 |
| Public MatchSet summary and replay metadata | Existing pages/routes | Already service-backed | Existing public DTO schemas | Internal lookup behind service implementation | Existing fixture-backed GET reads | None | Preserve |
| Full replay projection and owner debug | Replay page/server | Public metadata only in service | Replay privacy contracts | Chronicle retrieval and authorization internals | None | None | Deferred |
| Go read-model route inventory | No production web traffic by default | TypeScript fixture source of truth | `SERVICE_API_ROUTES` canonical metadata | No direct Go persistence | Exactly one new public Strategy GET route | None | Selected with rollback criteria |
| Runtime isolation and non-JS | Labels/diagnostics only | None | Runtime product semantics and promotion criteria | None | None | Evidence-only adapters; JS/TS counted defaults | Preserve non-promoted |

## Locked Decisions

- v1.10 reduces report-only debt only through proven read/user service slices.
- v1.10 adds exactly one Go read-model route: public `GET /public/strategies/{strategyId}`.
- Web production traffic remains on the TypeScript service path unless a later phase explicitly documents a rollback switch and required-boundary no-fallback checks.
- Runtime isolation and non-JS remain evidence-only and non-counted.
- No game rules move into React components, route handlers, DTO mappers, or Go handlers.

